# Neckline 19:00 晚间链编排锚。一次拉起 facts → strategy → report 三个 oneshot。
#
# 三段用 `Wants=` 而不是 `Requires=`：任一段失败，后续仍运行并在报告中如实披露。
# 具体顺序由 service 的 `After=` 链保证。
#
# `StopWhenUnneeded=yes` 不能删除。target 若在三段结束后仍保持 active，timer 会停在
# running 而不重新计算下一次触发，表现为“首晚成功、之后静默不跑”。三个 service 也
# 不得设置 `RemainAfterExit=yes`，否则下一晚不会再次执行 `ExecStart`。
#
# 安装/改名顺序（禁止启动真实 target 做演练，它会产生 LLM 费用并发送通知）：
#   systemctl stop neckline-evening.timer
#   cp deploy/neckline-evening.target /etc/systemd/system/
#   cp deploy/neckline-{facts,strategy,report}.service /etc/systemd/system/
#   systemctl daemon-reload
#   systemctl stop neckline-evening.target
#   systemctl start neckline-evening.timer
#   systemctl list-timers | grep neckline-evening

[Unit]
Description=Neckline 19:00 evening chain (facts -> strategy -> report)
Documentation=file:///opt/neckline/PROJECT_PLAN.md
Wants=neckline-facts.service neckline-strategy.service neckline-report.service
After=neckline-facts.service neckline-strategy.service neckline-report.service
StopWhenUnneeded=yes
