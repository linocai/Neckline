# Neckline 晚间链 **第 3 段**:报告装配 + 落库 + APNs。
# (unit 名沿用 V1/V2 —— 它本来干的就是"出报告"这件事,⛔ 本版不动 unit 拓扑。
#  V1 = `scripts/report.py --notify` 单跑;V2 = 晚间链的 `review,report` 两段;
#  V2.5.0 = **只剩 `report` 一段**:K8 的 ⑨ 盘后复盘引擎随裁定 8 整块退役,
#  周复盘搬去了 `neckline/review/` 的交割单分析台(架构 §六,由 App 触发,不进本链)。)
#
# ⚠ **`--segments report` 就是全部,别再找第二段**:V2 那句「⛔ 别把
#   `--segments review,report` 拆成两个 unit」讲的是**当时**那两段的语义依赖
#   (报告要读 ⑨ 当日落的行)。那两段今天只剩一段,那句禁令**已无对象** ——
#   保留这段沿革是为了让下一个人知道它为什么消失,⛔ 不是让谁去把 `review` 加回来。
#
# ⚠ **V2.3.2 的 `basket_dropped_handoff` 跨进程交接机制已随 S1 物理删除**
#   (`neckline/selection/` 整包退役)。报告的「今日未定档篮子」那一节连同 K8 报告
#   一起没了;K9 报告的三态与缺口披露见 `neckline/report/state.py`。
#   ⛔ 别照这段沿革去找 `basket_dropped_handoff.py`,它不在了。
#
# ✅ **配额已于 2026-08-11 V2.3.2 第 ⑥ 块部署时生产隔离实测复核,欠账销案。结论:
#    `TimeoutStartSec=2400` / `MemoryMax=1000M` **两个数都不动** —— ③-A 的增量约等于零。**
#
#    **要量的是什么**:V2.3.2-③-A 给 `SEG_REVIEW` 段加了一段(OUT 研究影子对照 D1 机械
#    记录,`review/out_shadow.py::record_day`),而这两个数是加它**之前**定的。
#    ⚠ `record_day` 在**未注入 `day`** 时会自建一份只含 OUT 票的 `DayMarket`(生产路径
#    就是这条,见 `out_shadow.py` 模块头)—— 那是一次真实的行情装配,要量的就是它。
#
#    **实测条件**:nk 上 `systemd-run --unit=… --property=Type=oneshot
#    --property=User=neckline --property=Group=neckline --property=MemoryMax=…` 瞬态
#    service(⛔ 未用 root 的 `--scope` —— 那会把行情文件写成 root 属主);
#    命令 = `evening.py --segments review,report`(⛔ 去掉 `--notify`:探针不该给用户
#    推一条重复报告;那一步是一次 APNs 调用,不影响量级)。
#
#    **读数**:
#      · **整段墙钟 65s**(两跑一致:20:23:51→20:24:56 与 20:27:28→20:28:33),CPU 5.1s;
#        其中**两次 LLM 流式调用占了约 63s**(⑨ 复盘 43.3s + 报告渲染 ~20s),机械部分 ~2s。
#        ⚠ 实测那天只有 **1 个篮子**,墙钟随篮子数走 —— 这是**轻负载样本**,见下面的登记。
#      · **内存**:峰值读数 312.2M / 294.0M;按纪律压低 `MemoryMax` 反证 ——
#        **400M 扛住**(`Result=success`)/ **256M 被 OOM-kill**(`Result=oom-kill`,
#        status 9)→ 真峰值落在 **(256M, 400M]**。
#      · **③-A 的增量单独量过**(合成 OUT 清单、`persist=False`、只读不写库):
#        **N=100 → 0.13s;N=500 → 0.16s**,进程 maxrss 80.8M / 82.1M(含 import)。
#        🔴 **对 N 几乎平坦** —— 成本在"读那一天的行情分区"这件固定开销上,不在票数上。
#        P0-23 核对结论(不构成新的全市场级批算路径)由此**被实测证实**,不再是纸面推断。
#
#    **为什么两个数都不动**:③-A 的增量 ≈ 0.2s / ≈ 0 内存,动不了 2400 / 1000M 的量级;
#    而 2400 / 1000M 本身**不是本次实测的对象**(它们是加 ③-A 之前为整段定的)。
#    ⚠ **登记(⛔ 别当成"已经量准了")**:本次实测跑在**只有 1 个篮子**的轻负载晚上,
#    `out_candidates` 当时还是 0 行(第一次带新代码的晚间链尚未跑)→ ③-A 那一段在
#    **整段实测里是空转的**,它的成本是另外单独量的(上面第三条)。**「2400 / 1000M 对
#    一个 5~7 篮的忙日够不够」这个问题,本次没有回答** —— 已挂 PROJECT_PLAN §七 backlog,
#    ⛔ 不在这里凭感觉调。
#
#    🔴 **改这两个数必须重新实测**(P0-23 纪律),⛔ 别凭感觉调。
#
# 安装:sudo cp /opt/neckline/deploy/neckline-report.service /etc/systemd/system/ && sudo systemctl daemon-reload
# ⛔ 无 [Install]:由 neckline-evening.target 拉起。

[Unit]
Description=Neckline evening chain seg3: report assembly + persistence + APNs
Documentation=file:///opt/neckline/PROJECT_PLAN.md
After=network-online.target neckline-basket.service
Wants=network-online.target

[Service]
Type=oneshot
User=neckline
Group=neckline
WorkingDirectory=/opt/neckline
EnvironmentFile=/opt/neckline/.env
ExecStart=/opt/neckline/.venv/bin/python scripts/evening.py --scheduled --segments report --notify
# ⚠ 这两个数是 **V2 负载**(⑨ 复盘含 LLM + 报告渲染)下定的;V2.5.0 本段只剩
#    报告装配(**只读已落好的表,零 LLM、零批算**,`report/pipeline.py` 模块头),
#    实际用量只会更小。⛔ 本版一个数都没改(§13.1-B5 等用户裁定)。
TimeoutStartSec=2400
MemoryMax=1000M
