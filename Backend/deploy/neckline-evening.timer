# Neckline 晚间链定时(plan §五 V2-⑯-D)。触发 `neckline-evening.target`,
# 由它串起 facts → strategy → report 三段。
#
# **时刻表**(总窗口按 ⑯-C 新机实测排,改点前先看那份计时表):
#   17:10  neckline-daily.timer    在供应商发布窗口后拉当日 EOD
#   17:30–21:20 recovery.timer     只补当天不完整分区并冻结 fp-4
#   19:00–21:30 本 timer           半小时有界重试晚间链；正式成功后后续槽位幂等跳过
#
# 三个 service 都带 `--scheduled`:Mon-Thu 绑定当天,Sunday 只绑定前一周五。若该日休市,
# 脚本成功跳过,绝不回退到更早交易日重复生成旧报告；若同一交易日的报告已在当天人工
# 补跑,定时槽也整链跳过,避免重复 LLM/APNs。人工补跑仍须显式传 YYYYMMDD。
#
# 此 timer 由发布流程显式启用，避免未经核验的重复调度。
#
# 🔴 **本 timer 的 NEXT 变成 `-` / `Trigger: n/a` 时,病不在这个文件里** —— 去看
# `neckline-evening.target` 的 `StopWhenUnneeded=yes` 还在不在(§七 **P0-45**:target
# 不落下 → timer 永远停在 `SubState=running`、不重算 NEXT → 首晚必成、次晚起必哑)。
# 自检一行:`systemctl show neckline-evening.timer -p SubState -p NextElapseUSecRealtime`
# —— 正常态是 `waiting` + 有值;`running` + 空值就是那个病。
#
# 安装见 neckline-evening.target 头部。

[Unit]
Description=Run and bounded-retry the Neckline evening chain

[Timer]
OnCalendar=Mon-Thu 19:00 Asia/Shanghai
OnCalendar=Mon-Thu 19:30 Asia/Shanghai
OnCalendar=Mon-Thu 20:00 Asia/Shanghai
OnCalendar=Mon-Thu 20:30 Asia/Shanghai
OnCalendar=Mon-Thu 21:00 Asia/Shanghai
OnCalendar=Mon-Thu 21:30 Asia/Shanghai
OnCalendar=Sun 19:00 Asia/Shanghai
OnCalendar=Sun 19:30 Asia/Shanghai
OnCalendar=Sun 20:00 Asia/Shanghai
OnCalendar=Sun 20:30 Asia/Shanghai
OnCalendar=Sun 21:00 Asia/Shanghai
OnCalendar=Sun 21:30 Asia/Shanghai
# 报告 / APNs 只能由上列真实定时槽触发，绝不让 systemd missed-slot 追跑。
Persistent=false
Unit=neckline-evening.target

[Install]
WantedBy=timers.target
