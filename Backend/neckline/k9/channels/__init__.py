"""K9 第二层 · 四个召回通道(K9 §三,架构 §二 边界②)。

🔴 **通道之间互不知道**(边界②)。结构性保证三条,守门单测 G4 逐条扫:

1. `pN_*.py` ⛔ 不 import 任何其它 `channels/*`;
2. `pN_*.py` ⛔ 不 import `ranking` / `quota` / `run` —— 拿不到别人的产物,
   也看不见名次与席位;
3. 签名固定 `run(pack, params) -> list[ChannelHit]`,四个通道逐字相同。

`k9/run.py` 是**唯一**同时看见四个产物的地方。

**共同规则**(K9 §3.6 / §3.7):
· **定义性条件 = 硬门槛**,在通道内直接过滤;
· **强度性条件 ⛔ 一律不设门槛**,全部转成 K9 第三层的打分项
  (唯一例外:形态 1 的放量倍数同时是定义性与强度性,裁定 15,理由见 K9 §3.6);
· **判据是形态私有的**,⛔ 不存在跨形态通用筛选条件 —— 把任一形态的判据当成全局门
  加到所有形态上,会把其他形态筛成零(K9 §3.7)。

**每个通道跑两档**(K9 §五-6):`strict` 与 `relaxed` 都跑、都记数,报告要能区分
「市场今天确实没有」与「判据卡得过严」(K9 §五 末段)。同一只票两档都中时,
只保留 `strict` 那条(成色取更好的那个,§五-7)。
"""

from __future__ import annotations

__all__: list = []
