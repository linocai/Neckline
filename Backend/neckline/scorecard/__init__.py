"""三条成绩线(架构 §五,PROJECT_PLAN §5.8)。**分开存放,互不进对方的分子分母。**

| 线 | 模块 | 状态 |
|---|---|---|
| 覆盖率 | `coverage.py` + `store.py` | **S4 已完工** —— 以涨停为口径,⛔ 不依赖任何待标定数字 |
| 清单成绩(五指标) | `listing.py` | K9-v2 D0/D1/D2 路径与 10:00 预案终值 |
| 我的成绩 | `mine.py` | 来源交割单,走周末复盘线(§5.9),与前两条**完全隔离** |

🔴 **本包⛔ 不许 import `neckline.k9`**(守门单测逐文件 AST 扫描)。
覆盖率是**参数包回来之前唯一能跑起来的成绩线**,做成「尺子」——
它一旦读到策略参数,就不再是独立于策略的尺子了。策略侧的东西(D−1 边界判定、
召回与名次)一律**通过 `k9_disposition` 这张表**递进来,不通过 import 递进来。
"""

from __future__ import annotations

__all__: list = []
