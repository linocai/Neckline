"""架构第四层 · K9-v2 预案层(K9 §六 + 架构 §3.4)。

为清单上每只票冻结**明天的关键价位与判定条件**。

    条件骨架 = 机械(按形态套用 K9 §6.3 的四个骨架)
    具体数值 = LLM 逐票填(第一压力位 / 第二压力位 / 失效位 + 各分支阈值)
    最终确认 = 用户盘后逐只过目、可修改(改动写 append-only 新版本)

🔴 **硬约束(架构 §3.4)**:预案输出必须是**结构化、可机械求值**的条件。
次日 9:26 的核对表与 10:00 的结算拍都要拿它**逐条比对** —— ⛔ 不许输出自然语言条件。
落地形状见 `model.py`:`MetricRef` 是**闭合枚举**,`Condition` 只有
`{op, lhs, rhs}` 三个位,rhs 只能是数值或另一个 `MetricRef`(⛔ 没有算术、没有自由文本)。

🔴 **`evaluate.py` 是唯一求值实现**(PROJECT_PLAN §5.6.3 / §5.7.2):
9:26 竞价核对表(`auction/checklist.py`)与 10:00 结算拍(`auction/settle.py`)
**共用它**,⛔ 不许各写一份 —— 两份求值器意味着「同一个条件在早上八分钟内有两种解释」。

🔴 **命名铁律(裁定 1)**:本层产出的 `first_resistance` / `second_resistance` /
`invalidation` 是 **LLM 判断**;`upside_room_mech*`(`k9/upside_room.py`)是**机械排序量**。
两者在 DTO 字段、数据库列、报告文案里**永不互相顶替**,也 ⛔ 不许把机械空间喂给
预案 LLM(§5.2 边界④:那等于邀请它把那个数原样吐回来当「第一压力位」,循环依赖当场复活)。

**分层边界**:预案层**知道形态,但不做好坏评价**(架构 §四 第 4 条)。
LLM 返回体只允许数值与价位键,出现任何自由文本评价键 → 校验拒绝(`fill.py`)。
"""

from __future__ import annotations

__all__: list = []
