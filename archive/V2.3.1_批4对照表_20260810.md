# V2.3.1 批 4(macOS 复盘板块 + 对账工作台)逐屏对照表 · 2026-08-10

> **这是 §五 V2.3.1-⑤ 的验收件**(PROJECT_PLAN §九 当日条一行 + 本文件全文,同一件事只存在一处)。
> **判据来源** = `Neckline视觉升级/Neckline macOS.dc.html` **1267–1573** 的 **inline style**
> + 文件尾 `<script type="text/x-dc">` 的 `pick()`,以及
> `Neckline视觉升级/Neckline 信息卡与对账.dc.html` **285–451**(对账工作台已上传态)。
> ⛔ 不是 `dcextract.py` 剥出来的纯文本。
>
> **判定只有三种**:`已对齐`(本来就一致)/ `已改齐`(本批改成一致)/
> `刻意不同`(必须带三类合法理由之一:① 契约支撑不了〔引 §五 〇〕· ② 平台差异 ·
> ③ 规范 §03/§04 要求向令牌就近对齐)。
> **⛔ 只收版式行**(字号 / 颜色 / 间距 / 圆角 / 排布 / 层级);数据差异不进表。
>
> **实拍**(窗口由 `NECKLINE_WINDOW_SIZE` 钉死;演示后端端口 8014、bundle id
> `top.linotsai.neckline.dev.b4`):
> `final_daily.png` / `final_selectionClock.png` / `final_tradeClock.png` /
> `final_cumulative.png` / `final_reconcile.png` 五张均 **1200×860**;
> `final_workbench.png`(对账工作台已上传态)、`b4_07_cumulative_tall.png`(累计页全长)、
> `b4_06_cumulative_profiles.png`(周度产物未生成态 —— 画像 / 观察项 / 移交件那半页)
> 三张为**同宽 1200 × 高 1410** 的附图(本机屏高上界,§五 〇d 第 9 条)。

---

## 屏 1 · 列表栏(原型 1267–1335 + `pick()` 1783)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 栏宽 376 / 底 `#FCFCFD` / 右分隔 `.5px rgba(60,60,67,.12)`(1267) | `RootView.swift:135/154` | 已对齐(批 1) |
| 2 | 标题区 `padding:18px 16px 12px`(1268) | `ReviewView.swift:73-94`(栏内边距 10 + `listHeaderExtraH` 6 = 16,底 12) | 已改齐(原为 `padding(.horizontal,6)` + 底 4/8 两段) |
| 3 | 「复盘」`22px/700; letter-spacing:-.3px`(1269) | `:78-79` `title2` + `.tracking(-0.3)` | 已改齐(原无 tracking) |
| 4 | 副标题 `11.5px rgba(.55); margin-top:2; line-height:1.5`(1270) | `:90-93` `caption`(11)+ `textSecondary` + `lineSpacing(3)`,VStack spacing 2 | 刻意不同 · 11.5→11 就近对齐令牌(规范 §03) |
| 5 | 行容器 `padding:0 10px; gap:2`(1273) | `RootView.swift:145` 横 10;`ReviewView.swift:72` `rowGap` 2 | 已对齐 |
| 6 | `pick()`:`padding:11px 12px; radius:9`;选中 = 白底 + `0 0 0 1.5px #0B6BCB` + `0 1px 3px rgba(0,0,0,.06)`(1783) | `NKListRow`(`BasketDailyView.swift:946-961`) | 已对齐(批 2) |
| 7 | 行**无图标**、两行都顶格(1274–1284) | `:96-115`(图标列整段删掉) | 已改齐(原为 12px 图标 + 16pt 列宽,次行被推进去) |
| 8 | 行首行 `13/600` + `gap:8` + 右端读数(1276–1282) | `:99-105` `body`(13)/semibold + spacing 8 | 已对齐 |
| 9 | 次行 `11px .55; margin-top:3; line-height:1.45`(1283) | `:107-111` `caption` + spacing 3 + `lineSpacing(3)` | 已改齐(原 spacing 2、无行高) |
| 10 | 「每日」右端**两枚**计数徽标(绿 / 红,`10/700 radius4 padding 1px 5px; gap:3`)(1279–1281) | `pageBadge` `:139-149` 两枚 `NKChip(tone:.good/.bad)` gap 3 | 已改齐(原为一枚中性徽标「3」——一个总数看不出验没验成) |
| 11 | 「选股钟」右端 `11px .40 tabular` **纯文字**「14 结案」(1293) | `:150-155` `caption` + `monospacedDigit` + `textTertiary` | 已改齐(原为徽标 —— 徽标语义是「要你处理」,读数不该长成那样) |
| 12 | 「交易钟」右端 `11px .40 tabular`「3 在跑」(1303) | `:156-161`(读 `tradeClock.detail.running`) | 已改齐(V2.3.0 无此读数) |
| 13 | 「累计」右端 `6px` 琥珀圆点(1315) | `:162-167`(仅当四分类有 `thresholds_undecided` 行时才画) | 已改齐(V2.3.0 无) |
| 14 | 「对账」右端琥珀徽标「未上传」/ 红徽标「N 条违纪」(1325 / 信息卡与对账 325) | `:168-174` | 已改齐(「N 条违纪」为新增;判据改读**服务端有没有那一周**,不是「本次会话上传过没有」) |
| 15 | 底部「本期窗口」卡 `margin:18px 12px; padding:11px 13px; radius:9; #fff + .5px .12`(1330) | `windowCard` `:230-265` + `:67` `.padding(.horizontal,2).padding(.top,18)` | 已改齐(V2.3.0 这块住**累计页详情栏**里,叫 `windowCard(ov)`) |
| 16 | 卡内 `gap:10`;← / → 各 `22×22 radius 6`(1331–1333) | `arrowButton` `:267-276` + `:231` | 已改齐(原为 13px 裸 chevron,无点击区) |
| 17 | 「本期窗口」`10/700 .40` 居中 + 日期 `12.5/600 tabular margin-top:1` 居中(1332) | `:234-240` `.nkLabel()` + `callout`(12)/semibold/`monospacedDigit`,VStack spacing 1 | 刻意不同 · 10→10.5(`label` 档)、12.5→12 就近对齐令牌 |
| 18 | 卡内脚注 `10px .40; margin-top:6; text-align:center; line-height:1.45`(1334) | `:241-245` `caption`(11)+ 居中 + `lineSpacing(3)` | 刻意不同 · 10→11 就近对齐令牌(9.5/10 两档已按规范 §03 整档删除) |
| 19 | 对账页选中时列表栏多「已上传的周」段(`10.5/700 ls .5` 段标 + 行 `padding:8px 12px; radius:8`,选中 `rgba(11,107,203,.10)`)(信息卡与对账 321–332) | `uploadedWeeksSection` `:196-227` | 已改齐(V2.3.0 该清单是详情栏顶部一条横向 Capsule 滚动条) |
| 20 | 列表栏底部「再拖入交割单」虚线块(信息卡与对账 335) | — | **刻意不同 · 契约支撑不了的反面**:拖放要一块**够大的落点**,376pt 栏里塞不下;该块与它的两句话整体移到详情栏底部(`ReviewWorkbenchView.swift:249-252`),⛔ 功能与文案一个没丢 |
| 21 | 列表栏**没有刷新按钮**(1267–1335 全文) | `:80-88` 保留一枚 11px `arrow.clockwise` | **刻意不同 · 契约支撑不了**:复盘五段 / 双时钟 / 选股时钟是与 `model.refresh()`(工具栏那枚)**无关的独立数据源**,去掉它这一板块就只能靠重启 App 刷新;原型是静态 mock,没有刷新这个概念 |

---

## 屏 2 · 详情 · 每日(原型 1337–1384)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 详情区 `padding:22px 26px 40px; gap:16`(1338) | `RootView.swift:162-164` + `ReviewView.swift:334` `cardGap` | 已改齐(原为 `NKSpace.gap` 12) |
| 2 | 页标题 `26px/700 ls -.4`(1340) | `ReviewPageTitle` `:948-966`,调用 `:336` | 已改齐(原为 `NKSectionHeader` 15/600) |
| 3 | 副标题 `12px .55; margin-top:3`(1341) | `:960-962` `callout`(12)+ spacing 3 | 已改齐 |
| 4 | 复盘卡 `#fff / .5px .12 / radius 12 / padding:16px 18px`(1344) | `NKCard`(`SharedUI.swift:31-37`) | 已对齐(批 2);描边 `刻意不同 · .12→.10 就近对齐 NK.hairline` |
| 5 | 卡头 `gap:10; margin-bottom:10`;名称行 `gap:7`(1345–1347) | `BasketReviewRow` `:1021-1038` | 已改齐(原 spacing 6、无卡头/正文分层) |
| 6 | 名称 `15px/600`(1348) | `:1024-1025` `headline` | 已改齐(原 `body` 13) |
| 7 | Tier 徽标 `10.5/700 白字实底`(T1 绿 / T2 琥珀)(1349) | `:1026-1028` `NKChip(tone:filled:)` | 已改齐(原为中性描边徽标,看不出档) |
| 8 | 判定徽标「已验证 / 被证伪」`10.5/600 淡底`(1350) | `:1029-1031` + `verificationLabel/Tone` `:1002-1018` | 已改齐(**V2.3.0 这一屏根本没画验证结论**,只画了「详复盘」) |
| 9 | 右端 `10.5 .40 tabular`(1352) | `:1034-1036` `caption` + `textTertiary` | 已改齐(原是「D0 日期」,与页副标题重复 → 换成深度) |
| 10 | 正文 `13px; line-height:1.7`(1353) | `:1041-1043` `body`(13)+ `lineSpacing(6)` | 已改齐(原 `callout` 12、无行高) |
| 11 | 披露行 = 13px 圆圈 i +「披露 · 参考、非指令」`11 .55`(1354–1357) | `NKDisclosure`(`NKDisclosure.swift:41-86`),调用 `:1046` | 已改齐(原为常摊在卡面的 `NKReferenceNote` 整句;整句一字未删,收进展开区) |
| 12 | 未生成 + 降级 **一句话** `12px 琥珀 line-height:1.6`(1377) | `degradedText` `:1064-1072` + `:1048-1051` | 已改齐(原为两行 `caption` 琥珀) |
| 13 | 虚线「审计视图」按钮 `padding:8px 12px; radius:8; .5px dashed rgba(60,60,67,.22); width:fit-content; 11.5/600 .65`(1378) | `NKAuditSection`(`NKDisclosure.swift:123-146`),调用 `:1056` | 已改齐(原为一条蓝色文字链接「展开机械判(九项)」) |
| 14 | 审计入口**只画在第三张卡上**(1359 / 1372 两张没有) | `:1055-1057` 三张都画 | 刻意不同 · 契约支撑不了的反面:`mech` 是 B 类冻结件、是审计资产,原型只在一张上画是 mock 的 `showAuditEntry` 开关(同批 2 ③b 行处置) |
| 15 | **没有「回看某一天」那张卡**(1337–1384 全文) | `dailyDatePicker` `:386-437` 保留 | 刻意不同 · 契约支撑不了的反面:P3-47 已结案的既有能力(另存快照、不覆盖 `report`),§五 ⑤ 明写「只对版式,⛔ 不动它的既有行为」 |

---

## 屏 3 · 详情 · 选股钟(原型 1385–1437)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 页标题 26/700 + 副标题 `12px .55 line-height:1.6`(带 `<b>` 加粗)(1387–1389) | `:441-448`(**字面量 `Text`**,`**加粗**` 才解析得了) | 已改齐 |
| 2 | 四态卡 `#fff .5px .12 radius12 padding:16px 18px`;卡头 `11/700 ls .5 .40; margin-bottom:12`(1392–1393) | `stateDistributionCard` `:482-536` | 已改齐(V2.3.0 无此卡) |
| 3 | 四态条 `height:26; radius:6; overflow:hidden`,段内计数 `11/700` 白字居中(1394–1399) | `:502-519` `GeometryReader` 按占比分宽 + `clipShape(radius 6)` | 已改齐 |
| 4 | 图例 `gap:16`,每项 `8×8 radius:2` 色块 + `11px .55`(1400–1405) | `:520-533` `NKWrapRow(spacing:16)` | 已改齐 |
| 5 | 图例文字 = `verified` / `partial` / `unclear` / `falsified`(1401–1404) | `:530-531` 走 `nkVerificationStateLabel` 出中文 | 刻意不同 · 契约支撑不了的反面:项目一贯**不把服务端枚举码印上屏**(§〇c 硬伤 2 + `nkBoardLabel` 先例);原型的英文是 mock |
| 6 | 结案表卡 `#fff .5px .12 radius12 overflow:hidden`(1407) | `NKRowsCard` `:970-982`,调用 `:539` | 已改齐(V2.3.0 是一篮一张大卡,三篮一屏只看得到两条) |
| 7 | 表头 `padding:10px 18px 8px; 10.5 .40` + 底 `.5px rgba(60,60,67,.08)`(1408–1410) | `NKRowsHeader` `:984-997` | 已改齐 |
| 8 | 五列 `60px 1fr 130px 100px 90px`(1408) | `:540-546` / `:557-586` 同宽 | 已改齐 |
| 9 | 行 `padding:11px 18px` + 底 `.5px rgba(60,60,67,.06)`,末行无线(1411–1432) | `:555-587` | 已改齐 |
| 10 | 档位 = `10.5/700 白字实底 radius4 padding 2/6 width:fit-content`(1412) | `:548-550` `NKChip(filled:)` | 已对齐 |
| 11 | 篮子名 `12.5px`(1416) | `:552-553` `callout`(12) | 刻意不同 · 12.5→12 就近对齐令牌 |
| 12 | D0 行情状态 / 引擎 `11.5px .55`(1417–1418) | `:568-576` `caption`(11) | 刻意不同 · 11.5→11 就近对齐令牌 |
| 13 | D0 行情状态**印英文码**(1417 `趋势延续` 是 mock 的中文,1430 用 `D0 无判定行`) | `:570` 走 `nkRegimeLabel`(新增,`Models.swift:3907`);无行 → 「D0 无判定行」 | 已改齐 —— 🔴 **V2.3.0 实拍印的是 `trend_continuation` / `high_divergence`**(硬伤 2 的**第九处**) |
| 14 | 引擎列写「C1 v1.0」(1418) | `engineText` `:590-593` | 已改齐 —— V2.3.0 写成「引擎 C1 C1 v1.0」(`engineVersion` 本就含引擎码) |
| 15 | D1 判定 `11.5/600` 语义色(1419) | `:583-586` `caption`/semibold + `tierAccuracyTone` | 刻意不同 · 11.5→11 就近对齐令牌 |
| 16 | 表里**没有未触发原因**(1407–1435) | `:559-566` 篮子列第二行 `caption` 琥珀 | 刻意不同 · 契约支撑不了的反面:K8 验证内容⑧「未触发原因」是结案件的一部分,「没触发」与「触发了但判错」是两件事,只留一个判定列就分不出来 |
| 17 | 灰底说明块 `padding:13px 15px; radius:10; bg rgba(60,60,67,.035); .5px .10; 12px .65 lh1.6`(1436) | `NKNoteBlock`(`SharedUI.swift:111-125`),调用 `:462` | 已对齐(批 3 建的共用件) |
| 18 | **没有逐篮九项验证入口**(1385–1437) | `NKAuditSection` `:463-472` | 刻意不同 · 契约支撑不了的反面:九项冻结件 + 结案时刻 + 骨架 / 条件集版本是审计资产,表里放不下 → 整体收进一枚虚线审计按钮(⛔ 审计件不许随版式一起消失) |

---

## 屏 4 · 详情 · 交易钟(原型 1438–1465)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 页标题 26/700 + 副标题 `12px .55 lh1.6`(带 `<b>`)(1440–1442) | `:603-610`(字面量 `Text`) | 已改齐 |
| 2 | 指路卡 `padding:14px 18px; gap:11`;16px 图标 + `13.5/600` + `11.5 .55` + 12px chevron(1443–1450) | `:612-631` | 已改齐(原 spacing 8 / 图标无尺寸 / 标题 `body`) |
| 3 | 六项卡 `padding:16px 18px`;卡头 `11/700 ls .5 .40; margin-bottom:14`(1452) | `tradeAttributionCard` `:648-680` | 已改齐(V2.3.0 这一页只有一枚 `DisclosureGroup`,六项一个都没画) |
| 4 | 三列网格 `repeat(3,1fr); gap:18px 20px`(1453) | `NKStatGrid`(`SharedUI.swift:170-181`),调用 `:666` | 已改齐 |
| 5 | 每格 `11px .55` 标题 + `20/600 tabular; margin-top:2` 数值(1454–1459) | `NKStatCell`(`SharedUI.swift:148-168`) | 已改齐 |
| 6 | 六格的**项目名** = 买点偏离中位 / 持有天数中位 / 止损执行率 / 离场早于参考 / 违纪笔数 / 主观说明覆盖(1454–1459) | `nkTradeClockItemLabel`(`Models.swift:3933`)给的是契约里真的那六项 | **刻意不同 · 契约支撑不了**:原型那六个量在 `eval/iteration.py::TRADE_ITEMS` 里**一个都不存在**(理想化 mock)。在客户端算出它们 = 造服务端没给的判定 |
| 7 | 卡内**没有覆盖率那一行** | `:672-678`(`note_coverage` 一行 + 分隔线) | 刻意不同 · 契约支撑不了的反面:`note_coverage` 是契约里真有的读数,且「主观说明覆盖」正是原型第六格想说的事 —— 放回它真正的位置 |

---

## 屏 5 · 详情 · 累计(原型 1466–1544)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 页标题 26/700 + 副标题 `12px .55 tabular`(1468–1470) | `:726` + `cumulativeSubtitle` `:757-771` | 已改齐 |
| 2 | **翻周器不在这一页**(1466–1544 全文) | 已移到列表栏(见屏 1 #15) | 已改齐 |
| 3 | 成绩单卡头 `gap:8; margin-bottom:14`:`15/600` 标题 + `10.5/600 灰底` 包徽标 + 右端 `10.5 .40`「离线落盘产物 · 在线不补算」(1474–1478) | `StratumCard` `:1191-1200` | 已改齐(原为「包 X」+ 两枚徽标,无右端口径句) |
| 4 | 三列读数 + 底部 `.5px`(1480–1484) | `:1202-1214` `NKStatGrid` + `:1215` `Divider` | 已改齐(原为三行 `caption` 文本) |
| 5 | 安慰剂段标 `11/700 .40; margin-bottom:9`(1486) | `:1218` `.nkLabel()` | 已改齐 |
| 6 | 三条基准线:名 `width:72`(真实臂 `11.5/600`,其余 `11.5 .55`)+ 轨道 `height:6 radius:999 bg rgba(60,60,67,.06)` + 右值 `width:56 右对齐 12px tabular`(1487–1489) | `PlaceboArmBar` `:1144-1178` | 已改齐(原为「中位 x · 均值 y · n=z」三段纯文本) |
| 7 | 三条的满刻度(66% / 24% / 33%) | `armScale` `:1185-1190` = 最大中位数 **× 1.5** | 已改齐(比例与原型一致;⚠ 不取 max 本身 —— 那样最长一条顶到尽头,读起来像「满分」) |
| 8 | 脚注 `10.5 .40; margin-top:9`(1490) | `:1225-1226` + `:1229-1231`(产物 `note` 走 `nkMarkdown`) | 已改齐 —— 🔴 **V2.3.0 把产物里的 `**不设最高追价上限**` 星号原样印上屏**(§五 〇d 第 7 条) |
| 9 | 四分类卡 `.5px solid rgba(232,145,10,.30); radius:12; overflow:hidden`(1495) | `IterationSegmentCard` `:1637-1651` `NKRowsCard` + 琥珀描边 | 已改齐(原为「琥珀提示卡 + 每行一张白卡 + 尾卡」三层) |
| 10 | 头块 `padding:13px 18px; bg rgba(232,145,10,.07); 底 .5px rgba(232,145,10,.20)`(1496) | `header` `:1654-1682` | 已改齐 |
| 11 | 头块内:琥珀实心徽标「分界线未定 · 待你拍板」`10.5/700 白字` + `14/600` 标题 + `12px .65 lh1.6` 说明(1497–1499) | `:1656-1670` `NKChip(filled:)` + `headline` + `callout` | 刻意不同 · 14→15 就近对齐 `headline`(规范 §03) |
| 12 | 表头 `1fr 90px 90px 96px`;`padding:9px 18px 7px; 10.5 .40`(1500) | `:1643-1648` `NKRowsHeader` | 已改齐 |
| 13 | 行 `padding:10px 18px` + `.5px` 分隔;因素**等宽体** `12px`;n / 命中率 `12px .55 tabular`;建议分类 `11.5/600` 琥珀(1502–1516) | `row` `:1684-1716` | 已改齐;`11.5→11` 就近对齐令牌 |
| 14 | 表里**没有相对基线 / 安慰剂对照** | `evidenceLine` `:1720-1726`(因素下第二行 `caption` 灰字) | 刻意不同 · 契约支撑不了的反面:`delta` / `placeboEdge` 是这一段的**证据**,收进审计视图就等于把结论摆出来、把依据藏起来 |
| 15 | 安慰剂判定码 `better/worse/inconclusive/unavailable` | `nkPlaceboEdgeLabel`(`Models.swift:3919`)出中文 | 已改齐(同 #5 屏 3:服务端英文码不上屏) |
| 16 | 画像卡 `padding:16px 18px`;`15/600` 标题 + `gap:10` 行(1520–1522) | `ProfileSegmentCard` `:1345-1382` + `SegmentShell` 段头 | 已改齐 |
| 17 | 画像一行 `gap:12`:左 `12.5/600` + `10.5 .40 mt1 tabular` 窗口;中 `10.5/600 灰底` 样本徽标;右 `13/600 tabular width:60 右对齐`(1523–1529) | `ProfileRowView` `:1427-1465` | 已改齐;`12.5→12` / `13→13` 就近对齐令牌;右列 60→72(多一行口径说明) |
| 18 | 低置信度那行:副行改 `10.5/600 琥珀`「样本不足,不给结论」+ 徽标转琥珀 + 右值 `—` 灰(1526–1528) | `:1440-1448` + `headlineValue` `:1400-1410` | 已改齐(V2.3.0 用 `label` 档 10.5/700 + 仍然列出全部度量) |
| 19 | 画像行的**维度 / 分组名** | `ProfileRow.title` `:1391-1394`(`nkProfileDimensionLabel` + `nkProfileValueLabel`) | 已改齐 —— 🔴 **V2.3.0 印的是 `role · leader` / `tier · 1` / `entry_style · within_zone`**(硬伤 2 的**第十处**) |
| 20 | 画像卡**没有原始度量表** | `:1355-1373` 收进 `NKAuditSection` | 刻意不同 · 契约支撑不了的反面:度量原件是审计资产,摊在卡面上会把「结论」和「怎么算的」混成一层 |
| 21 | 移交件卡 `padding:15px 18px; gap:12`;17px 图标 + `13.5/600` + `11.5 .55 lh1.5`,**整卡可点**(1538–1543) | `handoffSection` `:775-800` | 已改齐(原为「段头 + 卡 + 一个蓝字链接」三层) |
| 22 | 累计页**没有对账段** | `:733-736` 已删(对账是自己一页) | 已改齐(同一份数据不在两处各画一遍 —— 同 ② 持仓体检、同 ⑨ 逐篮明细两条既有纪律) |
| 23 | 段没取到时的原因句(`**会自愈**` / `**不补算**`) | `NKReasonEmptyState` `:922-944`(走 `nkMarkdown`) | 已改齐 —— 🔴 **V2.3.0 星号原样上屏**(§五 〇d 第 7 条;`NKEmptyState(subtitle:)` 是 `String`) |

---

## 屏 6 · 详情 · 对账(入口 · 未上传态)(原型 1545–1573)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 页标题 26/700「对账」+ 副标题 `12px .55`(1547–1549) | `ReviewWorkbenchView.swift:41` + `emptySubtitle` `:50-53` | 已改齐(原为 `title3` 17「复盘 · 交割单对账」+ `callout` 副题) |
| 2 | 拖入区 `1.5px dashed rgba(11,107,203,.35); radius:14; padding:38px 24px; text-align:center; bg rgba(11,107,203,.03)`(1552) | `dropZoneView` `:82-122` | 已改齐(原为 `radius 12`、灰 `hairline` 虚线、`height:140` 固定、白底) |
| 3 | 34px 蓝图标 `opacity:.7`(1553) | `:89-90` | 已改齐(原为 28px 灰 `tray.and.arrow.down.fill`) |
| 4 | `15/600`「把券商交割单 .xlsx 拖到这里」(1554) | `:91-93` `headline` | 已改齐(原 `body` 13 且把「可一次拖多份」并进同一行) |
| 5 | `12px .55 lh1.6` 两句诚实披露(1555) | `:94-99` `callout` + `lineSpacing(4)`,文案取服务端 `reconcile.note` 原文 | 已改齐(**V2.3.0 这两句一句都没有**) |
| 6 | 蓝实底「选择文件」按钮 `padding:8px 16px; radius:8; 12.5/600 白字; margin-top:16`(1558) | `:100-108` + `chooseFiles()` `:135-152`(`NSOpenPanel`) | 已改齐(V2.3.0 **只能拖,不能选**) |
| 7 | 下方「上一份对账 · 07-27 ~ 07-31 · 本周未发现违纪 · 查看 →」卡(1563–1570) | — | **刻意不同 · 契约支撑不了**:没有「列出已上传的周」这条端点(`GET /review?week=` 只按周查单条),客户端**无从知道上一份是哪一周**。⛔ 不许拿「往前猜几周」凑(那是替服务端编事实)。登记在 `ReviewWorkbenchView.swift:204-222` |
| 8 | 未上传时**没有第二张空态卡** | `emptyState` `:204-222`(没上传过时整块不画) | 已改齐(V2.3.0 拖入区下面还有一张「还没有对账数据」——同一件事说两遍,第二遍还更像故障) |

---

## 屏 7 · 对账工作台 · 已上传态(原型 `Neckline 信息卡与对账.dc.html` 337–450)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 标题块 `26/700`「2026-W32 对账」+ `12 .55 tabular` 副题 + 右端 `11 .40`「解析 / FIFO 闭合 / 三查 / 章程判定全在后端」(340–347) | `uploadedHeader` `:55-73` | 已改齐(V2.3.0 无标题块,直接就是拖入区) |
| 2 | 强制复盘条 `padding:15px 17px; radius:12; linear-gradient(100deg,#E5443B,#E8910A); gap:13`;21px 图标 + `15/700` 白字 + `12.5 .90 lh1.5`(358–366) | `ForcedReviewBanner` `:351-372` | 已改齐(原 `padding 14` / 16px 图标 / 标题 `body` 13) |
| 3 | 违纪清单卡 `overflow:hidden`;卡头 `padding:14px 18px 11px; gap:8`,`15/600` + 右端红实底 `10.5/700`「3 条」(373–378) | `violationsCard` `:266-306` `NKRowsCard` | 已改齐(原为 `NKSectionHeader` + 纯文字计数) |
| 4 | 每条 `padding:12px 18px; gap:11` + 顶 `.5px rgba(60,60,67,.07)`;正文 `13px lh1.55`(379–383) | `:288-304` | 已改齐(原 `callout` 12、spacing 6、无分隔线) |
| 5 | 每条左端**类型徽标**(单笔上限 / 冷却期 / 止损滞后)(380) | `:290-292` 统一用 `exclamationmark.triangle.fill` 图标 | **刻意不同 · 契约支撑不了**:`disciplineViolations` 是 `[String]`(服务端拼好的整句),**没有类型码那一格** —— 在客户端按关键词猜类型 = 造判定 |
| 6 | 本周章程卡 `padding:16px 18px`;卡头 `11/700 ls .5` + 右 `11.5 .55`(400–403) | `CharterVersionCard` `:375-431` | 已改齐(原标题为 `body`/semibold「本周章程」) |
| 7 | 两段**并排**:各 `flex:1 padding:10px 12px`,左段灰底 `rgba(60,60,67,.05)`、右段蓝底 `rgba(11,107,203,.06)` + `.22` 描边,中间 `26×2` 连接线(405–408) | `:396-405` + `segmentCell` `:414-428` | 已改齐(原为竖排「· v1.3.2 判定 4 笔(周初 起)」纯文本行) |
| 8 | 段内 `12/600` 版本 + `10.5 .45 mt2 tabular` 区间与笔数(405) | `:415-421` `callout` + `caption` | 刻意不同 · 10.5→11 就近对齐令牌 |
| 9 | 切换提示 `11.5 琥珀 mt11 lh1.55`(带 `<b>`)(409) | `:407-412`(**字面量 `Text`**) | 已改齐(V2.3.0 的切换行没有这句「⛔ 不是整周按周初那版判」) |
| 10 | 本周统计卡:卡头 `11/700 ls .5 .40 mb14` + 四列 `repeat(4,1fr) gap:18px 20px`,格 `11 .55` + `20/600 tabular`(414–421) | `ReviewStatsCard` `:433-471` `NKStatGrid(columns: 4)` ×2 | 已改齐(原为 `NKSectionHeader` + 两行 `headline`(15)读数) |
| 11 | 金额 `+¥2,864` / `¥186.4`(419–420) | `:451-456` 走 `NKFmt.signedAmount` / `NKFmt.amount` | 已改齐(原 `String(format:"¥%.0f")` —— 批 1 全局钉子的漏网点之一) |
| 12 | 成交表卡头 `padding:14px 18px 11px`,`15/600` + 右 `11 .40`「4 笔买入」(412–415) | `planChecksSection` `:308-348` | 已改齐 |
| 13 | 表 `1fr 92px 92px 92px 88px` 五列(标的·日期 / 实际买价 / **计划区间** / **偏离** / 台账)(413) | `:326-331` 四列(标的·日期 / 成交金额 / 计划核对 / 台账) | **刻意不同 · 契约支撑不了**:`ReviewPlanCheck` 里**没有** `planLow` / `planHigh` / `deviationPct` —— 服务端只发一句 `plan_status`。反推区间与偏离 = 造服务端没给的数 |
| 14 | 行 `padding:11px 18px`;标的 `12.5/600` + 日期 `11 .40 tabular`;数值 `12.5 tabular`(423–426) | `PlanCheckRow` `:474-511` | 已改齐 |
| 15 | 表底提示条 `padding:11px 18px 14px; bg rgba(232,145,10,.04); 顶 .5px; 11.5 .65 lh1.6`(434) | `:339-345` | 已改齐(V2.3.0 无) |
| 16 | 底部虚线「审计视图 · 成交回合明细、止损纪律核对、解析提示原文」`padding:10px 14px; radius:9; .5px dashed; 12/600 .65`(447) | `NKAuditSection` 调用 `:239-248`(回合 / 止损)+ `:189-201`(解析提示) | 已改齐(V2.3.0 这三块是三个常开的 `NKSectionHeader` 分节,把工作台拉得极长) |
| 17 | 「再拖入交割单」在**列表栏底部**(335) | `:249-252` 详情栏底部,文案换一套(「可一次拖多份 · 同一周再传会用新的那一份覆盖上面这份对账」) | 刻意不同 · 契约支撑不了的反面:见屏 1 #20;⚠ **文案必须换** —— 已上传的周再画「系统查过了 —— 本周确实没有上传记录」就是假话(实拍逮到) |

---

## 密度复查(批 1 `NKChip` 去胶囊后,本批六屏逐张看过)

| 屏 | 徽标密度 | 结论 |
|---|---|---|
| 列表栏 | 每日两枚 `10.5/600 radius4`、对账一枚;窗口卡零徽标 | 无挤压;两枚计数徽标 gap 3 与原型一致 |
| 每日 | 名称 + Tier + 判定三件走 `NKWrapRow(spacing:7)` | 376+ 的详情栏内一行放得下;窄栏会自动折行(⛔ 不竖排) |
| 选股钟 | 表内档位徽标固定 60pt 列 | 无挤压 |
| 累计 | 成绩单卡头两枚徽标 + 四分类头块一枚实心徽标 | 无挤压 |
| 对账工作台 | 违纪清单一枚实心红徽标 | 无挤压 |

---

## 施工期新逮的两条(已在 §五 〇d 同族,本批为新增现场)

1. 🔴 **硬伤 2 的第九、第十处**:选股时钟的 `regimeAtD0`(`trend_continuation`)与画像行的
   `dimension`/`value`(`role` / `leader` / `within_zone` / `tier` / `1`)**都是服务端英文码直接进 `Text`**。
   已按 `nkBoardLabel` 先例补 `nkRegimeLabel` / `nkProfileDimensionLabel` / `nkProfileValueLabel` /
   `nkPlaceboEdgeLabel`(未识别值原样透传)。**判据仍然是那一条:见到 `dict` 的 key 或枚举码直连界面就停一下。**
2. 🔴 **`Text(String)` 不解析 Markdown 的第三、第四处现场**:这一次不是客户端文案,而是
   **服务端按 markdown 写好的产物文案**(校准 `unavailableReason` 的 `**会自愈**` / `**不补算**`、
   安慰剂 `note` 的 `**不设最高追价上限**`、四分类 `suggestion` 与 `disclaimer`)。
   已补共享件 `nkMarkdown(_:)`(`SharedUI.swift:138`,解析失败原样返回纯文本,⛔ 不吞内容)。
   ⚠ **别拿它去"清洗"星号** —— 那是删信息,渲染成加粗才是原意。

## 登记但未改(超出批 4 范围)

| # | 事 | 归谁 |
|---|---|---|
| 1 | `generatedAt` 在标题块里是 ISO 原串(`2026-08-15T09:12:00+08:00`),原型是 `08-08 09:12` | 展示层格式化,与批 4 的版式无关;整个 App 的时间戳展示口径应一次收(挂账) |
| 2 | 对账段 `note` 末尾「(上传在 macOS 端的复盘 · 对账页)」在 macOS 上是废话 | **服务端文案**,⛔ 客户端不改写(`_reconcile_segment`) |
| 3 | 「上一份对账」卡要一条「列出已上传的周」端点 | 服务端(本版 ⛔ 不碰 `neckline/**`) |
