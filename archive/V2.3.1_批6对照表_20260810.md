# V2.3.1 批 6(两个全屏状态:退潮红色刹车 / 报告未生成)逐屏对照表 · 2026-08-10

> **这是 §五 V2.3.1-⑦ 的验收件**(PROJECT_PLAN §九 当日条一行 + 本文件全文,同一件事只存在一处)。
> **判据来源** = `Neckline视觉升级/Neckline 状态.dc.html` 的 **inline style**
> (屏 1 = **37–187 行**;屏 2 = **188–328 行**)+ macOS 原型文件尾 `<script type="text/x-dc">`
> 里的 `pick()` / `nav()` / `gearBtn()`。⛔ 不是剥出来的纯文本。
>
> **判定只有三种**:`已对齐`(本来就一致)/ `已改齐`(本批改成一致)/
> `刻意不同`(必须带三类合法理由之一:① 契约支撑不了〔引 §五 〇〕· ② 平台差异 ·
> ③ 规范 §03/§04 要求向令牌就近对齐)。
> **⛔ 只收版式行**(字号 / 颜色 / 间距 / 圆角 / 排布 / 层级);数据差异不进表。
>
> **实拍**(`/private/tmp/claude-501/nk-v231-b6/shots/`,窗口由 `NECKLINE_WINDOW_SIZE` 钉死 **1200×860**):
>
> | 文件 | 内容 | 演示后端 |
> |---|---|---|
> | `b6_01_brake.png` | **屏 1 · 退潮红色刹车激活**(工具栏 + 通栏刹车条 + 列表栏 + 篮子卡详情) | `:8016`(`sentinel_events` 有 `retreat/brake` 行) |
> | `b6_02_notready.png` | **屏 2 · 今日报告尚未生成**(排程表 · 回归实拍) | `:8017`(空库,一份报告都没有) |
> | `b6_03_overview_regress.png` | **回归**:今日概览(⑤ 数据新鲜度改版后,批 2 屏 3 复查) | `:8018`(有报告、无刹车) |
> | `b6_04_positions_regress.png` | **回归**:持仓详情(刻度尺标签防重叠 + 状态徽标后,批 3 屏 1 复查) | `:8018` |
>
> ⚠ **为什么多了两张回归**:本批动了三件**双端 / 跨屏共用件**(`DataFreshnessDetail` /
> `NKStopScale` / 仓位行状态徽标),它们分别落在批 2 与批 3 已验收过的屏上 ——
> 「改共用件只在自己这一屏看一眼」正是 V2.3 一路踩过的那个坑。

---

## 屏 1-A · 工具栏(原型 38–78 行)

| # | 原型值(行号) | 落地值(`.swift` 行号) | 判定 |
|---|---|---|---|
| 1 | 栏高 50 / `padding:0 14px` / 底边 `.5px`(38) | `NKToolbar.swift:26-28`、`:65-68` | 已对齐(批 1) |
| 2 | 红绿灯 12px×3 · gap 8 · 距 Logo 8(39–43) | `NKTrafficLightAligner` `:292-344` | 已对齐(批 1);系统按钮 14pt 框 / 节距 23 → **刻意不同 · 平台差异**(三颗是系统控件,尺寸不可改) |
| 3 | **退潮刹车胶囊** `gap:6; padding:4px 10px 4px 8px; radius:7; background:#E5443B` + **6px 白圆点** + `12/700 #fff`(67–70) | `NKToolbar.swift:129-134` → 改走 `regimeShell(dot:.white, …, bold:true)`;`:146-155` 新增 `bold` 形参 | **已改齐** —— V2.3.0 是**另写一个分支**:没有白点、左右内边距被拉平成 10/10、字重 600。三态胶囊形状必须同一个壳,否则"翻红"看起来像换了个别的控件 |
| 4 | 胶囊 radius 7(67) | `NKRadius.control` = 8 | 刻意不同 · **就近对齐令牌**(规范 §04) |
| 5 | 计数徽标 `10/700 #0B6BCB; bg rgba(11,107,203,.12); radius 4; padding 1px 5px`(53) | `NKToolbar.swift:94` `NKChip(tone:.info)` | 已对齐(批 1 徽标去胶囊) |
| 6 | 持仓胶囊上 6px 红点(57) | `:98-100` | 已对齐 |
| 7 | metaLine `11.5 rgba(60,60,67,.55) tabular`(71) | `:161-163` `caption` + `textSecondary` | 已对齐(批 2) |
| 8 | 刷新按钮 `padding:5px 10px; radius:7; #0B6BCB` + `11.5/600 #fff` 时刻(72–75) | `:216-238` | 已对齐(批 2) |
| 9 | 齿轮 28×28 / radius 7(76) | `:241-265` | 已对齐(批 2) |

## 屏 1-B · 通栏刹车条(原型 79–87 行)🔴 本批主体

| # | 原型值(行号) | 落地值 | 判定 |
|---|---|---|---|
| 1 | **不进卡片流**、压在工具栏正下方通栏(79) | `RootView.swift:71` `brakeBar` 挂在壳上(双端) | 已对齐(V2.3.0 起) |
| 2 | `padding:14px 18px`(79) | `SharedUI.swift:401` `.padding(.horizontal, 18).padding(.vertical, 14)` | **已改齐**(原为横 `pagePad` 22 / 纵 9) |
| 3 | `gap:14`(79) | `:374` `HStack(spacing: 14)` | **已改齐**(原 9) |
| 4 | 图标 24×24 **线性**三角 stroke `#fff` 1.7(80) | `:375-376` `exclamationmark.triangle` `.system(size: 24)` | **已改齐**(原 `NKFont.body` 13 的**实心**三角) |
| 5 | 标题 `16px/700; letter-spacing:-.2; #fff`(84) | `:379-380` `NKFont.title3` + `.bold()` + `.tracking(-0.2)` | **已改齐**;16 → 令牌 17 = 刻意不同 · **就近对齐令牌** |
| 6 | 次行 `12.5px; rgba(255,255,255,.90); margin-top:3; line-height:1.5`(85) | `:383-385` `callout` + `.opacity(0.90)` + `lineSpacing(2)`;`:377` `spacing: 3` | **已改齐**(原 `caption` 11 / `.opacity(0.92)` / spacing 2) |
| 7 | 🔴 次行内容 = **依据一句**(「14:07 触发 · 炸板率…」),⛔ 不是把标题重说一遍(85) | `AppModel.swift:421-431` `retreatWarning` **去掉「退潮红色刹车已触发 · 今日计划作废、不建议开新仓(依据:…)」前缀** | **已改齐** —— V2.3.0 实拍上**同一句话在同一条栏里连着出现两遍**(标题一遍、次行前缀一遍) |
| 8 | 右端按钮「看哪几条触发了」`padding:7px 14px; radius:8; bg rgba(255,255,255,.20); border .5px rgba(255,255,255,.35); 12.5/600 #fff`(87) | `SharedUI.swift:414-431` `actionButton` | **已改齐**(V2.3.0 整枚没有) |
| 9 | 按钮**去哪**(原型只画了按钮) | `RootView.swift:105-116`:切到**持仓 · 盘中动态**(`positionsPaneRequest="board"`) | **刻意不同 · 契约支撑不了** —— 原型配的是那张「三个条件族 · 逐条阈值读数」明细卡,`RetreatBrake` 契约只有 `{active, reason}`(见屏 1-D 第 5 行);盘中动态是**已有的、能说得出口的**那一屏(刹车依据 + 哨兵事件含黄色预警) |
| 10 | 底色 `linear-gradient(100deg,#E5443B 0%,#E8910A 130%)`(79) | `SharedUI.swift:330-337` `barGrad`(同两个色值,起止点 `(0,0)→(1.3,0.6)`) | **已改齐** —— 共用令牌 `NK.alertGrad` 的 `topLeading→bottomTrailing` 是给方块用的,拉到 1200×72 通栏上**橙色提前吃满**。⚠ **不是新色令牌**:两个 hex 与 `alertGrad` 逐字相同,只有几何不同 |
| 11 | `box-shadow:0 2px 12px rgba(229,68,59,.28)`(79) | `:405-407` `.shadow(color: NK.down.opacity(0.28), radius: 6, y: 2)` | **已改齐**(CSS blur 12 ≈ SwiftUI radius 6) |
| 12 | 按钮在右端 `flex:none`(87) | macOS 一行到底;**iOS 换到文案下面** | 刻意不同 · **平台差异**(CLAUDE.md 已登记的 402pt 挤压坑:一行放「24 图标 + 两行标题 + 110pt 按钮」会把标题挤成竖排单字) |

## 屏 1-C · 列表栏(原型 89–125 行)

| # | 原型值(行号) | 落地值 | 判定 |
|---|---|---|---|
| 1 | 栏宽 376 / `#FCFCFD` / 右分隔 `.5px`(89) | `RootView.swift:135`、`:154` | 已对齐(批 1) |
| 2 | 标题区 `padding:18px 16px 10px`(90) | `BasketDailyView.swift:130-142` | 已对齐(批 2) |
| 3 | 「选股」`22/700; letter-spacing:-.3`(91) | `:133-134` | 已对齐(批 2) |
| 4 | 副标题 **`11.5px; color:#E5443B; font-weight:600`**「今日计划已作废 · 篮子仍然列出,点得开」(92/94) | `:135-141`(刹车时 `NK.down` + `.semibold`)+ `listSubtitle` `:158-166` | **已改齐**(V2.3.0 刹车时这一行仍是灰色的篮子计数) |
| 5 | 副标题 `margin-top:3`(94) | VStack `spacing: 2` | 已对齐 —— macOS 主原型 84 行是 `margin-top:2`,状态原型 94 行写 3;**同一元素两份原型差 1px,取 macOS 主原型** |
| 6 | 行容器 `padding:0 10px 4px`(93) | `NKSpace.listPadH` 10 | 已对齐(批 2) |
| 7 | 「今日概览」行 `padding:11px 12px; radius:9` + 13px 图标 + `12.5/600` + 右端 `10.5 .40`「①·情报·⑤」(94–99) | `overviewRow` `:167-193` | 已对齐(批 2) |
| 8 | 今日概览行**只有一行**(94–99) | 落地有**次行摘要**(「情绪半额 · 板块数据落后 1 日」) | 刻意不同 · **契约支撑不了 §五 〇 的反面**:macOS 主原型 87–95 行**画了次行**,状态原型这一屏是简化 mock;取主原型(批 2 已定案) |
| 9 | T1 分组头 `gap:7; padding:12px 16px 7px` + `10/700` 实心 Tier 徽标 + `10.5/600 ls .5 .40` + 细线(101–104) | `NKGroupHeader` `SharedUI.swift:45-56`;`BasketDailyView.swift:302-307` | 已对齐(批 2) |
| 10 | 篮子行选中 = 白底 + `0 0 0 1.5px #0B6BCB` + `0 1px 3px rgba(0,0,0,.06)`(106) | `NKListRow` | 已对齐(批 2) |
| 11 | 🔴 行内红条「今日计划作废 —— 这篮的卡与判定照常可读」`margin-top:9; padding:6px 9px; radius:7; background:rgba(229,68,59,.08); 10.5/600 #E5443B`(123) | `BasketDailyView.swift:1032-1042` | **已改齐**(V2.3.0 整条没有);radius 7 → `control` 8 = 就近对齐令牌 |
| 12 | 红条挂在**每一个**篮子行上(原型只有一篮) | 落地 T1/T2 两行都挂 | 已改齐 —— 作废的是**今天整份计划**,不是某一篮;只标一行会读成"只有这篮作废" |
| 13 | 六关灯条 / 成员行 / 分数(原型这一屏简化掉了) | 落地保留(批 2 定案) | 刻意不同 · **契约支撑不了 §五 〇 的反面**:主原型 118–147 行画了,状态屏是 mock 简化 |

## 屏 1-D · 详情栏 · 篮子卡(原型 128–172 行)

| # | 原型值(行号) | 落地值 | 判定 |
|---|---|---|---|
| 1 | 详情区 `padding:22px 26px 40px; gap:16`(129) | `RootView.swift:162-164` | 已对齐(批 2) |
| 2 | 徽标行 `gap:7` + 「T1 第 1 位」`10.5/700 #fff bg #0FA968 radius 4 padding 2px 6`(132) | `BasketCardView.swift:88-95` | 已对齐(批 2) |
| 3 | 🔴 **多一枚「今日计划作废」`10.5/700 #fff bg #E5443B radius 4 padding 2px 6`**(133) | `BasketCardView.swift:104-107`(卡就绪)/ `:136-138`(卡未就绪) | **已改齐**(V2.3.0 详情栏对刹车零反应) |
| 4 | 大标题 `26/700; letter-spacing:-.4`(135) | `:112-114` | 已对齐(批 2) |
| 5 | 🔴 **红描边卡** `#fff; border:.5px rgba(229,68,59,.30); radius:12; padding:16px 18px`(138) | `brakeNoticeCard` `BasketCardView.swift:170-215` | **已改齐**(整块 V2.3.0 没有) |
| 6 | 正文 1 `13.5px #1D1D1F; line-height:1.7`,含 `<b>今日的开仓计划</b>`(139) | `:180-183` `NKFont.body` + `lineSpacing(5)` + **整条字面量 `**加粗**`** | 已改齐;13.5 → `body` 13 = 就近对齐令牌。⚠ 写成**一整条字面量**才解析 Markdown(§五 〇d 第 7 条) |
| 7 | 正文 2 `12.5 rgba(60,60,67,.65); line-height:1.65; margin-top:10`(140) | `:184-188` `callout` + `lineSpacing(4)` + `.padding(.top, 10)` | 已改齐 |
| 8 | 双按钮 `margin-top:14; gap:9`;主 `padding:8px 15px; radius:8; #0B6BCB; 12.5/600 #fff`;次 `padding:8px 15px; radius:8; border .5px rgba(60,60,67,.20); 12.5/600 rgba(60,60,67,.75)`(141–144) | `:189-199` + `brakeButton` `:219-232` | 已改齐 |
| 9 | 🔴 「仍要补录开仓」**不灰化**(散文 31 行 + `⛔ 这个按钮任何情况下都不灰化`) | `:196-199` 无任何 `disabled`;`PositionsView.swift:341-349` `addPositionButton` 同样无条件 | **已对齐**(V2.2-⑤-B 拆熔断时定案;本批复核了两处入口) |
| 10 | 尾注 `11px rgba(60,60,67,.40); margin-top:9; line-height:1.5`(145) | `:200-203` `caption` + `textTertiary` | 已改齐 |
| 11 | 🔴 **「三个条件族 · 哨兵 14:07 落库」整张卡**(148–172):三行读数(炸板率 61.4% ≥ 50% / 跌停 11 只 ≥ 5 只 / 跳水 -3.8% ≤ -3.0%,各带分母)+ 双级制 / 早盘加严两条 + 琥珀口径块 | **不画** | 🔴 **刻意不同 · 契约支撑不了** —— `RetreatBrake` 只发 `{active, reason}`(`Models.swift:2352-2355`;服务端 `api/app.py::board()` 把 `payload.body` 原样塞进 `reason`),**逐条阈值、读数、分母、双级制档位客户端一个字都没有**。画出来就是在客户端造服务端从未下发的数。⚠ 已把 `reason` 全文放进刹车条次行 + 盘中动态那一屏,**能说的都说了** |

## 屏 2 · 今日报告尚未生成(原型 188–328;**批 2 已做,本批只做回归**)

| # | 原型值(行号) | 落地值 | 判定 |
|---|---|---|---|
| 1 | 列表栏副标题「今晚 16:35 出计划」`11.5 .55; margin-top:2`(231) | `listSubtitle` `BasketDailyView.swift:163` | 已对齐(批 2) |
| 2 | 今日概览行右端**一颗琥珀点** + 次行「报告未生成 · ⑤ 数据新鲜度可看」(233–235) | `:181-184` / `overviewRowCaption` `:198` | 已对齐(批 2) |
| 3 | 列表栏空态:30px 月亮 + 两行 `12 .40` 居中(240–246) | `notReadyListPlaceholder` `:261-273` | 已对齐(批 2) |
| 4 | 详情 = **一张卡** `padding:30px 26px 26px`(253) | `notReadyCardMac` `:453-515`(`NKCard(padding: 26)`) | 已对齐(批 2) |
| 5 | 44×44 radius 12 图标块 + `20/600 ls -.3` 标题 + `13 .55` 副标题(255–261) | `:456-470` | 已对齐(批 2) |
| 6 | 「今晚的流水线」`11/700 ls .5 .40` + 右端说明 `10.5 .40`(264–266) | `:475-481` | 已对齐(批 2) |
| 7 | 🔴 **「排程表 · 非实时状态」那句在不在**(§五 〇-2 硬要求) | `:478-480` 右端整句 = 「排程表 · 非实时状态 · 16:35 是晚间链启动时刻,不是报告落地时刻」,**琥珀色** | **已对齐 · 本批当面复核过**(实拍 `b6_02_notready.png` 可读) |
| 8 | 四节点时间轴 + 连线,格宽 88(268–275) | `pipelineTimeline` `:518-539`(格宽 96) | 已对齐(批 2;96 = 中文标签宽一档) |
| 9 | 16:05 那格 = **琥珀 + 4px 光晕 +「日更进行中」**(272) | 四个时刻**同样着色**,折叠区当面写「哪一段正在跑,客户端此刻并不知道」 | **刻意不同 · 契约支撑不了**(§五 〇-2:客户端没有任何通道观察批算进度;画进度标记 = 把「没看」讲成「看到了」) |
| 10 | 「16:35 之后三段串行」灰块 `padding:11px 13px; radius:9; bg rgba(60,60,67,.035); .5px`(277–283) | `pipelineSegments` `:542-564` | 已对齐(批 2) |
| 11 | 底部「两回事」灰块 `padding:12px 14px; radius:9`(286) | `:487-499` | 已对齐(批 2) |
| 12 | ⑤ 卡头右端徽标「这一段恒在 · 但此刻只说得出一句」`10.5/600 .55; bg rgba(60,60,67,.07); radius 4; padding 2px 7`(289–292) | `freshnessSection` `:850-855` `NKChip` | 已对齐(批 2) |
| 13 | 20px 问号图标 + `14/600 .75` 一句 + `12.5 #E8910A` 一句(293–297) | `:869-882` | 已对齐(批 2) |
| 14 | 分隔 + `11.5 .55` 说明,含 `<b>恒在</b>` 与两处等宽 `degraded` / `dataFreshness`(299) | `:885-889`(**整条字面量**,`**恒在**` 解析成加粗) | 已对齐(批 2 修) |
| 15 | 蓝块「需新增端点 · 想在这里看到 16:05 批算跑到哪了的话…」(301–306) | 同一段话在 `notReadyCardMac` 的折叠区(`:501-511`) | 刻意不同 · 契约支撑不了的**同一件事只说一处**(⛔ 一屏两处都讲「客户端看不到进度」= 重复);文案未删(批 2 定案,本批沿用) |

---

## 共用件改动 · 影响面 · 据此重拍了哪几屏

| 共用件 | 改了什么 | 影响面 | 本批重拍 |
|---|---|---|---|
| `SharedUI.swift::RetreatBrakeBar` | 整块按原型 79–87 重画 + 新增 `actionTitle/action` | 双端壳(`RootView.brakeBar`)—— 只在刹车激活时出现 | `b6_01_brake.png`;iOS 侧刹车条**不在批 7 八屏之列**(iOS 原型没画),已确认 402pt 下按钮换行不挤 |
| `SharedUI.swift::DataFreshnessDetail` | 两列版式 → **7px 色点 + 定宽 130 标题 + 右值 + 行间 `.5px`**(原型 786–788) | macOS 今日概览 ⑤(批 2 屏 3)· iOS 选股 ⑤ | `b6_03_overview_regress.png`;iOS 见批 7 |
| `SharedUI.swift::MarketRegimeStrip` | 抽出 `regimeHeadRow`(macOS / 非 compact 原样)+ **iOS compact 换白卡 + 色点头行** | macOS 持仓列表栏(批 3)· macOS 今日概览 · iOS 两屏 | `b6_04_positions_regress.png`(macOS 窄块未变) |
| `NKToolbar.swift::regimeShell` | 加 `bold` 形参;刹车分支改走同一个壳 | macOS 工具栏三态 | `b6_01`(刹车)+ `b6_03/04`(常态) |
| `NKStopScale.swift` | **上排标签防重叠**(`topLabelCenters` 沿横轴推开) | macOS 持仓详情(批 3)· iOS 持仓详情 | `b6_04_positions_regress.png` |
| `PositionsView.swift` 仓位行 | 破线时名字旁**多一枚「已破止损线」实心红徽标** | macOS 持仓列表栏(批 3)· iOS 持仓主屏 | `b6_04`(列表栏第二行)+ 批 7 `s2` |
| `AppModel.swift::retreatWarning` | 去掉重复前缀 + 新增 `positionsPaneRequest` | 刹车条次行;单测 `AppModelTests:18-31` 仍绿(断言是 `contains("炸板率飙升")`) | — |

## 逮到的裸 `**` / 英文码

**本批零新增**。三处新写的 Markdown 文案(红描边卡两段 + 尾注)全部是**整条字面量**,已按 §五 〇d 第 7 条写;实拍逐字核对无星号上屏。服务端英文码在本批两屏上**不出现**(刹车条只吐 `reason` 自然语言,排程表是客户端常量)。

## 超范围登记(不属批 6,已记不改)

1. `AppModel.positionsPaneRequest` 是**字符串**而不是 `PositionsPane` 枚举 —— 枚举住在 `PositionsView.swift`(视图层),`AppModel` 反向 import 会把依赖方向倒过来。若以后跨板块请求变多,应把 `PositionsPane` 提到 `Models.swift`。
2. 刹车激活时 `NKToolbar` 的**行情状态被整枚顶掉**(胶囊位只剩「退潮刹车」)——原型同样如此(67–70 行没有行情状态胶囊),但契约上两者是**两个独立的量**,同时成立时用户看不到行情状态。要两枚并排得先量一次 1200pt 下工具栏右段还剩多少。
3. `RetreatBrakeBanner`(卡片式刹车横幅,`SharedUI.swift:299`)自 V2.3 起**已无任何调用点** —— 通栏条取代了它。本批未删(删组件不属"保真度收敛"),登记给收尾批。
