# V2.2.0 批 1 完工记录(块① 引擎注册表 + 块② 行情状态层)

> **归档件性质**:本批**完工验收**的全文留档(记录纪律:`PROJECT_PLAN.md` §九 只留一行 + 本链接)。
> **口径权威仍是 `PROJECT_PLAN.md` §五 V2.2.0**;本文只记「这一批实际做了什么、验收证据是什么、
> 施工期做了哪些 plan 未写死的裁定」,**不追改后续裁定**。
> **完工时间**:2026-08-08 23:4x CST(⚠ 日期说明见文末「一处日期出入」)。
> **状态**:本地施工完成、**⛔ 未部署**(部署是 §五 ⑦ 批 1 的独立动作,且 ⑦ 批 1 判据有一处
> 自相矛盾仍待用户裁定,见文末「悬案」)。

---

## 一、验收证据(orchestrator 独立复核过,非仅采信 builder 自述)

| 项 | 结果 |
|---|---|
| 全量套件 | **3166 passed / 3 skipped / 0 failed**(60.07s)。基线 3063 passed / 3 skipped / **2 failed** → 净增 103 例、P1-46 两条红转绿 |
| 表数 | 46 → **47**(+1 = `market_regime_daily`);`selection_packs` 列 10 → **12**(+2 = `line_code` / `status`) |
| 索引 | `idx_selection_packs_single_active` → `idx_selection_packs_single_active_per_line`(换一条) |
| `ENGINE_API_VERSION` | 1 → **2**(实测 `= 2`) |
| 边界 | `PROJECT_PLAN.md` / `CLAUDE.md` / `archive/packs_retired/K4-pack.json` / `archive/packs_retired/K7-pack.json` **git diff 全空** |
| 生产 | **零接触**(未部署、未 ssh);`HEAD` 施工期间未动(收尾时才提交) |
| 开发库 | `market_regime_daily` 0 行 / `selection_packs` 0 行 —— 施工期临时写入已清干净 |
| A8 探针(P4-48 判据) | `total=1 writable=0 readonly_uri=1` —— **可写命中集为空**,唯一命中是 `conftest.py::real_db_readonly_copy` 的 `mode=ro` 只读通道(设计内合法) |

**真实回放(块②)**:dev 库副本 + 真实 parquet,20260714–20260724 共 9 天全部落行;**20260722 打出真实
`rotation_confirmed`**(`rot.old_5d=-0.0550<0:ok; rot.gap=0.1316>0.0300:ok; rot.strength_3d=2>=2:ok;
rot.rank_gain=80>=10:ok`)—— 证明三态在真实数据上确实会变,不是恒落默认态。

**`retire_legacy_packs.py`(块①)**:临时库(含 `line_code='LEGACY'` + `is_active=1` 行)上演练前后
sha256 逐字节相同(零写);`--confirm` 后行变 `('K7-pack-v1','LEGACY','running',0)`、事件流追加
`deactivate/cli/'V2.2 K8 换线,LEGACY 包退役留档'`、**行数前后逐位相等(零 DELETE,硬断言)**、双备份齐;
二跑 `--confirm` = 幂等 no-op。

**四个新包走四道闸演练**:`K8-skeleton` / `C1` / `Z1` / `Y1` 各 `闸1通过 + 闸2通过 + [dry-run] 不写库`;
`archive/packs_retired/K7-pack.json` → `engine_api_version 不兼容:包声明 1,引擎现为 2` + `exit=2`(回滚锚已作废,钉成机器判据)。

---

## 二、块① 交付(引擎注册表 + 股票池骨架)

- `selection_packs` 幂等补 `line_code`(DEFAULT `'LEGACY'`)/ `status`(DEFAULT `'running'`);唯一现役约束
  由「全表唯一」改为「**每线唯一**」。
- 读侧四入口:`get_active_line` / `get_active_skeleton` / `get_active_engines`(C→Z→Y 确定性排序、
  过滤 `status='running'`)/ `get_active_pack`(= V 线薄封装)。
- `ENGINE_API_VERSION` **1 → 2**,两个回滚锚(`K4-pack-v1` / `K7-pack-v1`)**如设计作废**,回滚绳改
  「代码 commit + DB `.bak` 还原」;全仓已无「回滚 = 激活旧包」写法。
- 四个包文件:`packs/K8-skeleton.json`(`K8-V0.5`)+ `C1` / `Z1` / `Y1`(阈值逐条照 §五 ③-F,
  **全部带 `provenance`**,`audited` 三项 / 其余 `engineering_v1` + `basis` + `calibration:pending`)。
- 新原语 `industry_blacklist`(按 `stock_basic.industry` **精确归类**,`industry` 不进 `_ALLOWED_FEATURES`,
  **缺失保守判通过**)。
- `scripts/oneoff/retire_legacy_packs.py`(默认演练 / `--confirm` 才写 / 双备份 / 零 DELETE 硬断言)。
- **三张老账结案**:**P2-47**(卫生线双源 → 抽三个共享常量,`primitives.py` 与 `neckline/research/panel.py`
  同源 + 逐位对拍守门)· **P4-48**(测试隔离 → conftest 全局 `DB_PATH` 重定向,治类不治例)·
  **P1-46**(两条周末日期炸弹 → `frozen_clock` 体例冻结,锚 2026-07-29 周三)。
- **P4-54 两处文案**:`test_v13_exit_6y_baseline.py` skip 理由改如实(六年真回测基线 **N=1288 /
  total_return −20.53%** 在本仓**已永久无法复跑**,本文件是该冻结数字的唯一载体,⛔ 别删)·
  `compare_industry_stage_with_research.py` 文件头加迁出说明。

### 块① 施工期实测事实

- **`stock_basic.industry` 白酒真实取值 = 「白酒」(19 只)**;同库另有「酒店餐饮」(9)/「红黄酒」(9)/
  「啤酒」(8)是**不同归类值** —— 故包里写 `["白酒"]` 恰好只命中那 19 只,不殃及其余酒类
  (§五 ① 第 648 行要求先核实的就是这一句,已固化进原语 docstring + 包 notes + 单测)。

---

## 三、块② 交付(行情状态层 D0 盘后三态)

- 新表 `market_regime_daily`(逐日一行,八列照 §五 ② 原文);唯一判定实现 `neckline/scan/regime.py::
  decide_regime()`(纯函数、互斥、顺序即优先级、缺数不猜、`regime` 恒非 NULL、`_EPS=1e-9`);
  读写 `neckline/scan/regime_store.py`。
- 五维输入各自带 `available` / `unavailable_reason` 双位;**每维独立保险丝**(单维炸 →
  该维 `available=false` + `exception:<类名>`,判定照跑,⛔ 不升级为「当日无判定」)。
- 阈值住骨架包 `config.regime`(五键,全 `engineering_v1` + `basis` + `calibration:pending`);
  `pack.py` 加键名白名单 + shape 校验(**键写错闸 1 当场拒**,⛔ 不静默回退)。
- 挂 `report/evening.py` 的 `SEG_SCAN` 段(独立 try/except,失败只 WARNING);CLI 进
  `scripts/scan_layer.py regime`;端点 `GET /api/v1/market-regime`(只读、零现算、**一律 200**、
  空态 `available=false`、**零新增 reason 字符串**)。
- **两道锁(裁定 #7)已落成机器判据**:**前视锁** —— 往库里塞一行 D0 **当天**的验证结果,断言
  `RegimeDayResult` 整体 `==` **逐位不变**;**权限锁** —— AST 断言 `regime.py` 对 `selection.pack`
  只许 import 读入口、全模块零 `activate_pack`,且全模块字面 SQL 必以 `SELECT` 开头、零 `executemany`。
- 另有守门:`regime.py` 读 `position_quota` 但**零写入**(AST)· `sentinel/` 与 `strategy/` **全仓零判定引用**。

---

## 四、施工期裁定登记(plan 未写死、由本批落地的口径)

**orchestrator 裁定(2 条)**

1. **引擎包 `gates.*` / `tier_evidence.*` 的「白名单制」落成 `pack.py::_ENGINE_GATE_SCHEMA` 键名白名单,
   ⛔ 不绑 `PRIMITIVES`**。§五 ① 第 631 行字面写「必须引用已注册原语」,但 `PRIMITIVES` 是 **seeds 域**
   的逐票原语注册表,六关阈值不是 seeds 原语(六关实现在第 ③ 块 `gates.py`);硬塞 = 造一批永不被调用的
   假原语。产品语义零改动,只是把白名单落在正确的注册表上。**第 ③ 块以 `_ENGINE_GATE_SCHEMA` 为键名契约,
   ⛔ 别自创第二套名。**
2. **骨架包 ⛔ 不复刻 K7 的退役键 `tier.quality_lines.tier3_min`**。理由不是「无所谓」:`PROJECT_PLAN` §四
   写着那条 `tier3_min` WARNING「**会一直打到 V2.2 批 2 激活骨架线为止**」—— 骨架包若抄进这个键,
   那条警报**永远不会停**,等于把一条本该终结的过渡态焊死成常态噪音。

**builder 裁定(要点;全文在各模块 docstring)**

- `manifest.line_code` 为线码声明位(缺省 `LEGACY`);交叉校验:`V` → 必须 `seeds`+`tier`、禁 `engine`;
  `C/Z/Y` → 必须 `engine`、禁 `seeds`/`tier`,且 `engine_code` × `line_code` 逐位相等。
  **其他 `config` 顶层键一律不管**(留给 ② 的 `regime`、③ 的 `landing`)。
- `provenance` 形状 = `{value, provenance}` **恰两键**;`audited` 须非空 `ref`,`engineering_v1` 须非空
  `basis` + `calibration:"pending"`;缺失 / 多余键 / 未知 source 一律闸 1 拒。
- `_ACTIVE_PACK_CACHE` 改 `(db_path, line_code)` 双键分桶,失效取整库清;`activate_pack()` 的 prior 查找与
  deactivate 全部 `AND line_code=?`。
- **`status` 本版刻意无切换入口**(⛔ 无 CLI / 无端点 / 无写函数),手工 SQL 是唯一停线方式 ——
  **用户 2026-08-08 裁定「引擎线不必设计得那么完善,要开关就让它默认的放在那里」,⛔ 不是遗漏,
  别"顺手补全"**。读侧尊重该列,是为了将来真需要时不必改读侧。
- **块② 维 4 不调 `report/sector_moneyflow.py`,改为按 `stock_basic.industry` 行业聚合 `moneyflow_dc`**
  (⚠ **与 §五 ② 五维表「数据源」列的字面有出入,如实登记**)。理由:`compute_sector_moneyflow` 是
  **概念板块**口径(按 `sectors.py` 的板块成分聚合),而维 3 用的是 `industry_strength_daily` 的**行业**
  口径 —— 两者 join 正是 `CLAUDE.md` 明令禁止的「行业一对一 vs 概念板块多对多」混用。只复用其
  `MONEYFLOW_COVERAGE_START` 与诚实文案。**orchestrator 复核认可**。
- **`high_divergence` 判据的优先级读法 = 「A 或 (B 且 C)」**(§五 ② 第 716–717 行原文「A 或 B 且 C」
  含糊)。已写进 `decide_regime()` docstring + 正反例单测锁死(仅 B 不判分歧 / 仅 C 不判分歧)。
- **无骨架线现役时阈值回退引擎默认常量**,`skeleton_version` 如实写 `'engine_default'`
  (⛔ 不写 NULL、不伪造版本号),`regime_reason` 记 `missing:skeleton_pack`。**这不是可选项** ——
  §五 ⑦ 批 1 判据要求 `/market-regime` 200 且当日有行,而批 1 期间四个包一个都没激活。
- 维 5 正确率口径(**只留痕、不进首版三态公式**):`verified=1 / partial=0.5 / falsified=0`,
  `unclear` 不计入分母;限 T1/T2 篮;窗口最近 10 个交易日且**只吃结案日 ≤ D0−1**;零样本时
  `available=false`,⛔ **不当「正确率 0」**(P3-51 冷启动缺席已登记)。
- 旧主线 / 新方向的机械识别、5 日中位收益「必须凑满 5 天」、排名一律先确定性 tie-break 再 ordinal、
  核心角色取 `role_mech or role_llm ∈ {leader, core}`、RS 取当日 `pct_chg` 全市场分位 —— 详见
  `regime.py` docstring。

---

## 五、悬案与遗留(⛔ 未决,交下一批处置)

1. 🔴 **§五 ⑦ 批 1 的部署判据自相矛盾(实测证实,待用户裁定)**。批 1 代码一旦上产,
   `get_active_pack()` = `get_active_line("V")`,而生产现役 `K7-pack-v1` 是 `LEGACY` 行 → 实测返 `None`
   → `generate_seeds()` 返 `None` → **当晚零种子零篮子**。而 ⑦ 批 1 判据写的是「**当晚晚间链行为与
   V2.1 逐位相同**」。两句不能同时为真,且**与跑不跑 `retire_legacy_packs.py` 无关** —— 是
   `ENGINE_API_VERSION` bump + 读侧改线本身的直接推论(「批 1 不激活任何新线」与「行为逐位不变」
   在设计上互斥)。**orchestrator 推荐**:批 1 改成「代码 + retire LEGACY + **只激活骨架线 V**」
   (三引擎线仍留批 2)—— 晚间链不空窗,行为变化收敛成可解释的一处(选股域收窄:排科创、排白酒),
   归因仍分得清。**⛔ 未自行改 Plan,等裁定。**
   - 实测复现:
     ```
     库里那一行  : [('K7-pack-v1', 'LEGACY', 'running', 1)]
     get_active_pack()    : None
     get_active_skeleton(): None
     get_active_engines() : {}
     ```
2. **批 1 上产后所有 `market_regime_daily` 行将是 `skeleton_version='engine_default'`** —— 这是回退设计的
   目标态,不是故障;⑦ 激活 `K8-V0.5` 后自动切换,零代码改动。
3. **生产机隔离实测(〇b-5 / P4-50)未做,也不该由本批做** —— `market_regime_daily` 每日约 13 个单日
   分区读 + indexed SQL,开发机 <0.1s/日;但 P0-23 纪律要求上产前在 nk 上 `systemd-run --scope` 计时 +
   量峰值。`landing_state_daily`(第 ③ 块)才是那条真正的靶心。
4. **维 1 / 维 5 在生产初期会「没取到」**(维 1 要前一日 T1/T2 篮子核心成员;维 5 = P3-51 冷启动缺席)
   —— 如实披露,⛔ 不猜。早于 2023-09 的历史回放维 4 恒缺席(`moneyflow_dc` 覆盖边界)。
5. **本批动了注册表 schema + 版本闸 + 激活切换器**(按全局规范属高危区)——**两名 builder 各自
   主动建议叫一次 review**,orchestrator 已上报用户,**未跑**。
6. **客户端零改动**(纯新增端点、零删键);客户端接入归第 ⑥ 块。

---

## 六、一处日期出入(如实登记,⛔ 未自行批量修正)

本批施工与提交的**真实机器时间 = 2026-08-08**(立项三笔 commit 的时间戳同样是 `2026-08-08 19:28 / 20:05 /
20:16`)。但 `PROJECT_PLAN.md` §四 / §五 / §九 里 V2.2.0 立项那批条目、以及策略档案迁出那条,**文本写的是
2026-08-09**(`CLAUDE.md` 对同一件迁出事件写的却是 2026-08-08,两处本就打架)。

→ 本文与 §九 本批那一行**按真实日期 2026-08-08 记**;**⛔ 未去批量改 planner 写下的 08-09**
(全文级改动,须用户拍板)。查证历史时以 **git commit 时间戳**为准。
