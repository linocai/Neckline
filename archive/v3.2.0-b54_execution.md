# 3.2.0 / 双端 Build 54 · K10-v2 执行契约

**状态：已规划，已授权本地实施，未授权发布。** 本记录是 `PROJECT_PLAN.md` 的唯一当前版本记录；施工证据、偏离、提交和交接持续写在这里。现行策略以 [`/Users/linotsai/Lino/whynotme/K10.md`](/Users/linotsai/Lino/whynotme/K10.md) 为准，本记录只把它落为生产契约。

## 1. 基线、范围与硬边界

| 项目 | 已核事实／决定 |
| --- | --- |
| Git 基线 | `main` 的 `71d0e1a5d805cbf932300c1f78899168a8af793b`；B53 发布标签 `v3.1.0-b53` 指向 `76c0a9f`。现有未提交 `AGENTS.md`、`PROJECT_PLAN.md`、`README.md`、`archive/v3.1.0-b39_execution.md` 是停机诊断记录，保留。 |
| 目标 | 3.2.0、双端 Build 54、Schema 8、K10-v2；本地实现、构建、隔离库和共享原生 QA。 |
| 生产 | B53 已安装；run control closed，API／worker／全部 timer inactive+disabled。保留 9 月 8 日晚报及其两样本、9 月 9 日晨报 429／402 失败检查点，绝不重放或恢复退役 2,472 篇事故。 |
| 本轮禁止 | 真实 DeepSeek／Tavily／TuShare 调用，余额或权限探测，真实报告／任务重放，真实推送，生产部署、服务恢复、排程启用、生产数据库迁移或写入。 |
| 不做 | 交易计划、买卖价格、持有／退出、仓位、收益结算；新研究 Agent、全市场尽调、题材／涨幅／正文数量门槛、80／40 的活跃生产／校验／测试契约、自动裁判。 |

没有用户网页操作项。本轮没有需要网页开通或操作的外部事项。

## 2. 输入快照与本地资料导入

| 输入 | 固定值 |
| --- | --- |
| 策略 | `K10-v2`；`K10.md` SHA-256 `44817dfdb8f7815568909b4bf0bf8b6ba3054815be2a50d8682c7b4d8602dd3d` |
| 股票池 | `/Users/linotsai/Lino/whynotme/research/K10-v2初始股票池_20260909.json`，1,089 条，SHA-256 `161c1dbbf7119a3ce5bf9d6aa183635eb0af05eee7b77d9e962ede5f54eb3b48` |
| 企业档案 | `/Users/linotsai/Lino/whynotme/artifacts/output/k10-company-profiles-v2-20260909/profiles.jsonl`，1,089 行，SHA-256 `5bacdc5b2da3783a0c131d219d71bb6faf99d2cad01cc631f9a47e2587d21782` |
| 筛选索引 | 同目录 `screening_index.jsonl`，1,089 行，SHA-256 `c2ddfa747ec80344abfc16e6f018336d89f106ec88afb121ef6e9ba33a758d10` |
| 资料语义 | 完整档案含身份、主营、关系、匹配词、收入结构、市场、产业链／依赖、变化、证据与来源；索引只含召回字段。三份输入代码集合完全相等且无重复。 |

新增一个显式的本地导入命令，输入为 `--universe-file`、`--profiles-dir`、`--db`、`--confirmed-target` 和固定快照标识；名单与档案目录分别传入，绝不根据单一目录猜测研究仓布局。不向 `Backend/data/` 提交资料，也不在运行时保留对研究目录的路径依赖。导入前须验证文件哈希、JSON 结构、每个输入 1,089 条、三集合相等、代码／名称一致以及档案引用的原始证据文件可读；任一失败不写目标。成功时在一个事务中写入：

- `k10_v2_universe_snapshots` 与 `k10_v2_universe_members`：快照 ID、策略、来源哈希、截至日期、代码和名称；固定 ID 为 `k10-v2-initial-20260909`。
- `k10_v2_profile_snapshots`、`k10_v2_company_profiles` 与 `k10_v2_company_profile_evidence`：完整档案 JSON、筛选索引 JSON、每公司原始 `source_evidence` JSON、哈希、导入时间和来源状态；固定 ID 为 `k10-v2-profiles-20260909`。`profile_file`／`raw_evidence_file` 只作原始来源元数据，不得成为运行时路径。

资料内容、`review_status=local_draft_awaiting_user`、报告期、未知日期、来源引文、原始证据和缺项按输入保留。导入不是官方核验、批准或上云；运行时只从工程侧数据库读取。相同快照／相同哈希重复执行幂等；同 ID 不同哈希和跨集合不一致拒绝，绝不静默覆盖。测试只导入临时数据库；任何生产目标的导入或 Schema 切换留待新授权。

## 3. Schema 8 与历史保护

Schema 8 为只增不改的 v2 命名空间，保留 Schema 7 所有表和数据。新增：

1. `k10_v2_strategy_snapshots` 冻结策略文本哈希、池／资料快照 ID 与哈希、策略和执行配置 ID／修订、导入时间；任务和报告必须绑定此 ID，缺失或校验失败即 `not_configured`，没有默认值。
2. `k10_v2_report_runs` 表示一个晚／晨扫描报告（窗口、消息／查证／可查看截止、晚晨迟到标记、父晚报和状态）；`k10_v2_report_cards` 每报告每公司一条，保存排序、卡片事实／不确定性／来源 DTO 和关联公司窗口。晚报不超过 30；晨报新增不设业务限额，但 API 分页返回。
3. `k10_v2_card_catalysts` 把一张公司卡关联到一个或多个事件修订及可选机会；同公司多催化的理由、阶段和来源独立可追溯，不以标题数提升排序。

既有 `k10_events`／事件修订、原文、任务、外部尝试、机会、公司窗口、选择快照、行情和评价账本继续作为通用证据与固定 D1/D2 账本。v2 卡首次正式发布时，在同一事务内建立或复用其公司窗口和机会：

- 重复／转载／普通进展／旧催化再次显示：关联旧机会和旧窗口，只写本次卡及证据更新，绝不重开样本。
- 同一公司同一首发批次／D1-D2 的多催化：多条 `card_catalyst` 共享一个窗口与一份成绩。
- 实质新阶段或独立新催化：建立关联旧机会的新机会；若与该公司任一已发布窗口重叠，标为 `overlap`，按真实 D1/D2 完成但不计主命中率；不重叠才进入主样本。
- 一张今日卡同时关联旧机会和真正新催化时，卡 DTO 必须给出可操作的目标 `companyWindowId`。新机会总以 `unhandled` 开始，不继承旧窗口的 selected／skipped／unhandled；针对新机会的操作只写该窗口，旧窗口的 D1 冻结组和历史结果不可修改。

旧 v1.4 的已发布推荐、选择冻结和证据必须以明确 `K10-v1.4` 名称保留，不能被 v2 卡、选择或结果重解释。原 D1/D2 的窗口与选择不重置；未来获恢复授权后，既有行情事实与评价账本可追加修订以完成旧样本。删除 80／40 的活跃生产配置、校验器和测试契约，不为旧测试保留可运行兼容路径，历史数据中的旧参数照原样保留。生产回滚仍以已验证的 Schema 7 备份恢复，不能以便利 DDL 丢弃 v2 帐本；隔离迁移测试要证明升级失败时源库字节不变。

## 4. K10-v2 离线运行契约

### 标题、资料与事件

- 每个来源窗口的精确去重标题都进入短批处理。模型输入标题必要字段和按需召回的本地索引，输出事件候选、可能相关池内公司、正文需要与理由；代码、公司名、关键词、新闻分类、题材、涨幅、池外主体或标题长度都不能作程序硬删条件。
- 索引没匹配也必须由模型给出“无合理池内关联”后才跳过；索引命中只是一条线索，完整资料与消息原文才支撑公司关联。不得把 1,089 份完整档案或全文历史塞入每个请求。
- 跨批合并同一事件，优先阅读最完整原文；多公司共享事实与补查结果，各公司影响分开比较。正文、搜索摘录和完整网页仅在事件事实或公司映射的具体缺口需要时取得；没有 80／40、全文、搜索次数、来源数、文章数或 token 配额。
- 有效路径不足时保留未知并结束；正文缺失、模型协议、来源或比较失败是未完成状态，不能报告为“没有机会”。新证据只影响随后卡片／修订，不回填旧推荐。

### 比较、日推荐与晨间衔接

- 研究对象限定在固定池内有合理关联的公司；不把一条行业／海外／宏观线索扩张为全市场尽调。系统按事件完成比较后，合并同公司所有催化并重新选择当天公司；晚报最多 30 张不同公司卡，不凑数。
- 卡片必须包含：事情、公司关联、两日关注理由、当前价格反应（如已有）、未核实或分歧、来源／发布时间／取得与查证截止、事件关联和显式策略版本。不得输出未经校准的涨停概率，也不得以价格上涨当事实确认或新催化。
- `unverified` 是卡片披露，不是 `pending` 自动门槛。删除 `ready_for_comparison` 对发布资格的错误绑定；比较输出必须表达支持、反对、未知和失败的真实状态。核心理由被已核事实推翻时撤回该理由，其他独立理由照常判断，已发布窗口不断。
- 晨报只处理 `[D0 21:00,D1 09:00]` 增量：已有晚报公司的重要反证、事实／论点变化、到期或缺口写为更新；真正新增公司单列“晨间新增”。同公司不会出现第二张可选择卡，也不会替换、清空或重新冻结用户已有选择。晨间新增以稳定顺序分页，不设额外淘汰或正式推荐数量上限；首次可查看时间决定同日或下一交易日 D1，并保留 `morning`／`late` 标记。

### 用户选择、辩论、成绩

- 每张正式卡给出明确的目标公司窗口；选择 API 仍以窗口为原子单位。持久化冻结枚举为 `selected`、`skipped`、`unhandled`；卡 DTO 的 `currentSelectionState` 固定映射为 `kept`、`skipped`、`unhandled`（`selected → kept`），不得混用两套名称。D1 开盘前最后一次明确操作冻结，开盘后操作只作记录。同卡多催化共用选择；真正新机会从未处理开始，不能继承、覆盖或回改旧机会选择。
- 用户留下后才入队正方，再以同一资料截止读取正方全文执行反方；各一轮。任一方失败明确显示，不能删卡、改选择或重开窗口。没有裁判、额外夸大关卡或循环辩论。
- 保持现有 D1/D2、主指标、缺数／停牌／异常、重叠、价格观察和三选择组语义。结果页面按策略版本和窗口分组；总组与子组不相加，重叠结果单列。

## 5. API、客户端与页面契约

新增 v2 报告读模型，不用 `companyWindows + unhandled` 作为首页数据源：

| 契约 | 必含语义 |
| --- | --- |
| `GET /api/v1/k10/v2/reports/latest?window=evening|morning` 与报告详情／列表 | `reportId`、窗口、父晚报、消息／查证／可查看截止、晚晨迟到标记、状态、卡片列表和空态原因；晨报更新与新增分段，新增段为稳定分页。 |
| 卡 DTO | `cardId`、公司代码／名称、排序、可操作 `companyWindowId`、多催化、事件／机会关联、卡片理由、事实／推断／未核实披露、来源、价格观察、窗口与选择状态；`currentSelectionState` 仅为 `kept/skipped/unhandled`，并明确对应持久化的 `selected/skipped/unhandled`。混合卡同时带旧／新机会映射，动作目标永远明确。 |
| 既有窗口选择／分析接口 | 接受卡的 `companyWindowId`，保持幂等选择、固定冻结、正反分析链和历史窗口；不可因新日报卡改变其 D1/D2。 |
| `GET /api/v1/k10/configuration` 与 readiness | 返回运行时显式绑定的策略和执行 config ID／修订、K10-v2、池／资料快照 ID 与哈希、资料来源状态、每 scope 的 configured/not_configured 原因和 run control。任何绑定、引用快照或参数缺失时返回 `not_configured`，不从最新 scan／数据库行推断。 |

FastAPI schema、store 投影、Swift `Codable` 模型必须从同一隔离数据库产生的真实 DTO 验证。客户端改为：机会页先显示最新晚报和晨报分段卡，既有卡更新不重复，晨间新增可分页；选择入口与固定 D1/D2 清晰可见；详情显示多催化、未核实／失败、来源和 `K10-v2`；关注页沿用留下后正→反；表现页保留主／重叠和三组。历史 v1.4 明确标名，不显示裸 `vN`。

视觉延续白卡、蓝色强调、克制字级和明确导航。填充与空态均对照 `archive/Neckline_V3_界面参考/01-opportunities.png`、`03-morning-plan.png`、`04-results-settings.png`；策略行为优先于参考中的旧功能。

## 6. 可靠性、配置与错误语义

- 新的策略／执行配置显式包含 K10-v2、策略快照、资料快照、标题批处理、并发、模型单次选项、任务超时和重试策略。不存在配置或任一必填项无效时显示“今天没跑成 · 参数未配置”，不列推荐，不回退 v1.4 或样例值。
- 将 402 归类 `insufficient_balance`：记录脱敏状态、保留当前检查点、终止该受影响任务和下游依赖，不再把它当网络错误排队重试。将 429 归类 `rate_limited`：尊重可解析的 `Retry-After`，否则使用冻结配置的有限退避；只调度受影响步骤，成功后从检查点继续。其他协议、来源和客户端读失败各自保留原因，不伪装为零结果。
- 任务、报告、卡、机会、窗口和 usage 记录写入必须具有幂等键及中断边界；恢复同一输入不得新增第二张卡、机会、样本、选择冻结或重复计量。保留阶段输入大小、重复资料输入、缓存命中和合成 usage，但不得称为实际 token 节省或日成本。

## 7. 实施顺序与验收

1. **先建立离线护栏与 v2 快照导入测试。** 在测试基础设施中默认拒绝非 localhost 网络连接，确定性 provider／search／market transport 不经网络；导入固定输入至临时库，断言原子性、幂等、哈希／1,089 集合／草稿状态保护和 Schema 7 历史不变。
2. **实现 Schema 8、配置／readiness 和 v2 store。** 测试无默认、无旧 scan 回退、快照绑定、迁移失败回滚、GET 不 DDL，以及原生产目标未被本地命令触碰。
3. **替换离线发现与研究路径。** 用冻结输入覆盖全标题审计、跨批事件归并、名称未命中但正确关联、索引命中但不成立、无全文额度、按事件复用、未核可推荐、撤回、同公司多催化、日重选、晨间更新／新增／迟到、机会重用、混合卡选择隔离与重叠。
4. **实现 API 与 Swift 读模型。** 用真实 FastAPI 响应喂当前 Swift 模型，覆盖晚报、晨报更新／新增分页、撤回、缺数、分析修订、空态和配置未就绪；选择→正方→反方仅在选择后入队且顺序正确。
5. **固化失败恢复并完成原生 QA。** 用确定性 402／429、来源失败、断点恢复和客户端读取失败验证不重复；复用 `/tmp/neckline-v3-qa/macos`、`/tmp/neckline-v3-qa/ios` 与“主模拟器”，每平台单实例，测试 bundle 显式设置并核验 `NK_BUNDLE_SUFFIX`。比较实际填充／空态截图后才通过视觉验收。
6. **收口。** 后端 `pytest -q`，以及 macOS build、iOS Simulator build、iOS build-for-testing 三条 `xcodebuild` 全部通过；记录实际结果、已知真实供应商未覆盖项和恢复第一步。任何 `.swift` 改动不得省略三条构建。

验收不包含真实供应商可用性、真实 token 用量、选股效果、付费余额、推送送达、发布或生产状态变化。测试网络拒绝必须覆盖，不得把“余额为零”当隔离机制。

## 8. 风险、回滚与交接

最大风险是固定 1,089 池带来的批处理和事件归并负荷，使离线正确性被误当作真实供应商效果。通过短标题批、索引按需召回、事件共享、局部检查点、用量分阶段记录和严格离线验收控制工程风险；不承诺 350–750 万或任何实际 token 总量。

本地失败时删除临时数据库／QA bundle 并恢复 Git 可追溯状态；不得触碰 B53。未来获准生产切换前，必须重新核对目标、停止写者、创建并核验可恢复备份、在副本演练 Schema 8、验证 owner/group/mode 和 readiness DTO；出错只从该备份恢复。恢复 API、worker、timer、真实调用或部署均须另获授权。

**Builder 第一步：** 先为测试建立外网拒绝护栏，并把上述三份固定输入导入临时 SQLite，写出哈希／1,089 集合／草稿状态／幂等与失败不写入回归；随后才改 Schema 或运行链。

## 9. 实施与离线验证证据

### 范围与交接

Builder 已按开工读取的 `gpt-6-astra / medium` 配置施工，负责 Backend、主 Plan、本版本记录和 README；主会话负责全部 App 模型／界面／测试／版本／原生构建及 QA。直接在 main，HEAD 仍为开工基线 `71d0e1a5d805cbf932300c1f78899168a8af793b`，没有提交、推送、tag、部署或生产写入。本轮审查应包含该基线后的暂存／未暂存／未跟踪改动；开工已有 AGENTS.md、PROJECT_PLAN.md、README.md、archive/v3.1.0-b39_execution.md 及本记录，保留其中用户停机记录。App 与本记录都属于累计未提交范围，不能把 Git tracked diff 单独当全部施工范围。

### 已实现的关键边界

- Schema 8 以新增表承载固定池／公司档案／原始证据／策略快照、日报卡／催化及标题公司线索／阶段输入计量。显式双输入导入先验证四份输入哈希、1089 集合及引用完整性，再在单个事务写入；重复导入验证已存完整性，不把导入当批准。运行时仅读工程侧 SQLite。Schema 7 历史表和冻结记录原样保留，新的 title/admission 写入不再受历史 80/40 CHECK 约束；旧表 CHECK 仅用于保护原历史数据，不参与活跃运行。
- `k10-execution-v4.json` 删除全文名额；标题协议以本轮冻结输入数作完整性校验，不能把它当文章配额。全标题批量审计、跨批归并及终检仍有覆盖性校验；全部原文准入与补读按真实证据缺口发生。旧未核实自动阻断与配额测试已删除或改为相应新合同。
- 标题公司代码保留为不可覆盖线索；读原文和研究问题规划前提供固定池及本地资料。召回支持产品、子公司、合作关系和依赖词，不限于公司名称；`亚马逊云科技` 无公司名称输入的回归可召回神州泰岳相关关系。按当前事件／问题投影相关业务、关系、收入／区域／产业链字段，保留来源、缺失字段和草稿状态，不再把完整证据与笔记反复送进比较。池外公司问题／映射在校验处拒绝，恢复路径在真正搜索前再次检查；池外主体可保留证据背景，不能展开公司尽调。真实模型的语义映射质量未在本轮验证。
- 日报卡按公司分组，晚间 30 distinct，晨间更新与新增分开、新增分页不另设业务数量上限。旧催化重复展示复用原机会；混合旧／新卡选择新窗口，新窗口初始 unhandled，旧 selected 冻结组和 D1/D2 不变。`selected` 仅在 API 投影为 `kept`。机会／卡原子写入中断回归证明新批次一起回滚，历史批次保留。
- 新 API 包括日报 latest／历史列表／指定报告详情、显式快照 readiness、策略版本分组成绩及基于绑定池快照的公司名。报告失败返回实际脱敏原因，不伪装零候选；读取不迁移、不写库。真实配置必须显式绑定，历史 scan 不能替代运行配置。
- 402 首步与研究后段均进入终态，停止模型／搜索下游并保留失败检查点；后段错误不能被事件隔离误报为 partial 成功。429 由冻结执行策略与可解析 Retry-After 调度，供应商内部不继续抢跑，恢复只执行受影响步骤；回归证明标题、正文和 plan_gaps 各只成功一次。
- 测试默认拒绝外部 DNS、socket 连接及数据发送；Python 子进程继承 `sitecustomize`，直接网络工具和 shell 绕过被拒绝。阶段持久化输入字符数、资料字符数／份数及 SHA，实际 token 仍按 provider usage 记录；合成统计不是实测节省，也未添加日总 token 硬预算。

### 后端与跨模块验证

- 固定本地输入导入、真实 CLI enqueue→worker→handler、402/429 首步与后段、池外问题搜索前拦截、混合卡选择冻结、原子中断、配置及只读 API、网络子进程护栏均有临时库确定性回归。
- 最终后端全量 `.venv/bin/python -m pytest -q`：**989 passed / 21 个已有 Polars sortedness warnings / 34.81s**，日志 `/tmp/nk-v2-build54-final.log`。最终代码包含后段 402/429 回归；`git diff --check` 通过。前次通过后的新增 API／标题回归分别为 36 passed、45 passed，最终全量已统一覆盖。
- DTO 由 `tests/k10_v320_fixture.py` 创建真实 store 记录，经 FastAPI TestClient 导出；最终临时库 `/tmp/neckline-v320-final-fixture.sqlite`，输出 `/tmp/neckline-v320-dto/` 的 evening、morning、configuration、results、windows、detail、historical_results 七份 JSON。health、settings、settings/providers、usage/summary 均调用真实生产只读函数并明确绑定临时库，HTTP 200 且数据库字节不变。fixture 不修改生产模块的全局数据库绑定。
- 主会话最终 XCTest 共 43 项：**38 通过、5 个历史专用服务测试跳过、0 失败**。本轮七份真实 DTO 的 v320 测试实际执行通过，含混合窗口选择隔离、overlap、按策略分组成绩、草稿状态和公司名。不能写成 43 passed，也不能把历史专测跳过说成本轮 DTO 未执行。
- 最新 macOS build、iOS Simulator build、iOS build-for-testing 三条必跑 xcodebuild 均通过。日志 `/tmp/neckline-v3-qa/mac-b54-build.log`、`ios-b54-build.log`、`ios-b54-testbuild.log`、`ios-b54-tests.log`；xcresult 为 `/tmp/neckline-v3-qa/ios/b54-final.xcresult`。

### 原生 QA 与清理

主会话先记录模拟器 app inventory，显式传入 NK_BUNDLE_SUFFIX，并核验 Info.plist 与 xctestrun 的 host ID 为 `top.linotsai.neckline.qa`。只使用共享“主模拟器”（211DD03C-812D-4A42-97EF-F693D7DF924C）、固定两套 QA 路径和临时凭据。DEBUG QA 入口只接受 loopback，要求 NK_DISABLE_PERSISTENT_CREDENTIALS=1，离线模式拒绝外部 URL 与重定向。两端实际 localhost API 填充与 not_configured 空态已对照批准参考图，保持白卡、蓝色、克制层级和明确导航。

七张实际截图：

- `archive/v3.2.0-b54-evidence/macos-opportunities.png`
- `archive/v3.2.0-b54-evidence/macos-morning.png`
- `archive/v3.2.0-b54-evidence/macos-performance.png`
- `archive/v3.2.0-b54-evidence/macos-empty.png`
- `archive/v3.2.0-b54-evidence/ios-opportunities.png`
- `archive/v3.2.0-b54-evidence/ios-morning.png`
- `archive/v3.2.0-b54-evidence/ios-empty.png`

QA 已清理：8765／8766 本地服务停机，无 macOS QA 进程；本轮模拟器 QA app 卸载，主模拟器恢复 Shutdown；两套固定 build 路径各保留一份现行产品。`/Applications/Neckline.app` 仍为 3.1.0(53)，未替换或启动生产客户端。

### 限制与下一步

无产品范围偏离，无真实 DeepSeek／Tavily／TuShare／APNs、余额或权限探测、旧报告重放、生产数据库／服务操作。真实供应商可用性、模型关联与选股效果、实际 token、搜索覆盖率、APNs 送达均未验证。公司资料仍是用户待审初稿；离线结果不能当资料批准或收益证据。Schema 8 生产切换只能在未来另获授权后核验目标、备份并演练，失败时恢复已核验备份，不提供无备份的自动降级。用户未要求 review，本轮没有调用 reviewer；后续可按上述累计基线范围独立审查，再另行决定发布或首次真实运行。

## 10. 独立代码与 SOP 审查（2026-09-09）

用户随后明确要求独立审查代码及完整 SOP。Reviewer 使用当前 `gpt-6-astra / max`，未参与施工，直接对照 K10-v2 原文及实际入口。**结论：4 项 P1、1 项 P2，完整 SOP 存在发布阻断；未发现 P0。五项均未修复，不沿用施工测试通过作为可发布结论。**

审查从实际发布 `76c0a9f97937065ed17a5e6f64f4ee8829bbb756` 覆盖至 `71d0e1a5d805cbf932300c1f78899168a8af793b` 加全部暂存／未暂存／非忽略未跟踪文件。冻结 353 个路径，结束时 HEAD、全部文件与 K10 原文哈希均未变化。清单为 [target-manifest.json](v3.2.0-b54-evidence/review/target-manifest.json)，SHA256 `33ad385119390841aecef5e5571b0f8545b4e3f213e20e8140db510ed0125b80`；本节、主 Plan 状态和复现附件在审查结束后追加，业务代码未修改。

| 编号 | 严重度与确定性影响 | 定位（审查时行号） | 最小修复方向／原始反例 |
|---|---|---|---|
| R1 | P1：真实 API 留下成功且配置页四项 configured，但 analysis 任务无执行绑定，worker 返回 not_configured、正反零调用；晨间子复核同根因 | Backend/neckline/k10/store.py:1257、worker.py:235、pipeline.py:1915 | 创建付费子任务时原子冻结绑定；现有 test_k10_end_to_end.py:69 手动绑定绕过入口。`neckline-b54-review-selection.py` |
| R2 | P1：本轮 role=excluded、rank=null 的旧公司因 continuation 仍进入正式日报并占名额 | Backend/neckline/k10/discovery.py:1147、pipeline.py:1790；K10.md:97 | 每日推荐校验本轮角色，机会延续只决定复用窗口。`neckline-b54-review-repros.py` |
| R3 | P1：原始晨间资讯为明确否认，contradicted 被统一降成 needs_review，invalidated 变 risk，旧机会保持 active | Backend/neckline/k10/research_runtime.py:504、discovery.py:1115；K10.md:74、126 | 保留公司／催化层面已核反证，分别处理未知与已证伪。`neckline-b54-review-contradicted.py` |
| R4 | P1：独立变体后端已写 withdrawal／withdrawn，但新晨报 completed 且 updatedCards/addedCards 都空，App 保留旧晚卡 | Backend/neckline/k10/pipeline.py:1786；App/Neckline/App/AppModel.swift:237、Views/OpportunitiesView.swift:109；K10.md:41 | 生命周期变化进入日报与客户端投影，不重开窗口或重置选择。`neckline-b54-review-withdrawal.py` 的 DENIAL |
| R5 | P2：首次查询路径为空时继承空历史映射，不执行 close_research／compare_companies，直接 completed 零卡 | Backend/neckline/k10/research_runtime.py:578、431；K10.md:70、74、92 | 无路径仍用已有事实完成收口与关联判断，再决定比较或放弃。`neckline-b54-review-withdrawal.py` 的 NO_PATHS |

上述原始脚本已保存在 [review/](v3.2.0-b54-evidence/review/)；`neckline-b54-review-more.py` 保留早期组合反例。它们使用确定性 transport 和自己的临时 SQLite。复现从 Backend 目录运行：`PYTHONPATH="$PWD/tests/offline_guard:$PWD" PYTHONDONTWRITEBYTECODE=1 PYTHON_DOTENV_DISABLED=1 .venv/bin/python ../archive/v3.2.0-b54-evidence/review/neckline-b54-review-selection.py`，其余替换脚本名。未来修复须保留原触发条件并转为回归，不以手动补绑定或改弱夹具绕过真实入口。

另以同一外网拒绝环境执行 `pytest -q -p no:cacheprovider`，覆盖 `test_k10_v2_pipeline.py`、`test_k10_v2_profiles.py`、`test_k10_lifecycle.py`、`test_k10_migration.py`、`test_k10_execution.py`、`test_k10_evaluation.py`、`test_k10_evaluation_schedule.py`：**69 passed**。固定 D1/D2、三组冻结、重叠归属、缺数补正、事实修订、迁移回滚及已覆盖恢复路径未发现新增可报告问题；`git diff --check` 通过。

本次未运行真实供应商、生产迁移／服务、App／模拟器或重复原生构建；不证明真实模型质量、每日费用或真实调用成功。当前仅交付 findings，未获本轮修复指令，生产继续保持暂停。

## 11. 五项修复与复审交接（2026-09-09）

用户随后授权立即全部修复、修完直接独立复审并汇报。沿用未发布 3.2.0（54），没有新版本或发布操作。Builder 仍为 `gpt-6-astra / medium`、仅负责 Backend；主会话负责 App 与记录，复审交原独立 Reviewer `gpt-6-astra / max`。

修复前快照 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b54-repair-baseline-0uxu5hkb/` 包含 359 路径清单及只读 App/Backend 原文件；manifest SHA256 `e02b3c467358bddfb532096e038064a921a9f373801ff609f7337ec1b8208344`。与第 10 节审查目标的 App/Backend 哈希完全相同。当前 HEAD 仍为 `71d0e1a`，累计未提交改动保留。

- R1：创建留下、追问、晨间子任务及 CLI 付费任务时，原子绑定明确执行配置；缺配置／批准状态时拒绝，不从任意最新修订补默认。真实入口完成正反两次确定性调用、子任务继承及绑定中断回滚已测。
- R2：新催化与 continuation 都按本轮推荐角色筛选，excluded/pending 不作为正式日报卡；更新证据与重用机会仍保留。
- R3：按公司保留核验结果，已核反证进入撤回路径；同事件另一公司仍未知时不被一起撤回。
- R4：日报新增 `lifecycleUpdates`；催化新增 `lifecycleState`，公司卡新增 `canSelect`。风险／撤回／到期单独显示，含无今日卡的旧关注；当前状态持续投影，混合其他有效催化保留，窗口／选择／样本不改。App 独立优先展示机会变化，关闭目标禁止 keep/restore，底部明确标注当前公司与新机会，避免将消息提示误认作操作对象。
- R5：无有效查询路径仍调用收口，使用已有事实判断关联再比较或明确放弃，不强推未知或静默跳过比较。

修复前原始 6 个入口反例（R2 覆盖 excluded/pending）失败，日志 `/tmp/nk-b54-review-before.log`；最终后端 **1005 passed / 21 个既有 Polars warnings / 51.53s**，日志 `/tmp/nk-b54-repair-final.log`，其中 15 项覆盖原反例及原子中断、追问、晨间子任务、混合公司、无强推等影响面。`git diff --check` 通过。

当前 10 份实际 API 响应位于 `/tmp/neckline-v320-dto/`：原 7 份由最新代码及完整批准记录重新生成，另有 `repair_evening.json`、`repair_morning.json`、`repair_windows.json`；临时库分别为 `/tmp/neckline-b54-base-verified.sqlite`、`/tmp/neckline-b54-repair-verified.sqlite`。Swift 实际解码与投影测试最终 **46 项：41 通过、5 个历史专测跳过、0 失败**，日志 `/tmp/neckline-v3-qa/ios-b54-repair-verified-tests.log`，结果 `/tmp/neckline-v3-qa/ios/b54-repair-tests.xcresult`。覆盖无当天卡的变化、去重、混合目标可选／旧催化撤回、关闭目标不调用选择 API、原 D1/D2 保留。

最新 macOS build、iOS Simulator build、iOS build-for-testing 全通过，日志为 `/tmp/neckline-v3-qa/mac-b54-repair-build.log`、`ios-b54-repair-build.log`、`ios-b54-repair-testbuild.log`。所有 QA 构建明确 `.qa` suffix 并核验 bundle/xctestrun host；使用原共享模拟器和固定两套目录。实际双端晚／晨截图存于 [repair/](v3.2.0-b54-evidence/repair/)，已对照批准参考图核对撤回原因、来源时间、窗口、明确操作对象与白卡蓝色布局。

本轮 QA 服务与 App 退出，临时模拟器安装移除，主模拟器恢复 Shutdown；生产 `/Applications/Neckline.app` 仍 3.1.0（53）。无真实供应商调用、生产变更、旧任务重放、commit/push 或发布。代码现停止修改，等待独立复审结果；本节施工验证不代替独立结论。

## 12. 第一轮独立复审与异常边界补修（2026-09-09）

原 Reviewer 独立验证 56 项相关测试及额外不利场景，确认 R2/R3/R5 可关闭，R1/R4 尚有 4 项 P2：缺执行绑定时留下仍成功且恢复配置后旧任务无法恢复；晨间子复核失败未投影至 v2 日报；生命周期已提交、checkpoint 前中断重试重复模型并发生时间戳冲突；历史找回入口允许恢复关闭窗口。未发现新 P0/P1，不能据首轮门禁宣布闭环。原复现脚本保存在 [review/](v3.2.0-b54-evidence/review/) 的 `neckline-b54-rereview-boundaries.py` 与 `neckline-b54-rereview-closed-restore.py`。

首轮修复冻结清单 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b54-repair-review-964oo76n/target-manifest.json`，SHA256 `d8e97a131c2c560848e3eb810beabcd36ed1379a97cef6f15636c756826b621b`，365 路径在复审结束后逐项复核未变，K10 来源未变。第二轮源码快照 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b54-boundary-baseline-jdgjpui9/files`；按原授权继续修复，再交独立复审。

第二轮实现将缺绑定校验放在真实操作入口，恢复旧无绑定任务时采用明确运行绑定；晨间结果先持久保存输入哈希、模型结果和原时间，再完成生命周期及任务 checkpoint，重试复用原结果；晨间子任务状态持久投影到日报 `status/reason/coverageGaps/incompleteReviews`，恢复成功清除缺口，成功内容保留。日报卡、历史窗口和 keep/restore 接口使用同一窗口级可选判断，关闭旧窗口不影响同公司独立新窗口。

原四个不利场景先固化为 6 个真实 API/worker 回归并全部失败，之后补失败恢复、同公司窗口隔离及到期入口断言；原始脚本与业务断言保持。旧测试仅补明确批准执行档、真实临时 schema 和受控迁移前态，不放宽生产缺配置拒绝，也不跳过持久化。

实际 DTO 共 12 份 `/tmp/neckline-v320-dto/`：前 10 份按当前 schema 重生成，增加 `boundary_morning_failure.json` 和 `boundary_windows.json`。原生 XCTest **49 项：44 通过／5 个历史专测跳过／0 失败**，日志 `/tmp/neckline-v3-qa/ios-b54-boundary-tests.log`，结果沿用固定 `/tmp/neckline-v3-qa/ios/b54-repair-tests.xcresult`。覆盖历史关闭入口无调用、同公司新窗口可操作、实际晨间失败对象与成功卡保留。最新三构建通过，日志为固定 QA 根目录下 `mac-b54-boundary-build.log`、`ios-b54-boundary-build.log`、`ios-b54-boundary-testbuild.log`。

实际临时库分别为 `/tmp/neckline-b54-boundary-base.sqlite`、`/tmp/neckline-b54-boundary-repair.sqlite`、`/tmp/neckline-b54-boundary-verified/b39-e2e.sqlite` 和同目录 `history.sqlite`；失败库本地只读 API 使用明确 b39/b39-execution 绑定。双端实际失败晨报截图见 [repair/](v3.2.0-b54-evidence/repair/) 的 `macos-morning-incomplete.png`、`ios-morning-incomplete.png`，已确认白卡蓝色设计、部分完成状态、具体公司和原因可见、晨间成功更新仍可阅读。

最终后端全量 **1013 passed / 21 条既有 Polars sortedness warnings / 56.90s**，日志 `/tmp/nk-b54-boundary-final-green.log`；`git diff --check` 通过。QA 本地服务及客户端停止、主模拟器临时安装移除并恢复 Shutdown，生产 Mac 仍 3.1.0（53）。本轮保持无真实外呼、无生产操作、无提交与发布。第二轮代码现冻结，交原 Reviewer 独立复审。

## 13. 第二轮复审与生命周期投影收口（2026-09-09）

第二轮独立 Reviewer 运行 74 项相关测试全部通过，确认第 12 节四项 P2 已关闭，原 R2/R3/R5 关闭结论保留；新增两项 P2：普通有效 `evidence_update` 的历史窗口被误算为不可选；已有风险经过普通更新后，日报只取最后更新而错误清除风险，历史接口则仍保留风险。无新增 P0/P1。原反例保存在 [review/](v3.2.0-b54-evidence/review/) 的 `neckline-b54-second-review-active-update.py` 与 `neckline-b54-second-review-risk-continuation.py`，均通过真实 CLI/worker 和确定性 transport 复现。

已审清单 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b54-boundary-review-261dcbzw/target-manifest.json`，SHA256 `b90a0ff6e648a120b8a6ce871bfa7ac6e4cdba4d1ed5d0522fd73533d4c649aa`；Reviewer 结束后 371 路径及 K10 哈希全部复核未变。继续修复前的源码快照位于 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b54-selection-baseline-4bvjku7y/files`。两项均收口到共享生命周期聚合，普通更新不能清除未解除风险，已核实当前理由与完整来源才能解除风险，撤回／到期优先，未知不能默认可选；沿用原修复授权，不修改版本或恢复生产。

两条原 CLI/worker 反例在修复前均失败，修复后通过，补充已核解除、终态和未知状态负例。最终 Backend **1017 passed / 21 条既有 Polars warnings / 79.27s**，日志 `/tmp/nk-b54-lifecycle-full.log`；`git diff --check` 通过。相对本节快照仅 5 个 Backend 文件变化：共享 store 聚合、两处消费者和两份测试/fixture；App 源码哈希逐项确认未变，沿用第 12 节三构建。

12 份实际 DTO 由当前代码及全新临时库重新生成；repair 增加风险后的普通更新、有效新窗口 evidence_update，保留此前 closed/mixed 断言。库为 `/tmp/neckline-b54-lifecycle-base.sqlite`、`/tmp/neckline-b54-lifecycle-repair.sqlite` 与 `/tmp/neckline-b54-lifecycle-boundary/{b39-e2e,history}.sqlite`。最新 Swift **44 通过／5 个历史专测跳过／0 失败**，日志 `/tmp/neckline-v3-qa/ios-b54-lifecycle-tests.log`；双端再次读取当前临时 API 并验收截图，证据为 [repair/](v3.2.0-b54-evidence/repair/) 中的 `macos-lifecycle-final.png`、`ios-lifecycle-final.png`。QA 已停止和移除、主模拟器 Shutdown，无真实供应商或生产操作。现冻结后交原 Reviewer 最终窄复查。

### 最终独立结论

原独立 Reviewer 确认最后两项 P2 均关闭，本轮未发现可报告问题。原两条 CLI/worker 脚本原样复跑，普通更新后历史与日报均可选择，风险延续后两接口均为 risk；原脚本用于锁定旧缺陷的断言因此不再成立，场景和入口未被改弱。独立运行 **47 项相关测试全部通过**，另用 `/tmp/test_neckline_b54_final_lifecycle_order.py` 验证 **九个连续状态，1 项测试通过**：乱序到达、等价时区、同一时刻后追加风险、部分与完整解除、终态后再更新；日报与历史全程一致。该独立测试副本保存在 [review/test_neckline_b54_final_lifecycle_order.py](v3.2.0-b54-evidence/review/test_neckline_b54_final_lifecycle_order.py)。

最终已审目标为 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b54-final-review-lwo28fs8/target-manifest.json`，SHA256 `54eb16ff175651e7eed51e935f17d0fa84d5ca5f9262964c494076abde09325e`，376 路径、相对本节基线的 5 个 Backend 文件及共享聚合直接边界。Reviewer 与主会话分别核验 376 路径、K10 哈希和 HEAD 无漂移；随后仅更新本记录、主 Plan 和独立测试证据，业务代码未再变。此前原五项及四个追加边界的关闭结论保持，本轮全部 findings 闭环。

结论限于本轮代码、离线 SOP、实际 API/Swift/原生呈现及已验证恢复路径；未覆盖真实供应商质量、实际日费用、线上首次执行和生产迁移。版本仍 3.2.0（54），代码未提交，未发布；生产及已安装 Mac 保持暂停的 B53，QA 已清理。

## 14. 当前工程与 K10-v2 全文一致性独立校验（2026-09-10）

用户另行要求 Reviewer 从现行 K10-v2 文档检查当前完整工程，本次仅审查，不修复。原独立 Reviewer（gpt-6-astra / max）从策略逐章核对实际生产链、持久化、API 与 Swift，未将上一轮窄修复通过当作全文一致性的结论。App/Backend 相对第 13 节最终源码没有变化。

审查起点为实际部署 `v3.1.0-b53` / `76c0a9f97937065ed17a5e6f64f4ee8829bbb756`；目标为 HEAD `71d0e1a5d805cbf932300c1f78899168a8af793b` 加全部暂存、未暂存及未跟踪工程文件的 3.2.0（54）/Schema 8。冻结清单 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-k10v2-consistency-u3j19ern/target-manifest.json`，377 路径，SHA256 `6ed0ce87acf9587fe6b7d55923a2cb939a2912f8ec3bbaa0be99952ed1ac4b50`。K10 仍为 `44817d…2dd3d`；池、资料、索引哈希与第 2 节一致，三个公司集合完全相同且均为 1,089，资料保持 `local_draft_awaiting_user`。

结论：**尚未完全一致，新增 4 项 P2，未发现新 P0/P1。前轮已关闭问题保持关闭，本次四项未施工。**

| 项目 | 文档依据 | 触发与实际影响 | 代码定位及修复方向 |
|---|---|---|---|
| P2 晨间实质变化漏报 | K10.md:41，略过／未处理对象遇到新消息也更新 | 昨日正式推荐、未处理；晨间出现 material 事实／论点变化，但当轮比较不再推荐。旧晨报和生命周期已保存变化，v2 晨报仍 completed 且三个变化列表全空，当前页面不可见 | `Backend/neckline/k10/v2_store.py:166`、`store.py:3399` 只关联风险／撤回／到期。重要事实／论点变化应独立投影，不依赖重新入选，不重开窗口 |
| P2 已有价格反应丢失 | K10.md:99，展示已有价格反应 | CLI/worker 输入及候选持久化有 `pctChg=12.0`，正式日报仍 `priceReaction:null`，历史 comparison 也无该行情 | `v2_store.py:154` 固定写空，`App/Neckline/Views/OpportunitiesView.swift:297` 因此不显示。应展示截止内冻结行情及来源／时间，缺数明确标记，不作因果或买卖判断 |
| P2 正反摘要缺失 | K10.md:116，共同事实／分歧／未知项摘要 | 实际 keep→pro→con 均 completed，反方正文第五行起含三类信息；默认页只截前四行／210 字，三项全部被截掉 | `App/Neckline/Views/FocusView.swift:539`、`Backend/neckline/api/k10_schemas.py:517`。在既有两轮输出及持久化契约中保留三类摘要，仍保留全文、来源、版本，不加裁判轮次 |
| P2 连板未单列 | K10.md:148，触板比例和连板另列 | 实际行情采集／评价得到 D1、D2 均封板，主统计正确为 1 样本／1 命中，但记录与汇总无独立连板指标 | `Backend/neckline/api/k10.py:875`、`k10_schemas.py:621`、`App/Neckline/Views/PerformanceView.swift:155`。从已核两日状态提供独立连板记录／汇总并保留缺数；这不是主命中率算法错误 |

章节覆盖：

| 规范部分 | 判定与覆盖 |
|---|---|
| 一、范围与股票池 | 符合；固定池、资料来源和缺口、工程快照已核 |
| 二、日常 SOP | 部分不符；时间窗口与真实发布归组已核，存在晨间变化漏报 |
| 三、资料／标题／事件研究 | 离线路径符合；全标题批筛、跨批归并、池内检索、证据复用、取消旧配额、失败恢复已核 |
| 四、公司比较与卡片 | 部分不符；日比较、一公司一卡、晚间最多 30 家已核，缺价格反应 |
| 五、正反讨论 | 部分不符；留下入口、同截止证据、各一轮、失败保留已核，缺三类摘要 |
| 六、每日选择与机会延续 | 符合；更新／新机会分类、逐公司撤回、固定窗口、重叠与选择隔离已核 |
| 七、评价 | 部分不符；三组冻结、全部候选、到期分母、缺数／除权／重叠已核，缺连板单列 |
| 七.4／八、验证与工程职责 | 冻结记录、用量与恢复路径已核；真实效果、真实 token 消耗和研究对照无法全部离线证明 |
| 九、个人提醒 | 符合；未据此新增交易管理 |

独立执行 **108 项定向回归通过 / 21.02s**，日志 `/tmp/neckline-k10v2-consistency-targeted.log`。另由三个临时脚本确认四项反例，副本保存在 [review/](v3.2.0-b54-evidence/review/)：`neckline-k10v2-consistency-morning.py`、`neckline-k10v2-consistency-price-results.py`、`neckline-k10v2-consistency-debate.py`。从 Backend 目录，在 `PYTHONDONTWRITEBYTECODE=1 PYTHON_DOTENV_DISABLED=1 PYTHONPATH="$PWD/tests/offline_guard:$PWD"` 环境下用 `.venv/bin/python` 执行脚本；均创建自己的临时库并使用确定性 transport。

Reviewer 结束后，主会话复核已审 377 路径、K10 哈希及 HEAD 无漂移，随后只更新本记录、主 Plan 和反例副本。未修改业务源码、运行真实供应商、访问生产、启动原生客户端或恢复服务。生产仍按既有停机记录保持 B53；本次结论不证明真实供应商成功率、选股效果或实际 token 节省。

## 15. Build 55 一致性快修（2026-09-10）

用户授权 3.2.0（55）快修，保持 marketing version，不发布、不恢复生产、不调用真实服务。源代码基线仍为前节已审快照；快修前完整清单及 App/Backend 副本：`/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b55-baseline-7yuan6uy`。修复四项 P2，保留真实入口反例并验证跨端呈现，随后独立复查。


### B55 结果与验证

| 原审查缺口 | 最终行为 |
|---|---|
| 晨间旧公司未再次推荐时变化漏报 | `material` 随晨间结果落档；实质 `evidence_update` 独立链接日报，发布前／后两种写序均成立。保留原机会、时间、来源及 D1/D2；普通更新不冒充实质变化。 |
| 冻结的价格反应被丢弃 | 公司卡保存 `priceReaction` 与 `priceContext`（交易日、涨跌、截止、收集时间、来源）；只取冻结时点前已收盘的行情，缺数明确显示，不读最新行情补写历史、不作消息归因。 |
| 正反摘要遗漏共同事实／分歧／未知 | 原正方、反方各一次调用返回全文与三组结构化摘要。`k10-debate-v2` 契约本地校验，缺失／无效明确失败；原文、来源、谱系、使用量与摘要同一版本持久化，重试不重复已完成角色。正方仅列事实依据和待检视假设，反方列双方共同事实与真正分歧；无裁判、无第三调用。历史未存摘要明确提示，全文仍可查。 |
| 两日连板未单列 | 每个窗口增加 `consecutiveLimitUp` 三态，汇总／三子组／同批／同事件／重叠各自增加连板计数。两日都封板仍只算一次主命中；未到期、缺数、不完整和重叠不混入主分子／分母。 |

- 对外版本保持 **3.2.0**；`App/project.yml` 与工程双端 Build 均为 **55**，Schema 8 不变，无生产迁移。
- 原三个真实入口反例测试在改码前全部失败，覆盖四个发现；保留 [original-counterexamples.log](v3.2.0-b54-evidence/fastfix-b55/original-counterexamples.log)。修复后新增 20 个针对性用例、晨间写后中断回归扩展，完整 Backend **1039 passed / 21 个已有 Polars warnings**，67 秒。全量证据：[backend-tests.log](v3.2.0-b54-evidence/fastfix-b55/backend-tests.log)。旧共享假供应商仍原样传递晨间 JSON；只有明确的成功正反测试响应升级契约，未放宽生产校验。
- macOS build、iOS Simulator build、iOS build-for-testing 均通过；分别位于 `/tmp/neckline-v3-qa/{mac-b55-build,ios-b55-build,ios-b55-testbuild}.log`。复用固定 QA 目录，构建参数显式 `NK_BUNDLE_SUFFIX=.qa`；两端 Info.plist 及测试宿主均为 `top.linotsai.neckline.qa / 3.2.0 / 55`。
- 16 份真实 FastAPI 响应在临时数据库生成并由当前 Swift 解码，新增四份 B55 DTO 位于本节证据目录；XCTest **50 总项：45 通过、5 个既有历史专测跳过、0 失败**。统计：[swift-test-summary.json](v3.2.0-b54-evidence/fastfix-b55/swift-test-summary.json)。
- 原生验收使用单一本地 `127.0.0.1:8765` QA 服务与同一模拟器。macOS 检查晨间无新卡但实质变化可见、已冻结价格、正反三组摘要、连板及主命中各一次；iOS 检查晨间变化、连板和摘要排版，反方三组还经原生可访问树核对。对照 `Neckline_V3_界面参考/00-overview.png`，保持白卡、蓝色强调及阅读层级。截图均来自确定性测试资料，日期和数值不是实际推荐。
- 独立 Reviewer **四项 P2 全部关闭，无可报告问题**。另跑 **61 项相关测试＋6 项独立边界**：正／反摘要失败后 API 重试，反方落档后中断复用，实质变化在发布前／后写入，行情时区／NaN／缺数。脚本及日志在 [fastfix-b55/](v3.2.0-b54-evidence/fastfix-b55/)。真实供应商遵循 JSON 的成功率、实时行情、推送、实际选股效果及 token 成本未测。
- 审查起点为本节开头的 B55 前快照，目标 [reviewed-manifest.json](v3.2.0-b54-evidence/fastfix-b55/reviewed-manifest.json) 共 383 路径，SHA256 `223b781beeff9c1ea4473f059447ada121486889856687f03af1f1d0a79e4b4d`；Reviewer 与主会话均验证 App/Backend 无漂移。后续只有当前状态、README 与本节证据收口，不能将本结论复用于未来源码变动。
- QA 服务／客户端已退出，QA 模拟器安装已卸载，主模拟器恢复 Shutdown；模拟器生产 app 保留。`/Applications/Neckline.app` 仍为 **3.1.0(53)**。无提交、推送、标签、发布、生产读写、恢复排程或真实外呼。


## 16. Build 55 全范围独立复审（2026-09-10）

用户另行要求独立复审。本次使用全新 Reviewer（gpt-6-astra / max），覆盖部署基线 `v3.1.0-b53 / 76c0a9f97937065ed17a5e6f64f4ee8829bbb756` 至当前 `71d0e1a5d805cbf932300c1f78899168a8af793b` 加全部暂存／未暂存／非忽略未跟踪工程文件的 3.2.0（55），不只复查第 15 节四项修复。主会话负责迁移与发布静态契约补验。

**结论：3 项 P2 待修，另有 1 项非阻塞 P3 元数据问题。未发现 P0/P1；本次不作完整 SOP 全部通过结论。前节四项快修的关闭结论保留，本节发现为其他遗漏。本轮只审查，未实施修复。**

| 严重度 | 触发与影响 | 定位／修复方向 |
|---|---|---|
| P2 | 三次真实 CLI 晚扫 `initial → material_stage → continuation`，第三日报错误沿用初始机会而非新阶段窗口。应为 9 月 10–11 日，实为 9 月 9–10 日；旧窗口到期导致 `canSelect=false`，真实选择 API 409，而新阶段仍 active。 | `Backend/neckline/k10/v2_store.py:127–129` 优先 key 查询，命中时忽略明确关联。延续应沿用明确关联机会及其准确 key／窗口，并拒绝冲突映射。 |
| P2 | 正方、反方、晨间子复核各 402／429 共六个真实入口反例。429 直接 failed 且重试计划为 0；402 在 API 只显示通用模型失败，用量账退为 `provider_call_failed`。 | `Backend/neckline/k10/analysis.py:435`、`morning_runtime.py:167` 丢失错误类型与 Retry-After，`runtime.py:185/209` 直接终态。保留安全错误分类，429 有限调度受影响步骤，402 可读终态。主扫描已有相应处理，本次没有重复外呼、已完成正方未被重跑。 |
| P2 | README 当前配置指引仍指向已删除的 execution-v3，配置入口实测在打开数据库前报“执行配置文件不可读或不是 JSON”；v1.4／80/40／全文剩余额度描述与 K10-v2 冲突。 | `README.md:49–53` 更新为策略 v2、执行 v4、固定资料与策略绑定、无全文配额；历史 B53 配置单独标识。 |
| P3（非阻塞） | 本地 B55 的 `/api/v1/health` 返回 `releaseSet=v3.2.0-b54`，本地核验会看到版本不一致。 | `Backend/neckline/api/app.py:28`。既定 `App/scripts/prepare_release_candidate.sh:32,42` 会自动同步并校验，故不据此认定未来正式发布失败；本次未运行发布脚本。 |

Reviewer 独立执行 **38 项已有针对性回归通过**；**7 个新预期行为断言失败**，归并为前两项业务缺陷。原始脚本、实际 API 结果与日志保存在 [full-review-b55/](v3.2.0-b54-evidence/full-review-b55/)，供后续修复保留反例。测试使用真正返回的任务与绑定、确定性 HTTP 和临时库，无真实付费服务。

主会话另执行迁移与固定资料 **11 项测试通过**；从部署标签原始 schema.py 建立 Schema 7 临时库，经当前受控迁移到 8，**50 个旧表全部旧列／行等价**，备份哈希保持，校验回滚至 7 后完全一致。实际 health GET 确认本地 B54 标识；配置入口确认 README 路径失败。未访问生产、运行原生客户端、启动 API 服务、恢复开关或执行发布。

冻结目标 [target-manifest.json](v3.2.0-b54-evidence/full-review-b55/target-manifest.json) 共 **401 路径**，SHA256 `d59ba245ca6852bcc010948d746dff814c67b64922bb920f06c4f5ca1e78bb64`。Reviewer 与主会话均核对全部文件、HEAD、K10 `44817d…2dd3d` 无漂移；审后仅更新主 Plan、本节及证据附件。真实模型质量／余额／成本／数据服务、推送、线上迁移及新一轮原生行为未覆盖；既有构建与原生结果仍属前节同源码证据，不能替新反例作证。


## 17. Build 56 · 完整复审问题快修（完成，未发布）

- 用户授权：快去修复；3.2.0 保持不变，双端 Build 56；未授权发布或恢复。
- 起点：main `71d0e1a5d805cbf932300c1f78899168a8af793b` 加完整 B54/B55 未提交实现。修改前本地源码快照 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b56-baseline-j02aini5`，清单 SHA-256 `eedf6e1734be88fc3bd2fc7975a5d22a3aa4edcd066ffef61571723dfa52abc6`。原七个反例已进入 `Backend/tests/test_b56_review_regressions.py`，修正阶段卡应允许选择的业务断言，保留实际 API／CLI 入口。
- 范围：修复第 16 节三项 P2，顺带同步 P3 本地健康标识；验证后交独立 Reviewer 窄复查修复及直接影响。全程隔离临时库和确定性 transport，外网拒绝，生产不动。

### 实施与根因

- 第 16 节原七个反例全部保留并扩展：分类后的普通更新继承显式前驱的 opportunityKey；日报发布根据新机会／延续分别解析，严格校验 ID、公司和 key。原 Sep9/10 与新阶段 Sep10/11 两个窗口保持不变，第三晚延续绑定新阶段，仍只有两份样本且主／重叠归属不变。
- 正方、反方、晨间子复核统一保留 402／429 安全码；402 可读余额不足终态，429 按冻结执行包和原 Retry-After 有限排程。成功正方、冻结资料和既有卡片／窗口均复用；排队中的晨间复核与恢复完成均在任务事务内更新报告覆盖。
- README 当前配置切换为 K10-v2／execution-v4、固定资料导入与显式绑定，无 80／40 或剩余额度；B53 暂停配置保留为历史事实。App 两端 Build 与本地 health 发布集均为 56，对外版本仍 3.2.0。
- 五项早期 B54 反例的读时钟原依赖电脑日期，实际进入 D2 收盘后会误报；将观察时刻固定在原场景的生成之后，不改源时间、绑定或业务断言。

### 独立复查及追加关闭

初次固定目标 `0f83effb8089bcef627f8f4a7e7cde453478a931ae45e977b42e1930d182eeee` 独立复查确认原四项修复有效，另复现两项 P2，已在同一快修范围内关闭：

1. 429 排程合法但晚领取 7 小时后仍可调用：在尚未完成的 pro／con、未缓存晨间模型结果外呼前检查持久起点和冻结期限；已持久完成结果允许过期后只收尾。
2. 失败调用账／artifact 已提交、任务状态尚未提交时中断，租约重领绕过 402 停止或 429 原等待：失败回执与调用账在同一事务保存，含原收到时刻和 Retry-After；worker 先恢复原终态／排程，其提交与清除回执同事务。回执事务失败时账本也回滚，started／unknown 不能通过换 attempt key 自动再次调用。

最终固定目标 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b56-receipt-review-9mp8q6b3`，manifest SHA-256 **`3759cc57879d12a0932b239b5847a10c9dd1d8ec96c32f4f26e99d164ec8d6ec`**。Reviewer 独立运行 **33 个回归通过**，原始中断脚本确认 402／429 恢复均无新增调用，结论为“未发现可报告问题，两项 P2 均可关闭”。审查范围为本轮修复及直接影响，未重审无关模块、未覆盖真实供应商或生产。收口只更新控制／证据文档，App／Backend 业务源码相对最终目标无漂移。

### 最终验证与交接

- Backend **1073 passed / 21 个既有 Polars warnings**；所有运行使用 offline_guard、禁用 .env、隔离数据库和确定性 transport。涵盖原 API/CLI 实际 task ID、正常延迟／过期领取、自然租约恢复、已有成功结果复用、回执事务回滚及未知调用结果阻断。
- macOS build、iOS build、iOS build-for-testing 均通过；QA Info.plist／xctestrun 主机均核对 `top.linotsai.neckline.qa`、3.2.0（56）。XCTest **46 passed / 5 historical skips / 0 failed**，新增 10 份真实隔离 API 响应通过现行 Swift 解码。双端抽查阶段卡、晨间 402、正方完成且反方 402 页面，保留白卡蓝色和文字层级设计方向。原生验证后只修改后端恢复内部，未再次宣称原生端已演练进程中断时序；最终后端完整门禁与独立复查已覆盖这些时序。
- 证据集中在 [fastfix-b56](v3.2.0-b54-evidence/fastfix-b56/)：完整改动及恢复追加 patch、固定 manifest、原始复审反例、测试／构建日志、XCTest 摘要、10 份 DTO 和双端截图。
- QA API、Mac QA 进程、iOS QA 已退出；QA iOS 安装移除，本轮临时场景库清理，共享主模拟器 Shutdown。生产 Mac 仍为 3.1.0（53），未改生产配置／服务／数据库、未真实调用供应商或 APNs、未推送／提交／打标签／发布。
- 下一步仅在新授权后进入发布／迁移／恢复；不要把离线验证当作真实接口成功、选股表现或日 token 保证。


## 18. Build 56 再次全范围独立复审（2026-09-10，三项 P2 待修）

用户本轮只要求独立复审。新 Reviewer 从部署 tag `v3.1.0-b53`（`76c0a9f97937065ed17a5e6f64f4ee8829bbb756`）审至 B56 全部暂存／未暂存／未跟踪工程改动，按当前 K10-v2 和 Plan 检查主要调用边界；不沿用第 17 节局部复查作为整体通过结论。

目标为临时快照 `neckline-b56-independent-_01xfg51`，294 个明确文件，manifest SHA-256 **`98d00210b06c570d274c972f37d784081ee59a9ac422bd119253cd6c68411203`**。Reviewer 结束时核对全部匹配；收口仅更新记录和保留复现，未修改 App／Backend 源码。

1. **P2 · 父扫描恢复重新激活终态 402 子任务。** `Backend/neckline/k10/pipeline.py:1921–1926` 对 failed／not_configured 子任务无条件调用 `retry_task`。真实 CLI later_scan → worker，在晨间子任务已提交余额不足失败后中断父任务，重复自然恢复父任务即可使调用 1→3，超过执行包 networkMaxAttempts=2，未人工重试。应复用终态子结果及原预算，人工重试另走显式入口。7 小时后的固定期限仍阻止外呼，未发现绕过期限。
2. **P2 · 扫描早退没有写入 v2 日报失败状态。** `pipeline.py:2927–2931` 等早退绕过 `2989` 后 execute_scan 结果投影。离线 provider resolution 设为未配置后，9 月 9 日晨任务 not_configured，v2/latest 却 empty／尚无日报；同日晚任务失败但接口仍是 9 月 8 日 completed、reason=null，配置 scopes 仍 configured。应将所有扫描终态归于统一收尾，使用户能看到本轮失败及原因。
3. **P2 · 晨间分页在选择刷新后退回第一页。** `App/Neckline/App/AppModel.swift:192` 用第一页替换当前已加载晨报，`282–283` 的选择动作触发刷新。用当前 AppModel／APIClient／Models 编译的独立 Swift 驱动证明：31 张 → 略过第 31 张 → 30 张，当前卡消失，View 的 onChange 随即重置位置。相同 reportId 刷新应保留已加载范围并更新选择状态。

最小复现及固定 manifest 保留于 [independent-review](v3.2.0-b54-evidence/fastfix-b56/independent-review/)。本轮使用隔离数据、确定性响应和离线拦截，未真实调用供应商、访问生产或启动 App／模拟器；未重复全量门禁及原生视觉验收。上轮 1073 项后端、三项构建和 46 项客户端测试的记录已核对，但这些通过不能覆盖本轮新增反例。三个问题均未施工，版本保持 3.2.0（56），生产继续暂停。


## 19. Build 57 · 三项复审问题快修（完成，未发布）

用户授权快修，3.2.0 不变，双端 Build 57。施工基线：第 18 节固定 B56 快照 `98d00210b06c570d274c972f37d784081ee59a9ac422bd119253cd6c68411203`，包含全部未提交实现；Git HEAD `71d0e1a`，生产仍 B53 暂停。范围只含第 18 节三项根因及直接影响，不发布、不恢复、不真实调用供应商。


### 实施与恢复边界

- 父扫描恢复不再调用 `retry_task` 重开终态晨间子任务；保留原 402 停止、429 排程及上限。原旧测试要求自动重试，与确认规则相反；改成断言父恢复保持失败、显式重试能成功，原真实入口反例另行保留，未放松关键业务断言。
- 在 `finish_task` 的同一写事务中投影 v2 扫描失败，覆盖 worker 前置检查、handler 早退和异常。只用该任务精确策略绑定及截止时刻，无绑定不虚构快照；已发布报告不被覆盖，失败新增无卡日报，GET 无 DDL。事务中断回滚后可自然恢复。
- 同一份晨报刷新重新读取已加载页，再一次提交完整内容；保留卡片 ID、更新选择状态。新日报从自身首页开始，分页读取失败保留旧内容并明示错误，拒绝重复游标或没有新增内容的页。
- 独立复查追加 P2：早期失败的 `+08:00` 截止时刻与恢复摄取 UTC 文本冲突。`create_scan` 改用既有 `_utc_instant` 比较实际时刻，原存储值不重写，实际时刻／窗口种类／配置变化仍拒绝；新失败扫描统一写 UTC。
- 保留同一冻结秒继续验证，又复现五组晨报 ID 仅由扫描、秒和项目数生成，导致失败与成功撞号；加入报告覆盖及分组内容到 ID，恢复追加成功版本、保留失败历史，重复相同输入不重复追加。
- 恢复夹具的交易日历补齐至 9 月 15 日，与 Reviewer 原反例一致。仅固定 API／pipeline 时钟；不替换 `store.datetime`，避免 datetime 子类污染真实 publication 类型检查。源消息时间、真实任务 ID、执行绑定和恢复输入均未预先改写。

### 独立复查及证据

初次 B57 固定目标 `e22790ef32c10905e771b1c7904d5377f38f4aa0c6aa5be0368a2f03ec432c03`：Reviewer 关闭原父恢复和分页问题，确认失败可见性／事务正确，同时追加显式恢复的时区 P2。

最终目标 `/var/folders/_v/s6hfcbrj5v3fyxk8jzwh7ft00000gn/T/neckline-b57-recovery-review-o5gdc07z`，manifest SHA-256 **`e2c40ff58e603553b2fdcee50923fcbb3130dcb0d6a1cc18e47131ee6f0fe11b`**。Reviewer 独立运行 **21 条定向回归通过**，原始晨／晚 API 重试反例均完成并清除错误；同秒恢复及相同输入重入不改写历史、窗口或已公开日报。结论“未发现可报告问题，追加时区 P2 已关闭”。范围为本轮修复及直接影响，未扩展全项目审查。

- Backend 最终 **1095 passed / 21 个既有 Polars warnings**；首次 14 个反例在修复前全部失败，追加时区两个原反例也保留失败证据。21 项最终回归覆盖真实生产者绑定、父子恢复、12 类晨晚早退／前置失败、事务中断、分页选择、显式恢复、时刻与配置不变性。
- 三条原生构建均通过；双端 Info.plist 为 `top.linotsai.neckline.qa`、3.2.0（57），xctestrun host ID 已核对。XCTest **50 passed / 5 historical skips / 0 failed**，包含 31 公司真实 API 响应驱动的操作刷新。共 10 份本轮实际 API 响应保留；最后两份恢复响应使用当前 Swift 模型独立解码，状态 completed、reason=nil。
- 双端正常卡片与当日失败空态均检查，并与 `Neckline_V3_界面参考/01-opportunities.png` 对照白卡、蓝色和排版层级。最后追加只改后端，原生页面未重新启动演练同秒恢复；该边界由完整后端门禁、真实 API 解码和独立复查验证。
- 证据集中于 [fastfix-b57](v3.2.0-b54-evidence/fastfix-b57/)：相对 B56 的 patch、最终 manifest、修复前失败及最终通过日志、原恢复脚本、DTO、Swift 解码、四张页面截图及原生测试记录。源码对最终目标无漂移，收口仅更新记录。
- QA 只复用既有双端路径和“主模拟器”，各平台至多一实例；已退出／卸载 QA、关闭隔离 API、清理本轮临时库并关闭模拟器。生产 Mac 保持 B53。无真实模型、搜索、数据商或 APNs 调用，无探测余额权限、生产访问、部署、提交或标签。

本轮全部修复和复查完成。发布、迁移、恢复及首次真实供应商运行需新的明确授权；不把离线通过表述为真实调用、选股效果或费用保证。

## 20. 3.2.0（57）一条龙发布

2026-09-10 用户明确授权完整发布。审查起点是实际部署 `v3.1.0-b53` / `76c0a9f`，目标为 B54–B57 全部实现及证据，包含当前未提交与未跟踪文件。发布按现有路径完成，Schema 8 导入保留公司资料原始来源状态，生产 worker、timer 和持久运行开关保持关闭；不调用真实模型、搜索、行情或 APNs，不恢复旧任务。

现场只读已确认宁波主机 `ser657204219523`，根目录 `root:root /0755`、DB `neckline:neckline /0600`，API、worker 和全部 timer 均 inactive+disabled。后续迁移、签名与最终回执继续记在本节。

### 发布结果与恢复证据

- 发布源码 `8e5cec47b599e152fc32dacbf32a43ec7b9f8b9b`，main 与不可变 `v3.2.0-b57` 已推送。累计范围为实际 B53 `76c0a9f` 至此提交（含此前未提交的 B54–B57 和旧生产记录），225 个变更文件。292 个 App／Backend 文件与最终独立复查 manifest 完全一致，未因打包改变运行源码。完整 1095 项后端、双端三项构建和 50/5/0 XCTest 沿用最后相同源码的验证；本轮另做双端 Release 签名归档及生产验收。部署助手由主会话核对并在真实副本验证，未另行召唤 reviewer。
- Ningbo Schema 7→8：迁移前后逐表核对 67 个旧表的全部旧列／行；配置只追加独立 ID，历史配置内容、报告、选择、窗口、任务与用量保留。真实副本上先验证迁移与配置，再从迁移后副本执行 SQLite backup 恢复至精确原库，随后正式切换。旧 API、worker、timer 均停写后建立备份，实际迁移及只读 API 后均验证完整性和外键。
- 正式导入池／档案／索引各 1,089，资料来源仍为 `local_draft_awaiting_user`。策略 `k10-v2-production` rev 1、执行 `k10-v2-execution-production` rev 1，标题 policy 内容与批准修订精确核对，策略快照 `k10-v2-20260909`；首次 V2 扫描不存在时，显式绑定的四范围 configuration 均 configured。晚／晨日报均 empty，无假造首报。
- 恢复集 `/opt/neckline/data/backups/v3.2.0-b57-predeploy/` 保留 B53 runtime、旧绑定、pre/post DB、完整回执和 API 响应。pre SHA256=`fc143260522404287e31b8883580351dc24401d014f3a0f4941c5753cb633878`；post SHA256=`3ab5192ecf5aaac222fcd40bb072c4f1785b0b27f560adc77b15e9e48b18c148`。根 `root:root /0755`、DB `neckline:neckline /0600` 不变。临时演练库已清理；正式恢复集保留。今后回滚前先停写、备份最新现场，不能覆盖上线后的新数据。
- 生产 API 本机／公网严格 TLS 健康通过，未鉴权 configuration 返回 401；带鉴权 configuration、晚／晨空日报、旧机会和结果均通过实际 DTO 校验及当前 Swift 解码。API active+enabled；持久控制 closed，worker、报告／行情／备份 timers inactive+disabled。发布后任务仍为 completed 1、failed 2、cancelled 2、not_configured 1，旧用量与外呼表内容不变；无新任务、真实供应商或 APNs 调用。API 最近 warning/error journal 无新记录。
- Mac universal（arm64+x86_64）Developer ID 签名与 Apple 时间戳、严格验签、ZIP 解包验签通过；iOS arm64 development 签名归档通过。首次 Mac 签名因 Apple 时间戳服务不可用失败，保持原安全参数重试即成功，没有禁用时间戳或降低签名。沿用未 notarize 的既有发布方式，未声称通过网络下载后的 Gatekeeper 验收。
- Mac 已通过 `ditto` 换装 `/Applications/Neckline.app`，B53 恢复副本 `/Users/linotsai/Lino/app_backups/Neckline-v3.1.0-build53-pre-v320-b57-20260910.app` 严格验签。仅一个生产进程；CUA 核实正式设置显示 3.2.0（57）/ K10-v2、四项已配置与暂停状态，历史两份机会可读，未操作选择。iOS 无设备安装、无 IPA；签名工程及归档供用户通过 Xcode 安装。
- [GitHub Release v3.2.0-b57](https://github.com/linocai/Neckline/releases/tag/v3.2.0-b57) 六项资产全部下载，比对字节和 SHA256 一致。发布文件位于 `/Users/linotsai/Lino/releases/Neckline/v3.2.0-b57-20260910/`；服务器同包／wheel／manifest 位于 `/opt/neckline/releases/v3.2.0-b57/`。公司资料输入、生产数据库和密钥均不在公开资产中。
- 本轮临时上传、迁移、解包、重复下载与构建安装副本收口；保留正式签名归档、旧客户端恢复包和已有 QA 证据。`NB_info.md`、实际 HZ 事实文件 `~/Lino/hz_info.md` 已同步，杭州实例未操作。

脱敏回执、签名 manifest、下载校验和正式 Mac 验收集中于 [release-b57](v3.2.0-b54-evidence/release-b57/)。真实响应仅留服务器，不进入公开证据。脚本为本次受控一次性发布记录，不能脱离已确认主机／版本／备份条件重复运行；其通用指纹和文件操作引用此前 [production_release.py](v3.1.0-b39-evidence/production_release.py)。发布完成，后续真实试跑和报告恢复仍待用户另行授权。


## 21. B58 SOP 快修（2026-09-10，未发布）

用户先要求独立只读检查 SOP，随后明确授权快修六项已确认问题；历史越多刷新越慢一项明确不处理。本轮基线为 `b8be3f68c5faf75eb755ddd90662df9d3d65ddeb`，源码与已发布 `v3.2.0-b57` 一致。保持 3.2.0，只把客户端 build 与后端 release set 升至 58；无 schema 迁移、策略阈值、真实供应商调用、生产操作、提交或发布。

### 已修复行为

1. Tavily Search／Extract 保留 402、429 和安全 Retry-After；429 持久延期受影响步骤，失败回执与外呼结算同事务，跨进程后不提前调用。402 停止当前任务后续付费请求。重试受原每步次数和任务完成时限约束。
2. 已成功外呼但结果未保存、标题阶段遗留 running、搜索结果未知时，自然恢复停止重复付费。明确业务校验失败先保存对应 attempt 的失败确认；只有用户 API 重试或既有受控恢复入口才能授权确切旧失败，不能用旧失败记录放行后来丢失的成功结果。
3. 晨间原始推荐资料使用首发实际 availableAt 读取精确冻结版本，单独保留原新闻 cutoff；原资料不完整明确失败，不冒充复核完成。
4. 新版公司窗口讨论合并全部正式催化的原文、事件和公司映射，正反双方共享同一冻结输入。已有 B57 成功正方的修订沿用其原输入完成反方；新增修订才采用完整多催化资料，不重写或重付旧正方。
5. 次日新消息输入和可复用历史证据分开许可，按同事件、提示契约、模型参数、来源版本及时间筛选，排除已更正旧来源。公司关系仅用最新明确收口集合，纠正和显式空集覆盖中途猜测，不复活已删除映射；仍不复用旧排名或完整原文 prompt。
6. 全部发布 hook 写入后再检查固定 D1 边界，跨 09:30 时整笔事务回滚；成功则统一最终可查看时间，原批次重放不重新取时。

### 复现与独立复查

原晨间取得时间、多催化原文、hook 跨开盘三个场景先复现 3 failed，修后 3 passed；正／反外呼结算到结果落档间中断先复现 2 failed，修后通过；次日全新文章无法复用旧事实也先确认失败。新回归使用实际 CLI／API 生产者创建任务和执行绑定，经过 worker／handler，不能手工补绑来让入口通过。

首轮修复复查补出历史临时映射盖过最终结论、B57 已完成正方升级恢复输入漂移、明确用户重试被过宽防重／旧 402 阻断，均已采纳并保存回归；二次复查再覆盖语义失败确认与失败 artifact 落档间中断。失败确认先于 artifact，先确认后中断仍禁止自然重复付费，明确用户重试可以继续。

最终源文件与测试冻结清单见 [fastfix-b58/source-manifest.json](v3.2.0-b54-evidence/fastfix-b58/source-manifest.json)，SHA256 `5d08f24e1cc09127c41e2a65006810866f624395187441c3094d0e18e170da73`，27 个文件，含未跟踪 B58 回归。发现与生命周期 reviewer 在二次快照 `5a99896c…88d76` 通过；三次快照仅将正反失败确认前移及增加两项中断测试，执行 reviewer 仅复查这项直接增量。

### 验证与边界

最终后端 **1119 passed**，21 个既有 Polars warnings（139.53 秒）；本轮 B58 定向回归 23 项全部纳入。执行 reviewer 对第三快照最后两项中断回归独立 2 passed 并确认无可报告问题，发现、生命周期、执行三个审查范围全部闭环。完整日志见 [backend-tests.log](v3.2.0-b54-evidence/fastfix-b58/backend-tests.log)。

macOS build、iOS Simulator build、iOS build-for-testing 三项通过，QA Info.plist 均核对 `top.linotsai.neckline.qa`、3.2.0（58），xctestrun host ID 同样核对。无 Swift 源码变更；本轮未安装／启动 QA，也未声称重新做了原生页面视觉验收。当前 Swift 模型独立编译并解码 5 份实际隔离 FastAPI 响应，覆盖同窗口两轮各两份原文、晚报、晨报和空态，见本节证据目录。

所有 Python 验证自进程启动即用 `tests/offline_guard` 拒绝外网，并禁用 dotenv；数据库全部是隔离临时库，模型与搜索使用确定性替身。没有使用真实 DeepSeek／Tavily／TuShare／APNs、检查余额、重放旧生产任务、恢复 timer 或访问生产数据库。生产仍为 B57 及既有暂停状态；本轮证明本地工程回归，不证明真实供应商质量、实际 token 消耗或选股效果。

## 22. B59 双端 BYOK 快修（2026-09-10，未发布）

用户授权在 macOS/iOS 补全模型端点、模型名称和 Key 的编辑。按快修保持 3.2.0，双端及后端 release set 递增为 Build 59；承接全部未提交 B58 修复。本轮不包含提交、发布、生产配置修改、恢复报告或真实供应商调用。

- 基线：Git `b8be3f68c5faf75eb755ddd90662df9d3d65ddeb` 加第 21 节已完成的 B58 未提交工作区。B59 增量共 21 个源码／测试／版本文件，前后哈希见 [source-manifest.json](v3.2.0-b54-evidence/fastfix-b59/source-manifest.json)，清单 SHA-256 `8c5fda66d11967d03b5d0a88882b1898a134bc88b9eada20ab8fa3a99cf70367`。B58 的独立审查结论只适用于其冻结快照，不覆盖新增 B59 改动。
- 双端共用模型编辑器：选择已有连接、新增连接、编辑 HTTPS 地址／模型 ID、写入或替换 Key、显式清除 Key、选择当前连接、删除连接。基础地址自动补全 Chat Completions 路径；准确端点和模型名称由用户填写，DeepSeek 示例仅在主动点击时填入。失败保留输入，成功在编辑器内提示；较慢返回的旧设置读取不能覆盖已保存连接。
- API／存储：启用某连接和停用其他连接在同一事务中完成；重复名称或无效修改不能改变当前连接。Key 省略表示保留，显式空字符串表示清除；跨主机或端口改变地址须同时提交替换 Key 或明确清除。连接名称按路径段编码，中文和空格的更新／删除经实际 API 验证。校验失败响应去掉原始输入，避免错误类型的 Key 被回显。
- 执行链：发现、正反分析、晨间更新都解析用户选择的实际端点和模型，执行包的既定策略及单次请求边界保持原值；实际连接另存无 Key 的任务 `providerBinding`。启用新连接只影响尚未开始的新任务，旧任务继续使用原连接；同组轮换 Key 可恢复。若直接修改原组端点／模型或删除原组，旧任务停止，不混用两套配置。旧版本已有外呼但没有连接身份的任务停止猜选，不自动恢复。原始生产失败晨报及旧晚报没有被触碰。
- 请求和复用：通用模型不发送 DeepSeek 专用推理扩展；实际模型和端点进入请求摘要、发现步骤缓存、正文事实缓存及研究复用参数摘要。事实缓存记录实际模型，Key 不入摘要。已有 DeepSeek 工程参数不被改成新的策略默认。

验证均使用进程启动即禁止外部联网的测试环境、隔离 SQLite、合成 Key 和确定性 HTTP 响应：

- Backend 全量 **1135 passed / 21 个既有 Polars warnings**；最终事实缓存模型标记修正后，BYOK 定向 **15 passed**。覆盖真实 CLI 入队 → worker → 自定义模型发现 → 正式发布，以及实际选择 API 返回任务 ID → 正反分析 → 持久连接固定。模型 URL、ID、鉴权头和协议字段逐项检查；任务内切换连接、Key 轮换、直接改模型、重复名称、错误输入、清除和删除均有回归。
- 最终源码 macOS build、iOS Simulator build、iOS build-for-testing 全部成功（另有 macOS build-for-testing）。构建参数显式 `NK_BUNDLE_SUFFIX=.qa`；双端 Info.plist 和 xctestrun 的宿主均核实为 `top.linotsai.neckline.qa` / Build 59。
- macOS 与共享“主模拟器”各 **3 项新增 XCTest 通过**：真实本地 FastAPI 设置读写、实际 FastAPI DTO 解码与原生窗口／页面、慢读取覆盖保护。macOS 另跑 **2 项既有跨服务连接代际回归**。本地 API 只监听 loopback，客户端设置外网拒绝和禁用持久凭据；测试未调用模型。
- 对照批准的 `04-results-settings.png` 检查白卡、蓝色操作和文字层级。原生宿主实际呈现空白与已配置连接，iOS 滚动后可读 Key 状态、清除／启用开关和保存／删除按钮；首次 iOS 截图因测试窗口未绑定 scene 得到空图，修正为真实 UIWindowScene 后重新截图并检查，不以测试绿灯替代视觉验收。证据：[Mac 空页](v3.2.0-b54-evidence/fastfix-b59/macos-empty.png)、[Mac 已配置](v3.2.0-b54-evidence/fastfix-b59/macos-providers.png)、[iPhone 空页](v3.2.0-b54-evidence/fastfix-b59/ios-empty-top.png)、[iPhone 已配置](v3.2.0-b54-evidence/fastfix-b59/ios-providers-top.png)、[iPhone 滚动后](v3.2.0-b54-evidence/fastfix-b59/ios-providers-bottom.png)。对应 API 原始公开响应保存在同目录 `empty.json` / `providers.json`，无 Key 内容。

收尾：临时 loopback API 已停止，合成凭据、临时数据库与带测试环境变量的 xctestrun 已移除；QA 客户端已退出，共享模拟器恢复原来的关闭状态，生产客户端未替换。

交接：代码留在 main 的未提交工作区，客户端与后端需共同发布才能启用新 BYOK 行为；生产仍是 B57，run control、worker、timer 保持暂停。没有真实模型兼容性、供应商成功率或选股效果验证；当前支持 OpenAI 兼容 Chat Completions，不包含 Anthropic 原生或 Responses 协议。后续恢复入口为本节与 `PROJECT_PLAN.md`，发布须另获用户授权。


## 23. B59 一条龙发布（2026-09-10）

用户明确授权完整发布路径。本次实际部署基线为 `v3.2.0-b57` / `8e5cec47b599e152fc32dacbf32a43ec7b9f8b9b`，线上 manifest 所列运行文件逐项无漂移；本地起点为 `b8be3f68c5faf75eb755ddd90662df9d3d65ddeb` 加第 21–22 节未提交的 B58/B59 改动。累计检查包含全部暂存、未暂存、新源码与测试；B58 独立审查证据沿用其冻结范围，B59 和本次发布助手由主会话核对，未声称新增独立审查。

- 目标仍为 3.2.0，双端 Build 59；后端 `v3.2.0-b59`，Schema 8。执行既有发布准备脚本，将同一图标资源更名为 `AppIconV320B59`，图案内容不变。此前 B59 源码快照与本次运行代码一致，仅追加构建资源标识。
- 发布前重新执行外网拒绝、禁用 dotenv 的全量回归：**1135 passed / 21 个既有 Polars warnings / 143.09 秒**。macOS build、iOS Simulator build、iOS build-for-testing 三项成功，显式 QA bundle 后缀。双端 BYOK XCTest、实际 FastAPI 解码和空／已配置原生截图沿用第 22 节相同源码证据。
- 已核实宁波主机、运行版本、根目录／数据库／绑定权限、恢复条件；API active+enabled，其余 worker/timer inactive+disabled、run control closed。部署只更新 runtime/wheel，无 schema、参数、模型配置或环境绑定迁移。真实副本 API 验证强制拒绝网络；生产验收只读，本次不会触发模型、搜索、行情或 APNs。
- 一次性发布助手和验收脚本见 [release-b59](v3.2.0-b54-evidence/release-b59/)，通用备份／文件操作引用第 20 节的历史 helper。正式切换前保留 B57 runtime、wheel、环境绑定与数据库备份；只在隔离目标验证恢复能力，不覆盖生产库。切换失败恢复 B57 运行文件和 wheel，保留数据库现场。

### 发布结果

- 源码 `c228c0f2e9dc362876c467a06bb365cad9563107`、main 和不可变 `v3.2.0-b59` 已推送，累计包含 B58/B59；本轮运行源码冻结清单覆盖 297 个 App/Backend 文件。正式打包后运行源码未变。后续提交仅补发布／运维事实，tag 保持不动。
- 宁波 `/opt/neckline` 已部署相同提交的 116 文件 runtime 与同源 wheel；正式切换前后逐文件核对。真实生产副本启动新 API 的验收强制拒绝 socket 外连；四项显式绑定配置、V2 晚／晨空日报、旧机会／结果与原 BYOK 均通过实际 DTO 校验。恢复机制在隔离数据库验证后才切换。
- 85 张表全部行／列完全一致；pre/post SQLite backup 的 SHA256 均为 `3ab5192ecf5aaac222fcd40bb072c4f1785b0b27f560adc77b15e9e48b18c148`。模型配置、绑定、环境与旧数据原样保留；无迁移。恢复集 `/opt/neckline/data/backups/v3.2.0-b59-predeploy/` 保留 B57 runtime、环境／绑定、pre/post DB 和私有 API 响应；旧 B57→B53 恢复集继续保留。后续恢复前必须先检查新写入，不直接覆盖。
- 本机及公网严格 TLS health 返回 B59；实际 API 经当前 Swift 解码，包含四项配置、两份空 V2 日报、两份原机会、两项评价和原模型连接。未鉴权 configuration 为 401。Mac 启动后再次逐表核对数据库仍未变。API active+enabled，其余 worker/timer 仍 inactive+disabled，run control closed；任务为 completed 1、failed 2、cancelled 2、not_configured 1；付费调用新增 0、最近 20 分钟 API warning/error 0。
- Mac universal x86_64+arm64 Developer ID／Apple 时间戳／hardened runtime 通过；iOS arm64 development 签名归档通过。Mac ZIP 解包严格验签与可执行文件哈希一致。沿用未 notarize 的既有发布方式，未声称网络下载后的 Gatekeeper 放行。
- `/Applications/Neckline.app` 已用 ditto 换装 B59，原 B57 严格验签恢复包位于 `/Users/linotsai/Lino/app_backups/Neckline-v3.2.0-build57-pre-b59-20260910.app`。正式 Mac 单实例显示 Build 59、四项配置、暂停状态；BYOK 端点／模型编辑与底部控件可见，两份原观察、原选择与 D1/D2 可读。仅查看未改配置或选择。备份停 API 时旧客户端短暂收到 502，恢复后读取正常。
- iOS 工程和正式签名归档准备完成，用户通过 Xcode 安装；没有导出、交付 IPA 或真机安装。先前双端页面验收覆盖相同 Swift 源码；本轮另核对实际正式 Mac。
- [GitHub Release v3.2.0-b59](https://github.com/linocai/Neckline/releases/tag/v3.2.0-b59) 六项资产已逐一下载并核对字节／SHA256。发布目录 `/Users/linotsai/Lino/releases/Neckline/v3.2.0-b59-20260910/`；服务端 runtime 包、wheel 和 manifest 同存 `/opt/neckline/releases/v3.2.0-b59/`。公开资产不含生产数据库、公司资料输入或 Key。
- 临时上传、演练库、重复下载、本地生产响应与过时 QA／安装副本已清理，正式归档、原客户端备份、生产恢复集和验收证据保留。`NB_info.md` 与实际 `~/Lino/hz_info.md` 已更新，杭州未操作。

详细证据集中于 [release-b59](v3.2.0-b54-evidence/release-b59/)：源码快照、回归结果、签名／runtime manifest、生产回执、Swift 解码、Mac 安装／页面验收、下载校验和最终运维状态。发布完成；真实供应商兼容性、日 token 消耗、选股效果、首次试跑与报告恢复仍未覆盖。


## 24. 2026-09-10 21:00 首次真实晚间试跑

用户于北京时间 21:04 明确授权打开开关并从现在开始试跑今天 21:00 任务。本次授权允许该任务需要的真实模型／搜索／资讯调用；先前禁止供应商调用的发布边界不再适用于本次受控试跑。不包含余额／权限探测、旧失败任务恢复、事故批次重放或常规定时器恢复。

线上已核实 B59 / `c228c0f` 无文件漂移，原六个任务均为终态。用户当前模型已改为 `deepseek-flash`，沿用其实际端点与 Key，不改配置。先备份当下数据库，再打开持久 run control，通过正式 CLI enqueue 生成固定 `2026-09-10T21:00:00+08:00` 的新任务；用生产 `run_once(task_id=...)` 边界只处理 CLI 返回的任务，遵守原有切片、not-before、尝试上限和完成期限。单次 runner 不执行会处理旧观察的通知／行情维护，也不领取其他任务。运行脚本见 [trial-20260910](v3.2.0-b54-evidence/trial-20260910/)。

### 真实结果与原因

- 21:07:15 CLI 创建 `task_c9c0feab5c83a08e8a17138ebfb0041a`，21:07:17 开始，21:09:52 失败于 `title_incomplete` / `title_protocol_invalid`；扫描 `scan_d39fbd5c631ea9a7df5f569dbd432b4d`。单任务进程正常退出不代表业务成功；本次业务明确失败。
- 原成功采集水位为 9 月 9 日 09:00，正式窗口补到 9 月 10 日 21:00，冻结 2,786 条输入。TuShare 已取 20 页，出现 `upstream_rejected`，coverage=partial，未推进成功水位。现有封装只保留通用错误码，无法仅凭本次存档判定是限流、权限或其他拒绝；未追加账户／权限探测。
- 标题计划 44 批，保存了 22 批各 64 条的结构化响应。通过只读、禁止外连的检查发现，一条结果的 companyCodes 包含固定 1,089 公司池外的 `300487.SZ`；`title_runtime.py` 的池内校验直接抛出 TitleTriageProtocolError。失败引用为 `doc_6db6bd36d3a59105a72da4abcf4c1556` rev 1，对应 `model:titleBatch:7e4fc0d1ffab3252d40ed1d3065c14d69ede9e2bed248769371bc330c0c47f7d`。
- 该池内校验发生在模型结果已写为 completed 检查点之后；22 个检查点都为 completed，repair=0，却没有完成标题业务阶段。这是下一轮要处理的校验／恢复问题；直接重试可能继续读取同一坏缓存。真实失败结构化响应只保存在服务器私有 `outside-pool-checkpoint.json`，不公开源材料，不改写本次付费台账。
- 模型 22 次 HTTP 调用均已结算，输入 **733,328**、输出 **68,051**、合计 **801,379 tokens**；无 started/unknown 遗留。没有进入正文研究／事实搜索或全局标题协调，Tavily 模型链调用为 0，没有 APNs 发送。未换算金额，未将其作为日 token 预测。
- 实际只读 API 返回 envelope state=available、report.status=failed、reason=title_protocol_invalid，零卡片且未发布；available 表示有本次状态可读，不表示选股成功。正式 Mac 刷新显示“消息截至 9月10日 21:00”“本轮状态：失败”“今天没跑成 · 处理未完成”。
- 原六个任务逐列完整保留，发布批次和已有选择不变。run control=open（遵从打开请求），单次 runner、常规 worker 和所有 timer 均 inactive；没有自动或手工追加付费重试。首试只覆盖发现链截至标题阶段，未证明正文研究、选股输出或通知完整成功。
- 试跑前备份 `/opt/neckline/data/backups/trial-b59-20260910-2100-pre/neckline-pre.db` SHA256 `9c88ef5ec613e61ff22178cd8c9954affcfe6e1a477ea41d7b780db41ad8dcbf`；试跑后 `neckline-post.db` SHA256 `6157fcf9a01c6cb7e56fba88d3777ac00b8fa327139ce0c2b98c76d02996b817`。已有真实输入、结果和付费写入，禁止用旧快照直接覆盖现场。

本轮不修改 App／Backend 运行源码、不升级 build，也未发布新包；已更新工程当前状态与宁波／HZ 运维事实。失败回执、紧凑最终状态与 API 验收见 [trial-20260910](v3.2.0-b54-evidence/trial-20260910/)。后续应先离线复现并修复池内校验与对应检查点恢复，再处理资讯缺口；是否继续真实付费恢复由用户后续指令确定。

## 25. B60 池外线索局部过滤与当晚恢复（2026-09-10）

用户明确纠正：池外代码应直接排除，不能拖停整份报告；授权快修并继续今晚原任务。基线为部署的 B59 `c228c0f`，工作分支 main；对外版本不变、build 递增为 60。

- 标题结果在 fresh/cached decode 同样过滤池外 companyCodes，保留所有原文引用、状态与判断；请求内容、缓存身份和付费检查点不改写。
- 研究问题混合列表保留池内部分，纯池外问题与其查询／全文申请排除；映射、评估中的池外行排除。有效研究继续，证据和身份校验仍保留。
- 离线回归先复现旧代码 4 例失败，修复后目标套件 15 passed；真实旧 64 条 checkpoint 回放仅移除 1 个池外代码。真实 CLI 入队 → worker → failed/completed 检查点 → 正式恢复入口 → 原 task/scan 发布用例验证已付标题不重付，原 completed 记录逐列未变化。
- 本次部署仅更新代码和 wheel，不迁移数据库／参数，不改变 BYOK；新备份将保留本次试跑与用户模型设置。验证及回执见 [release-b60](v3.2.0-b54-evidence/release-b60/)。

发布门禁：1141 passed / 21 个既有 Polars warnings；双端正式签名归档通过，Mac 解包严格验签通过。无 Swift 源码变更。

- B60 源码 `03fb8613b0af60de98fd85bc8072456ed223d59f` 已提交推送 main，tag `v3.2.0-b60` 不可变；后端部署完成，85 表完全不变，pre/post DB SHA256 均为 `6157fcf9a01c6cb7e56fba88d3777ac00b8fa327139ce0c2b98c76d02996b817`。生产副本上全部 22 个旧 completed 标题检查点通过过滤后解码。
- 21:25:47 原 task/scan 经正式 recover-scan 恢复，冻结输入 hash `f9c9e0bd0b4a00513eb8886d2cac63f65165dd85c5453691e05f32d1d3ea5770`；恢复入口前后原付费检查点、计费与其余任务完全相同。专用单任务进程已运行，未启动常规 worker/timer。
- Mac B60 已用 ditto 换装并启动单实例，B59 可恢复备份 `/Users/linotsai/Lino/app_backups/Neckline-v3.2.0-build59-pre-b60-20260910.app`；iOS 签名归档就绪，无 IPA。

## 26. B61 截断恢复、状态同步与定向推送（2026-09-10）

用户明确完成标准：今晚这份报告完整生成、客户端可读且实际推送成功，才能结束。实际 B60 基线 `03fb861`。

- B60 恢复后 44 批标题全部完成，2,782 个去重标题中 no_value=2,205、uncertain=426、candidate=144、same_matter=6、correction_or_denial=1。标题累计 1,593,969 tokens。随后归并输出达到 32,768 tokens，输入 55,877、共 88,645 tokens，21:29:34 截断失败；旧标题与失败记录保留。
- B61 沿用批准的单次 JSON 修复次数与 token 容量：明确截断后精简输出并关闭扩展思考，将容量留给必要结果；不缩减必须审阅输入、不减少公司或捏造结论。旧 response_truncated 检查点经官方同任务恢复生成可追溯新尝试，旧标题复用。
- 未发布日报的读取状态采用同一扫描绑定的真实 task 状态，清除恢复中的旧失败展示，不在 GET 写数据库。定向通知派送只领取指定 notification_id，防止一并派发历史失败通知。
- 新增 4 项真实 CLI/worker/恢复/API 回归，旧代码复现失败；标题/正文截断自动精简恢复、旧截断恢复及 queued/running API 均通过。定向通知回归验证只发送目标且不重复投递。APNs 本地配置 ready，已登记 2 台设备，晚报推送偏好启用；未提前发送报告。

- B61 最终源码 `99a83b3e38027734b7d1d69d3622522609478132`：1147 passed / 21 个既有 warnings，macOS build、iOS Simulator build、iOS build-for-testing、双端签名归档与严格验签通过。生产副本 44 个付费标题检查点离线解码通过，新备份 SHA256 `364688bf3dc8bca04e57ad59cf1aa4d9327baecda3001943b8852636fdb4617a`，85 表保留。

- B61 已推送 main 并发布不可变 `v3.2.0-b61`，宁波本地/公网健康均为 B61；85 表部署前后不变。21:49:41 官方恢复入口返回原 task ID，44 个旧付费标题检查点及计费、旧任务摘要不变；专用 B61 进程继续运行，成功后定向发送本报告通知。Mac B61 安装完成并实际显示“任务已恢复”。

## 27. B62 标题协议修复反馈与付费结果保留（2026-09-10）

用户的报告完成／客户端可读／实际推送验收目标不变。B61 实际基线 `99a83b3`。

- B61 于 21:50:20 再次停在标题阶段：归并 HTTP 成功、JSON 完整，55k 级输入加输出共 59,955 tokens；业务校验留下 `title_protocol_invalid`。原实现只存笼统码且丢弃未通过校验的响应，无法从该记录确定具体字段；不得猜称为已证实的计数或引用错误。44 批标题仍保留，累计归并一截断一完整响应。
- B62 把标题输出校验错误纳入已批准的单次 JSON 修复，给模型返回具体固定校验约束；旧 generic 协议失败允许经正式同任务恢复处理。既定输入覆盖、来源、排序与更正保护要求保持。
- 未通过校验的已付结构化结果另存 DB 旁 `model-diagnostics`，目录 0700、文件 0600，包含输入摘要和未验证标记语义，绝不作为 completed 缓存或公开 API。后续授权恢复先按同一输入和当前严格校验尝试复用标题响应，只有仍不合格才调用模型；保留旧失败及账单。
- 精简恢复在后续协议修复里保持关闭扩展思考，避免第二步又恢复长篇思考。新增 3 项回归证明完整 JSON 错计数能纠正、重复错误保留付费结果、修复旧校验后直接复用保存的标题响应而不再外呼。目标套件 25 passed，全部离线。

- B62 最终离线全量 1151 passed / 21 个既有 warnings（179.55s），macOS/iOS 签名归档、严格验签及 Mac ZIP 解包验签通过。应用逻辑未改，双端 build 递增为 62。仅准备替换代码、保留数据库并继续同一晚报，发布准备不作为报告完成。

- B62 源码 `4500397c4c4b0ac18d6630ae6870d8f756927ff5` 已发布；85 表前后备份 SHA256 均为 `6f8b20bb1e616741e9c474adc8946393c24c32cf9128f14fd43f3c48ad7b9109`。22:11:24 官方入口恢复原任务，44 个付费标题和旧任务摘要不变。22:12:01 标题输出校验仍失败；两份原回复已私有保存。Mac 锁屏暂留 B61，B62 六项 Release 资产下载校验通过。

## 28. B63 标题统计和辅助归并局部容错（2026-09-10）

实际基线 B62 `4500397`。验收仍是今晚完整报告、客户端可读和实际推送。

- 已证实的原回复缺陷：`selectionComplete=true`、`reviewedCount=2782`（全部标题，参与者实际577）；18 行入选里第18行重复 i=1106 且理由为空；9 个归并中7个目标未入选。系统已持有准确计数，不应让此类统计和辅助关系挡住真实入选来源。
- B63 只接受参与数或已完整短筛总数这两个可证明的计数；保留显式完成声明。重复来源保留第一次判断；对未入选文章的辅助归并不建立关系、不增加入选文章，两端仍独立记录为未入选。已入选原文、原因、顺序不变，陌生引用和更正保护仍校验。
- 原付费回复在本地离线直接回放通过：17入选、2有效归并、558参与者未入选；未请求模型。匿名回归保留2782/577计数、全部原索引和归并拓扑，旧代码2红；修复后目标套件47 passed，并经过真实 CLI → worker → 原任务恢复 → 发布验证，复用保存响应时没有额外标题外呼。
- 按 self-improvement 将用户反复纠正的容错原则提升到项目 AGENTS；遵守项目单一证据记录，不另建并行日志。

- B63 最终源码 `5f229c0c5d1a926025b8156e52635391457fbe0c`，1157 passed / 21 个既有 warnings（161.39s），双端签名归档及严格验签、六项发布资产下载校验通过。不可变 tag `v3.2.0-b63` 已推送。首次部署前隔离验收脚本误将 API 终态错误码预期为内部校验码，实测为 `model_json_repair_exhausted`；仅修正验收脚本，生产代码和DB未动。未完成演练副本另存 `v3.2.0-b63-prepare-attempt1`，重新备份与隔离恢复演练通过。

- B63 生产发布完成，本地/公网健康B63；85表pre/post备份SHA256 `5dea6d3c9e656b60a6e9879bef4d1b892cf22cdbf3b85d458c62df743741f14f` 完全一致。22:26:11 官方CLI恢复同一任务，44个付费标题、旧任务、外呼账单摘要未变。22:26:41 已出现完成的归并检查点，外呼数未增加，证实线上直接复用保存结果；专用B63进程继续后续步骤并在成功后定向派送本报告。

- 22:29 实际进度：终检后13篇正文名单冻结、2,786条标题去向完整持久化；13篇正文理解全部完成，研究问题／检索规划及真实Tavily取证已开始。原全局归并未新增外呼；后续正文局部错误已通过原任务恢复继续，不把运行中的历史失败检查点当作整份报告终态。Mac仍锁屏，等待实际页面验收。

## 29. B64 证据引用局部处理与研究结果复用（2026-09-10）

实际基线 B63 `5f229c0`。用户要求仍是今晚完整报告、客户端可读和实际推送，不能以修复完成或恢复启动代替。

- B63已完成44批标题、13篇正文，并推进真实检索和事件研究；部分审读不断返回越界证据引用、改写原命题或重复申请已处理全文。B64局部剔除不可用引用/命题更新，保留有效判断；失去支持的状态回到未核实，受影响的已回答问题重新保留缺口，不将未知资料当作事实。
- 重复申请已fulfilled/rejected的同问题同来源全文不再新增外呼；真实未知来源仍不能成为证据。已保存私有审读回复仅在明确同任务恢复、相同输入摘要、当前typed解码和实时证据边界全部通过后复用；原失败和计费记录不覆盖。
- 恢复回归又确认一处输入身份问题：本地公司检索串的JSON字段顺序在持久化前后不同，会把相同研究输入认成新请求。序列化改为确定顺序，真实CLI/worker恢复测试验证不再为同一审读重复付费。
- 新回归旧代码5红；修复后首轮46通过。真实付费审读回复与失败时第20版研究快照离线配对，原错误“问题引用了未提供证据”复现；局部移除2项越界引用及1项重复全文申请后通过现有严格验证，保留partially_supported判断和2个待核问题，新增模型调用0。补充全文回归及最终目标套件43通过。
- 为代码切换先建立 `/opt/neckline/data/backups/v3.2.0-b63-predeploy/pre-b64-drain.db`，SHA256 `d0886cf6b08e15c0214b5ae8f8a7d79f2e73ad9c157f4e6f99cf52060ee01744`，关闭新外呼准入并等待在途调用全部结算。22:50:19原任务安全退出，所有started外呼/running检查点为0；开关随后重新打开，专用进程已退出，等待B64部署和正式同任务恢复，常规worker/timer保持停用。没有发布报告或发送APNs。
- 双端B64签名归档与严格验签、Mac ZIP解包验签通过。全量首轮1162通过、1个纯解码兼容测试失败；明确证据allowlist未提供时只做字段复原，实际运行仍必须使用显式证据包。最终全量正在运行，未声称最终通过。

- B64最终源码 `0c0a789d7c3cde0b0020400dacfe1dde7787438a`：1164 passed / 21个既有warnings（152.45s），双端签名归档/严格验签和Mac ZIP解包验签完成，正在部署。

- B64已部署，本地/公网健康B64；85表pre/post SHA256均为 `06dd67ca0fd537428c810b146b035a3ae7787c898747f894b0c1525ebba2f38b`。22:58:40官方CLI恢复原task，全部216个完成检查点SHA256 `c24a39f8c0841c2903253bd5e46885ed5a06639162db4c43d7b6f36fe94dc078` 和账单、旧任务摘要不变；专用B64进程继续并在成功后定向派送本报告。Mac仍锁屏，B64暂存包及B61恢复副本已验签，未冒充已安装。

- B64六项GitHub Release资产已重新下载，SHA256SUMS列出的五项全部匹配；23:05原任务进入首个公司比较，尚待最终报告和实际推送。

## 30. B65 公司比较局部过滤（2026-09-10）

实际基线 B64 `0c0a789`，目标仍是当晚完整报告和实际推送。B64继续产生有效正文审读，但首份比较在一家公司之外额外列出5家已排除公司，因“公司比较覆盖不完整或重复”被拒。

- B65仅保留明确比较名单内公司，对重复公司保留首次判断；名单内缺失判断仍需模型补齐，不填造评价。原6行拓扑（1主判断、5额外excluded）离线重现并过滤通过，最终结果和证据边界亦通过，新增外呼0。
- 同输入已付比较回复纳入受控恢复复用；真实CLI生产者→worker重现旧错后，经官方同task恢复直接使用保存结果，未重跑标题、正文或公司比较。新回归旧代码3红，目标48 passed。
- B64切换前先保存 `pre-b65-drain.db`（位于B64备份目录），SHA256 `575d86cdec9d8ef85395d9fbef1b90c33d9253538a1abbb6d55cceaf30c77572`；关闭新外呼准入并等在途结算后，23:12确认全部started/running为0、专用进程inactive，随后重新打开开关。常规worker/timer仍停用。实际API错误理由为`incomplete`，按观察值验收。

- B65最终离线全量1169 passed / 21个既有warnings（160.78s），双端签名归档/严格验签及Mac ZIP解包验签通过。双端仅build和图标资源标识递增，App Swift代码未改；准备上线并继续原任务。
