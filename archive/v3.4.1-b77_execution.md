# Neckline 3.4.1 / Build 77 执行记录

## 1. 目标、授权与基线

2026-09-20用户授权：“走工作流修复，3.4.1（77）；build结束后不要过度测试；走起”。本轮修复前版全局review的10项确认问题，完成必要本地验证，随后仅复查修复及其直接影响；无产品待决定事项。Planner只维护本记录和主Plan，主会话直接推进builder，不索要重复开工确认。

- 双端和后端目标：3.4.1 / Build77。内部Schema9、公开报告Schema8、通知Schema2保持；不作数据迁移。
- HEAD/main：`606f41777448d09beb727be19b40c6b5e4d0af39`。B76源码和验收文件大量未提交／未跟踪，必须保留，不从HEAD重建工作区。
- 修复起点：B76已审不可变tree `ece55483d8410620d3df5f4b77f1576af97fb9de`，覆盖当时全部非忽略源码。其后新增前版review记录与主Plan状态不算B77实现。
- 生产起点：后端`0a834a9c898b634c26ecc3f29a0b23c987befcbb`（`v3.3.0-b75`）；双端`988a7b5dc3c943c99167daba38c16103016eadf3`（B69），其App到B75无差异。本轮未部署，未来发布审查仍须覆盖生产到最终版本累计差异。
- 原findings、精确旧行号及复现出处：[v3.4.0-b76_execution.md 第12节](v3.4.0-b76_execution.md)。证据根：`/Users/linotsai/Lino/releases/Neckline/v3.4.0-b76-20260916/evidence/review-20260920/`。Delivery复现摘要为已捕获输出重建，数据库已清，不能称仍持有原现场。
- 本轮产物根：`/Users/linotsai/Lino/releases/Neckline/v3.4.1-b77-20260920/`；`evidence/baseline.json`由主会话固定基线，临时物归`temporary/backend`或`temporary/app`，必要QA沿用项目规定的`/tmp/neckline-v3-qa/{macos,ios}`。

## 2. 事实、技术决定与范围

已知：B76全套门禁没有覆盖本次10个断点；真实适配链已复现成功回执恢复失败。尚未证明：新协议真实模型质量、整份正式报告的实际成本／耗时／送达。本轮离线修复不能补成生产成功记录。

1. **保留B76协议身份。** 新建B77任务继续冻结`runtimeContract={reportDelivery:"k10-report-delivery-3.4.0-b76",research:"k10-research-3.4.0-b76"}`，字段恰为这两项。77修复既有协议实现，不改wire prompt、模型／端点、策略语义或Schema；不为营销版本机械换协议字符串，不改已冻结payload。B76本地任务可在原授权及原期限内继续消费原检查点；B75缺失／未知／额外字段协议始终不执行、不重绑。将来真改模型输入或协议必须另立身份，不能套此兼容结论。
2. **本地对账优先于未知判定，但没有回执绝不重发。** 恢复只能从同task、原冻结绑定与精确wire对应的已存成功回执重建当前解析结果。模型调用边界必须具备显式“只读回执、禁止发请求”的路径，不能删除running检查后走普通可联网call。
3. **阻塞按父报告和供应商绑定传播。** 持久余额／权限失败阻止同父报告、同冻结供应商执行绑定的后续新外呼。已经准入的请求只结算；不同父报告／不同绑定不受连带永久熔断。模型服务和Tavily按可确定的服务身份分别处理，不能把一方余额错误猜成另一方失效。
4. **新建和重排必须同事务过control。** 只读幂等回放可以返回已有身份；不得以回放为由补binding、改协议、创建outbox或重置状态。需要新付费任务的留意动作若暂停则整体409并回滚，不留下“已留意但分析未创建”的半成功。纯不触发付费的既有用户动作保留原规则。
5. **稳定目标判重，不做模糊阈值。** 用必要问题、已验证purpose／targetRefs、冻结证据版本和明确来源定位确定路径身份；query、intent、pathId或newPathReason换措辞不能成为新资格。真实证据增量、不同且可验证来源定位、首次计划中已声明而尚未执行的独立路径必须保留。无可执行增量时诚实收口，不伪造answered。
6. **有限验收。** 每finding保留最小真实回归，一次合并相关后端门禁；原生仅必要三项构建、受影响DTO／展示测试和双端窄验。修后一次有限复查不新增全局测试计划。

用户网页操作清单：无。无生产部署、真实供应商／APNs、恢复定时、停止／重跑／延期旧任务或更改生产control授权。API／worker可能仍有活动；不重复轮询、不声称全部费用停止。异地备份保持退役，旧报告保持删除，B74/B75及清报告恢复点按原规则保留。Xcode旧问题／Apple环境清理及约1.42GiB旧废纸篓限制不在本轮。

## 3. 后端修复契约

### R01 / P1：成功回执到上层checkpoint的恢复

定位：`pipeline._CheckpointedDiscoveryModel`的running／unknown拦截；`model_execution._reserve`；`metering.MeteredProvider`与`store`原wire回执读取。使用前版research复现的真实`DeepSeekDiscoveryModel → advance_merged_research → execute_model_operation → MeteredProvider`链。

- 对账至少核实task、原端点／模型与请求SHA、外呼attempt所属task／stage、回执payload SHA与相符已知ledger结果；沿用当前可见来源manifest及解析约束。不得用“存在某条success”或跨task／改输入命中证明成功。
- 上层running／unknown仅允许进入显式本地恢复路径。它重新构造原请求身份，在无外呼能力的模式下查找并验证回执，运行现有完整领域validator，再补齐单步结果与合并研究派生。派生仍同事务全有或全无；不能部分写plan_gaps／plan_queries或assess_evidence／close_research。
- 不增加网络attempt、usage或receipt行；不能通过先清running／unknown制造新请求资格。缺回执、hash不符、ledger不符、无法重放的回执继续阻塞并保留原证据；本地重验不通过保留准确失败，不重发／改模型／改输入求通过。
- 保持租约、原deadline和control栅栏。暂停期间可结算已准入回复／写其checkpoint，但不能继续下一研究步骤或公开报告。真正started／unknown且没有可信回执仍不可terminal partial。
- 验收：在成功receipt提交后、上层completed写入前注入中断，真实worker恢复得到原结果；HTTP=1、external成功=1、receipt=1、usage不重复、派生恰一组。对缺失／损坏／输入不匹配回执断言HTTP不增加且仍阻塞；最小参数化即可，不另跑全规模。

### R02 / P1 与 R09 / P2：兄弟任务阻塞及错误保真

定位：`store.begin_external_attempt/_model_attempt_admission`、晨报parent/child编排、`metering`terminal映射、`analysis._call`安全码白名单。

- 在外呼准入写事务内解析报告归属：晨报child的已存`parentScanId`和父scan任务关系须可验证；对同一报告的父／兄弟任务，比较已冻结执行绑定（config id/revision/content SHA及可判定的供应商服务身份），不读取当前全局配置替代冻结身份，不把Key写入身份／日志。
- 首个余额或鉴权终态提交后，同作用域后续新attempt返回terminal及原安全reason，不写started、不发POST；并发边界前已准入的请求允许一次结算。先在ledger共用边界保证安全，编排不能靠停掉一个child就假定全父任务已停。
- 被阻塞的child不留永远queued／running；记录真实阻塞原因并让父报告按B76既定规则收口：已有可精确复用的完整排序才可零外呼partial，否则failed；未决在途仍先等待结算。不要为该修复新增跨任务全局熔断表或新恢复授权机制。
- 保留已有显式、原任务范围的重试授权边界；兄弟重启、普通resume和新建子task不得消除源阻塞。不能给同作用域其他任务扩发授权。
- 安全reason逐层映射：`provider_authorization_failed`保持权限／凭据失败，`insufficient_balance`保持余额不足；未知错误走已有安全通用码，绝不转抄供应商原文／地址／凭据。
- 验收：真实两个同parent同绑定morning_review，首个鉴权失败后兄弟0POST；不同parent及不同绑定各允许正常准入，额度案例可同表参数化。覆盖故障提交与新准入事务顺序、已准入结算；经MeteredProvider及analysis持久化／API可见reason正确。

### R03 / P2 与 R04 / P2：所有producer／retry的事务准入

定位：`store.observe_company_window`、追加分析、`_insert_task/enqueue_task`、`retry_task`、晨报child及CLI／API调用处。检查本次相关新建／重排入口，选择共用数据库事务gate，不能只补前端按钮或只靠caller可选参数。

- 新建付费任务、后续分析、晨报child、显式retry和相关恢复重排，在同一写事务确认control open再修改任务／观察／版本／binding／checkpoint／retry schedule／outbox；closed或缺control安全拒绝。既有claim和ledger gate继续保留。
- 幂等键已命中且输入一致、完全不变更任务的回放可返回原身份；不能补binding或增建付费副作用。冲突的幂等键仍409；不替用户换新task。
- 通用retry在任何修改前核验原payload的严格B76 runtimeContract；缺失、未知、错误值、额外字段均409，不自动补协议。错误响应沿用现有409 envelope；已知paused明确中文原因，旧协议明确不能重试且不泄露冻结配置。校验先于为旧任务获取／附加现行binding。
- 合法B76显式retry保持原task、冻结输入／期限／attempt边界和已付费成果；仍须未正式发布、状态及expectedAttemptCount匹配。409不清error、lease、retry schedule或授权字段。
- 验收：真实selection keep API、追加分析API、CLI enqueue、父handler生成child及通用retry；closed全部不增加任务／binding／outbox／观察动作。用返回task ID测试open合法路径及严格worker；旧协议retry409前后行内容一致。保留已有并发close边界测试，按新增入口补最小覆盖，不手工替被测producer绑定配置。

### R05 / P2：等价研究路径收口

定位：`research_context.route_identity/collapse_repeated_work`及研究执行保存的`questionDependency`、路径消费端；修正`test_v321_context.py`把改写query当新搜索的旧断言。

- identity由系统生成：必要问题稳定身份＋purposeKind＋排序去重targetRefs＋原证据dependency版本＋可核验来源／定位。只有已显示／验证的source ref、内容版本、明确URL／文档定位等能够代表来源增量；不能让任意targetSource/newPathReason文字自证新路径。
- 使用已执行路径当时保存的dependency，不用今天的新证据倒灌旧路径。相同内容转载不能仅因新文档ID获得资格；有可靠content hash时按已有来源事实合并，没有可靠依据时不猜相同。
- 同一目标且依赖不变，query/intent/pathId改写仍去重；query文本保留在请求和审计中，但不是新资格。首次计划的独立来源／定位路径各可执行一次；换实际证据版本、出现必要新来源定位可继续。旧路径缺结构信息时不得回填虚构purpose/target，也不能凭文字重复开放，保留可证明的历史与安全未知。
- **施工期Planner细化（已采纳）：** 不依赖模型新增字段。由现有targetSource和query中的明确URL／site域名冻结首个接受计划的来源路径集合；缺URL时可保留不同规范化targetSource为首次来源声明，注明这只证明声明身份，不代表来源事实已验证。同源改query合并，不同已声明来源各可执行一次；续轮换字不能扩大集合，只消费未执行项或凭真实可见证据增量／明确新定位继续。集合沿已有stage结果及dependency持久化，不修改prompt、冻结协议或引入相似度阈值。
- 验收：真实worker仍执行首条路径，后续只改措辞0新增search并收口；同一问题真实证据revision变更、不同明确来源定位或未执行的既定独立路径各保留。验证合并回复和重启恢复不会因为identity变化重做已执行搜索。无模糊相似阈值／新模型裁判／总配额。

### R06 / P2：通知补偿游标

定位：`notification_runtime.reconcile_terminal_notifications`的(updated_at,task_id)游标参数。SQL条件保持`updated_at > ? OR (updated_at = ? AND task_id > ?)`，绑定必须为(time,time,id)。

验收直接保留原默认limit100边界：同秒101个失败scan，前100已有outbox，第101缺失；维护补齐第101一次，再跑新增0。兼顾相邻时间行；不降低分页大小冒充原场景，不live APNs，不改通知幂等键或重复发旧通知。

## 4. App、DTO与版本契约

### R07 / P2 与 R10 / P3：旧失败和实际拒绝码

- `k10OpportunityEmptyPresentation`优先保留not_configured、真实读取失败规则；delivery缺失时依reportStatus识别failed终态，使用实际responseReason，不显示“正式报告完成后…”或推断complete。没有delivery的旧报告仍不伪造完整度。
- `k10DeliveryGapReasonText`映射生产实际`content_policy_refused`；已有旧别名可兼容，不能只修测试为不存在的码。保留安全通用错误回退。
- 基于实际旧失败DTO断言具体原因可见；基于保留的实际partial DTO断言拒绝中文。现有complete零卡／partial零卡／已结束历史／仅更新晨报的断言保留，必要受影响组一次跑完。
- 双端原生各窄验旧失败页与带拒绝原因的缺口展示，复用既有合格参考风格；不重录完整报告矩阵、不改导航／设计。

### R08 / P2：默认测试不依赖个人release目录

- 将前版`evidence/api`实际确定性JSON中现有4项测试需要的文件固定到版本控制下的App测试资源，测试用`Bundle(for:)`等target资源定位。共享scheme默认运行不要求`NK_B76_RELEASE_ROOT`；删掉强制个人绝对路径，不改成skip，也不只用手写DTO代替真实API输出。
- 在测试资源附简短manifest：原生成版本／场景／时间、来源路径与SHA、是否仅synthetic或旧协议投影。复用原始JSON不重新跑全规模；只保留fixture所需无敏感内容。不要把原始生产回执、数据库或凭据放进资源。
- 资源与xcodegen项目保持一致，干净检出且没有release目录／环境变量可解码并运行默认这4项测试。保留既有不可变B69 decoder断言的真实范围，不将B77回归改名冒充重新生产采样。
- Backend全规模fixture的研究仓绝对路径依赖已知，但不扩到全套fixture治理；本轮新增最小回归应自包含或复用可移植小fixture，不能再引入个人release目录硬依赖。

### 版本一致性

后端`neckline.__version__`／API VERSION及RELEASE_SET目标3.4.1／v3.4.1-b77；App `project.yml`的版本／build、独立`AppIconV341B77`资产名及生成pbxproj一致。复用现有图稿，不生成新设计；用xcodegen生成项目。旧注释指向不存在的`test_v250_s12_app_guard`无需复活，现行版本门禁为`test_client_version_governance.py`。B76 runtimeContract与fixture历史文件名保留原事实，不全局替换76字符串。

## 5. 有限验证与结束条件

先在各修复实现时运行必要回归；成功结果按最终相关源码记录。全部修改汇合后仅一次相关后端门禁，具体node清单由backend builder在开测前冻结并记录到本节。覆盖R01–R06/R09新增回归、受影响旧模型执行／control／research／notification及版本治理测试；不默认重复1610项全套，更不把1,089公司×多报告组合再跑一遍。

App最后Swift源码确定后，项目强制三项各一次：macOS build、generic iOS Simulator build、generic iOS Simulator build-for-testing；显式`NK_BUNDLE_SUFFIX=.qa`及独立／已协调derivedData目录。沿用所得产物运行受影响K10 DTO／空态测试一组，无`NK_B76_RELEASE_ROOT`。最终必要双端窄验只运行各一QA实例，安装前核验QA bundle ID及现有主模拟器清单，始终禁用持久凭据／使用最小进程环境和loopback，不启动production client。

- 各R项通过其原真实断点，不能把Mock放在故障边界上方、手工替producer补binding、缩小默认分页、改成skip或删除业务断言。修复影响已验证源码后，只重跑被影响部分；新失败先说明根因及范围，不盲增门禁。
- 业务时间显式传入真实handler，lease时间一致；正常terminal测试不留started／running／unknown外部attempt，专门未知结果测试明确保留阻塞作为预期。
- 所有Backend测试用隔离DB及网络拒绝；登记自有TMPDIR／pytest basetemp／cache，`tmp_path_retention_policy=failed`。不live provider、APNs、SSH、生产API或额外余额／权限探测。
- 修复完成一次有限复查：只审B76已审tree到B77最终固定源码的这10项差异和直接依赖，复用已记录验证；不新开全局audit、默认full-suite或原生重建。需要补测只能对应具体新缺口，不能为增加信心机械重复。复查发现的范围内问题修复后只复核其影响。
- 结束交付必须区分：10项修复情况、准确源码范围与结果、未覆盖生产事实、未发布、资源回收或受限项。没有P1/P2已确认未解决项才可称本轮修复完成；不因此自动宣布可部署。

## 6. 分工、顺序与回滚

- **Backend builder独占整个`Backend/`**，包括R01–R06/R09、后端版本和版本治理测试；store／pipeline／metering／research互相牵连，不拆多人同时改。先从前版research最小复现建立R01回归，形成严格本地恢复边界，再处理R02/R09、R03/R04、R05/R06。不改App、AGENTS或主Plan。
- **App builder独占整个`App/`**，包括R07/R08/R10、资源、版本、xcodegen及三项构建／窄验；先核实并固定原实际DTO为测试资源，再修展示及版本。与backend约定错误码和Schema保持本记录，不同时改后端测试。
- **主会话**协调产物／最终相关门禁／有限复查，收录builder简短证据并替换主Plan当前状态；本记录是唯一B77版本记录，避免多人同时写。AGENTS如需更新只记新增成熟约束，不把目标版本写成已上线。
- Builder均不是唯一施工者，不回退他人成果；新增意外契约或Schema需求交回主会话判断，不擅自扩建。第一步读本记录R01或R08、核对B76tree对应差异和自有临时目录后直接施工。
- 当前回滚仅本地：相对已审tree精确撤销有问题的B77 hunk／测试资源变更，保留B76全部成果与同期他人改动；禁止reset到HEAD、checkout整目录或覆盖数据库。若后续发版出现风险，重新按生产活动／原协议／新写情况制定部署回滚，不能沿用旧快照覆盖。

## 7. 资源与施工证据（由主会话续写）

规划与实施均未操作生产。Backend builder独占后端施工；App builder完成代码、构建与测试后遇到模型容量错误，主会话接手已有产物核验及回收，没有重跑已通过构建。

### 7.1 App 已完成及验证限制

- App固定修复tree：`861435c06ccf22c779e2013cb3ddb80d9f3d54ef`，只更新基线中的App目录；其余目录仍是B76，不能作为最终Backend版本。`evidence/app/source-hashes.json`固定当前App源码与资源。
- R07/R10：旧failed且无delivery时保留失败终态／具体原因；实际`content_policy_refused`映射“内容被供应商拒绝”。R08：10个保留的实际隔离API JSON固定进测试Bundle，来源及SHA逐一与历史文件核对（`evidence/app-resource-provenance-check.json`）；无新增报告生成或外部目录运行依赖。
- 版本3.4.1／77、AppIconV341B77、xcodegen项目一致。macOS build、iOS Simulator build、iOS build-for-testing三项通过。首轮6项测试因Bundle资源平铺导致定位失败；仅修测试加载器后增量build-for-testing及原6项测试通过，0失败／0跳过。初次三构建后未改App运行源码，无需重建不受测试助手影响的App目标。
- 证据根：`/Users/linotsai/Lino/releases/Neckline/v3.4.1-b77-20260920/evidence/app/`，保留构建／测试日志、两轮测试摘要、实际截图、资源及源码hash。最终测试运行于既有“主模拟器”iOS26.5，未使用`NK_B76_RELEASE_ROOT`。
- 原生窄验：Mac旧failed“今天没跑成／余额不足，已停止本次任务”与展开缺口“内容被供应商拒绝”均可见。iOS partial页面正常渲染，但截图缺口未展开；旧failed截图仅白屏，不能算通过。接手时CUA无法连接Simulator窗口（Invalid app），因此按用户限制不追加构建／测试；保留4张代表截图并明确**iOS两项具体文案的视觉验收未完成**，DTO／展示函数6项测试通过不替代这一限制。
- Apple支持检查仅只读，`pending_devices=["iPhone18,4"]`；未prepare／apply，不处理暂停的旧Xcode问题。
- QA与loopback进程停止，仅移除模拟器QA应用，保留生产bundle。9,354项本轮文件按UID、hash及无打开句柄精确回收，包括3个构建目录、QA副本、两项本轮xcresult及临时脚本；已核验目录占用 **773,320,704字节 → 0**。详见`evidence/app/cleanup.json`和精确manifest。原生截图／日志留证，无远端产物；旧废纸篓约1.42GiB限制未处理。

### 7.2 后端实施与聚焦门禁

为并行收口，主会话接手R03/R04的`_insert_task/enqueue_task/retry_task`与独立回归文件：所有付费kind公共入口强制control，暂停只允许不补改绑定的原身份回放；所有通用retry调用均校验原B76协议及有效原绑定，不能把无效任务重新排入队列。`tests/test_v341_producer_gates.py` 18项通过（0.43s，源码内真实producer及strict worker准入），覆盖closed／missing、旧协议及合法原task恢复；证据`evidence/producer-gates/targeted.log`，临时目录`temporary/producer-gates`已由主会话按UID／hash／无句柄核验清理，16,384字节→0；证据`evidence/producer-gates/cleanup.json`。此后store交还Backend builder，该文件已纳入33项相关门禁。 主会话另补`tests/test_v341_authorization_reason.py`：实际API创建→MeteredProvider模拟401→生产analysis handler／strict worker→持久化→实际API返回，1项通过（1.32s）；鉴权原因保真、仅1请求、无反方调用。该测试临时物16,384字节→0，证据在`evidence/authorization-reason/`，已纳入33项相关门禁。

施工核对发现并退回修正：R05首稿读取未进入QueryPath解码／持久化的locator字段，R03首稿仍依赖enqueue调用方的可选control开关；两者均不能作为完成证据。随后R01–R06/R09首轮修复及真实CLI／production worker回执恢复进入33项聚焦门禁并通过，准确范围见7.3节。App限量复查完成：原reviewer只读审查固定tree中的R07/R08/R10及版本／资源，未发现新增可报告问题；未重跑测试或构建，iOS视觉限制仍保留。后端限量复查及补修见7.4节；未发布。

### 7.3 首轮固定源码与有限复查

主会话固定完整非忽略源码tree `6ae75e3e450ab95a54571ff3f8180874f8902be1`，起点为B76已审tree；包含本轮暂存／未暂存／未跟踪源码，未动实际Git index或HEAD。`evidence/final-review-scope.json`与`final-source-tree.txt`保存范围，App子树与已审`861435c0…`一致。原两位后端reviewer仅分别复查R01/R05及R02/R03/R04/R06/R09修复，不新增测试／构建或全局审计。

Backend builder报告聚焦门禁33项通过（13.42s），晨报402用例收紧实际API断言后单项通过（7.56s）。该402样例是无B76 delivery manifest的既有报告格式，只证明实际API partial／incompleteReviews原因和父子终态；不能称B76部分交付协议完整验收。精确node及测试过程保存在`evidence/backend/focused-gate.md`（捕获输出的忠实摘要，非原始重定向日志）。首轮曾32通过／1条父状态断言失败，修正并收紧业务断言后通过；未重跑历史1610项。自有Backend临时目录已核验UID／hash／无句柄并清理，33,296KiB→0，证据为`evidence/backend/cleanup-*.json`；主会话另回收两项自有源码测试缓存49,152字节→0。6ae是首轮候选，后续R01／R05补修另固定版本，不沿用旧tree冒充最终已审。

### 7.4 限量复查发现及修复

App复查无新增；后端R02/R03/R04/R06/R09及版本复查无新增。研究复查在`6ae75e3e…`确认三项P2：R01不可重放回执进入普通失败循环、改变running并虚增上层network次数；R05旧路径用最新question倒灌冻结依赖；R05未用可靠contentSha，同内容新documentId可重新获搜索资格。这些并非33项绿测已覆盖，仍须关闭后才能交付。

主会话R01补修只改pipeline：missing／invalid／unreplayable均保持原行blocked；任何单次receipt-only对账都退出网络／repair循环，合法JSON／领域校验失败只保存一次。`tests/test_v341_receipt_review.py`新增semantic-only／tool_calls／invalid-json三例，加原成功恢复一例，**4 passed／0.86s**；验证原HTTP仅一次、receipt／external行不变、无法重放时checkpoint整行不变、真实本地校验失败不虚增次数。R01专属不可变tree `b54c81a35addff550d325fedb27c954fd22a1e23`（基于6ae，只更新pipeline及该测试）经原reviewer只读复核已闭环，无新增问题，未重复测试。证据`evidence/review-r01/`；临时`temporary/review-r01`的798项文件已按UID／hash／无句柄核验回收，20,553,728字节→0，源码及简短证据保留。

Backend builder只补R05及对应最小回归，使用`temporary/review-r05`；不重跑33项或App构建。运行源码固定tree `3cff9c741befd5d8486101cbca2dc7473d109b12`的窄复核进一步发现同一修复的迁移缺口：历史ref格式dependency与新content-hash格式直接比较不等，升级后可能重新准入同义搜索。已交builder用历史dependency自身不可变ref读取已存hash并在比较前归一化，保留原持久证据，不借当前question；原3个精确节点补强为真实解码→持久化→恢复→消费链路。补修后原3节点最终**3 passed／0.72s**，详见`evidence/backend/review-r05-results.md`。前一轮3 passed／0.62s未覆盖完整恢复链路，不作为最终证据；两项回归随后改为真实解码、Schema9 stage写入、read_research_state恢复和consumer判断。原reviewer只读确认迁移缺口闭环，未重跑测试。R05自有临时物在测试自动回收后，清理前仅9个空目录、逻辑／分配均0字节，核验UID／无句柄后逐项rmdir，清理后根不存在；详见`review-r05-cleanup-pre.json`及`-post.json`。


### 7.5 本轮最终交付

- 最终App／Backend固定源码tree：`0cc69076e0b0b4bbfabed0ed528e3258a57fc634`，相对B76已审tree包含全部暂存／未暂存／未跟踪非忽略源码。HEAD及实际index未变，未提交／推送／打标签。最终复核App与已审`861435c0…`、R01与已审`b54c81a3…`完全一致；其余后端相对首轮候选仅R05两文件及回归有补修。
- 原10项全部采纳并关闭：R01回执断点；R02同绑定供应商阻塞；R03暂停入口；R04旧协议重试；R05同义／同内容重复搜索与旧依赖恢复；R06默认通知分页；R07旧失败展示；R08Bundle实际DTO资源；R09权限原因保真；R10真实内容拒绝文案。有限复查发现的R01／R05边界与迁移问题均已闭环，最终指定范围未发现可报告问题。
- 验证采用已有33项聚焦门禁、402单项收紧、R01四项及R05三项，App三项构建与最终6项测试；有具体新缺口才补受影响用例，未重跑1610项或整份报告矩阵。独立复查全程只读、未追加测试。`evidence/repair-review-summary.json`与`final-review-scope.json`区分各已审固定版本。
- 资源最终核验：`temporary`及`/tmp/neckline-v3-qa/{macos,ios}`均0文件／0文件占用／0句柄，最终App／Backend源码仍匹配固定tree；见`evidence/final-closure.json`。仅保留回归源码、必要构建／测试摘要、代表截图、来源与清理清单。无远端产物、无生产操作；旧B76废纸篓约1.42GiB限制仍未处理。
- 剩余验证限制：iOS两项具体文案视觉验收未完成；Apple只读检查`iPhone18,4`待处理。402旧fixture不能充当B76 delivery manifest验收，Tavily第二兄弟请求未独立实测（共享阻塞helper已审）。真实模型质量、全量正式成本／耗时及报告实际送达未验证，不能声称真实报告已跑通或费用已停止。
- 本轮本地修复结束，线上仍B75／客户端B69，四timer与Codex心跳继续停用。未恢复／停止／延期／替换旧task，未部分发布旧报告、重复推送或覆盖生产库。后续发布需按线上版本到最终版本累计审查，不能沿用本次补丁复查冒充发版通过。


## 8. 2026-09-20 完整独立 review（仅审查）

用户在本地修复交付后要求“先做一次完整的独立review”，本轮范围重新扩至实际线上到B77的完整累计差异，不沿用前述十项补丁复查代替。后端起点`0a834a9c898b634c26ecc3f29a0b23c987befcbb`、客户端起点`988a7b5dc3c943c99167daba38c16103016eadf3`；最终不可变tree `60a0e67adc800e90eff38852eb69b39c91dc9d58`，134文件／15,554插入／1,051删除（包含源码、资源、测试与记录）。App／Backend内容与前次0cc69076完全一致，文档为本轮开审前状态。包含全部暂存／未暂存／未跟踪非忽略文件，实际index／HEAD未改。

三位未参与施工的新reviewer分别负责研究／计费、任务／持久化／报告API、App／通知；主会话核查新增导出与离线验证脚本及覆盖交界。优先静态审查，必要具体疑点才最小隔离复现，不重跑全套、全规模报告或App构建，不真实外呼／APNs／SSH／生产读写，不改变调度或旧任务。用户本轮只要求review，发现问题仅报告。

范围与证据在`/Users/linotsai/Lino/releases/Neckline/v3.4.1-b77-20260920/evidence/global-review/`，临时物仅使用同版本`temporary/global-review/`。完整结果如下；不另建平行审查台账。


### 8.1 Findings：2项P1、8项P2，未实施修复

- **G01／P1：暂停被保存为失败报告且重开后不会续领。** `Backend/neckline/k10/worker.py:260`。真实CLI任务领取后、handler前关闭control；或执行中暂停时，task failed/paused、scan/report failed，重新open后worker不领取原task；安全暂停被当成执行失败。修复方向：同事务保留非终态/归队、checkpoint和deadline，暂停不得调用失败报告投影。归属：B75已有任务终态化，B76新增failed delivery投影加深影响。 证据：`evidence/global-review/pause-after-claim-result.json`。
- **G02／P1：Tavily已计费成功响应在写库断点后无法恢复。** `Backend/neckline/k10/verification.py:383`。成功HTTP和attempt succeeded结算之后，文档/检查点写入之前退出或写错时，原始成功回复未留receipt，恢复只能报outcome_unknown，已付费材料不能本地恢复。修复方向：响应回执与结算原子保存，再由回执幂等派生文档和checkpoint，禁止重复POST。归属：B75既有缺陷，非B76/B77新增回归。
- **G03／P2：版本hash误作正文hash，转载仍可能触发重复研究。** `Backend/neckline/k10/research_runtime.py:199`。相同正文从不同URL/sourceRef/metadata入库时，contentSha256包含非正文信息，形成不同dependency；手工同hash测试未覆盖生产writer。修复方向：使用可靠规范正文identity并兼顾旧记录；回归经真实writer。归属：B77 R05新误用既有存储hash语义。
- **G04／P2：新可见定位依赖真实输出契约未声明的字段。** `Backend/neckline/k10/research_context.py:658`。旧路径已消耗，同一knownEvidence ref经local read出现新可见locator，正常回复仅在query/targetSource引用时，sourceLocator未在outputContract声明，也未由系统派生，合法路径仍被判重复；首次来源和新revision不受影响。修复方向：保持prompt契约，由系统按已验证可见ref/location派生绑定元数据。归属：B77 R05新定位准入未兑现。
- **G05／P2：成功恢复报告仍保留旧失败文案。** `Backend/neckline/k10/v2_store.py:182`。同一未公开failed报告合法替换为complete/partial时，API completed/complete且delivery.gaps空，coverageGaps仍称公开前终止，B69和B77均可显示矛盾。修复方向：替换时去掉旧delivery派生缺口，保留独立业务缺口和新manifest缺口。归属：B76新增delivery替换逻辑。 证据：`evidence/global-review/gap-replacement-result.json`。
- **G06／P2：暂停期间maintenance仍新增行情/评估任务。** `Backend/neckline/k10/worker.py:376`。常驻worker初始open、单轮claim前后关闭control，随后maintenance执行时，closed状态新增2行情采集+1评估queued任务，恢复后可领取；复现无外呼。修复方向：两种kind的enqueue/retry事务强制control，worker辅助跳过相应maintenance。归属：B75旧路径，B76/B77统一control契约未覆盖。 证据：`evidence/global-review/paused-maintenance-result.json`。
- **G07／P2：旧无绑定分析可创建不能执行的新追问。** `Backend/neckline/k10/store.py:2083`。合法旧库初始分析已完成但缺execution binding，直接POST followup时，API200、新queued task和request各1、binding0，后续worker必not_configured。修复方向：创建前事务校验原父binding，缺失409零写，不能擅自绑定当前参数。归属：B75条件绑定旧缺口，B77control修复未覆盖。 证据：`evidence/global-review/unbound-followup-result.json`。
- **G08／P2：晨间更新卡配上晚报完整状态并隐藏晨报缺口。** `App/Neckline/Views/OpportunitiesView.swift:14`。晚报complete，关联晨报partial且updatedCards替换晚间主页卡时，时间/内容来自晨报，完整度和缺口仍取晚报，用户看不到晨报执行缺口和未完成复核。修复方向：更新卡显式使用晨报交付上下文，或同时明确两份报告的状态。归属：B76新delivery UI未覆盖已有晨间替换展示。
- **G09／P2：报告通知不能定位其对应报告/时段。** `Backend/neckline/k10/notifications.py:628`。冷启动点击晨报通知，或新报告出现后点击旧报告通知时，reportId/windowKind未进入payload，客户端只refresh latest且不切时段，可能展示另一报告。修复方向：增量携带并解析reportId/windowKind，按现有reports/{id}读取对应报告。归属：B75旧深链结构，B76保持同report契约未兑现。
- **G10／P2：整报失败页隐藏实际可操作的供应商原因。** `App/Neckline/Views/K10Presentation.swift:262`。delivery存在且reasonCode为insufficient_balance/provider_authorization_failed/rate_limited/provider_call_failed时，安全envelope reason被隐藏，缺口映射不含实际码，只显示通用消息/英文内部码。修复方向：映射真实安全码，保留不重复的envelope安全说明。归属：B76 delivery展示分支与实际错误码不一致。

### 8.2 覆盖、验证与资源收口

- 三位新reviewer独立检查完整累计变更，未参与此前方案／施工，也未沿用旧复查结论。研究／计费、任务／持久化／报告API、App／通知分区全部交回；主会话额外查六个新增导出／matcher／validator／loopback脚本、版本与交界。脚本未确认新增行为缺陷；一处fixture行尾空白仅记检查结果，不列finding。
- 主会话只针对4个具体疑点进行最小隔离复现：真实CLI→暂停后worker→报告API；publisher替换→API；真实worker loop→maintenance；旧持久态→追问POST。全部网络连接被拒绝，实际0外呼。第二项只证明publication边界，不冒充完整同任务恢复；第四项未启动新增追问worker。暂停首例已执行成功，但最后摘要误读Task不存在的stage属性报错，随后仅读同一DB补证，未重建任务或重跑handler；准确过程写在该JSON。
- 未重跑pytest全套、全规模报告、App构建或视觉；本轮未新增真实provider/APNs/生产API/SSH／timer/control操作。Tavily回执缺口、内容身份、新定位、三项App/通知问题采用静态调用链证据；不冒称动态端到端验证。已有真实整报未验证与iOS两项文案视觉限制继续保留。
- 所有本轮复现进程结束后，逐文件核对UID/hash、根目录无句柄，回收5项临时文件及空目录，分配占用**28,274,688字节→0**。TMPDIR限定本轮根；无远端／原生临时物。保留4个最小复现脚本、结果、范围与清理清单，未保留DB。`evidence/global-review/cleanup.json`保存精确清单。
- 当前源码仍为固定tree `60a0e67…`的App／Backend内容，实际index／HEAD不变；本轮只更新主Plan、当前版本记录和审查证据，**没有修复这些新findings**。完整列表及触发／方向／验证边界见`evidence/global-review/findings.json`。当前有未解决P1/P2，不宜直接发布；等待用户下一步指令，不自动扩大修复或部署。
