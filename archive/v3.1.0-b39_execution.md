# Neckline 3.1.0 / 双端 Build 39 / Schema 7 执行记录

## 目标与边界

将 K10-v1.4 的冻结文章调查落为可恢复的工程流程：文章正文先拆关键命题，复用适用的已核来源事实与公司关系，再围绕具体问题执行 Tavily 搜索和受控全文，审读支持、反驳、冲突和缺口后有据收口，最后完整比较公司并派生发布结果。它不恢复交易计划、持仓／退出、收益结算、K9 或 K8。

施工基线是本地 `0c8a143`（B38 未发布），实际部署基线是 `v3.0.4-b36 / Schema 4`。初始施工仅授权本地验证；随后用户明确要求“一条龙发布”，授权部署、迁移和客户端换装，完成证据见本记录末尾。worker、晚间与晨间 timer 继续保持 disabled，不恢复被用户作废的任务，也不重跑 B38 的付费全量窗口。B38 的比较失败响应没有保存，任何实现或测试不得声称已经定位或修复它。

用户本轮已经定案：

- 未核传闻可正常形成推荐并进入 30 家、D1/D2 与成绩。它不是 verified，必须保留传闻性质、源头已知／未知、未证实环节、关键反证及条件化判断；不能编造来源、公司代码、事实或唯一排序。是否推荐仍遵从完整 K10 判断，不能把每条传闻都自动推入候选。
- 不为未来核验全文保留固定名额。冻结文章和获准的额外全文都按实际可用的 80／40 admission 处理；不能另设数字、预算或审批。

策略资料中仍写作“待决定”的两项以上述最新用户决定为准。本记录不修改研究仓，也不使生产 import `whynotme`。

## 公共契约

### 研究与执行状态

研究状态只能为：`ready_for_comparison`、`continue_research`、`pending_verification`、`abandon_recommendation`、`background_only`、`comparison_complete`。执行状态只能为：`ok`、`paused`、`failed`。两者由程序和持久化检查点共同投影；模型不能自报工具成功。

`comparison_complete` 表示输入范围内的每家公司已有结果，允许其中有 `pending` 或 `excluded`。它不等于已发布。执行失败时保留可复用证据和研究快照，但状态必须是 `failed`，发布不可进行；task 的 worker 终态不能覆盖这个事实。

每个公司评估使用：`primary`、`alternative`、`tied`、`pending`、`excluded`。前 3 个是可发布派生集，需有事件内 rank；`tied` 可共享 rank，`pending`／`excluded` 没有 rank。所有输入公司必须出现一次，不能删除错误对象使校验通过。比较发现某个竞争对象的未解问题足以改变唯一主推时，保留为 `pending_verification`／`continue_research`，不强排。

### 最小持久化模型

继续使用 `k10_source_document_versions` 作为真实来源版本，`k10_execution_item_checkpoints` 与 external attempt ledger 作为模型／网络恢复账，`k10_article_admissions` 作为正文准入账，候选／发布表及其 `comparison_json` 作为正式发布历史。Schema 7 新增的对象只覆盖以下关系：

| 对象 | 最少内容与键 | 作用 |
|---|---|---|
| 研究快照 | task、event/revision、消息 cutoff、实际查证 cutoff、上下文／Prompt／模型版本摘要、research/execution status、递增 revision | 一个事件当前调查状态与可恢复边界 |
| 命题 | snapshot、claim ID、原文陈述、主体／说话者／对象／动作、性质、增量、阶段／条件／时间、验证状态、决策影响 | 分开事实、预测、观点、宣传和传闻；不把原文称述直接升级为事实 |
| 命题—证据关系 | claim ID、真实 document/revision、支持／部分支持／反驳、定位／适用范围、来源／时间状态 | 可核对引用，不复制正文或伪造编号 |
| 问题与路径 | snapshot、question ID、关联命题／公司、支持和反驳条件、判断影响、目标来源、answer state、恢复条件；每条路径的查询、差异理由、预期增量与实际结果 | 防止 headline 一搜和同义词／转载循环 |
| 全文申请 | question、真实 source ref、摘录不足原因、预期判断变化、admission ref、requested/admitted/rejected/fulfilled 状态 | Extract／全文只在程序批准后供模型使用 |

这些对象可由少量关系表加结构化 payload 实现，但不能只保存模型散文，也不能为 A–G 每阶段建立独立表。写入使用 task lease 与版本比较；同一输入 digest 恢复时读取 checkpoint／快照，绝不重复外呼或领取第二个名额。读 helper 只能 read connection，`init_schema()` 仍只能出现在启动、显式写命令或受控迁移。

来源事实缓存键必须含 document ID/revision、内容哈希、调查契约／Prompt 版本和模型参数摘要。它只缓存稳定的原始来源事实。事件判断、公司关系适用性、市场上下文和比较结果另绑定 news cutoff、verification cutoff、公司池／市场时点；后取得的旧资料可支持新报告，不能回填历史报告。证据记录保留 publisher、标准化 URL、真实 origin／`unknown_origin`、转载关系、定位、发布时间及精度、fetched／first-seen 时间与 after-cutoff 状态；未知时间保持未知。

### 研究模型与管线接口

在 `discovery.py`／`pipeline.py` 增加单一 typed investigator adapter，而不是把七个阶段变成七个 agent 或固定七次调用：

```text
advance_research(snapshot, *, action, evidence_packet) -> ResearchStageResult
action = extract_claims | plan_gaps | plan_queries | assess_evidence | close_research | compare_companies
ResearchStageResult = claims | questions | query_paths | evidence_updates |
                      fulltext_requests | conclusion | company_assessments
```

管线仅在当前快照确实需要该 action 时调用它：已有证据足够可直接收口；无有效路径则待核；无公司映射可结束为背景／放弃；研究失败不继续伪造后续阶段。每一次模型请求经现有 checkpoint／attempt 账以 `task + stage + item + input digest` 幂等记录。执行配置新增明确的 investigation model option／Prompt contract revision，不使用默认模型参数。

阶段语义如下：

1. `extract_claims` 是一次原文理解产生的结构化结果：已合并进既有 `understand` 请求，再由程序记录此阶段，绝不为每个事件重新调用模型读原文。它拆出命题的性质、增量、说话者、条件和定位；综合报道的旧背景／无关分支不发现新选题。
2. `plan_gaps` 读取命题和适用的事件事实／公司关系缓存，提出能改变真实性、阶段、映射、最强反证、两日理由或比较的问题。确定公司映射后冻结该公司范围的市场／历史上下文，比较可明确待核；上下文加载本身不暗中启动旧 headline 搜索。
3. `plan_queries` 为每个开放问题给出中性查询、反驳路径、目标来源、预期信息增量与判断改变；程序拒绝越出已选事件／公司范围、没有新路径或已做过的意图。
4. 程序用 Tavily Search 执行获准路径。结果成为带查询路径和真实 metadata 的 source-document version；默认只给模型摘录。需要全文时 `assess_evidence` 返回全文申请，程序先以现有 `admit_article` 检查实际剩余、冻结名单和来源身份，再用 Tavily Extract／受控全文取得并记录。全文不可用或超限是待核理由，绝不是事实反证。
5. `assess_evidence` 以真实 evidence ref 更新支持、部分支持、反驳、同源重复、无关或冲突；可回到新路径。`close_research` 分别给事件／公司收口状态、缺口和恢复条件。`compare_companies` 只在可比较范围内完成全体评估，输出共同事实、五问、反证、未知和改变顺序条件。

`_event_evidence` 不再把整篇冻结原文在 verify、mapping、compare 中反复传递。阶段 A 和获准全文阶段可读取正文；后续 evidence packet 只装实际引用的证据卡、定位摘录、结构化命题／问题和所需市场／历史资料。程序从 refs 组装 packet，禁止模型引用未传入、伪造或 after-cutoff 的资料。

`TavilyEvidenceGateway`／checkpoint 的粒度改为问题和路径（而非 `event.headline`）。Search 保持 `include_answer=false`、`include_raw_content=false`，且每个请求保存问题、意图、新路径理由和真实结果；不以搜索次数、相关性或转载数判断证据充分性。Tavily Extract 不得绕过上述全文申请和 article admission。

### 未核传闻与发布 DTO

为评估、候选比较和公开投影增加显式 `EvidenceDisclosure`：

```text
verificationStatus: verified | partially_supported | unverified | contradicted
isRumor: bool
originStatus: identified | unknown
originEvidenceRef: optional real ref
unverifiedReasons: non-empty when status is unverified
conditionalAnalysis: non-empty when isRumor
```

`unverified + isRumor` 可以随 `primary`／`alternative`／`tied` 进入发布，条件是：公司代码通过真实股票池／硬排校验，比较覆盖完成，原始传闻来源或其“来源未知”的记录真实存在，且 `EvidenceDisclosure` 完整。它不能变成 `verified`，也不能因为有卡片或 D1/D2 结果反向升级。`pending` 的理由必须是会影响当前判断的缺口、冲突、时间／全文约束或有效路径，而不是“传闻未核”这一个标签。模型没有给出所需传闻标记时，程序从 claim kind／证据关系派生；无法安全派生则拒绝该输出为协议错误，绝不默默标成 verified。

扩展 `EventComparison` 为完整 assessment 集并提供 `publishable_assessments` 派生视图；现有 candidate creation、`OpportunityPublicationInput`、晚间 30 家、跨日关联、重叠样本、实际 availableAt 与 D1/D2 只消费该派生视图。完整 assessment 另保存在研究快照，正式 `comparison_json` 冻结当时 disclosure；历史 B36/B38 没有该字段时为 absent，不倒推或伪造历史标签。

### API、Swift、晨报、通知与分析

保持既有 K10 DTO 兼容性，新增字段均为可选／可加性投影，使新客户端读取 B36 时安全降级。新增 scan research summary（总事件、问题 answered/open、公司 `comparable/pending/excluded`、comparison state、execution failure）以及按 scan 查询完整 assessment 的 API；不返回全文、Prompt、原始模型答复、私有 URL 或凭据。

在发布 sample、机会详情、分析链、晨报条目和通知 payload 使用同一 `EvidenceDisclosure`。若已发布推荐以未核传闻为依据，所有可见路径都显示“未核实”，呈现源头已知／未知、条件和缺口；不能只在当前卡片补一个字而让晨报、推送或详情声称“已核验”。`analysis.py` 的 `_core_context` 与正反双方的 `prompts.pro_messages`／`con_messages` 必须传入冻结的 disclosure、引用和不确定性；后来的核验只形成新分析／证据版本，不回写旧分析或其结论。Swift 在 `K10Models`、client、presentation 和相关 views 解码并显示这些字段；策略标签明确为 `K10-v1.4`，分析修订明确为“分析第 N 版”。

执行进度新增研究计数和 research/execution status。`failed` 显示“执行失败，比较未完成”及安全错误码；`partial` 只能描述覆盖状态，不得显示为“已完成比较”或“没有机会”。`completed` 技术任务与 `comparison_complete` 分列。现有空态和填充态均须测试。

## 实施拆分

1. **数据／配置 builder：** `schema.py`、`store.py`、`config.py`、`migration.py` 与迁移测试。Schema 6→7 加上述最小对象和索引，4→7 先沿受控 4→6 路径建立 B38 所需表，再加 7，绝不创建已废的 B37 budget/template runtime；开发 Schema 5 保持现有清理到 6 后再到 7。扩展 rollback 策略为迁移前备份恢复，不设计会删除研究快照的在线 down migration。
2. **研究／搜索 builder：** `discovery.py`、`pipeline.py`、`verification.py`、`verification_checkpoints.py`、`prompts.py`、`title_runtime.py` 与测试。实现 typed snapshot adapter、evidence packet、问题路径、Search／Extract admission 和事实缓存边界；保留 B38 标题 triage、实际 ledger、pause、retirement、有限 retry 与 worker lease。
3. **发布／运行时 builder：** `runtime.py`、`morning_runtime.py`、`morning.py`、`notification_runtime.py`、`worker.py`、`cli.py`、`opportunity_discovery.py` 与测试。将完整比较和执行失败贯穿 finalize／publish，不改写已发布机会或成绩；真实 CLI enqueue→worker→handler 仍是回归入口。
4. **API／双端 builder：** `api/k10.py`、`api/k10_schemas.py`、`App/Neckline/Networking/`、`Views/`、`Push/`、`SettingsView.swift`、K10 tests 及版本工程文件。升级 `Backend` package、`App/project.yml` 和生成的 Xcode project 到 3.1.0/39，按现有工程脚本同步验证版本字段。

模块可并行，但数据契约先落定；研究／发布实现依赖 Schema 7，API／Swift 依赖稳定 DTO。任何实际代码偏离这些状态、公开字段、发布资格或 migration 边界时先交回主会话更新本记录。

## 验收矩阵

| 范围 | 必须证明 |
|---|---|
| 命题与范围 | 综合报道的新事件、旧背景和无关主题被正确分开；预测、宣传、传闻、否认、时间不明均不自动成为事实；说话者、条件、定位可追溯 |
| 证据与查询 | 复用适用来源事实；问题有改变判断的理由；同源转载合并；支持／部分支持／反驳／冲突正确；重复路径被拒；无有效路径以待核停止 |
| 全文 | 晚 80／晨 40；冻结缺正文不替补；Extract／缓存重读均 admission；无名额不读第 81／41 篇；不预留名额；摘录能解决则不读全文 |
| 传闻 | 已知源头和未知源头都保留真实状态；未核传闻可以形成 primary/alternative/tied 和发布，显示全部披露；不会标 verified、编造 code／ref 或无条件变成 pending |
| 比较／发布 | 所有公司包括 pending/excluded；主推、备选、并列有具体依据和改变条件；关键竞争缺口阻止硬排；只发布派生集；30 家、重叠、availableAt、D1/D2 和历史 comparison 保持不变 |
| 中断／恢复 | 引用、JSON、模型、网络、Tavily、lease 中断及公司覆盖错误均保存安全定位，失败不伪装为 complete／no candidates；成功 input 恢复不重复外呼／正文计数，明确授权的已知语义失败可追加可追溯尝试，未知请求不盲目重发 |
| 迁移 | 临时数据库从 4→7、6→7 成功，B36 历史、B38 title／article／attempt audit 与 publication data 可读；备份 integrity、受控 restore 和 GET 无 DDL 通过 |
| 全链路 | deterministic transport 的 CLI→worker→handler 覆盖时区、来源延迟、不确定发布时间、producer failure、client read failure；隔离 FastAPI 实际响应由当前 Swift models 解码，包含晨报、withdrawn、overlap、缺数与分析修订 |
| Native | 改任一 Swift 后跑 macOS build、iOS Simulator build、iOS build-for-testing；用唯一 `/tmp/neckline-v3-qa/{macos,ios}`、主模拟器 `211DD03C-812D-4A42-97EF-F693D7DF924C` 和显式 `NK_BUNDLE_SUFFIX`，安装前核对 Info.plist 与 xctestrun host bundle ID，比较填充／空态截图与 `archive/Neckline_V3_界面参考/`；现役 Mac 只读核验仍为 B36 |

后端门禁为 `cd Backend && .venv/bin/python -m pytest -q`。任何 Swift 改动后必须顺序完成项目 `AGENTS.md` 所列三条 `xcodebuild`。测试只用临时数据库或显式只读快照，不能落入工作数据库。

## 隔离验证边界

本轮可执行的真实资料边界只有既有 B38 一次隔离窗口的历史证据：服务器 `/tmp/neckline-v306-validation-20260908/private/validation-final.sqlite` 已关闭、去凭据，且只能在先确认主机、路径、只读模式和临时 DB 的前提下用于离线、确定性回归。它不得启动 task、worker、timer、Tavily／DeepSeek 或向生产写入，也不能重放 1,430 条标题的付费请求。报告将标题输入、冻结文章、实际正文、事件、问题、公司状态、比较、发布和执行失败分开，不能把 B38 的 37 篇已读或 9 次搜索叫作新流程真实质量。

本 Plan 不授权任何新的最新窗口真实外呼，故 builder 不复制密钥、不自行试跑。若后续用户授权，计划必须先固定：一个最新有效窗口、临时数据库／进程凭据、标题全量审计、晚≤80或晨≤40的实际正文上限、质量／协议错误时在正文前或后立即停止、最终安全回执位置；仍不启动生产 worker／timer、不写生产库、不恢复旧任务，也不做全量付费 replay。

## 风险、回滚与交接

最大产品风险是把未核传闻的“允许推荐”错误实现为“允许当事实”，或反过来被旧 verified gate 隐性阻断。防线是独立 disclosure、真实引用／股票池校验、条件化文案、发布派生集和跨 surface 契约测试。第二风险是多问题搜索或 Extract 绕开正文上限；防线是每次外部调用的问题路径和任何全文的 article admission。第三风险是迁移损害 B36／B38 审计；防线是受控备份、临时 migration／restore 和生产不在本轮范围。

本地问题回滚到 `0c8a143` 前先保存可复现的临时测试 receipt；生产未来迁移只能由已验证备份恢复，不能用 Schema 7 的 destructive down migration。无需用户网页操作。

## 实施结果与本地证据

- 本地实现 commit：`4bcfd5b`。最终后端 `.venv/bin/python -m pytest -q`：**905 passed，21 warnings**；警告为既有 Polars 分组 sortedness 提示。`git diff --check` 通过。本轮没有独立 review、发布、真机安装或新的付费验证。
- 起点为 `0c8a143`；研究协调器集中在 `research_runtime.py`，typed contract／存储／prompt 分别提供职责清楚的接口。没有新增独立 agent 进程、定时器或总费用限额。
- 每次定向查询记录问题、路径、预期增量和真实来源。适用证据支持 narrow claim，不要求额外凑一次 Tavily 或第二来源；搜索无结果、正文缺失和名额不足都保留实际缺口。已核命题必须有对应 supports 关联。
- Tavily Extract 在正文准入后才执行，省略会返回查询片段的 `query/chunks_per_source`，保存真实全文新修订和获取时间；旧发布日期不能证明新版正文在截止前存在。依据：[Tavily Extract 官方契约](https://docs.tavily.com/documentation/api-reference/endpoint/extract)。
- 同一全文跨问题复用正文版本和准入，模型首次提取后只复用结构化事实；收口阶段新提出的全文请求也须完成审读后再收口。程序拒绝已处理问题的重复全文和重复查询路径。
- 研究输入、可复用旧来源事实和公司比较上下文分别冻结。跨任务复用只限同事件、同来源修订和契约／模型版本，保留出处与适用时点；不把旧排名／市场判断拿来当新结论。
- 正常 CLI→worker→handler 的确定性 HTTP 用例经过两条不同查询路径后，正式发布未核 primary，同时在实际 API 保留 pending／excluded；D1/D2 仍为推荐可查看后的完整交易日。畸形结果变为 task／scan／snapshot failure，不能生成正式候选。
- 已知失败的恢复保留原 task、冻结哈希和正文准入，先重试确实失败的阶段。回归证明标题、正文理解和已完成路径不重跑；运行流水版本不再改变同语义模型输入身份。原有 B38 实测失败响应未保存，其具体模型失败原因仍不能追认。
- 模型检查点仍为 running 或任意 `*_outcome_unknown` 时，基础组和受控恢复派生组都在进入适配器前被拒绝，剩余网络重试次数不能成为重复付费理由。合法 JSON 若后续被公司／引用校验判错，通过精确 CAS 标记为语义失败，保留原结果和用量，再允许已授权的恢复；不永久复用错误的“completed”。
- 仅在临时数据库演练了 Schema 4／6→7 的受控备份、迁移和恢复。种子包含来源／任务／事件／候选，Schema 6 另含标题政策、清单、正文准入与实际尝试账。核对旧字段／记录、暂停控制、备份 integrity／SHA256，以及恢复后的原版本／文件哈希；不以此替代未来生产副本的发布迁移验收。

### 历史资料离线验收

只读导出的 B38 既有资料已去凭据，冻结选择摘要为 `8fba20413500bcb458f6d62b510c63b5ea22929f91beceb412425ad42e07564c`。共 1,431 条原始审计、1 条精确重复、1,430 条标题输入，冻结 37 篇／37 个事项。临时 Schema 7 验证原文准入、标题边界与后续正文禁重传，耗时 1.403 秒；129,207 bytes 可读正文，148 次后续正文旁路被拒，新增付费外呼及生产写入均为 0。

回执：[frozen-contract-receipt.json](v3.1.0-b39-evidence/frozen-contract-receipt.json)。这仅证明历史输入的程序契约，不代表新的 DeepSeek 判断、Tavily 覆盖或真实推荐质量通过。

### Native 与 API 验收

- 实际隔离 FastAPI 响应由当前 iOS Swift DTO 解码；定向 API 26 项、native service／AppModel 2 项通过。临时服务已停止。供给脚本 `/tmp/neckline-v3-qa/v310_fastapi_fixture.py`；DTO 日志 `b39-v310-swift-dto-final.log`，native model 日志 `b39-native-model-tests.log`。
- 最终三条构建通过，日志在 `/tmp/neckline-v3-qa/logs/b39-last-macos.log`、`b39-last-ios.log`、`b39-last-ios-tests.log`。构建显式绑定 QA ID，安装前检查 Info.plist 与 xctestrun host；QA 实例已退出卸载，主模拟器生产包保留，过期 DerivedData 已清理。
- 实际 iOS 点击“设置→来源覆盖与缺口”并展开公司比较，核对执行失败、主推／待核／排除、未核传闻、来源未知及条件判断。最初 QA fixture 没供给 B39 状态的问题已修正；生产数据加载另由实际 API→Swift 与 AppModel 回归验证。
- 与批准参考图核对了白卡、蓝色强调、文字层次与留白。证据：[研究摘要](v3.1.0-b39-evidence/ios-v310-b39-research-summary.png)、[展开比较](v3.1.0-b39-evidence/ios-v310-b39-full-assessments.png)、[空态](v3.1.0-b39-evidence/ios-v310-b39-empty-opportunities.png)。Mac 构建与设置填充页已验，新增研究页逐项视觉验收以实际 iOS 截图为准，未做真机安装。

## 2026-09-08 一条龙发布完成

### 范围、归档与验证

- 用户授权为“直接走发布一条龙”。累积核对范围为真实生产 `v3.0.4-b36` / `6017a0c36bf1bbc6e842ef512ca66a29b7e7f607` → `a3732ab3a1c6ddf7042246c30710ee109fa8208b`，包含中间未发布 B37／B38、B39 施工及未发布差异，不只看最后提交。无独立 reviewer 调用；迁移前已按工作流提出一次高危区复审建议，不增加确认门槛。
- 核对真实服务器旧 manifest 的全部源码哈希，无额外漂移；本轮主要增量为 Schema 4→7、显式标题／研究执行配置、冻结准入、调用恢复、API 和双端展示。systemd 单元在此累计范围未改变，沿用实际停用状态。
- 发布时发现 wheel 配置清单仍只打包旧 V1 JSON；`a3732ab` 将配置 package data 改为全部显式 JSON。实际 wheel 解包核验 84 个 Python／JSON 包文件及全部 5 个配置 JSON，哈希与发布源码一致；未带数据、环境文件或私钥。
- 不可变标签 `v3.1.0-b39` 与 main 已推送。Mac 归档源码提交 `7c52f0ac70c5be68a4933584d3ddf5e791b25f75`、iOS 归档源码提交 `a3732ab3a1c6ddf7042246c30710ee109fa8208b`，两者 App tree 均为 `0a981cc1eaa8c202c4ee11515ddc1fad56f62d6a`，等于发布标签的 App tree。后续文档提交不移动标签。
- 发布重新运行后端 **905 passed**（既有 21 项 Polars warning）；双端三条 App 构建与实际 API→Swift、native 验证见前节。暂停状态探针修正后，相关 API 回归另有 **33 passed**。

### 真实迁移、检查失败与回滚

目标明确核验为宁波 `deploy@114.66.2.205` / `ser657204219523`，`/opt/neckline/data/neckline.db`。备份前停止 API、worker、全部 Neckline 数据写入服务及 timer，建立唯一数据库基线；受控副本先升级和恢复，再操作正式库。

- Schema 4→7 演练和正式迁移均对 **50 个旧表**逐表核对所有旧列／行哈希与记录数；通知 Schema 保持 2。既有配置、任务、来源、通知和市场数据保留，追加策略修订 3、执行修订 2 与持久关闭控制。
- 第一次检查失败：runtime manifest 包含 `data/.gitkeep`，但部署正确排除了整个运行数据目录。上线前文件核对拒绝该清单，并自动恢复 B36 runtime／wheel／数据库／绑定，恢复指纹一致。修正清单排除该占位路径，未改生产数据策略。
- 第二次检查失败：发布探针把数据库 `closed` 错当成 API 的状态值。实际 API 契约是 `paused`，现有测试已明确这一映射。安全检查触发第二次完整 B36 回滚；修正探针按 DTO 判断，增加明确失败原因，相关 33 项回归通过。
- 第三次发布成功，21:39 CST 终验确认 B39 / Schema 7、数据完整性和代码哈希；前两次恢复集保留为 `v3.1.0-b39-predeploy`、`v3.1.0-b39-predeploy-2`，成功恢复集为 `/opt/neckline/data/backups/v3.1.0-b39-predeploy-3/`。旧原始回执的 prepared 字段以 rollback 字段为准；脱敏汇总明确记为 rolled_back。
- 发布探针错误的教训：临时发布助手同样必须遵循真实 DTO，不能混用持久化与客户端词汇；manifest 只校验实际部署的运行文件，数据目录排除项须在构造时同步排除。本项记录在本版本，沿用项目已有 readiness 规则，不另建平行台账。

生产根 owner/group/mode 全程保留 `root:root / 0755`，数据库 `neckline:neckline / 0600`，绑定文件保留原属主与权限。迁移前后备份哈希见 [脱敏生产回执](v3.1.0-b39-evidence/production-release-receipt.json)。数据库与旧绑定只留服务器，未下载或提交。受控助手留档为 [production_release.py](v3.1.0-b39-evidence/production_release.py)，含严格旧版本／主机断言，仅解释此次发布，不是可直接重复执行的通用恢复命令。

### 实际服务与客户端

- 本地及公网 health 均为 `v3.1.0-b39`；实际配置四范围无 missing／errors，绑定策略 `k10-v1.4-production@3`、执行 `k10-execution-production@2`。真实列表按各自 DTO 解码，未鉴权返回 401，GET 前后数据库业务指纹一致。
- 持久 run control `closed`、API `paused`；worker MainPID=0、disabled（保留历史 failed 状态），晚／晨 timer inactive+disabled。仅恢复原已运行的行情更新／重试 timer。没有新 paid call，没有扫描或旧任务恢复。
- APNs readiness 为 ready，实际私钥可读和签名能力检查通过；本轮没有试发通知。任务仍为 2 cancelled、1 failed，用量记录仍为 2,528 条。
- Mac 使用 `ditto` 换装 `/Applications/Neckline.app`，Developer ID 严格验签、ZIP 解包验签与 arm64／x86_64 架构通过；仅运行一个正式实例。实际界面核验 `3.1.0 / Build 39 / K10-v1.4`、Prod、四项已配置、资讯暂停及推送就绪，历史失败状态仍如实显示。
- Mac 可恢复副本：`/Users/linotsai/Lino/app_backups/Neckline-v3.0.4-build36-pre-v310-b39-20260908.app`，严格验签通过。正式签名产物在 `/Users/linotsai/Lino/releases/Neckline/v3.1.0-b39-20260908/`。
- Mac 尚未 notarize；本机无可用 notarytool profile，既有 B36 也未通过 Gatekeeper 分发评估。B39 本机已正常启动，但不能宣称网络下载后的 Gatekeeper 验收通过；发布说明明示该限制，未弱化系统保护。
- iOS 对既有真机目标完成 Release 签名归档，3.1.0（39）、Team `HX73DFL88G`、arm64 和严格验签通过；通过 Xcode 由用户安装，未安装设备、未导出 IPA。过期构建副本清理，保留正式归档。

### 发布交付与后续边界

[GitHub Release：v3.1.0-b39](https://github.com/linocai/Neckline/releases/tag/v3.1.0-b39) 含 Backend tar、wheel、Mac ZIP、runtime manifest、Apple manifest 与 SHA256SUMS.txt，共 6 个资产；全部重新下载，与本地逐字节和 SHA256 一致。校验清单见 [release-SHA256SUMS.txt](v3.1.0-b39-evidence/release-SHA256SUMS.txt)。服务器同一后端归档、wheel 与 manifest 留在 `/opt/neckline/releases/v3.1.0-b39/`。

发布完成不会自动开启付费研究。B39 尚无新的 DeepSeek／Tavily 真实质量验证；历史离线协议、生产健康与签名验收分别记录，不互相替代。后续真实验证与恢复扫描需另行明确安排。恢复旧数据库前必须重新停止全部写入者并核对升级后业务写入；客户端和行情 timer 已运行，不能再直接假定可用发布前快照覆盖当前库。

## 2026-09-08 首轮正式晚间运行（失败后保护暂停）

用户在发布后明确授权打开开关，跑 9 月 8 日正式晚间报告作为首次正式运行；原有 21:00／09:00 定时器一并恢复，禁止补跑事故批次。目标源码／数据库及停机状态再核验，开启前建立 `/opt/neckline/data/backups/v3.1.0-b39-first-evening-20260908/`，保留 SQLite 一致备份、原绑定与启动回执。

首次官方 evening.service→CLI 入队被 policy 关联校验拦住：执行 JSON 内有批准规则，但生产 `k10_title_triage_policy_revisions` 未登记，readiness 未识别该缺口。启动保护重闭开关，未开启 worker、付费调用 0；已产生的 queued task 保留。随后用既有 append API 登记配置包内原样、同哈希、已获用户批准的 `k10-title-triage-v1@1`，重走同一 CLI 幂等入队并绑定执行修订 2，没有创建第二个任务。此为遗漏生产配置的补齐，没有更换规则或改写策略修订。部署入口、readiness 和入队原子性缺口已记 Backlog。

正式 task `task_8ee84154911c2463c54880feeefb93a5`，21:49:02 CST 开启 worker；策略修订 3、执行修订 2、深读上限 80。已存在真实来源成功水位，清空过时的首次部署 bootstrap 参数；按原晚到资料回查规则限定当前窗口，不读取事故任务冻结清单。worker、晚／晨 timer active+enabled，下一晨间为 9 月 9 日 09:00、晚间为 9 月 9 日 21:00，Persistent=false 不补偿旧计划。实际发布可查看时间不得伪造为 21:00。

### 首轮最终结果与停止

- task 在 21:51:17 CST 终止为 failed / title_incomplete，scan 为 partial；实际 API publicationStatus=not_published、无批次、正式列表为空。这不是“无机会”的选股结论。
- 收到 1,063 标题，6 个批次完成 384 条；失败批次尝试 2 次，另一个批次首试失败、第二次成功，共 3 次 response_json_invalid。合计 9 次调用、92,021 tokens；正文准入、深读、Tavily 搜索与全文均 0。无未知外呼结果。
- 现有记录未保存原始模型响应，目前不能确认具体语法或根类型问题。先对失败批次做最小诊断，补充不含原文／凭据的结构证据，再有针对性修复；不能仅放宽校验后称为修复。
- 21:53:01 CST 以 first_formal_title_contract_failed 关闭持久开关并 disable／stop worker、晚晨 timer；实际 API paused，三个单元 inactive+disabled、worker MainPID=0，明晨不会重跑。此状态覆盖 21:49 的临时开启记录，行情 timer 保持原态。
- 任务和成功标题检查点保留；旧事故任务仍 cancelled，尝试次数保持 1／12。试跑后完整现场一致备份为开启前备份同目录的 neckline-after-first-run.db，仅留服务器。
- [启动回执](v3.1.0-b39-evidence/first-evening-start-receipt.json) 与 [最终结果](v3.1.0-b39-evidence/first-evening-outcome.json) 只含运行摘要，无凭据和文章内容。

## Build 40 快修：首轮标题协议与原任务恢复

用户要求立即诊断并解决报告生成失败；基线为 main `84337cb`，实际生产为 `v3.1.0-b39` / `a3732ab`。对外版本保持 3.1.0，双端 Build 递增为 40，Schema 7 与既有策略／执行配置不变。

- 对失败的原 64 条输入做一次受控真实复现，输入哈希 `575cfdadaf48475f9e8d790d3169334dd132f3e2ac7141e47a791d3b7013e420`。DeepSeek 返回有效 JSON 数组、finish=stop、64 个完整唯一索引；旧 transport 仅接受对象而拒绝。复现付费 1 次、9,442 tokens，之后用保存响应离线验证 64 条全部符合原标题契约。历史原响应未保留，不能断言前三次错误格式完全相同。
- 仅当请求明确约定一个数组字段时，机械补回缺失的对象容器；其后仍做完整领域校验，不修补事实、不删除坏行，不接受截断、代码围栏或任意根类型。失败保存安全结构诊断；有限修复调用携带校验反馈。
- 新标题执行失败记为 failed；精确识别 B39 尚未发布且冻结输入的 title_incomplete/partial，原子纠正后恢复同一任务。成功标题批次复用原检查点，已知 JSON 失败只进入一个稳定恢复组；未知调用禁止重发，原窗口／名额／用量不重置。
- 回归覆盖真实 CLI→worker→冻结失败→原任务恢复→正式发布、64 条数组、缺行／重复索引拒绝、成功批次复用与恢复幂等。后端全量 918 passed；恢复中的未知调用补测随后执行。未请求独立 review。
- 部署与正式续跑结果待下文补全；此记录不代表已生成正式报告。

### B40 发布与续跑实证

22:12 代码部署完成，生产源码 `ec13745` / `v3.1.0-b40`。部署前后数据库 SHA-256 均为 `a046ade2be5c8896dbf3607df4c52b412f2a39b0181bf428ff6761fbdd27aa9b`，Schema／配置／权限不变；[部署回执](v3.1.0-b39-evidence/b40-deploy-receipt.json)。双端签名归档通过，Mac 已换装并保留 B39 恢复副本，GitHub B40 发布完成。

22:13 从原任务恢复，[启动回执](v3.1.0-b39-evidence/b40-resume-receipt.json)。所有新标题调用均成功，原数组问题没有再现；随后实际 taskSlice 结束，重试排程替换 checkpoint 时丢失 recoveryAuthorized。下一分段改走原失败 checkpoint，返回 model_network_attempts_exhausted，未进入正文。原成功标题结果仍在；B40 修复未闭环，继续前向修复而不重放标题。

### Build 41：修复跨分段的恢复状态持久化

基线为实际 B40 / `ec13745`。只修任务运行字段：schedule_task_retry 和 finish_task 替换进度时保留由受控恢复入口写入的原授权；handler 不能伪造或覆盖它。新的实场景回归先复现 KeyError/recoveryAuthorized，再验证“已完成标题检查点→真实 worker 分段→下一段→同任务正式发布”，期间标题只调用一次，原时限不变。对外仍为 3.1.0，Build 41；部署和正式续跑结果后补。

- 同一回归进一步发现入口只允许失败状态，拒绝恢复后正常处于 running 的扫描；同步允许已授权原任务的正常续段及既有 completed 幂等收口路径，冻结哈希和原任务绑定检查仍必需。
- 后端最终 919 passed，双端签名归档及 ZIP 解包严格验签通过。不可变标签 `v3.1.0-b41` 指向 `1893e3c`；双端 App tree 一致，Mac 已单实例换装，B40 完整备份保留。GitHub B40 五个资产与校验文件下载回验通过。
- 22:21 宁波 B41 部署成功，[部署回执](v3.1.0-b39-evidence/b41-deploy-receipt.json)。前后数据库 SHA-256 均为 `f84749c8e6c7f5cd88c939ce51d9bd479926522d235392643adb3c9c05ee7d32`；Schema 7、绑定、文件权限未变。
- 22:22 原任务续跑，[启动回执](v3.1.0-b39-evidence/b41-resume-receipt.json)。17 个已完成标题批次原样保留，原输入哈希不变；当前进入全局选择，尚未发布最终报告。旧事故任务状态及尝试次数原样保留。

### Build 42：正文契约诊断与受影响正文恢复

B41 正式续跑已成功通过全局选择和真实分段。1,063 条输入中 1 条完全重复，1,062 个标题完成筛选；初选 73 篇经复核冻结为 67 篇，全局名单哈希 `30d0b7b681de4e7eb661d2ad259cc229af39a1b18102bf1cb51a14f8738aa1bc`。进入正文后集中出现 understand_json_contract_invalid，关闭新增外呼后等待已有响应正常落账；21 篇完成、21 篇已失败、25 篇仍准入待处理，无未知外呼，未进入 Tavily／研究。task failed/paused，scan 保留 running 冻结现场，worker 与晚晨 timer 均停用。

- 对已失败的同篇冻结正文做一次受控复现，输入哈希 `70fab3d7b45fec98d6f56884b91e58237ec6b8e8ec04bad31f55fe131ff940e2`；返回合法 JSON，但全部 6 个命题的 decisionImpact 为空，触发非空约束。诊断调用 2,316 tokens。
- 提示词明确 decisionImpact 是后续事实核实／公司关联／风险判断的影响，不要求当前价格或买卖判断；不能空白，未知须说明缺少的关联依据。有限修复传递程序生成的字段名、期望类型／枚举值，绝不传凭据或把缺失事实用默认值补齐。
- 使用同篇正文、新提示及具体字段反馈做一次真实验证：2,096 tokens，1 个事件通过原严格 claims／引用解码。正文数仍是同一已准入资料，未扩充名单。
- 增加已知正文协议失败的稳定恢复组，保留成功正文／标题缓存；暂停后 task failed/paused、scan running 的原冻结任务可经原子受控入口恢复，未结算或未知外呼阻止恢复。
- 新回归还发现所有正文均失败时会生成 completed/partial 的空结果。B42 在正文理解不完整时停止后续研究和正式发布，标成可恢复的正文失败；不能把解析失败冒充“无机会”。
- 后端 923 passed；覆盖字段级修复、失败正文同任务恢复、正文已落账后真实暂停→恢复→发布，正文名额不增加。

### Build 42 部署结果与 Build 43 定点修复

B42（`fe7bc3a`）于 22:44 部署、22:45 原任务恢复；双端签名归档、Mac 换装、GitHub 六资产下载校验通过。部署前后 DB SHA-256 均为 `a5bd29df22b65c156cf263c6c563d492267cc6337a13ee6faee2b91e5d61bdab`，恢复集 `/opt/neckline/data/backups/v3.1.0-b42-predeploy/`。原任务完成 66/67 篇，剩一篇失败，未发布；已关闭新增外呼并停止 worker，无 started/unknown 调用。

B43 基线为 B42 / `fe7bc3a`，版本仍 3.1.0，双端 Build 43。对唯一失败原文 `doc_35f62200de24a512079e17850dd5f8e8@1` 受控复现一次（3,202 tokens）：有效 JSON 的两个事件均漏 facts 对象，旧校验只说结构不完整，重试终态还误标网络耗尽。修复明确 facts 对象要求并反馈准确字段／类型；JSON 耗尽保持 JSON 原因。受控恢复同时冻结当时失败的模型输入哈希，仅这些旧失败可再开一个稳定恢复组；本次新失败不因重复 worker tick 无限重试，任何已完成或未知调用均不可重发。原 67 篇名单、输入截止、完整调用台账与已完成正文不变。

18 项定向回归通过，包括真实 worker 的再次显式恢复、同一次授权不多发调用、标题复用、facts 字段修复。全量验证和部署结果待补。

B43 全量后端 925 passed，双端签名归档及 Mac ZIP 解包验签通过；标签 `v3.1.0-b43` / `d2dd3d8`。22:55 部署并恢复原任务，代码切换前后数据库 SHA-256 同为 `ea94dd7d6db94a40274da6389ad83f82cfd8c596438c9fa469c172a13d55c8f0`；恢复集 `/opt/neckline/data/backups/v3.1.0-b43-predeploy/`。Mac 已换装 B43，B42 整包恢复副本保留。正式报告仍在生成，晚晨 timer 待验收后恢复。

### Build 44：事件调查并发与截断修复

B43 正式结果：67/67 正文全部完成，66 个事件提取结果；已完成首事件的问题规划、查询规划及 5 次 Tavily 搜索，取得 25 条摘录。首个问题规划耗时 83 秒，事件调查却仍串行；证据审读又在 8,192 输出配额处截断。23:01 所有在途调用已结算后暂停，未丢未知调用，已完成正文／问题／搜索全部保留。

B44 在现有 deepReadConcurrency=6 内并行不同事件，每个事件内部仍按问题→搜索→审读→收口→比较顺序执行。共享 Tavily 调用串行保护计数，研究进度写入加锁；每个研究阶段之间检查既有 taskSliceSeconds，已付费模型检查点先持久化，即使研究快照写前分段，下一段也直接复用。最终按原事件顺序汇总，不能按完成速度抢占候选名额。

调查 Prompt 明确 claims/questions 只提交增量变化，避免每轮复述完整输入。仅调查输出截断允许既有修复次数内的一次精简增量反馈，输入证据、单次输出配额和模型参数不改；绝不接受半截 JSON、裁掉必要公司或伪造结论。定向回归覆盖并发上限／顺序、真实 CLI worker 的研究模型已完成→快照未写→分段恢复（模型不重调），以及截断修复不重复搜索。

B44 全量 929 passed；双端签名归档、Mac ZIP 解包验签及单实例换装完成。iOS 原设备暂不在线，改用 generic iOS 目的地签名归档成功。标签 `v3.1.0-b44` / `a9a85b8`；23:04 宁波部署、23:05 原任务恢复，现场确认 6 路调查外呼，已完成正文及搜索均复用。部署前后 DB SHA-256 同为 `b538343a42f66c3575012b3020a433b30ef55b434b91d98e5c4d630f7b8e6397`，恢复集 `/opt/neckline/data/backups/v3.1.0-b44-predeploy/`。最终报告尚未发布，晚晨 timer 尚待恢复。

### Build 45：研究输出信封与字段反馈

B44 正式并发后出现 action_mismatch、typed result_invalid，首事件审读仍有截断。23:08 关闭新增外呼，23:10 已全部正常结算，无未知调用；task paused，scan running，67 篇正文与 5 次搜索仍在。

代码检查发现输出契约与 action 放在不可信证据包中，通用 repair 文案又要求不得省略任何输入对象，和研究增量输出冲突。B45 把程序指定的输出形状和根 action 放到证据标记外明确说明；研究修复提示不再要求复制所有输入。仅接受单一 outputContract 对象容器，或对形状明确且匹配请求阶段的缺失 action 补回已知路由元数据；不补事实、不接受显式错误阶段、不接受任意根对象。显式阶段错误及有字段诊断的 typed 错误进入原有有限 JSON 修复，日志只记录程序已知字段是否存在、类型和期望，原文／回答／凭据不落日志。

研究检查点使用受控恢复时冻结的失败输入哈希，允许再次修复已有失败恢复组，同次授权新失败不无限重调，完成组优先复用、未知组拒绝。另修复 answered 问题不能清空 missingEvidence 的矛盾；未解决问题仍必须声明缺口，原 question/claimIds 不得改义。
