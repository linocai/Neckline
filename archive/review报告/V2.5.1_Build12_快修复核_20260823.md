# V2.5.1 Build 12 快修复核

日期：2026-08-23（Asia/Shanghai）  
结论：快修实现与本地门禁全部通过，已到部署发版前；未提交、推送、部署、换包或触发报告。

## 裁定对照

- 成立率只统计已有 10:00 终值的 `confirmed + rejected`，观察与未结算不进入分母。
- 消息剔除后不再受轮数上限截断：只要机械后备池仍有票，就继续筛查和补位；清单只有在
  后备池耗尽后才允许短于原目标。
- `maxBackfillRounds` 已从正式参数、样例、参数模型、运行逻辑、审计动作和测试中删除。
- K8 方向流水线配置、`neckline-scan.service`、`neckline-basket.service` 已删除；现役链为
  `neckline-facts.service → neckline-strategy.service → neckline-report.service`。
- 方向 sidecar 不自动重试。新增的管理入口要求精确 pack、旧 claim token 和原因；产生费用的
  retry 还要求逐字确认 pack id。新 token 会阻止迟到的旧进程覆盖人工恢复结果。

## 复核中发现并修正

1. 删除 `ExplainParams` 后，全量测试里的参数类数量下限仍写死为 14。保留真正的
   `declared == reached` 完整性守门，将数量同步为 13。
2. 一条客户端测试把数据库内部 snake_case 当成在线 API 响应；服务端实际已经通过
   `_explain_api` 转为 camelCase。测试改用真实 API 形状，没有反向破坏生产 DTO。

## 验证证据

- 后端全量：`1358 passed`；另有 21 条 Polars sortedness 已知 warning，无失败。
- 后端窄回归：`213 passed`。
- `scripts/smoke_api.sh`：通过，使用临时数据库。
- `scripts/smoke_review.sh`：通过，真实 xlsx 上传、回读和设置回写均使用临时数据库。
- macOS 测试：44 项，38 通过、6 项按设计跳过（需要 127.0.0.1:8002 开发服务），0 失败。
- iOS 26.5 Simulator 测试：44 项，38 通过、6 项同上按设计跳过，0 失败。
- macOS build：通过。
- generic iOS Simulator build：通过。
- generic iOS Simulator build-for-testing：通过。
- `git diff --check`：通过。
- 参数包：`k9-params-20260823-r2`；SHA-256
  `d926cb048c011796d4a1a8df3555f11849332dd89daef5d8d1708861df25f1a7`。

## 部署边界

- 参数机械值不变、schema 不变，不回写历史事实、清单、报告或成绩。
- 部署时必须先停止 `neckline-evening.timer`，再替换 target 与三个 service，删除旧
  `scan/basket` unit，执行 daemon-reload 并核对依赖后重新启动 timer。
- 不允许用真实 `neckline-evening.target` 做演练；它会产生 LLM/Tavily 费用并可能推送。
