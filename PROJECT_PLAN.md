# Neckline V3 · 3.0.4 / 双端 Build 36（施工中）· K10-v1.4

本文件是唯一工程控制面。正式策略见 [`whynotme/K10.md`](../whynotme/K10.md)，生产不得读取或导入研究仓；运行、安装与恢复入口统一见 [README.md](README.md)。

## 当前目标与事实

- 用户已授权快修和 Build 36。目标是让 K10 在大规模输入、模型结构异常、短暂网络故障、进程中断与 APNs 配置失效时，有界地继续、如实呈现，并可从冻结输入恢复；不改变 K10-v1.4 的选股、发布、两日观察或统计口径。
- 已发布基线为 **3.0.4 / 双端 Build 35 / K10-v1.4 / Schema 3**，源码 `15628894046762fa0b2085964af91efc7e76e612`、不可变标签 `v3.0.4-b35`、生产策略配置 `k10-v1.4-production` 修订 2。Build 36 仍为 3.0.4，发布标签将为 `v3.0.4-b36`。
- 2026-09-07 21:00 晚扫正常入队和采集，21:01:16 冻结 2472 篇输入；第 7 次 DeepSeek 理解调用在 21:07:23 后使整轮进入 `failed/discovery_failed`。已存 7 条成功用量记录，无候选、发布批次或公司窗口；API/worker 未崩溃。当前证据只能定位到理解输出的解析、结构或引用校验链，未保存原始模型答复，不能倒推具体坏字段。
- 旧实现串行理解全部文档后才进入核验；完成的理解没有逐项检查点，单项异常终止整轮，任务 `maxAttempts` 只限制领取次数、不调度恢复。前七次平均耗时只可用作约 35.9 小时串行估算，不能作为实测时限或发布承诺。
- 冻结资料实际含 HTML 广告、`script`/`iframe` 等噪声：共 21,001,454 字符、正文 2456 个唯一版本，中位数约 6,882、P90 约 15,382 字符。提纯必须保留文章事实、表格、否认与更正，冻结原文保持不变。
- 同一失败通知因 APNs `.p8` 路径不可读而截至 08:40 约尝试 2.04 万次、无送达；B35 和 B33 runtime 备份都无该文件，尚不能从此判定丢失时点。后续受控检查已在历史部署备份发现与配置 Key ID 对应的私钥候选，并已验证可本地签名；发布前只校验存在性、权限和签名能力，绝不输出私钥内容。
- 事故诊断阶段未改生产、重跑任务或调用模型/搜索；本地 Build 36 施工已开始。生产的 2472 篇输入、失败任务、来源水位、现有记录和已发布窗口都不得被删除、回填或用旧备份覆盖。

## 不变的 K10-v1.4 产品裁定

- K10 是纯选股器：消息分析、事件内公司比较、选择、所选公司正反分析、晨报和固定 D1/D2 效果验证保留；交易计划、价位、持有/退出、仓位和收益结算已退役。
- 股票池仅创业板；无股价/金额上限；排除 ST/*ST 和申万 2021 白酒Ⅱ `801125.SI`。模型统一 DeepSeek V4 Pro。缺少或无效策略包时只显示“今天没跑成 · 参数未配置”，不产生候选。
- 流程仍为“增量采集 → 基础去重 → 明确模板初筛 → 轻量理解 → 重点核验 → 候选”。来源主体可在池外；不得用栏目、利好关键词、题材、长度或代码命中做隐蔽删选。疑难资料留核查通道，降级资料有计数和抽查入口。
- 推荐前必须完成事件共同事实、公司映射、主推/备选或并列比较、优先原因、差距和改序事实；全部完成的可推荐公司再统一全局排序。晚间正式推荐最多 30 家不同公司，同公司多催化仅一张卡、一份选择和一份成绩。任何局部先完成的事件都不能先占名额。
- 所有正式样本依实际 `availableAt` 固定 D1/D2，用户的留下、略过或未处理不回改窗口。旧消息更新、用户改选、撤回和普通跨日延续不重开机会；重叠机会单列且不重复进入主命中率。补跑昨晚输入的可查看时间只能是这次实际完成并可读取的时间。
- 主成绩只统计到期、两日涨停状态完整可核的 primary 样本在 D1/D2 内是否至少一次收盘封板；停牌、缺数、异常、未到期和 overlap 分列。价格变化不是实盘收益。

## Build 36 工程设计

### 1. 冻结策略与执行配置分离

- 保留每个 scan 已冻结的 K10 策略配置、截止时间、输入资料版本和机会快照。新批大小、并发度、单项/整轮重试、退避、超时与完成时限属于 **执行配置**，建立独立、追加、哈希可核的 execution profile；所有字段显式验证，无代码兜底值。
- 新建扫描任务在入队时同时绑定策略配置和 execution profile 的 ID、修订和内容哈希。扫描执行、任务检查点和对外 DTO 都返回这两个命名空间，避免把工程参数显示为 K10 策略版本。
- 对 Build 35 失败的首轮扫描，创建受控的恢复子任务并先写入新的执行绑定：父任务、原 scan、原策略配置、2472 个 `documentId@revision`、原 cutoff 和输入摘要必须逐一一致；恢复任务禁用重新采集、禁用来源水位推进、禁用新证据替换。旧失败任务保持历史终态。
- 恢复或新任务的自动重试都在绑定 execution profile 的次数和退避上限内运行；超过上限才终态失败。未绑定 execution profile 的遗留排队任务不得以隐含参数执行，应被明确标为未配置或由受控入口重新绑定。

### 2. 可恢复的分层发现

- 普通程序按稳定来源版本做基础去重，提供具名模板过滤与待核通道。目前策略没有已定义的模板规则，生产启用规则为 0 条；不把 HTML 提纯冒充模板筛选，也不私设关键词排除。记录输入、去重、理解、待处理与失败计数。
- 冻结 HTML 以安全解析得到文章正文与关键段落，排除脚本、广告和嵌入噪声，同时保留表格、否认、更正、数值和事实原文；原始 `original_text` 绝不修改。每篇先给模型标题、时间和关键段落；模型明确要求全文时，仅该条按原冻结全文继续。
- 理解请求可小批量并行，但每条结果独立校验、原子持久化为规范化事件草稿或安全失败状态。原始模型答复、上游异常正文、token 和凭据不得存入错误文本或 API。
- 持久进度至少能逐 `scan + execution + document revision` 恢复：过滤结果、理解/全文状态、已校验结构化输出、尝试数、下一次允许时间、安全阶段错误码和更新时间。进程在任一写入后中断时，只重做未完成或允许重试的项目，不重复调用已成功项目。
- 文档级解析、结构、引用、超时或可恢复网络失败在限额内隔离；其余独立资料继续。不可恢复或用尽尝试的资料标为 `failed_pending`。扫描只能据已完整理解的事件继续，且必须把积压、失败和来源覆盖写成 `partial`；不得宣称资料已全量处理或“全局最佳 30 家”。
- 事件级 Tavily 核验、公司映射和同事件比较只消费完整事件；缺关键独立证据、映射或比较的事件不能形成正式候选。所有完成比较的事件汇总后一次性执行全局优先级和 30 家不同公司的配额；再持久化和发布，保持既有机会去重与窗口逻辑。
- 晨报的已发布候选反证、论点变化和到期提醒走独立既有路径；发现积压不得把这些必需内容伪装为“无变化”或无限阻塞。

### 3. 任务、进度和安全诊断契约

- Schema 4 采用追加迁移，保存 execution profile、scan/execution 绑定、逐资料发现进度及任务 `notBefore` 重试调度；迁移须在真实生产 DB 副本验证旧 Schema 3 行、2472 输入和失败任务业务等价，并提供只向前恢复路径。读取继续零 DDL。
- Worker 只把明确可恢复的、带安全代码的故障按已冻结计划重新排队；租约丢失由下一拥有者继续，不能由失租 worker 落终态。最终 task/scan 状态、检查点和失败通知对应同一逻辑执行，不能把已安排恢复伪装成终态失败。
- `ScanOut` 追加可选 `executionProgress`：`state`、`stage`、`documentCounts`、`eventCounts`、`coverageStatus`、`nextRetryAt`、`safeFailures`、`strategyBinding` 与 `executionBinding`。历史 scan 可为空，不得伪造。`safeFailures` 只允许 `stage + code + documentId@revision`，不含模型原文、上游响应体、令牌、私钥、内部 URL 或未经清洗的异常字符串。
- 客户端以这些 DTO 呈现已获取、理解中、待核验、公司比较、统一排序、发布或失败待恢复；部分结果明确“仍有资料待处理，不能代表完整覆盖”，成功候选仍沿用 K10 卡片、比较、选择和统计的既有语义。

### 4. 通知可靠性

- 通知迁移独立升级，记录 `notBefore`、退避尝试、阻塞原因和终态；重复 transient 失败按显式 `notification-delivery-v1.json` 运行配置退避，不能每个 worker loop 重发。通知不依赖 scan 执行绑定，旧失败通知与分析通知均可恢复；缺少通知运行配置不猜参数。
- APNs 在启动、发布 readiness 和发送前检查配置四要素、私钥可读性与实际签名能力。凭据缺失或不可用时将通知置为可见的 `blocked`，记录安全代码，停止无效发送；配置恢复后才按受控状态重新排队。设备失效仍按现有规则单独清理。
- 新增只读 `GET /api/v1/k10/operations/readiness`，返回 `notificationReadiness.state`、`reasonCode`、`nextRetryAt` 与 `checkedAt`；HTTP 200、过往 scan 或任意历史配置修订不能替代当前运行绑定。Swift 可选刷新该状态，不暴露私钥路径。

## 施工分工与接口边界

- **发现层**：`discovery.py`、`pipeline.py` 的 HTML 提纯、批理解、按需全文、事件核验/比较/统一排序，以及 discovery/pipeline 回归；只接收 execution 层给出的冻结资料和逐项持久回调，不直接写 task/recovery schema。
- **执行层**：`schema.py`、`store.py`、`worker.py`、`config.py` 的 Schema 4、execution profile、绑定、检查点、调度与受控恢复；对发现层暴露一次性、租约受保护的 progress repository。不得要求旧策略配置拥有新执行字段。
- **通知层**：`notifications.py`、`notification_runtime.py`、`push/apns.py` 的 readiness、阻塞和退避迁移；不改发现、候选或统计语义。
- **API/客户端层**：安全 progress / notification readiness DTO、Swift 解码与白卡风格状态展示；不从手写 fixture 推断后端状态。根会协调生产恢复、性能验收、版本、签名和发布。

## 验收门槛

- 保留“第 7 条理解输出失败整轮”的最小复现：该文档出现 JSON、字段、引用或全文路由异常时，其余资料仍持久，重启后仅继续未完成项；最终全量或 partial 状态、事件、候选和任务重试记录一致。测试同时覆盖 HTML 噪声提纯不删事实、批次局部失败、网络超时、provider 重试耗尽、租约中断、旧冻结恢复和新资料不得混入。
- 覆盖模板降级/抽查、关键段落到全文、事件级 Tavily 核验、缺关键证据/比较不发布、同公司多催化、全局统一排序后再限 30、partial 不装完整、晨报反证不被积压遮蔽，以及既有 K10-v1.4 的窗口、撤回、overlap、选择和缺数统计回归。
- 从真实 CLI 入队和 worker/handler 边界执行隔离的 2472 篇冻结快照：记录实际分层数、调用配置、token、重试、并发、耗时、失败隔离和恢复耗时；本轮显式执行预算为首次实际执行起 7200 秒，正常分片间隔 1 秒（失败退避另算）；重启不得重置起点，实测须验证能在预算内完成。另注入畸形输出、网络错误、worker 重启与 APNs 私钥缺失，证明无无限循环、无重复调用成功项、无假完成。
- API 集成测试必须由临时 SQLite 的实际 FastAPI 响应喂给当前 Swift models；双端实际填充和空态验证 progress、partial、withdrawn、morning、overlap、缺数和分析修订。若改动 Swift，必须通过 AGENTS.md 列出的三条 xcodebuild；Backend 全量 pytest 必须通过。
- 发布前在确认目标服务器的生产 DB 副本演练 Schema 4、旧首轮恢复绑定和回滚；正式部署前备份当前 DB，绝不恢复 B35 发布前快照覆盖 2472 篇已写资料。恢复 APNs 私钥只从已定位的受控历史备份复制，验证 owner/group/mode 和签名，不记录内容。
- Release Build 36 必须完成版本/签名/哈希/部署 DTO readiness 核验、Mac 单实例替换与旧 QA 清理、iOS IPA 交付、`main` 提交推送和不可变 `v3.0.4-b36` 标签；新恢复任务若发布候选，`availableAt` 取可读取的真实完成时间，绝不回填昨晚。

## 待决定项

无。批大小、并发度、重试和完成时限由隔离 2472 篇实测后写入显式 execution profile；这是工程配置选择，不改变 K10 策略。

## 下一步

完成真实 2472 篇 CLI/worker/handler 恢复验收与发布包冻结，然后受控生产迁移、APNs 恢复和旧失败任务恢复，替换 Mac 单实例并交付 iOS IPA。最后发布不可变 `v3.0.4-b36` 并将本计划和 README 收敛为当前运行事实。

## 施工检查点 · 2026-09-08 10:38 CST

- 双端元数据为 3.0.4 Build 36；生产仍 B35。没有提交、标签、推送、生产切换或正式客户端替换。
- Schema 4、通知 Schema 2、追加执行配置、逐项持久、模型/Tavily 有界调用与缓存、冻结输入恢复、固定 deadline 和通知 readiness/退避均已接通。实际 `recover_scan → run_once → production_scan_handler` 在 TuShare token/adapter 不可用时仍复用原冻结资料。
- 根最新 Backend 全量为 **749 passed**，21 条既有限价数据 warnings，`git diff --check` 通过。双端三条 xcodebuild 全过；macOS/iOS XCTest 各 34 项、3 条条件跳过、0 失败；真实 FastAPI → Swift 临时后端契约另行通过。QA 与临时 API 已关闭，保留生产客户端。
- 首次批次屏障实测未达门槛，已改持续有界队列：并发 12、待执行容量 24、理解输出上限 8192，正常分片 110 秒后停止提交并保存已付费在途结果，1 秒后继续；整轮时限从首次实际执行起固定 7200 秒。其他模型阶段参数和 K10 策略不变。
- 真实快版第一片 121.766 秒：300 次调用、560189 tokens、299 篇理解成功、1 篇引用校验失败隔离。此前两个截断资料均成功。自然退出后保存 598 个成功模型/文档检查点的逐行哈希，再恢复同任务；不重建 DB、执行绑定或起点。
- 服务器私有 `/tmp/neckline-b36-qa/full.sqlite` 的恢复任务 `task_b04bbf7808525d32bb317fb2f4388ad0` 正在运行；首次起点 `2026-09-08T02:20:26+00:00`，2472 冻结来源、原策略/截止/机会快照、来源水位和父失败任务每片校验不变。当前真实模型用量和进度见本地 `/tmp/neckline-b36-validation/full-fast-resumed.log`；全轮未结束，不能宣称上线就绪。
- 实际恢复观察发现并修复 API 把已领取任务的历史 retry 行当成等待恢复的问题：仅 queued 任务显示下次恢复时间，running 显示实际阶段。该补丁只改只读聚合，不改任务、模型输入或执行配置；无需重跑已成功模型。
- `/tmp/neckline-b36-qa/source-snapshot.sqlite` 是含凭据的服务器私有只读备份，SHA256 `3fdef3da6ef3147ddbe33e4ffef116d12303e2d52fa0b53cd384ef82963fff5a`，不得下载或覆盖生产。失败首版测试另保留 `full-batch8.sqlite`，不作为验收通过证据。
- 发布助手 `/tmp/neckline-b36-release/production_release.py` 尚未执行；迁移函数已在真实 DB 副本演练，45 个旧表全部旧列/行等价，Schema 4/通知 Schema 2/执行修订 1，策略修订 2 不变。正式迁移前仍须停全体写入者并重新备份当前生产。
- APNs 活动私钥仍缺失；已在受控历史备份验证对应 P-256 私钥可签名，正式切换时恢复并核验路径/权限/签名。新 worker 开启前失败可恢复本次预部署 DB 配套 B35；开启新写入后只前向修复。
