# Neckline V3 · 3.0.6 / 双端 Build 38 · K10-v1.4

本文件是唯一工程控制面。策略行为以 [`whynotme/K10.md`](../whynotme/K10.md) 为准，生产不得导入研究仓；本轮施工契约见 [archive/v3.0.6-b38_execution.md](archive/v3.0.6-b38_execution.md)。

## 概述

3.0.5 / Build 37 的未发布方案把“控制用量”错误实现为总 token、调用和搜索预算门槛，也仍会将资料包正文送入首轮模型。本轮将其删除，改为完整标题理解后再按资讯篇数限额深读：晚间最多 80 篇、晨间最多 40 篇。标题初筛用 DeepSeek V4 Pro 分批理解每条标题，完成跨批全局去重、排序，再对拟入选标题做只减不补的终检；最终清单冻结后才读取正文。

## 稳定技术决定

- 不设整轮、整日或阶段的金额、token、模型调用和搜索调用总上限，也不以巨额数字伪装“无限”。保留逐次真实用量账、请求身份、缓存、持久暂停和有限重试，目的在于可追溯和防重复，不作为额度闸门。
- 每篇标题输入严格只有 `documentId/revision`、`sourceKey`、`publishedAt` 和标题；禁止正文、摘要、关键段、URL 内容或开放 metadata 字典混入。另可提供已有正式候选代码和旧机会标题作为优先级上下文，不含旧正文或分析。每条去重后标题均有可审计去向；漏 ID、重复/伪造 ID、截断或全局合并不完整时，有限重试后以 `partial` 终止，零篇正文深读。
- 标题批次只做逐条语义判断和事项指纹，不能每批先截取 Top N。全部批次完成后再全局合并同一事项、保留更正/否认/重大反证并冻结全局排序；同事项转载无新增事实仅保留最合适的一个真实来源文章。来源、海外/上游、无创业板代码、篇幅、栏目和利好词均不是硬排条件。
- 正文深读按真实独立来源文章计数：晚间 `≤80`、晨间 `≤40`，不足不凑数。资料包、全文回退和 Tavily 新抓全文均不能绕过该数；已选正文缺失也保留其名额和原因，不以补读新文章悄悄替换。Tavily 只核验入选事件，默认使用搜索摘录；若实际读取新增文章全文，同轮也计入 80/40。
- 优先级仍为已发布候选的重大反证、既有事实变化、一般新事项。入选后沿用 K10 的事件核验、公司比较、晚间不同公司 30 家上限、跨日延续、D1/D2 与成绩样本规则；不恢复交易计划、收益结算或 K9。
- 生产仍停机。此次授权包含本地施工及一次真实、隔离、最新时窗的验证：标题全量理解、深读不超过当窗 80/40、先检查标题审计质量，任一协议/质量错误即停止。它不授权部署、开启 worker/timer、恢复历史任务或全量付费回放。

## 当前状态

- 线上仍为 `v3.0.4-b36` / Schema 4，worker、晚间 timer 和晨间 timer 停用；API 仍运行。旧 scan `scan_1e8502168c080f33b4e5d0ac5e775c48` 与关联任务是用户已弃用历史，绝不恢复。
- 本地基线 `cc384f1`（3.0.5 / Build 37，未发布）。其中 V2 总预算、资料包首轮正文和相关 UI/测试已删除；B37 仅保留历史执行记录，不作为兼容契约。
- V3 标题选择、冻结正文准入、实际请求账、API 与双端已接通；后端全量 824 项通过，双端三条构建、当前 FastAPI→Swift 解码及填充/空态 UI 通过。一次 QA 误覆盖主模拟器，已恢复 B36；模拟器旧本地数据无法确认保留，真实手机/Mac未动，详见版本记录。
- 隔离晨间验证已停止，不能判定全链路通过：1,431 篇输入精确去重为 1,430 条，23 批标题及全局选择/终检完成；37 篇冻结正文全部完成，Tavily 9 次搜索获得45条摘录、无新增全文。公司比较出现一次 `model_execution_invalid` 后隔离控制自动关闭，最终 scan 为 partial、正式候选为0；未完成核验的事项不能归因于全部缺证据。原始失败响应未留存，具体校验原因尚未定位；已在本地补齐比较分项安全错误码，不改变既有校验行为。
- QA 进程已退出，9份临时/备份数据库中的复制凭据全部清除，库完整性通过；生产 API active+enabled，worker 无进程且 disabled、晚/晨 timer disabled。最终回执、用量和未完成项见本版本记录。
- 2026-09-08 13:58 北京时间已备份并取消旧批次的两个任务，旧 scan 写入 `retiredByUser`；业务记录保留，生产进程未启动。回执位置见本版本记录。

## 当前 Plan

1. **定位公司比较失败，补齐真实验收。** 安全分项诊断已完成并回归通过；原失败响应不可回放，当前证据不足以声称此项已修复。先只读最终回执与保存的模型/核验检查点，设计仅针对失败公司比较的诊断验证，不能放松 K10 校验。下一次真实调用必须是明确范围的验证，保留已完成标题/正文结果，不重跑全量，不恢复事故任务。验收要求比较通过或明确给出可解释的无候选结果，不能用 task completed 替代 scan 完整性。

## 待用户决定

无。发布、恢复定时器及生产部署不在本轮授权内。

## Backlog

- 公司比较缺口解决后建议独立审查；本轮未调用 reviewer。发布仍须覆盖线上 B36→目标版本的完整差异、回滚和 schema 迁移。

## 里程碑索引

- 3.0.4 / Build 36：已发布后因全量逐篇模型处理被用户叫停；边界见 [archive/v3.0.4-b36_execution.md](archive/v3.0.4-b36_execution.md)。
- 3.0.5 / Build 37：本地未发布的预算/资料包方案，已被本轮替代；历史见 [archive/v3.0.5-b37_execution.md](archive/v3.0.5-b37_execution.md)。
- 3.0.6 / Build 38：标题全量初筛与 80/40 深读已实现，824 项回归通过；真实公司比较验收未通过、未发布。见 [archive/v3.0.6-b38_execution.md](archive/v3.0.6-b38_execution.md)。
