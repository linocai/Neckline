# Neckline · V2.7.0 Backend Build 22 / K9-v3

> macOS/iOS 客户端与分发集合仍为 V2.7.0 Build 19 / `v2.7.0-b19`；NB 云后端在同一 API 合同上推进到 `v2.7.0-b22`，客户端无需重新安装。K9-v2 已退役，不再作为发布、等待、迁移或历史成绩处理对象。
>
> 本轮状态：**V2.7.0 Build 19 已发布，但首个 K9-v3 报告现已确认是错误空包，不能使用**。19:00 首跑因 fp-4 错把 40 日上市门槛实现成精确上市生涯计数而 `not_run`；热修复删除该过度依赖并补齐 60 个 fp-4 后，受控重跑写出了 `empty`。后续逐闸审计证明：共同边界把 TuShare 千元口径的 `daily.amount` 直接与 CNY 阈值比较，且当日 5545 行 `free_share/free_float_mv` 全空仍通过 readiness，导致 5545→0 后三个通道根本没有召回池。当前生产空包保留作审计，但不得再称为可信结果；尚未修复、未重跑、未生成纠正版正式候选。
>
> 方向输入（只读，不导入生产）：`/Users/linotsai/Lino/whynotme/K9.md`（SHA-256 `ba6d915e355c66166ba7e0b4c4546d88be6c66acfabc2db4afa09aff5a27728f`）与 `Neckline新架构_20260818.md`（`c665ba562d03e6af7ff00cc2b41905a6b6e569d6311bcb08803e999e04c3841e`），均在 2026-08-30 读取；若施工前源文档变更，先更新本计划的合同与指纹。

## 1. 目标与不可变边界

- 交付 `V2.7.0 / K9-v3` 的后端、macOS 与 iOS 同 API/DTO 合同；客户端显示 `2.7.0 (19)`，分发集合为 `v2.7.0-b19`，后端数据正确性修复身份为 `v2.7.0-b22`。本轮冻结 `fp-4`、`k9-params-v3`、`d2-v2` 的工程契约命名和校验命名空间。
- K9-v3 参数包 `k9-params-20260831-v3-r1` 已由用户批准并按原字节接入 `Backend/config/k9-params.json`（SHA-256 `feb24c8199b061b31e33fa9b47603e3d9cc27d76eaa0aff064f1b451d01b41a2`）。加载器只接受完整、不可变的 `k9-params-v3`；缺失、错误、旧包或混合包统一显示“**今天没跑成 · 参数未配置**”，不生成正式候选、预案、成绩包或推送。
- Neckline 只消费已批准的 JSON 参数原件，绝不导入、执行或读取 `whynotme`；测试夹具必须隔离，不能被生产路径发现。
- K8 与 K9-v2 均已退役；不恢复它们的运行时代码、路由、缓存、界面或成绩链，也不把其在途状态、迁移或历史处置列为 V2.7 发布门禁。
- 所有策略读当日冻结事实包；策略层无联网、无 LLM、无默认阈值。解释与预案不能改变机械召回、排序或额度。

## 2. 已决合同与版本矩阵

> **2026-08-30 用户裁决 · K9-v3 预案链**：机械层冻结候选、通道、排名、额度与每通道条件骨架；LLM 仅能基于冻结 fp-4 事实填写逐票的支撑、压力、失效、高开透支、行业条件与解释，严格校验后生成自动冻结的“预案第 1 版”。用户修改只可追加“预案第 N 版”，原版本永久保留。D1 9:26 核对冻结当时最后版本及完整条件，此后不得改写该批 D1/D2 判定。LLM 缺配置、失败或非法输出一律 `playbook_not_generated/not_run`，不建立半包、不推送，也不得以机械或样例数值替代。
>
> **2026-08-30 用户裁决 · P4 盘中行业证据**：不向新浪/腾讯股票实时适配器请求申万 `.SI` 行业代码。D0 必须从 fp-4 `signalTradeDate` 的时点成员事实冻结每个 P4 行业的完整成员集合、哈希、数量、聚合合同和批准基准身份；D1 只采集冻结成员股与冻结基准的真实成交快照，再计算行业中位收益、上涨宽度和相对基准收益。成员不完整、覆盖率不达冻结预案要求或基准缺证据时如实 `unavailable`，不得临时读取当前成员表或以行业指数/日线替代。

| 身份 | V2.7.0 的固定值 / 行为 |
|---|---|
| 系统与 API | `2.7.0`；生产后端 release set `v2.7.0-b22`；服务端与两端 App 使用同一 API/DTO 合同 |
| 客户端构建 | macOS/iOS 均为 `2.7.0 (19)`；发布标签 `v2.7.0-b19` |
| 策略与事实 | `K9-v3`、预留冻结事实契约 `fp-4`；不得原地改变 fp-3 语义 |
| 参数与结算 | 预留 `k9-params-v3`、`d2-v2` 命名空间；参数到位时全部阈值、权重、额度、启用通道、P4 基准和 D1/D2 数值必须显式必填 |
| 无参数态 | 服务、报告和 App 可正常工作；报告保留失败原因，既有成绩包照常推进，绝不回退 K9-v2 |

K9-v3 的工程形状已经确定：P1 完全暂停；P2 保留超跌反弹但改为单层通道内排序；P3 分离“热门强博弈身份”与“上涨机会优选”；P4 改为申万 2021 二级行业超跌修复两阶段。取消严格/宽松、跨通道统一总分、跨日接力、保底席位、补位和固定 60 条队列；数量不足就少出。

| 层 | K9-v2 现状 | K9-v3 落地合同 |
|---|---|---|
| 硬边界 | strict/relaxed 活跃度与 spike-fade | 单一不可放宽闸门：可交易身份、未 ST/退市风险、价格/上市天数、有效报价、60 日活跃度、D0 流动性及收盘涨跌停；通道私有条件不外溢 |
| 通道与排名 | P1–P4、统一排名与配额 | P2/P3/P4 各自召回、去重、通道内排序和独立额度；合并卡保留全部通道署名与位次 |
| P4 | 资金领先价格 | 行业先筛集体超跌/宽度/修复，再选行业内相对强且流动的股票；概念板块不进入机械计算 |
| 在途控制 | 滚动候选队列 | 新 D0 排除紧邻两个正式选股包的所有股票；只取下一名，不放宽阈值；按包序而非自然日计算 |
| 成绩 | 滚动 `k9_predictions`、D0 收盘起算、五指标 | 不可变独立成绩包，D1 10:00 可交易参考价起算；D0/D1/D2 只向前追加，历史永久可查 |
| D1/D2 | 9:29 两段、10:00 三分支 | 9:29 为拒绝/确认不可买/待开盘；10:00 为成立/放弃/观察/不可买；另存 D1 收盘增强/保持/转弱/不可评价及 D2 八类结论 |

## 3. 施工阶段与验收

### P0 · 冻结合同与删除边界

- 将本计划、K9-v3 指纹、版本矩阵和字段字典作为施工唯一依据；将旧 V2.6 发布流水从控制面移除，不改写 Git 标签或研究仓。
- 列出待删除的 V2 运行语义：P1、`Tier`/`SeatKind`、严格/宽松、`quota.py`、`ranking.py`、旧 P4 moneyflow、滚动评分、固定 60、旧 DTO/缓存/路由；先由替代链路覆盖，再物理删除。

验收：代码中不存在 K9-v2 回退或裸 `vN` 展示；任何版本、参数、事实、预案修订均有命名空间。

### P1 · fp-4 事实与参数合同

- 建立 fp-4 schema、构建/冻结/回补/完整性与研究快照导出；审计并冻结不复权价、真实自由流通市值、上市天数、历史时点退市状态、收盘涨跌停、60 日金额/参与度、SW2021 L2 每日成员、行业中位数/宽度与批准市场基准。
- 建立 `k9-params-v3` 的无默认校验结构：后续参数包必须显式提供 P2/P3/P4 判据、启用位、排序权重、额度、P4 基准、D1/D2 门槛、风险线、版本来源和审批信息；生产代码不得预填数值，并拒绝 fp-3、K9-v1/v2、未知键、缺键和交叉约束错误。

验收：同一事实输入字节级稳定；缺数据、未命中、不可交易可区分；无合格参数时端到端只产出“参数未配置”。

### P2 · K9-v3 引擎与独立候选包

- 以单一硬边界后分别实现 P2、P3、P4；P3 机会优选与身份召回分层，P4 使用冻结行业成员；所有数值仅由包输入，P1 不产生任何输出。
- 以 `batchId` 创建不可变选股包，保存 `selectionDate`、`signalTradeDate`、D1/D2 交易日、策略/参数/事实/标签谱系、候选、全通道排名、冻结预案、基准和门槛；重选只能创建显式修订包。
- 用包序实现两期去重，并为空候选、周末双日期、同股票多通道和重跑幂等建立金样。

验收：策略读取范围不越过冻结日；独立通道不会互相加分；相同包与事实重跑不重复、不覆盖、不串版本。

### P3 · D1/D2 追加式结算与调度

- 新建批次、候选、D1、D2 与报告引用表，D0 候选/预案不可改写，D1/D2 只追加；读路径不得执行 DDL。
- 夜间任务按交易日历独立且幂等地执行“结算到期 D2 → 更新到期 D1 → 创建今日 D0”；任一步失败不清空其他包，报告失败也不阻塞到期结算。
- D1 冻结 9:29 三段、10:00 四分支、可买性与收盘延续；D2 固化八类结论、U/C/L、路径状态和原始收益。K9 可交易账、D0 方向诊断账、真实交易账严格隔离；风险标签不覆盖结论。

验收：生命周期只允许 D0→D1→D2；不可评价按期结案并标覆盖状态；聚合只读同一策略/参数/事实版本且保留 P2/P3/P4 切片，不产生综合总分。

### P4 · API、缓存与 macOS/iOS

- 新接口合同：`GET /scoreboard/packages?state=active|settled` 返回可筛选包摘要；`GET /scoreboard/packages/{batchId}` 返回冻结候选和当前阶段；`GET /checklists/{batchId}` 只返回该包 9:29 核对；报告 DTO 以批次引用今日 D0、到期 D1、到期 D2。旧 `/scoreboard/listing` 与按交易日 verdict 路由在硬切后删除，不做适配层。
- 选股页显示 `K9-v3`、全部通道及通道内位次；个股页和核对表显示 `K9-v3 · 预案第 N 版`。核对表改三段，成绩页改“进行中/已结算历史”日期包卡与详情，分别呈现选股结果和预案结果，不展示 60 队列、P1、总评分或优秀评级。
- DTO、AppModel 与 `ReportSnapshotStore` 使用策略/事实/参数/标签合同和 V2.7 缓存命名空间；升级时显式失效 V2.6/fp-3 快照，禁止旧清单冒充新策略。

验收：后端 JSON、macOS 与 iOS 对空包、失败、双日期、不可买、覆盖状态理解一致；设置、选股、详情、核对、成绩历史均无裸版本号。

### P5 · 发布候选与上线

- 在临时数据库演练前滚/回滚，验证备份哈希、既有事实不变、GET 零 DDL、定时器重复触发及崩溃恢复；K9-v2 不参与发布判断。
- 更新 `project.yml`、pbxproj、后端 `VERSION`、发布守门与签名材料为 2.7.0/19；后端与两端 App 作为一个 `v2.7.0-b19` 候选签名、验签和交付。
- NB 云权威事实以 `/Users/linotsai/Lino/NB_info.md` 为准；现行主机 `114.66.2.205`，业务入口 `nk.linotsai.top`。发布顺序：停新报告与早盘写入→确认目标/定时器/可恢复备份→部署及后端健康检查→部署匹配客户端→恢复任务。回滚恢复同一备份中的服务、schema、配置、参数状态和可恢复客户端，绝不保留混合版本。

验收：生产健康、schema、任务审计、服务/API/客户端版本均为 2.7.0/19；无参数态不出清单，已有历史不被覆盖。

## 4. 参数接入与 K9-v3 Day 1 启用门

参数包接入不重新设计系统：已校验原始字节、哈希、审批、`K9-v3/fp-4/d2-v2` 工程谱系和全量约束；D1/D2 分类阈值随本次完整参数包一并获批，发布时由生产 `K9_PARAMS_PATH` 显式指向同一原件。隔离 fp-4 重放不是发布门禁。首次 K9-v3 Day 1 仍只读取当日真实冻结事实；readiness 不完整时链路自身 fail-closed，不生成候选、成绩包或通知。

## 5. 验证门禁

- 后端：全量 pytest；参数拒绝、事实时点/来源、P2/P3/P4 金样、两期去重、日历/周末、批次幂等、D1/D2 八类、三账本隔离、API 契约、迁移前滚/回滚、无参数安全态。测试只用临时库或只读快照。
- App：每次 Swift 改动必须依次通过 `xcodebuild` macOS build、iOS Simulator build、iOS build-for-testing 三条门禁；再以实际安装产物核验 macOS/iOS 的选股、预案、核对、成绩历史、设置和缓存失效。
- 发布：签名/哈希、NB 备份恢复、timer 状态、API health、安装路径与进程版本全部留可复核证据；无成功参数包不运行策略或生成 Day 1。

## 6. 本轮范围外

- 参数搜索、回测调优、研究侧校准和 K9-v3 参数批准；它们只在 `whynotme` 完成。
- K10/多策略并行、真实交易账的写入、阅读行为记录、数据供应商与安全边界改造。
- 任何 K9-v2 兼容页面、旧策略默认值、P1 占位通道、影子候选池或隐式补位。

## 7. 已决：退役边界

- 2026-08-31 用户明确裁决：K9-v2 已退役，不再处理其在途状态、D2 等待、迁移、历史成绩或展示，也不得让这些事项阻塞 V2.7 发布。
- 隔离 fp-4 重放不再是发布前置条件；正式链路仍须遵守无默认参数和当日事实 readiness 的 fail-closed 合同。

## 8. 当前状态与首个施工动作

- [x] 已完成 K9-v3、架构、V2.6 实现、版本治理、数据库/API/App/NB 发布路径盘点；本计划已从 258 行/19KB 收口为当前唯一控制面。
- [x] P0：前后端版本已统一为 `2.7.0 (19)` / `v2.7.0`；V2.6 运行测试与滚动报告测试已退役，V3 的参数拒绝、成绩包生命周期和两包去重测试已建立。
- [x] P1–P2：`fp-4` 已成为独立构建/冻结/读取合同（不复权行情、CNY 自由流通市值、交易日龄、时点 SW2021 成员与行业原子事实）；缺少历史成员时点、关键字段或完整参数都会安全拒绝。`k9-params-v3` 已对 P2/P3/P4、边界、额度、结算、版本及权重做无默认严格校验；K9-v3 仅含 P2/P3/P4 的单一硬边界、通道内截断和两包去重补位，D0 只能冻结真实预案/基线/阈值。
- [x] P3–P4：fp-4 已接入日更、显式回补和就绪门；晚间链按 D2→D1→D0 隔离推进，报告以 `k9_package_reports` 投影包状态。D1 10:00、D1 收盘与 D2 均为不可覆盖追加；D2 固化八类结论、原始收益、独立 risk tag 与 coverage，并保留不可交易拒绝的诊断基线；只聚合同版本 P2/P3/P4 切片。旧预案、旧行情采集、V2 覆盖率表/API 和 live schema 已删除，既有旧表只允许显式迁移工具读取。App 已切换为不可变成绩包：新 DTO、三段 9:29 / 四态 10:00 / 四态 D1 收盘 / 八类 D2、双日期和版本谱系全部从包读取；选股、核对、成绩历史不再调用滚动覆盖率、旧 verdict 或可写预案路由，缓存只接受 `K9-v3/fp-4`。无成绩包、无连接及读失败都会结束核对表 loading，成绩包的日期、数量和预案修订均经纯文案函数测试，防止字面插值回归。后端固定读合同为 `GET /scoreboard/packages?state=active|settled`、`GET /scoreboard/packages/{batchId}`、`GET /checklists/{batchId}`。无获批参数/机械预案仍严格 not_run，不能上线。
- [x] P5 本地候选：V2.6→V2.7 显式迁移工具已在临时生产一致性夹具完成前滚/回滚演练；默认只读 preflight，A（全 D2 结案）与 C（显式 `superseded_by_k9-v3`）均要求操作员确认、在线 SQLite 备份、SHA-256、`integrity_check`、受保护事实指纹和成对报告恢复。V3 新库只建成绩包表，读路径零 DDL；`V2.7.0 Build 19 / v2.7.0-b19`、AppIconV270B19、后端/API/pyproject 与两端工程已统一，本地守门覆盖迁移、版本、路由、无参数与旧运行时残留。
- [x] 三轮 Review 修复与预案裁决已落实：非空 D0 由冻结机械骨架经 `playbook` LLM 路由逐票填写；输出按候选/通道/价格关系/P4 行业条件严格校验，失败一律 `playbook_not_generated/not_run`。D0 自动保存 LLM 第 1 版；用户只能追加预案第 N 版，9:26 核对冻结最后版本和完整条件，之后修改冲突，D1/D2 始终使用该冻结件。参数合同删除了机械预案比例。分时链改为 Asia/Shanghai 解析、有效成交和累计量/额证据；零成交、停牌和读/写失败如实 `unavailable`，Parquet 同目录原子替换后才记 `captured`。P2/P3 透支/守位与 P4 行业+个股条件进入 D1 四态；平坦有效路径收盘位置为 50%。报告日期、参数 SHA、事实/标签与预案修订只从不可变包投影，错配拒绝。macOS/iOS 可显示预案来源/历史并在 9:26 前追加经后端严格校验的版本。回归：Backend `780 passed`、`scripts/smoke_api.sh`、`git diff --check`；macOS build、iOS Simulator build、iOS build-for-testing 均通过。仍未标记可上线，生产须验证实时源和 SW L2 盘中证据持续可用及按第 7 节完成切换。
- [x] P4 盘中行业证据已按用户选定方案改为“冻结成员股实时聚合”：D0 从 fp-4 `signalTradeDate` 的行业行一次性冻结去重成员代码、数量、哈希、聚合 schema 与批准基准；D1 只采集这些成员股、候选和基准的真实成交，聚合中位收益、上涨宽度（有效成员收益 `>0`）、覆盖率和相对收益并连同证据时间追加到 D1 raw。申万 `.SI` 已在股票实时适配器 fail-closed，P4 recorder 静态/闭环金样证明不请求 `.SI`；成员/基准缺失或覆盖率不足如实 `unavailable`，任一其他通道明确放弃仍优先 `rejected`。新增 P4 预案允许范围合同，LLM/用户修订必须显式填写行业确认/失速线与成员覆盖率。回归：Backend `789 passed`、定向 SQLite+Parquet 闭环通过、`scripts/smoke_api.sh` 与 `git diff --check` 通过；无 Swift 修改。
- [x] 第四轮 Review 修复：分时读取改为时区感知、秒级边界（D1 仅含 `09:30:00..10:00:00`），每个结算/P4 聚合都二次要求累计成交量或成交额的正增量；首样本只建基线、计数回退和展示价回填一律 fail-closed。事实包同字节 SHA/行数核验并覆盖目标日历，异常清单即拒绝。fp-4 构建只向通用市场装配器注入目标日 SW 快照，完全脱离 current SW 表；`market_json` 冻结完整 as-of SW L2 成员 sidecar（含无日线成员）及哈希，P4 覆盖率分母只读该冻结件；按用户已选方案，历史 SW2021 L2 只能由受控 CLI 导入逐日完整可靠快照，每日须显式 `complete: true`、声明并匹配成员数，来源时间须为有时区且有序的 ISO-8601，记录原文件/逐日内容哈希和来源，fp-4 `sources_json`、`market_json` 与 readiness 共同校验其不可变谱系，缺失/篡改即拒绝。P2/P3 放量均严格采用各日之前 20 个有效交易日成交量，P2/P4 跨日相对收益先对同窗各自复利再相减，缺日/非有限即拒绝；P3 热度为全市场当日成交额/换手率显式加权，近 5 日事件按每票当日冻结涨跌停宽度归一化且窗口独立；D1/D2 持久化完整路径低点、同一起点基准收益与相对基准。D1、D1 收盘和 D2 重跑只追加缺失逐票结果，10:00 事件只在整包 D1 已冻结后落账。新增独立 `k9_playbook_freezes`，D1 上海时间 `09:26` 起永久禁止预案修订，9:26 后以同一事务落冻结标记和 checklist；断点重试从已冻结 checklist 恢复真实计数，通知仅在成功或确定性终态后落 dedup。新增持久化 lifecycle attempt/stage 表，重试后从持久三段重新汇总，成功 identity 不重入且正常报告不可被失败重试降级；report-only 进程仅在 D2/D1/D0 全部成功时可投影正常清单或空清单，缺失、运行中、失败均 `not_run` 且禁推送；证据采集异常也先持久化 failed。所有 V3 生产/演练日历读取显式绑定目标 SQLite，缺覆盖 fail-closed；最小事实历史只取公共边界与已启用通道。早盘业务时钟固定 Asia/Shanghai（服务单元另设 `TZ` 纵深防御）。回填/迁移请求仅在每个交易日有完整 fp-4 覆盖时成功，真实 V2.6 旧库允许新增空 V2.7 表而迁移前存在受保护表哈希不变。回归：Backend `827 passed`、`scripts/smoke_api.sh`、`git diff --check` 通过；无 Swift 修改。
- [x] 候选收口：P2/P3-only 包也冻结并采集基准；APNs 改为逐设备持久化投递账，暂时失败只重试未送达设备；生命周期读取优先已成功 attempt，失败重试不能遮住正常结果。App 将方向与逐票预案从原始 JSON 改为可读卡片，原合同折叠保留；版本错配明确指出先升级服务端或 App，语义等价版本不误报；设置页显示真实临时覆盖地址，iOS 长页为浮动导航留出安全底距。iOS→macOS 视觉审查已核对选股、预案、成绩、设置及版本错配。最终回归：Backend `832 passed`；App `41 tests / 6 skipped / 0 failures`；macOS build、iOS Simulator build、iOS build-for-testing、`git diff --check` 全部通过。
- [x] 参数接入：获批原件 `k9-params-20260831-v3-r1` 已从研究侧按原字节写入 `Backend/config/k9-params.json`，源/目标 SHA-256 均为 `feb24c8199b061b31e33fa9b47603e3d9cc27d76eaa0aff064f1b451d01b41a2`；严格加载器、41 项 K9-v3 定向测试、Backend `832 passed` 与 API 冒烟全部通过，未触碰业务数据库、未生成报告。
- [x] NB SSH 入口恢复：`NB_info.md` 记录的现行 IP `114.66.2.205` 与 ED25519 指纹 `SHA256:iB1q/KoqSAhV5OpQyJPX3u8P/0fwk3P4YkJ6lmTnz0A` 已逐位核对，既有 Mac mini 公钥以 `deploy` 登录成功；过期入口已从 Neckline 现行运维说明中移除。
- [x] 发布前视觉复核修复：iOS 选股页已恢复“今日清单 / 次日核对表”常驻切换器，交易时段默认落在核对表后仍可双向切换；macOS“研究服务”最近 5 日用量已改为卡内通栏左对齐。最新模拟器与 macOS 实际界面复核通过，macOS build、iOS Simulator build、iOS build-for-testing 与 `git diff --check` 全部通过。
- [x] P5 生产收口：提交 `52653e1` 与不可变标签 `v2.7.0-b19` 已推送；NB 云先建立并校验 `/opt/neckline/data/backups/v2.7.0-b19-pre-20260831-120844`，随后停写、同步、显式设置 `K9_PARAMS_PATH`、受控建表并恢复服务与三个正式 timer。旧 28 张表行数全部不变，迁移后数据库 `integrity_check=ok`，公网 health 为 `v2.7.0 / v2.7.0-b19`，服务 0 次重启。macOS Developer ID 双架构分发包与 Apple Development 本机安装包均严格验签，`/Applications/Neckline.app` 已运行 `2.7.0 (19)`，旧 App 备份在 `/Users/linotsai/Lino/app_backups/Neckline-v2.6.0-build18-pre-v270-20260831-121500.app`；iOS arm64 包已签名交付。K9-v3 首个 Day 1 只允许由 19:00 正式晚间链读取完整冻结事实生成；readiness 不完整时不得生成包或推送。
- [x] 发布制品：GitHub Release `v2.7.0-b19` 已附 macOS ZIP（SHA-256 `0a4567bbc4fe30213acab897dfc38204c46cf1366c972e7aa14b12d0eabf2c9d`）、iOS App ZIP（`74299c507d0628f4c3ef04990216a85cfb2188935ba5cab6890b1c379198b504`）与 iOS IPA（`cc1be9369f20f8db9fdaa240bf477eae60a91fbd0b3afe7a675c572ea206c948`）。
- [x] 新 iOS 设备接入：已确认 `Caeieo` 早已配对、在 Build 19 描述文件内且已安装 `2.7.0 (19)`；本次无需新增 UDID 或重新签名。通过同一 Wi-Fi 使用隔离临时引导版把既有生产凭据写入 iOS Keychain，再以无注入启动证明凭据来源为 `keychain`，随后覆盖回官方 Build 19。官方包启动后生产 `devices` 记录于 2026-08-31 13:28 CST 更新为 iOS，公网健康为 `v2.7.0 / v2.7.0-b19`；未卸载 App、未暴露凭据，临时工作树与构建目录均已删除。
- [ ] 首个 K9-v3 Day 1 纠错：19:00 首跑的 D2/D1 成功、D0 因精确上市生涯日历依赖失败，报告为 `not_run`。提交 `dcf3f31` 删除精确 lifetime 计数，提交 `7c275ec` 修正历史申万闭区间并以 `v2.7.0-b19-hf2` 上产；60 个 fp-4 和三阶段随后均显示成功，首包候选/预案为 0、APNs `sent=2 / failed=0`。21:20 的逐闸审计推翻了“可信 empty”：当日 5545 行进入 fp-4，非金额边界后仍有 2133 行，但千元 `amount` 被当作元比较后变为 0，且 `free_float_mv` 5545 行全空。只读内存对拍以正确 CNY 换算和同源稍后完整返回的 `free_share` 得到共同池 1754、P2=4、P3=7、P4=0、不同代码 11；这只是缺陷证明，不是正式报告或交易清单。下一步必须先修单位合同与关键字段 readiness，再以新的追加版本、完整冻结事实和可审计链生成纠正版，禁止覆盖旧 fp-4 或直接采用反事实候选。
- [ ] iOS 报告可见性：生产日志已证明 `Caeieo` 多次成功取得 `selection/latest`，但 Build 19 不渲染报告正文；当前错误空包只剩“今天没有 / 本次清单 0只”状态卡，停在“次日核对表”时连该卡也不可见，恢复前台也不会强制刷新。下一次 App 热修复须明确呈现 `empty / has_list / not_run` 三态、恢复前台刷新，并以真实 iPhone 的“选股 → 今日清单”完成可见性验收；Swift 修改必须重跑三条 `xcodebuild` 门禁。
