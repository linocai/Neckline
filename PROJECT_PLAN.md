# Neckline · V2.5.0 收口计划

> 目标：V2.5.0 · Build 10　　基线：生产 V2.4.2 Build 9　　更新：2026-08-23
>
> 本文件是仓库唯一当前计划。旧实现和旧计划不承担现行指令；源码追溯使用 Git。

## 1. 当前目标

Neckline 现行生产链是 K9：

```text
行情与申万 L2 → 冻结事实包 → 四通道机械召回 → 排序与名额
→ Tavily 消息核验 → LLM 解释 → 机械预案骨架 + LLM 填价
→ D1 9:26 核对 → D1 10:00 结算 → 覆盖率/清单成绩
```

App 只保留“选股 / 成绩 / 复盘”三块业务界面和设置。系统不跟踪持仓，不持续盯盘，
不提供盘中交易指令。离线研究、回测和参数标定在 `/Users/linotsai/Lino/whynotme`；
生产代码不得反向 import 研究仓。

## 2. 已裁定的产品与策略契约

以下事项已定，不在施工中重新讨论：

1. 排序使用机械“上方空间”；预案使用 LLM 给出的“第一压力位”，两者不得混名。
2. 行业强度采用申万二级行业成员当日涨跌幅中位数，不使用行业指数或概念板块。
3. 只有全天停牌成员不进入行业中位数；盘中临停仍计入。
4. 放量倍数由一处共享计算。P1 为 `≥ V`、P3 为 `< V`，V 不分档。
5. 参数没有默认值。缺包或参数非法时必须报告“今天没跑成 · 参数未配置”，不得出清单。
6. 9:26 只能给“已触发放弃 / 待开盘后观察”；10:00 才是三分支终值唯一权威。
7. `report_date` 管标题、推送和可见身份；`trade_date` 管行情、清单、预案和审计键。
8. LLM 不进入机械策略层。联网只由 Tavily 完成；现役任务只有 `news_scan`、
   `explain`、`playbook`。
9. 退役能力物理删除：不保留运行代码、表、路由、设置、数据、兼容入口或恒空 UI。
10. 当前通知只保留 `report_ready` 与 `precall`。
11. 晚间报告链在周一至周四及周日 19:00（北京时间）启动；周五留到周日处理，
    周日绑定前一个周五盘面并纳入周末消息；目标交易日休市则整链跳过，不回退旧交易日。

## 3. 2026-08-22 B1–B22 最终裁定

| 编号 | 最终裁定 |
|---|---|
| B1 | 9:26 与 10:00 共用 `first30_low`：9:26 是竞价价，10:00 是本场至今最低价。 |
| B2 | P2 放弃条件保存和展示明确价格，不保存、反算或展示百分比。 |
| B3 | 解释与预案的 K 线长度为模块常量，不进入策略参数包。 |
| B4 | V 只有一个值；标定说明必须写明它只在 P1/P3 之间搬运候选，不能扩大联合召回。 |
| B5 | 宁波云规格按 4 vCPU / 3.8 GiB；部署前用真实数据测 Linux 内存，再调整 unit。不得先缩 120 日窗口或拆服务。 |
| B6 | V2.5.0 部署时一次性停掉旧日更，不做新旧版本混跑。 |
| B7 | `review.reconcile.WeeklyStats` 就是“我的成绩”，不另建 `scorecard/mine.py`。 |
| B8 | 旧校准段和 `/eval/weekly` 全部下架。 |
| B9 | 概念板块代码、接口、测试和数据全部删除。 |
| B10 | 主收益为 D0 收盘到 D+4 收盘；D+1..D+4 最高收益只作辅助读数，不合成。 |
| B11 | 在线 explain 信封使用 camelCase；冻结快照不回写。 |
| B12 | 旧一次性脚本及测试删除。 |
| B13 | 读取 helper 不执行 DDL。 |
| B14 | iOS 门禁增加 `build-for-testing`，不绑定具体模拟器。 |
| B15 | 通知只保留 `report_ready`、`precall`；旧 kind、映射、设置、文案和测试全部删除。 |
| B16 | 放量倍数满窗才给读数，不新增覆盖率参数。 |
| B17 | K9 原文固定值逐字转录并强校验，参数包不得改写。 |
| B18 | `rank` 保留全市场候选名次，App 解释跳号属于正常现象。 |
| B19 | 同日重跑先删当天旧 `k9_channel_hits`，不保留悬空运行记录。 |
| B20 | 上产后记录一周 9:26 双源 `open` 分布，再决定质量状态是否调整。 |
| B21 | 正常空榜保持沉默；D0 `not_run` 时早盘推送“昨日没跑成”。 |
| B22 | 成立率分母为 `k9_listing_entries` 全量正式清单；verdict 只提供分子。 |

## 4. 本版落地范围

### 后端

- 事实层、四通道召回、机械排序、名额、解释、预案、9:26 与 10:00 两拍均已接通。
- 清单成绩保存 D0→D+4 收盘收益和 D1..D4 最高收益，提供五项独立指标：
  行业分、选票分、成立率、兑现率、误杀率。
- SQLite 新库只建立 26 张现行表；升级会保留现行设置/复盘数据、迁移有效任务防重记录，
  再删除所有退役表与退役列。
- 当前 LLM 路由只接受三个现役任务；历史任务键读时过滤、下一次写回即消失。
- 日更、API 冒烟、复盘冒烟和 systemd 注释已收口到现行契约。

### App

- 成绩页接入真实清单成绩；行业分与选票分分开展示，不合成总分。
- 个股解释字段使用 camelCase；全市场名次跳号给出人话说明。
- 通知仅处理报告完成与竞价核对，并在 iOS/macOS 共编译的路由表里维护。
- 版本固定 V2.5.0、Build 10、主图标 `AppIconV250`。

### 已删除

- 旧选股链、旧表、旧路由、旧设置、旧数据读取器、旧 UI 占位。
- 概念板块整链与本地抓取数据。
- 旧校准读取链和 `/eval/weekly`。
- 旧通知类型和旧盘中提醒。
- 旧 one-off 写脚本、兼容导出与对应测试。
- K8 骨架包及 C/Z/Y 六份退役策略包；Git 历史承担追溯。

## 5. 数据库升级边界

`init_schema()` 是写操作，只允许 API 启动、明确写命令或部署迁移调用。任何 GET、报告读取、
复盘读取都只能只读探测；缺文件、表或列时返回该接口约定的空态，不能顺手迁移。

正式部署前必须：

1. 明确目标为宁波云生产库并停止旧定时链；
2. 备份数据库及 WAL/SHM，记录校验值，执行 `PRAGMA integrity_check`；
3. 在备份副本上先跑 `init_schema()`，核对 26 张现行表和关键设置值；
4. 生产迁移后再次做完整性、表清单、设置、Provider、Tavily、设备数与最近报告核对；
5. 旧版本回滚必须连同迁移前数据库备份一起回滚，不能让旧代码读取新 schema。

## 6. 发布前门禁

```bash
cd Backend
.venv/bin/python -m pytest -q
bash scripts/smoke_api.sh
bash scripts/smoke_review.sh

cd ../App
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'platform=macOS' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'generic/platform=iOS Simulator' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline build-for-testing -destination 'generic/platform=iOS Simulator'
```

发版元数据必须一致：API `v2.5.0`；App Marketing Version `2.5.0`；Build `10`；
asset set `AppIconV250`。

## 7. 部署前仍需完成的现场事项

这些不是代码欠账，不能在本地伪造完成：

- **生产参数包（已完成）**：用户批准的 whynotme `k9-params-20260822-r1` 已原样纳入
  `Backend/config/k9-params.json`；与标定原件逐字一致，SHA-256 为
  `5775641b989e9553ad29e0178a059007f1f663b422e8134130c99922e0dee952`。
- **B5 Linux 内存实测**：在宁波云用真实数据运行无报告产出的 facts 与
  `k9,explain,playbook` 探针，记录 `MemoryPeak`，再决定三个 unit 的 `MemoryMax`。
  2026-08-22 已确认宁波目标 `114.66.0.38` 为 4 vCPU / 3.8 GiB、无 swap；旧脚本默认的
  `118.178.122.194` 是 1.6 GiB 的杭州旧机，不能再拿它代表宁波规格。已在宁波临时隔离目录
  和生产库在线备份上完成 facts 半程实测，生产代码、生产库和正式报告均未改动：当日 facts
  峰值 107.6 MiB / 0.60 秒，申万元数据刷新峰值 133.3 MiB / 6.83 秒，补齐 131 个交易日
  `suspend_d` 峰值 172.4 MiB / 16.63 秒，154 个交易日事实包回填峰值 128.2 MiB /
  33.16 秒（冻结 131、复用 23、缺失 0、异常 0；登记、文件与 SHA-256 为 154/154）。
  生产 `suspend_d` 目前只从 2026-07-22 起存在，正式迁移前必须按同一完整性口径补齐此前
  131 个交易日；不得伪造空分区或放宽门禁。后半程使用正式参数包完成：严格档候选 241，
  初始清单 20，消息面剔除 4、后备补位 4，最终 20 只；20/20 解释和 20/20 预案成功，
  价位次序违规 0。全段墙钟 2:00:20、CPU 7.875 秒、Linux `MemoryPeak` 245 MiB、swap 0；
  报告装配 0.43 秒、峰值 48.7 MiB。逻辑调用为 DeepSeek 64 次、Tavily Basic 检索 24 次；
  Token 聚合探针的唯一 stdout 长管道在收尾丢失，故不猜测 Token 数值，也不为补账重复消费
  这 64 次调用。三个 unit 据实测改为 facts 512M、basket 768M、report 512M；basket 保持
  `TimeoutStartSec=infinity`，不缩策略窗口、不增加累计时间截断。
- **部署与发版授权**：确认备份、迁移、同步代码/unit、重启、健康检查、macOS/iOS 换包。
- **B20 上产后一周证据**：收集 9:26 双源 `open` 分布后再裁定质量状态，不在发布前猜。

## 8. 状态

- 代码状态：实现已收口；后端全量测试、两组冒烟、macOS 构建、iOS 构建和 iOS
  `build-for-testing` 全部通过。正式参数包门禁与宁波 unit 语法验证也已通过；B5 实测和
  `MemoryMax` 收口完成，已停在部署前。
- 生产状态：仍为 V2.4.2 Build 9；本轮没有部署、没有运行生产迁移、没有重跑报告。
- 下一动作：取得部署授权后，按备份 → 副本迁移验证 → 生产补齐 131 个 `suspend_d`
  历史分区 → 正式迁移 → 健康检查 → 发版换包的顺序执行。
