# Neckline V3 · 3.0.4 / 双端 Build 35（已发布）· K10-v1.4

本文件是唯一工程控制面。正式策略见 [`whynotme/K10.md`](../whynotme/K10.md)，生产不得读取或导入研究仓。
运行、安装与恢复入口统一见 [README.md](README.md)。

## 当前状态

- 用户授权的施工和一条龙发布已完成。源码 `15628894046762fa0b2085964af91efc7e76e612`，不可变标签
  `v3.0.4-b35`；后续记录提交不移动标签。此前未单独发布的 3.0.3 / Build 34 增量已并入本版本。
- 生产为 **3.0.4 / 双端 Build 35 / K10-v1.4 / Schema 3**，发布集合 `v3.0.4-b35`。
  API/worker 健康，公网版本与安装 wheel 一致，三范围配置就绪；未鉴权 401，发布后重启/警告为 0。
- 显式生产配置绑定 `k10-v1.4-production` 修订 **2**。仅增加 TuShare 来源的 `lateArrivalReplaySeconds=86400`，
  修订 1、Schema 3、全部既有记录不变。密钥、设备和个人连接保留；K9/K8 不在活动链。
- Mac `/Applications/Neckline.app` 已换装、启动并实际核验生产设置，只运行一个正式实例。
  iOS development IPA 已交付 Downloads 与 GitHub，沿用用户自行安装约定。
- 六个发布文件云端 SHA256 与本地一致。QA 进程/API 已关闭，固定模拟器 QA 安装与过期构建副本已清理；
  正式客户端、签名归档及恢复备份保留。

## 冻结产品与工程裁定

- K10 是纯选股器：消息分析、事件整体公司比较、选择、所选公司正反分析、全候选晨报、两日效果验证。
  交易计划、买卖价位、持有/退出、仓位和收益结算已退役；交易纪律仅作个人提醒。
- 仅创业板，无股价/金额上限，排除 ST/*ST 与申万 2021 白酒Ⅱ `801125.SI` 全部成员。
  全部模型固定 DeepSeek V4 Pro；费用上限为空，不由客户端编造模型费用。
- TuShare 长篇通讯是发现源；Tavily 仅对冻结事件作定向核验与历史同类检索，不回流全市场发现。
  快讯/全量公告权限缺口、迟到与未知发布时间如实标记。没有参数包时不生成候选；不得引入策略默认值。
- 事件共同事实只讲一次，发布前明确主推、备选或并列、理由、差距和改序事实。晚间最多 30 家不同新公司，
  所有主推/备选/折叠公司都占名额；同公司同批多催化共用卡片、一次选择和一份成绩。
- 所有正式公司窗口都跟踪，开盘前最后一次明确操作冻结为留下、明确略过或未处理。浏览不作选择，
  晚选保留真实时间，不改变历史分组、样本归属、D1/D2 或成绩。
- D1 为可查看后的第一个完整交易日，D2 为下一交易日。晚间和开盘前晨间共享窗口并保留来源；
  开盘后才发布的机会标迟到，从下一交易日观察。更新旧候选不重开窗口。
- 无变化、转载、常规进展和补充资料延续旧机会；重要反证优先提示，确认理由失效可撤回。
  撤回、取消关注、未处理、缺数或仍看好都不删除样本、不延长 D2、不唤醒旧消息。
- 实质新阶段/独立新催化须发布时说明新增事实与新的两日理由，不能按后续涨跌改分类。
  同公司新旧窗口相交时，新窗口单列 overlap，完整观察但不进入主成绩分子分母。
- 主指标只含 D2 到期且两日涨停状态完整可核的主样本：两日内至少一次收盘封板算一次机会命中。
  触板、首次日期、价格变化、未到期、停牌、缺数、异常和 overlap 分列。价格变化不是假定成交收益。
  行情事实按公司＋交易日共享；同事件多公司不能相加当作多次独立催化验证。
- 晨报固定重大反证、论点变化、继续/到期、新机会、待核五段，覆盖全部正式窗口与新到期提醒。
  独立核验不得由普通来源冒充；资料不足只能待核。所选公司正反共享冻结证据，新增问题/合法证据才追加分析版次。
- API 维持 additive `k10-api-v2`，旧字段缺失诚实显示“未记录/待核”。读入口不得执行 DDL；
  生产写入须核对目标与恢复路径。测试只使用临时 DB；确认复现必须保留为从入口到投影的回归。
- 视觉遵循参考图白卡、蓝色重点、克制文字和清晰导航，逻辑呈现优先，不追求像素级统一。
  测试只复用固定 macOS/iOS QA 目录和一个模拟器，启动新实例前关闭旧实例。

## 本轮 11 项闭环

1. 文档版本与本次 scan 接收标记原子提交；失租后恢复未消费的新资料，旧＋新混合回补只处理新资料一次。
2. 历史案例可共享来源；coverage refs 稳定去重，case refs 完整保留，合法共享不阻断发布。
3. 每窗口冻结评价配置控制所有投影；库中已有旧成绩也不能绕过，未配置窗口保留样本并净化统计，原行不改写。
4. 事件内 rank 与全局 displayRank 分离，并列保持。旧已发布记录不猜事件排序，旧未发布冻结草稿明确失败；
   running 恢复收尾为 failed，不静默重算，不更改旧发布哈希。
5. 递归拒绝模型派生文本中的未经校准涨停/封板概率，包括历史 assessment/classification；明确否定语句仍合法。
6. DeepSeek 原始比较数组在压成字典前拒绝重复公司；公司集合、角色与排序校验保留。
7. 缺失涨停状态纳入数据缺数，不充作未命中；未来日期不伪作已收市缺数。
8. 事件分组增加独立 overlap 指标，避免重叠公司被读成全零或混入主命中率。
9. API/Swift 显示来源时间质量、未知时间数量、待核资料与回补范围；完整抓取不能冒充完整时间精度。
10. 晨报 fallback 统一 coverage.status，完整项显示“已核”，不再因字段错配显示待核。
11. 撤回独立反证保留至生命周期、详情、下一晨 target 与客户端阅读；已核撤回须有对应独立反证。

## 验收与发布证据

- 最终 Backend **682 passed**；21 条既有 Polars sortedness 提示，无失败。所有已确认复现均有持久回归，
  包含真实 CLI/worker 失租恢复、旧草稿三个恢复分支、已有成绩配置门禁与合法历史兼容。
- macOS build、iOS Simulator build、双端 build-for-testing 通过；每端 XCTest **32 passed、1 skipped**。
  跳过的是旧可选 smoke，新增 Build 35 真实 handler → 临时 SQLite → FastAPI → Swift 解码实际执行并通过。
- 双端填充/空态与来源覆盖详情实测；Mac 另核对事件 overlap 分组、完整 fallback、撤回独立来源阅读。
  正式 Mac 设置页确认 Prod 连接、3.0.4 / Build 35、候选/正反/评价均已配置。
- 双端 Release archive、iOS 导出、安装包结构与严格验签通过；Mac 通用架构、签名身份和权限与 B33 一致。
  源码 manifest 与提交保持一致，包内无临时凭据或合成运行参数。
- 在确认的服务器真实 DB 副本先演练追加修订，再停止写入、备份、部署、复验逐表数据与备份哈希。
  部署后恢复四个 timer，后端源码 173 个文件、wheel、健康检查与云端安装包哈希均通过。
- 测试/发布原始证据在 `/tmp/neckline-v304-validation/`、`/tmp/neckline-v304-release/`；
  持久发布文件在 GitHub 与服务器 `/opt/neckline/releases/v3.0.4-b35/`。

## 恢复与下一步

- 当前恢复集 `/opt/neckline/data/backups/v3.0.4-b35-predeploy-20260907/`，Mac 备份
  `/Users/linotsai/Lino/app_backups/Neckline-v3.0.2-build33-pre-v304-20260907.app`。具体校验值与撤回步骤见 README。
  本次无 schema 迁移；撤回优先恢复代码和绑定，不用历史 DB 快照覆盖新增业务。
- 无待用户拍板的工程项。今晚 **2026-09-07 21:00 CST** 首次扫描，明晨 **2026-09-08 09:00 CST** 首次晨扫；
  未提前入队。真实整晚规模覆盖、耗时和推送尚未产生运行证据，不能用离线回归宣称策略有效。
- 本轮完成后停止；下一次从本文件、README、git status 与实际生产 health 重新定位，不移动既有标签。
