# Neckline V3 · 3.1.0 / 双端 Build 39 · K10-v1.4

本文件是工程控制面。详细施工契约见 [archive/v3.1.0-b39_execution.md](archive/v3.1.0-b39_execution.md)；策略行为以 [`whynotme/K10.md`](../whynotme/K10.md) 及用户本轮最新决定为准，生产不得导入研究仓。

## 概述

3.1.0 将 B38 的“冻结文章→一次事件搜索→核验→比较”改为可检查、可恢复的命题调查：原文先拆关键命题，复用适用证据，围绕具体缺口受控搜索／全文，再按证据收口并完整比较公司。目标是让推荐、待核与执行失败各自有真实含义，且不重置已发布机会的 D1/D2 成绩。

## 稳定技术决定

- 目标为 `3.1.0 / 双端 Build 39 / Schema 7`，策略仍为 `K10-v1.4`；执行配置继续显式绑定 DeepSeek V4 Pro、Tavily、80／40 和无整轮资源总上限。
- Schema 7 以既有 source-document versions、external attempts、checkpoints、article admissions 与发布 `comparison_json` 为基础，只新增最小研究快照、问题路径和证据关联；不为每个 Prompt 阶段建立一套表或进程。读 helper 绝不执行 DDL。
- 研究状态与执行状态分开。`task completed`、`scan partial` 或零正式候选均不得表示“比较完成”或“无机会”；协议、引用、模型或程序错误保留定位与检查点，投影为执行失败。
- 每个输入公司的比较结果必须是 `primary`、`alternative`、`tied`、`pending` 或 `excluded` 之一；仅前三类组成发布派生集并按既有名额／机会规则发布。`pending`／`excluded` 仍持久化并可查看，不为通过校验而漏公司或硬排。
- 未核传闻可正常参与比较、正式推荐、晚间 30 家名额及 D1/D2 成绩。它必须显示“未核实”、传闻性质、真实源头或“来源未知”、未证实环节和条件化分析；不得伪造成已核事实、来源编号、公司身份或硬排序。未核本身不是 `pending` 的自动理由。
- 80／40 仍限制实际正文深读。冻结正文及受控补读均走既有 admission；不预留核验全文名额，额外全文只使用当时实际剩余且不得挤占冻结名单。搜索摘录不计全文，Extract／全文交模型前必须获准。
- 事实缓存以来源版本、内容哈希、调查契约／Prompt／模型参数版本键控；稳定来源事实可复用。问题答案、市场判断、比较和发布始终绑定消息截止、实际查证截止与上下文版本，不能回填旧报告。
- B39 已发布；用户授权首轮 9 月 8 日晚报，已执行但标题协议失败。为避免同错定时重跑，当前开关与 worker／晚晨 timer 再次关闭。保留检查点，不复活事故任务；80／40 和无整轮总上限不变。

## 当前状态

- **2026-09-08 已发布 3.1.0 / 双端 Build 39 / Schema 7**，不可变标签 `v3.1.0-b39` 指向 `a3732ab`；[发布资产](https://github.com/linocai/Neckline/releases/tag/v3.1.0-b39)。发布记录见当前版本记录。
- 累计发布范围从真实生产 B36（`6017a0c`）覆盖到 B39；生产副本迁移／恢复演练和正式迁移均核对 50 个旧表全部旧列／行等价，新增策略修订 3、执行修订 2。
- 首轮 task `task_8ee84154911c2463c54880feeefb93a5`（scan `scan_fdd466988114709026e4e922af505ce6`），截止 9 月 8 日 21:00，21:49 启动后因标题 JSON 协议失败终止；task failed／title_incomplete，scan partial，无正式发布。
- Mac 已换装 `/Applications/Neckline.app` 并单实例启动；实际设置页确认 3.1.0（39）、Prod、四项已配置、推送就绪与资讯暂停。B36 可恢复备份保留。
- 双端签名归档通过；Mac Developer ID 严格验签通过但尚未公证。iOS 真机签名归档及 Xcode 配置就绪，用户自行安装，不生成 IPA。
- B39 支持正文命题提取、动态查证与完整公司比较；未核传闻带披露推荐，待核／排除保留，执行失败阻止发布。恢复复用已完成调用，未知结果禁止重发。
- 后端 **905 passed**，双端三条构建、实际隔离 API→Swift 解码及 native 填充／空态已通过。发布探针修正后再跑 33 项 API 回归；6 个 GitHub 资产下载后逐一核验。
- B39 首轮收到 1,063 标题、完成 384 条；模型调用 9 次（6 成功、3 失败），92,021 tokens，正文与搜索均 0。失败原响应未持久化，目前只知 JSON 对象协议不合格，具体格式原因未知。
- 21:53 保护暂停后，API 正常，持久开关 closed／API paused；worker 与晚晨 timer inactive+disabled，明晨不会自动重跑。旧事故任务不变；成功标题结果、用量和开启前／试跑后备份保留。

## 当前 Plan

发布已完成，首次正式试跑已结束但未生成报告。当前阻塞为标题模型输出协议失败；不得标成“无机会”或盲目重跑整轮。未做独立 review。

## 待用户决定

无本轮发布待决定项。

## Backlog

- 对本轮失败批次做最小诊断：先补不含原文／凭据的解析与返回结构证据，再修复有证据支持的原因；恢复时复用今天原任务和成功标题检查点，不从 1,063 标题全量重来。
- 发布配置完整性补强：此次首次入队发现数据库缺少配置包指向的已批准标题 policy，readiness 四项仍显示已配置。已按用户既定规则补登记完全相同的 policy 修订 1，同一幂等任务成功绑定；后续修复部署配置入口／readiness／入队原子性，补此真实场景回归。

## 里程碑索引

- 3.0.4 / Build 36：线上已发布，后因全量逐篇处理停机；见 [archive/v3.0.4-b36_execution.md](archive/v3.0.4-b36_execution.md)。
- 3.0.5 / Build 37：未发布预算／资料包方案，已替代；见 [archive/v3.0.5-b37_execution.md](archive/v3.0.5-b37_execution.md)。
- 3.0.6 / Build 38：标题初筛与 80／40 已实现，真实比较中断且未发布；见 [archive/v3.0.6-b38_execution.md](archive/v3.0.6-b38_execution.md)。
- 3.1.0 / Build 39：已发布，首轮正式试跑在标题阶段失败，21:53 再次保护暂停；见 [archive/v3.1.0-b39_execution.md](archive/v3.1.0-b39_execution.md)。
