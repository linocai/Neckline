# Neckline V3 · 3.0.4 / 双端 Build 36 · K10-v1.4（已发布）

本文件是唯一工程控制面。正式策略见 [`whynotme/K10.md`](../whynotme/K10.md)，生产不得读取或导入研究仓；运行、安装与恢复入口见 [README.md](README.md)。

## 当前状态

- 2026-09-08 11:42 后端切换完成，11:45 正式发布 `v3.0.4-b36`。营销版仍 3.0.4，双端 Build 36，Schema 4、通知 Schema 2。
- 发布提交 `6017a0c36bf1bbc6e842ef512ca66a29b7e7f607`；不可变标签和 GitHub 六资产已发布、下载哈希复验。双端签名归档源 `c6e91795617d778503820377f6d843587fccc36c` 的 App 源码树与发布提交相同，真实来源保存在 manifest。
- Mac 单实例正式换装、签名和实际设置页通过；iOS development IPA 已交付 Downloads，由用户安装。三项功能均已配置，推送已就绪，API/worker 与四个 timer 正常，K8/K9 无活动链。
- 工程和发布已完成。**昨晚输入的生产补跑仍在运行，尚无生产最终候选结论**；下方保留精确任务和观察入口，不能把隔离测试结果冒充生产结果。

## 不变的 K10-v1.4 产品裁定

- K10 是纯选股器：消息分析、事件内公司比较、选择、所选公司正反分析、晨报和固定 D1/D2 效果验证保留；交易计划、价位、持有/退出、仓位和收益结算已退役。
- 股票池仅创业板；无股价/金额上限；排除 ST/*ST 和申万 2021 白酒Ⅱ `801125.SI`。模型统一 DeepSeek V4 Pro。缺少或无效策略包时只显示“今天没跑成 · 参数未配置”，不产生候选。
- 流程仍为“增量采集 → 基础去重 → 明确模板初筛 → 轻量理解 → 重点核验 → 候选”。来源主体可在池外；不得用栏目、利好关键词、题材、长度或代码命中做隐蔽删选。疑难资料留核查通道，降级资料有计数和抽查入口。
- 推荐前必须完成事件共同事实、公司映射、主推/备选或并列比较、优先原因、差距和改序事实；全部完成的可推荐公司再统一全局排序。晚间正式推荐最多 30 家不同公司，同公司多催化仅一张卡、一份选择和一份成绩。任何局部先完成的事件都不能先占名额。
- 所有正式样本依实际 `availableAt` 固定 D1/D2，用户的留下、略过或未处理不回改窗口。旧消息更新、用户改选、撤回和普通跨日延续不重开机会；重叠机会单列且不重复进入主命中率。补跑昨晚输入的可查看时间只能是这次实际完成并可读取的时间。
- 主成绩只统计到期、两日涨停状态完整可核的 primary 样本在 D1/D2 内是否至少一次收盘封板；停牌、缺数、异常、未到期和 overlap 分列。价格变化不是实盘收益。

## 本轮问题与修复

- 9/7 夜扫正常采集并冻结 2472 篇资料，第 7 次理解调用后整轮失败。旧串行链无逐项检查点，输出解析/结构/引用异常终止整轮，maxAttempts 不安排继续。未存原始坏响应，不能断言具体坏字段。
- 冻结 HTML 提纯保留事实、表格、否认与更正；只移除脚本和嵌入噪声，原始资料不改。目前没有已定义模板规则，启用模板筛选为 0，不私设利好词、题材、栏目或长度排除。
- 理解有界并行，每条已校验结果独立持久；分片排队、有限网络/JSON 修复、租约保护、总时限和按需全文均显式配置。完成项恢复时不再调用；失败资料隔离并如实 partial。
- 事件核验、映射、比较和全局排序缓存成功结果；Tavily 缺关键独立证据的事件不得发布，全部完成事件统一排序后限制 30 家不同公司。跨片恢复先注册冻结原文，事件缓存键包括引用和提纯文本 SHA，避免已缓存理解跳过后丢失核验上下文。
- 新任务冻结策略和执行两套 ID/修订/哈希。旧失败夜扫以受控恢复子任务使用原 scan、cutoff、策略、2472 个资料版本和机会快照，禁止重新采集/水位推进；父失败任务不改写。
- 实际 API→Swift 显示阶段、计数、部分覆盖、恢复时间与安全错误码；不含模型原答、提示词、上游异常正文、token、密钥或私有地址。GET 不执行 DDL。
- 推送检查实际可读/可签名私钥；配置坏时阻塞而非每秒发送，恢复后有界退避并按设备去重。此前两条积压通知 11:42 已各产生两份投递记录；不清零旧尝试数，不宣称用户已读。

## 执行配置与发布验收

- 策略仍 `k10-v1.4-production` 修订 2，SHA `d3733ddf84e597e98fc0aa54d19dbde0e35add1cadbcc3c2b53579419be67dae`。
- 执行 `k10-execution-production` 修订 1，SHA `b2fc4e1acce0a455a3bee246b7cdc9101a6efda005da683045e8964fa1ba612c`：理解并发12、容量24、8192输出token、每片110秒、正常继续1秒；网络最多2次、JSON修复最多1次；首次执行起7200秒。事件核验/比较输出3500、统一排序2200，DeepSeek V4 Pro；Tavily整轮128次沿用原策略上限。不修改冻结绑定或重置deadline。
- Backend **750 passed**；双端三条必需 xcodebuild、双端各34项 XCTest（各3项条件跳过）、真实 FastAPI→Swift 契约和已填充/空态 native QA 均通过。签名、架构、权限和无凭据检查通过。
- 真实隔离2472篇从CLI→worker→handler完成，2452理解成功、20异常隔离，21事件核验/4事件比较/1统一排序，1家正式测试候选，明确partial。Tavily128次、报告tokens 4,744,325；总体4750秒，包含暂停、恢复与现场修复，未超7200秒。
- 原598条成功检查点逐条哈希相同，原输入/策略/cutoff/机会快照/父任务/水位未改；原两条截断资料在显式8192配置下完成。跨片上下文缺失已由真实全量发现并修复，旧测试失败账本仍保留。模型畸形、截断与缺独立证据仍会导致待核，本轮不代表完整覆盖或策略收益验证。
- 隔离候选实际availableAt为9/8 11:39:35，D1=9/9、D2=9/10，没有领取旧表现；实际FastAPI DTO、partial计数、安全字段和≤30公司验收通过。
- 停全部写入者后创建当前生产备份并演练/执行迁移，45个旧表全部旧列/行等价。生产根权限、DB owner、schema、显式绑定、真实DTO、401、APNs签名、定时器和发布后日志检查通过。备份/前向修复边界见README。
- QA客户端/临时API已退出，清理约714 MiB可再生编译产物；正式归档、旧Mac恢复副本和验证日志/截图保留。

## 生产补跑与当前观察

- 11:43:46 创建一次恢复任务 `task_b04bbf7808525d32bb317fb2f4388ad0`；scan `scan_1e8502168c080f33b4e5d0ac5e775c48`，父失败任务 `task_6ff897cea3495e25bc13b385f9c4df4c`。
- 原2472条引用 SHA `5c6fc62e825edbb9d59096b4bff4cf1707adadd10364c24573a2eaf30526286a`，生产只读对照迁移前备份确认原始版本、父任务和来源水位未变。生产与隔离任务ID由同一冻结输入确定性生成，数据库完全独立，没有导入隔离候选/成绩/模型缓存。
- 11:47 快照：任务running，已跨两片继续，理解686成功、6异常隔离；真实API返回understanding和partial。暂无正式生产批次；后续完成以真实availableAt为准，盘中补跑若发布，D1=9/9、D2=9/10。
- 原9/8晨报失败保留：原冻结上下文没有已发布夜间候选，不能将补跑后资料伪装成9:00已存在。下一次正式晨扫为9/9 09:00，晚扫仍为今晚9/8 21:00。
- 继续观察实际覆盖、模型结构失败率、Tavily核验预算和耗时；partial不是完整覆盖。未核资料不能变成推荐；费用只记录真实用量，不给未核实的实付估值。无待用户拍板项。

## 恢复时先读

先读本文件并检查 main 和工作区；通过生产只读API查看上述scan的 `executionProgress`，不要重复 `recover-scan`。
本轮服务器只读验收助手保存于 `/opt/neckline/data/backups/v3.0.4-b36-predeploy/verification/`：
`production_recovery_audit.py --mode status` 可看运行中摘要；终态后 `--mode final` 核对原输入、状态、公司数和实际D1/D2。
`final_check.py` 核对发布/备份/配置/API/timer，`notification_delivery_audit.py` 只读查看投递/退避。
它们仅在已确认服务器运行；不可将含凭据DB或恢复集下载。最终补跑完成后只更新当前观察，不累计运行日记。
