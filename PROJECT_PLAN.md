# Neckline · 目标 3.5.1 / Build82 · K10-v2

本文件是唯一工程控制面。2026-09-22用户明确进入工作流，授权修复报告生成审查的全部R1–R9，目标 **3.5.1（82）**。本轮实施与复查已完成；用户随后明确授权“一条龙发布”，现执行B82完整发布。旧报告重跑或额外真实provider试跑不在授权范围。契约和证据统一放在 [archive/v3.5.1-b82_execution.md](archive/v3.5.1-b82_execution.md)。

## 概述

让有效研究能够形成正式complete/partial报告；消除模型冗余字段、重复内容、续跑误判和瞬时存储争用造成的非必要阻断，保留真实证据、完整比较、费用确定性与原子交付。九项范围见 [B81审查第14节](archive/v3.5.0-b78_execution.md#14-b81-报告生成阻碍全量审查2026-09-22)，产品裁决沿用 [肃清方案](archive/2026-09-20_过度设计肃清方案.md) 的已批准部分。

## 稳定技术决定

- 固定1,089池、全标题批量审查；不增加正文/搜索/初选公司/金额/token配额，晚报最多30家公司。保留可见证据、来源、未核实披露、公司比较与正式排序，策略参数无默认值。
- 晚报前一自然日21:00启动，新任务没有整报业务截止；请求超时、暂停、未知费用保护保留。旧任务身份、冻结输入、配置、费用和期限不变。
- 晨报交易日08:30启动，消息窗口前一自然日21:00至08:30，保留水位与迟到回看；09:20前有可读结果。无法排序或费用未结算时只提供诊断/安全材料，不伪造推荐，不过期自动补发。
- 晨报余量由冻结timeout、重试及最终排序请求推导；每次新增研究外呼检查准入，已有回执和本地成果仍可恢复。费用结算与09:20可读交付分开。
- 局部失败进入统一partial，排除失败关联公司及共享依赖，不能只留好消息；输入、引用、账本、完整排序或存储不可信仍阻断正式发布。
- 模型负责业务判断，程序负责计数、序号、命题身份与执行状态；统一解码→归一化→证据/业务验证。未知来源、错误合并、缺真实比较不因容错放行。
- 不增加标题第三次终检；材料足够直接比较，有实质缺口才查。新研究一份回执推进一次结果，不恢复旧双阶段或平行状态层。
- 任务负责执行、报告负责交付、回执负责费用；数据库原始付费回执是精确恢复事实，侧车不是依赖。晨报复核为父任务内工作项，报告/卡片/通知待发同一提交边界。
- 用户1A：安全材料只读，无排名/选择/机会/D1D2/成绩；用户2A：双端一起升级、历史可读。B82保持公开Schema9、历史Schema8读取、内部Schema10，不安排DDL。
- B82新任务标题全局归并采用同模型的结构化输出模式：关闭该阶段额外thinking，保留既有maxTokens、重试及总额度；使用明确版本化执行契约并冻结。旧任务配置不改、不换provider/model，不新增策略默认值。
- Apple最低iOS/iPadOS/macOS及开发测试环境27.0，Xcode27以上；共用iPhone18Pro/iOS27「主模拟器」，按名称校验；保持Swift模式和用户原有配置。
- 历史机会、选择、D1D2和重叠样本口径不变；只有正式发布启动观察窗口。任务完成、HTTP200、材料可读都不能代替正式报告验收。
- 本轮不处理定时/heartbeat，不恢复或替代历史失败/删除任务。只做隔离离线验证，不据合成通过宣称真实模型质量或生产整报已跑通。

## 当前状态

- **生产仍为3.5.0（81）**：runtime `626c902097a91481513ac9792257309858c9e031` / tag `v3.5.0-b81`，后端Schema10与Mac已部署、iOS归档就绪；发布和恢复证据见 [B81记录第12节](archive/v3.5.0-b78_execution.md)。
- B81两次真实交付失败：9月21日晚报标题协议失败，9月22日晨报跨slice收口失败。正式报告均0卡，晨报18条只读材料已实读；旧测试不覆盖这些缺陷。
- **B82本地工作流完成，正在执行已授权发布。** R1–R9及复查新增缺口全部闭环；最终tree `8779f1cb59e1531b4a13faf7e77dc44b132549c2`独立复查无剩余finding。全量1796通过、1旧断言失败、1历史输入缺失跳过；仅修该测试后相关47项通过，生产源码不变。App三态真实Swift、历史读取、双端画面及三构建通过；临时资源/进程核验完成，详见本版第7节。
- 开工HEAD `08cf321ae411e33d4ddf4af26407bb03ee1cde26`；原有两项App用户修改、前轮审查文档及未跟踪`Backend/.learnings/`保留。基线及证据位置见本版第1节。
- 已核验Xcode27.0/SDK27、部署目标27.0；唯一共享主模拟器当前关机。固定QA根初始不存在。
- B75/B81恢复集及清报告专用快照继续保留；本次发布只追加显式配置/策略绑定，不做DDL、不用旧整库覆盖新写。

## 当前 Plan

- 一条龙发布进行中：核对B81累计差异→签名归档/不可变制品→生产副本与回退验证→部署及新配置绑定→Mac替换/iOS Xcode交付→GitHub发布→资源收尾。验收要求见本版第8节；保持四定时器原启用状态，不触发旧任务或额外provider调用。

## Backlog

- 真实provider的输出质量/耗时仍需实际任务观察；本轮不额外真实试跑，不承诺离线通过等于生产成功。
- 交易所日历2026年底到期；TuShare历史缺口、旧废纸篓权限/空间问题沿用旧记录，不扩大本轮。
- 恢复集与B81→B75受控Schema回退限制见B81记录第12节；清报告快照等首份新正式报告完成且可读后再清。

## 里程碑索引

- 3.0.4–3.4.1历史见 [B77记录](archive/v3.4.1-b77_execution.md) 及原索引；B75后端/客户端B69为上次发布起点。
- 2026-09-20：产品审计及 [肃清方案](archive/2026-09-20_过度设计肃清方案.md)，用户确认1A/2A/4A及晨报时限。
- 3.5.0/B78–B80：本地实施、快修、复查和收尾，见 [B78记录第1–9节](archive/v3.5.0-b78_execution.md)。
- 3.5.0/B81：前端快修、一条龙发布与收尾见同记录第11–12节；9月21日开关恢复历史见第13节。
- 2026-09-22：B81报告生成review确认R1–R9，见同记录第14节。
- 3.5.1/B82：R1–R9及追加问题本地修复、双端验收、独立复查与清理完成，未发布；见 [本版记录](archive/v3.5.1-b82_execution.md)。
