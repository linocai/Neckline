# Neckline — A 股短线量化交易系统 · PROJECT_PLAN

> 本文件是本项目唯一权威施工件。全局规范见 `~/.claude/CLAUDE.md`。
> **铁律:任何 token / key 绝不写进任何被 git 跟踪的文件,一律从 `.env` 读。**
> 项目名 Neckline(颈线)= 技术形态的分水岭,寓意「过线才动手」的纪律内核。

---

## 一、概述

### 1.1 项目定位

Neckline 是一套**只审计、不代下单**的 A 股短线量化决策系统。它把用户的短线交易从「拍脑袋 + 情绪驱动」重构为「回测验证过的规则 + 盘后计划 + 盘中执行哨兵 + 事后违纪对账」。核心信条:**研究先于产品,规则先于直觉,系统给纪律与证据、下单与止损由人和券商执行。**

系统三态同码(同一套信号代码):喂历史 = 回测,喂今日 = 盘后报告,喂单票 = 问询台。回测引擎是常驻地基,不是一次性脚本。

### 1.2 用户画像与约束(全部写死为设计前提)

- **资金**:总资金约 12–13 万,**不再入金,固定分母**(历史越亏越入金是死因之一,新系统按固定盘子做风控)。
- **时间(v1.2 修正,2026-07-25 策略线 register)**:~~本职工作忙,盘中约每 20–30 分钟看一次手机~~ → **用户非上班族、时间充裕可全天盯盘**;交易制度 T+1 不变。**盘中人判层(集合竞价 / L2 盘口)从「不可用」变为「可用」**(此前按上班族注意力设计,盘中只做提醒不做人判;现用户可亲自做盘中强度 / 竞价 / 盘口判断)。**边界(必读,防误用)**:① 本条只改「系统线当前设计前提」这一处;`STRATEGY_LAB.md` 雷区地图信封定义里「上班族注意力」是**历史口径、策略线明确保持原文不改**——此修正**不复活**任何已判死的信封(三场战役死因是 alpha 灭失、非注意力不足),**不得据此宣称信封判决失效**。② 因此可**重新讨论**的既有设计仅限「盘中人判层能力」(如 v1.2.1 呼吸打法的竞价 / 盘口择 T 由人判承担期望假设);**本次不扩范围**,回测审计仍受 EOD 数据边界约束。
- **执行分工**:止损已改用**券商条件单**执行(在系统之外),每笔开仓必挂 -5% 止损。系统只做盘后计划、盘中提醒、盘后对账,**永不自动下单**。
- **实盘节奏**:2026-05 开始实盘,阶段 0–1(数据层 + 策略研究)期间用户继续实盘,按下方「纪律章程」**手动**执行,系统尚不介入。

### 1.3 交割单归因(立项依据,核心发现)

对 2026-05-12 → 07-17 全量交割单做了归因(源表 `/Users/linotsai/Downloads/2026年股票交割单整理.xlsx`;归因脚本在会话 scratchpad,仅参考,**不入仓库**):

- 账户级:净投入 15.28 万,已实现亏损约 2.51 万(**-16.4%**,约 10 周)。
- 闭合回合 37 笔,**胜率 40.5%**、**盈利因子 0.47**(总盈 / 总亏),典型「小赚扛大亏」。
- **13 笔破 -5% 未止损**多亏约 1.38 万 → **占已实现亏损 85%**。→ 结论:止损纪律是第一死因,故 -5% 条件单强制化。
- 买「绿盘大阴线 / 下跌途中票」是主要死因;**追强势票反而 67% 胜率** → 母战法定为强势股短线。
- **7 笔买入次日跌停**,集中在高弹性题材 / 次新票型 → 该类票型入黑名单或强制减仓。
- **持有 4–7 自然日**是唯一打平的持有桶 → 时间退出思想(D4)有据。
- 开仓日上证在 **MA20 下方 28 笔合计 -1.95 万、上方 9 笔 +0.32 万** → 市场过滤器有强信号,但作为「争议项」交回测定生死。
- **越亏越重仓越入金**的加仓行为与亏损正相关 → 仓位纪律(单笔上限、总敞口上限)写死。

### 1.4 与前作 LinoN 的关系(继承与切割)

前作 LinoN(`/Users/linotsai/Lino/LinoN`,FastAPI + SQLite + SwiftUI 双端 + ECS 部署)被用户判定为「**交易记录系统而非量化**」——选股拍脑袋、无回测、LLM 深判是模板腔,已弃用。

- **切割**:Neckline 以**回测引擎为地基**,一切规则参数须过回测才落地;LLM 一律自由对话体,**禁枚举模板卡**。
- **继承**:LinoN 的**工程资产**(尤其大量已踩平的数据源坑)必须吸收,详见 §3.7。LinoN 的坑清单权威在 `/Users/linotsai/Lino/LinoN/CLAUDE.md`(builder 落地对应模块前必读对应小节)。

### 1.6 策略研究档案迁出(2026-08-09,**本文一切 `research/…` 指针的口径声明**)

K8 立项后,**本仓只留回测引擎与生产代码**,策略研究档案由用户整体迁出:

- **新家根**:`~/Lino/whynotme/` —— 现役策略需求 `K8.md`(入仓副本 = `K8_STRATEGY_ARCH.md`)。
- **档案根**(下文简称**「档案」**):`~/Lino/whynotme/Archive/Neckline量化研究档案_K2-K7/`
  - `档案/research/` = K2–K7 的 41 个 runner + 14 份结果报告(`stage1_report.md` / `k2_report.md` / `k3_report.md` / `k4_pre{,2,3}_report.md` / `k4_assembly_report.md` / `k5_cb_report.md` / `k6_etf_report.md` / `k7_pre{,2}_report.md` / `h9_exit_reform.md` / `winners_anatomy.md` / `strategy_atlas.md`)。
  - `档案/docs/` = `STRATEGY_LAB.md`(策略线原权威)· `K4_STRATEGY.md` · K2/K3 研究施工图 · K4 研究 Plan 漏斗定稿 · `K4_advisory_B3退役_20260804.md` · 交接_K7 系统线需求草案。
- 📌 **口径声明(读全文时按这条理解,后文不再逐处重复长路径)**:**本文凡出现 `research/<文件>` 或 `STRATEGY_LAB.md` / `K4_STRATEGY.md` / 上列 archive 文件名,一律指「档案」里的那一份;本仓已无这些路径。** 历史条目里的写法原样保留(它们是当时的事实,改了反而看不懂),⛔ 不逐条重写。
- ⚠ **三个极易搞混的例外(它们**没有**搬走,仍在本仓)**:
  1. **`neckline/research/`**(`panel.py` / `eventstudy.py`)—— 回测面板与事件研究**引擎**,与迁出的顶层 `research/` **是两个东西**。本文早期条目里写作 `research/panel.py::base_universe_expr` 的,**指的是它**(已在关键处逐处改写为 `neckline/research/panel.py`)。
  2. **判分引擎唯一源 `neckline/eval/exit_sim.py`** —— V2-⑨ 下沉件,周复盘 / 能力画像 / 周度校准 / 信息卡都在吃它。**「考卷设计不用了」≠ 这份能动。**
  3. `neckline/{backtest,strategy}/` —— 回测地基,一行未动。
- **迁出的代价(如实登记,不粉饰)**:① `research/_cache/*.parquet`(5.5 G 面板缓存)**已删且不可回滚**,要重跑任何一场战役得先重建面板(耗时以小时计,档案 README 记了办法);② `tests/test_v13_exit_6y_baseline.py` 的**六年真回测基线**(N=1288 / −20.53%)**在本仓已无法复跑** —— 它依赖 `research/lab.py` + `k3_panel.parquet`,现在恒 skip(见 §七 **P4-54**);③ `tests/test_eval_exit_sim.py` 的「research 三处 import 同一函数对象」守门恒 skip(⛔ 别删,拷回即复活)。

### 1.5 V2.0.0 定位(2026-08-02 立项,重心转向)

V1(v1.0 → v1.5.2)把系统建成了一套**纪律 / 回测 / 记录 / 归因**都完善的减损系统,但用户实测出四个真问题:开仓前填写内容过多、大量字段反复给出相同理由、**选股能力没有成为产品中心**、每日 20 只候选高度集中于同一板块(相关性高、信息重复)。

**V2 的总体目标**(蓝图 §一):

> **用轻量纪律保护账户,把主要计算、数据和 LLM 能力投入选股;系统从严格的家教老师转为选股雷达与持仓助手。**

- **选股单位由单票改为股票篮子**(同一主要驱动、次日方向相似或明显联动的 1–3 只),按 **Tier-1/2/3** 分层、D0 冻结篮子卡、D+1 验证与复盘、周期性校准 —— 语义换血总表见 **§2.8**。
- **持仓由「严格家教」改为「全能助手」**:极简台账 + 监控 + 通知分级 + NL 提醒 + 计划继承 + 资金对账 + 交易画像;**纪律外壳(§2.1)一行不动**。
- **继承关系总原则**:**换心脏(候选管线 → 篮子引擎)+ 减负(表单 → 自动快照)+ 装新感官(盘中存拍 + 复盘评价),骨架(数据、纪律、对账、哨兵、推送)全部继承 V1。**
- 施工图 = **§五(旧-V2.0.0)存根 → `archive/V2.0.0_施工图_20260805归档.md`**(17 块,2026-08-04 全部完工上线、08-05 归档);产品语义总纲 = 仓库根 `新版本量化交易APP与选股架构.md`(**用户蓝图,非施工权威**);工程推演原稿已归档 `archive/V2架构设计稿_20260731_20260802立项归档.md`(**已转历史档案,不得作为第二权威**)。

---

## 二、设计共识与领域规则(方向已定案,不得擅改)

> 本章是产品圣经。build 照此实现;**任何改动方向须回 planner**。参数值凡标「回测定」的,由阶段 1 过堂后写死,不由 build 拍。
>
> ⚠ **2026-08-02 V2.0.0 立项**:本章 §2.2~§2.6 中有若干条款被 V2 语义取代(单票候选 → 篮子、排序 → Tier、六类推送 → 三级、决策日志表单 → 自动快照等)。**换血对照总表 = §2.8**,该节凌驾被它标注取代的条款;**历史拍板全文一律保留不删**。**§2.0 第〇原则与 §2.1 纪律章程在 V2 全文有效**(四锁的 V2 精确表述见 §2.8-C,**必读**)。

### 2.0 第〇原则:LLM 是参考,硬条款才是纪律(2026-07-29 用户口述定案,v1.5.0 立项)

**LLM 的作用永远是参考,硬条款才是纪律。** 这条凌驾于本章其余各节对 LLM 的一切描述,新增任何 LLM 产出物前先过这条:

1. **参考件不触发任何机器动作**。LLM 产出的买入 / 离场参考区间、明早证伪剧本、审判评语、问询台回答——**一律不进哨兵判据、不进推送、不进排序键、不改候选去留、不写决策日志、不改任何纪律阈值**。它们只出现在"给人看"的位置。
   > ⚠ **2026-08-03 记名豁免一处(V2 review 判定线 🟡-3 收口)**:本条「**不进推送**」自 V2 起有**且仅有**一个例外 —— `take_profit` kind 的**触达告知**(用户 2026-08-03 拍板开该 kind)。**豁免的边界、四条前提与机械 sanity 闸全文见 §2.8-C-3 与 §五 ⑪-D**;本条其余部分(尤其「**不改任何纪律阈值**」)**一字不变、无任何豁免**。⛔ **不得据此把买入参考区间 / 明早剧本 / 审判评语 / 问询台回答接进任何推送或判据。**
2. **纪律只住章程**。−5% 止损 / 回落止盈 8% / 时间退出两档 / 仓位三仓 / 熔断阈值的唯一事实源仍是现役 `strategy_versions` config(+ 熔断的命名常量),**LLM 说什么都不改它**。「参考在上、纪律兜底」:参考件负责让用户少等几个 8%,纪律负责在用户不动手时兜住。
3. **机器不禁、人可复核**。LLM 判风险大 → 只收回参考件(不给区间、只给不买理由),**票与信息卡照留**;不许把 LLM 的判断做成闸门(§2.5 v1.3.3 拆墙的同一条精神)。
4. **参考件必须自带"参考、非指令"标注**,终选权在用户(与「排序 = 注意力优先级,不是收益预测」并列为语义红线两条)。
5. **参考件必须落库**并可与实际走势 / 成交对拍(LLM 参谋成绩单)——参考的价值要靠事后归因证明,不靠语气。

> ⚠ **勿与 v1.4-④ 的「第〇原则」混淆**:那一条说的是**考卷信息集 ≡ 实盘情报包信息集(同构)**,是信息供给的原则;本节说的是 **LLM 产出物的权限边界**。两条同时成立,互不替代。

### 2.1 纪律章程 v0.1(系统只审计,不执行)

> 🔴 **前置提示(2026-08-09 V2.2.0 立项写;本节其余全文在 `v2.2-k8` 激活前继续一字有效)**:V2.2 将按 K8 §十三 持仓原则**修订本节的退出侧三条** —— 第 2 条的**回落止盈 8% 与时间退出两档整体退役**、第 1 条的 **−5% 强制条件单改为「止损警戒 + 离场决策」**(`stop_pct=0.05` 这个**数值与唯一源地位一字不动**,改的是它触发什么)。**第 3 条仓位三仓制一字不动**(K8 沉默 ≠ 废除);⚠ **第 7 条熔断另案 —— 2026-08-09 用户裁定 #8「整体删除」**(锁定态 / 次日只减不加 / 强制复盘解锁三件机制全删,只留「连续 3 笔止损」作纯提醒),施工在 §五 ⑤-B、**属纯代码、部署即生效、不随章程 staged**。⚠ **第 4 条「单周亏损 ≥ 总仓 2% → 强制复盘」不是熔断,一字不动、⛔ 别连坐删掉。****改动、连带面、以及不得删的风险登记全文见 §五 ⑤ 与 §2.9-A 表末两行**;走 `activate_charter.py --target v2.2-k8`,**staged:用户清空全部持仓 + 明确确认后才激活**,激活前仍按现役 `v1.3.3` 执行。⛔ **在激活之前,任何人不得按 K8 口径实现或解释本节。**

1. **每笔开仓必挂 -5% 止损条件单**,只许设、不许撤、不许下调;系统盘后对账查单完整性。
2. **止盈 / 时间退出(v1.3 退出规则改革,2026-07-25 用户知情越线采纳,staged 生效)**:① **-5% 止损不变**;② **回落止盈 5%→8%**;③ **时间退出仅对非浮盈单**——D5(第 5 个持有交易日)收盘扣双边费后**净浮盈 ≤0 → 次日退出**;④ **浮盈单豁免时间退出**,交回落止盈 8% + -5% 止损管到浪停,**硬上限 15 个交易日**(防"无限期"名义下变相长线)。**⑤ 判向在 D5 定格一次,此后不再重判(2026-07-27 用户拍板,方案 A)**:D5 那天 16:35 用**当日 EOD 收盘**净浮盈判一次向并持久化,③④ 两态自那一刻起**写死**——D6 起无论净浮盈怎么变,豁免的仍豁免、判该走的仍该走,**只有 ④ 的 D15 硬上限例外**(始终按持有交易日数无条件触发)。理由(两条,勿删):**(一)回测验证过的规则才是能守的规则**——回测引擎就是「D5 判一次、豁免后一次性续命到硬上限」,逐日重判等于实盘执行一套从未被回测验证过的规则;**(二)堵死违纪被事后合法化**——「D5 判该走 → 用户没走 → D6 转浮盈 → D7 系统改口豁免」这条路必须封死,否则纪律形同虚设。配套:周复盘新增**时间退出违纪审计**(系统说该走、台账显示没走 → 记违纪),§2.1 第 2 条自此有周线兜底。唯一事实源 = 现役 `strategy_versions` config(`take_profit_retrace=0.08` / `max_hold_days=5`〔非浮盈时间退出档〕/ `max_hold_days_profit=15`〔浮盈硬上限〕/ `time_exit_only_if_unprofitable=True`);哨兵/回测读库自动跟随,**生效于 v1.3 激活**(staged,同第 3 条,激活前仍按 K1 现役值〔回落 5%/hold≤5〕执行)。**风险登记(不得删,原样入 charter 注释)**:② 回落 8% 系 H9 V0 网格观察免测采纳(六格网格唯一同时改良全期与 2026 的格);③④ 组合版**未整体回测**(最接近的 H9-V3 差 724 元未过 2026 生存门禁);**用户知情行使决策权越线采纳(2026-07-25)**,风险已当面告知——组合退出规则的真实期望未经样本外验证,误差主要体现在"刚好在盈亏平衡线附近"的单子判向。证据链:`research/h9_exit_reform.md` + `research/winners_anatomy.md`。**附注(v1.4-①-B,2026-07-28,§七 P0-2 口径落地,不改上文任何阈值)**:持仓票**当日无 EOD 行**(停牌 / 数据缺口)且**尚未定格**时,时间退出**判向挂起**(第五态 `suspended_hold`)——不定格、不推 D5、不推硬上限;`d_count` 照常按交易日累计并展示(纪律口径是「持有交易日数」,不因停牌暂停计数);**复牌当日 16:35 用复牌当日 EOD 正常定格**(届时若 `d_count` 已越过 `max_hold_days`,就在复牌当日定格)。**已定格的判向不因停牌撤回**(⑤ 的「判一次定格」优先)。**这是对既有保守兜底的定向收窄,不是放宽**:只针对「当日无 EOD 行」这一情形(原行为会催用户去卖一只根本卖不掉的票),其余无定格情形(EOD 管线断跑等)仍保守判 `time_exit_next_day`,一字不改。~~止盈不设固定线:回落止盈或时间退出(D4 思想),参数回测定(K1:回落 5%/hold≤5,v1.3 前)~~
3. **仓位纪律(v1.2 章程修订 = 三仓制,staged 生效)**:**最多持 3 只**(注意力约束「只做 3 仓」,不会更多);**单笔金额不定死**——由用户视股价与当时想控仓位当场在区间内自定,系统只把 **4 万**作为**违纪判定上限**(`single_cap`,**非推荐值**)、不替用户拍单笔金额;总敞口设满仓档(`max_exposure_frac`≈1.0)。**-5% 止损不变;止盈与时间退出按上方第 2 条 v1.3 新规**(~~回落止盈 5%、hold=5 不变~~ —— 本行 v1.2 立项时的写法已被第 2 条取代,勿按旧值实现)。唯一事实源 = 现役 `strategy_versions` config;**与第 2 条同批、生效于 v1.3 章程行激活**(staged:用户清掉现有持仓 + 确认后由切换器 `activate_charter.py --target v1.3 --confirm` 激活;激活前仍按 K1 现役值执行。⚠ 库里那行过时的 `v1.2` 章程行〔回落 5%/hold=5〕**保留但永不激活**,见 §五 v1.3-①)。~~单笔仓位上限 2 万;最多持 5 只;总敞口 ≤60%(K1 原值,2026-07-25 前;v1.2 激活后由三仓制取代)~~ **章程回滚 SOP(v1.5-④-A5,§七 P1-24)**:何时该回滚 / 走 `activate_charter.py` 哪条路 / 同步清单 / 要不要重跑受影响周复盘,见 `archive/SOP_章程回滚_20260730.md`(未经实战演练,首次真回滚后按实际经历修订)。
4. **单周实现亏损 ≥ 总仓 2%** → 当晚**强制复盘**(材料由系统生成)。~~「≥5% 次周单笔减半」挂起项~~ → **已否决**(2026-07-20 用户拍板:回测显示纯空效应 + 用户明确不要,永久关闭)。
5. **同票割肉后冷却**:冷却期天数参数回测定(初值 10 个交易日)。
6. **违纪审计并入周复盘**:用户每周提供交割单,系统对账(实际成交 vs 当周报告、条件单完整性)。
7. ~~**熔断纪律**~~ **⛔ 2026-08-09 用户裁定 #8:整体删除**(原文下方全文保留作历史拍板留痕,**一字不删**)。用户原话:「**我不需要你替我做决定;这个程序永远是提醒 —— 连续三笔止损真的发生了,那也是提醒**」。**退役边界(写死)**:**锁定态 / 次日只减不加 / 强制复盘解锁**三件机制**全删**;「连续 3 笔止损」这个**事件本身保留**,降为**一条推送(复用既有 `circuit` kind,改纯告知文案)+ 一条看板事件**,**零状态、零锁、零行为改变**;单日亏损 −4000 那一档**一并删**(用户只点名了连续三笔)。⛔ 不许「为了安全」偷偷留锁定标志 / 灰化按钮 / 自动「建议别开仓」状态位。**风险登记与十处逐项清单见 §五 ⑤-B**(含:退役后系统对连亏只剩提醒、「次日只减不加」的 9:26 汇总必发豁免一并取消、生产历史触发情况必须只读 SQL 查证后如实登记)。**表 `circuit_breaker` 停写留档不 DROP**。以下为原条款全文(**历史留痕,已不生效**):**熔断纪律(v1.2 新增,2026-07-25 用户批准,数字已定;落库即生效,不随三仓章程 staged)**:**连续 3 笔止损** 或 **单日实现亏损 ≥ 4000 元**(约总仓 3%)→ **当日停止开新仓、次日只减不加**,**完成一次强制复盘后解锁**(解锁复用第 4 条 / 4D 强制复盘机制,不另造)。系统**无法物理阻止下单** → 以**哨兵强提醒 + 客户端状态标记 + 日志留痕**实现;**熔断是纯提醒层,绝不代下单 / 撤单**(§3.8)。触发与解锁事件都落库(归因用)。**诚实边界**:熔断只能基于**用户已补录进台账**的成交判定,漏录则失灵——判定所依据的数据与时效须显式呈现给用户(看板 / 报告卡注明「基于台账 N 笔已补录成交」)。**阈值单一源 = 命名常量**(住熔断模块,**非** `strategy_versions` config;理由同第 4 条强制复盘线 `FORCED_REVIEW_LOSS_FRAC`——政策值非回测参数,不进大脑);连续止损判据用到的 `stop_pct` 仍读现役 config(不硬编 -5%)。**与第 3 条 staged 章程的关系**:第 3 条三仓制改的是 `strategy_versions` config 仓位字段、须 staged 激活才生效;熔断不读那三个字段、只读命名常量阈值 + `stop_pct`,故**代码部署即生效、不等章程激活**。**触发链口径补记(2026-07-27 审计,原为未写明的既有行为,实盘触发时不要当 bug)**:① **连续止损链无时间窗** —— 横跨数月的 3 笔止损同样触发(链只看「尾部连续」,不看间隔);② **解锁后链不重置** —— 解锁后第 1 笔新止损即可能再次熔断(尾部仍连着旧链)。两者都是**保守方向**(宁可多提醒),是刻意保留的口径。**「次日只减不加」的落点**:9:25:30 盘前 tick 查锁定态,锁定则 9:26 汇总推送**必发**(即便当日零判定也不被「平静清晨不轰炸」的门槛吞掉)并前置「熔断中:今日只减不加」;客户端横幅是拉取式,不能替代这条推送。

~~「买点 / 止损 / 目标 / 证伪条件」四件套~~ 的止损口径 **-5.0** 是全系统单一常量,只在一处定义,禁止各处漂移(继承 LinoN「规则常量唯一源」教训)。**v1.5.0 起候选卡四件套由「参考三件套」取代**(§2.3 需求 9 拍板 / §五 v1.5-①),**但这条常量纪律一字不变**:参考买入区间那一行里显示的止损价 = `close × (1 − stop_pct)`,`stop_pct` 仍**读现役 config**,不许硬编 0.05、不许由 LLM 产出。

### 2.2 母战法(策略内核)

- **风格**:强势股短线,持有 **2–5 个交易日**。
- **两类买点**:① 强势票**回调低吸**;② 平台**放量突破**。
- **禁买规则(第一版,阈值均回测调)**:
  - 当日跌幅 ≤ **-3%** 的绿盘大阴线禁买(初值)。
  - 距 20 日高点过远的**下跌途中票**禁买(阈值回测定,初值 -15%)。
  - **次新股 / 易跌停高弹题材票型**入黑名单或强制减仓。
- **强势的定义交回测赛马**(见 §6):涨停基因 / 20 日涨幅分位 / 量价结构 三选一或组合。

> **⚠ 阶段 1 判决(2026-07-20,以 SQLite 大脑 `strategy_versions.v1` 为唯一现行内核,本节以上文字保留作历史假设记录)**:日线 2–5 日窗口 A 股均值回归,「追强势」无正净期望——强势三候选**全否决**(`strength=none`);绿盘大阴线禁买、距前高禁买、同票冷却期均**被回测否决删除**;次新剔除否决、~~**高弹题材剔除采纳(风控属性)**、限主板~~(**v1.3.3 起解除,见下条**);买点走 pullback,hold=5,回落止盈 5%。规则 v1 定位:**减损纪律系统,非正 alpha 策略**;正 alpha 开放路径 = 情绪信号(阶段 2)+ LLM 消息面(实盘归因考核)。全文见 `research/stage1_report.md`。

> **⚠ 高弹题材剔除(P6 / 限主板)自 v1.3.3 起解除 —— 拆墙(2026-07-27 用户拍板)。** 章程 `v1.3.3` 把 `forbid_high_elasticity` **True → False**(承 v1.3 全部字段,只改这一个)。**为什么拆**:用户实操域就是高弹板块(五常驻里创新药 / 机器人 / 储能全是创业板 / 科创板大户),而 v1.3 起候选情报管线(§2.3 需求 5)**已刻意含全板块 MAIN/GEM/STAR**;`forbid_high_elasticity=True` 却仍在**四处**生效——周复盘违纪判定(`review/reconcile.py`)、问询台(`api/inquiry.py`)、自选体检(`report/watchlist_check.py`)、回测 entry mask(`strategy/momentum.py`)——**同一系统两套口径**,用户每买一笔创业板都会被周复盘误标违纪。本次把纪律判定域与生成域对齐。**风险登记(不得删)**:① **此 config 与任何回测基线都不同** —— K1 六年回测里高弹是被**剔掉**的,当年作为**风控**采纳(§1.3 第二死因:20% 涨跌幅品种与 -5% 止损不匹配、易一字跌停),关掉它 = 入场域扩到**从未被该 config 回测过**的范围,**冻结基线 N=1288 / −20.53% / 95361.4988 不适用于 v1.3.3**;② 拆墙后高弹的风险**改由 -5% 止损 + 三仓纪律(3 仓 / 单笔 4 万违纪上限 / 回落止盈 8%)+ K4 安检(hard_cut 拦、avoid_flag 标)承担,不再由「不许碰」承担**;③ 这是 §2.3「生成域刻意含高弹、止损频率代价已在策略线审计定价」(2026-07-25)的自然延伸。**四处代码零硬编**——全部由 `cfg.forbid_high_elasticity` 驱动,拆墙不需改一行代码(已逐处核对);`v1.3` 保留在切换器白名单作**唯一合法回退目标**,回退后墙立刻回来。

### 2.3 盘后报告(每交易日 16:00,CLI/文件形态先行)

> ⚠ **V2.0.0 起本节大部分被取代**:候选 20 只 / 排序 / 候选卡 / 自选体检 → **篮子 + Tier + 篮子卡**,报告重构为**篮子日报**。对照见 **§2.8-A**,施工见 §五 V2-⑤⑥⑦⑬⑭。以下原文保留作历史拍板留痕。
>
> 定 16:00 是因 A 股有盘后交易,收盘数据 16:00 后稳定。

- **情绪仪表盘**:涨停家数 / 跌停家数 / 连板最高高度 / 炸板率 / 昨日涨停股今日平均溢价 → 输出**明日仓位额度**(满额 / 半额 / 休息)。
- **强势板块**:**加权不圈死**——板块只加分,全市场强势形态票均可入池;必须含**板块年龄因子**(启动第几天、涨停梯队扩缩、龙头是否掉队;启动早期加分、连续主线 4–5 天后衰减降权)。
- ~~**候选 20 只带评分**,每只**四件套:买点 / 止损 / 目标 / 证伪条件**~~(v1.5.0 起由三件套取代,见下条)。
  - ~~**前 10 只**过 **LLM 逻辑审判**(读新闻 / 题材 / 公告,判催化持续性,**有一票否决权**)~~(v1.5.0 起 20 只全覆盖)。
  - ~~**后 10 只**只给分数与形态标签,不耗 LLM~~(v1.5.0 起退役)。

> **⚠ 候选输出层变更拍板(2026-07-29 用户口述定案,需求 9,落地 §五 v1.5-①②③)——四件套 → 参考三件套,20 只全覆盖。** 承 §2.0 第〇原则(LLM 是参考,硬条款才是纪律):
> - **候选卡输出层 = 三件 LLM 参考件**:① **买入参考区间**(基于 60 日 K 线 + EOD 数据推"若明日买入,适合的价格区间";**唯一底线 = 数字必须落在明日涨跌停价区间内,出界不显示**;具体止损价 `0.95×` 并进这一行一笔带过)② **离场参考区间**(本轮上涨的压力位区间,**明示参考、非止盈线**;回落止盈 8% 纪律一字不动、仍是被动兜底)③ **明早证伪剧本**(面向 T−1 晚阅读的开盘行动指引,带分支;生成时把哨兵的结构化阈值喂给 LLM,使剧本与盘中自动警报同频)。**LLM 其他评语保持现状。**
> - **止损独立条目删除**(−5% 是章程常量,不再每卡说教);**哨兵的机械结构化证伪条款保留在水下**(`invalidation_spec`/`entry_spec` 仍是盘中自动警报的唯一判据源,**只是卡面不再展示原始条款**)。
> - **20 只全覆盖**:每票**或出三件套、或出不买理由**(~~前 10 只审、后 10 只不耗 LLM~~ 退役);**风险否决 → 三件套全无、只出不买理由,但票与信息卡保留**(机器不禁、人可复核)。
> - **今日计划拆两块**:**持仓股** / **候选列表(每日 20 只)**。
> - **语义红线 +1**:参考件必须标注**「参考、非指令」**,终选权在用户;原「排序 = 注意力优先级,不是收益预测」不变。

> **⚠ 候选语义变更拍板(2026-07-25 用户,需求 5,落地 §五 v1.3-③-C3)**:自 v1.3 起,**候选列表不再是「系统认为会涨的票」,改为「过完安检、值得用户花注意力的票」,终选权在用户**。生成源从 **K1 entry mask 退役**——改走「情报筛选管线」四步(板块拥挤度 top〔五板块常驻 + 当日暴起〕→ 全板块 MAIN/GEM/STAR 只过卫生线 + 趋势向上、**不套 K1 主板 only 与回调买点** → K4 安检〔`hard_cut` 拦 / `avoid_flag` 标,读 DB `K4.k4_advisory`〕→ 情报排序〔资金流强度 + 题材持续天数**反用**〕)。**生成域刻意含高弹板块**(贴用户实操,与 K1 哲学相反,用户知情拍板,止损频率代价已在策略线审计定价)。候选卡文案须跟上此语义(不再标「推荐买点」)。**§3.8 铁律「同码三跑道」已相应重述**(候选生成解耦、纪律核对仍同码)。~~候选 = 系统认为会涨的票(v1.3 前 K1 口径)~~
- ~~大盘 MA20 市场过滤器 = 争议项~~ → **已否决定案**(2026-07-20 用户拍板,与阶段 1 回测结论一致:回顾性分层相关真实,但做成实时闸门样本内外双双更差——滞后指标不可交易)。**市场择时职责归情绪仪表盘**(其阈值第一版为启发式、标注未回测,靠实盘归因迭代)。

> **⚠ 自选体检拍板(2026-07-21 用户,v1.1 新增报告节)**:16:35 报告新增「**自选体检**」节——对用户自选池(§2.4 v1.1)每只票**用同一套评分管线**跑当日评分 + 形态标签 + 纪律红绿灯(禁买线 / 票型黑名单 / ST / 板块限制核对)+ 形态触发买点条件的给完整四件套。**LLM 只审「当日状态较上一份报告发生变化的」∪「用户点名(pinned)的」**(控成本 / 控时长),其余确定性输出。自选体检**不改候选 20 评分逻辑、不进候选榜**,是独立一节(同码复用评分,只扩输入范围到自选池)。落地 §五 v1.1-C。

### 2.4 盘中哨兵(轮询免费实时源,1 分钟一拍)

> ⚠ **V2.0.0 改动两处、不动判定**:① 关注池组成换成「持仓 + T1/T2 篮子成员 + 相关板块 ETF/指数 + 昨日涨停」;② 推送白名单**六类 → 三级 × N kind**。**四哨兵判定逻辑一字不动**,新增篮子验证状态机与盘中存拍。对照见 **§2.8-A**,施工见 §五 V2-⑧⑪。
>
> **铁原则:盘中不产生任何新决策,只执行前晚计划,永不盘中推荐新票。**

1. **买点哨兵**:候选触达预设买点**且**确认条件成立(量能折算、站稳 VWAP)→ 进盘中看板。
2. **退潮哨兵**:盘中情绪恶化(炸板率飙升 / 跌停扩大 / 主线板块跳水)→「**今日计划作废、禁开新仓**」红色刹车。
3. **持仓哨兵**:持仓票放量跳水逼近止损线 / 触达目标区间 / 所属板块跳水预警。
4. **证伪哨兵**:候选分时走坏(低开不回 / 全天 VWAP 下方 / 量能异常 / 板块梯队瓦解,证伪条件**前晚写死**)→「剔除勿进」。**盘中主力资金流免费源不可靠,证伪只用价量结构**(VWAP / 量能折算 / 高低开)。

> **⚠ 推送路由拍板(2026-07-20 用户,推翻阶段 3「四哨兵各自推送」的默认设计)**:APNs 锁屏推送**只留两类**——① 每日 16:00 盘后报告就绪;② 退潮红色刹车(默认开、设置屏可关)。**其余哨兵事件(买点触发 / 证伪剔除 / 持仓预警)一律不推 APNs,只进 App「盘中看板」板块**(打开即看)。每类事件的推送开关在设置屏可配。理由:比行情速度必输同花顺,系统价值是把前晚计划执行成判决(看板),不是抢报新闻。上文四哨兵的**判定逻辑一字不改**,变的只是「触发后去哪」——看板(全部四类)vs APNs(仅退潮 + 报告)。阶段 3 已建的 `sentinel/channels.py` 通道抽象保留、阶段 4 新增 APNs 通道(复用 LinoN `push/apns.py`),**Bark 降为备用通道**。

> **⚠ 盘前校准 tick 拍板(2026-07-21 用户,v1.1 新增第 5 类哨兵动作 + 推送白名单扩到四类)**:交易日 **9:25:30** 加一次「**盘前校准 tick**」(集合竞价快照 9:25 后即含开盘价 / 竞价量)——**纯规则判定、零 LLM**:候选高开超阈→「买点已变形今日失效」;低开踩证伪线→「开盘即证伪预警」;竞价量能异常标注;持仓大幅低开→「止损条件单今日可能触发」。**9:26 一条汇总推送 + App 内明细**。这仍是 §2.4 铁原则的题中之义——**盘前不产新决策,只判前晚计划是否已被集合竞价作废**(执行层,不是选新票)。哨兵进程(FastAPI lifespan asyncio 单 unit)加盘前窗口分支,**现有 9:35 起 intraday 判逻辑一字不改**。
>
> **推送白名单(v1.1 四类、v1.2 扩五类、v1.3 扩六类,推翻更早「只两类」)**:① 16:35 报告就绪、② 退潮红色刹车、③ **9:26 盘前校准汇总(v1.1 新)**、④ **D5 时间退出(v1.1 新)**、⑤ **熔断提醒(v1.2 新,§2.1 第 7 条;默认开、与退潮刹车同级)**、⑥ **K4 持仓派发警报(v1.3-② 新,用户 2026-07-26 拍板独立 category `HOLDINGALERT` + 独立开关 `push_holding_alert`,默认开)**——各自独立开关(设置屏可配)、各自独立 APNs category。**D5 时间退出**是规则 v1 `hold=5` 时间退出纪律的**执行器**(此前无人触发 = SOP 最大裸奔点):D5 当天看板置顶 + 推送。**熔断提醒**是 §2.1 第 7 条熔断纪律的执行器(连续 3 笔止损 / 单日亏损 ≥4000 触发即推 + 客户端状态标记)。**K4 持仓派发警报**只推**强价量证据**命中(年线下涨停 / 放量大阳派发 / 换手 >10%);题材持续天数=概念板块成分弱证据,只进看板不推(守铁律「证伪只用价量结构」)。买点 / 证伪 / 普通警示仍只进看板不推 APNs(不变)。落地 §五 v1.1-A(盘前校准)/ v1.1-B(D5)/ v1.2-A2(熔断)/ v1.3-②(K4 派发警报)。
>
> **⚠ K4 持仓牌强警示推送(v1.3 需求 7;用户 2026-07-26 拍板 = 第六类,推翻 planner「复用 D5EXIT」默认)**:持仓牌每日重算的**强警示级别**(年线下涨停 / 放量大阳 = 派发警报、题材持续 ≥4 天、换手 >10%)推 APNs;**普通警示**(题材 2-3 天等)只进盘中看板。**用户拍板独立第六类推送**(不复用 D5EXIT):理由 = K4 派发警报说的是「你的票可能在被派发」,与 D5 的「持有到期了」是两回事,合并后关一个会连坐另一个 → **独立 category `HOLDINGALERT` + 独立开关 `push_holding_alert`(默认开)**;`notify.__all__` 白名单结构守护五→六、apns category 五→六、`app_settings` 幂等加列、`GET/PUT /settings/push` 契约扩六字段。落地 §五 v1.3-②。守铁律「证伪只用价量结构」:换手/年线下涨停/放量大阳=强价量证据(触发 APNs)、题材天数=概念板块成分弱证据标「参考」(**不单独触发强警示**)。

### 2.5 问询台(LLM 问询板块)

用户丢外部消息源的票进来 → 系统先跑**确定性材料**(同一评分管线跑分 + 纪律/硬线核对 + 板块年龄 + K4 安检)→ LLM **带工具调用**(实时取数 / 重算 / 联网搜索)自然语言回答。

> **⚠ 定位变更拍板(2026-07-27 用户,v1.3.3「拆墙」同批)——审判员 → 自由分析师。** 用户实测拿创业板票(300759 康龙化成)问,只得到一句「未通过硬性纪律核对,不予放行」,**问不出任何东西**;问询台的价值本该是"把牌摊开",却退化成一道闸。自 v1.3.3 起:
>
> - **硬栏杆全拆**:**任何票都走完整流程、拿到实质回答**,不再有「纪律不过 → 直接终止、不劳 LLM」的短路。
> - **裁决二值退役**:`不符合` / `初审通过进海选池` 两个枚举值**删除**。`verdict` 字段保留(客户端契约不破),但取值改成**纯描述性标注**`已分析` / `已分析·有风险提示` —— **它不是判决,不授权也不禁止任何操作**。
> - **纪律降级为警告标注**:真硬线(ST / 退市风险 / 流动性太差 / 次新 / 停牌)与 **K4 安检命中**(如「年线下涨停,K4 标为派发域」)仍在回答里**如实提示并解释含义**,**但一律不拦**;prompt 里明写「不要因此拒绝分析这只票」。
> - **软护栏(保留,但改成软形式)**:**不下「买 / 卖」指令** —— 可以把走势、逻辑、风险、赔率分析得很透,但不产出买卖指令。**软 = 只在 prompt 层约束**,刻意**不做**枚举强校验、**不做**输出后处理拦截(旧的"三重保险"连同二值裁决一并拆除)。理由(定案,勿改回):**LLM 的买卖判断没有回测支撑,不该塞进用户的决策链;分析归系统,扣扳机归用户。** 强校验换来的是把模型逼进模板腔(违背 §2.7),而真正的护栏本就是 §3.8「系统永不自动下单」这条铁律,不是在文本里 grep「买」字。
> - **「初审通过进海选池」退役**:问询台**不再自动写 `inquiry_pool`**。想让一只票进当晚报告,由用户在客户端**一键加自选**(「+自选」按钮早已在问询卡上,且不看裁决值)——自选池本就进当晚**自选体检** + 哨兵**关注池**,路径更短也更符合"终选权在用户"。`inquiry_pool` 表与报告侧消费逻辑(`load_pending_inquiry_codes`/`mark_inquiry_pool_consumed`)**保留不动**(向后兼容,空池 noop,历史待消费行仍正常消费),只是不再有自动写入方。
>
> ~~**裁决只有两种**:「不符合 + 依据」或「初审通过,进当晚海选池」~~(v1.3.3 退役)。**「永不产出『现在就买』」仍然成立**,只是实现方式由"代码级二值裁决 + 枚举 + 强校验"改为"prompt 层软护栏 + §3.8 系统永不下单"。

> **拍板落地(2026-07-20,部分被上条取代)**:阶段 4 把问询台做成 App「问询台」板块(自由对话体聊天,继承 LinoN `/chat` 无状态、客户端持有上下文的对话工程)。~~「初审通过进当晚海选池」= 落 `inquiry_pool` 表(当日),`report.py` 生成当晚报告时把海选池的票强制纳入候选评分 universe~~(v1.3.3 退役,改一键加自选;表与消费侧保留)。~~裁决二值 = 硬约束,system prompt guardrail + verdict 枚举只两值双保险~~(v1.3.3 退役,见上条)。

### 2.6 回测引擎(系统地基,带笼子的策略进化)

- **同一套信号代码三跑道**:喂历史 = 回测、喂今日 = 报告、喂单票 = 问询台。
- **回测必须正确处理**:前视偏差、T+1、涨停买不进 / 跌停卖不出、停牌、复权(前复权)、滑点、手续费。
- **版本号双线规约(2026-07-22 用户定)**:**系统版本走 `v` 字头**(v1.1、v1.2……,产品/工程升级);**策略版本走 `K` 字头整数**(K1、K2……,`strategy_versions` 表标签,回测过堂的大脑)。两线独立演进互不挂钩;现役策略 = **K1**(原名 v1,2026-07-22 双端 DB 已改名)。任何文档/报告/客户端展示的策略号一律 K 字头。
- **策略进化带笼子**:
  - 按月 / 季调参;任何调整须过**同一道门**——回测 + walk-forward **样本外跑赢现役版本**才可上线。
  - 大脑带**版本号 + 变更日志 + 实盘表现按版本归因**。
  - **调参最终批准权在用户。**

### 2.7 LLM 输出风格(全系统硬约束)

- 一律**自由对话体、自然叙述**;**禁止结构化模板卡 / 枚举腔**(用户明确厌恶 LinoN 三轴卡)。
- 语气自然但**立场铁**:纪律不过,不放行。
- ⚠ **V2 附注(2026-08-02)**:第二句「纪律不过,不放行」的**语气**保留,但**问询台自 v1.3.3 起已是自由分析师、不拦票**(§2.5),V2 延续;**篮子卡与剧本同理 —— 参考件不做闸门**。禁的是模板腔,不是禁止一个机器可读收尾标签 / 一段供夹逼校验的 JSON(`llm/judge.py` 模块头已论证)。

### 2.8 V2.0.0 语义换血总表(2026-08-02 立项;**本节凌驾 §2.2~§2.5 中被标注取代的条款**)

> **读法**:§2.1(纪律章程)与 §2.0(第〇原则)**继续全文有效**;§2.2~§2.6 中被下表标「**取代**」的条款,自 V2.0.0 起以本节为准,**原文一律保留作历史拍板留痕,不删**(照 §2.1 / §2.3 既有的删除线 + 附注体例)。施工细节全部在 §五 V2.0.0。

#### A. 逐条换血对照

| V1 条款 | V2.0.0 处置 | 落地块 |
|---|---|---|
| §2.3 **候选 20 只单票**(含五常驻板块保底 + 竞争位) | **取代** → **股票篮子**:1–3 只/篮,必须说明共同驱动与「为何是这几只」;**仅历史相关性不足以成篮**;同票多篮时主归属唯一(行业闸 lift 最高者) | ⑤⑦⑬-1 |
| §2.3 **候选排序(排序键三级)** | **取代** → **Tier 分层**:T1≤2 / T2≤5 / T3≤10,**上限非配额,允许 T1 为空**;机械分定档、LLM 只在同档内微调 | ⑥ |
| §2.3 **候选卡参考三件套 / 老四件套** | **取代** → **篮子卡 11 项**(蓝图 4.6);夹逼 / 冻结 / 口径指纹 / disclaimer 体例**原样移交** | ⑦⑬-3⑬-6 |
| §2.3 **自选体检节** | **删除**(裁定 #9:自选池 + 同花顺对账直接删) | ⑬-11 |
| §2.4 **哨兵关注池 = 候选 + 持仓 + 昨日涨停** | **取代** → **持仓 + T1/T2 篮子成员 + 相关板块 ETF/指数 + 昨日涨停**;仍是**代理样本不是全市场** | ⑧-A |
| §2.4 **四哨兵判定逻辑** | **不动**(纪律外壳一行不动);新增**篮子验证状态机**(四态)与**四项持仓监测**,与纪律分支并列不替代 | ⑧-C⑪-A |
| §2.4 **推送白名单六类** | **取代** → **三级 × N kind**(立即 / 重要不紧急 / 盘后汇总;开关按 `kind` 配)。「新增推送须用户拍板」这条纪律**保留** | ⑪-B |
| §2.5 **问询台 = 自由分析师** | **保留**;但「+自选」写回通道随自选池删除而消失 → 问询台成**纯分析入口** | ⑬-11 |
| §2.6 **同码三跑道 / 情报筛选管线** | **取代** → 选股主链 = **扫描 → 聚合 → Tier → 卡 → 验证 → 复盘**,可配部分读**现役策略包**;§3.8-(a)(纪律与回测同码、参数单一源)**不变** | ③④⑤⑥ |
| §2.6 **策略进化门禁** | **扩** → 章程走 `activate_charter.py`(不变)+ **选股策略包走 `activate_pack.py`**;两条版本线、两张表、两套激活流程,**互不混用** | ③ |
| §2.1 **纪律章程 v0.1 全文** | **一行不动**(现役 `v1.3.3`);红黄牌**降格为风险信息与模型特征,不等同禁买**(蓝图 7.2) | 全域 |
| §2.3 **决策日志强制表单(论点五必填 + 追价上限)** | **取代** → **开仓自动快照(`entry_snapshots`)+ 用户字段全部可选**;`decision_log` 停写留档 | ⑩-C |

#### B. V2 新增的产品级设计共识(V1 无对应条款)

1. **事实 / 用户行为 / 模型判断三类分存,互不覆盖**(蓝图 2.4):事实 = parquet 分区 + 冻结快照表(只增不改);用户行为 = `user_actions`(只追加);模型判断 = 篮子 / 卡 / Tier / 复盘 / 画像(**新判断 = 新版本,不回写**)。
2. **选股与持仓职责独立、信息互通**(蓝图 2.3):可互读、可追加,**不得回头修改对方已冻结的历史信息**;选股日报可追加用户行为观察,**但不得因此修改原始 Tier 与篮子判断**。
3. **外松内紧**(蓝图 2.1):只对可能伤害账户生存的事项保留硬约束(单笔与账户风险上限、券商止损条件单状态、连亏与单日亏损熔断、停牌退市重大公告行情失效);其余改为提示 / 自动记录 / 可选输入,**不再用长表单阻断交易**。
4. **不吸收的做法(蓝图 7.3,写死为禁令)**:满仓一击;跌停后用「格局」代替证伪;把一次成功修复机械复制到其他股票;为增强选股而无节制叠加指标;用用户偏好直接替代市场证据;**因持仓盈亏回头修改原始选股结论**。
5. **画像初期不得反向影响客观 Tier**(蓝图 4.4);解禁路径只能是展示层个性化,且须另行立项。

#### C. 第〇原则四锁的 V2 精确表述(**必读 —— V1 原文逐字照搬会与 V2 直接冲突**)

§2.0 的四锁原文是「参考件不进排序键 / 不进哨兵 / 不进推送 / 不改候选去留」。V2 里有两处**表面冲突**,必须按下面的精确表述执行,**不许各自理解**:

1. **「不进排序」→ 精确为「不进机械分」**。V2 明确允许 **LLM 在同档内微调排序**(架构裁定,第〇原则的 Tier 变体),但:**机械分五维的输入白名单 `_TIER_SCORE_INPUTS` 里不得出现任何 LLM 产出字段**;LLM 只能改**档内序**、**不得跨档**;`rank_mech` 与最终 `rank_in_tier` **两个都落 `tier_history`**,微调理由必须留痕。
2. **「不进哨兵」→ 精确为两条**:(a)**LLM 产出的自由文本与数字一律不进哨兵判据** —— 哨兵只读**机械生成或过了机械闸的结构化 spec**(`verification_spec` / `invalidation_spec`)与现役章程 config;(b)**关注池成员选择受篮子影响 = 注意力分配,不是纪律判定** —— 盯谁可以由篮子决定,盯到之后每只票的判定阈值仍只来自章程与机械 spec。
   > ⚠ **2(b) 的适用边界(2026-08-03 收紧,V2 review 判定线 🟡-4 打出来的;这是通用规则,不只针对退潮刹车)**:**2(b) 只覆盖「盯谁」,⛔ 不覆盖任何「聚合纪律触发器」的样本组成。**
   > **为什么必须分开**:2(b) 能成立,靠的是「**每只票的判定阈值仍只来自章程与机械 spec**」这句 —— 对**逐票判定**,盯谁与判什么是两件事,换样本不改判据。但对一个**聚合量**(如「主线板块均值跌幅 ≤ 阈值」),**样本组成本身就是判据的输入** —— 换一批票平均,结论直接变;「阈值没动」是真的,但不相干。**⛔ 凡进入纪律触发器的聚合量,其样本必须是机械派生的**,不得由 LLM 的选择塑形(哪怕只是间接塑形)。
   > **判据(自查用)**:问一句「**换一批成员,这个数会不会变**?」—— 会变 = 样本即判据 = 不得沾 LLM;不会变(纯逐票比阈值)= 才落在 2(b) 的豁免里。
3. **「不进推送」→ 精确为「判定动作永远机械;数值来源分两类」(2026-08-03 改写,V2 review 判定线 🟡-3 收口;原文「触发条件永远是机械的」在 `take_profit` 上与用户拍板正面打架,故在此收口而非留字面冲突)**:
   - **默认(无豁免)**:参考件本身不推 APNs;推送由机械事件触发,文案里可以引用参考件的结论,**触发所用的数值也必须是机械的**。
   - **记名豁免(目前有且仅有一个)**:**`take_profit` kind 的触达告知**(2026-08-03 用户拍板)。参考件的数值可以作为**触发位置**,但**四条前提必须同时成立,缺一即豁免失效**:
     1. **不改变任何纪律判定** —— 止损 / 回落止盈 / 时间退出 / 仓位 / 熔断**一字不动**,不产生任何持仓动作;
     2. **该数值已过机械 sanity 闸**(⑪-D 的两道),未过 → **该票此 kind 不武装**;
     3. **纯告知型文案**,禁指令词,必须点明「这是你计划里的参考位、不是止盈线,纪律仍是回落止盈」;
     4. **用户可关**(per-kind 开关)。
   - ⛔ **豁免不外延**:买入参考区间、明早证伪剧本、审判评语、问询台回答、篮子 `falsified` —— **一律不得**据此接推送或判据。要再开一个,走 `basket_falsified` 那条路(用户拍板 **且** 三处红线同步改)。
4. **「不改去留」→ 精确为「不做闸门」**:LLM 判风险大 → 只影响卡面内容(不给区间、只给不买理由),**篮子与成员照留**;`hard_cut` 之类的机械闸仍由 K4 advisory 与卫生线承担,**不由 LLM 承担**。
5. **不变的两条**:参考件**必须自带「参考、非指令」标注**;参考件**必须落库**并可与实际走势 / 成交对拍(参谋成绩单)。

### 2.9 V2.2.0 K8 语义换血总表(2026-08-09 立项;**本节凌驾 §2.2~§2.6 与 §2.8 中被它标注取代的条款**)

> **读法**(照 §2.8 既有体例):**§2.0 第〇原则继续全文有效**(⚠ 但 §2.1 纪律章程本版有实质修订,见下表最后两行);被下表标「**取代**」的条款自 V2.2.0 起以本节为准,**原文一律保留作历史拍板留痕,不删**。需求原件 = `K8_STRATEGY_ARCH.md`(一字未改);施工细节全部在 §五 V2.2.0。
>
> ⚠ **本节只写「什么变了」**。K8 的完整需求去 `K8_STRATEGY_ARCH.md`,落地怎么做去 §五 —— 三处**不重复**,冲突时:需求以 K8 原件为准,实现口径以 §五 为准。

#### A. 逐条换血对照

| V2 / V2.1 条款 | V2.2.0 处置 | 落地块 |
|---|---|---|
| §2.8-A **Tier 分层 = 机械分五维加权 + 质量线定档** | **取代** → **K8 六道关口门槛制**(市场 / 驱动 / 板块 / 核心 / 位置 / 证据);机械分与百分制卡**保留但降级**为「档内排序 + 展示标度」,**不再是定档的闸** | ③-A/③-D |
| §2.8-A **选股主链 = 扫描 → 聚合 → Tier → 卡 → 验证 → 复盘** | **扩** → 链首插入 **行情状态层**(D0 盘后三态),链中插入 **六道关口 + 落地起跳位置关**;其余环节不变 | ②③ |
| §2.8-A **策略包 = 单包制(`selection_packs` 全表唯一现役)** | **取代** → **一条骨架线(`K8-V0.5`)+ C/Z/Y 三条引擎线并跑**,每线独立版本 / 独立激活 / 运行·停止态;唯一现役约束由「全表唯一」改为「**每线唯一**」 | ① |
| §2.6 / §2.8-A **候选/篮子的版本指纹 = `pack_version` 单值** | **扩** → 篮子级记 `skeleton_version`,**成员级**记 `engine_code` × `engine_version`(K8:每票唯一主引擎 + 准确版本);**归因因此必须在成员级做** | ①③-E |
| §2.3 / §2.8-A **基础股票池含 `MAIN/GEM/STAR`** | **收窄** → **`MAIN/GEM`**(K8 §三 排除科创板)+ **排除白酒**(按 `stock_basic.industry` 精确归类);**股价无上限 = 确认现状、零改动**。🔴 **只收选股生成域,章程 `forbid_high_elasticity=False` 一字不动**(K8 只说「不选」,没说「不许买」) | ① |
| §2.8-A **篮子 1–3 只 / T1≤2 / T2≤5 / 合计 ≤7 / 允许空缺** | **不动** —— 与 K8 §七/§八 **逐字吻合**,V2.1-② 已改到位(本版零改动,如实登记这是 V2.1 批 1 的红利) | — |
| §2.8-A **复盘 = 篮子日复盘(`basket_review_daily`)+ 周度评价引擎** | **扩** → **双时钟**:**选股时钟**(覆盖 D0 全部 T1/T2、D1 收盘统一验证一次、**买没买不影响样本**、验证完即**结案**)+ **交易时钟**(实际买入唯一启动条件,跟到全部离场结案)。日复盘不废,是双时钟的素材层 | ④-A/④-B |
| §2.8-B-2 **选股与持仓职责独立、信息互通** | **不动**,并由双时钟**结构性加固**:选股时钟的写入路径零 import 持仓(守门单测),「买没买不影响样本」不靠自觉 | ④-A |
| §2.8-A **进化门禁 = 章程走 `activate_charter` + 选股包走 `activate_pack`** | **扩** → 选股侧变成**四条线各自过闸**(骨架 + C/Z/Y);迭代输入 = 复盘板块的**四分类修改建议**(保留 / 观察 / 降权 / 淘汰),**用户确认后才发新版本**。🔴 **⛔ 零自动回写**(V2.1 裁定 #3 一字不变) | ①④-D |
| §2.1-2 **回落止盈 8%(机械)** | **取代** → **退役**(K8 §十三「盈利离场不设统一机械比例」),改由「进入目标离场区间后按当时走势决定离场」 | ⑤ |
| §2.1-2 **时间退出两档(D5 判向定格 / 浮盈 D15 硬上限)** | **取代** → **整体退役**(K8 §十三:上涨效率下降 → **保留主观换股权**,不设机械规则) | ⑤ |
| §2.1-1 **每笔开仓必挂 −5% 止损条件单**(只许设不许撤) | **取代** → **「止损警戒 + 离场决策」**(K8 §十三)。`stop_pct=0.05` **值与唯一源地位一字不动**,改的是它触发什么;周复盘由「违纪判定」降为「**警戒记录**」 | ⑤ |
| §2.1-3 **仓位纪律三仓制** | **一字不动** —— K8 **沉默**,而**沉默 ≠ 废除** | ⑤-A |
| §2.1-7 **熔断纪律(锁定态 / 次日只减不加 / 强制复盘解锁)** | 🔴 **整体删除**(2026-08-09 用户裁定 #8,**不是 K8 要求,是用户对"程序替我做决定"的否决**):三件机制全删,只留「连续 3 笔止损」作**纯提醒**(一推送 + 一事件,零状态零锁零行为改变);单日 −4000 档一并删。⚠ planner 上一轮裁的「熔断锚点」问题**随之作废** | ⑤-B |
| §2.1-4 **单周亏损 ≥ 总仓 2% → 强制复盘** | **一字不动** —— ⚠ **它不是熔断**,⛔ 别连坐删 | — |
| §2.8-A(隐含)**篮子成员可跨引擎归属** | **搁置不采用 → 单篮子单引擎**(2026-08-09 用户裁定 #9):引擎标在**篮子**上、**成员继承**;归因落篮子级。K8 §四 字面允许混引擎,**本版不用**(C「已确认主线」与 Z「新方向」语义互斥)。**未来要开:升 `K8-V` 骨架版本再议**,⛔ 本版不预建 | ③-E |

#### B. V2.2 新增的产品级设计共识(V2 无对应条款)

1. **行情状态是一等公民**:D0 盘后三态(趋势延续 / 高位分歧 / 切换确认)落表,**选股、持仓、复盘三处共读同一张表**;持仓侧**纯展示、⛔ 无动作**(K8 §十三 只把它列作「辅助证据」)。
2. **「落地起跳」四态是本项目第一个位置类机械判据族**:下落(排除)/ 落地待确认(T2)/ 支撑确认起跳(T1)/ 高位加速(低优先级)。**只产注意力分层,⛔ 不是买入期望背书**。
3. **交易资格四件套是下单资格**(K8 §十一:上涨判断 / 入场区间 / 目标离场区间 / 判断失效位置)。落地 = **T1 的必要条件** + 开仓时缺件出**警示**;⛔ **不是拦截**(系统只审计不代下单,§3.8)。
4. **阈值必须自报来源**:引擎包里每个阈值强制带 `provenance`(`audited` + `ref` / `engineering_v1` + `basis` + `calibration:pending`),**闸 1 校验缺失即拒**。这是把「⛔ 不冒充审计结论」变成过不去的闸,而不是一句自觉。
5. **用户主观说明是唯一的人工输入**(K8 §十五「用户只补充系统无法识别的主观原因,每次一条简短说明」)—— 落 `trade_clock_events.user_note`,**只追加**;⛔ 不做 LLM 代猜(§七 P3-28 纪律不变)。

#### C. 第〇原则在 K8 门槛制下的精确表述(**必读 —— K8 §六 逐字照搬会与 §2.0 直接冲突**;✅ **本节五条已于 2026-08-09 由用户裁定 #6/#7 逐条拍板**,⛔ 不重开)

K8 §六 说「选股采用门槛制…… LLM 负责组织证据、识别矛盾、比较核心地位和说明结论」;§2.0 第 4 锁说「LLM **不做闸门**」。两者不加处理会正面打架。精确表述:

1. **六关拆两类**:**机械关**(市场 / 板块)判「过 / 不过」,全机械、可复现、可回放 → **硬否决**;**证据关**(驱动 / 核心 / 证据 / 🆕 **位置**)由 LLM 组织证据 → **只能降级(T1→T2→退出正式候选),不能除名**。
   > 🔴 **2026-08-09 用户裁定 #11 改判**:位置关**由机械关移入证据关**(原因:K8 对它只有五句定性、零数字;工程侧翻译出的十二个阈值连乘后交集近乎为空)。**第 4 锁「LLM 不做闸门」因此不但没被突破,反而更严** —— 六关里能硬否决的只剩两道读客观预计算量的关。⛔ 不得改回。详见 §五 ③-C。
2. **「退出正式候选」≠「从报告里消失」**:被降到出局的票**仍在报告 ③b 未定档披露里列名 / 分 / 卡在哪一关 / 原因码**。§2.0 第 3 条「机器不禁、人可复核」原样成立。
3. **⚠ 与 K8 字面的一处出入(如实登记,✅ 用户已拍板照此办)**:K8 允许证据缺口导致「退出」,本节把 LLM 侧的「退出」收窄到上面第 2 条那一层。⛔ 这不是打折,是把它落到与第〇原则相容的位置。
4. **LLM 给的引擎归属必须过机械对拍**:`basket_reason` 输出的**每个篮子**一个 `engine_code`(裁定 #9 单篮子单引擎,不是每票),要与该引擎的机械关阈值对拍,**不满足的成员直接出篮**(⚠ 与角色对拍的 `role_conflict`「两说并存」姿势**刻意不同**,别套错:篮子只留 1–3 只本就是筛选,不是并存的场合),⛔ 不静默采信 LLM。
5. **行情状态第五维的两道锁**(前视锁 + 权限锁)见 §五 ②:**「市场现在处于哪一段」是状态,可由历史统计推出;「选股规则该怎么改」是规则,只能由人改。** 第五维落在前者。

---

## 三、技术选型(在此定死,不留给 build 猜)

### 3.1 语言与运行环境

- **Python 3.11+**(本地研究期,不受 LinoN 的 3.9 系统解释器约束;polars / pyarrow 在 3.11 上性能与内存更优)。阶段 0–3 全本地跑。
- 依赖装库走**阿里云 PyPI 镜像**(继承 LinoN 坑:直连公网 PyPI 易超时)。虚拟环境 `.venv`,`requirements.txt` 钉死版本。

### 3.2 数据源

- **主源 TuShare 600 元档 = 6000 积分**(token 从 `.env` 的 `TUSHARE_TOKEN` 读;权限已于 2026-07-19 按官网权限对照表逐项确认,限频 **500 次/分钟**)。
  - **可用**:`daily`(日线)、`daily_basic`(换手 / 量比 / 市值 / PE-PB)、`adj_factor`(复权因子)、`index_daily`(指数日线)、`stock_basic`(代码→行业)、`namechange`(ST 状态历史)、`trade_cal`(交易日历)、`moneyflow_dc`(东财个股资金流,`net_amount` = 主力净额,万元)、**`top_list`(龙虎榜)**、**概念板块和成分**(板块层 / 板块年龄因子的数据源)、融资融券、财务三大报表、沪港通列表、**`stk_holdertrade`(股东增减持,2026-07-26 v1.3-③-C4 探活新确认,2000 积分档,`in_de`=IN增持/DE减持结构化字段,消息面扫描「减持」类数据源,见 §五 v1.3-③-C4)**。
  - **不可用(档位不够,方案已绕开)**:**`limit_list`(涨停榜单,15000 积分)→ 涨跌停全部自算**(见 0.4b 衍生表:涨停价 = `round(pre_close×(1+幅度),2)`,幅度按板块 10%/20%/ST 5%/北交所 30%,`close==涨停价` 判涨停、`high==涨停价且close<涨停价` 判炸板、连续涨停日计连板;新股上市首日无涨跌幅按 `stock_basic.list_date` 剔除);筹码分布 / 量化因子(8000 积分,不依赖);游资数据 / 个股及行业热板(15000 积分,不依赖);**新闻资讯为单独权限 1000 元/年,未购**(见 §8 决策项);**`anns_d`(公告接口)同样为单独权限 1000 元/年、未购**(2026-07-26 v1.3-③-C4 真实 token 探活确认:调用返回「无接口访问权限」,与新闻资讯是 TuShare 两个不同的独立付费产品——立案 / 暴雷 / 监管三类消息面扫描因此走 LLM 联网搜索兜底,不依赖 `anns_d`)。
  - **无分钟线。**
- **盘中实时源(免费)**:新浪 `hq.sinajs.cn`(主)/ 腾讯 `qt.gtimg.cn`(备)。仅供盘中哨兵,分钟级轮询。解析坑见 §3.7。
- **TuShare 调用姿势铁律**:`ts.pro_api(token)` **直传**,**禁用 `ts.set_token()`**(会往家目录写缓存,继承 LinoN 坑 5)。全市场批量接口只带 `trade_date`(不带 `ts_code`)一次返回全市场。限频:分批 + sleep(阶段 0 落地脚本按 TuShare 每分钟调用上限退避)。

### 3.3 存储(选型论证)

**规模估算**:A 股约 5400 只 × 2020-至今约 1600 交易日 ≈ **850 万行/表**;`daily` + `daily_basic` + `adj_factor` + `moneyflow_dc` 四张大表合计约 **3000–4000 万行** numeric;自算涨跌停衍生表(0.4b)只存涨停/炸板/连板行,约百万行;`index_daily` 极小。回测「喂历史」是**按交易日顺序迭代、每日取全市场横截面**的列式扫描负载。

**选型结论:Parquet(行情大表)+ SQLite(元数据 / 业务台账)混合。**

- **Parquet + polars(回测主力)**:`daily / daily_basic / adj_factor / index_daily / moneyflow_dc / limit_derived(自算)` 落 `data/parquet/<表>/year=YYYY/…`,**按年分区**。列存 + snappy 压缩后全量预计 **< 2 GB**;polars `scan_parquet` 惰性扫描 + 谓词下推,按 `trade_date` 切片近乎零拷贝,反复回测调参内存友好。→ **回测数据层用 polars lazy,选它就是为吃这个负载。**
- **SQLite(`data/neckline.db`)**:交易日历、股票 / 行业元数据、回测结果与净值曲线、盘后报告与候选、持仓审计、纪律台账、大脑版本与变更日志、问询会话。这些要事务、点查、关系查询,且与 LinoN 工程一致、后期客户端好接。
- **pandas 仅用于边缘衔接**:TuShare 返回的是 pandas DataFrame,落地时用 pandas 承接后转 polars / 写 Parquet;回测核心不碰 pandas。
- **为何不全 SQLite**:千万行横截面反复全表扫描,行存聚合慢、库文件臃肿。**为何不全 Parquet**:业务台账要事务与点更新,Parquet 不擅长。故混合,各取所长。

### 3.4 LLM

> ⚠ **V2.0.0 起本节的「GLM/Kimi 枚举」被取代为 Provider 自填制 + 双 Agent 分工**(裁定 #2),见 **§3.10-B**;降级链 / 短读超时重试 / 带搜索 90s 读超时 / `MockTransport` 免联网单测等**工程姿势一字不变**。以下原文保留作历史拍板留痕。

- **多供应商可切换(2026-07-19 用户拍板,不用 DeepSeek)**:候选 **GLM 5.2 / Kimi K3**(两家 API 均原生带联网搜索工具,正好覆盖 2.4 消息面方案)。设计约束:① `llm/` 层做**供应商抽象**(统一 chat + 工具调用 + 搜索接口,provider 实现可插拔);② key 与 provider 选择从配置读(研究期 `.env`:`LLM_PROVIDER` / `LLM_API_KEY`;**阶段 4 客户端里必须有「填 API key + 切换供应商」的设置入口**,运行时可切不重启)。**阶段 4 拍板(2026-07-20)**:App 可改 key/provider → 走 `PUT /settings/llm` 落服务端 `app_settings` DB 单行(**高危区**:key 服务端存取,DB 文件 600、gitignored、rsync 永不同步覆盖);`get_provider()` 解析优先级改为 **DB 覆盖 → `.env` 兜底**,每次调用现读故运行时生效;③ key 用户后续自填,**缺 key 时全链路优雅降级不崩**(继承 LinoN 降级链思想)。
- 继承 LinoN 的**降级链**(缺 key / 超时 / 非 200 / 非法 JSON → 优雅降级占位,全链路不崩)与**短读超时 + 每次全新连接重试**(`_READ_TIMEOUT=12` / `_CONNECT_TIMEOUT=6` / `_MAX_ATTEMPTS=3`,治 EdgeOne CDN 偶发单连接卡死)。
- **对话工程**:问询台的工具调用(实时取数 / 重算)沿用 LinoN v1.2.1 `/chat` 的姿势重写。

### 3.5 客户端载体(**已拍板 = 方案 A**,2026-07-20)

阶段 0–3 **完全不需要客户端**,CLI + 本地文件(markdown / HTML 报告)即可跑通全部研究与报告逻辑。载体决策推迟到阶段 4 前:

| 选项 | 优点 | 代价 |
|---|---|---|
| A. 复用 LinoN SwiftUI 双端 + APNs | 工程资产在、锁屏推送体验最好 | 最重;要维护 Xcode / 签名 / 真机 |
| B. Bark(推送)+ 轻量 Web(报告 / 问询台) | 最省;盘中哨兵推送即插即用,报告 / 问询台用浏览器看 | 推送样式简单,无原生交互 |
| C. 纯 Web(FastAPI + 简单前端) | 单端维护、跨设备 | 盘中推送需另配通道(可 + Bark) |

~~**推荐 B**:盘中哨兵推送走 Bark + 报告/问询台走轻量 Web。~~ → **已否决**。

> **⚠ 客户端载体拍板(2026-07-20 用户定案 = 方案 A)**:复用 LinoN 的 SwiftUI **iOS + macOS 双端**工程底子 + **APNs 推送**;**新 App、新 Bundle ID `top.linotsai.neckline`**(不改 LinoN 旧 App)。上表 A/B/C 保留作决策留痕。选 A 理由:锁屏推送体验最好、LinoN 工程资产(xcodegen 双端 / DesignTokens / APIClient / PushManager / 平台分叉)可整块搬、绿涨红跌沿用。**信息架构全新**(iPhone 四板块 = 今日计划 / 盘中看板 / 问询台 / 设置;macOS 四板块 + 周复盘工作台),非照搬 LinoN 今日台。复用清单与坑吸收见 §五 阶段 4C。

配色偏好(阶段 4 落地时遵循):用户沿用 LinoN 的**绿涨红跌**(与 A 股本地相反,是用户明确选择,勿「纠正」)。

### 3.6 部署

> ⚠ **V2.0.0 起本节被取代**:hz 单机(1.6G 内存硬约束)→ **100% 更换更强云服务器 + 晚间管线多段化**(裁定 #3),见 **§3.10-D** 与 §五 V2-⑯。以下原文保留作历史拍板与老机事实留痕(老机在用户完成换包前仍在跑)。

- **研究期(阶段 0–3)本地跑。**
- ~~盘中哨兵上不上 ECS = 后期决策项。~~ → **已拍板(2026-07-20)= 上 hz ECS,复用 LinoN 基建**。

> **⚠ 部署拍板(2026-07-20 用户,阶段 4 落地)**:后端 + 哨兵跑 **hz 杭州云 ECS**(`deploy@118.178.122.194`),照搬 LinoN 的 **FastAPI + systemd + nginx 反代**模式。复用 LinoN 资产:nginx 站点 + 证书 **`ln.linotsai.top`**(证书 ECDSA 到 2026-09-20 cron 自动续期健康,DNS 已在,**零新增用户网页操作**;域名 `ln` 语义留痕于 LinoN 是可接受的表面不符,不值得为改名新开子域 + 新证书——后者还要碰本机 certbot 已知问题,见 `hz_info.md §13`)、APNs `.p8`(Key ID `Q963AP3VY8` / Team `HX73DFL88G`,**账号级密钥可直接给新 Bundle ID 用**,APNs topic 换成新 Bundle ID 即可)。
>
> **端口/账户/目录**:Neckline 用**独立端口 8002**(⚠ 拍板文字说的「8001 空闲」已过时——`hz_info.md` 2026-07-20 记 LinoN 07-20 晨已恢复运行、仍占 8001 作记录工具,**8001 未空闲**;Neckline 用 8002 与 LinoN 共存,不抢端口)、独立 nologin 用户 `neckline`、独立目录 `/opt/neckline`(`deploy:neckline` setgid 2770,`.env`/`.p8`/`neckline.db` 600 `neckline:neckline`)。**接班切换(阶段 4 部署上线并联调通过后)**:nginx `ln.linotsai.top` upstream 由 `127.0.0.1:8001` 切到 `:8002` → `linon.service` stop + disable(LinoN 退役,兑现 `hz_info.md` 决策)→ 视需归档 `linon.db` / 清理旧站点。
>
> **内存硬约束(hz 仅 1.6G + 2G swap,还跑着 lf / fiscal / 主页 / postgres,v1.4.1 实测 used ~853M / available ~759M)是本阶段第一约束**。polars 全市场重活(backfill / 报告生成)能否在 ECS 上跑,**必须实测门禁定生死**(见 §五 阶段 4B),两套架构方案都写清,以实测定,回退路径 = Mac 定时落数 + 报告生成 → rsync 产物上云,ECS 只管 API + 哨兵轮询(轻量,关注池 ≤200 只)+ APNs。**哨兵不另起进程**——折进 FastAPI 单 unit 的 lifespan asyncio 任务(照搬 LinoN 单 unit 省内存),`scripts/sentinel.py` 独立脚本留本地。

### 3.7 继承资产(从 LinoN 搬什么,搬前必读其 CLAUDE.md 对应节)

代码可搬可重写,由 build 定;但**下列已踩平的坑必须吸收**(权威原文见 `/Users/linotsai/Lino/LinoN/CLAUDE.md`):

- **`tushare_client`**(LinoN `backend/app/data/tushare_client.py`):`TushareResult{ok,data,reason}` 永不抛异常的封装;`pro_api(token)` 直传;字段单位表——`daily.amount`=千元、`daily.vol`=手;`daily_basic.total_mv`=万元;`moneyflow_dc.net_amount`=万元(= 超大单 + 大单,东财主力口径,与用户同花顺一致)。**Neckline 需新增全市场批量拉取 + 落 Parquet。**
- **交易日历**(LinoN `app/calendar/`):静态兜底表(官方休市日 + 调休补班周末股市仍休)+ `trade_cal` 比对告警。**Neckline 需把日历从 2020 起补全**(回测六年)。注意包名勿与标准库 `calendar` 冲突(LinoN 坑:包内绝对导入)。
- **实时源解析**(LinoN `app/data/realtime.py`):新浪**必带 `Referer: https://finance.sina.com.cn`**(否则 `Kinsoku jikou desu!` 无数据)、GBK 解码、volume 股→手;腾讯 GBK、volume=手、amount=万元、bid/ask「价先量后」(**与新浪相反,易写反**)。归一目标:`volume` 单位手、`amount` 单位元。
- **盘中 VWAP 纯函数**(LinoN `app/data/intraday.py`):`VWAP = amount/(volume×100)`(元/股),不自拉价、不联网。
- **LLM 层工程姿势**(LinoN `app/llm/deepseek.py`,供应商换了但姿势保留):降级链 + 短读超时重试 + `MockTransport` 可注入免联网单测。
- **黑名单口径**(LinoN 教训):按**板块整段正则** `^(30|688|689|8|4|920)` 挡创业板 / 科创 / 北交所,**勿枚举精确子段**(会漏新增子段);白酒用 `stock_basic.industry` 精确归类,非名称关键词。

### 3.8 铁律(全项目硬约束,build 逐条守)

- **token / key 绝不入 tracked 文件**:一律从 `.env` 读;`.env` 已在 `.gitignore`。**scratchpad 里的归因脚本 `entry_profile.py` 硬编了一个 TuShare token —— 严禁复制进仓库,重写时一律从 `.env` 读。**
- **系统永不自动下单 / 撤单 / 改止损**;一切成交由人 + 券商执行。
- **同码 = 纪律与回测同码,情报筛选独立(v1.3 重述,取代旧「同码三跑道」)**:旧表述「回测/报告/问询共用同一套信号代码」在 v1.3 需求 5(K1 entry mask 退役)后与候选生成直接冲突,故拆成两条**同时成立**的表述——
  - **(a) 纪律 / 退出规则的参数单一源 + 回测与实盘同码(不变,是「减损纪律系统」的地基)**:止损 / 回落止盈 / hold 两档 / 仓位 / 熔断阈值各有唯一源;退出规则的模拟(回测引擎 `strategy/momentum.py` + `backtest/`)与实盘哨兵读同一现役 config,**回测口径 = 实盘口径**,不得各写一份(否则报告与回测漂移)。**问询台 / 自选体检的纪律判定仍与报告同码**(选股域 `neckline/research/panel.py::base_universe_expr`〔⚠ **是包内那份,没随策略档案迁出**,见 §1.6〕 + config 禁买过滤,同一个 `report/watchlist_check.py::discipline_checks`,见 §五 v1.3-⑤)。**⚠ v1.3.3 起「同码」只到判定为止,消费方式两处刻意不同**:自选体检把命中项当**红灯**(`green_light=False`),问询台把同一批命中项当**警告标注**(提示不拦,§2.5 自由分析师)——**同一份判定、两种展示**,是刻意取舍,不是漂移;禁止任何一方另起一份阈值。
  - **(b) 候选生成自 v1.3 起改为「情报筛选管线」,与回测信号解耦**:候选生成(拥挤度 → 卫生线 → K4 安检 → 情报排序)**不再套 K1 entry mask、不再声称是回测过的 alpha 信号**——它输出「值得关注的票」而非「会涨的票」,终选权在用户(三场战役判死「机器自动出信号」)。**故不要求(也不可能)与回测同码**;回测引擎仍是纪律参数的过堂载体与「减损系统」验证地基,与情报管线解耦。落地 §五 v1.3-③-C3。**⚠ V2.0.0 附注**:情报筛选管线自 V2 起由**篮子引擎**(扫描 → 聚合 → Tier → 卡)取代,可配部分改读**现役策略包**;本条 (a)(b) 的**分工原则一字不变** —— 纪律与回测仍同码同参数源,选股链仍与回测信号解耦、仍输出「值得关注」而非「会涨」。见 §2.8-A 与 §五 V2-③④⑤⑥。
- **规则常量单一源**:-5.0 止损、仓位上限、各阈值只在一处定义,禁止漂移(继承 LinoN 教训)。
- **无前视偏差**:回测第 T 日任何计算都不得读到 > T 的数据;数据访问层强制截断 + 单测守护。
- **LLM 一律自由对话体**,禁模板卡。

### 3.9 建议仓库布局(build 可微调,大方向照此)

```
Neckline/
  .env                      # gitignored,TUSHARE_TOKEN / LLM_PROVIDER / LLM_API_KEY
  .gitignore                # 已含 data/*.db、data/parquet/
  PROJECT_PLAN.md           # 本文件,唯一权威
  requirements.txt
  neckline/                 # 主包
    config/                 # 读 .env、路径常量
    data/                   # tushare_client / realtime / intraday / 数据访问层(polars)
    calendar/               # 交易日历(勿撞标准库名)
    backtest/               # 引擎、撮合、组合、walk-forward
    strategy/               # 母战法信号(同码三跑道的源)、大脑版本
    report/                 # 盘后报告管线(情绪仪表盘 / 板块 / 候选评分)
    sentinel/               # 盘中四哨兵
    llm/                    # 供应商抽象(GLM/Kimi 可插拔)/ 深判 / 问询台对话与工具
    review/                 # 周复盘 / 违纪对账
  scripts/                  # 落地脚本、每日增量、回测 CLI、报告 CLI
  data/                     # Parquet + SQLite(gitignored)
  archive/                  # 历史 plan 全文、超长记录
  tests/
```

### 3.10 V2.0.0 技术选型增补(2026-08-02 立项;在此定死,不留给 build 猜)

> 只写 V1 之上的**增量与变更**;未提及的一律沿用 §3.1~§3.9。

**A. 存储与数据层**
- 存储路线**不变**(Parquet + polars 跑行情大表 / SQLite 跑元数据与业务台账)。V2 新增 **18 张 SQLite 表 + 2 张 parquet 表**,清单与 DDL 要点在 §五 V2-① 定死。
- **`intraday_ticks` / `auction_snapshots` 走 parquet**(按日分区,`write_table_day` 铁律),不进 SQLite:它们是「只增不改的高频事实」,量级 ≈ 关注池 200 只 × 240 分钟/日,列存 + 按日分区最合身。**必须同步进 `_VALID_TABLES` + `TABLE_FLOAT_COLS` 声明**。
- **盘中存拍落盘时机 = 内存累计 + 15:05 一次性落盘**(§五 **D4 已拍板**);理由:`write_table_day` 的语义是「一日一文件整段替换」,逐分钟 append 会反复重写整日文件并拖累哨兵主循环。
- **分时数据双轨**(裁定 #1):自给存拍从 V2 上线日起积累;用户外源方案落定后**走同一张表回补**(`source` 列区分),复盘引擎无感切换。

**B. LLM 层**
- ~~多供应商可切换 = GLM / Kimi **枚举**(§3.4)~~ → **取代:Provider 自填制**。`llm_providers` 注册表(name / base_url / model / api_key / `has_web_search` / `search_engine` / enabled),**任意 OpenAI 兼容端点可配**,App 设置屏增删改;`openai_compat` 仍是唯一传输底座;GLM / Kimi 两个类降级为**预置模板行**。
- **任务 → Provider 路由表**(`app_settings.llm_task_routes` JSON),缺路由回退 `llm_default_provider`。**默认双 Agent 分工**(裁定 #2):**GLM = 检索型任务**(联网检索目前只有它这一条通道)、**DeepSeek = 推理型任务**;两段式流水(检索产物带来源+日期 → 喂推理),**单侧故障单侧降级并诚实披露**。
  ⚠ **2026-08-05 用户拍板修订**:生产以**单 LLM(GLM 一家兼任检索与推理)为常态**,裁定 #2 的双 Agent 分工**降级为可选扩展路径** —— 路由表机制、两段式流水、三本账、诚实披露**一律不变**(推理类任务走「路由未命中 → 回退 `llm_default_provider`」这条既有路径),接入第二家 Provider 即恢复分工,**不需要改代码**。
- **预算三本账**(检索 / 推理 / 复盘)独立不合并;**降级次序定死**:先丢 T3 简评 → 再丢 T2 复盘细节 → **绝不牺牲篮子卡冻结与纪律外壳**。
- **`prompt_context`(日期锚 + 时效纪律 + 检索词年份引导)对所有 Provider 一律注入**,唯一实现不许有第二份。
- ⚠ **`has_web_search=0` 的 provider 一律不发 `tools` / `search_query`**(不给上游不认识的参数 —— v1.3.4 案底:会 `ok=True` 静默返 0 条)。

**C. 选股引擎与策略包**
- 新包 `neckline/selection/`(`engine_api.py` / `primitives.py` / `pack.py` / `aggregate.py`)+ `neckline/scan/`(三张预计算表)+ `neckline/eval/`(评价与判分)+ `neckline/profile/`(画像)。
- **策略包 = 声明式配置包,不是代码插件**(§12.1 定案,勿重开):包里只装参数与规则声明,执行引擎永远住系统线仓库。原语白名单制,**特征只能来自预计算表**;`engine_api_version` 兼容不过**拒绝激活、fail loud**。
- **判分引擎唯一源下沉**:`research/h9_exit_reform.py::_sim_one` + `SLIP` / `BROKER` 常量搬进 `neckline/eval/exit_sim.py`,research 三处改 import 它(**生产不许 import `research/`**,方向恒为 research → neckline),搬迁前后**逐位对拍单测**锁死。**⚠ 2026-08-09 附注**:下沉**已完成且成果留在本仓**(`neckline/eval/exit_sim.py` 是唯一源,周复盘 / 画像 / 校准 / 信息卡都在吃它);被迁走的只是 `research/` 那一侧的调用方(→ §1.6「档案」),故「research 三处 import 同一函数对象」那条守门自此**常态 skip**、⛔ **不许删**(删了等于承诺研究件永不回来)。方向纪律(research → neckline,反向禁止)**一字不变**。

**D. 部署与运行环境(取代 §3.6 的 hz 单机方案)**
- ~~后端 + 哨兵跑 hz 杭州云 ECS(1.6G 内存硬约束)~~ → **取代:100% 更换更强云服务器**(裁定 #3,部署前置项)。建议规格 ≥ 4 vCPU / 8 G / 100 G SSD(§五 D3,用户可推翻);属主纪律、`.env` / `.p8` / `neckline.db` 600、`sync_code.sh` / `sync_data.sh` 体例**原样复刻**。
- **晚间管线多段化**:扫描 / 聚合+Tier+卡+报告 / 复盘+评价 拆成**三个 oneshot 服务串行**、各自 `MemoryMax`;**重活一律不进常驻 `neckline.service`**(它与盘中哨兵同进程,`MemoryHigh` 先节流 = 卡死不报错)。
- **域名与割接(§五 D2 = A 路,2026-08-02 用户拍板定死)**:新机挂新子域 **`nk.linotsai.top`**(**DNS 解析由用户自己做**),老机 `ln.linotsai.top` 原样服务到用户完成 ⑰ 双端换包 → **老 App 打老机、新 App 打新机不交叉,契约一次性换血、不留过渡键**。~~沿用老域名的 B 路~~ **已否决,不是选项**(§五 ⑭-D)。
- **P0-23 纪律平移**:所有新批算必须 **EOD 预计算落表、在线只读**,且**在新机上单独计时 + 量峰值后才准进主链**;探针只在收盘后隔离串行跑,`load > 4` 立即停手。

### 3.11 V2.2.0 技术选型增补(2026-08-09 立项;在此定死,不留给 build 猜)

> 只写 V2 / V2.1 之上的**增量与变更**;未提及的一律沿用 §3.1~§3.10。逐块落地在 §五 V2.2.0。

**A. 版本线载体 —— 复用 `selection_packs`,⛔ 不新建第二套注册表**
- K8 的「骨架 V + 引擎 C/Z/Y」四条线**全部住 `selection_packs`**,靠新增两列 `line_code` / `status` 区分;四道闸(`scripts/activate_pack.py`)、append-only 事件流(`selection_pack_activation_log`)、按版本归因分层**四套机关原样平移**。
- **理由(定死,⛔ 不重开)**:① 新建第二张表 = 第三套激活流程,与「两条版本线不许混用」这条纪律直接打架(会变成三套,后人必混);② 两个历史包留在同一张表里,查证与留档路径不破;③ 归因分层键只是从 1 个字段扩到 3 个字段,不需要换载体。
- **唯一现役约束改口径**:`UNIQUE(is_active) WHERE is_active=1` → **`UNIQUE(line_code, is_active) WHERE is_active=1`**(DROP + 重建,仍走既有 `try/except IntegrityError` 体例)。
- **`ENGINE_API_VERSION` 本版 `1 → 2`**(判定依据与代价见 §五 ①):**两个回滚锚随之作废,回滚改走「代码 commit + DB `.bak` 还原」**;⛔ 全项目自此不许再出现「回滚 = 激活旧包」的写法。

**B. 两张新的全市场级预计算表(P0-23 正面靶心)**
- `landing_metrics_daily`(**逐票 × 60 日回看**,🔴 **裁定 #11 后只落原始读数、⛔ 无四态无阈值**;原名 `landing_state_daily` 已作废,该表从未上产)与 `market_regime_daily`(逐日三态)**一律 EOD 预计算落表、在线只读**;取数走 `market_data._scan_table(years=…)` **年分区裁剪**(P1-26 既有修法),⛔ 不全 glob。
- **上生产前必须在生产机上 `systemd-run --scope` 隔离实测计时 + 量峰值**(收盘后 15:00 之后、避开 16:00–17:00、串行、`load > 4` 立即停手),达标才准进主链、才改 `neckline-scan.service` 的配额。⛔ 别拿常驻 `neckline.service` 当小白鼠。
- 两张表都进 SQLite(不是 parquet):它们是**有主键、要点查、要按日回放**的判据表,与 `industry_strength_daily` / `industry_stage_daily` 同族同处置。

**C. LLM 层 —— 引擎并跑不增加调用**
- **⛔ 禁止「每引擎跑一遍 LLM」**:引擎归属由既有 `basket_reason` **那一次**调用一并给出(prompt 带三引擎定性边界,输出每票一个 `engine_code`),再经机械闸对拍;证据关 / 驱动关 / 核心关的 LLM 侧产出**同样复用那一次的结构化输出**,新增字段而非新增调用。
- **预算三本账、降级次序、流式 / 非流式分工、`prompt_context` 注入**一律不变;`SEARCH/REASON/REVIEW_BUDGET_SECONDS` 与 `neckline-basket.service::TimeoutStartSec=5400` **一字不动**(调用数上界一个没涨,算术原样成立)。

**D. 阈值治理 —— `provenance` 强制字段**
- 引擎包里每个阈值必须二选一自报来源:`{"source":"audited","ref":"档案/research/k7_pre_report.md#H10"}`(路径口径见 §1.6) 或 `{"source":"engineering_v1","basis":"从 K8 §x 哪句翻译来的","calibration":"pending"}`;**`activate_pack.py` 闸 1 校验缺失即拒绝激活**。
- 校准路径 = 选股时钟攒样本 → 周度按引擎归因 → 四分类建议 → **用户确认** → 新引擎版本(`C2`/`Z2`/`Y2`)走四道闸。⛔ **零自动回写**。

**E. 章程侧唯一一处类型放宽**
- `MomentumConfig.max_hold_days: int` → **`Optional[int]`**,`None` = **不设时间退出**(与既有 `stop_pct: Optional[float]` 的 `None` 语义同构)。⛔ **否决用哨兵位 `9999`**(哨兵位是"看不出来"的病)。放宽后必须跑 K1 六年回测**逐位不变**护栏。

---

## 四、当前状态

**2026-08-10(当日第三次交接)· 🎨 V2.3.1 保真度返工 **批 0 / 批 1 / 批 2 完工**,⛔ 未部署 / 未换包**。
**Python 全量 3581 passed / 3 skipped / 0 failed** · **iOS 214 tests / 0 failures** · **双端 BUILD SUCCEEDED**。

> ⚠ **V2.3.0 已被用户当场打回**(「完全达不到参考文件的要求」)—— 它的结构 / 信息架构 / 诚实披露
> 文案是资产,缺的是**视觉保真度**。现行口径 = **§五 V2.3.1**(⛔ 别再照 V2.3.0 那一节施工)。
>
> **V2.3.1 进度**:批 0 验收装置 ✅ · 批 1 全局钉子 ✅ · **批 2 macOS 选股板块 ✅** ·
> **批 3(持仓)/ 批 4(复盘 + 对账)/ 批 5(设置 + 弹层 + 信息卡)/ 批 6(两个全屏状态)/
> 批 7(iOS 八屏)/ 批 8(收尾 · 版号 · 部署换包)未开工**。⛔ **版号仍是 `2.3.0`,归批 8 统一升。**
>
> **批 2 交付**:macOS 选股板块五屏 + 报告未生成态逐项对到原型 inline style(列表栏两套页边距 /
> `pick()` 选中态白底 + 1.5px 蓝描边 / 六关宫格居中三行 radius 9 +「硬」角标 radius 3 / 成员卡改
> 「卡里的卡」/ ①②③ 并成一张卡 / ① ⑤ 情报三处段名进卡头 / 昨日回执**新增详情屏** /
> 报告未生成态换横向排程时间轴)。**新增两枚展示层换算与一个布局原语**:`nkRegimeDimLabel`(五维
> 缺维印英文码,硬伤 2 的第七处)· `NKGroupHeader` · `NKWrapRow`(`flex-wrap` 等价物)。
> **实拍六张**在本次会话 scratchpad(`b2_0{1..6}_*.png`),对照结论见 §五 ③ 完工段。

- **生产侧此刻仍是 V2.2.0,与今早上产后逐位相同 —— 本次一行代码都没上产**。nk 对外版号仍
  `v2.2.0`(仓库里 `app.py::VERSION` 已改 `v2.3.0`,**没部署**);macOS 客户端仍是今早换的 **2.2.0**;
  现役四线 `V:K8-V0.5 / C:C1 / Z:Z1 / Y:Y1`;章程 **`v2.2-k8` 已激活**(今早 09:33 盘中,闸 2 自然放行);
  熔断已整体退役;生产 SQLite 52 表。
- **🔴 今日 15:05–15:55 那两件仍然必须办**(今早部署时被迫后移,**与 V2.3 无关、别因为交接就漏掉**):
  ① P0-23/P4-50 `landing_metrics_daily` 生产隔离实测;② `neckline-weekly.service` 配额占位值校准。
  **必须赶在 16:35 晚间链之前** —— 不达标还来得及关掉对应段。

**上一版动作(仍然有效的地基)= V2.3.0 六批全部落地(只改呈现,一个判定 / 阈值 / 诚实披露文案都没动)**

- 交接包 `Neckline视觉升级/`(规范 + 六份原型 + 图标)已全部落进 `client/**`:字阶收成**八档 + 两数字档**、
  macOS **去侧栏改 50px 工具栏 + 列表栏 376 / 详情栏自适应**、五个新组件(披露折叠 / 止损刻度尺 /
  六关灯条与宫格 / 成员卡 / 工具栏)、原始件下沉审计视图、篮子卡由 sheet 改详情栏、App 图标换红橙。
  **455 个裸 `.system(size:)` → 60**(余下全是图标尺寸 + 令牌定义)。**逐批细则见 §五,别在本节找。**
- **唯二碰后端处**(用户当日拍板):**版号三处同升 `2.3.0`** · **`PositionsOut.circuit` 真删键 + 客户端删
  `CircuitState`/`CircuitEpisode`**(两步淘汰第二步)。⚠ **删键零风险的判据是逐版核实过的**:历代客户端
  `/positions` 一律解进 `PositionsListResponse { holdings }`,**从没有一版声明过 `circuit`**(2.0.0 那台
  iPhone 读的是独立端点 `GET /circuit`,自 V2.2 起 404,与本键无关)。**⛔ 别把它读成「零删键铁律可以不守」。**
- **§七 P3-47 在本版结案**(复盘每日页日期选择器,复用 `GET /report?date=`,读冻结快照)。

**🔴 施工期核实出来的四处「mock 画了、契约支撑不了」(规范 §08 只列了两处),全部按「补支撑或诚实降级」处置**

① 券商条件单勾选 → **补支撑**(UserDefaults,⛔ 不写服务端)· ② 批算进度 → **降级成排程表**并当面标「非实时状态」·
③ 🆕 **六关「判不出」是篮级不是格级**(冻结卡没有逐关 `available`)· ④ 🆕 **「占总仓 %」的分母不在契约里**
(`Settings.total_capital` 从未下发)→ 持仓页只显示金额。**全文与理由见 §五 〇b。**

**⚠ 下一步(交用户决定)**

1. **服务端两处改动 + 双端换包必须同批上**(版号三处恒等有守门单测锁着,分批上会让版本号长期打架)。
2. **macOS 三区布局尚无视觉核对** —— 用户正式 App 当时正在运行(同 bundle id),启动 Debug 构建只会切到那个
   还在跑的 2.2.0、截出来是旧版;按 CLAUDE.md 兜底走了「iOS 截图 + 双端 BUILD SUCCEEDED」。**换包后请自行过目**。
3. iPhone 那台仍需用户自己装(§八 第 20 项);装上 2.3.0 后,2.0.0 遗留的两处错话一并消失。

**回滚绳**:本版**没上产,不需要回滚绳** —— 生产仍是今早那份 V2.2.0(其回滚绳见 §九 当日条:
双备份 `preV22b2345-20260810-085832` + macOS 旧包 `~/Lino/app_backups/Neckline-2.1.0-20260810-090427.app`)。
仓库侧回退 = `git` 回到本次提交之前。

**待办总入口 = §七 Backlog**;K8 需求原件 = `K8_STRATEGY_ARCH.md`;策略研究档案已迁出本仓(口径见 §1.6);
工程坑清单在 `CLAUDE.md`。⚠ **K8 的假设零回测背书**,前向证伪义务见 §七 **P3-49**(⛔ 结案前不许称"有效")。

> 📁 **本节自 2026-07-28 起为快照制**:每次会话交接**替换**本节全文,不追加;历史价值内容归 §九 一行 + `archive/` 详版。v1.0 → v1.3.5 的历史交接账本 → `archive/当前状态_历史账本_20260719-20260728.md`;v1.4 → v1.5.2 的收官快照 → `archive/v1.5_施工图_20260802归档.md` 文末附录;**V2.0.0 上线初期(2026-08-04 割接 → 08-05 初始化)的快照 → `archive/V2.0.0_施工图_20260805归档.md` 文末附录**;**V2.0.0 常态运行快照(2026-08-05 收官 → 08-08 V2.1 批 1 上产前)→ §九 2026-08-05/08-07/08-08 各条**;**V2.2.0 上产快照(2026-08-10 上午)→ §九 当日条**。

---

## 五、V2.3.1 施工图(2026-08-10 立项 · 前端视觉**保真度**返工)

> **一句话**:V2.3.0 把房子按新图纸重盖了 —— **结构、信息架构、诚实披露文案全部是对的**,
> 但**没有拿六份原型逐屏做像素级比对**,只照着规范文档的文字核对了一遍。用户拿实机截图与
> 原型并排一看:「**完全达不到参考文件的要求**」「顶上那个红绿灯一栏是多少版本前的设计了」
> 「icon 也没换」「反正就是离参考文件都很远」「**我要的和参考文件一致的前端**」。
> **V2.3.1 的唯一目标 = 补上"逐屏比对"这件事**:每一屏都跟原型的 inline style 对到数,
> 差异要么改齐、要么写明理由。
>
> ⛔ **不推翻重来**:V2.3.0 的文件布局、组件划分、诚实披露文案**是资产**。本版是在它上面做
> **保真度收敛**,不是第二次重写 —— 凡是"顺手重构一下"的冲动都按越界处理。
>
> **来源 = 唯一验收基准**:`Neckline视觉升级/` 的六份 `.dc.html` 原型(规范 §02–§07 只是
> 索引,**对不上时以原型的 inline style 为准**)。
> **边界**:**只动 `client/**`**;⛔ 不碰 `neckline/**`、`scripts/**`、`packs/**`、`deploy/**`
> —— **唯一例外 = 版号那一行**(`app.py::VERSION`,见 ⑧)。
> **版号 `v2.3.1`,三处同升**(`app.py::VERSION` + `client/project.yml` + `pbxproj` app target);
> 守门单测 `tests/test_client_version_governance.py` 锁三处恒等。

### 〇、⛔ 不得重开的四条(继承 V2.3.0,施工时一条都不许"顺手修回原型的样子")

| # | 结论 | 为什么本版仍然成立 |
|---|---|---|
| 1 | **券商 -5% 条件单勾选走 UserDefaults**(`NKStopOrderLedger`,按 `positionId` + 勾选时刻) | 它记的是「我在券商那边做了什么」,不是系统能核实的事实。⛔ 不写服务端 |
| 2 | **报告未生成时只画排程表**(timer 固定时刻)+ 当面标「排程表 · 非实时状态」 | 要实时进度得先有一个不依赖报告快照的端点,本版**不碰后端** |
| 3 | **六关宫格「判不出」是篮级不是格级**:格级只画 pass/degrade/reject,缺键 → 「未记录」灰格 | 冻结卡 `mech_breakdown_json.gates` 没有逐关 `available`(服务端只落 `verdicts` + **篮级** `blocks_t1`)。⛔ 绝不渲染成「过」,也⛔ 不猜是哪一格 |
| 4 | **持仓「占总仓 %」只显示金额、不显示占比** | 分母唯一源 `Settings.total_capital` **从未下发**。⚠ **原型里画了**(macOS 原型 868 行「61.2% 占总仓」、1028 行刻度尺第四格「占总仓 35.3%」)—— 🔴 **那两处是"原型画了、契约支撑不了",⛔ 不许照着补上去**,在客户端写死 12 万 = 给一个钉死的领域常量造第二份事实源 |

> 🔴 这张表是本版最容易被"逐屏比对"这个任务本身带翻的地方:**比对表里遇到这四处,判定一律
> 写「刻意不同 · 见 §五 〇」,⛔ 不许改齐**。

### 〇a、方法论(这是上一轮翻车的根因,判据写死在这里)

**A. 判据来源 = HTML 源码,不是剥出来的文本**

- ⛔ **不许只用 `dcextract.py` 那类纯文本剥离**当判据 —— 字号 / 颜色 / 间距 / 圆角 / 布局
  **全在 inline style 里**,剥文本会把它们**全部丢掉**。上一轮"照着规范文档的文字核对"翻车,
  就是这个病的同款。剥文本只用来**快速定位是哪一屏**。
- **判据在两处,漏掉第二处等于漏掉全部选中态**:
  ① 各元素的 `style="…"` inline 值;
  ② **文件尾部 `<script type="text/x-dc">` 里的样式函数**(macOS 原型 1754 行起):
  `pick()` = 列表行(常态 / **选中 = 白底 + `0 0 0 1.5px #0B6BCB` 描边 + `0 1px 3px rgba(0,0,0,.06)`**)、
  `nav()` / `navText()` / `navIcon()` = 板块胶囊(**选中图标是 `#0B6BCB` 蓝、未选 `rgba(60,60,67,.45)`**)、
  `gearBtn()` = 齿轮(**28×28 / radius 7 / 选中有白底**)、`memCard()` = 成员卡(**radius 10 / 选中
  `#FAFAFC` + `inset 0 0 0 1px rgba(11,107,203,.28)`**)、`memRow()`。

**B. 每一屏必须产出「对照表」,批次验收看表不看感觉**

三列:**原型值(带文件 + 行号)| 落地值(带 `.swift` 文件 + 行号)| 判定**。
判定只有三种合法取值:

| 判定 | 含义 | 合法性 |
|---|---|---|
| `已对齐` | 本来就一致 | — |
| `已改齐` | 本批改成一致 | — |
| `刻意不同` | **必须写一句理由**,且理由只能是三类之一 | ① 契约支撑不了(引 §五 〇 那四条)· ② 平台差异(SwiftUI 无等价物,如 CSS `box-shadow` 的 spread、`repeating-linear-gradient` 斜纹)· ③ 规范 §03/§04 明确要求向令牌就近对齐(如原型 12.5 / 11.5 → `callout 12` / `caption 11`,**规范原文:「mock 里还有少量字号漂移,落地时一律向下表就近对齐」**) |

⛔ **除这三类之外的一切理由都不成立** —— 尤其⛔ 不许写「视觉上差不多」「实机看着更好」。

**C. 🔴「版式没做到」与「数据碰巧不同」必须分开**

上一轮实机跑的是 08-07 的旧报告(当时无 K8 六关数据、`regimeReason` 是机器串
`rot.old_5d=0.1155<0:fail;…`),而原型用的是理想化 mock。

- ⛔ **不许把数据差异当成版式缺陷去"修"** —— 那会修出假数据(在客户端补一个服务端没给的值,
  就是造假)。
- **正确做法**:先用**演示库**把每一屏喂到与原型**同构**的数据状态(有六关、有成员、有 LLM 理由、
  有刻度尺四刻度、有降级项),**再**比版式。
- **对照表里只允许出现版式行**(字号 / 颜色 / 间距 / 圆角 / 排布 / 层级)。**数据行不进表** ——
  「原型写 74.2 / 实机写 68.1」不是缺陷,不用记。

**D. 演示库口径**(照 `CLAUDE.md`「V2.3 前端视觉整改新增坑」末条)

- `DB_PATH=<临时库>` 起隔离后端,`API_TOKEN` **必须 len ≥ 16**(否则 lifespan fail-fast);
  临时库只从真库 `ATTACH` 拷 `trade_cal` / `strategy_versions` / `stock_basic` / `namechange`
  四张只读参考表,业务表手写假数据。🔴 **⛔ 全程不碰 `data/neckline.db`**。
- 本次立项会话的演示种子在 `…/scratchpad/seed_demo.py`(同 session 可直接复用);
  **不在就按上面重建,⛔ 不要退回"拿 08-07 旧报告将就"**——那正是上一轮把数据差异和版式差异
  搅在一起的原因。
- 内容纵向溢出时**把演示卡前面那几段砍短**(演示库改一行),⛔ 不改 `client/**` 去迁就截图。

### 〇b、🔴 macOS 视觉核对路径(本版**选定 ①**:独立 bundle id 的 dev 变体 + 窗口定向截图)

**macOS 是主平台,而上一轮 macOS 一张截图都没有。本版不许再跳过。** 立项时已把可行性实测完:

| 实测项 | 结果 | 说明 |
|---|---|---|
| `screencapture` 能不能跑 | ✅ 能(exit 0,产出真位图) | — |
| 屏幕录制权限有没有 | ✅ **有** —— `CGWindowListCopyWindowInfo` 能读到**窗口标题**(23/25 个窗口有 name) | 有 name = TCC Screen Recording 已授予 |
| `screencapture -l<windowid>` 能不能只截一个窗口 | ✅ 能(实拍了 Dock 那个窗口,画面里**只有 Dock、没有用户桌面**) | 🔴 **这条就是"不拍到用户桌面"的解** |

**选定路径 = ①(独立 bundle id 的 Debug 变体),取舍如下**

- 为什么不选 ②(请用户授权):**权限本来就有**,②要解决的问题不存在;而且它救不了真正的病 ——
  **同 bundle id 时 LaunchServices 只会把用户那个还在跑的正式 App 切到前台**(CLAUDE.md 已登记)。
- 为什么不选 ③(纯目视验收):上一轮就是这么结的案,用户当场打回。③ 只当**兜底**,不当主路。
- ① 还**顺手拆掉一颗雷**:2026-08-10 那次「把用户正式 App 打成连不上」的事故,根因是
  `defaults write top.linotsai.neckline …` 写进了**真 App 同一个 UserDefaults 域**。dev 变体
  bundle id 不同 → **域天然隔离**,`defaults write top.linotsai.neckline.dev NK_BASE_URL_OVERRIDE …`
  是安全的。

**做法(⛔ 不改 `project.yml` / `pbxproj`,零工程文件风险)**

1. `xcodebuild -scheme Neckline -destination 'platform=macOS' -configuration Debug -derivedDataPath <scratch>/dd build`
2. `ditto` 出的 `Neckline.app` 到 scratch(🔴 **`ditto` 不用 `cp -R`**,全局坑)
3. `PlistBuddy` 改副本的 `CFBundleIdentifier` → `top.linotsai.neckline.dev`、`CFBundleName`/
   `CFBundleDisplayName` → `NecklineDev`
4. `codesign --force --deep --sign - <副本>`(macOS 侧**无 entitlements**、非沙盒,ad-hoc 签名够用)
   + `xattr -cr`
5. `defaults write top.linotsai.neckline.dev NK_BASE_URL_OVERRIDE http://127.0.0.1:<port>` /
   `… NK_API_TOKEN <演示 token>` → `open -a <副本>`
6. 找窗口号:`CGWindowListCopyWindowInfo` 过滤 `kCGWindowOwnerName == "NecklineDev"` →
   `screencapture -x -o -l<windowNumber> out.png`
7. 收尾:`osascript -e 'quit app "NecklineDev"'` 或 `pkill -f NecklineDev`(🔴 `pkill -f` 前先
   `pgrep -af` 确认 pattern 不匹配自己)+ `defaults delete top.linotsai.neckline.dev`

**🔴 三条纪律**

- ⛔ **绝不 `defaults write top.linotsai.neckline`**(无后缀那个 = 用户正式 App 的域)。
  🔴 **每批收尾自检只查一个键:`defaults read top.linotsai.neckline NK_BASE_URL_OVERRIDE`
  必须报「does not exist」**。⛔ **绝不许按 `grep NK_` 判空、更不许「不为空当场清掉」** ——
  该域里的 `NK_API_TOKEN` **就是用户从设置屏亲手填进去的真 token**(`AppConfig.tokenKey` 写的
  就是这个键;2026-08-10 实测:拿它打生产 `/positions` 返 **HTTP 200**)。照 `grep NK_` 清空 =
  **把用户正式 App 的鉴权当场清掉、下次打开一直 401**,比原来要防的那颗雷更狠。
  ⚠ 要防的雷**只有一颗**:`NK_BASE_URL_OVERRIDE` 被写进宿主域(它压过默认 prod 基址)。
- **截图窗口必须是 1200×860**(= 原型画布 `Neckline macOS.dc.html` 21 行 `width:1200px; height:860px`),
  否则"自适应详情栏"的比对没有基准。手段自选(临时改 `.defaultSize` 或加一个 DEBUG-only 尺寸钩子),
  ⛔ 但**别把临时尺寸留在交付代码里**。
- **兜底(只在 ① 当场失败时才准用)**:`screencapture -l` 拿不到窗口 → 退「iOS 截图 + 双端
  `BUILD SUCCEEDED`」,**并在本节当场记一行为什么失败**;⛔ 不许默默跳过。

### 〇c、已查实的**四处硬伤**(立项时查完了,施工侧不必重查)

**硬伤 1 · 双标题栏(macOS 壳)** —— 🔴 用户第一眼看到的那条

`Neckline/App/NecklineApp.swift` 的 macOS `WindowGroup` **缺 `.windowStyle(.hiddenTitleBar)`**
→ 系统原生标题栏还在,自建 50px `NKToolbar` 挂在它**下面**,成了**两条栏**;而
`NKToolbar.swift:28` 那句 `Color.clear.frame(width: 68)` 红绿灯占位因此变成**一段纯空白**。
原型是**一条**:红绿灯(12px 圆点 ×3,gap 8)与 Logo / 板块胶囊**同排**(macOS 原型 23–27 行)。
⚠ 隐藏标题栏之后有两件必须一起办,漏一件就是新的坏:

- **窗口拖动**:工具栏空白处要能拖窗。⛔ **不许用 `NSWindow.isMovableByWindowBackground = true`**
  —— 那会让**整个内容区**(含列表、卡片)都能拖窗,列表点选会变成拖窗口。正确做法是只给工具栏
  那一条挂一个可拖区域。
- **红绿灯垂直居中**:系统把三颗按钮钉在标准标题栏(28pt)的垂直中线,**不是** 50px 工具栏的中线。
  要与原型一致必须自己挪 `standardWindowButton(_:)` 的 frame。**这处若判定为"平台差异·刻意不同",
  必须实拍一张附在对照表里**,⛔ 不许口头说"差不多"。

**硬伤 2 · 角色码印英文** —— 界面上直接出现 `leader` / `core`

`Models.swift:614` 的 `BasketMember.roleDisplay` 把服务端原值**原样返回**;生产实际发的是
`leader` / `core` / `elastic`(源 `neckline/selection/aggregate.py:102`,`role_mech` 还有第四值
`unknown` = **算不出、不是一种角色**,见 `neckline/scan/leader.py`)。原型印的是中文
(「龙头」/「跟随」/ 冲突时「角色两说」)。

- **这正是 `nkBoardLabel` 那条先例管的事**(`Models.swift:186`):服务端只发英文码、中文在客户端
  展示层换算、**未识别值原样透传不瞎翻译**。
- ⚠ **同一份逻辑在 `Models.swift` 里抄了三份**(614 / 1828 / 1910 行,`BasketMember` /
  `InfoCardBasketPeer` / `InfoCardBasket`)+ `AppModel.swift:685` 拼串一处 —— **收成一个
  `nkRoleLabel(_:)` 函数**,⛔ 不许只改看得见的那一处。
- ⚠ **既有单测 `AppModelTests.swift:174-179` 是绿的**,因为 fixture 直接喂了中文
  「龙头」/「跟随」—— **绿的测试没有拦住线上印英文**。改完必须补一条**喂英文码**的用例。

**硬伤 3 · 全 App 徽标还是胶囊**(新查实,上一轮漏了)

规范 §04 写死「**徽标(方)4–5** · 原来全是胶囊」,`NKRadius.badge = 5` 也定义了 —— 但
`SharedUI.swift:70` 的 `NKChip` **仍然是 `Capsule()`**,而 `NKChip` 是全 App 徽标的唯一实现。
原型里所有徽标一律 `border-radius:4px`(macOS 原型 367–376 行角色 / 判定徽标、40 行篮子计数、
253–258 行 Tier 与卡版本徽标 …)。**这是本版覆盖面最广的一处系统性偏差**。
原型的徽标规格还与现状差三项:`font-size:10.5px`(现 `caption` 11)· `padding:2px 6px`
(现 `h8 / v3`)· 底色 `.10–.14 alpha`(现 `.12`,基本对得上)。

**硬伤 4 · App 图标 —— 🟢 包里已经是新的,Dock 是缓存**(立项时实证结案)

| 查验 | 结果 |
|---|---|
| `client/**/AppIcon.appiconset/*.png` vs 交接包 | **7/7 md5 逐字节相同** |
| `/Applications/Neckline.app` 版本 | `2.3.0`(用户已换包) |
| 装机包 `Assets.car` | `assetutil --info` 列出 **10 条 mac 渲染项齐全**(16→1024,`RenditionName` 全是新 PNG) |
| 装机包 `AppIcon.icns` | `iconutil -c iconset` 抽出位图 —— **就是新的红橙 + 白色颈线 + 突破点圆环** |
| **用户 Dock 上那一枚** | `screencapture -l<Dock 窗口>` 实拍 —— **是旧的青绿渐变折线图标**(= `Neckline视觉升级/current-appicon-1024.png`) |

**结论:包对了、缓存没刷。⛔ V2.3.1 不要再动图标资产**(动了反而会引入新问题)。本版要办的是两件:
① 把上面这条**验证链固化成一条可重跑的命令**(`assetutil --info` 看渲染项 + `iconutil` 抽位图目视),
⛔ 不许再靠"文件时间戳看着对"结案;② 把**刷新缓存的办法**写进 ⑩ 的用户手动清单
(它需要 `sudo`,agent 办不了)。

### 〇d、批 0/1 施工期登记(结转给后面各批,⛔ 别重新踩)

| # | 事实 | 结转给谁 |
|---|---|---|
| 1 | 🔴 **`screencapture -l` 会在会话中途对任何 App 失效**(拿 Xcode 窗口对照测过)。立项时用的判据「窗口标题读得到 = 有屏幕录制权限」在 macOS 26 上**不成立**:`CGWindowListCopyWindowInfo` 仍读得到 27/31 个标题、`CGPreflightScreenCaptureAccess()` 仍返 `true`,但 `SCShareableContent` 报 **`displays=0`**。已绕开(**dev 变体自截自己的窗口**,见 `NKDevCapture.swift`)。**权限判据改成 `SCShareableContent.displays` 非空。** | 批 2–7 全部出图路径 |
| 2 | `.hiddenTitleBar` 落地后 UserDefaults 里的 `NSWindow Frame …` **不再生效**,窗口每次起在 1080×774(最小宽 × 90%)→ **截图基准会漂**。已由 `NECKLINE_WINDOW_SIZE` 钉死。 | 批 2–6 每张实拍都要带同一尺寸 |
| 3 | **`NECKLINE_SKIP_PUSH_PROMPT=1` 没挡住 iOS 推送授权弹窗**(批 1 密度复查时实拍到弹窗盖住页面中部)。 | **批 7 出 iOS 八屏前先查这个钩子是不是失效了**,或按 CLAUDE.md 走 `simctl shutdown`+`boot` |
| 4 | `NKRadius.badge` 由 5 收到 **4**,顺带改到了六关宫格(`NKGateViews.swift:149/154`)。**原型六关格是 radius 9、「硬」角标 radius 3**,两者都不是 4。 | **批 2 做六关宫格时单独定值**,⛔ 别以为 `badge` 令牌已经覆盖 |
| 5 | 硬伤 2(角色码印英文)实际有**六处**,§〇c 只列了四处 —— 多出的两处是 `NKMemberCard` 展开区「机械判 / LLM 判」两格与 `InfoCardView` 的两枚徽标。**已全部收进 `nkRoleLabelOrDash`**(属批 1 范围内)。 | 后续别再另写第二份换算 |
| 6 | 🔴 **硬伤 2 还有第七处、且不是角色码**:行情状态五维的缺维在界面上直接印 `moneyflow_migration` / `t1t2_accuracy`(服务端 `regime.py` 的 `DIM_*` 英文常量)。已按 `nkBoardLabel` 先例补 `nkRegimeDimLabel`(未识别原样透传)。**教训:凡是把服务端 `dict` 的 key 直接印到界面上的地方,都是这个病的潜在现场。** | 批 3–7 见到 `.keys` 直接进 `Text` 就停一下 |
| 7 | **`Text(String)` 不解析 Markdown**,`Text("字面量")` 才解析 —— 把带 `**加粗**` 的文案改成 `+` 拼接或传成 `String` 参数,星号会原样印在界面上(批 2 实拍逮到两处)。要传参就把类型写成 `LocalizedStringKey`。 | 批 3–7 所有诚实披露文案(它们大量用 `**`) |
| 8 | **演示库的 `sentiment_json` / `sectors_json` 是 snake_case 原样透传**(客户端 CodingKeys 就是 snake_case,`position_quota` 还得是中文「半额」);写成 camelCase 会**静默**解不出来 → 界面显示「本次没有情绪仪表盘数据」,看起来像组件坏了。 | 批 3–6 造演示数据 |
| 9 | **窗口最高只到 1410pt**(本机屏 2560×1410),`NECKLINE_WINDOW_SIZE=1200x1600` 会被系统截成 1200×1410。成员卡展开态在 860 高里装不下,批 2 的处理 = **五张主实拍恒 1200×860**,另出一张**同宽 1200 × 1410** 的附图,并把演示数据砍到 2 只成员。 | 批 3(刻度尺)/ 批 5(信息卡整页)同样会溢出 |

### ① 批 0 · 验收装置(不改任何界面,只建"能验"的能力)

| 项 | 内容 |
|---|---|
| 目标 | 让后面每一批都能拿出「原型 ↔ 实拍 ↔ 数值对照表」三件套 |
| 交付 | ① 演示库 + 隔离后端起得来(六关 / 成员 / 刻度尺 / 降级项都有数)· ② macOS dev 变体从构建到截窗口整条跑通(见 〇b)· ③ iOS 模拟器截图链复用既有 QA 钩子(`NECKLINE_INITIAL_TAB` / `_MODAL` / `_BASKET_ID` / `_INFOCARD_CODE` / `_REVIEW_PAGE` / `NECKLINE_SKIP_PUSH_PROMPT=1`)· ④ 对照表模板 |
| **验收** | 🔴 **交出第一张 macOS 真窗口截图**(1200×860,画面里只有 App 窗口)。⚠ 这张图**预期是"坏"的**(双标题栏还在)—— 它就是硬伤 1 的证据照,同时证明装置可用 |

### ② 批 1 · 全局钉子(四处系统性偏差,一次收干净)

| # | 判据(原型出处) | 落点 |
|---|---|---|
| 1 | **窗口壳一条栏**:`hiddenTitleBar` + 工具栏可拖 + 红绿灯 12px×3 / gap 8 / 距 Logo 8(macOS 原型 23–27) | `NecklineApp.swift`(硬伤 1) |
| 2 | **徽标去胶囊**:`radius 4`(令牌 `NKRadius.badge`)· `10.5/600` · `padding 2/6` | `SharedUI.swift::NKChip`(硬伤 3);⚠ 改完**全 App 都变**,后面每一批的实拍都要复查一遍密度 |
| 3 | **两栏底色分开**:列表栏 `#FCFCFD` / 详情栏 `#F6F6F8`(macOS 原型 81 vs 247);分隔 = `border-right .5px rgba(60,60,67,.12)` | `RootView.swift::NKSplitLayout` 现在**两栏都是 `NK.pageBg`** → 列表栏与详情栏糊成一片。⚠ 新增底色令牌一枚,**⛔ 不改既有色令牌**(V2.3 定的「色令牌一个没加、一个没改」在本版**改成:只为这一处加一枚,理由记在令牌注释里**) |
| 4 | **角色码中文换算** + 单测喂英文码 | `Models.swift` 三处 + `AppModel.swift:685`(硬伤 2) |
| 5 | **卡片描边 alpha**:原型 `.12`(卡)/ `.10`(卡内子块),现 `NK.hairline` = `.10` 一档打通 | `SharedUI.swift::NKCard` —— 判定「已改齐」或「刻意不同 · 就近对齐令牌」二选一,**必须写进表** |

**验收**:macOS 工具栏那一条与原型 23–77 行**逐项**对照表 + 一张 1200×860 实拍;
`xcodebuild` 双端 `BUILD SUCCEEDED` + iOS Simulator `NecklineTests` 全绿。

### ③ 批 2 · macOS 选股板块

| 屏 | 原型出处(`Neckline macOS.dc.html`) |
|---|---|
| 列表栏(标题区 / 今日概览行 / 昨日回执行 / T1·T2 分组头 / 篮子行 + 行下成员) | **81–246 行** |
| 详情 · 篮子卡 | **249–643**(分数卡 263–289 · 六关 290–339 · 驱动三段 340–362 · 成员卡 363–626 · 参考件 627–643) |
| 详情 · 未定档篮 | **644–707** |
| 详情 · 今日概览 | **708–826** |
| 详情 · 昨日回执 | **827–852** |

**必查的钉子**(不止这些,但这几处最容易漏):

- 列表栏**标题区 `padding:18px 16px 10px`,而行是 `padding:0 10px 4px`** —— 两套页边距,现在
  `NKSplitLayout` 是**整栏一个 `listPadH:10`** → 标题贴太左。
- 列表行 `pick()`:`padding:11px 12px; radius 9`;**选中 = 白底 + 1.5px `#0B6BCB` 描边 + 轻投影**。
- 详情区 `padding:22px 26px 40px; gap:16`(249 行);卡 = `#fff / .5px rgba(60,60,67,.12) / radius 12 / padding 16px 18px`;卡内子块 = `radius 9 / padding 12px 13px / .5px rgba(60,60,67,.10)`。
- 分数卡:`32/600` 主数 + `/ 100 · 机械分 7.4`(12px `.40`)+ 五条 `height:5px radius 999` 进度条,
  列宽 `76 / flex / 42 / 34`;**缺数那一维用 `repeating-linear-gradient` 斜纹**(SwiftUI 无等价物 →
  判「刻意不同 · 平台差异」并给出替代画法)。
- 六关宫格:`grid 6 列 gap 8`;机械关 `1.5px 边框 + 底色 + 「硬」角标`(`9px/700`,白字,
  `rgba(60,60,67,.50)` 底,radius 3),证据关 `.5px` 描边;格 `radius 9 / padding 9px 8px / 居中`,
  关名 `11.5/600`、结论 `14/600`。
- 成员卡 `memCard()`:`radius 10`,**选中 `#FAFAFC` + `inset 0 0 0 1px rgba(11,107,203,.28)`**;
  收起行 `padding 11px 12px / gap 9`,名称 `14/600`、代码 `11 tabular .40`、角色徽标与两枚判定徽标
  `10.5/600` 与 `10/600`。**macOS(详情栏 ≥700pt)仍一行**,⛔ 别把 iOS 的两行搬过来。
- **报告未生成态**(`Neckline 状态.dc.html` **188–328 行**)一并在本批做:排程表 + 「非实时状态」标注
  (⛔ 见 §五 〇-2)。

**验收**:五张 macOS 实拍(列表栏 + 四种详情)+ 每屏一张对照表;`BUILD SUCCEEDED` + 测试全绿。

**✅ 批 2 完工(2026-08-10)**。**六屏逐项对照表全文 → `archive/V2.3.1_批2对照表_20260810.md`**
(共 6 张表 / 111 行判定,含密度复查与超范围登记)。实拍六张(五张 **1200×860** + 成员卡展开态附图
**1200×1410**,同宽)在 `…/24c2fb33…/scratchpad/b2_0{1..6}_*.png`。**Python 3581 / 3 skipped / 0 failed · iOS 214 tests / 0 failures ·
双端 BUILD SUCCEEDED · 宿主域 `NK_BASE_URL_OVERRIDE` does not exist**。
**判「刻意不同」的五处**:① 持仓占比(§五 〇-4,本屏未涉及)· ② 六关「判不出」画成篮级琥珀条 + 格级
「未记录」(§五 〇-3)· ③ 排程表**四个时刻同样着色**、⛔ 不画原型那颗「16:05 日更进行中」的琥珀光晕
(§五 〇-2:客户端观察不到批算进度)· ④ 缺数维的进度条**斜纹**改「半透明琥珀 + 虚线描边」(平台差异:
SwiftUI 无 `repeating-linear-gradient` 等价物)· ⑤ 一批字号就近对齐令牌(12.5→12 / 11.5→11 / 10.5→11 /
9.5→11 / 16→15 / 14→15,规范 §03 原文「一律向下表就近对齐」)。
**顺带收的两处系统性偏差**(见 〇d 第 6、7 条):行情状态五维缺维印英文码 → `nkRegimeDimLabel`;
`Text(String)` 不解析 Markdown 导致 `**加粗**` 星号上屏。
**登记但未改(超出批 2 范围)**:`DroppedBasketRow` / `NKAuditSection` / `GateLightBar` / `BasketListRow`
四处是双端共用件,本批的改动**同时改到了 iOS**(③b 行去掉白卡外壳、审计视图收起态换虚线按钮、
灯条改「六点 + 一句摘要」)—— **iOS 侧未做实拍核对,归批 7**。

### ④ 批 3 · macOS 持仓板块

| 屏 | 原型出处 |
|---|---|
| 列表栏(标题 + 补录开仓按钮 + 汇总条 + 盘中动态 / 临时提醒行 + 合并敞口块 + 仓位行) | **856–983** |
| 详情 · 仓位(**止损刻度尺是主角**) | **985–1109**(刻度尺 1012–1029 · 条件单勾选 1031–1046) |
| 详情 · 其余仓位 / 已清仓 | **1110–1197** |
| 详情 · 盘中动态 | **1198–1228** |
| 详情 · 临时提醒 | **1229–1263** |

**必查的钉子**:

- 刻度尺整块 `height:44 / margin:0 4px`:轨道 `top:20 height:5 radius 999`,安全区渐变
  `rgba(15,169,104,.25) → .55`;止损 `3px×17 @top14`、成本 `2px×15 @top15`、现价 `3px×23 @top11`
  (标签在**下方** `top:36`,`11/700`)、峰值 `2px×15 @top15`;刻度标签 `10px` + `translateX(-50%)`。
  **破线态**:现价标记落到止损线左侧 + 轨道浅红底 + **整卡 1px `NK.down` 描边**。
- 刻度尺下四格读数:`11px .55` 标题 + `16/600` 数值 —— 🔴 **第四格原型是「占总仓 35.3%」,
  本版判「刻意不同 · §五 〇-4」,只留三格或换成金额,⛔ 不许补占比**。
- 条件单勾选卡:`padding:14px 18px`、勾选框 + `13px` 文案 + 勾选后 `10.5px .40` 的
  「X月X日 HH:mm 勾选 · 本机记录,换机不同步」(V2.3.0 已落 `NKStopOrderLedger`,本批只对版式)。
- 列表栏汇总条第一格原型是「61.2% 占总仓」→ 同上,**判刻意不同**。

**验收**:五张实拍 + 对照表;刻度尺**正常态与破线态各一张**(演示库造两笔仓)。

**✅ 批 3 完工(2026-08-10)**。**六屏逐项对照表全文 → `archive/V2.3.1_批3对照表_20260810.md`**
(6 张表 / 88 行判定,含密度复查、复核订正与超范围登记)。实拍六张(五张 **1200×860** +
披露全展开附图 **1200×1410**)在 `…/bf39a624…/scratchpad/b3r_0{1..6}_*.png`。
⚠ 本块是**两段完成的**:界面代码先落盘,再由第二个 agent 做**独立复核**并当场改齐两处 ——
🔴 **① `plan_json.*_clamp` 的服务端英文机器码被当中文正文印上屏**(`本次不可用(rejected_not_above_close)`,
硬伤 2 的**第八处**;上一轮演示种子把这格喂成中文「夹逼拒收」,**实拍看不出来**)→ 正文改说
「本次不可用」、机器码降到 `monoKey` 等宽灰字;⛔ 不在客户端补中文表(人读文案的唯一源在服务端
`clamp_reason_text`,根治要 `positions_entry._member_plan_fields()` 补搬 `*_unavailable_reason`,**已登记**)。
**② 三列表头「离场参考区间(不是止盈线)」→「离场参考区间」**(原型 1054 + 批 2 篮子卡侧先例;
「不是止盈线」整话仍在下方披露区)。**四处判「刻意不同」**:占总仓三处(§五 〇-4:汇总条 / 合并敞口
展开行 / 刻度尺第四格)· **回落止盈线**那一格(同类:`RetraceState` 契约只发 `{peak, retracePct,
triggered}`,比例从未下发)· 计划卡右端偏离提示(`planDeviationNotice` 只在开仓响应里发一次)·
提醒卡主语只有代码(`CustomAlert` 无名称字段)。**验收**:Python **3581 passed / 3 skipped / 0 failed** ·
iOS **214 tests / 10 skipped / 0 failures** · 双端 **BUILD SUCCEEDED** · 宿主域
`NK_BASE_URL_OVERRIDE` = **does not exist**。**登记未改**:🔴 金额缺千分位且带两位小数
(`NKFmt.price`/`signedMoney` 是四板块共用的全局格式化器,**批 1 全局钉子的漏网项**,改它会同时改掉
批 2/4/5/6/7 的每张实拍)· clamp 的服务端根治 · `NKMemberCard` 同款兜底分支。

### ⑤ 批 4 · macOS 复盘板块 + 对账工作台

| 屏 | 原型出处 |
|---|---|
| 列表栏(五页 + **每行「这一页答什么」**) | `Neckline macOS.dc.html` **1267–1335** |
| 每日 / 选股钟 / 交易钟 / 累计 | **1337–1384 / 1385–1437 / 1438–1465 / 1466–1544** |
| 对账(入口) | **1545–1573** |
| **对账工作台已上传态**(整屏) | `Neckline 信息卡与对账.dc.html` **285–451**(列表栏 308 起 / 详情 337 起) |

**必查的钉子**:列表栏 `padding:18px 16px 12px` + 副标题 `11.5 .55 line-height 1.5`;五行
`gap:2`、行内 `13/600` + 右侧计数徽标(`10/700`,radius 4)+ 次行 `11 .55 line-height 1.45`;
「触发强制复盘」主按钮 `15/700` 白字(信息卡与对账 351 行)。
⚠ 复盘每日页的**日期选择器**(V2.3.0 已结 P3-47)只对版式,⛔ 不动它「另存一份快照、不覆盖
`report`」的既有行为。

**验收**:六张实拍(列表栏 + 五页)+ 对账工作台一张 + 对照表。

### ⑥ 批 5 · macOS 设置 + 四个弹层 + 信息卡整页

| 屏 | 原型出处 |
|---|---|
| 设置列表栏(四组) | `Neckline macOS.dc.html` **1577–1616** |
| 设置详情 · 连接 / Provider / 推送 / 版本 | **1618–1671 / 1672–1706 / 1707–1732 / 1733–1887** |
| 四个弹层 | `Neckline 弹层.dc.html` 全文(**共同形状 = 56px 标题条〔取消 / 标题 / 主操作〕+ 内容区分组 + 底部说明**,22 行原文) |
| 信息卡整页 | `Neckline 信息卡与对账.dc.html` **37–284** |

**必查的钉子**:弹层标题条 **56px**、取消/主操作 `13.5px #0B6BCB` 且**取消固定宽 60**;
补充说明「七标签」由 `Form` 七行改**两列可点方块**(弹层 133 行原文,主操作叫「记下」、取消叫
「跳过」);离场原因**九码一排可点标签**、已选那枚**按语义着色**(止损 = `NK.down`)。
⚠ 设置屏的「Provider 注册表与任务路由不下放 iOS」那句**已在 V2.3.0 说出口,只对版式不动文案**。

**验收**:设置五张 + 四个弹层各一张 + 信息卡整页一张 + 对照表。

**✅ 批 5 完工(2026-08-10)**。**十屏逐项对照表全文 → `archive/V2.3.1_批5对照表_20260810.md`**
(10 组表 / 100 行判定,含密度复查与超范围登记)。实拍十张:设置四屏 + 补录清仓 + 补充说明
(各 **1200×860**)· 新建提醒 + 编辑 Provider(**1200×1000**,弹层装不下时**只加高不改宽**)·
信息卡上半 / 下半(各 **1200×1340**;整页约 2100pt,下半那张按 CLAUDE.md **先砍演示数据**)。
**⛔ 只动 `client/**`、未改版号、未部署。**
⚠ **弹层标题条实测取 52,不是本节写的 56** —— 原型自己的 inline style 是 `height:52px`
(弹层 31/76/133/190 行逐字相同),56 只出现在同一份原型的**散文说明**里;按 §五 〇a-A
「对不上时以 inline style 为准」。
**新增一件 `client/Neckline/Components/NKFormKit.swift`**(弹层壳 `NKSheetShell` + 分组字段卡
`NKFieldCard/Row/Label` + `NKSwitch` / `NKSegmented` / `NKTagButton` / `NKCheckSquare` /
`NKTextFieldBox` / `NKOutlineButton` / `NKDashedButton` / `NKInlineMenu` / `NKInlineNote` /
`NKTintedNote`)—— 🔴 **理由是 `Form(.grouped)` 的圆角 / 页边距 / 分隔线 / 段标题字号全由系统定、
改不了**,逐项对不到原型的 inline style;⚠ **iOS 侧原样保留 `Form`**(iOS 逐屏比对归批 7)。
**本批逮到的四处真缺陷**:🔴 ① **硬伤 2 的第九处** —— 信息卡「判不了的标注」直接印服务端英文码
(`pullback_leader` / `warn_streak_top` / `warn_chase_zone`,源 `selection/member_tags.py`),
按批 3 clamp 先例改「正文说人话 + 机器码降 `monoKey` 等宽灰字」,⛔ 不在客户端补中文表;
🔴 ② **§〇d 第 7 条的服务端版本** —— 七项确认卡第 ⑥ 行的服务端原文里带 `**有延迟**`
(`custom_alerts.py:116`),`Text(String)` 不解析 Markdown → **星号原样上屏**,新增
`nkServerMarkdown()` 修(双端一起);③ 红黄牌 `level` 写成 `== "strong" ? "强" : "普通"`,
第三种取值会被**静默讲成「普通」**;④ RS 线 / 行业分歧线默认 y 轴带 0,**把线压成一条平线**
(图还在,但它要回答的问题看不出来)→ `includesZero: false`。
**判「刻意不同」的十处**:信息卡副行「主板」(⛔ 客户端不重推分类)· 右上「现价」(契约无此字段,
改画 K 线末根收盘并当面标明)· 同篮对比表的「今日」列与「本票自己那一行」· RS 卡下那句
「累计跑赢 +18.4%」· 三张图**保留坐标轴**(原型 SVG 是纯装饰位图,不给刻度就读不出价位)·
两列并排卡不等高(要拉平得改共用件 `NKCard`)· 补充说明多一格「标的」(本 App 从工具栏独立打开,
没有可自动关联的标的)· 路由表 / 推送表各多一枚「保存」(全量覆盖式 PUT,没有入口就存不下)·
Base URL 单行不折行 · 一批字号就近对齐令牌。
**验收**:Python **3581 passed / 3 skipped / 0 failed** · iOS **218 tests / 10 skipped / 0 failures** ·
双端 **BUILD SUCCEEDED** · 宿主域 `defaults read top.linotsai.neckline NK_BASE_URL_OVERRIDE`
= **does not exist**。
**登记未改(超出批 5 范围)**:服务端 `tags_absent` 应改发 `{code,label}` · 全仓「`Text(String)`
× 服务端 Markdown」未做系统性排查 · `NKCard` 缺一档「填满行高」导致并排卡不等高 ·
🔴 **`kill -9` 反复杀 dev 变体会留下损坏的 Saved Application State,下次启动窗口永不出现**
(进程活着、0% CPU、日志为空,极难归因;解法两步已写进对照表 §超范围-4,建议并进 `shot_slot.sh`)。

### ⑦ 批 6 · 两个全屏状态

| 屏 | 原型出处 |
|---|---|
| 退潮红色刹车激活(工具栏胶囊翻 `NK.down` 实底白字 + 通栏刹车条 + 板块仍全部列出) | `Neckline 状态.dc.html` **37–187** |
| 报告未生成(排程表) | **188–328**(已在批 2 做完 → 本批只做**回归实拍**) |

**必查的钉子**:刹车条 `16/700` 白字 + 次行说明 + 「看哪几条触发了」入口;⛔ **刹车条不进卡片流**;
🔴 **补录开仓按钮不灰化**(硬拦 = 帮用户瞒报,V2.2-⑤-B 已拆过一次熔断)。

**验收**:两张实拍 + 对照表(演示库里把 `retreatBrake.active` 打开)。

### ⑧ 批 7 · iOS 八屏

`Neckline iOS.dc.html`,八屏各自的行号范围:

| # | 屏 | 行 |
|---|---|---|
| 1 | 锁屏推送(两条通知卡) | 28–83 |
| 2 | 持仓 · 盘中主屏 | 84–220 |
| 3 | 持仓详情 · 推送落地页(**动作按钮吸底**) | 221–307 |
| 4 | 选股 | 308–445 |
| 5 | 篮子卡 · sheet | 446–559 |
| 6 | 复盘 | 560–644 |
| 7 | 补录开仓 | 645–733 |
| 8 | 设置 | 734–825 |

**必查的钉子**:画布 402×874;状态栏 + 大标题 58;内容页边距 16;玻璃 TabBar `radius 26` + 底部留 40;
成员卡收起行 **iOS 分两行**(CLAUDE.md 已登记的 402pt 挤压坑,⛔ 别改回一行);
`sheet` 顶 `radius 28`。⚠ **锁屏推送那一屏是深色壁纸上的 `.regularMaterial`** —— 它是通知系统渲染的,
客户端只能控制标题 / 正文文案与 App 图标,**版式判定基本全是「刻意不同 · 平台差异」**,如实写。

**验收**:八张 `xcrun simctl io screenshot` + 对照表。内容溢出视口时按 CLAUDE.md:**先砍演示数据**,
再考虑临时 iPad(用完 `simctl delete`)。

### ⑨ 批 8 · 收尾 · 全量验收 · 部署换包

1. **版号三处同升 `2.3.1`**:`neckline/api/app.py::VERSION`(**本版唯一被迫碰后端的一行**)+
   `client/project.yml`(顶层 + app target 两处,值必须相等)+ `pbxproj` app target Debug/Release。
   ⚠ 加过新 `.swift` 文件就必须 `xcodegen generate`(pbxproj 是显式文件引用);它会顺手改
   **project 级** `MARKETING_VERSION`(守门单测不比这一处,别以为绿了就没漂)。
2. **全量验收闸**(缺一不可):
   - Python 全量 `python -m pytest tests/ -q` —— 基线 **3581 passed / 3 skipped / 0 failed**
   - `xcodebuild build` **iOS + macOS 双端 SUCCEEDED**
   - iOS Simulator `NecklineTests` 全绿(基线 211;角色码新增用例后 ≥ 212)
   - **每一批的对照表齐了**,`刻意不同` 行**全部带三类合法理由之一**
   - `defaults read top.linotsai.neckline NK_BASE_URL_OVERRIDE` 报 **does not exist**
     (🔴 UserDefaults 域自检;⛔ **不是** `grep NK_` 判空 —— `NK_API_TOKEN` 是用户真 token,见 §〇b)
3. **部署**:服务端只有版号一行 → 与 macOS 换包**同批上**。
   🔴 **窗口**:交易日 **15:05–15:55** 或 **17:15 之后**,或**周末全天**;⛔ **永不在 16:00–17:00**。
   ⚠ V2.3.0 那两处后端改动(版号 + `PositionsOut.circuit` 删键)**至今未部署**,本次一并上 ——
   删键零风险的判据(历代客户端从没有一版声明过 `circuit`)见 §五(旧-V2.3.0) 〇。
4. **换包**:macOS 旧包先备份到 `~/Lino/app_backups/`(`ditto`,⛔ 不用 `cp -R`);iOS 用户自装。

### ⑩ 用户手动清单(非网页,agent 办不了)

| # | 事 | 怎么办 |
|---|---|---|
| 1 | **刷新 macOS 图标缓存**(硬伤 4 的真正解;包里已经是新图标) | 退出 Neckline → 终端跑 `sudo rm -rf /Library/Caches/com.apple.iconservices.store` → `/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -f /Applications/Neckline.app` → `killall Dock; killall Finder`。⚠ 若图标是**钉在 Dock 上**的,还要把它从 Dock 拖走再拖回来 |
| 2 | **iPhone 装 2.3.1** | 沿 §八 第 20 项 |
| 3 | **macOS 换包后逐屏过目** | 对照 `Neckline视觉升级/Neckline macOS.dc.html`(浏览器直接打开);这是 ① 之外的**第二道**,不是替代 |

### ⑪ 角色码中文换算(**2026-08-10 用户已拍板,⛔ 不得重开**)

角色码中文换算(硬伤 2)里,`leader → 龙头`、`core → 跟随`、冲突 → 「角色两说」**三项原型有据**
(macOS 原型 369 行 / A-workbench 376 行);**剩下两个原型里没出现过**:

| 码 | 默认落地 | 依据 |
|---|---|---|
| `elastic` | **「弹性」** | `neckline/scan/leader.py` 口径 = 簇内排除头名后的后一半,注释原话把它对举成「投机跟风」;「弹性」是三者里最不带褒贬的一个 |
| `unknown` | **整枚徽标不画**(⛔ 不画「未知」) | 服务端注释写死「`unknown` = 算不出,**不是一种角色**」;画出来就是把"没算出来"讲成了一种判断 |
| 其余未识别值 | **原样透传** | `nkBoardLabel` 先例 |

🔴 **用户 2026-08-10 原话:「elastic 就叫弹性」** —— `elastic → 弹性` **已定案**;
`unknown` 整枚不画、其余未识别值原样透传两条按上表默认执行。**⛔ 施工时不得再当「待定」处理。**

## 五(旧-V2.3.0)、V2.3.0 施工图(**结构已落地 · 保真度不达标 → V2.3.1 返工**;⛔ 未部署 / 未换包)

> 🔴 **2026-08-10 用户当场打回**:拿实机截图与参考原型并排比对后 ——「完全达不到参考文件的要求」
> 「顶上那个红绿灯一栏是多少版本前的设计了」「icon 也没换」「我要的和参考文件一致的前端」。
> **本节记录的结构、信息架构、诚实披露文案全部有效且是资产**(⛔ V2.3.1 不推翻);**缺的是视觉
> 保真度** —— 上一轮只照着规范文档的文字核对,**没有拿六份原型逐屏做像素级比对**。
> **现行口径以 §五 V2.3.1 为准**;本节保留两样东西继续生效:**〇 的两条用户裁定** 与
> **〇b 的四处「mock 画了、契约支撑不了」处置**(V2.3.1 §五 〇 原样继承,⛔ 不得推翻)。

> **一句话**:把设计交接包 `Neckline视觉升级/` 落进 SwiftUI —— **只改呈现**,一个判定、
> 一个阈值、一句诚实披露文案都没动。信息架构(三板块 + 设置沉底)、段名(①②③③b④⑤)
> 全部按现状执行。
> **来源**:`Neckline视觉升级/Neckline 视觉规范.dc.html`(落地口径)+ 六份原型 + `AppIcon.appiconset/`。
> **边界**:`client/**` 为主;**唯二碰后端的地方** = 版号一行 + `PositionsOut.circuit` 删键,
> 两处都经用户 2026-08-10 当面拍板。

### 〇、用户当日两条裁定(⛔ 施工时不得重开)

| # | 裁定 | 落点 |
|---|---|---|
| 1 | **版号三处同升 `2.3.0`**,服务端那一行**随后一起部署**(不为一行版号单独走一次生产部署) | `app.py::VERSION` / `project.yml` / `pbxproj`,守门单测锁恒等 |
| 2 | **`circuit` 两步淘汰第二步纳入本版**,排最后一批、与版号同一次碰后端 | 服务端删键 + 客户端删两个 DTO + 守门反向锁翻面 |

### 〇b、施工期核实出来的**四处「mock 画了、契约支撑不了」**(规范 §08 只列了两处)

**处置口径统一 = 规范 §08 那句**:补支撑或降级成现有契约说得出口的样子都行,
**唯一不许照原样实现** —— 那会做出一个看起来在工作、其实观察不到任何东西的假成功面。

| # | 缺口 | 本版处置 |
|---|---|---|
| 1 | 「已在券商挂 -5% 条件单」勾选刷新即丢(规范 §08-1) | ✅ **补支撑**:按 `positionId` 落 UserDefaults + 记勾选时刻(`NKStopOrderLedger`)。⛔ 不写服务端 —— 它记的是「我在券商那边做了什么」,不是系统能核实的事实 |
| 2 | 报告未生成时的**批算进度**(规范 §08-2) | ✅ **降级**:只画**排程表**(timer 固定时刻)并当面标「排程表 · 非实时状态」+ 一段折叠说清「⛔ 不是进度条」。要实时得先加一个不依赖报告快照的端点 |
| 3 | 🆕 **六关宫格里的「判不出」是格级的** —— 冻结卡 `mech_breakdown_json.gates` **没有逐关 `available`**(服务端对判不出的关发的是 `VERDICT_PASS + available=False + blocks_t1=True`,而 `tier.py::_gate_breakdown` 只落 `verdicts` 与**篮级** `blocks_t1`) | ✅ **降级**:格级只画 pass/degrade/reject,缺键 → 「未记录」灰格(⛔ 绝不渲染成「过」);「某一关判不出」由**篮级**那句文案承载,⛔ 不猜是哪一格。要格级得服务端先把逐关 `available` 落进冻结卡 |
| 4 | 🆕 **「占总仓 %」的分母不在契约里** —— 唯一源是服务端 `Settings.total_capital`(默认 12 万,`.env` 可覆盖),`schemas.py`/`app.py` **零出现** | ✅ **降级**:持仓页只显示**金额**(合计成本 / 合计浮盈),⛔ 不显示占比。在客户端写死 12 万 = 给一个钉死的领域常量造第二份事实源,用户改了 `.env` 界面就会一直说谎 |

### ①~⑥ 六批(全部完工)

- **批 1 地基**:`NKFont` 收成**八档 + 两数字档**(删掉 9.5 档)· `NKSpace`/`NKRadius` 分 macOS/iOS 两档 + 分层密度(概览宽松 / 明细紧凑)· macOS 画布 `pageBg` 改深一档 `#F6F6F8`(白卡 + hairline 描边,**⛔ 不加阴影**)· `AppIcon.appiconset` 整目录替换 · `NKLogo` 换红橙 + 图标同一条折线。**色令牌一个没加、一个没改**;品牌色改用既有 `NK.alertGrad`(**已知代价**:那两条红橙横幅的独占性会弱一点,用户拍板接受)。
- **批 2 五个新组件 + 审计视图**:`NKDisclosure`(披露折叠,收起一行必须说清有几条、什么性质;异常态琥珀圈)· `NKStopScale`/`NKStopScaleCard`(止损刻度尺,破线整卡描红)· `GateLightBar` 改 7px 点(**机械关方角 + 外环 / 证据关圆点**)+ 新 `GateGrid`(macOS 6 列 / iOS 3×2,机械关实心边框 + 「硬」角标)· `NKMemberCard`(**只有标题行是开关**)· `NKToolbar`(macOS 50px)· `NKAuditSection`/`NKAuditGroup`(承接下沉的 `NKJSONTable`)。
- **批 3 macOS 壳**:240px 玻璃侧栏**整个删掉** → 50px 统一工具栏(三个板块胶囊 + 齿轮沉右端,**齿轮 ⛔ 不做成第四个胶囊**)· 新 `NKSplitLayout`(列表栏 **376 固定** + 详情栏自适应,四板块共用一套滚动/页边距实现)· 通栏刹车条上移到壳(`RetreatBrakeBar`,**⛔ 不进卡片流**;`BasketDailyView`/`PositionsView` 里那两条删掉)· 篮子卡由 700×780 `.sheet` 改**右详情栏**(iOS 保留 sheet)。**⛔ 内容不再硬性居中 900px。**
- **批 4 四板块重排 + 字号迁移**:选股 = 概览/篮子列表(**成员直接列在行下面**)+ 篮子卡详情;持仓 = 仓位列表 + 详情(**刻度尺是主角**);复盘 = 五页列表(**每行带「这一页答什么」** —— 用户说的「五个页签分不清」的解法)+ 页内容;设置 = 四组。**455 个裸 `.system(size:)` → 60**(余下全是图标尺寸 + 令牌定义本身)。
- **批 5 iOS**:推入式持仓详情 + **动作按钮吸底**(盘中单手够得到)· 选股概览压一张卡 · 复盘段控下恒跟一行「这一页答什么」· **Provider 注册表与任务路由不下放**(配置动作留桌面),并在设置里**把这件事说出口**(⛔ 不留白让用户以为少了功能)。
- **批 6 收尾**:两处施工侧决断落地(见 〇b)· **§七 P3-47 结案**(复盘每日页日期选择器,复用 `GET /report?date=`;**另存一份快照,⛔ 不覆盖 `report`** —— 那是全 App 的"今天",换掉它用户切回选股会看到三天前的篮子却毫不知情)· `nkDroppedReasonLabel` 拆「主句 / 补充句」两级 + **`bad`(系统缺席)加实心徽标拎出来**(⛔ 码字面一字未改)· 版号三处同升 · `circuit` 删键。

### ⑦ 验收(2026-08-10 完工时实测)

**Python 全量 3581 passed / 3 skipped / 0 failed**(基线 3579,+2 = `_FORBIDDEN_IN_CLIENT` 新增的两枚针脚)·
**双端 `BUILD SUCCEEDED`** · **iOS Simulator `NecklineTests` 211 tests / 0 failures**(基线 214,−3 = 随两个 DTO 删除的三条测试)。
**iOS 截图核对**(演示库 + 本地 `DB_PATH` 隔离后端,⛔ 未碰 `data/neckline.db`):六关宫格的机械/证据二分、
披露折叠展开态、成员卡三层(判定徽标 / LLM 理由 / 四项等宽读数)、篮子行下的成员列表、复盘日期选择器 —— 均已实拍确认。
**截图期逮到并当场修的两处**:① `NKChip` 空文案渲染成**一枚没字的灰胶囊**(现改为整枚不画);
② 成员卡收起行在 iPhone 宽度下把名称挤成两行、判定徽标压成**竖排单字**(现 iOS 分两行、macOS 仍一行)。

**🔴 macOS 视觉核对未做**:用户的**正式 macOS App 正在运行**(`/Applications/Neckline.app`,同 bundle id)——
启动本次 Debug 构建只会被 LaunchServices 切到**那个还在跑的 2.2.0**,截出来的是旧版、比没有截图更误导;
全屏 `screencapture` 又会拍到用户桌面。故按 CLAUDE.md 既定兜底走「iOS 截图 + 双端 BUILD SUCCEEDED」等价证据,
**macOS 三区布局待用户换包后自行过目**。

### ⑧ 部署(未做,交用户决定时机)

**本版没有任何东西上产**:服务端两处改动(版号一行 + `circuit` 删键)与 macOS/iOS 换包**都还没做**。
⚠ 服务端那两处**必须与客户端换包同批上**:`circuit` 删键本身对老客户端零风险(历代都没解过这个键),
但版号三处恒等由守门单测锁着,分批上会让「服务端说 2.2.0 / App 说 2.3.0」持续更久。

## 五(旧-V2.2.0)、V2.2.0 施工图(2026-08-09 立项 · **2026-08-10 六块全部上产完工**)

> **一句话**:把 K8 装进已经建好的房子 —— 选股从「五维加权排序」换成「**行情状态 → 六道关口门槛制 → 落地起跳位置关**」,单包制换成「**一条骨架线 + C/Z/Y 三条引擎线并跑**」,复盘从「一份日复盘」拆成「**选股时钟 / 交易时钟**双时钟」,章程按 K8 持仓原则改一次(🔴 高危独立批)。
> **来源**:`K8_STRATEGY_ARCH.md`(= 用户 2026-08-09 亲手转交的 `whynotme/K8.md` 入仓副本,`K8-V0.5` + 引擎 C1/Z1/Y1,**原文一字未改**)。用户原话:**K8 没有讨论空间,新玩法全部按 K8 来**。
> **地基**:V2.1 批 1 六块已上产(三板块 IA / 复盘板块 / 引擎两档化 / 问询台整链退役 / 百分制打分卡 / 契约守门收口),**全部是本版的地基**;V2.1 批 2 作废、批 3 并入本版第 ④ 块(§五(旧-V2.1.0) 存根)。
> **边界**:前端视觉大整改**继续顺延 v2.3**(本版前端只做 K8 必需的信息呈现);⛔ 本版不碰 `packs/K7-pack.json` 与 `selection_packs` 里 `K7-pack-v1` 那一行的**内容**(只清 `is_active`,留档)。

### 〇、已拍板的设计前提(2026-08-09 用户逐条裁定,**施工时不得重开**)

| # | 议题 | 裁定 |
|---|---|---|
| 1 | K8 权威性 | **全按 K8,无讨论空间**;K8 内部版本记 `K8-V0.5` 防与系统 v 字头撞名。**三条版本线**:系统 `v` / 纪律章程 / K8 引擎 `C`·`Z`·`Y` **各自独立**(命名规则见 `K8_STRATEGY_ARCH.md` 文件头) |
| 2 | V2.1 残批 | **批 2(`K7-pack-v2` 发版激活)作废**——K8 换选股哲学与包模型,不为将死的包走一次高危四道闸;**批 3(周度 unit)并入本版第 ④ 块** |
| 3 | 版本定位 | **V2.2 = K8 落地;V2.3 = 前端视觉整改**(继续顺延) |
| 4 | 零冷启动 | **直接做到位**:C1/Z1/Y1 定量阈值由**工程侧把 K8 定性边界翻译成首版**,每条登记来源与依据、标「工程首版待时钟数据校准」,**⛔ 不冒充审计结论**;迭代走 K8 自己的引擎机制(时钟攒正确率 → 复盘四分类 → 用户确认 → C2/Z2/Y2) |
| 5 | 章程 | **按 K8 持仓原则修订**(独立高危批):止盈机械比例退役 / 时间退出让位主观换股权 / −5% 由强制条件单改为「止损警戒 + 离场决策」;走 `activate_charter.py` 四道闸 + staged;**照 v1.3 先例全文风险登记不得删**(含 §1.3「破 −5% 未止损占亏损 85%」历史归因),**纪律责任自此转移到用户盘中执行** |

**第二轮裁定(2026-08-09 同日,planner 交回五条遗留点后用户逐条拍板;同样不得重开)**

| # | 议题 | 裁定 |
|---|---|---|
| 6 | 六道关口二分 | ✅ **照 planner 的拆法**:机械关(市场 / 板块 / 位置)**硬否决**、证据关(驱动 / 核心 / 证据)**只降级**;LLM 侧的「退出」收窄为「**退出正式候选、仍在 ③b 列名**」。落 ③-A / §2.9-C |
| 7 | 行情状态第五维 | ✅ **正确率只推状态、永不改规则**;前视锁 + 权限锁照写。落 ② / §2.9-C-5 |
| 8 | 🔴 **熔断** | **整体删除**。用户原话:「**我不需要你替我做决定;这个程序永远是提醒 —— 连续三笔止损真的发生了,那也是提醒**」。语义 = **锁定态 / 次日只减不加 / 强制复盘解锁 三件机制全删**;「连续 3 笔止损」这个**事件本身**保留为**纯提醒**(一条推送 + 一条看板事件,**零状态、零锁、零行为改变**)。⑤ 因此扩为「章程修订 + 熔断退役」;planner 上一轮裁的「熔断锚点」问题**随整体删除作废** |
| 9 | 混引擎篮子 | **搁置不采用 → 单篮子单引擎**(修法必须可搁置):③-E 归因简化为**篮子级引擎标注、成员继承篮子引擎**;未来要开混引擎 **升 `K8-V` 骨架版本再议**(留门一句,不预建机制) |
| 10 | P3-49 位置关 | ✅ **按 K8 走,「信实盘别信回测」**。两条义务保留(100 个结案篮子前不称有效 / 结论无论正负都上报);**判据来源写死 = 选股时钟实盘数据**,⛔ **不为此立回测战役** |

**第三轮裁定(2026-08-09 施工中,用户推翻自己的裁定 #4 在位置关上的适用;同样不得重开)**

| # | 议题 | 裁定 |
|---|---|---|
| 12 | 🔴 **核心关也不要机械闸** | 承 #11 同款。**触发**:核心关判据 `leader_rs_rank ≤ 3` 取数自**簇内**口径(簇 = 当天一起涨停且同行业/概念的 ≥2 只票),生产实测全市场**仅 75 只票**有该行(**1.4%**)。**根子上的矛盾**:K8 三引擎找的都是「还没怎么涨、刚要动」的票,这道关的入场券却是「今天必须涨停」——**涨停是结果,K8 要的是结果之前的那一刻**。⚠ **不是阈值定严了**(`≤3` 是 H10 `audited`),**错的是尺子的取数域**。**三条**:(a) **`≤3` 退出闸门**,簇内名次降为补充读数、缺席不挡;(b) **⛔ 不要任何机械低保**(含「行业内前 X%」);(c) **要「龙头」不是 K8 的「容量核心」**(二者常非同一只票,⛔ 别把市值/容量类的量加回来)。**「那一群」= 行业**(一对一、100% 覆盖;⛔ 不用概念板块)。全文落 ③-C2 |
| 11 | 🔴 **位置关不要机械层** | 用户原话:「**推翻,不要搞这个机械层了…其实对于 LLM 来说,它能够完全决定的。所以说这个地方的判定直接给到大模型。**」**触发**:K8 §二 对「落地起跳」只有五句定性、零个数字;裁定 #4 授权工程侧翻译成首版定量,planner 遂写出十二个阈值 / 13 个子门 —— 施工后实测**连乘交集近乎为空**(`liftoff_confirmed` 全市场当日 1~2 / 5526,14 个 D0 回放**零 T1**)。**三条落点**:**(a) 范围只限位置关**(市场关 / 板块关维持机械硬否决 —— 它们读的是已预计算的客观量,不是拍出来的阈值);**(b) 位置关 = 证据关、只降级不除名**(§2.0 第〇原则第 4 锁「LLM 不做闸门」**因此完好无损**,这是用户在两条自己的裁定冲突时选的那一边);**(c) 保留读数、删阈值与四态**(机械层只算事实、不下结论)。全文落 ③-C(原文已折叠留档、⛔ 作废) |

### 〇b、本版最大的不同:**换心脏,不是换参数**(⛔ 全版通读一次再动手)

V2.1 是删东西(问询台 / T3)与搬展示,回滚只需 `git checkout`。**V2.2 改的是判定本身**,以下六条是本版铁律:

1. **仍是对一台活着的生产系统做在线升级**(承 V2.1 〇b):本地施工全做完、全量测试绿、双端 build 绿之后才进 ⑦ 分批部署;⛔ 不许「改一点传一点」。
2. **`ENGINE_API_VERSION` 本版 1 → 2,两个回滚锚随之作废** —— 故 **⛔ 不许再写「回滚 = 激活旧包」**(那会变成一条假回滚绳,比没有更危险)。批 2 起的回滚绳一律是「**代码 commit 回滚 + DB 双备份还原**」,写死在 ⑦。
3. **零删键**(承 V2.1 铁律 4,理由未变:用户 iPhone 何时换包不可控):本版只**新增可选键**、只**新增端点**;任何人想顺手删一个老键 —— 那要走「本版客户端先改 `decodeIfPresent` → **下一版**服务端才删」两步,本版不做。
4. **涉常驻 API(`neckline.service`)的重启只在收盘后**:窗口 = **交易日 15:05–15:55 或 17:15 之后,或周末全天**(避开周六 09:00 周度 unit);⛔ 永不在 16:00–17:00。
5. **两张新的全市场级预计算表是 P0-23 的正面靶心**(`landing_metrics_daily` 逐票 / `market_regime_daily` 逐日):**在线路径只读、EOD 预计算落表**,且上生产前必须在生产机上**隔离实测计时 + 量峰值**(收盘后 15:00 之后、避开 16:00–17:00、串行、`load > 4` 立即停手),⛔ 别拿常驻 `neckline.service` 当小白鼠。
   🔴 **⚠ 2026-08-09 实操更正:在 nk 上必须用 `systemd-run --unit=… --property=User=neckline --property=Group=neckline …` 的瞬态 service,⛔ 不许用 root 的 `--scope`**(会把行情文件写成 root 属主、导致服务写入失败,`NB_info.md` 登记)。⚠ 另:`Memory peak` 读数不可轻信(cgroup 对已在页缓存的文件页不重复计费),**要可信上界就压低 `MemoryMax` 反证**。
6. **章程 + 熔断批(⑤)与选股批(②③)之间必须留出观察窗** —— 两批同时上 = 出了问题分不清是「K8 选的票不行」还是「新退出规则不行」。⑦ 把它们排在不同批、各留一个完整交易日,**这不是排期偏好,是归因的前提**。
7. 🆕 **熔断整体删除后,系统对「亏下去」这件事只剩提醒,没有任何状态** —— 这是裁定 #8 的字面结果,**不是遗漏**。⛔ 施工时不许「为了安全」偷偷留一个锁定标志、一个灰化按钮、或一个「建议今天别开仓」的自动状态位:**用户明确说了不要程序替他做决定。** 留下的只有一条事件与一条推送。

### 〇c、红线(承 V2 / V2.1,本版逐条仍有效 + 两条本版新增)

- **§2.0 第〇原则四锁 + §2.8-C 的 V2 精确表述**一字不动。**本版新增的关口判定必须服从它**:见 ③-A「机械关 / 证据关」二分裁定。
- **纪律章程(`strategy_versions`)与 K8 选股版本线(`selection_packs`)两套表、两套激活流程,永不混用**;本版两边都动,**但分在两个批次、走各自的切换器**,⛔ 不许写一个「一键升级」脚本把两者串起来。
- **删表 / 停用一律「停写留档不 DROP」**(§七 P4-31 七张表清单不变;`K7-pack-v1` 与 `K4-pack-v1` 两行同理**只清 `is_active`、不删行**)。
- **诚实披露体例不许退化**:「没有」与「没看」永远分开(`available` + `unavailableReason` 双位);⛔ 不许在读侧遮蔽。**本版三处新落点**:行情状态缺行、位置关缺行、选股时钟未结案。
- 🆕 **「引擎并跑」体现在判据分支,⛔ 不体现在 LLM 调用次数上**(见附「成本与超时算术」)。三引擎各跑一遍 LLM 是本版最容易犯、也最贵的错。
- 🆕 **K8 的位置关落在策略线已判死的域里,本版只产注意力分层、⛔ 不得被读成买入期望背书**(§3.8-(b) 一字不变)。理由与义务见 ③-C 的「雷区对照」段与 §七 **P3-49**。

---

### ① 引擎注册表:一条骨架线 + C/Z/Y 三条引擎线(🔴 本版地基,先做)

**目标**:`selection_packs` 从「单包制」升级成「**多版本线注册表**」——骨架 `V` 一条 + 引擎 `C`/`Z`/`Y` 各一条,**每条线独立版本、独立激活、独立运行 / 停止**;历史归因永久保留当时版本(K8 §四「引擎版本」原文)。

**为什么复用 `selection_packs` 而不新建表(裁定,⛔ 不重开)**:① 四道闸 / append-only / 激活事件流 / 按版本归因分层,四套机关全部现成,平移成本最低;② 新建第二张表 = 第三套激活流程,与「两条版本线不许混用」这条纪律直接打架(会变成三套,后人必混);③ 两个历史包仍在同一张表里,**留档与查证路径不破**。

**涉及文件**:`neckline/db.py`(两列迁移 + 索引重建)、`neckline/selection/pack.py`(读侧 + schema)、`neckline/selection/engine_api.py`(bump)、`scripts/activate_pack.py`(闸 1/2/3 扩)、新增 `packs/K8-skeleton.json` / `packs/C1.json` / `packs/Z1.json` / `packs/Y1.json`、新增 `scripts/oneoff/retire_legacy_packs.py`。

**表与契约变更**

- `selection_packs` 幂等补两列(`_COLUMN_MIGRATIONS`):
  - `line_code TEXT NOT NULL DEFAULT 'LEGACY'` ∈ `{V, C, Z, Y, LEGACY}`(历史两行 = `LEGACY`,靠 DEFAULT 天然落位)。
  - `status TEXT NOT NULL DEFAULT 'running'` ∈ `{running, stopped}`(K8 §四「引擎状态」:停止 = 不产候选,**保留历史版本与复盘数据**)。
- 唯一现役约束**改口径**:`idx_selection_packs_single_active` **DROP → 重建**为
  `CREATE UNIQUE INDEX IF NOT EXISTS idx_selection_packs_single_active_per_line ON selection_packs(line_code, is_active) WHERE is_active = 1`。
  - ⚠ **重建顺序铁律**:必须**先**由一次性脚本把 `LEGACY` 行的 `is_active` 清零,**再**建新索引。生产现役是 `K7-pack-v1`,顺序反了新索引照样能建(单行不冲突),但接下来激活骨架线时会出现「LEGACY 与 V 两行同时 `is_active=1`」——两行都合法、`get_active_*` 各取各的,**这正是 V2 契约线审计 🔵 B3 要防的「今天用的是哪个包看运气」**。
  - ⚠ 建索引仍走既有 `try/except IntegrityError` 体例(**脏库不许被开机脚本锁死**,`init_schema` 被 API / 哨兵 / 报告全线调用)。
  - ⚠ `init_schema` **只建表建列建索引,⛔ 不改业务行** —— 清 `is_active` 是业务动作,必须住一次性脚本(既有纪律)。
- `scripts/oneoff/retire_legacy_packs.py`:默认演练、`--confirm` 才写、双备份;把 `line_code='LEGACY'` 的行 `is_active → 0`,并在 `selection_pack_activation_log` 追加一条 `action='deactivate'` / `via='cli'` / `note='V2.2 K8 换线,LEGACY 包退役留档'`。**⛔ 不 DELETE 任何行。**

**读侧 API(定死,⛔ 别自创第五个入口)**

- `pack.get_active_line(line_code) -> Pack | None` —— 唯一实现。
- `pack.get_active_skeleton() -> Pack | None` = `get_active_line("V")`。
- `pack.get_active_engines() -> Dict[str, Pack]` —— `line_code ∈ {C,Z,Y}` 且 `is_active=1` 且 **`status='running'`**,返回按 `C → Z → Y` **确定性排序**的有序字典(⛔ 不靠 SQL 行序)。
- `pack.get_active_pack()` **保留,改成 `get_active_line("V")` 的薄封装**,docstring 写明「K8 起返回**骨架线**现役行」。
  - 🔴 **为什么这样映射恰好正确、且现有调用方零改动**:K8 §一 明写「系统骨架管理**股票池、篮子、梯度**」——而现有包 config 的两段正是 `seeds`(股票池 + 种子资格)与 `tier`(梯度容量 / 权重 / 质量线)。故 `scan/seeds.py` 与 `selection/tier.py` 这两个既有消费方**继续读 `get_active_pack()`,语义反而更准了**。⛔ 别顺手把它们改成读引擎线 —— 引擎线里根本没有 `seeds`/`tier` 段。
- **无骨架线现役 = 当日不产任何种子**(承 `get_active_pack()` 既有 docstring 的 fail-loud-的-对立面是-如实披露);**无运行引擎 = 当日不产任何候选**,`basket_stage_handoff` 落一行 `no_running_engine`(承 P0-39:零产出必须另有一处段状态可查)。

**包 schema 扩展(骨架线 / 引擎线两套,⛔ 不混)**

- **骨架线**(`line_code='V'`,`pack_version='K8-V0.5'`):`config` = 既有 `seeds` + `tier` 两段,**结构一字不改**。本版只改值:
  - `seeds.stock_hygiene.allowed_boards`:`["MAIN","GEM","STAR"]` → **`["MAIN","GEM"]`**(K8 §三「排除科创板」,**纯包配置、零代码改动**)。
  - `seeds.industry_blacklist.industries = ["白酒"]`(K8 §三「排除白酒」,**新原语**,见下)。
  - `seeds.stock_hygiene.close_min` **维持 2.0**、**全仓无价格上限**(K8 §三「股价不限,允许 100 元及以上」= **确认现状、零改动**;已核实 `stock_hygiene` 与 `intel_candidates` 均无 `close_max`)。⚠ 在包注释里写死「**刻意无上限**」,防后人「顺手加一个」。
  - 🔴 **与 §2.2 v1.3.3「拆墙」的关系(极易搞混,写死)**:拆墙拆的是**纪律判定域**(`forbid_high_elasticity`,住**章程**);本次收的是**选股生成域**(住**包**)。**两条线互不影响** —— 章程的 `forbid_high_elasticity=False` **一字不动**,否则用户买一笔科创板会被周复盘判违纪。**K8 只说「不选」,没说「不许买」。**
- **引擎线**(`line_code ∈ {C,Z,Y}`,`pack_version ∈ {C1,Z1,Y1}`):`config` = **新增 `engine` 段**(骨架线不许出现该段,反之亦然,schema 交叉校验):
  ```
  config.engine = {
    "engine_code": "C",                 # 必须与 line_code 逐位相等(闸 1 交叉校验)
    "applies_to": "主线核心健康回调再启动",  # 人话,不进任何判据
    "gates": { "market": {...}, "sector": {...}, "position": {...}, "core": {...}, "evidence": {...} },
    "tier_evidence": { "t1": {...}, "t2": {...} }
  }
  ```
  - `gates.*` 与 `tier_evidence.*` 的每个键都必须引用**已注册原语**(白名单制照旧);要新玩法先加原语(§七 [机制] 已登记的既有代价)。
- 🆕 **`provenance` 强制字段(把裁定 #4 落成机器判据)**:引擎包里**每一个阈值**必须带来源标注,二选一:
  - `"source": "audited"` + `"ref"`(指向 `research/*.md` 具体一节)—— 有策略线审计背书的量(如 `leader_rs_rank` 名次档 ← H10 十二格、`industry_stage` 五态 ← H11)。
  - `"source": "engineering_v1"` + `"basis"`(一句话:从 K8 哪句定性边界翻译来的)+ `"calibration": "pending"` —— 工程首版。
  - **闸 1 校验:缺 `provenance` 即拒绝激活。** 🔴 这条的全部意义是**让"不冒充审计结论"变成过不去的闸,而不是一句自觉**。

**`engine_api.ENGINE_API_VERSION` 本版 `1 → 2`(🔴 裁定,理由写死)**

按 ③-K7 定下的判定规则(「旧包原样重新校验仍通过 **且** `get_active_pack()` 对旧包行为逐位不变」→ 才不 bump):第一条成立(schema 只加不减),**第二条不成立** —— `get_active_pack()` 语义已改为取 `line_code='V'` 现役行,`K7-pack-v1` 是 `LEGACY` 行,**不再会被返回**。故必须 bump。

- **代价(如实登记,⛔ 不粉饰)**:`is_compatible()` 是逐位相等判据 → **`K4-pack-v1` / `K7-pack-v1` 两个回滚锚当场作废**,任何激活尝试被闸 2 硬拒。
- **这正是我们要的**:V2.2 是换心脏,那两个包在新引擎下**确实**不能用;让闸说真话,好过留一条自己都不信的回滚绳。**回滚改走「代码 commit + DB 备份」**(⑦ 写死)。
- 两行**留在表里不删**(留档 + 查证 + 历史报告的 `pack_version` 指纹仍指得到)。

**新原语(1 个)**

`industry_blacklist(row, *, industries: Sequence[str] = ())` —— 按 `stock_basic.industry` **精确归类**排除(继承 LinoN 教训:白酒用 industry 精确归类,**非名称关键词**);`industry` 属「身份 / 分类数据」,**不进 `_ALLOWED_FEATURES`**(既有约定,同 `board`/`is_st`)。
- ⚠ **施工时先跑一句 SQL 核实 TuShare `stock_basic.industry` 里白酒的真实取值**(可能是「白酒」也可能是「酿酒」或两者并存),把实测取值写进包 + 完工记录;⛔ 别照 plan 字面写死一个没核过的中文串 —— 写错了的表现是**静默不排除**。

**顺带清掉的三张老账(本版正好碰到,成本极低)**

- **§七 P2-47**(选股域与回测域卫生线判据双源,无运行时纽带)——**本块结案**。K8 改股票池(排科创 / 排白酒)正好动的就是这条缝,不修则一改必漂。修法取**首选**:抽共享常量(`selection/primitives.py::_stock_hygiene` 的默认值与 `neckline/research/panel.py::base_universe_expr` 指向同一处),做不到再退守两处逐位对拍守门。
- **§七 P4-48**(测试隔离经 `db_path=None` 兜底静默写真实开发库)——**本块结案**。修法取**治类不治例**:conftest 全局兜底重定向。⚠ **验收铁律:「MD5 没变」不等于没泄漏**(幂等写照样是泄漏),必须用 A8 的 `sqlite3.connect` 探针法重跑一次全量套件、命中集为空才算。
- **§七 P1-46**(两条周末日期炸弹测试)——**本块顺带修**(A7 冻结时钟 `frozen_clock` fixture 体例照搬)。本版要长期跑全量套件,留着这两条红会持续磨损「零失败」的信噪比。

**测试与守门**

- `tests/test_selection_pack.py`:每线各自唯一现役(同线两行 `is_active=1` → 库级 IntegrityError)、跨线可并存、`status='stopped'` 的引擎线**不出现在 `get_active_engines()`**、`get_active_pack() is get_active_line("V")` 行为等价。
- **反向守门**:`ENGINE_API_VERSION == 2`;`is_compatible({"engine_api_version": 1})` 为 **False**;拿仓库里真的 `packs/K7-pack.json` 走闸 2 **必须被拒**(把「回滚锚已作废」钉成机器判据,⛔ 不留一条自己都不信的绳)。
- `tests/test_activate_pack_script.py`:骨架包 / 三个引擎包各走一次四道闸**演练**全绿;`engine.engine_code ≠ line_code` 被闸 1 拒;**任一阈值缺 `provenance` 被闸 1 拒**(正反两例)。
- `tests/test_selection_primitives.py`:`industry_blacklist` 命中 / 未命中 / `industry` 缺失(**缺失保守判通过**,不臆造「反正不是白酒」——与 `non_new_stock` 的保守方向刻意相反,理由:黑名单缺数时排除会误杀,白名单缺数时放行会漏网,两者代价不对称)。
- `tests/test_db_isolation_guardrail.py`:A8 探针法零命中(P4-48 的机器判据)。

**依赖与部署批次**:无前置。→ **批 1**(纯 schema + 读侧 + 包文件,**不激活任何新线**,行为逐位不变)。

**验收**:`init_schema` 幂等跑后表数 +0 / 列 +2 / 索引换一条;`retire_legacy_packs.py` 演练与 `--confirm` 各跑一次、库 sha256 与备份齐;四个新包文件走演练四道闸全绿且**一个都还没激活**;全量套件绿且开发库 A8 探针零命中。

---

### ② 行情状态层:D0 盘后三态(K8 §一「行情状态层」)

**目标**:每个交易日盘后判定 **趋势延续 / 高位分歧 / 切换确认** 三态之一并落表,**选股、持仓、复盘三处共读同一张表**(K8 原文);判定全机械、可复现、可回放。

**涉及文件**:新增 `neckline/scan/regime.py`(唯一判定实现)+ `neckline/scan/regime_store.py`(读写)、`neckline/db.py`(新表)、`neckline/report/evening.py`(挂进 SEG_SCAN)、`scripts/scan_layer.py`(CLI)、`neckline/api/app.py`(只读端点)。

**表**(EOD 预计算落表、在线只读,**P0-23 纪律**)

```
CREATE TABLE market_regime_daily (
  trade_date        TEXT PRIMARY KEY,
  regime            TEXT NOT NULL,   -- trend_continuation | high_divergence | rotation_confirmed
  regime_reason     TEXT NOT NULL,   -- 机器可读原因码串(逐条判据留痕)
  inputs_json       TEXT NOT NULL,   -- 五维原始读数(每维带 available/unavailable_reason)
  strengthening_json TEXT NOT NULL,  -- 增强方向(行业/概念 + 依据)
  weakening_json    TEXT NOT NULL,   -- 减弱方向
  skeleton_version  TEXT NOT NULL,   -- 口径指纹(阈值住骨架包)
  computed_at       TEXT NOT NULL
);
```

**五维输入(全部读已有数据源,⛔ 零新增 TuShare 调用)**

| 维 | K8 原文 | 数据源(现成) |
|---|---|---|
| 1 核心强度 | 「主线核心」 | 昨日 T1/T2 篮子**核心角色成员**今日 RS(`basket_members` + `daily`)+ 连板梯队最高高度(`report/sentiment.py`) |
| 2 板块广度 | 「板块广度」 | 强度行业数 ÷ 总行业数(`industry_strength_daily`)+ 涨停家数(`limit_derived`) |
| 3 相对强弱 | 「旧主线持续转弱,新方向形成相对强势」 | 旧主线行业 vs 候选新方向行业的 5 日中位收益差(`industry_strength_daily.median_ret` 序列) |
| 4 资金迁移 | 「资金开始迁移」 | 行业维度主力净额排名变化(`moneyflow_dc` + `report/sector_moneyflow.py`) |
| 5 近期 T1/T2 正确率 | 「近期 T1、T2 正确率」 | **只读已结案的选股时钟样本**(④ 的 `selection_clock`;④ 上线前退化读 `basket_verification` 的 EOD 定论行) |

**🔴 第 5 维的两道锁(✅ 2026-08-09 用户裁定 #7 已拍板:**正确率只推状态、永不改规则**。K8 文本内部有一个反馈环,必须在这里断开,⛔ 不许含糊)**

K8 §一 把「近期 T1、T2 正确率」列为行情状态的判断依据,而选股时钟的验证内容第 1 项又是「D0 行情状态」—— **这是一个环**。两道锁:

1. **前视锁**:第 5 维**只吃「已结案且结案日 ≤ D0−1」的样本**,窗口 = 最近 10 个交易日。D0 当天的验证根本还没发生,读它就是前视偏差(§3.8 零容忍)。守门单测:往库里塞一行 D0 当天的验证结果,断言 `decide_regime()` 输出**逐位不变**。
2. **权限锁**:第 5 维**只影响 `regime` 三态,⛔ 不改任何引擎阈值、不改任何包、不改任何篮子去留**。改规则的唯一通道仍是 V2.1 裁定 #3 的人工闭环(攒够样本 → 用户带材料去策略台 → 新包 → 四道闸)。守门:`regime.py` **零 import** `neckline.selection.pack` 的任何写入口,AST 断言。
   > ⚠ **这条不与 V2.1 裁定 #3「⛔ 不做任何自动反馈回写选股」冲突**,但**边界只有一线之隔**,故在此写死判据:**「市场现在处于哪一段」是状态,可以由历史统计推出;「选股规则该怎么改」是规则,只能由人改。** 第 5 维落在前者。**若将来有人想让正确率去动阈值 —— 那是后者,须重新拿用户拍板。**

**判定(`decide_regime()`,纯函数,互斥、优先级从上往下、`regime` 恒非 NULL —— 照 `scan/stage.py::decide_stage` 既有体例)**

首版判据(**全部 `source: engineering_v1`,阈值住骨架包 `config.regime`**):

```
rotation_confirmed   旧主线 5 日中位收益 < 0  且  新方向行业 5 日中位收益 > 旧主线 + ROT_GAP(0.03)
                     且 新方向近 3 日至少 2 天为强度日  且 资金迁移排名上升 ≥ ROT_RANK(10 名)
high_divergence      核心强度较 5 日均值下降 ≥ DIV_CORE_DROP(0.30 分位)  或
                     板块广度分位 ≤ DIV_BREADTH(0.35)  且 涨停家数环比降 ≥ DIV_LIMIT_DROP(0.30)
trend_continuation   其余(默认态)
```
- **顺序即优先级**:先判切换、再判分歧、剩下算延续。⚠ **默认态是「延续」而不是「不知道」** —— 这是刻意的:三态里只有「延续」不改变任何门槛(见 ③-B 市场关),把不确定归到最不激进的那一态。
- **缺行 = 不知道,⛔ 不许猜**:`market_regime_daily` 当日无行 → 市场关**不拦**(降级方向 = 不拦)+ 报告与契约 `available=false` + 可读原因(承 P0-39:零产出必须有一处段状态可查)。**⚠ 但「不知道」时该票不得进 T1**(见 ③-D)。
- **每一维各自带 `available` / `unavailable_reason`**,某维缺席时判定照跑并在 `regime_reason` 里记 `missing:<dim>`;**⛔ 不许把缺维当作 0 参与比较**。

**⚠ 与既有 `sentiment.position_quota`(满额 / 半额 / 休息)的关系:两者并存、职责不同、⛔ 不合并**

`position_quota` 回答「明天能下多少仓」(K8 选股顺序第 2 步「市场赚钱效应」的现成实现),自此成为 `market_regime_daily.inputs_json` 的一项输入;`regime` 回答「市场处在哪一段」。守门:`regime.py` 读 `position_quota` 但**不反向改它**(AST 断言零写入)。

**三处消费方**(K8 原文「选股、持仓和复盘共同读取该状态」)

- **选股** = ③ 的第 1 关(市场关),按 regime 分支调门槛。
- **持仓** = 灰色地带的**辅助证据展示位**(K8 §十三「行情转入高位分歧可作辅助证据」)。🔴 **纯展示,⛔ 不触发任何持仓动作、不进任何推送、不改任何纪律判定** —— 守门单测扫 `sentinel/` 零 import `regime_store` 的判定路径。
- **复盘** = 选股时钟验证内容第 1 项 + 周度「行情状态判断准确率」(④)。

**端点**:`GET /api/v1/market-regime`(`date` 缺省最近一日;可选 `from`/`to` 区间)——**只读、零现算、不 404**(空态走 `available=false`),**零新增 reason 字符串**。

**测试与守门**:三态各一例 + 默认态 + 缺维 + 缺行五态;前视锁(塞 D0 当天验证行,输出逐位不变);权限锁(AST 零写包);`sentinel/` 零判定引用;`inputs_json` 每维 `available` 双位齐;端点空态 200。

**依赖与部署批次**:依赖 ①(阈值住骨架包)。→ **批 1**(新表 + 只读端点,**不接进任何判定**)。⚠ 接进市场关是 ③ 的事,在**批 2**。

**验收**:任取近 20 个交易日回放,每天都能说出三态之一 + 逐条原因码 + 五维各自「有 / 没有 / 没取到」;`sentinel/` 与 `strategy/` 全仓零判定引用。

---

### ③ 六道关口管线 + 落地起跳位置关 + 三引擎首版阈值(🔴 换心脏)

> 🔴 **2026-08-09 施工中被用户裁定 #11 局部推翻**:**位置关由机械关改判为证据关**,机械层只出读数、
> 判定交 LLM(只降级不除名)。**③-A / ③-B / ③-C / ③-D / ③-F 已按此改写**;③-C 原文折叠留档、⛔ 作废。
> **市场关与板块关不受影响,仍是机械硬否决。**

**目标**:选股主链从「五维加权分 + 质量线定档」换成 **K8 §五 六道关口的门槛制**;新增 **位置关**(🔴 裁定 #11 后 = **LLM 判定 + 机械读数**,不再是机械判据族);每票标注**唯一主引擎 + 准确版本**(K8 §四)。

**涉及文件**:新增 `neckline/selection/gates.py`(六关唯一实现)、`neckline/scan/landing.py` + `landing_store.py`(位置关**读数**预计算,🔴 零判定)、`neckline/db.py`(两张新表);改 `neckline/selection/aggregate.py`(成员选择接关口 + 引擎归属)、`neckline/selection/tier.py`(定档改门槛制 + 机械分降级为档内排序)、`neckline/selection/basket_store.py`、`neckline/report/evening.py`、`neckline/report/basket_daily.py` / `render.py`(报告十项产物)。

#### ③-A 关口二分裁定(✅ **2026-08-09 用户裁定 #6 已拍板照此办**,理由写死、⛔ 不重开)

**六关拆两类**:

| 类 | 关 | 判定者 | 不过的后果 |
|---|---|---|---|
| **机械关** | ① 市场关 · ③ 板块关 | 全机械、可复现、可回放 | **硬否决**:退出正式候选,落 `gate_evaluations` 留痕 |
| **证据关** | ② 驱动关 · ④ 核心关 · ⑥ 证据关 · 🆕 ⑤ **位置关** | LLM 组织证据 + 机械最低标注 | **只降级**:T1 → T2 → 退出正式候选(**仍在 ③b 未定档披露里列名**) |

> 🔴 **2026-08-09 用户裁定 #11:位置关由机械关改判为证据关(推翻裁定 #4 在位置关上的适用)**。
> 上表原为「机械关 = 市场 / 板块 / **位置**」,现位置关移入证据关。**⛔ 不得改回。** 全文见 ③-C。

🔴 **为什么必须这么拆**:K8 §六 说「门槛制 + LLM 负责组织证据、识别矛盾、比较核心地位、说明结论」,而 §2.0 第〇原则第 4 锁说「**LLM 不做闸门**」。

- 若六关全交 LLM 判过 / 不过 → LLM 成了闸门,与第〇原则**正面冲突**;
- 若六关全要机械 → 驱动关的「逻辑的持续性」、证据关的「来源独立、方向一致」在 EOD 数据上**根本算不出来**,等于把 K8 砍掉一半。

拆两类是同时兑现两边的唯一走法:**机械的归机械**(可复现、可回放、可归因),**定性的归 LLM 但只能降级不能除名**(§2.0 第 3 条「机器不禁、人可复核」原样成立)。

⚠ **与 K8 §六 字面的出入(如实登记,✅ 已交用户并获拍板)**:K8 原文说关键缺口时「候选**降级或退出**」——本裁定把 LLM 侧的「退出」收窄为「**退出正式候选**」(= 不进 T1/T2,但仍在报告 ③b 里列名 / 分 / 原因码)。**票不会从报告里消失。** ⛔ 这不是打折,是把「退出」落到与第〇原则相容的那一层。

#### ③-B 六关逐关定义(机械侧写死,LLM 侧给结构化产出契约)

| 关 | 机械侧判据(骨架 + 引擎阈值) | LLM 侧产出(结构化字段,复用 ⑤ 那一次调用) |
|---|---|---|
| ① 市场关 | 读 `market_regime_daily.regime`,按引擎包 `gates.market` 的三态分支取门槛;缺行 = 不拦但不给 T1 | — |
| ② 驱动关 | 机械低保:驱动必须有 ≥1 条带日期的证据条目(`evidence_status != 'search_unavailable'`) | 四问必答(K8 §五-2):`why_now` / `common_trait` / `persistence` / `strengthen_and_invalidate`;缺任一 → 降级 |
| ③ 板块关 | 簇内协同成员数 ≥ `gates.sector.min_members`;行业强度 / 阶段态按引擎分支;扩散度 = 簇成员近 3 日强度日占比 | 层级描述(龙头 / 核心 / 弹性)—— 与 `leader_structure_daily.role_mech` **对拍**,冲突两说并存(既有机关) |
| ④ 核心关 🔴 **裁定 #12 重写:零阈值,只出读数** | **⛔ 无机械低保、⛔ 无及格线** —— 只出**行业域**读数(见 ③-C2) | **判定在这里**:「这只是不是它那一群的**龙头**」+「为何是这只而不是同题材其他票」 |
| ⑤ 位置关 🔴 **已改判为证据关(裁定 #11)** | **机械侧不判定**,只从 `landing_metrics_daily` 取**原始读数**喂给 LLM(见 ③-C) | **判定在这里**:给 K8 §二 五句 + 引擎 `gates.position.guidance` + 该票读数,产 `position_verdict ∈ {ok, weak, unfit}` + 一句理由;`weak`→降一档、`unfit`→退出正式候选(**仍在 ③b 列名**)。⛔ 复用 `basket_reason` 那一次调用 |
| ⑥ 证据关 | 机械低保:证据条目去重后 ≥ `gates.evidence.min_independent`;**高度相关的技术指标按一份计**(K8 原文)→ 机械侧按 `evidence_kind` 归并,同 kind 只计一份 | 矛盾识别(K8 §六「识别矛盾」):列出彼此打架的证据并说明取舍 |

**落表**:新表 `gate_evaluations`(**append-only**,每候选每关一行)

```
CREATE TABLE gate_evaluations (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  trade_date    TEXT NOT NULL,
  candidate_key TEXT NOT NULL,   -- 篮子候选键(⑤ 的 basket_key 或种子键)
  ts_code       TEXT,            -- NULL = 篮子级关口(市场/驱动/板块/证据),非空 = 成员级(核心/位置)
  gate          TEXT NOT NULL,   -- market|driver|sector|core|position|evidence
  gate_kind     TEXT NOT NULL,   -- mech|llm
  verdict       TEXT NOT NULL,   -- pass|degrade|reject
  score         REAL,            -- 该关的定量读数(可空)
  threshold     REAL,            -- 当次生效阈值(可空)
  engine_code   TEXT,            -- 成员级关口才有
  engine_version TEXT,
  evidence_json TEXT NOT NULL DEFAULT '{}',
  created_at    TEXT NOT NULL
);
CREATE INDEX idx_gate_eval_day ON gate_evaluations(trade_date, gate);
```
- 用途两条:① 报告 ③b「未定档披露」的升级版(现在说得出**卡在哪一关、差多少**);② ④ 周度「各因素对正确率的贡献」(K8 §十六 选股侧第 8 项)的**直接数据源** —— 没有这张表,那一项算不出来。

#### ③-C 位置关 = 落地起跳(🔴 **2026-08-09 用户裁定 #11 整节重写:判定交 LLM,机械层只出读数**)

> **本节 2026-08-09 被用户推翻重写。原文(机械四态 + 十二个阈值)已作废,保留在文末「③-C 原文(已作废)」
> 供查证,⛔ 不得当施工口径、⛔ 不得"恢复"。**

**用户裁定 #11 原话与三条落点(⛔ 不得重开)**

用户原话:「**推翻,不要搞这个机械层了。我们现在在研究这个机械,研究的要死。其实对于 LLM 来说,
它能够完全决定的。所以说这个地方的判定直接给到大模型。**」

**背景(如实登记,这是本版第一次推翻自己的裁定)**:K8 §二 / §五-5 对「落地起跳」**只有五句定性**、
零个数字。裁定 #4(零冷启动)授权「工程侧把 K8 定性边界翻译成首版定量」,planner 据此在立项时
(commit `2a97a3f`)写出十二个阈值、13 个子门。施工完成后实测:**13 个子门连乘的交集近乎为空**
—— `liftoff_confirmed` 全市场当日仅 1~2 / 5526,14 个历史 D0 回放**零 T1**。用户据此判定:
**这一关本就不该量化**,量化出来的不是 K8 的意思。

| # | 裁定 | 落点 |
|---|---|---|
| 11-a | **范围只限位置关** | 市场关(读 `market_regime_daily`)与板块关(读 `industry_strength_daily`/`industry_stage_daily`)**维持机械、维持硬否决** —— 它们读的是**已预计算的客观量**,不是拍出来的判据阈值。⛔ 别顺手一起改 |
| 11-b | **位置关 = 证据关,只降级不除名** | LLM 判位置不佳 → T1 降 T2 → 再降退出正式候选,**票仍在 ③b 列名 + 写明理由**。🔴 **§2.0 第〇原则第 4 锁「LLM 不做闸门」因此完好无损** —— 这是用户在两个自己的裁定冲突时选的那一边,⛔ 不得改成硬否决 |
| 11-c | **保留读数、删阈值与四态** | 机械层**只算原始读数**(事实),**不下任何结论**。四态枚举(`falling`/`landing_pending`/`liftoff_confirmed`/`high_extended`)与十二个阈值**全部删除** |

**机械层剩下什么:只出读数,不出判断**

表改名 `landing_state_daily` → **`landing_metrics_daily`**(⚠ **原表从未上产**,批 1 只部署了 ①②,
生产零行零表 —— 故可干净重构,**不受「停写留档不 DROP」约束**,该红线保护的是已上产数据)。

```
CREATE TABLE landing_metrics_daily (
  trade_date       TEXT NOT NULL,
  ts_code          TEXT NOT NULL,
  metrics_json     TEXT NOT NULL,  -- 原始读数(事实),⛔ 不含任何四态/结论/阈值比较结果
  metrics_missing  TEXT NOT NULL,  -- 哪几项没取到 + 为什么(诚实披露,⛔ 缺数不当 0)
  computed_at      TEXT NOT NULL,
  PRIMARY KEY (trade_date, ts_code)
);
```

`metrics_json` 收 K8 五句话各自对应的**可观测事实**(逐项举例,**全部无阈值、无及格线**):

| K8 那句话 | 机械层给出的读数(事实) |
|---|---|
| 下跌或调整已经结束 | 近 5 日最低价 / 前 20 日区间最低价之比 · 当日是否创 20 日新低 |
| 关键位置形成有效支撑 | `close` 相对 MA20 的偏离 · `close` 相对近 20 日收盘 20% 分位的偏离 |
| 抛压明显衰减 | 近 5 日下跌日均额 ÷ 近 20 日均额 · 近 5 日最大单日跌幅 |
| 价格开始向上转强 | `close` vs MA5 偏离 · 当日涨跌幅 · RS5(相对所属行业中位 5 日超额) |
| 当前仍处于启动早期 | `dist_from_high_60d` · 近 3 日累计涨幅 · 当日是否涨停 · 是否创 60 日新高 · `platform_days`(近 N 日振幅 ≤ X 的连续天数) |

- 🔴 **机械层的唯一职责 = 把数算对、把缺的说清楚。⛔ 任何形如「≥ X 即通过」「属于第几态」的东西
  一律不许出现在 `landing.py` 里** —— 那正是本次被推翻的东西。
- **缺数照旧不猜**:某项算不出 → 进 `metrics_missing` 写明原因,**⛔ 不填 0、不填默认值**
  (喂给 LLM 的必须是「这项没取到」而不是一个假数)。

**LLM 侧(位置关的判定在这里发生)**

- **判定者 = `basket_reason` 那一次调用**(⛔ **不新增任何 LLM 调用,增量仍是 0** —— 附「成本与超时算术」
  五条写死的第 1 条一字不变)。
- prompt 里给三样东西:① K8 §二 五句话原文;② 该引擎的**定性位置准则**(见下 `gates.position.guidance`);
  ③ 该票的 `metrics_json` 读数 + `metrics_missing`。
- 产出结构化字段:`position_verdict ∈ {ok, weak, unfit}` + `position_reason`(一句人话)。
  映射:`ok` → 不降级;`weak` → 降一档;`unfit` → 退出正式候选(**仍在 ③b 列名**)。
- **落 `gate_evaluations`**:`gate='position'` / `gate_kind='llm'` / `verdict ∈ {pass,degrade,reject}`
  / `evidence_json` 收读数与 LLM 理由 —— 归因链不断,④ 周度「各因素对正确率的贡献」照样算得出。

**引擎包 `gates.position` 改为定性准则(⛔ 无数字)**

三个引擎的 `position` 段**删掉全部阈值键**(`t1_landing_states`/`t2_landing_states`/`pullback_depth_range`
/`landing_states`/`dist_from_high_60d_min`/`platform_days_min`/`platform_amplitude_max`),改为单键:

```
"position": { "guidance": "<该引擎位置准则的定性描述,取自 K8 §四 该引擎原文>" }
```

- `guidance` **是定性文本不是阈值** → **不走 `provenance` 闸**(闸 1 白名单里单列),它进 prompt、
  **不进任何机械判据**(同 `applies_to` 的既有体例)。
- 三引擎的位置差别自此由**定性描述 + LLM 判断**承担,⛔ 不再由数字承担。

🔴 **雷区对照(仍然全部有效,必须原样写进 `landing.py` 模块头)** —— ⚠ **判定改由 LLM 做,
不会让这些案底失效**:被 K3 判死的是那个**形状**(升势回撤 + 启动确认),不是"谁来判"。

- 「升势回撤 + 启动确认」这一形状 = **K3-B2 臂③**,被判「**确认信号 = 死猫跳顶点,比直接买更差**」。
- 「只看跌完了没」这一形状 = **K3 系统化超跌反弹四臂全灭**。
- 「排除当日急拉 / 涨停 / 破新高后追入」= **K2「追强势」全否决 + K7-C1 诱多做局**的正面兑现
  (这一半**站在案底同侧**)。
- **本关只产注意力分层,⛔ 不得被读成买入期望背书**(§3.8-(b) 一字不变)。真伪由 K8 自己的选股时钟在
  **前向样本**上回答,**不由本版声称**。义务见 §七 **P3-49**。
- 🆕 **判定交给 LLM 之后,P3-49 的证伪义务不减反增**:现在连"当时按什么标准判的"都不再是一组可回放的
  数字,而是一段模型输出 —— 故 `gate_evaluations.evidence_json` **必须同时存下当次读数与 LLM 理由**,
  否则事后无法复核它到底在拿什么下判断。

**性能(P0-23 靶心,⛔ 不许跳过)**:`landing_metrics_daily` 仍是**全市场逐票 × 60 日回看**的批算
(读数还是要算的,只是不再下结论)。① 取数一律走 `market_data._scan_table(years=...)` **年分区裁剪**
(P1-26 既有修法),⛔ 不全 glob;② 增量日更只算当日一行 ×5400 票;③ **上生产前必须在生产机隔离实测**
(⑦ 批 2 前置,**⚠ 用 `User=neckline` 瞬态 unit,⛔ 不用 root `systemd-run --scope`**,见 `CLAUDE.md`)。

<details>
<summary>③-C 原文(2026-08-09 被裁定 #11 推翻,⛔ 作废不得引用,仅供查证)</summary>

**新表**(全市场逐票,EOD 预计算,**P0-23 纪律**):

```
CREATE TABLE landing_state_daily (
  trade_date       TEXT NOT NULL,
  ts_code          TEXT NOT NULL,
  state            TEXT NOT NULL,  -- falling|landing_pending|liftoff_confirmed|high_extended|none
  state_reason     TEXT NOT NULL,  -- 五项判据逐项通过/未通过的原因码串
  metrics_json     TEXT NOT NULL,  -- 五项判据的原始读数
  skeleton_version TEXT NOT NULL,
  computed_at      TEXT NOT NULL,
  PRIMARY KEY (trade_date, ts_code)
);
```

**五项判据 → 首版机械阈值**(K8 §二 五条状态的逐条翻译;**全部 `source: engineering_v1`,阈值住骨架包 `config.landing`**)

| # | K8 原文 | 首版机械判据 | 依据(翻译自 K8 哪句) |
|---|---|---|---|
| 1 | 下跌或调整已经结束 | 近 `N_LOW=5` 日最低价 > 前 `N_BACK=20` 日区间最低价 ×(1+`LOW_TOL=0.01`),**且**当日未创 20 日新低 | 「不接下落」= 不允许仍在创新低 |
| 2 | 关键位置形成有效支撑 | `close ≥ max(MA20, 平台下沿) × (1 − SUP_TOL=0.01)`;平台下沿 = 近 `PLATFORM_WIN=20` 日收盘的 20% 分位 | 「均线、平台、关键价位或筹码密集区形成支撑」 |
| 3 | 抛压明显衰减 | 近 5 日**下跌日**均成交额 ÷ 近 20 日均成交额 ≤ `SELL_DECAY=0.90`,**且**近 5 日最大单日跌幅 ≥ `−0.05` | 「抛压明显衰减」+ 排除恐慌大阴 |
| 4 | 价格开始向上转强 | `close > MA5` **且** `close > 昨收` **且** `RS5 > 0`(相对所属行业中位 5 日超额) | 「价格开始向上转强」;RS 口径复用 `industry_strength` 现成量 |
| 5 | 当前仍处于启动早期 | `dist_from_high_60d ≤ −HIGH_GAP=0.03` **且** 近 `LIFT_WIN=3` 日累计涨幅 ≤ `LIFT_MAX=0.12` **且** 当日非涨停 **且** 未创 60 日新高 | 「仍处于启动早期」+ K8 §九排除项第 2 条(当日急拉 / 涨停 / 突破新高后临时追入) |

**四态映射**(K8 §二 原文逐条):

```
falling            判据 1 不成立                     → 排除(「下落阶段:排除」)
landing_pending    1+2 成立,4 不成立                 → T2 候选(「落地待确认」)
liftoff_confirmed  1+2+3+4+5 全成立                  → T1 候选(「支撑确认并开始起跳」)
high_extended      1+2+3+4 成立但 5 不成立            → 低优先级(「等待下一次调整与启动」):可进 T2 尾部,⛔ 不进 T1
none               判据缺数(state_reason 逐条说明)   → 不拦,但 ⛔ 不给 T1
```

🔴 **雷区对照(必须原样写进 `landing.py` 模块头,防后人当新发现 / 也防后人当禁令)**

- 判据 1+2+4 的形状 = **K3-B2 臂③「升势回撤 + 启动确认」**,那一臂被判「**确认信号 = 死猫跳顶点,比直接买更差**」(`research/k3_report.md`)。
- 判据 1 单独用的形状 = **K3 系统化超跌反弹四臂全灭**。
- 判据 5 的排除项 = **K2「追强势」全否决 + K7-C1 诱多做局**的正面兑现(这一半是**站在案底同侧**的)。
- **本关因此只产注意力分层,⛔ 不得被读成买入期望背书**(§3.8-(b) 一字不变)。它的真伪由 K8 自己的选股时钟在**前向样本**上回答(K8 §十七),**不由本版声称**。义务已挂 §七 **P3-49**。

**性能(P0-23 靶心,⛔ 不许跳过)**:`landing_state_daily` 是**全市场逐票 × 需要 60 日回看**的批算。① 取数一律走 `market_data._scan_table(years=...)` 的**年分区裁剪**(P1-26 既有修法),⛔ 不全 glob;② 增量日更只算当日一行 ×5400 票,回看窗口由 `scan_table_range` 覆盖 1~2 年;③ **上生产前必须隔离实测**(⑦ 批 2 前置)。

</details>

#### ③-C2 核心关 = 龙头识别(🔴 **2026-08-09 用户裁定 #12,与裁定 #11 同款处理**)

**为什么改(如实登记)**:核心关原判据 `leader_rs_rank ≤ 3` 读的是 `leader_structure_daily`,
而那张表是**簇内**口径 —— **簇 = 当天一起涨停、且同行业/同概念的 ≥2 只票**。生产实测
(20260807):全市场涨停 **75 只** → 181 个簇 → 814 行,**去重只有 75 只票有 `rs_rank`,占全市场 1.4%**。
其余 98.6% 的票**判不出**核心地位 → `blocks_t1` → 结构性进不了 T1。

🔴 **根子上的矛盾(这才是重点,不是覆盖率不够)**:K8 三个引擎找的都是「**还没怎么涨、刚要动**」
的票(C1 健康回调后再启动 / Z1 早期右侧启动 / Y1 平台完成后启动早期),而这道关的入场券却是
「**今天必须涨停**」。**涨停是结果,K8 要的是结果之前的那一刻** —— 用涨停与否判「刚要动的票是不是
核心」,尺子量的不是要量的东西。⚠ **这不是阈值定严了**:`≤3` 的 provenance 是 `audited`(H10),
数字本身没问题,**错的是那把尺子的取数域**。

**三条裁定(⛔ 不得重开)**

| # | 裁定 | 落点 |
|---|---|---|
| 12-a | **`≤3` 退出闸门** | 簇内 `rs_rank` **降级为补充读数**(有簇才有、**缺席不挡任何档**)。⚠ H10 审的是**簇内**名次,挪到行业域后同一个「3」意思完全变了,**⛔ 不许直接搬** |
| 12-b | **⛔ 不要任何机械低保** | 核心关**零阈值、零及格线**(含「行业内前 X%」这类)—— 用户明确否决。机械侧只出读数 |
| 12-c | **要「龙头」,不是 K8 的「容量核心」** | K8 §五-4 四句里有两句讲**容量 / 承接**(「最能代表资金态度的容量核心」),用户选的是**龙头**。⚠ 二者在 A 股常常**不是同一只票**(龙头多为弹性小盘、容量核心多为机构重仓大票)。读数与 prompt 一律**朝龙头对齐**,⛔ 别自作主张把市值 / 容量类的量加回来 |

**「那一群」= 行业(⛔ 不是概念、⛔ 不是涨停簇)**

`stock_basic.industry`,**一只票恰好一个行业、100% 覆盖**,110 个行业/日,`industry_strength_daily`
既有链路已有 175,890 行历史。⛔ **不用概念板块** —— 一只票挂多个概念,「它的板块」无唯一答案;
且 `CLAUDE.md` 明令概念板块只做展示、**不当判据源**(行业一对一 vs 概念多对多是两个量)。

**机械侧只出这组读数(⛔ 无一条带阈值)**

| K8 / 用户要的 | 读数 | 来源 |
|---|---|---|
| 谁最强(龙头主轴) | `industry_rs_rank_20d` + `industry_rs_pct_20d` | `daily` 20 日收益 + `stock_basic.industry` |
| **今天谁在领** | `industry_ret_rank_1d` | `daily` 当日涨跌幅行业内排名 |
| 连板高度(龙头的典型形态) | `consec_limit_up_days` | `limit_derived`,**全市场覆盖**(⚠ 稀疏表:不在命中行 = **确定的 0**,不是缺数 —— 返工时踩过) |
| 资金关注度 / 辨识度 | `industry_amount_share`(当日成交额占本行业比) | `daily.amount` |
| 🔴 **分母** | `industry_member_count` | 没有它,「第 3 名」是 3/8 还是 3/80 完全没法读 —— **必须给** |
| 补充(有簇才给,**缺席不挡**) | `cluster_rs_rank` · `cluster_amount_share` · `cluster_size` | `leader_structure_daily` |

- **判定者 = `basket_reason` 那一次调用**(⛔ **LLM 增量仍是 0**,同裁定 #11)。产出
  `core_verdict ∈ {ok, weak, unfit}` + 一句理由,映射与位置关**完全同构**
  (`weak`→降一档 / `unfit`→退出正式候选但**仍在 ③b 列名**)。
- **缺数不猜**:某读数算不出 → 如实标「未取到」,**⛔ 不填 0**。
- **成本**:核心关读数**只需要候选成员那几只**(≤21),⛔ **不许为它新建第三张全市场预计算表**,
  除非能证明按需现算不可行(要证明就先量,别先建)。

#### ③-D 定档改门槛制(K8 §八)

- **T1** = 六关**全过**(机械关 ①③ pass + 证据关 ②④⑤⑥ 全 pass,**含位置关 `position_verdict = ok`**)且 **次日交易预案完整**(四件套齐,见 ③-E)且 `market_regime` **可得**。**上限 ≤ 2 篮,允许为空。**
- **T2** = 机械关全过、**证据关(含位置关)至多一处 degrade**。**上限 ≤ 5 篮,允许为空。**
- 🔴 **裁定 #11 后此处的实质变化(写死,⛔ 别照原文改回)**:T1 不再要求任何机械态枚举
  (`liftoff_confirmed` 已删)。原文那套「T1 要 `liftoff_confirmed` / T2 要 `landing_pending`
  或 `high_extended`」**整体作废** —— 那正是把 T1 掐成近乎不可达的那一条(实测全市场当日
  `liftoff_confirmed` 仅 1~2 / 5526、14 个 D0 回放零 T1)。
- **达不到 T2 → 退出正式候选**(K8 §八 原文),落 ③b 未定档披露(名 / 分 / **卡在哪一关** / 原因码)。
- 🔴 **机械分与百分制卡不作废,降级为「档内排序 + 展示标度」**:`mech_score()` 五维、`_TIER_SCORE_INPUTS` 白名单锁、`tier_history.mech_breakdown`、V2.1-④ 的百分制打分卡与五维拆解 —— **全部原样保留**,只是**不再是定档的闸**(门槛制在前)。⛔ 别删,那会连带作废百分制卡与 ⑨ 归因的一整条标度。
- `quality_lines`(`tier1_min=0.60 / tier2_min=0.40`)**保留为档内排序的辅助下限**,但 K8 的「多好才配叫 T1」由六关 + 证据成熟度接管 → §七 **P3-33 主体在本版作废**(处置见 §七)。
- **容量 T1≤2 / T2≤5 / 合计 ≤7 与 K8 §八 逐字吻合** —— V2.1-② 已经改到位,**本块零改动**(这是 V2.1 批 1 的红利,如实登记)。

#### ③-E 引擎归属与交易资格

- 🔴 **单篮子单引擎(✅ 2026-08-09 用户裁定 #9:混引擎搁置不采用)**:`baskets` 幂等加三列 `engine_code TEXT` / `engine_version TEXT` / `skeleton_version TEXT`;**成员继承篮子引擎**,`basket_members` **不加引擎列**。K8 §四「每只入篮股票必须标注唯一的主引擎及准确版本」由「篮子标 + 成员继承」满足 —— 每票**仍然**有唯一确定的主引擎与准确版本,只是它由篮子决定。
  - **为什么这么落(理由写死)**:K8 §四 允许「一个篮子包含不同引擎归属的股票」,但 C1(已确认主线的健康回调)与 Z1(新方向的早期启动)在语义上互斥 —— 同一个驱动不可能既是「已确认主线」又是「新方向」。用户裁定搁置该情形,**引擎归属因此上移到篮子级**,归因随之简化为篮子级分层(⚠ 与 ④-C 的分层键一致)。
  - **留门一句(⛔ 只留一句,不预建任何机制)**:**若未来要开混引擎,升 `K8-V` 骨架版本再议**(骨架版本管的正是「篮子」这件事,K8 §一)。届时改法 = `basket_members` 补两列 + 归因下沉到成员级;**⛔ 本版不预留列、不预留分支、不写"以后可能用得上"的代码** —— 预建就是替未来的自己猜。
  - **LLM 侧的落法**:`basket_reason` 那一次调用为**每个篮子**给一个 `engine_code`(不是每票),再经机械闸对拍(篮内成员是否都满足该引擎的机械关阈值)—— **不满足的成员直接出篮**(篮子只留 1–3 只本就是筛选,不是"两说并存"的场合;⚠ 这与角色对拍的 `role_conflict` 姿势刻意不同,别套错)。
- **交易资格四件套**(K8 §十一):上涨判断 / 入场区间 / 目标离场区间 / 判断失效位置 —— **篮子卡已有全部四项**(⑦ 的 11 项里的 #3/#7/离场参考/#9),本块只做一件事:**把「四件齐」做成 T1 的必要条件**,并在 `position_plans` 继承时校验;缺任一 → 该票**不进 T1**、开仓时在客户端与周复盘各出一条**警示**(⛔ **不是拦截** —— 系统只审计不代下单,§3.8)。

#### ③-F 三引擎首版阈值(裁定 #4 的落点;每条带 `provenance`)

| 关 | **C1** 主线核心健康回调再启动 | **Z1** 新方向核心早期右侧启动 | **Y1** 中期平台完成后核心启动 |
|---|---|---|---|
| 市场关 | `trend_continuation` 主场;`high_divergence` 下要求板块广度分位 ≥ 0.60;`rotation_confirmed` 下**不产 T1** | `high_divergence` / `rotation_confirmed` 主场;`trend_continuation` 下要求 `stage ∈ {ignition, fermentation}` | 三态皆可;`rotation_confirmed` 下不额外加严 |
| 板块关 | `industry_rank ≤ 10` 且近 5 日强度日 ≥ 3 天(主线延续) | `stage ∈ {ignition, fermentation}` 且簇成员 ≥ 3(扩散成层级) | `industry_rank ≤ 30`(中期驱动有效,不要求当下最热) |
| 核心关 | `leader_rs_rank ≤ 3` | `leader_rs_rank ≤ 2`(新方向要更严:「率先表现出强度」) | `leader_rs_rank ≤ 5` |
| 位置关 🔴 **本行阈值已随裁定 #11 全部作废** | `guidance`(定性,无数字):「已确认主线核心的**趋势内健康回撤**后再启动 —— 不是趋势破坏,也不是已经拉开的加速段」 | `guidance`:「新方向核心的**早期右侧启动** —— 从支撑或整理区向上转折,回撤天然浅」 | `guidance`:「**中期平台走完之后**的启动早期 —— 平台够长、振幅够收敛」 |
| 证据关 | 独立证据 ≥ 3 份 | ≥ 3 份且**必须含一份消息 / 政策类来源**(K8 Z1「驱动明确」) | ≥ 2 份(中期驱动证据自然稀) |

- **`provenance` 分布(如实登记)**:`leader_rs_rank` 三档 ← **`audited`**(H10 十二格审计,`档案/research/k7_pre_report.md`,§1.6);`stage` 五态取值 ← **`audited`**(H11,同上);`industry_rank` 名次档 ← **`audited`**(v1.3.1 行业闸先例)。**其余全部 `engineering_v1` + `calibration: pending`** —— ~~位置关五项阈值、回撤深度带、`platform_days`~~(🔴 **随裁定 #11 全部删除,位置关自此零阈值**)、证据份数、板块广度分位。
- ⚠ **`platform_days` 需要一个新原语 / 新派生列**(近 N 日振幅 ≤ X 的连续天数),落在 `landing.py` 的 `metrics_json` 里一并算,⛔ 不另起一张表。

**测试与守门**

- `tests/test_selection_gates.py`:六关各自 pass / degrade / reject 三态;**机械关 reject → 硬否决**、**LLM 关 degrade → 只降档且仍出现在 ③b**(③-A 裁定的机器判据,正反双向)。
- `tests/test_scan_landing.py`:四态 + `none` 各一例;五项判据逐项开关的真值表;缺数不猜(`state_reason` 逐条);**bulk 与 day-by-day 与读回三路等价**(⚠ 比较前 `.drop("computed_at")` —— P1-36 已定案的体例,别再踩)。
- `tests/test_selection_tier.py`:门槛制在前、机械分只排档内序(喂一个机械分 0.95 但**机械关被硬否决**的候选 → **不进任何档**,这是本块最该有的一条;⚠ 裁定 #11 后位置关**不再是**这条测试的抓手,改用市场关或板块关构造);`_TIER_SCORE_INPUTS` 白名单锁不回归。
- 🆕 **裁定 #11 的机器判据(正反双向)**:`position_verdict='unfit'` 的票 **⛔ 不得从 ③b 消失**(只降级不除名);`landing.py` **零阈值** —— AST/文本守门断言该模块不出现任何形如「≥ 阈值」的判定与四态枚举字面量。
- **反向守门**:`gates.py` / `landing.py` **零 import** `neckline.report.score_display`(V2.1-④ 立的方向性规则);`landing.py` 零 import `neckline.sentinel.*`。
- `tests/test_selection_pack.py`:三个引擎包的阈值**全部能被闸 1 解析出 `provenance`**,且 `engineering_v1` 那些条**都带 `basis`**。

**依赖与部署批次**:依赖 ①②。→ **批 2 🔴**(换心脏 + 骨架线与三引擎线首次激活)。

**验收**:任取一个历史 D0 回放,`gate_evaluations` 说得出每个候选卡在哪一关、差多少;当晚只出 T1/T2 且每个成员带 `engineType×version`;`landing_metrics_daily` 全市场逐票**有读数**、缺的项在 `metrics_missing` 里逐条可读(**⛔ 不再有"态"这个东西**);位置关行 `gate_kind='llm'` 且 `evidence_json` **同时**存下当次读数与 LLM 理由;③b 未定档披露里出现被证据关(含位置关)降级到出局的票(**没消失**)。

---

### ④ 双时钟复盘 + 周度按引擎归因 + 修改建议四分类(K8 第三部分)

**目标**:把 K8 §十四 的**选股时钟 / 交易时钟**建成两个有「结案」语义的对象;周度按 **引擎 × 版本** 成员级归因;复盘板块输出**保留 / 观察 / 降权 / 淘汰**四分类修改建议(K8 §十七)。**并入 V2.1-⑥ 的周度 unit(§七 P3-42 在本版结案)。**

**涉及文件**:新增 `neckline/review/selection_clock.py` / `trade_clock.py` / `neckline/eval/iteration.py`;改 `neckline/review/basket_review.py`(结案接线)、`neckline/eval/metrics.py` + `calibration.py`(分层键扩容)、`neckline/review/handoff.py`(移交件扩节)、`neckline/api/app.py`;新增 `scripts/weekly.py` + `deploy/neckline-weekly.{service,timer}`(**逐字继承 V2.1-⑥**)。

#### ④-A 选股时钟(`selection_clock`)

**K8 §十四 原文四条规则,逐条落成机器判据**:覆盖 D0 全部 T1/T2 篮子 · D1 收盘统一验证一次 · **用户是否买入不影响样本** · D1 验证完成后**结案**。

```
CREATE TABLE selection_clock (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  basket_id      INTEGER NOT NULL UNIQUE,   -- 一篮一次,UNIQUE 即「只结一次案」
  d0_date        TEXT NOT NULL,
  d1_date        TEXT NOT NULL,
  covered_tier   INTEGER NOT NULL,          -- D0 那天的 tier(1|2)
  regime_at_d0   TEXT,                      -- K8 验证内容①(NULL = 当日无行,如实)
  mech_json      TEXT NOT NULL,             -- 九项验证内容逐项(每项带 available/source)
  engine_breakdown_json TEXT NOT NULL,      -- 引擎归因快照(裁定 #9 单篮子单引擎 → 两键即可)
  tier_accuracy  TEXT,                      -- K8 验证内容⑨:T1/T2 分层准确性判定
  untriggered_reason TEXT,                  -- K8 验证内容⑧:未触发原因(触发了则 NULL)
  closed_at      TEXT NOT NULL,
  skeleton_version TEXT NOT NULL,
  verification_ruleset_version TEXT NOT NULL
);
```

- **九项验证内容**(K8 §十四)→ `mech_json` 九个顶层键,**顺序即 K8 原文顺序**:`regime_at_d0` / `driver_persistence` / `sector_sync` / `core_strength` / `entry_zone_triggered` / `liftoff_signal` / `intraday_support_and_close` / `untriggered_reason` / `tier_accuracy`。**大半有现成实现可复用**:`basket_review.py` 的九项机械判、`basket_verification` 的四态、`landing_state_daily` 的 D1 态。⛔ **不重写**,复用 + 补齐缺的三项(`entry_zone_triggered` 用卡上冻结的建仓观察区间对 D1 最低 / 最高价;`liftoff_signal` 读 D1 的 `landing_state`;`untriggered_reason` 由前两者派生)。
- 🔴 **结案 = 只增不改**:写入走 `INSERT OR IGNORE`(同 `basket_cards` 冻结律),重跑同日 = 幂等 no-op。⚠ **与 `basket_review_daily` 的 `UNIQUE(basket_id, review_date)` 覆盖式刻意不同**:后者是「每日复盘」(可重跑覆盖),前者是「**结案**」(结了就是结了)。**⛔ 别"统一"它们。**
- 🔴 **「买没买不影响样本」做成结构性保证**:守门单测断言 `selection_clock.py` 的写入路径**零 import** `neckline.sentinel.positions` 与 `positions` 表(靠"没有那条路"担保,不靠自觉 —— 同 `basket_cards` 冻结表的既有体例)。
- **未买入的票在选股时钟结案后结束跟踪**(K8 §十四「双时钟衔接」第 4 条)—— 结案即终态,⛔ 不再往下写任何行。

#### ④-B 交易时钟(`trade_clock`)

**启动唯一条件 = 实际买入;全部离场后结案**(K8 §十四 原文)。

```
CREATE TABLE trade_clock (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  position_id INTEGER NOT NULL UNIQUE,      -- 确定性外键(⛔ 不做任何近似匹配,见 §七 P3-38)
  ts_code     TEXT NOT NULL,
  basket_id   INTEGER,                      -- NULL = 非篮子来源的手动开仓(合法)
  opened_on   TEXT NOT NULL,
  closed_on   TEXT,
  status      TEXT NOT NULL,                -- running | closed
  entry_plan_json TEXT NOT NULL,            -- 开仓时从 position_plans 冻的四件套快照
  final_json  TEXT,                         -- 结案时的八项验证(K8 §十四)
  created_at  TEXT NOT NULL,
  updated_at  TEXT NOT NULL
);
CREATE TABLE trade_clock_events (            -- append-only
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  trade_clock_id INTEGER NOT NULL,
  event_date TEXT NOT NULL,
  kind TEXT NOT NULL,          -- d1_open | daily_check | target_zone | invalidation | manual_note | close
  mech_json TEXT NOT NULL DEFAULT '{}',
  user_note TEXT,              -- K8 §十五:用户补充系统无法识别的主观原因,每次一条简短说明
  created_at TEXT NOT NULL
);
```

- **八项验证内容**(K8 §十四)→ `final_json` 八个顶层键:买入位置 / 入场条件 / 预期路径 / 驱动结构与支撑 / 目标区间处理 / 上涨效率变化 / 失效后的止损 / 原始理由的一致性。
- **「上涨效率变化」的机械定义**(K8 §十三 灰色地带的量化对应):持有期内**每日涨幅的 3 日滑动均值**相对入场后前 3 日的比值 —— 首版 `source: engineering_v1`,阈值住骨架包。⚠ 它**只进复盘与展示,⛔ 不触发任何持仓动作、不进推送**(K8 明写「保留主观判断,不设机械规则」)。
- **用户主观补充**(K8 §十五「用户只补充系统无法识别的主观原因,每次一条简短说明」)= 唯一的写端点 `POST /api/v1/clocks/trade/{position_id}/note`。**这条正好是 §七 P3-28(归因标签稀疏)的落点** → 同时落**覆盖率指标**(「本期 N 笔中有 M 笔带说明」);⛔ **不做 LLM 代猜**(P3-28 原文纪律)。

#### ④-C 周度按引擎归因(K8 §十六)

- **分层键扩容**:`pack_version × verification_ruleset_version` → **`skeleton_version × engine_code × engine_version × verification_ruleset_version`**,**篮子级**(✅ 裁定 #9 单篮子单引擎后归因天然落在篮子级,③-E;⚠ 早期草案写的「成员级」已作废,**⛔ 别照那个实现**)。
- ⚠ **`selection_clock.engine_breakdown_json` 随裁定 #9 简化**:篮子只有一个引擎 → 该列退化为 `{"engine_code":…, "engine_version":…}` 两键(**保留该列不改名**,将来开混引擎时它能原地扩成逐成员映射,零 schema 变更)。
- **选股侧 8 项**(K8 §十六):行情状态判断准确率 / 各状态下引擎表现 / T1·T2 入场信号正确率 / C·Z·Y 各版本表现 / 主要驱动有效性 / 支撑与启动形态表现 / 核心与替代标的表现 / **各因素对正确率的贡献**(← 直接吃 ③ 的 `gate_evaluations`)。样本 = `selection_clock` **全部** T1/T2。
- **交易侧 6 项**:原始判断正确率 / 入场与预案一致性 / 判断成立时的离场质量 / 上涨效率下降时的主动离场质量 / 判断失效时的止损质量 / **用户实际选择相对全部候选的表现**。样本 = `trade_clock` 真实买入。
- **判分只有一份**:可交易收益仍一律走 `neckline/eval/exit_sim.py`(既有 grep 守门单测),⛔ 本块不写任何退出 / 成交逻辑。
- **显著性一律走既有 `eval/placebo.py` 安慰剂对照**,⛔ 不新造统计口径。

#### ④-D 修改建议四分类(K8 §十七)

新模块 `neckline/eval/iteration.py::classify_factors()`:输入 = 周度分层指标 + 样本量;输出每因素一行 `{factor, klass ∈ {keep, observe, downweight, retire}, n, evidence, suggestion}`。

首版判据(`engineering_v1`,住骨架包 `config.iteration`):

```
n < MIN_N(20)                                    → observe(样本不足,⛔ 不给结论)
n ≥ 20 且 显著优于安慰剂基线                        → keep
n ≥ 40 且 显著劣于安慰剂基线                        → retire
其余                                             → downweight
```

- 🔴 **只出建议,⛔ 零写回**(V2.1 裁定 #3 一字不变,K8 §十七「复盘板块输出修改建议。**用户确认后**,新规则从下一版本生效」正好同向):`iteration.py` **零写库到 `selection_packs`**,AST 守门断言。唯一出口 = 复盘板块的**移交件**(`review/handoff.py` 扩一节),用户带着它去策略台 → 新引擎包 → 四道闸激活 → `C2`/`Z2`/`Y2`。
- **观察项清单守门照旧**(V2.1-⑤ 的机关):`HANDOFF_OBSERVATIONS` 每个 id 必须能在 §七 grep 到字面 `[P3-xx]`。**本版清单改成五条(定死,⛔ 别自由发挥)**:`P3-32`(主归属 lift 门槛,原样保留)· `P3-34`(零审计背书登记册,**已扩容含位置关五项 + 三引擎首版阈值**)· `P3-37`(退潮刹车灵敏度,原样保留)· 🆕 **`P3-49`**(K8 位置关落在 K3 已判死域的**前向证伪义务** —— 这条是移交件里最该让用户看见的一条)· 🆕 **`P3-51`**(状态层第五维冷启动缺席)。⚠ **`P3-33` 从清单里去掉**(主体已随门槛制作废,§七 已标),⛔ 别留一条会 grep 到但已经没意义的观察项。⚠ 拆 id(如写成 `P3-34a`)会让守门 grep 不到 —— 既有教训,别重踩。

#### ④-E 周度 unit(承 V2.1-⑥,**逐字继承,⛔ 不重新裁定**)

- 形态 = 候选③「折中」:**自动只跑不依赖交割单的两件**(画像批算 + 周度校准报告落盘)+ **本版新增第三件**(周度按引擎归因 + 四分类建议落盘);**对账仍等用户上传交割单**。
- **排程 = timer 直接触发单个 `oneshot` service,⛔ 不用 `.target`**(§七 P0-45)。`OnCalendar=Sat 09:00 Asia/Shanghai`,**`Persistent=true`**(与 evening 的 `false` 刻意不同,理由写进文件头);⛔ **永不加 `RemainAfterExit=yes`**。
- **⛔ 不推送**(不新增任何 kind → 不触发「新增推送须用户拍板」纪律)。
- `scripts/weekly.py` 三步各自 try/except、一步失败另一步照跑;**任一步失败 → `exit 1`**(让 `ExecMainStatus` 说真话)。
- **§七 P4-46 在本块结案**:`GET /eval/weekly` 由**在线现算**改为**读周度 unit 落盘的产物**(查不到才降级)。理由:分层键从 2 个扩到 4 个,现算成本必然上升,P4-46 的触发条件「> 5s」大概率当场成立;而它跑在常驻 `neckline.service` 里、与盘中哨兵同进程(P0-23 原教旨)。

**测试与守门**

- `tests/test_selection_clock.py`:结案幂等(同篮二次调用零新行、内容逐位不变)、九项各自三态、**零 import 持仓**(AST)、未触发 / 已触发两路。
- `tests/test_trade_clock.py`:开仓建行 / 部分平仓不结案 / 全平结案 / 八项齐 / `manual_note` 追加不改既有行。
- `tests/test_eval_metrics_placebo.py`:四键分层不丢样本(**历史 `LEGACY` 包的样本仍按老分层算,⛔ 不许并进新层** —— 承 V2.1-② 「历史样本不许消失」同一条纪律)。
- `tests/test_eval_iteration.py`:四分类边界(19/20/39/40 四个 n)、**零写 `selection_packs`**(AST)。
- `tests/test_scripts_weekly.py` + unit 文件守门:`neckline-weekly.timer` 不含 `Unit=…target`、`neckline-weekly.service` 不含 `RemainAfterExit`(把 P0-45 两条教训钉成机器判据)。

**依赖与部署批次**:依赖 ③(要有引擎归属才谈得上按引擎归因)。→ **批 3**。

**验收(承 P0-45「凡周期性的东西验收必须验第二次」)**:① 隔离实测通过(⑦ 批 3 前置);② 首跑 `ExecMainStatus=0` **且**时间戳是本次那一跑,三类产物齐;③ 🔴 **跨两次触发的不变量**:首跑结束后 `systemctl show neckline-weekly.timer -p SubState -p NextElapseUSecRealtime` 必须是 **`waiting` + 有值**;并用 drop-in 把 `ExecStart` 临时换 `/bin/true`、`OnCalendar` 临时改成分钟级,**观测连续两次触发**后删 drop-in 复原(⛔ 别用真跑验第二次)。

---

### ⑤ 纪律章程按 K8 持仓原则修订 **+ 熔断整体退役**(🔴🔴 高危,独立一批,独立回滚)

**目标**:两件事,同一批:**(A)** 出新章程行 `v2.2-k8`,承 `v1.3.3` 全部字段、**只改退出侧三处**,走 `activate_charter.py` 四道闸 + **staged 生效**;**(B)** 按用户裁定 #8 **整体退役熔断**,只留「连续 3 笔止损」作**纯提醒**。

> 🔴 **裁定 #8 原话(不得摘要、不得软化)**:「**我不需要你替我做决定;这个程序永远是提醒 —— 连续三笔止损真的发生了,那也是提醒**」。
> **语义边界写死**:**锁定态 / 次日只减不加 / 强制复盘解锁**三件机制**全删**;**「连续 3 笔止损」这个事件本身保留**,但降为**一条推送 + 一条看板事件**,**零状态、零锁、零行为改变**。⛔ 施工时不许「为了安全」偷偷留一个锁定标志 / 灰化按钮 / 自动"建议别开仓"状态位。
> ⚠ **planner 上一轮裁的「熔断连续止损链锚点」问题随整体删除作废**,⛔ 别再去实现它。

**涉及文件**:`neckline/strategy/momentum.py`(`MomentumConfig` 一处类型放宽)、`scripts/activate_charter.py`(白名单 + 核心值核对)、`neckline/sentinel/holding.py`(文案与分支)、`neckline/review/reconcile.py`(违纪判定收窄)、`scripts/oneoff/seed_charter_v22k8.py`(落库,默认演练 / `--confirm` / 双备份);**熔断面另见 ⑤-B 的十处清单**。

**config 逐字段改动(⛔ 只有这三处,其余承 `v1.3.3` 逐字节)**

| 字段 | `v1.3.3` | `v2.2-k8` | K8 依据 |
|---|---|---|---|
| `take_profit_retrace` | `0.08` | **`None`** | §十三「**盈利离场不设统一机械比例**」;改由「进入目标离场区间后,根据当时走势决定离场」 |
| `max_hold_days` | `5` | **`None`** | §十三 时间退出让位「上涨效率下降 → 保留主观换股权」 |
| `max_hold_days_profit` | `15` | **`None`** | 同上(浮盈硬上限随时间退出整体退役) |
| `time_exit_only_if_unprofitable` | `True` | **`False`** | 同上(无时间退出时该开关无意义,回落到 K1 默认值以免留一个假旋钮) |
| `stop_pct` | `0.05` | **`0.05`(值不动,语义改)** | §十三「单只股票亏损达到 5% 时进入**止损警戒和离场决策**」 |
| 仓位三件(`single_cap` / `max_positions` / `max_exposure_frac`) | 40000 / 3 / 1.0 | **一字不动** | K8 **沉默** → 沉默 ≠ 废除 |
| `forbid_high_elasticity` | `False` | **一字不动** | K8 排除科创板是**选股域**,不是纪律域(见 ①) |

**⚠ 三处工程细节(定死,⛔ 别现场发挥)**

1. **`MomentumConfig.max_hold_days: int = 3` 是非 Optional,给 `None` 会炸回测引擎**。改法**取 (a)**:字段放宽为 `Optional[int]`,`None` 语义 = **不设时间退出**(与既有 `stop_pct: Optional[float]` 的 `None` 语义同构)。⛔ **否决 (b) 用哨兵位 `9999`** —— 哨兵位是"看不出来"的病。放宽后必须跑一遍 **K1 逐位不变护栏**(`tests/test_v13_exit_guardrail`),旧 config 加载吃默认 `3`、行为一字不变。
2. **`stop_pct` 值不许改成 `None`** —— 它同时是三处的唯一源:① 篮子失效条件(`verification_rules.py` 的章程止损线)、② 熔断的连续止损链判据、③ 卡上止损价 `close × (1 − stop_pct)`。语义改的是**它触发什么**(条件单 → 警戒),不是它是多少。
3. **熔断的「连续 3 笔止损」锚点**:−5% 从强制条件单改警戒后,「一笔止损」的定义会变含糊。**裁定:锚点仍是「实际以 ≤ 止损线成交的卖出」**(台账 / 交割单口径,与现状同),⛔ **不改成「触发警戒的次数」**(那会让熔断被提醒频率驱动,而不是被真实亏损驱动)。写死。

**连带改动(⛔ 漏一处就是两套口径)**

- `sentinel/holding.py`:`check_take_profit`(回落止盈)与时间退出分支在 config 为 `None` 时**天然不触发**(既有 `Optional` 判空路径),**代码零改动**;`check_stop_loss` 的推送**文案**改「止损警戒」并点明「离场决策在你」。⚠ `take_profit` kind(触达**离场参考区间**,§2.8-C-3 记名豁免)**保留且更重要了** —— K8 的「目标离场区间」正是它。
- `review/reconcile.py`:① **`check_time_exit_discipline` 整项作废**(章程无时间退出 → 无违纪可判),改为在周报里如实写「本周期章程无时间退出条款」;② `classify_stop_discipline` 由**违纪判定**降为**警戒记录**(仍统计破线未走的笔数与金额 —— 这是 §1.3 第一死因的持续体检 —— 但**不再计入违纪计数**)。
- **§2.1 全文改写一节**:照既有体例 —— **原文加删除线保留,新条款附在后面**,⛔ 不删历史拍板留痕。

**🔴 风险登记(照 v1.3 先例**全文**入 charter 注释 + §2.1,⛔ 不得删、不得摘要)**

1. 本版退掉的是 **§1.3 交割单归因里仅有的两条被真金证明过的防线**:① **−5% 强制止损** —— 13 笔破线未止损多亏约 1.38 万,**占已实现亏损 85%**;② **时间退出** —— 持有 4–7 自然日是唯一打平的持有桶。**退役后这两条的责任 100% 转移到用户盘中执行**,系统只提供警戒与事后归因。
2. **回落止盈 8%** 是 H9 六格网格中**唯一同时改良全期与 2026** 的格(`档案/research/h9_exit_reform.md`,§1.6),退役 = 放弃一个有网格证据的改良项。
3. **新退出规则(按预案 + 目标区间 + 主观换股)从未被回测验证,且无法被现有回测引擎验证** —— 它依赖盘中人判层,EOD 数据够不着。→ 本版章程**不是**「过了进化门禁的章程」,是**用户行使决策权的越线采纳**(同 v1.3 先例,风险已当面告知)。
4. **历史样本上,退掉时间退出是负向的**:`档案/research/winners_anatomy.md`(§1.6)实证 —— **封顶赢家的正是 hold=5 日历退出**(大赢家 88.6% 由它了结,彼时均值 +17.6%),而回落止盈卖飞率仅 11.1%。K8 用「主观换股权」替代它,该替代的期望**无任何样本背书**。
5. **冻结基线全部不适用**:`v1.3.3` 起入场域已扩到从未被回测过的范围(N=1288 / −20.53% / 95361.4988 早已不适用),本版再改退出侧 → **与任何回测基线都无可比性**,⛔ 不许拿旧基线给新章程背书。

**staged 语义(写死)**

- **① 用户清空全部 open 持仓**(切换器闸 2 硬校验)**+ ② 明确确认**后,才由 `sudo -u neckline .venv/bin/python scripts/activate_charter.py --target v2.2-k8 --confirm` 激活;**激活前一律仍按 `v1.3.3` 执行**。
- ⚠ **闸 2 的「纯入场侧 diff」窄豁免在本版必然不适用** —— 本版改的全是**退出侧**,正是闸 2 当初要防的那一类。守门单测**正面钉死**:对 `v1.3.3 → v2.2-k8` 的 diff,`_is_entry_side_only_diff()` 必须返 `False`。
- **闸 1 白名单**加 `v2.2-k8`;**闸 3 核心值核对** `_CORE_EXPECTATIONS` 给它一条(四个退出字段的期望值 + `stop_pct=0.05` + 三个仓位字段照旧)。
- **回滚目标 = `v1.3.3`**(已在白名单),SOP 走 `archive/SOP_章程回滚_20260730.md`。

**测试与守门**:`v2.2-k8` 落库后 config 逐字段对拍(只有五个键与 `v1.3.3` 不同);`Optional[int]` 放宽后 K1 六年回测**逐位不变**护栏;时间退出 / 回落止盈在 `None` 下**恒不触发**(哨兵 + 回测双侧);熔断链锚点未变(喂一串警戒但无实际止损成交 → **不熔断**);`reconcile` 无时间退出条款时**不产违纪也不静默跳过**(周报有一句如实说明)。

#### ⑤-B 熔断整体退役(裁定 #8;**逐项列清,⛔ 漏一处就是"删了但还锁着"**)

**十处逐项处置(定死)**

| # | 面 | 处置 |
|---|---|---|
| 1 | §2.1 第 7 条(熔断纪律全文) | **加删除线保留 + 附退役附注**(照 §2.1 既有体例,⛔ 不删历史拍板留痕);附注写明裁定 #8 原话与「只留纯提醒」的边界 |
| 2 | `neckline/sentinel/circuit.py` | **锁定 / 解锁 / 幂等 / 派生状态整套删**;保留一个**无状态纯函数** `count_tail_consecutive_stops(db_path) -> int`(尾部连续止损笔数,读 `positions`,⛔ 不写任何表)。常量 `CIRCUIT_CONSECUTIVE_STOPS=3` **留**(它是提醒阈值);`CIRCUIT_DAILY_LOSS_YUAN=4000.0` **删**(单日亏损档是"熔断"专属,用户只点名了连续三笔) |
| 3 | `circuit_breaker` 表 | **停写留档不 DROP**(§七 P4-31 由七张扩到**八张**);读函数一并删(无下游消费方,同 `inquiry_log` 先例,查历史走 `sqlite3`) |
| 4 | 端点 `GET /circuit` · `POST /circuit/unlock` | **删两条**。⚠ **`POST /circuit/unlock` 是「解锁」动作,随机制消失**;`GET /circuit` 没有替代端点 —— 提醒走推送与看板事件,不走状态查询 |
| 5 | `PositionsOut.circuit` 字段 | 🔴 **本版不删键**(〇b-3 零删键铁律:用户 iPhone 何时换包不可控,`CircuitStateOut` 在 2.0.0/2.1.0 客户端是**必需解码**)→ **恒发 `locked=false` 的空态**,并在 schema docstring 标「已退役,恒 false,下一版删」;**真正删键排 v2.3**(两步淘汰纪律) |
| 6 | `kind = circuit`(`NKIMMEDIATE`) | **保留 kind、改语义**:由「熔断触发」改为「**连续 3 笔止损提醒**」,文案改纯告知(⛔ 禁指令词、⛔ 不许出现"停止开仓"/"只减不加")。**⚠ 刻意不新增 kind** —— 新增要用户拍板,而复用现有 kind 改文案不触发那条纪律;开关 `push_circuit` 原样保留(用户可关) |
| 7 | `sentinel/precall.py` 的 `EVENT_CIRCUIT_LOCKED` / `circuit_locked` 字段 / 9:26 汇总推送前置「熔断中:今日只减不加」 | **全删**。⚠ 连带:§2.1 第 7 条那句「锁定则 9:26 汇总必发(即便零判定也不被『平静清晨不轰炸』门槛吞掉)」的**豁免随之消失** —— 汇总推送回归正常门槛 |
| 8 | `api/notify.py::push_circuit_breaker` + `build_precall_digest(circuit_locked=…)` 参数 | 前者改名 `push_consecutive_stops_notice` 并改纯告知文案;后者**删该参数**(无调用方后) |
| 9 | `POST /positions/{id}/close` 与 `scripts/positions.py` 的 `evaluate_after_close` 接线 | 改为**只算尾部连续止损数 → 达 3 推一条提醒**,⛔ 不建行、不锁、不改任何返回值语义 |
| 10 | 周复盘 `review/reconcile.py` 熔断相关审计 + `circuit.auto_unlock_for_reviews` 接线 | **删**(强制复盘线 `FORCED_REVIEW_LOSS_FRAC` 与 §2.1 **第 4 条**「单周亏损 ≥ 总仓 2% → 强制复盘」**一字不动** —— ⚠ **第 4 条不是熔断**,别一起删) |

**客户端连带(归 ⑥ 一起做)**:熔断横幅 / 灰化 / 「熔断复盘」解锁按钮**全删**(`PositionsView.swift` / `PositionExtras.swift` / `AppModel.swift` / `APIClient.swift`);`CircuitStateOut` 的解码**保留**(服务端恒发空态),⛔ 本版不动它。

**🔴 风险登记(照 v1.3 体例入 §2.1 附注,⛔ 不得删)**

1. **本版删掉的是 §1.3 交割单归因指认的第三条防线的机械部分**:熔断的设计初衷是拦住「越亏越重仓越入金」(§1.3 明列的行为与亏损正相关)。退役后系统对连亏**只剩一条提醒**,**是否停手 100% 由用户当场决定**。
2. **「次日只减不加」这条唯一会改变次日行为的提示随之消失** —— 它此前靠 9:26 汇总推送的**必发豁免**送达;豁免一并取消。
3. **⚠ 生产历史触发情况:施工时必须用只读 SQL 查证后如实填进本条,⛔ 不许拿本地开发库冒充**。planner 2026-08-09 只查得到**本地开发库 `circuit_breaker` = 0 行 / `positions` = 0 行 / `sentinel_events.circuit_locked` = 0 行**,而**那个库不是生产库**(现役 K1、零持仓、无 `selection_packs` 行,与 §四 记的生产态对不上)→ **它证明不了任何事**(承 P0-39:「读得出表 ≠ 跑过」,零行有两种相反成因)。生产查证 SQL 三句:`SELECT count(*) FROM circuit_breaker;` · `SELECT trigger_reason, trigger_ref_date, unlocked_at, unlocked_via FROM circuit_breaker ORDER BY id;` · `SELECT count(*) FROM sentinel_events WHERE event_key='circuit_locked';`。**若查出确实触发过 → 必须把每一次的日期与解锁方式登记进本条**(那是"这个机制救过场"的证据,删它时用户有权看到);**若确实零触发 → 也如实写"零触发,故本次退役无历史损失可计量"**,⛔ 别写成"反正没用过"。

   ✅ **2026-08-09 生产实测回填(orchestrator 在 `nk` 上跑的只读 SQL,⛔ 非本地库)**:
   - `SELECT count(*) FROM circuit_breaker;` → **0**
   - 逐条明细查询 → **零行**
   - `sentinel_events` 全表 **655 行 / 8 类**(`invalidation` 259 · `precall` 247 · `entry` 107 ·
     `retreat` 17 · `holding` 12 · `capture` 6 · `attention` 5 · `d5exit` 2),
     其中熔断/锁定类 → **0 条**
   - **结论(如实写,⛔ 不粉饰)**:**熔断机制自上线以来在生产上从未触发过一次**,
     故**本次退役无历史损失可计量**。⚠ **这不等于"它没用"** —— 未触发的保险与无用的保险
     在数据上长得一模一样,三条真实防线的退役代价仍以 §八 第 19 项登记的那些数字为准
     (−5% 那 13 笔 / 1.38 万 / 85% 是**真发生过**的,与熔断零触发是两件事,⛔ 别混为一谈)。

**测试与守门(熔断面)**
- **反向守门(防复活)**:全仓 `not hasattr(circuit, "get_state" / "is_locked" / "unlock" / "evaluate_after_close" / "auto_unlock_for_reviews")`;`CIRCUIT_DAILY_LOSS_YUAN` 不存在;`grep` 全仓无 `circuit_locked` 字段与 `EVENT_CIRCUIT_LOCKED`。
- **停写守门**:`circuit_breaker` 表零新增行(照既有六表体例)。
- **端点**:`GET /circuit` / `POST /circuit/unlock` 返 **404**;`PositionsOut.circuit.locked` 恒 `false`(老客户端解码不炸的机器判据)。
- **纯提醒语义**:喂一串 3 笔止损 → **推一条 + 零建行 + 零状态**;第 4 笔止损 → **再推一条**(无链重置概念了,⛔ 别自己发明"解锁后才重推");`POST /positions/{id}/close` 的返回值逐字段不变。
- **⚠ 反向锁一条**:§2.1 **第 4 条**强制复盘线仍在(`FORCED_REVIEW_LOSS_FRAC` 存在且周复盘仍判),⛔ 别连坐删掉。

**依赖与部署批次**:与 ②③④ 无代码耦合,但**必须排在 ②③ 上线并观察一个完整交易日之后**(〇b-6:两批同时上 = 归因分不清)。→ **批 4 🔴🔴,独立一批。**⚠ **(A) 章程与 (B) 熔断是同一批但两个动作**:(B) 是**纯代码**、部署即生效;(A) 是 **staged**、等用户清仓 + 确认才激活。**⛔ 别把 (B) 的生效当成 (A) 已激活。**

**验收**:`activate_charter.py --target v2.2-k8`(不带 `--confirm`)四道闸演练全绿且闸 2 如实拦「有持仓不许切」;用户清仓 + 确认后激活,现役行切换、`strategy_activation_log` 追加事件;当晚哨兵 journal 里**零回落止盈 / 零时间退出**判定、止损文案已是「警戒」;下一次周复盘里时间退出违纪项消失且有一句如实说明;`GET /circuit` 返 404 且客户端无熔断横幅;连续 3 笔止损**只推一条提醒、库里零新行**。

---

### ⑥ 契约与客户端:K8 呈现(双端)

**目标**:把 ①–④ 的产物端给用户 —— 行情状态、六关灯、落地起跳态、引擎徽标、双时钟、四分类建议;**零删键**。

**契约新增(全部可选新增键 / 新增端点,⛔ 零删除、零收窄取值域、零新增 reason)**

| 位置 | 新增 |
|---|---|
| `ReportOut` | `marketRegime: MarketRegimeOut?`(三态 + `strengthening`/`weakening` + `available`/`unavailableReason`) |
| `BasketOut` | **`engineCode: String?` / `engineVersion: String?`**(⚠ 裁定 #9 单篮子单引擎 → **单值,不是数组**;早期草案的 `engineCodes: [String]` **已作废,⛔ 别照那个实现**)· `gates: [GateOut]`(六关 verdict + score + threshold)· `landingState: String?` |
| `BasketMemberOut` | 🔴 **裁定 #11 改**:~~`landingState`(四态枚举)~~ 已作废 → 改发 **`positionVerdict`**(`ok`/`weak`/`unfit`)+ **`positionReason`**(LLM 那句人话)+ 可选 `positionMetrics`(读数,给信息卡展开用)。位置关仍是**成员级**判据。⚠ **引擎两键不加在成员上**(裁定 #9:成员继承篮子引擎);要显示成员引擎,客户端从所属篮子取 |
| `PositionsOut.circuit` | **⛔ 零改动、零删键** —— 熔断退役后服务端**恒发 `locked=false` 空态**(⑤-B 第 5 项),客户端 2.0.0 / 2.1.0 照常解得出;**真删键排 v2.3**(两步淘汰纪律) |
| `DroppedBasketOut` | `blockedGate: String?` / `blockedReason: String?`(③b 现在说得出卡在哪一关)—— ⚠ **`reason` 码字符串一字不改**(改码 = 按原因码的归因断线) |
| `ReviewOverviewOut` | 新增三段:`selectionClock` / `tradeClock` / `iterationSuggestions`(**各自 `available` + `unavailableReason`**) |
| 新端点 | `GET /market-regime` · `GET /clocks/selection` · `GET /clocks/trade/{position_id}` · **`POST /clocks/trade/{position_id}/note`**(唯一写端点) |

- 🔴 **`POST …/note` 是本版唯一新增写端点**,须:① 404 只在 `position_id` 不存在时触发,**复用既有 `not_found` reason 字符串**(⛔ 不新增 reason → 客户端 `mapReason` 一字不动);② 长度上限 + 纯追加(写 `trade_clock_events`,⛔ 不改既有行)。
- **DTO 一律手写 `init(from:)`**(CLAUDE.md V2-⑮ 铁律):B 类冻结快照(`selection_clock.mech_json` / `trade_clock.final_json` / `gate_evaluations.evidence_json`)是**硬要求**,守门按类型名精确锁。⚠ **新建 B 类 DTO 别放在同前缀类型之后**(守门用 `split("struct <Name>")` 取首个匹配,会切错块)。
- **`NKJSON` 解码顺序:Bool 必须排在 Double 之前**(既有坑,新 DTO 照守)。

**客户端(双端)**

- **选股板块**:每篮卡头加**引擎徽标**(`C1`/`Z1`/`Y1`,混引擎时并列显示)+ **六关灯条**(过 / 降级 / 否决三色,点开看每关的 score/threshold)+ **落地起跳态**标签;百分制打分卡(V2.1-④)**原样保留**在下方,标注改为「档内排序标度」。
- **报告顶部**:行情状态条(三态 + 增强 / 减弱方向;`available=false` 时如实说「本段未取得」)。
- **持仓板块**:持仓卡加「**补一条主观说明**」入口(→ `POST …/note`)+ 灰色地带提示(读 `marketRegime`,**纯展示、⛔ 无动作**)。
- **复盘板块**:每日页 → 双时钟两页(选股时钟结案列表 / 交易时钟运行中与已结案);累计页 → 加「**修改建议四分类**」区(保留 / 观察 / 降权 / 淘汰,每条带样本量与证据,底部一句「**改包唯一通道是你带材料去策略台**」)。
- 🔴 **熔断 UI 全删(⑤-B 的客户端连带,双端)**:持仓页熔断横幅 / 开仓按钮灰化 / 「熔断复盘」解锁按钮 / `POST /circuit/unlock` 调用 —— **全删**(`PositionsView.swift` / `PositionExtras.swift` / `AppModel.swift` / `APIClient.swift`)。⚠ **`CircuitStateOut` 的解码保留、⛔ 本版不动**(服务端恒发空态;删 DTO 排 v2.3,与服务端删键同一版)。⛔ **不许留一个「曾经熔断过」的历史入口** —— 裁定 #8 要的是这套机制在产品面消失,不是换个地方显示。
- **§七 P3-47 在本块结案**:复盘每日页加**日期选择器**,复用既有 `GET /report?date=`(P3-47 原文点名的首选做法)。⛔ **不许**新建「复盘历史」端点去现连 `basket_review_daily`(那会绕开冻结快照,让回看到的复盘与当时那份报告讲不同的话)。K8 §十五「完整历史保存在案例详情中」是硬需求。

**报告十项产物**(K8 §十)—— `report/render.py` 的 markdown 段按 K8 十项重排并**逐项对号**:

```
① 行情状态、主要风格及增强与减弱方向   ← ②
② 主要驱动及持续性判断                ← ⑤ 驱动聚合(既有)
③ 按共同驱动建立的股票篮子             ← 既有 ③ 节
④ 每个篮子的核心与替代标的             ← basket_members.role(既有)
⑤ 每只股票的主引擎及准确版本           ← ③-E(新)
⑥ 已启用定量阈值的检查结果             ← gate_evaluations(新)
⑦ T1、T2 等级及依据                   ← ③-D
⑧ 预期上涨路径                        ← 卡 #6 三剧本(既有)
⑨ 入场区间、目标离场区间、失效位置      ← 卡 #7/离场参考/#9(既有)
⑩ D1 的支撑、转强和失效信号            ← 卡 verification_spec/invalidation_spec(既有)
```
⚠ **markdown 既有段标题是审计锚**(V2.1-⑦ 已登记):新增段用**新标题**,⛔ 不改既有段标题的字面。

**测试与守门**:`tests/test_contract_crosscheck.py` 三方对拍(schema 声明 → app 转发 → Swift 解码)扩到新键;**「零新增 reason」显式断言**(拿 V2.1 收官 commit 当分界线);`FROZEN_SNAPSHOT_DTOS` 扩容;iOS Simulator `xcodebuild test` + 双端 `build` 绿。

**依赖与部署批次**:依赖 ②③④ 的服务端已上线。→ **批 5**(客户端换包:macOS 由 builder 做,iOS 用户自理)。

**验收**:双端都能看到三态 / 六关 / 引擎 / 落地态 / 双时钟 / 四分类;老 2.1.0 客户端在新服务端上**不崩**(零删键的机器判据:拿 2.1.0 的 DTO 定义解一份新报告 JSON)。

---

### ⑦ 分批部署上云(在线升级,⛔ 无割接窗口保护)

**通用纪律(每批都做)**:① 部署窗口 = **交易日 15:05–15:55 或 17:15 之后,或周末全天**(避开周六 09:00),⛔ 永不在 16:00–17:00;② 上传前 `sha256` 对拍,上传后再对拍一次;③ **双备份** `data/backups/neckline.db.{bak,cpbak}-preV22b<N>-<ts>`,`.bak` 走 `sqlite3 .backup` 且验 `integrity ok`;④ 重启后核 `NRestarts`、`ActiveEnterTimestamp` 已刷新、journal 干净、公网 `/api/v1/health` 200 且 `version=v2.2.0`;⑤ **每批之间留一个完整交易日**(晚间链跑一整条)。

| 批 | 内容 | 回滚绳 | 判据 |
|---|---|---|---|
| **批 1**(🔧 **2026-08-09 用户裁定改写,见下「批 1 判据的更正」**) | ①(注册表 schema + 迁移 + 四个包文件)+ ②(行情状态层落表 + 只读端点)+ **`retire_legacy_packs.py --confirm`** + **只激活骨架线 `K8-V0.5`**(⛔ 三引擎线仍留批 2) | `git checkout cf24189` + `sync_code.sh` + restart;**DB 侧加了一表两列一索引 + 清了 LEGACY 的 `is_active` + 追加两条激活事件** → 回滚需 `.bak` 还原 | `/health` = `v2.2.0`;`init_schema` 幂等跑**表 +1、列 +2**、索引换一条;`selection_packs` **恰一行 `is_active=1` 且 `line_code='V'`**;`/market-regime` 200 且当日有行、`skeleton_version='K8-V0.5'`;**当晚晚间链照常产出种子与篮子**,与 V2.1 的差异**恰好收敛为选股域收窄一处**(排科创 + 排白酒),⛔ 无第二处行为变化 |
| **批 2** 🔴 | ③(关口管线 + 位置关表)+ **骨架线 `K8-V0.5` 与 `C1`/`Z1`/`Y1` 四次激活**(四道闸,先各跑一次不带 `--confirm` 的演练) | **代码 commit 回滚 + DB `.bak` 还原**(⛔ **不是「激活旧包」** —— `ENGINE_API_VERSION` 已 bump 到 2,两个旧包被闸 2 硬拒,这是设计,不是故障) | 四线各一行现役;当晚 `basket_stage_handoff` `reason_stage=ok`;`gate_evaluations` 有行且说得出卡在哪关;成员带 `engineCode×engineVersion`;`landing_state_daily` 全市场有态 |
| **批 3** | ④(双时钟 + 周度归因 + 四分类)+ `neckline-weekly.{service,timer}` + `enable --now` | `systemctl disable --now neckline-weekly.timer`;新表可留空 | 见 ④ 验收三条(**含跨两次触发的不变量**);`/eval/weekly` 已改读产物 |
| **批 4** 🔴🔴 | ⑤-A 章程 `v2.2-k8`(staged:**等用户清仓 + 确认**)**+ ⑤-B 熔断整体退役(纯代码,部署即生效)** | 章程侧 `activate_charter.py --target v1.3.3 --confirm`(白名单内,SOP 见 `archive/SOP_章程回滚_20260730.md`);熔断侧 = 代码 commit 回滚(`circuit_breaker` 表**未 DROP**,历史行还在,回滚即复活) | 现役行切换 + 事件流追加;当晚哨兵零回落止盈 / 零时间退出判定;`GET /circuit` 返 404 且 `PositionsOut.circuit.locked` 恒 false(老客户端不炸) |
| **批 5** | ⑥ 客户端双端换包 2.2.0(macOS builder 做,iOS 用户自理) | macOS 旧包 `~/Lino/app_backups/Neckline-2.1.0-<ts>.app`(`ditto` 备份) | 双端能看到 K8 全部呈现;老 2.1.0 客户端在新服务端上不崩 |

**批 2 的 P0-23 前置(⛔ 不许跳过)**:`landing_state_daily` 是**全市场逐票 × 60 日回看**的新批算路径,`market_regime_daily` 亦是新批算。上生产前必须 `systemd-run --scope -p MemoryMax=… -p CPUQuota=…` **隔离单跑**,量时长与峰值,**收盘后 15:00 之后、避开 16:00–17:00、串行、`load > 4` 立即停手**;实测数达标才把 `neckline-scan.service` 的 `TimeoutStartSec`/`MemoryMax` 改成实测值(带理由)。⛔ 别拿常驻 `neckline.service` 当小白鼠。⚠ 顺带把 §七 **P4-43**(nk 资源限额待校准)的数据一并收了。

**🔧 批 1 判据的更正(2026-08-09 用户裁定,⛔ 原判据已证伪,不得回退)**

立项时批 1 写的是「**不激活任何新线**」+「**当晚晚间链行为与 V2.1 逐位相同**」——**这两句不可能同时为真**,
施工期实测证伪:批 1 代码一上产,`get_active_pack()` 已改为 `get_active_line("V")`,而生产现役
`K7-pack-v1` 是 `LEGACY` 行 → 返 `None` → `generate_seeds()` 返 `None` → **当晚零种子零篮子**。

```
库里那一行  : [('K7-pack-v1', 'LEGACY', 'running', 1)]
get_active_pack()    : None      ← 实测
```

**根因**:这与跑不跑 `retire_legacy_packs.py` **无关**,是 `ENGINE_API_VERSION` bump + 读侧改线**本身**的
直接推论 ——「批 1 不激活任何新线」与「行为逐位不变」在设计上互斥。**裁定**:批 1 改为
「**代码 + retire LEGACY + 只激活骨架线 V**」,三引擎线仍留批 2。理由:① 晚间链不空窗(不拿生产停摆
换一个"更纯"的批次划分);② 行为变化收敛成**可解释的一处**(选股域收窄:排科创、排白酒),
③ 归因仍分得清 —— **批 1 只动股票池,批 2 才动定档逻辑**,〇b-6 的观察窗前提不受影响。

**⚠ 三条部署顺序硬约束**

0. 🆕 **`market_regime_daily` 的 P0-23 隔离实测是「批 1」前置,不是批 2** —— 下面那段「批 2 的 P0-23 前置」
   写在 ② 落批 1 之前,**这里更正**:`market_regime_daily` 随批 1 上产,故它的 `systemd-run --scope`
   隔离计时 + 量峰值必须在**批 1 部署前**做完(`landing_state_daily` 那半仍是批 2 前置)。
   ⚠ 开发机实测 `<0.1s/日` **不是生产结论**(CLAUDE.md 铁律:P0-23 原教旨)。
1. **① 的 `retire_legacy_packs.py` 必须在索引重建之前跑**(见 ① 的重建顺序铁律)。
2. **⑤(章程)必须在 ③(选股换心)上线并观察一个完整交易日之后** —— 否则出了问题分不清是「K8 选的票不行」还是「新退出规则不行」。这是**归因的前提**,不是排期偏好。

**验收(V2.2 产品级)**:每天的报告按 K8 §十 十项产出;每票带主引擎与准确版本;每篮说得出六关过没过、卡在哪;选股时钟覆盖**全部** T1/T2 且与买没买无关;交易时钟跟到全部离场结案;周度按 `骨架 × 引擎 × 版本` 出成绩单 + 四分类建议;章程按 K8 持仓原则运行且风险登记在册。

---

### 附:成本与超时算术(三引擎并跑之后重算一遍)

**🔴 结论先行:K8 三引擎并跑的 LLM 调用增量 = 0 次。**「三引擎」体现在**机械关的阈值分支**与**成员的引擎归属标注**上,**⛔ 不体现在 LLM 调用次数上**。

| 段 | 任务 | 联网? | 调用数由什么决定 | V2.1 上界 | V2.2 上界 |
|---|---|---|---|---|---|
| ⑤ 检索 | `driver_search` | ✅ 按次费大头 | **种子数**(`MAX_SEEDS_AGGREGATED=20`) | 20 | **20(不变)** |
| ⑤ 推理 | `basket_reason` | ❌ 流式 | 1 次(全部种子塞一个 prompt) | 1 | **1(不变)** |
| ③ 证据关 / 驱动关 / 核心关的 LLM 侧 | — | — | **复用 `basket_reason` 那一次的输出**(结构化 JSON 多几个字段) | — | **0(新增)** |
| ⑥ 定档 | `tier_rank` | ❌ 流式 | 1 次 | 1 | 1 |
| ⑦ 卡 | `script` | ❌ 流式 | 篮子数 | 7 | 7 |
| ⑨ 复盘 | `review` | ❌ 流式 | 昨日篮子数(**选股时钟结案文字并进这一次**) | 7 | 7 |
| ⑭ 消息面 | `news_scan` | ✅ 按次 | 持仓 + 今日篮子成员去重 | 3 + ≤21 | 3 + ≤21 |

**五条写死**

1. **⛔ 不许「每引擎跑一遍 LLM」** —— 这是本版最容易犯、也最贵的错(账单 ×3 而信息量不增,还会产出三份互相打架的篮子提案)。**引擎归属由 `basket_reason` 那一次一并给出**(prompt 里带三引擎的定性边界,输出里每票一个 `engine_code`),再经**机械闸对拍**(该票是否满足该引擎的机械关阈值;不满足 → 落 `role_conflict` 同款的"两说并存",⛔ 不静默采信 LLM)。
2. **检索段一分不省也一分不多** —— 它按**种子**跑,与引擎数、篮子数都无关。§七 **P3-41**(检索段并发化)因此**在本版仍零影响**,⛔ 别记成「已被 V2.2 缓解」。
3. **预算常量与 unit 超时:一字不动**(算过了,不是没算)。`SEARCH_BUDGET_SECONDS=20min` / `REASON_BUDGET_SECONDS=30min` / `REVIEW_BUDGET_SECONDS=15min` / `neckline-basket.service::TimeoutStartSec=5400` —— 调用数上界一个没涨,算术原样成立。⚠ `tests/test_llm_router_budget.py` 把 unit 文件与预算常量钉在一起,**改一个不改另一个会红**。
4. **本版真正涨的是 CPU 不是 LLM**:两张新预计算表(`landing_state_daily` 全市场逐票 + `market_regime_daily`)落在 `neckline-scan.service` 段 —— **那才是要实测的地方**(⑦ 批 2 前置)。
5. **§七 P3-44 的真实降幅仍是 0**(V2.1 实测:生产从未产出过 T3),本版不改变这个事实,⛔ 别把 K8 写成"省钱"。

---

## 五(旧-V2.1.0)、V2.1.0 施工图(批 1 已上线 · 批 2 作废 · 批 3 并入 V2.2 → archive)

> **全文已归档 → `archive/V2.1.0_施工图_20260809归档.md`**(2026-08-09 V2.2.0 立项时摘出,原文一字未改)。冷启动读下面三行就够:
>
> - **批 1 = ①②④⑤⑦⑧ 六块已上产**(2026-08-08 18:09 CST,对外版号 `v2.1.0`)—— 问询台整链退役 · T3 全链退役 · 百分制打分卡 · 复盘板块服务端 · 客户端三板块 IA + 复盘板块 · 契约对拍守门收口。**六块全部是 V2.2 的地基**(尤其 T1≤2/T2≤5 两档化 = K8 §八 逐字吻合,V2.2 零改动)。
> - **批 2(③ `K7-pack-v2` 发版与激活)⛔ 作废,永不执行**(2026-08-09 用户裁定 #2:K8 换选股哲学与包模型,不为将死的包走一次高危四道闸)。`packs/K7-pack.json` 与 `selection_packs` 里 `K7-pack-v1` 那一行**原样留档不删** —— V2.2 第 ① 块只把它的 `is_active` 清零并打 `line_code='LEGACY'`。
> - **批 3(⑥ 周度 unit + P0-23 隔离实测)并入 V2.2.0 第 ④ 块**(形态裁定、`Persistent=true`、⛔ 不用 `.target`、⛔ 不加 `RemainAfterExit` 等结论**逐字继承**);§七 **P3-42** 因此改在 V2.2.0 结案。

---

## 五(旧-V2.0.0)、V2.0.0 施工图(已完工上线 → archive)

> 全文已归档:`archive/V2.0.0_施工图_20260805归档.md`(〇 用户逐条裁定表 / ⛔ 红线 / D1–D8 定案 / 21 张新表汇总 / 分块序列 / 铁律 / **①→⑰ 全部施工块与完工记录** / 追加子项〔③-K7 · ④b · ⑤-b · ⑤-c · ⑥-b · ⑦-K7 · ⑦-K7-b · ⑦-b · ⑧-E · ⑧-F · ⑧-G · ⑪-D〕/ 跨块裁定〔篮子四表落库次序〕/ **V2 验收标准全文**,**外加交接时的 §四 上线初期快照**)。
> **V2.0.0 于 2026-08-04 22:00–22:05 CST 割接上线**:现役生产机由 hz 老机换成新机 **`nk`** / `https://nk.linotsai.top`;2026-08-05 完成初始化(持仓清空 · 08-04 EOD 缺口补拉 · 08-04 报告完整成型 4 篮 4 卡 · P0-39 / P0-40 结案),当晚 16:35 起常态运行。**现役章程 `v1.3.3`(V2 全程未动章程)、现役选股包 `K7-pack-v1`**。**查 V2 的施工细节、完工证据、验收标准一律去该归档文件**,本正文不复述。
> **⚑ 交叉引用约定**:本文件 §一~§四、§七~§九 里出现的「§五 V2-①…⑰」「§五 ⑧-G」「§五 D1–D8」「§五 裁定 #n」等指针,**一律指向本存根 → 该归档文件**。
> **⚠ 两处已被后续裁定修订,以本文件现文为准(归档不追改)**:① **裁定 #2「双 Agent 分工(DeepSeek + GLM)」** → 2026-08-05 用户拍板 **单 LLM(GLM)为常态**,双 Agent 降级为可选扩展路径(`llm_task_routes` 路由机制原样保留,接第二家 Provider 即恢复分工,不改代码;§三 3.10-B 已加同一条注);② **⑯-D 遗留的「⑫ 周度 unit 形态未定」** → 已登记 §七 **[P3-42]**,并于 **2026-08-07 由 §五 V2.1.0 第 ⑥ 块定案**(折中③ + timer 直触 service);③ **V2.1.0 起 T3 档与问询台整体退役** —— 归档件里凡涉 `T1/T2/T3` 三档、`tier3_min`、T3 简评、问询台的施工细节**只作历史留痕**,现行口径一律以 §五 V2.1.0 为准。
> **未随归档退场的 V2 常态口径**(留在正文,施工细节才在归档):**§2.8** V2.0.0 语义换血总表(凌驾 §2.2~§2.5 被取代条款)、**§3.10** V2.0.0 技术选型增补、**§七 ⚫** 选股策略包门禁(`selection_packs` 四道闸,与纪律章程两条版本线永不混用)。

---

## 五(旧-v1.5)、v1.5.0 → v1.5.2 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.5_施工图_20260802归档.md`(① 参考件三件套地基 / ② LLM 20 只全覆盖与预算 / ③ 卡面改版与老四件套退役 / ④ 存量配菜四件 / ⑤ 客户端跟进 + 版本号治理 / ⑥ 部署上云 / ⑦ 双端换包 / ⑧ review 黄牌修复 → v1.5.1 / ⑨ 提示词日期锚快修 → v1.5.2,**含「v1.5 LLM 预算账」与「v1.5 客户端契约清单」全文,以及交接时的 §四 v1.5.2 收官快照**)。
> v1.5.0 → v1.5.2 全部已上云;现役章程 `v1.3.3`(v1.5 未动章程)。**查 V2 之前的客户端字段一律去该归档文件**,本正文不复述。
> **⚑ 交叉引用约定**:本文件 §一~§三、§七 里出现的「§五 v1.5-①…⑨」指针,**一律指向本存根 → 该归档文件**。



## 五(旧-v1.4)、v1.4.0 + v1.4.1 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.4_施工图_20260729归档.md`(① P0 地基 / ② 行业强度单一源 / ③ 正选漏斗排序键三级 / ④ 信息卡与考卷同构 / ⑤ exec_hint + 决策日志第⑨项 / ⑥ 判定精度 / ⑦ 功能补全 / ⑧ 客户端跟进 / ⑩ P0-23 预计算落表 / ⑨ 部署上云,**含「v1.4 客户端契约清单」全文**)。
> v1.4.0 于 2026-07-29 17:00 上云,同日 18:51 追加热修 **v1.4.1**(§七 P1-26 结案);现役章程 `v1.3.3`(v1.4 未动章程)。**查 v1.5 之前的客户端字段一律去该归档文件**,本正文不复述。
> **⚑ 交叉引用约定**:本文件 §一~§三、§七 里出现的「§五 v1.4-①…⑩」指针,**一律指向本存根 → 该归档文件**。

## 五(旧-v1.3)、v1.3 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.3_施工图_20260728归档.md`(① 退出规则章程变更 / ② 持仓管理层 K4 牌 / ③ 情报官四件 / ④ 挂单未成交追踪 / ⑤ 问询台选股域清理 / ⑥ 客户端跟进 / ⑦ 部署 + 章程激活,**含「v1.3 客户端契约清单」全文**)。
> v1.3 → v1.3.5 全部已上云,现役章程 `v1.3.3`(见 §四 / §九)。**查 v1.4 之前的客户端字段一律去该归档文件**,本正文不复述。
> 未随 v1.3 交付的残项:**v1.2.1-D 打法 × 论点双维归因** → §七 **P3-11**(施工图全文在 `archive/v1.2_v1.2.1_施工图_20260728归档.md`)。
> **⚑ 交叉引用约定**:本文件 §一~§三、§七 里出现的「§五 v1.3-①…⑦」「§五 v1.2-*」「§五 v1.1-*」「§五 阶段 4*」等指针,**一律指向本节这三个存根 → 对应 `archive/` 归档文件**(正文不再复述历史施工细节)。

## 五(旧-v1.2)、v1.2 + v1.2.1 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.2_v1.2.1_施工图_20260728归档.md`(三仓章程 + 熔断纪律 + 决策日志八项 + 呼吸台账+ 归因闭环;除 v1.2.1-D 外全部随 v1.3-⑦ 上云)。**v1.2.1-D(打法 × 论点双维归因)未实现**,挂账 §七 P3-11,施工图全文在上述归档文件内(保留勿删)。

## 五-归档、v1.1 施工图(已完工上线 → archive)

> v1.1(SOP 补洞:盘前校准 tick / 持仓生命周期 D5 / 自选板块 + 体检 / 问询窗口修复)于 2026-07-21 全部完工上线(状态见 §四、变更日志见 §九)。**施工图全文已移 `archive/v1.1_施工图_20260721.md`**(照 §五B / §五C 留指针体例),正文不再复述。

---

## 五B、K2 策略研究(已否决归档 → 策略线)

> **策略研究已迁至策略研究中心权威文件 `STRATEGY_LAB.md`(2026-07-25 架构拆分)。**
> K2 施工图全文与结果**已随策略档案迁出**(§1.6):`档案/docs/K2_研究施工图_20260722.md` + `档案/research/k2_report.md`。
> 判决:中心命题否决,K2 落库不激活,K1 仍现役。

---

## 五C、K3 策略研究(已否决中止 → 策略线)

> **策略研究已迁至 `STRATEGY_LAB.md`(2026-07-25 架构拆分)。**
> K3 施工图全文与结果**已随策略档案迁出**(§1.6):`档案/docs/K3_研究施工图_20260725.md` + `档案/research/k3_report.md`。
> 判决:B2 四臂全灭、2026 生存门禁七臂尽墨,停止挖矿条款触发,K3 中止(未落库);B3/B5 残项转 STRATEGY_LAB Backlog。
---

## 五附、已完工路线图(阶段 0 → 4,研究先于产品 · 均已完工存档 → archive)

> 全文已归档:`archive/已完工路线图_阶段0-4_20260728归档.md`(阶段 0 数据地基 → 阶段 1 回测研究 → 阶段 2 LLM 审判层 → 阶段 3 盘中哨兵 → 阶段 4 产品化与上云的全部施工图与验收记录)。

## 六、回测参数清单(已全部过堂 → 策略线)

> P1–P10 全部有结论并写死进大脑 K1,清单全文原在 `STRATEGY_LAB.md` 雷区地图节 —— **该文件 2026-08-09 已迁出本仓**,见 **§1.6**(`档案/docs/STRATEGY_LAB.md`)。

---

## 七、Backlog(按优先级分组;冷启动从 P0 往下读)

> **2026-07-28 交接重整 + v1.4.0 立项标注**。分组含义:**P0** 影响判定正确性(越早越好)/ **P1** 已知错判或噪音(有明确修法)/ **P2** 审计欠账 / **P3** 功能挂账 / **P4** 运维与历史局限(留档为主)/ **📌** 认知对齐(v1.4 立项必读)/ **⚫** 长期机制(不排期)/ **✅** 已了结(压缩留痕,勿再当待办)。`P0-1`…`📌-22` 是**稳定 ID**,跨会话引用请带编号。**策略假设 / K 字头版本一律不在此**,权威在 `STRATEGY_LAB.md`。
>
> **⚑ v1.4.0 吸收标注(2026-07-28)**:被 v1.4.0 施工图吸收的条目在原地标「**→ v1.4.0 第 N 块**」,**稳定 ID 与原文一律保留不删**(施工细节以 §五 对应块为准,本节只留「这条去哪了」)。未标注的条目 = 本版不做,仍是待办。
>
> **⚑ v1.5.0 吸收标注(2026-07-29)**:同一体例,标「**→ v1.5.0 第 N 块**」。本版吸收四条:**P1-7**(search_engine 定案 → ④-A3)、**P1-24**(章程回滚 SOP → ④-A5)、**P4-25**(测试卫生 → ④-A4)、**✅ 节「诱多做局反向哨兵」的残留半边**(自选池侧未覆盖 K4 派发 → ④-A1)。**P4-16**(报告内存账)由 **⑥-E** 继续复核。新挂 **P3-27**(候选四件套老键删除,等双端换包后)。
>
> **⚑ V2.0.0 逐条处置(2026-08-02 立项)**:三态标注 —— ① 被 V2 吸收的标「**→ V2.0.0 第 N 块**」;② V2 后不再适用的标「**⛔ V2 后废弃**」,**稳定 ID 与原文一律保留不删**(留痕,防后人当新问题重开);③ 未标注 = 仍是待办。本次吸收 **P1-5 / P1-8 / P3-11 / P3-15 / P3-27**,废弃 **P4-16**,新挂 **P3-28 / P3-29 / P4-30 / P4-31**。
>
> **⚑ V2.1.0 立项逐条处置(2026-08-07)**:同三态体例 —— ① 被吸收的标「**→ V2.1.0 第 N 块**」;② 被 V2.1 结构性废掉的标「**⛔ V2.1 后废弃**」,**稳定 ID 与原文一律保留不删**;③ 未标注 = 仍是待办。本次**吸收 P3-42**(⑫ 周度 unit 形态 → 第 ⑥ 块定案);**部分落地 P3-44**(降费杠杆③「T3 不出卡」随 T3 删除天然兑现,杠杆①② 仍挂账,**估算须按 §五 附「成本与超时算术」重写**);**部分作废 P3-33**(其中 `tier3_min` 初值一项随 T3 删除消失,其余三项仍挂账);**新挂 P4-46 / P3-47**。**P3-41 明确不动**(它由种子数决定,T3 删除对它零影响,⛔ 别记成"已缓解")。
>
> **⚑ V2.2.0 立项逐条处置(2026-08-09)**:同三态体例 —— ① 被吸收的标「**→ V2.2.0 第 N 块**」;② 被 K8 结构性废掉的标「**⛔ V2.2 后废弃**」,**稳定 ID 与原文一律保留不删**;③ 未标注 = 仍是待办。本次**吸收并在本版结案 6 条**:**P3-42**(周度 unit,承 V2.1-⑥ 全文 → 第 ④ 块)· **P3-47**(复盘逐日回看,K8 §十五 硬需求 → 第 ⑥ 块)· **P4-46**(`/eval/weekly` 在线现算 → 第 ④ 块改读产物)· **P2-47**(卫生线双源,K8 改股票池正好碰它 → 第 ① 块)· **P4-48**(测试隔离写库 → 第 ① 块,治类不治例)· **P1-46**(周末日期炸弹 → 第 ① 块顺带)。**主体作废 1 条**:**P3-33**(Tier 质量线初值 —— 门槛制取代排序制后,「多好才配叫 T1」由 K8 §八 接管;只剩两小项挂账)。**扩容 2 条**:**P3-34**(零审计背书登记册 → 位置关五项 + 三引擎首版阈值一并入册)· **P1-8**(启发式阈值组 → K8 首版阈值并入,校准路径改走引擎升版)。**部分落地 1 条**:**P3-28**(归因标签稀疏 → `trade_clock_events.user_note` 提供入口 + 覆盖率指标;LLM 代猜仍不做)。**新挂 4 条**:**P3-49 / P4-50 / P3-51 / P3-53**。**明确不动 2 条**:**P3-41**(按种子数,K8 零影响,⛔ 别记成"已缓解")· **P3-44**(真实降幅仍是 0,⛔ 别把 K8 写成"省钱")。
> **⚑ 同日第二轮(2026-08-09 用户五项拍板 + 策略档案迁出)**:**销案 1 条** —— **P3-53**(载体已随策略档案迁出本仓,问题随之消失;⛔ 但 **P3-49** 不受影响、仍在)。**改判 1 条** —— **P3-49** 追加用户裁定 #10:**判据来源写死 = 选股时钟实盘数据,⛔ 不为它立回测战役**。**新挂 1 条** —— **P4-54**(策略档案迁出后代码面 4 处存活引用,**只登记不修**,批 1 顺手清障)。**扩容 1 条** —— **P4-31** 停写留档表由七张扩到**八张**(`circuit_breaker` 随熔断整体退役加入)。
>
> **⚑ V2.0.0 收官标注(2026-08-05,V2 上线后的账本状态)**:V2 施工期条目里凡指向「§五 V2-*」的,施工图全文已归档 → `archive/V2.0.0_施工图_20260805归档.md`(§五(旧-V2.0.0)存根)。本次收官**当日结案** **P0-39 / P0-40**;**新挂** **P3-42**(⑫ 周度 unit 形态未定 = V2 唯一仍开放的规划项,从 §四 移入本节以免随快照替换丢失)、**P4-43**(nk 上线初期待首验 + 资源限额待校准);**P3-41** 按用户 2026-08-05 裁定标「**暂不做,留观察**」。**P2-9 / P2-10 审计欠账保持原状** —— 用户同日拍板「V2 定型、不再补 review」,两条只作留档,不再自动升级为待办。

### 🔴 P0 · 影响判定正确性(P0-1/2/3 → v1.4.0 第 ① 块已完工上线;P0-23 → v1.4.0 第 ⑩ 块已结案;**P0-39 / P0-40 / P0-44 → 2026-08-05 当日修复上线,均已结案**;**P0-45 → 2026-08-07 当日修复上线并演练验证,已结案**)

- **[P0-45] ✅ 已结案(2026-08-07 09:2x–09:33 修复 + 演练验证,@builder-pro)** · 🔴 **晚间链 timer 只成功触发一晚,次晚起静默全哑 —— ⑯-D 的设计缺陷,08-06 整晚没跑**。**现象**:08-06 16:35 晚间链未触发,五个 unit 的 journal **一条都没有**(不是失败,是根本没启动);`systemctl list-timers` 里 evening 那行 **LAST=08-05 16:35、NEXT=`-`**,`status` 显示 **`Trigger: n/a`**,timer 自身 `ActiveState=active` 但 **`SubState=running`(不是 `waiting`)**;同期 `neckline-daily.timer`(**直接触发 service**、不经 target)两晚均正常 —— 差别就在"触发的是不是 target"。**根因在 timer 的重新武装边,不在 target 的启动作业**:systemd 的 timer 一旦触发就进 `TIMER_RUNNING` 停着,**唯一的重新武装入口是 `timer_trigger_notify()`,而它只在"被触发的那个 unit 转成 inactive/failed"时才把 timer 拨回 `TIMER_WAITING` 并重算 NEXT**;`.target` 是同步点、**没有"跑完"的概念,三段 oneshot 退出后它照样 `active` 挂着**(⑯-D 交付时漏了 `StopWhenUnneeded=`),那条边**永远不来** → NEXT 永远算不出来。⛔ **已证伪,别再往这个方向查**:「target 已 active,所以后续 start 被 systemd 吞掉」**是错的** —— 本机 target 自 **08-04 14:36:36**(⑯ 交付演练那次手动 `start`)起就一直 active,而 **08-05 16:35 那次触发照样把整链完整跑完**(三段 `ExecMainStatus=0`,16:35:20 → 17:07:20);**被卡住的从来不是"这次触发",是"下一次什么时候触发"**。08-05 之所以能跑,是因为 08-04 22:01:08 那次 `enable --now` 让 `timer_start()` 走了一遍 `timer_enter_waiting()`,**它算 NEXT 时根本不看被触发 unit 的死活** —— 所以这个病的形状**必然是「首晚必成、次晚起必哑」**,不是"一直不跑"(那会当场被发现)。**✅ 修法 = `deploy/neckline-evening.target` 加一行 `StopWhenUnneeded=yes`**(零代码、零契约):三段跑完后 target 因既有 `After=` 三段而**最后**才转 active,此刻没有任何 unit 以 `Wants=`/`Requires=`/`BindsTo=`/`Upholds=` 反向钉住它(**timer 的 `TriggeredBy=` 不算钉**),systemd 立刻把它停下 → timer 收到 inactive 通知 → 回 `waiting` → NEXT 重算。**链内不会被提前停掉**:`unit_is_unneeded()` 对「还挂着 job」和「尚未 active」的 unit 一律返回 false,三段严格串行(`After=` 既有序)一字未动。**⛔ 没选另外两条路**:① 链尾 `ExecStartPost=` 停 target —— seg3 跑的是 `User=neckline`(nologin 非特权)、要么提权要么配 polkit,且"被 target 拉起的 unit 反手停这个 target"是自指作业冲突,**最要命的是 seg3 没跑到那一步(失败/被掐)时 target 就落不下、病原样复发** = 把状态机的复位挂在业务段的成败上,方向反了;② timer 改触发 wrapper oneshot —— 要把 `Wants=` 保险丝哲学整套重写进脚本,收益为零。**⛔ 连带铁律**:三段 service **永远不许加 `RemainAfterExit=yes`** —— 那会让它们跑完仍 `active`,下一晚 `unit_start()` 直接返回 `-EALREADY`、**ExecStart 一行都不执行**,链变成 unit 全绿的静默空跑(**这才是真正的"已 active 被吞"**)。**验收(⛔ 不真跑烧钱全链)**:drop-in 把三段 `ExecStart` 临时换 `/bin/true` 演练,journal 微秒级证据五连 —— scan `.344829`→`.348961` / basket `.351239`→`.365568` / report `.368220`→`.377808`(**每段都严格在前一段 Finished 之后才 Starting**,串行未破)→ `.378479` **Reached target**(三段全退才转 active)→ `.384077` **Stopped target**(`StopWhenUnneeded` 自动落下,**间隔 5.6 ms**,无任何人工命令);演练后 drop-in 删净、三段 `ExecStart` 逐字复原(含 seg3 的 `--notify`)、五个 unit 文件与 `/opt/neckline/deploy/` md5 全 MATCH、`NEXT=Fri 2026-08-07 16:35:00`、`neckline.service` `NRestarts=0` 未受扰。**⚠ 为什么 ⑯-D 验收没抓到(本条最该记的一句)**:当时只验了「首跑成功」+「NEXT 存在」,而这两项**在首晚都为真** —— 缺的判据是一个**跨两次触发的不变量**:「跑完之后 target 有没有落下」。**通用教训:凡"周期性"的东西,验收必须验第二次,或者验那个让第二次得以发生的状态复位;单次跑通证明不了排程还活着。**
  - **🟠 次生事故(2026-08-07 09:26,施加修复时当场踩,已如实登记)**:`daemon-reload` **会异步地**把 `StopWhenUnneeded` 应用到那个已 active 三天的 target 上(⚠ reload 后**立刻同步查一次仍显示 `active`**,几百毫秒后才落下 —— 我据此误判"没生效、需手动 stop"),target 一落下 timer 立刻重新武装,而 `timer_enter_waiting()` 算下一个日历槽是**以 `LastTriggerUSec` 为基点**的 —— 基点还停在 08-05 16:35,于是算出的"下一槽"是**已经过去的 08-06 16:35** → **当场立即触发整条链**。⚠ `Persistent=false` **拦不住**(它只管重启后补不补,不管本次开机内的过期槽)。**后果**:09:26:38–09:27:25 全链空跑一遍(当时 09:26 盘中、当日无 EOD → 0 种子 / 0 篮 / 0 卡,`basket_stage_handoff` 落 20260807 `no_seeds/no_seeds/0`)+ 写了一份空的 `20260807` 报告 + **seg3 带 `--notify`,真推送了一条 APNs(`sent=1`)到用户设备** + 2 次 GLM 调用。**自愈性**:`reports` 是 `ON CONFLICT(trade_date) DO UPDATE`,今晚 16:35 真跑会整行覆盖(`candidates_json` 那条 `[]` 保护分支只保护非空历史值,不挡本次);`basket_stage_handoff`/扫描层同为按日覆写。**唯一不可逆的是那条推送**。**🧹 善后(2026-08-07 用户裁定「删掉」)**:那行空 `20260807` 已删(删前双备份 `data/backups/neckline.db.{bak,cpbak}-predel0807row-20260807-094144`,`.bak` integrity ok、两份各 45 453 312 B;**全列留痕** `data/backups/deleted_report_row_20260807-20260807-094144.json`〔8836 B〕+ markdown 全文;内容核实**确无归因价值** —— `candidates_json`/`sectors_json`/`watchlist_json` 全 `[]`、`basket_daily_json` 零篮、`intel_json` 三条 warning 全是「当日 daily/limit_derived/index_daily 无数据」、`position_quota` 因 0 涨停被算成"休息",**无任何 V1 冻结数据**)。删后 `reports` 15 → 14 行、`integrity ok`,公网 `/report/latest` 已回退到 **20260805(7 篮)**。⚠ **刻意保留两件**:`data/reports/20260807.md` 与 `basket_stage_handoff` 的 `20260807 no_seeds` 行 —— 前者今晚报告会重写、后者是 `INSERT OR REPLACE` 会被覆盖,且它俩**如实记录了 09:26 确实跑过一次**,删了反而是抹掉痕迹。**修法已固化进 `deploy/neckline-evening.target` 文件头**:在已卡住的机器上首次施加这一行,**必须先 `systemctl stop neckline-evening.timer` 摘掉触发器**,改完 + `daemon-reload` + 显式 `stop` target 之后再 `start` timer(`timer_start()` 从"现在"重算 NEXT,不看旧基点)。一旦 `LastTriggerUSec` 更新成当天,该陷阱自动消失,**只有第一次施加时存在**。
- **[P0-44] ✅ 已结案(2026-08-05 当晚修复 + 上线 + 08-05 报告完整成型,commit `0a3cdc8`)** · 🔴 **同一个病当天复发:抬高固定读超时赌不过晚高峰 → 当日仍不成篮**。**现象**:P0-40 中午刚把推理类读超时 90s→240s(中午实测该调用 173s,通过),**当晚 16:35 的生产链推理段 3/3 次精确各花 240s**(journal 16:51:53 / 16:55:53 / 16:59:53 / 17:03:54)→ `basket_stage_handoff` 20260805 行 `search=ok / reason=call_failed:ReadTimeout / basket_count=0`,检索段 20 次调用全通。**分级机制本身没问题**(`router.py` 的 240s 已部署生效、`factory.py` 已接线),**是那个固定数字本身不对**:晚高峰 GLM 吐字慢于中午。**根因(比 P0-40 更深一层)**:「整段生成必须在 X 秒内回完」这个判据,要求我们**提前猜准一个与上游吞吐挂钩的数字,而那个数字每天都不一样** —— 再抬到 480 只是把下一次翻车推迟,**抬数字这条路已被生产证伪两次**。**✅ 修法 = 换判据,不是换数字**:大上下文推理调用改 **SSE 流式**(`stream: true`)。httpx 的 read 超时天然作用在**每次 socket 读**上,于是它从「整段墙钟」变成「**chunk 与 chunk 之间**最多静默多久」—— 判「**还在不在吐字**」而非「一共要吐多久」,**与上游吞吐无关、不需要猜**;生成多长都合法,真卡死(90s 一个字都没有)照样快速掐断,短读超时的原始价值一点没丢。**落地**:`openai_compat` 加 `use_streaming`(默认 `False` = 既有行为逐字节不变)+ `_assemble_stream()` 把 SSE 行流拼成**与非流式响应体同形状**的 dict(单测**含键序**逐字节比对)→ `chat()` 与全部调用方**零改动**;`_post` 拆成重试壳 + `_attempt_post`/`_attempt_stream`,「网络异常才重试、非 200 与解析失败当场降级」的旧分工原样保留。**容错三条**:`[DONE]` 缺失可容忍 / 单条坏 chunk 只跳过它自己(全坏则如实报错,⛔ 不许静默返空内容的成功体)/ 上游不认 `stream:true` 原样回整份 JSON 时按**数据本身**(有没有 `data:` 行)兜底,⛔ 不看 `Content-Type`(缺头写错头的实现太多)。⛔ **流到一半断掉绝不把半截当成品** —— 半截 JSON 可能正好解出一个"看着合法"的残缺篮子,比干净失败危险得多。**⛔ 三处刻意没做**:① **检索类不开流式**(GLM `web_search` tools 协议 × 流式本项目从未验证,v1.3.4 案底说明这种组合坏起来是**静默的**;非流式 payload 仍逐字节 `stream:false` + `tools` 原样,单测双向锁死);② **不给流式加"总墙钟上限"**(那就又变回提前猜一个固定数字 = 本条要根治的东西;由 chunk 间隔 + 预算账 + `TimeoutStartSec` 三层兜);③ **删掉** P0-40 的 `LONG_CONTEXT_READ_TIMEOUT_SECONDS=240.0` 而不是留着 —— 留着后人会"顺手"再抬一次,单测 `hasattr` 反向守门。**预算算术重算**:检索账 20min + 一组非流式溢出 4.5min + 推理账 30min + 单次整段生成悲观额度 10min(`STREAM_GENERATION_BUDGET_ALLOWANCE_SECONDS`,**只用于算术与守门、不是运行时会去掐的超时**)≈ **64.5min**,**比 P0-40 时的 66.5min 还宽** → `deploy/neckline-basket.service` `TimeoutStartSec=5400` **不必再动**(值未变 = 不必 `daemon-reload`,本次只改注释)。**测试(+17,3012 → 3029 passed + 2 skipped)**:流式/非流式拼装**逐字节等价** + `LLMResult` 全字段等价 / `stream:true` 真进 wire / 检索类恒非流式(router 层 + 工厂端到端双向守门)/ chunk 间隔超时重试后干净降级**且不返半截** / **整段生成远超单 chunk 容忍仍能跑完**(本次修复的核心命题)/ `[DONE]` 缺失·坏 chunk·全坏·非 SSE 四态容错 / `reasoning_content` 不并入 content / `tool_calls` 分片按 `index` 拼回 / 墙钟埋点留痕 / 已证伪常量不许复活。**生产复验**:见 §九 当日一行。**观察判据**:journal 里 `流式生成完成:%.1fs / %d 个 chunk` 那行 —— 若单次逼近 10min,回来重算预算账。

- **[P0-40] ✅ 已结案(2026-08-05 当日修复 + 上线 + 08-04 报告完整成型,commit `3cb96f6`)⚠ 其"240s"部分已被同日晚的 [P0-44] 取代**(分级机制保留、数字与常量删除,改流式;**读本条时别照着 240 那个数字办事**) · 🔴 **推理类大上下文调用被 90s 读超时确定性掐死 → 当日恒不成篮**。**现象**(生产实打,不是推演):`llm_providers` 两处配置缺口补齐后重跑 08-04,检索段 200 OK 全通,**推理段 3/3 次恰好 90s `ReadTimeout`**(12:29:43 起,每次整 90s)→ `reason_stage=call_failed:调用异常 ReadTimeout` → 0 篮子。**根因**:`OpenAICompatProvider.read_timeout=90.0` 那个数字是给**带联网搜索的单次审判/问询**调的(v1.3.4 实测 30-60s+),而 V2 的 ⑤ 是另一种工作量 —— **一次把 20 颗种子 + 每颗的成员机械数据塞进同一个 prompt**,再要一份结构化 JSON 出来;**确定性超长,不是网络抖动**,重试三次只是把同一个「生成没跑完」重放三遍。**这是 CLAUDE.md「带联网搜索的 LLM 调用不能用短读超时」的推理侧翻版** —— 同一条纪律,换了一种慢法。**✅ 修法**:读超时**按 task 类别分级** —— `llm/router.py` 新增 `LONG_CONTEXT_TASKS`(`basket_reason`/`tier_rank`/`script`/`review`)+ `LONG_CONTEXT_READ_TIMEOUT_SECONDS=240.0` + `read_timeout_for_task()`(**分级判据唯一实现**,返回 `None` 表示"不覆盖",与"恰好等于 90"分得开);`openai_compat.__init__` 加可选 `read_timeout`(默认 `None` = 不改类属性 → **直接 new 出来的 provider 逐字节不变**);唯一接线点 `factory.get_provider(task)`。**⛔ 三处刻意没做**:① **不全局翻倍**(短读超时的价值在快速掐断真卡死的连接,全局翻倍会把真卡死的每次等待也拖成 240s、白吃预算账);② **不做成 `chat()` 参数**(那要改 ⑤⑥⑦⑨ 每个调用点,漏一个退回 90s 且**看不出来**;工厂是 provider 的唯一出生地,一处接全覆盖);③ **检索类 90s 一字不动**(有实测背书)。**同步抬 `deploy/neckline-basket.service` `TimeoutStartSec` 3600 → 5400**:该值本就是按预算算术定的(检索账 20min + 一组检索超时溢出 4.5min + 推理账 30min + 一组推理超时溢出 **12min** ≈ 66.5min),旧值恰好卡在旧算术(≈59min)边上,**抬读超时不抬它 = 最坏情况被 systemd 掐成「链断了」**;单测已把 unit 文件与两个常量钉在一起,改一个不改另一个会红。**测试(+16)**:四个推理任务各拿 240 / 五个检索与轻量任务 + `task=None` 仍 90 / 直接 new 的 provider 不变 / **探针捕获 `httpx.Timeout` 证明数字真进了 wire**(⚠ 光断言属性不够)/ 两个任务集不重叠 / 最坏一组重试仍在推理账内 / unit 超时 ≥ 预算算术最坏;全量 **3012 passed**。**生产复验**:同日第三次重跑 **[basket] ok {baskets: 4, cards: 4, dropped: 0}** —— **推理调用实测 173 s(2 min 53 s)**,正好卡在 90s 与 240s 之间,这个数字就是本条的判据。

- **[P0-39] ✅ 已结案(2026-08-05 当日修复 + 上线 + 08-04 报告就地修正,commit `99f0e76`)** · 🔴 报告 ③ 节把「⑤⑥ 引擎没跑」讲成「今天市场上没有够格的篮子」(2026-08-05 补跑 08-04 时**在生产上真打出来**,不是推演)。**现象**:`llm_providers` 配置不全 → `[aggregate] 推理段缺席(no_provider)—— 当日不成篮`,`baskets` 表零行;而报告 ③ 节输出的是「**今日无篮子达到定档标准。这是合法输出,不是故障——今天没有共同驱动清晰、成员结构够格的篮子,系统不会为了让报告好看而放宽质量线。**」—— 一句**实质性市场判断**,而系统其实什么都没判。`basketDaily.basketsAvailable=true` / `basketsUnavailableReason=null` 同样在说谎,`degraded` 也是 `false`。**根因**:`report/basket_daily.py::build_basket_daily` 里 `out.baskets_available = True` 挂在 `load_today_baskets()` **读表成功**这个条件上 —— 它只证明「表读得出来」,**不证明引擎跑过**。⚠ **同一个函数里 ③b 做对了**(`dropped=None` = 本次没跑 ⑥ → `available=False` + 「本段未取得」;空序列 = 跑了零溢出 → `available=True`),③ 缺的正是这个三态区分。**⑤ 侧的信息本来就有**:`selection/aggregate.py` 的 `STAGE_NO_PROVIDER`/`STAGE_CALL_FAILED`/`STAGE_BUDGET_EXHAUSTED`/`STAGE_PARSE_FAILED`/`STAGE_NO_SEEDS`/`STAGE_PARTIAL` 是一等状态、注释明写「语义不合并」,只是**没有落到报告层能读到的地方**(`baskets` 表零行时,报告无从分辨「跑了没成篮」与「压根没跑」)。**修法方向**(已施工,见下):把 ⑤ 的段状态**落库**(候选落点:`baskets` 之外单起一张当日段状态表,或复用 ⑧-B 存拍那种「当日状态行」体例),报告层据此三态输出 —— 「跑了、确实不成篮」/「没跑(no_provider 等)」/「部分跑成」,⛔ 不许把后两者渲染成第一种。**这条与 v1.5「写侧收口、不读侧遮蔽」和 B1「`card_corrupt` ≠ `card_not_ready`」是同一条纪律的第三次现身** —— 「不知道」与「知道没有」必须分开,合并的代价一律落在用户的判断上。~~**⚠ 已交付的 2026-08-04 那份报告 ③ 节据此不可信**~~(**已于 2026-08-05 12:00 就地修正,见下**)。

  **✅ 修法(2026-08-05 @builder-pro,取「单起一张当日段状态表」那条,⛔ 未碰篮子四表)**:新表 **`basket_stage_handoff`**(`trade_date` 主键 + `INSERT OR REPLACE`,同 ⑯-D `basket_dropped_handoff` 的搬运工体例,DDL 追加在 `db.py` 末段、不进 `_COLUMN_MIGRATIONS`),读写 + **判读唯一实现** = `neckline/selection/basket_stage_handoff.py`。**⚠ 两张表不合并**:⑤ 缺席时 `_run_basket_segment` 在 `if not result.baskets` 就早返回、压根走不到 ⑥ —— 那时 dropped 表**没有行**而本表**必须有行**,两张表的「有没有行」回答的是两个问题。**写侧**(`report/evening.py::_run_basket_segment`):⑤ 一返回**立刻**落表 —— 位置定死在「没篮子就早返回」那句**之前**(缺席场景恰恰走那条早返回,晚一行就永远记不下来);SEG_BASKET 整段炸在编排层时覆写一行 `segment_failed:*`(**本次结论不许被表里旧行救回来**,同 ③b 既有纪律)。**读侧**(`report/basket_daily.py`):**有篮子 = 引擎跑过的活证据** → `available=true`;**零篮子**才查段状态三态定夺 —— 跑了(`reason_stage=ok`,或有现役包的 `no_seeds`)→ 保留「今日无篮子达到定档标准…不是故障」原文案;没跑成 → `available=false` + 「**本段未取得**:聚合/定档引擎本次未运行(原因:`no_provider` 等)」;**无行 = 不知道** → 同样未取得(⛔ 不许拿「不知道」冒充「知道没有」,同 ③b 在历史回放时的姿势)。**判据锚在 `reason_stage`**(推理段 = 决定能不能成篮的那一段);`search_stage` 只留痕不进判读(检索缺席时 ⑤ 仍会出篮子,卡上 `evidence_status` 自有披露)。**一个真陷阱已堵**:⑤ 自己那道保险丝返回的是 `AggregateResult` 的**默认字段值**(`reason_stage=no_seeds`),只看段状态会把故障读成「跑了、今天真没种子」→ 判读必须先看 `notes` 里的 `aggregate_failed:*` / `no_active_pack_or_seed_set`;**未知码一律保守判没跑成**。**契约形状零变化**(`basketsAvailable`/`basketsUnavailableReason` 是既有键)、`build_report`/`build_basket_daily` 签名不变 → **常驻 API 无需重启**(实测 `NRestarts=0`、`ActiveEnterTimestamp` 全程停在 08-04 22:01:08,而公网已返回新文案 = 快照读回路径本就不经进程内代码)。**测试**:四态(`no_provider` / 预算尽 / 真零篮子 / 正常有篮子)的 ③ 节文案 + `available` 标志各一例、无行第五态、默认段状态陷阱、写侧落点顺序正面钉死、③b 三态不回归;新增 `tests/test_basket_stage_handoff.py` 18 例 + `test_basket_daily.py` / `test_evening_chain.py` 增补;**全量 2996 passed / 2 skipped**。**⚠ 未擅动的两处**:`llm_providers.base_url` 与 `llm_default_provider` 两个配置缺口**照旧未改**(持久配置归用户),本次修的是「缺席时不许撒谎」,不是「让它别缺席」——所以 08-04 重跑后 ③ 节仍然是「未取得」,只是**这一次是诚实的**。

- **[P0-23] ✅ 已结案** · ~~`compute_industry_strength` 扫全历史,在生产机不可完成 —— 挡住 v1.4.0 整版上云~~(2026-07-29 ⑨ 首次部署实测发现并回滚,**当天由 ⑩ 修复、傍晚重新上云验证通过**)。`report/industry_strength.py::_load_ret1d_panel(start=None)` 对 `daily` 做**全历史 `scan_parquet`**(生产 1591 分区 / **7,841,894 行**),模块 docstring 的「本地实测全历史加载廉价(<1s)」**只在开发机(Mac)成立**;生产(2 vCPU / 1.6G RAM)实测:**700M cap → OOM-kill(exit 137)**、**1400M cap → 600s 未跑完(exit 124)**。**为什么要紧**:v1.4-② 把它立成 A2/B3 判据唯一源后,三个调用点全部中招 —— ① `pipeline.py:163` 在 **16:35 报告主链**(`neckline-report.service MemoryMax=800M`,而 07-28 那份**不含**本步的报告峰值已经恰好顶满 800M)→ 上了大概率**当日无报告**;② `info_card.py:652`(新端点,实测公网/本地**永不返回**,120s 与 600s 两次都超时);③ `inquiry.py:297`(问询台)。②③ 跑在**常驻 `neckline.service` 内、与盘中哨兵同进程**,`MemoryHigh=420M` 先节流导致进程陷在回收死循环(`memory.events.high` 飙到 54 万次、`oom_kill=0`)——**表现为卡死而非崩溃**,盘中被点一次就拖累哨兵。**⚠ 抬 `MemoryMax` 不是解**(1400M 都跑不完,是算法成本不是配额),⑨-F 原文「视情抬到 1G」按此作废。**✅ 修法已裁定(用户 2026-07-29)= 方案②「预计算落表」→ 施工图 §五 v1.4-⑩**(🔴 @builder-pro,**⑨ 重跑的硬前置**):新 SQLite 表 `industry_strength_daily`(110 行/日,含未达标行业),16:05 日更增量算一天(**只读当日一个分区**),`persist_days` 用「上一评定日 streak + 今日强度日标记」递推 → **不需要任何人为窗口**;三个在线调用点 + ④ 信息卡 60 日中位数序列全部改**只读表**,缺行走保险丝(降级方向 = 不拦 + 显式披露)。~~① 给 `start` 加下界窗口~~ **否决**(与「持续天数看多远由上次断裂决定」的设计意图冲突,会低报长 streak,§3.8 宁可多算不可少算);~~③ 只给 `info_card`/`inquiry` 注入报告已算好的结果~~ **否决**(治标,16:35 主链照旧中招)。**修完必须在生产机上实测复验**(⑩-G,含新立的「探针只许收盘后跑」纪律),**且 review 前移到上云之前**(§五 前提第 4 条)。**✅ 2026-07-29 本地部分完工**(@builder-pro):新表 + store + CLI + 16:05 挂载 + 四处读侧改造 + 保险丝四态 + `dataFreshness` 三键 + 客户端横幅;守门单测(四文件零现算入口 + 三路等价)绿;本地真数据演练 bootstrap 全历史 3s / 17.5 万行 / 库 +25MB、`verify` 三项绿、随机 3 日单日对拍逐位一致、读侧 1.3ms、日更增量 0.02s、信息卡 0.56s。**演练打出一个真洞并已修**:`rank(ordinal)` 并列由行序打散 → 按年块 bootstrap 与单日日更算出**不同 rank**(20230803 实测),修法 = 排名前按 `(median_ret 降序, industry 升序)` 排定(已入 CLAUDE.md 纪律 + 单测)。**✅ 2026-07-29 生产侧亦完工并随 ⑨ 上云验证**:旁路 bootstrap 落表 175,120 行 / 1592 交易日 / 110 行业,⑩-D 五条 + ⑩-G 五条判据全绿;⑨ 上云后生产实测 —— `verify` 三项自检全绿(exit 0)、**上午「永不返回」的 `GET /report/{date}/info-card/{code}` 现返 200 / 15.6s / 16431B**、16:35 报告主链恢复正常。**残留**:信息卡 15~20s 仍偏慢,根因是 `_load_single_code_panel` 全 glob(**与行业强度无关**,那一维已是读表 5.3ms)→ 另立 **P1-26**,不再挂在本条。
- **[P0-1] 补录持仓无法指定真实买入日**。`api/app.py::open_position`(~703 行)写死 `buy_date=date.today()`、`PositionOpenIn`(`api/schemas.py:304`)无买入日字段 → 用户 2026-07-27 补录的 **3 笔历史持仓买入日全被盖成当天**。**为什么要紧**:D 计数、时间退出 D5/D15 判向、回落止盈峰值追踪起点、周复盘持有天数、按打法归因的持有周期,**全部起点错**。**怎么修**:① `PositionOpenIn` 加可选 `buyDate`(`YYYYMMDD`,缺省今天,校验不晚于今天);② `app.py` 透传 —— **领域层 `sentinel/positions.py::open_position` 与 CLI `scripts/positions.py add` 本来就收 `buy_date`,缺口只在 HTTP 契约 + 客户端**,改动面很小;③ 客户端补录 sheet 加日期选择器(默认今天);④ **纠正已落库的 3 笔**:需用户提供真实买入日,改前 `sqlite3 .backup`,改后复查 `holding_eod_check` 是否已按错误 D 定格(`time_exit_locked_*` 三列有值就要清掉重算)。~~归 v1.3.6~~ → **v1.4.0 第 ① 块(①-A)**,🔴 高危区:碰持仓判定。⚠ 纠正 3 笔**需用户先提供真实买入日**(§八 第 11 项)。**✅ 2026-07-28 完工**:契约 `buyDate` + 两个 400 reason(`not_trading_day`/`future_buy_date`)+ 客户端 mapReason 两 case 已落;生产 3 笔已按用户提供的真实买入日纠正(`scripts/oneoff/fix_position_buy_dates.py`)。**客户端日期选择器 UI 归第 ⑧ 块,代码上云归第 ⑨ 块。**
- **[P0-2] `002036.SZ`(联创电子)自 2026-07-23 起无行情分区**,而 `stock_basic.list_status` 仍 `L`;用户自述该票「正筹划股权转让」。**为什么要紧**:该持仓无法定价 → 持仓卡市值/浮盈、K4 每日体检、D5 判向拿不到收盘价;`resolve_time_exit` 无定格时**按设计保守返回「明日时间退出」**,可能催用户去卖一只停牌根本卖不掉的票。**怎么修**:① 先核实是停牌还是数据源缺口(查 TuShare `suspend_d` / 直接看该票近 5 日 `daily` 行);② 定**停牌票在持仓链路的显示口径**(建议:沿用最后有效收盘价 + 明示「停牌 N 日,价格为最后成交价」,时间退出判向挂起且不推送,绝不静默按老价当今日价);③ 口径写进 §2.1 或本节。~~归 v1.3.6 + v1.4~~ → **v1.4.0 第 ① 块(①-B)**,诊断与口径一起落(口径已在 §五 定死,诊断只决定 `reason` 标签取值)。**✅ 2026-07-28 完工**。**诊断结论(真 token 实证,勿再查)**:① `suspend_d` 在 600 元档**可用**;② `002036.SZ` 自 **20260723 起连续在停牌名单**(`suspend_type=S`)= **真停牌,不是数据源缺口**,与用户「筹划股权转让」自述一致;③ 20260728 全市场同类 **9 只**(`002828.SZ`/`300242.SZ`/`300615.SZ`/`300862.SZ`/`300955.SZ`/`300996.SZ`/`688277.SH`/`920685.BJ`)= **普遍现象非个案**。口径落地:`PositionOut.priceStale` 三字段 + 第五态 `suspended_hold` + K4 体检 `dataUnavailable` + §2.1 第 2 条附注。
- **[P0-3] 概念板块表压根没有日更路径 → 「当日暴起板块」那一路候选长期失效**(不是一天的偶发)。`ths_daily`/`ths_member`/`ths_index` 只有**一次性 backfill**(`scripts/backfill_concept.py`,`END="20260722"` 硬编码 + 「已存在则跳过」),而 16:05 的 `scripts/daily_update.py` 只跑 `backfill.DAY_TABLES=[daily, daily_basic, adj_factor, moneyflow_dc]`(+`index_daily`/`limit_derived`),**概念板块表不在日更清单里**;本地实测 `ths_daily` 最大 `trade_date=2026-07-22`。**为什么要紧**:`report/sectors.py::compute_sector_strength` 当日无行 → **返回空列表且不报错**(优雅降级),于是 `intel_candidates` 第①步的「当日暴起 top-N」整路为空(07-27 那 20 只全来自五常驻 + 竞争位)、题材持续天数(`board_age`)也拿不到 → 情报排序少一维;五常驻板块因走 `ths_index` 名字静态解析,反而看不出坏了。**怎么修**:① 给日更加概念板块增量(`ths_daily` 按日追加;`ths_member` 低频重拉,~400+ 指数各一次调用,配额消耗要与 P4-20 一起算账);② **至少先加「板块数据过期 > N 天」的显式告警**,别让它继续静默降级。~~归 v1.3.6 + v1.4~~ → **v1.4.0 第 ① 块(①-C)**,日更管线 + 过期告警一起做。**它是需求 8 板块维度的硬前置**;新增配额消耗与 P4-20 一起算账、部署块 ⑨-E 复核。**✅ 2026-07-28 完工**:`ths_daily` **一次调用取全板块**(实测 `trade_date=` 可用,不必按 code 拉 400 次)、尾窗 5 交易日重拉自愈、按 `ths_index` 过滤成概念板块;`ths_index`/`ths_member` 周更带 `.bak` 且失败不覆盖;`SECTOR_DATA_STALE_MAX_LAG_DAYS=2` + `ReportOut.dataFreshness` + 报告顶部告警 + 最强题材小节标不可信。**新增配额 = `ths_daily` 5 次/日 + `suspend_d` 1 次/日 + `ths_index`&`ths_member` ~400 次/周**(⑨-E 复核)。

### 🟡 P1 · 已知错判 / 噪音(有明确修法)

- **[P1-46] 🟡 → V2.2.0 第 ① 块(顺带修)** · 周末日期炸弹测试 ×2(2026-08-09 用户裁定入册待办;同日 V2.2 立项时改判 —— 本版要长期跑全量套件,留着这两条红会持续磨损「零失败」的信噪比,成本又极低,故并入 ① 一起做) · `test_api_positions.py::test_buy_date_historical_trading_day_written_through` 与 `test_fix_position_buy_dates.py::test_unchanged_position_does_not_bump_updated_at` 直连 `date.today()`,**逢周末/非交易日全量套件必红 2 条**(近期基线里的"2 failed"即它),长期磨损"零失败"信噪比。修法 = A7 冻结时钟体例照搬(`frozen_clock` fixture 先例);A 组修理时已列全仓同款清单(A7 完工记录),可顺带扫尾。来源 = V2.1-① 施工时拆的待办卡。
- **[P1-26] ✅ 已结案(2026-07-29 v1.4.1 热修上云)** —— **信息卡端点在生产要 14 秒,而客户端默认超时 12 秒 → 用户实际体验是「信息卡总是加载失败」**。**修法 = 取数层按 `year=` 裁剪分区(修法①)**:`market_data._scan_table` 加 `years=` 参数,由 `get_market_slice`(单日→1 个年目录)/ `get_stock_history` / `scan_table_range`(区间→覆盖年份)传入;`strategy/features.market_state_labels` 的起点由写死 `date(2019,1,1)` 改为按 MA 窗口回看。**纯 I/O 优化、结果逐位不变**(`TestYearPartitionPruning` 5 条等价单测锁死),合法性依据是结构性的:分区路径由 `day_file_path` 按 `trade_date.year` 生成,`year=YYYY` 目录里只可能有该年的行。客户端 `fetchInfoCard` 同步 `timeout: 60`(照问询台惯例)。**公网实测**:修前 **14.15 / 13.98 / 13.89s**、3 次调用 `memory.events high` **+8945**;修后**冷调 3.05s、热调 2.33 / 2.25s**(**6.2 倍**)、连打 6 次 `high` **恒 0**、`MemoryCurrent` 从 440MB(≈`MemoryHigh`,已在节流)降到 287MB。**⚠ 顺带收窄了脏分区的传染半径**(已如实改写守门单测而非删断言):v1.3.5 那条「历史脏分区不会自愈」现在只对**跨到脏那年**的读成立。~~原文:根因是 `_load_single_code_panel` 全 glob~~ —— **诊断只对了一半**:单票面板确实全 glob,但真正的大头是 `compute_sentiment` 一次请求做 **5 次**全市场横截面(每次全 glob 1592 个 footer),连同单票面板 2 次 + 大盘线 1 次 = **8 次全 glob**;所以修在 `_scan_table` 这一层(而不是只修 `_load_single_code_panel`)才对。
- **[P1-26 原文存档] 信息卡端点在生产要 18~20 秒(可用但慢),根因是 `_load_single_code_panel` 全 glob**(2026-07-29 ⑩-G 判据 4 实测,**plan §五 v1.4-⑩-G 已预授权「若仍 >10s 单独立项,不阻塞 ⑩」**)。⑩ 修完 P0-23 之后该端点**从「永不返回」变成「exit 0、峰值 98MB」**(300M cap 下,3 只实测 20.15s / 18.41s / 18.70s),行业强度那一维已经是**读表 5.3ms**、不再是瓶颈;**剩下的时间全在 `report/info_card.py::_load_single_code_panel`** —— 它为了取单只票的 60 日 K 线,仍走 `year=*/*.parquet` 打开 **1592 个 parquet footer**(开发机 0.56s,生产 2 vCPU 慢 33 倍)。**为什么不算 P0**:它返回、内存也不高,与既有 `holding_k4_check` 同一类路径、已在生产跑通多日;只是交互体验差(点开信息卡等 20 秒)。**怎么修(三选一,先量再选)**:① 按 `year=` 只 glob 窗口覆盖的 1~2 年(60 个交易日最多跨两年,是纯 I/O 节省、不改语义,与 P0-23 方案①「加窗口会低报 streak」的设计冲突**无关** —— K 线窗口本来就是固定 60 日);② 复用报告生成期已装配的面板(需要端点能拿到报告态,契约面更大);③ 同 P0-23 走预计算(体量大,不划算)。**倾向 ①**。**未排期**,归 v1.5 或独立快修。
- **[P1-24] ✅ 已结案(2026-07-30 v1.5-④-A5)** · 章程「回滚」的运维口径未定(代码侧已安全,流程侧空白)(2026-07-29 review 判定线 🟡-1 修复后新立)。判定层已经不怕回滚了 —— 激活历史是 append-only 事件流(`strategy_activation_log`),回滚只在时间轴末尾追加一条事件,回滚前每一段治权的逐笔判定逐位不变;切换器也会打重激活告警 + 留痕。**但「什么情况下该回滚、回滚后 STRATEGY_LAB 现役标注/§九/`~/hz_info.md` 怎么同步、要不要顺手重跑受影响周的复盘」这套流程从没写下来**,而回滚必然发生在事故现场(那时没人有空现编)。**怎么修**:~~等真需要回滚一次之后按实际经历补一节运维口径,别现在凭空编~~ → **改判(用户 2026-07-29 纳入 v1.5 配菜)= v1.5.0 第 ④ 块(④-A5)**。**✅ 2026-07-30 完工**:一页纸 SOP `archive/SOP_章程回滚_20260730.md`(四节:① 什么情况该回滚〔判据非感觉,三档:客观配置错误 / 实盘归因证明更差 / 紧急止损〕② 操作序列〔四道闸 + 双备份 + append-only 事件流语义〕③ 同步清单〔STRATEGY_LAB 现役标注只在跨 K 血缘时才需要动、PROJECT_PLAN §2.1/§四/§九、`~/hz_info.md`〕④ 要不要重跑受影响周复盘〔判据 = 回滚时刻是否落在已生成复盘的周内 + 命令 = 重传同周交割单,`reviews` 表按 week 幂等覆盖〕),**文首如实标注「未经实战演练,首次真回滚后按实际经历修订」**——只写代码侧已确认事实与同步清单,不编造未验证的操作细节(原条"别凭空编"的精神保留)。§2.1 第 3 条末已加一行指针。
- **[P1-4] ✅ 已结案(2026-08-04 核实兑现)** —— 修法已落地:`brain.config_governing_at()` 存在,`review/reconcile.py` **6 处**按成交时刻逐笔取章程(v1.4-⑥-A 兑现,`RawTrade.trade_time` 只有日期时按该日收盘取)。原文如下(留痕)。
  **[P1-4 原文]** 周复盘按「周」粒度取 governing 章程,激活当周的交易被旧章程判。`brain.config_governing_for_week(week_start)` 判据是「激活日 `<` week_start」,整周一把抓。**后果**:本周(2026-07-27~08-02)用户按 v1.3.3 三仓 4 万打,周复盘仍按 K1 判 → 单笔 >2 万、买创业板会被**误标违纪**(**已知假警报,用户已接受本周噪音,不为一周的显示问题动纪律时间线**)。**正解**:改成**按成交时间戳逐笔判**(成交时刻早于激活时刻按旧章程、之后按新),`review/reconcile.py` 取 config 的入口从「周」下沉到「笔」,报告里注明该周发生过章程切换并分段计数。→ **v1.4.0 第 ⑥ 块(⑥-A)**,🔴 碰纪律判定,列入 review 必审项。
- **[P1-5] ✅ 已结案(2026-08-04 核实兑现)** · → V2.0.0 第 ② 块 —— `llm/budget.py` 三本账 **`LEDGER_SEARCH` / `LEDGER_REASON` / `LEDGER_REVIEW`** 齐备且互不透支(单一 300s 墙钟已退役)。消息面 LLM 扫描 300s 墙钟预算不够。**V2 处置**:预算重排为**三本账**(检索 / 推理 / 复盘,独立不合并)+ 降级次序定死(先丢 T3 简评 → 再丢 T2 复盘细节 → 绝不牺牲卡冻结与纪律外壳);扫描域也随选股换血从「持仓 + 自选」变为「持仓 + T1/T2 篮子成员」(自选池已删)。原文如下(留痕)。
  **[P1-5 原文]** 消息面 LLM 扫描 300s 墙钟预算不够。`report/news_alerts.py:161 LLM_SCAN_BUDGET_SECONDS=300.0`;3 持仓 + 16 自选 = 19 标的,2026-07-27 那次预算耗尽、**8 只自选未扫**(已由 `newsAlertsScan.codesSkipped` 如实披露,没有静默)。自选只会越来越多。**怎么修(三选一)**:抬预算(报告是 oneshot,但要顾 `MemoryMax` 与整体时长,见 P4-16)/ 并发(**先验 GLM 供应商限频,别拍脑袋开并发**)/ 分批轮扫(持仓优先已实现,自选可隔日轮)。→ **v1.4.0 第 ⑥ 块(⑥-B)**:先实测供应商限频,再按实测**二选一**(受控并发 or 自选隔日轮扫),不折中。
- **[P1-6] ✅ 已结案(2026-08-04 核实兑现)** —— `PositionOut.timeExitLockedDay` / `timeExitLockedLateDays` 已在 `api/schemas.py` 落地(v1.4-⑥-C 兑现;纯派生展示位,**判向逐位未变**)。原文如下(留痕)。
  **[P1-6 原文]** 「定格日 ≠ D5」无标注。EOD 管线连续断跑时,时间退出按「首个可得 EOD」定格(可能已是 D7/D8),系统如实写 `time_exit_locked_date` 但界面不提示。**怎么修**:`PositionOut` + 报告持仓卡显式标注「定格于 D{n},晚于 D{max_hold_days} {k} 天」,**只提示、不改判定逻辑**(定格语义是审计修复 🔴-1 的结论,不得回退)。→ **v1.4.0 第 ⑥ 块(⑥-C)**。
- **[P1-7] ✅ 已结案(2026-07-30 v1.5-④-A3)** · `search_engine` 选型待数据定。2026-07-27 实测 `search_pro` 2 条 / `search_std` 2 条 / `search_pro_sogou` 10 条(**单日样本,不足以定**);当前维持 `search_pro`(用户拍板),命中数埋点已随 v1.3.4 上线。**怎么修**:~~攒几天后捞 `llm_judgments` 的命中分布(按日 × 按引擎)再定~~ → **v1.5.0 第 ④ 块(④-A3)**。⚠ **原文那句"按日 × 按引擎"是当时的想当然,已纠正**:`llm_judgments` **没有引擎列**,且生产**从来只用过 `search_pro`** → 现有数据只能给「`search_pro` 的按日命中分布」。故 ④-A3 三步:① `llm_judgments` 幂等加列 `search_engine`(由 provider 写当次实际取值,老行 `NULL`=未记录、不回填猜测);② 捞 20260724(命中恢复日,见 P4-17)至今的按日基线(**只读 SQL,守生产探针纪律**);③ 基线 0 命中率显著(近 10 交易日 >20%)才做一次**受控 A/B**(同批 5~10 只候选,`search_pro` vs `search_pro_sogou`)再改常量,否则维持 `search_pro` 结案;**无 key / 样本不足 → 只出基线,本条继续挂账并写明"引擎维度需 A/B、未做"**。**不许在没有 A/B 证据的情况下换引擎。**

  **✅ 2026-07-30 三步完工**:①`llm_judgments.search_engine` 幂等加列(`_migrate_columns`)+ `OpenAICompatProvider._search_engine_value()` 钩子(默认 `None`)+ `GLMProvider` 覆盖读 `_SEARCH_ENGINE` 单一源常量(与 `_search_tools` payload 同一处,不重复硬编)+ `LLMResult`/`JudgeResult` 透传 + `store.save_llm_judgment` 落库(仅成功路径非 `None`,老行/降级路径 `NULL`=未记录不回填猜测)。②**生产只读 SQL 实捞**(`ssh deploy@118.178.122.194` + `sudo sqlite3 "file:...?mode=ro"`,收盘后 22:49 CST 执行,守生产探针纪律;只 SELECT `trade_date`/`ts_code`/`degraded`/`json_array_length(search_hits_json)` 四列,secrets 不落输出):窗口 20260724~20260729(命中恢复日至今,**实际仅 4 个交易日**——数据库里就这么多,不是抽样)、**40 条非降级审判**(4 日 × 10 只,当时仍是 v1.4.1 生产代码的 `TOP_N_JUDGED=10`)、**0 条降级**、**0/40 零命中(0.0%)**,分布 min=1/p25=10/median=10/p75=10/max=10(GLM 顶层 `web_search` 数组多数打满,少数几只命中偏低〔1~3 条〕但均非零,不构成"静默失效")。③**定案**:0% ≪ 20% 显著阈值(即便按二项分布保守估计,0/40 事件的 95% 置信上界≈7.5%,同样远低于阈值)→ **判据③「基线健康 → 维持 `search_pro`,结案」**,**未消耗任何 A/B(本地本就无可用 GLM/Kimi key,且基线健康也不构成 A/B 触发条件)**。诚实登记:窗口只有 4 个交易日(受限于「命中恢复日」到「今天」本就只隔这么多个交易日,并非抽样不足),但方向性证据在全部可得数据上一致(4/4 天零命中率均为 0%),继续观察不会改变判断方向;若未来出现零命中骤增,P1-7 可重开。
- **[P1-8] → V2.0.0 第 ③ / ⑥ / ⑨ 块(部分吸收,部分仍挂账)** · 启发式阈值待实盘校准:`intel_candidates.py` 的 `INDUSTRY_GATE_MIN_LIFT=2.0` / `QUOTA_PER_PERMANENT_BOARD=2`、熔断的 3 笔连亏与单日 −4000 元、盘前校准各阈值 —— 全是启发式,零实盘样本。**怎么修**:攒 1–2 个月实盘 + 周复盘数据后**一次性回看这一组**(哪个挡多了 / 放多了),不要逐个单独拍。~~归 v1.4+~~ → **V2 处置**:① **选股侧阈值**(行业闸 lift、卫生线、Tier 权重)全部进**策略包 config**(③),校准路径 = ⑨ 评价引擎周度报告 → 策略线出新包 → 四道闸激活,**不再是"改代码里的数"**;② `QUOTA_PER_PERMANENT_BOARD` 随五常驻删除**作废**(裁定 #9);③ **熔断的 3 笔 / −4000 与盘前校准阈值仍是政策值、仍挂账**(住命名常量,不进包也不进章程),本条为这三项继续保留。**⚠ V2.2 扩容(2026-08-09)**:K8 的**三引擎首版阈值 + 落地起跳位置关五项阈值**一并进本条这一组(细目登记在 **P3-34 (c)(d)**,本条只记「它们属于同一组、要一次性回看」);**校准路径已改** —— 不再是「攒 1–2 个月再一次性回看代码里的数」,而是 **选股时钟攒样本 → 周度四分类建议 → 用户确认 → 引擎升版(`C2`/`Z2`/`Y2`)走四道闸**(K8 §十七 自带的迭代机制)。⛔ 仍然:**不要逐个单独拍**、**不许自行改数**。

- **[P1-36] ④ 扫描层两条「三路等价」守门单测在全量套件下偶发失败、单独跑恒绿(V2-⑪ 施工期两次复现,2026-08-03 新挂;⑪ 完工记录「块外发现」的落点 —— 原文只写了"已挂账"但没有账本,在此补上)**。**症状**:`tests/test_scan_cluster.py::test_bulk_vs_day_by_day_vs_readback_are_identical` 与 `tests/test_scan_corr.py::test_bulk_vs_day_by_day_vs_readback` 在 `pytest tests/ -q` 全量下**偶发** failed,**单独跑恒 pass**;⑪ 开工基线首跑复现一次、施工期又复现一次,重跑即过,**与 ⑪ 零关联**(⑪ 一行没碰 `neckline/scan/`)。
  - 🆕 **第三例(2026-08-09 V2.2 ⑥ 收尾时 orchestrator 复跑撞上,换了一个文件、同一个病)**:
    `tests/test_report_consistency.py::TestHistoricalReplayAcrossMultipleDays::test_replay_of_the_same_day_twice_is_identical`
    —— **同一次交回后连跑三次:第 1 次 failed、第 2 次 passed、单独跑 passed**。已核实**仓库无
    `pytest-randomly`/`xdist`、无 `addopts`** → **执行顺序是确定的**,故「顺序随机」这个解释被排除,
    剩下的两种成因(夹具跨用例污染 / 真实不确定性)**都还在桌上**。⚠ **本例尤其刺眼**:这条测试的
    断言就是「**同一天回放两次必须逐位相同**」—— **一条检验确定性的测试自己不确定**,而报告可复现性
    是本项目归因与历史回放的地基。**⛔ 别再拿"重跑就绿"糊过去** —— 现在有三例、跨三个不同模块
    (`scan_cluster` / `scan_corr` / `report_consistency`),共性是**都在断言"多路/多次结果逐位相同"**,
    这更像有一处共享的不确定源(候选:`rank(ordinal)` 并列打散 · 字典/集合迭代序 · 浮点累加顺序),
    而不是三个各自独立的脏夹具。**下次专门查它时,从这个共性入手。**
  - **⚠ 为什么这是 P1 不是 P4(测试卫生)**:两种成因**性质完全不同,必须先分清是哪一种**——
    - **(a) 测试夹具的跨用例状态污染**(临时库 / 缓存 / 工作目录残留)→ 只是测试问题,修夹具即可,**良性**;
    - **(b) ④ 的 `bulk` 与 `day_by_day` 两条路径本身存在真实不确定性**(polars 分组顺序残留、`rank` tie-break 没排干净)→ **这是真 bug 且撞在项目明令禁止的那条线上**:CLAUDE.md「`rank(method="ordinal")` 的并列由行序打散 = 不确定性,**任何进判据 / 排序的 rank 必须先排定确定性 tie-break**」(2026-07-29 行业强度那次真数据打出来的)。若是 (b),意味着 `limit_cluster_daily` / `corr_matrix_daily` **同一天算两次可能不一样** → 历史回放不可复现、⑨ 的按包归因失去可比性。**这条守门单测存在的全部意义就是拦 (b)**,它偶发红本身就是它在报警。
  - **怎么查(判据先行,别一上来改测试)**:① 固定 `PYTHONHASHSEED` + `-p no:randomly` 复跑,能稳定复现就偏 (a);② 单独跑该文件 N 次 + 全量跑 N 次对比失败率;③ 失败时把两路输出 dump 出来**逐行 diff** —— 差在**行序**还是**数值**:行序 → (b) 的 tie-break 问题,数值 → 夹具数据被别的用例改过 = (a);④ 查 ④ 的 `bulk` 路径有没有对分组结果做 `sort` 收口。
  - **⛔ 在分清 (a)/(b) 之前,不许把这两条断言放宽或加 `flaky` 重试** —— 那等于把报警器拆了。
  - **归属**:④ 扫描层。**排期建议:与 ⑧-E 同批**(都在 ⑬/⑭ 之前),因为若是 (b),⑨ 攒的归因数据同样会被污染。
  - **✅ 2026-08-03 结案(@builder)——诊断 = (a) 类良性测试缺陷,已明确排除 (b)**:四步判据执行完毕。①②标准跑复现:目标两文件连续 20 轮 100% pass(标准);另跑 8 轮全量 `pytest tests/ -q`(每轮 63~120s,期间总数因 ⑫/⑪-B 并行施工持续增长 3012→3055,非本项异常)**未自然复现**——但据此**不能**判"问题不存在",故补做③工程复现:用 `unittest.mock.patch.object` 强制 `cluster._now()`/`corr._now()` 在 bulk 调用与 day-by-day 调用两阶段分别返回不同常量(等价于"两阶段跨越了墙钟秒边界"),**精确重放了症状**——`.equals()` 判 False;逐行 diff 后**业务列(cluster_key/ts_code/cluster_size/anchor_industry/anchor_concept;scope_key/code_a/code_b/corr/n_obs 等)全部逐位相同,唯一分叉的是 `computed_at`**。④ 复核 bulk 路径:两个测试早已在比较前 `.sort(["cluster_key","ts_code"])`/`.sort(["scope_key","code_a","code_b"])`,收口已在,不缺排序。**真因结论**:既不是原判 (a) 描述的「夹具跨用例污染」,也不是 (b) 的「polars 分组顺序 / rank tie-break 不确定性」——`refresh_limit_clusters`/`refresh_corr_matrix` 对同一天的计算在 bulk 与 day-by-day 两条路径下逐行代码等价(day-by-day 只是把同一份单日纯函数逐次调用),**业务结果从未分叉过**,故明确排除 (b),⑨ 的按包归因可比性不受影响。真因是第三种、比 (a)/(b) 更窄的良性缺陷:`computed_at` 是秒精度墙钟审计戳(docstring 原文即"审计:这行什么时候算的",按设计随每次调用变化),两个「三路等价」测试的 bulk 调用与 day-by-day 调用是**两次独立调用**,后者工作量更大(重复加载 `industry_of`/`concept_of`、3 次独立 SQLite commit 而非 1 次),全量套件下系统更忙 → 两次调用更容易跨越墙钟秒边界 → `computed_at` 合法不同 → 整行 `.equals()` 把审计列的正常差异误判成"业务不一致"。**同一坑本项目已有两处正确先例**(`tests/test_industry_strength_store.py` 的 `{k:v for k,v in r.items() if k!="computed_at"}`;`tests/test_scan_stage.py::_table_rows_excl_computed_at`,其 docstring 明文"排除 computed_at"),`test_scan_cluster.py`/`test_scan_corr.py` 建于同批(V2-④)却漏抄同一体例。**修法**:两个目标测试的 `.equals()` 比较前各自 `.drop("computed_at")`(business 列仍要求逐位相同,不放宽、不加 flaky 重试),docstring 补充说明根因;**未改动 `neckline/scan/cluster.py`/`corr.py` 生产代码一行**。验证:目标文件连续 20 轮 100% 绿;全量 `pytest tests/ -q` 零回归(与并行施工的 ⑫/⑪-B 共存,总数持续波动,详见 §九)。**⚠ 顺带发现但不在本次范围内(留档不改)**:`tests/test_scan_leader.py::test_refresh_is_deterministic_and_readback_matches` 同款比较 `first.equals(second)`(同日两次调用)存在相同的未排除 `computed_at` 潜在隐患,只是只有 1 次重跑(而非本条两测的 3 次独立调用)使窗口小得多、大概率从未被撞上,不代表天然安全;建议下次碰 `leader.py` 测试时顺手补 `.drop("computed_at")`。

- **[P3-37] 退潮红色刹车「主线跳水」路自 ⑧-G 起从"聋"变"能响",灵敏度待实盘校准 + 须让用户知情(2026-08-03 新挂,⑧-F 对拍打出来)**。**事实**:该路在 V1/V2 现口径下**近乎失效** —— 真跳水日(07-24 全市场中位 −2.86%、89% 个股下跌)样本读数 **−0.18%**,而主线板块本身 **−2.98%**(已贴 −3% 阈值),差 **2.8pp**;四天 0/4 触发。**V1 时代样本是候选池、同样偏动量,大概率一直就是聋的,只是从来没人量过。** ⑧-G 修好后它会**开始真的响**。
  - **✅ 2026-08-03 ⑧-G 已完工,实测把这条挂账收窄了一半(数字见 §五 ⑧-G 完工记录)**:**样本这一半已修好** —— 切片读数与「主线板块本身」per-seed 最大差 0.53pp、07-24 只差 0.05pp;**但同四天仍 0/4 触发**,因为主线板块本身(−2.75% per-seed / −2.98% pooled)就在 −3.0% 阈值**之上**。⚠ 故待校准的**只剩阈值灵敏度**,且「上线后第一次红色刹车」可能比预期来得晚(不是"修好就会天天响")—— ⑰ 换包说明的告知照写,但别把它写成"马上会经常触发"。
  - **⚠ 为什么不需要用户事前批准、但必须事后知情**:⑧-G **恢复的是 §2.4 早就写明的既定行为**(「主线板块跳水 → 今日计划作废、禁开新仓」),不是新增能力;而且 07-24 那种日子**本来就该响**(§1.3 交割单归因:买入下跌途中票是主要死因)。**但用户此前从未经历过这条刹车触发** —— 上线后第一次红色刹车会是全新体验(**全天禁开新仓 + 立即级推送**),**⑰ 换包说明里必须有一句告知**,别让它像故障一样冒出来。
  - **待校准的是灵敏度,不是要不要有**:`sector_dive` 阈值(−3% / 早盘 −4%)是 V1 传下来的启发式,**从未在"样本不聋"的前提下被检验过** —— 换句话说,这个阈值当年是配着一个失效的样本定的,现在样本修好了,**阈值的实际含义变了**。攒够实盘样本后须回看:触发频率是否合理、有没有把正常回调日误判成跳水。
  - **⚠ ⑧-G 完工后这条已经可以量化了,交给策略线 / 用户时直接用这组数(2026-08-03 补)**:
    - **老样本系统性偏乐观 1.51~2.93pp**(⑧-F ∩池样本 vs 主线本身,四天 per-seed 误差);**新切片误差 ≤0.53pp,真跳水日仅 0.05pp**。
    - **推论:同一个 −3.0% 阈值,在样本修好之后实际上比当年设它的人以为的严了约 1.5~2.9pp** —— 当年"读到 −3%"对应的真实主线跌幅其实是 −4.5%~−5.9%。**阈值的数字没变,它的含义变了。**
    - **最有说服力的一个点**:07-24 全市场中位 **−2.86%、89% 个股下跌**,主线本身 **−2.75%(per-seed)/ −2.98%(pooled)** —— **这样一天仍然不触发**。要不要在这种日子响,是**产品决策**,交用户。
    - ⚠ **别把这组数读成「所以该把阈值调到 −2.7%」** —— 四天不是样本,且 K=6 能凑出触发这件事恰恰说明**这个区间对参数很敏感**。**先攒实盘,再谈调数。**
  - **一并回看(补一条)**:`MIN_MAINLINE_SAMPLE=5` 作用在**票数**上,**没有**作用在**主线条数**上 —— 2 颗种子时 per-seed 估计量只是在平均 **2 个数**(n=8 已过下限)。这在数学上偏薄。**⛔ 现在不加第二个常量**(无证据、且实测日常 10~41 颗种子,离这个角落很远),但回看时一并看:要不要给**主线条数**也设个下限。
  - **⛔ 在有实盘证据之前**:阈值一字不动(⑧-G-F 已锁);⛔ **不许通过调 K 或调样本口径去凑触发频率**(那是偷偷改阈值);要动阈值走**用户拍板**(它是纪律触发器)。
  - **一并回看**:⑧-G 的 K=4、两份测量样本的保底配额、per-seed 估计量 —— 三者都是 planner 按统计与语义定的**工程默认,无实盘背书**。**什么时候回看**:积累 ≥1 个月实盘 + 至少经历 2~3 次真实急跌日之后。**不排期。**

- **[P3-38] 「对账结果」(周复盘 `WeeklyReview`/`RoundTrip`)未作画像数据源 —— 维持不接,正确做法留档(2026-08-04 补稳定 ID;⑫ 完工记录第 8 点 → ⑭ 已裁定,但一直没落 §七,属账本漏登)**。**裁定 = 维持不接,⛔ 不打通**(全文见 `archive/V2_契约三方对拍_20260803.md` §十),三条理由:① **无确定性外键是硬的** —— `RoundTrip` 是「交割单里的一笔回合」、`positions` 是「用户手录的一笔仓」,**没有共同主键**;靠「金额 ± 容差 + 日期邻近」匹配,在**同日同票分批建仓**这个本项目常见场景下会**系统性错配**。② **错配的代价落在画像上,而画像是「结论」** —— 一次错配就把别人的盈亏算到某个题材 / 角色头上;这与「样本不足时如实标低置信度」是同一条纪律的两面:**宁可没有这一维,不要一个可能是错的维**。③ **现有数据源已覆盖产品意图** —— 偏好 / 能力两张账吃 `positions` + `entry_snapshots` + `position_plans` + `user_actions` + ⑨ 评价引擎产出,「胜率 / 盈亏比 / 是否跑赢同篮未选」三个核心问题都答得了;对账能额外贡献的**真实费用与资金流**已由 ⑫-A 的 `review/cashflow.py` 四分类**单独产出、不必混进画像**。**⚠ V2.2 附注(2026-08-09)**:K8 交易时钟(`trade_clock`)**同样不与交割单做任何近似匹配** —— 它的外键是 `position_id`(确定性),与 `RoundTrip` 无关;本条「将来若要接」的路径**一字不变**。**⚠ 将来若要接,正确做法(留档,别到时候现想)**:**先有确定性关联再谈接入** —— 要么在录入侧让用户把成交与交割单行**显式配对**,要么等券商接口直读持仓(§9.3 后续升级点 6)拿到真实成交流水号;**⛔ 不许用近似匹配凑**。**不排期。**

### 🟠 P2 · 审计欠账(最该补的一块)

- **[P2-47] 🟠 → V2.2.0 第 ① 块(本版结案,修法取首选:抽共享常量)** · 选股域与回测域卫生线判据双源,无运行时纽带(2026-08-09 入册;同日改判 —— **K8 §三 改股票池〔排科创板 / 排白酒〕动的正是这条缝,不修则一改必漂**,故不再往后排) · V2.1-① 核实:`selection/primitives.py::stock_hygiene` 是**独立手写谓词**,其默认值(close_min 2.0 / amount_ma20_min 20000 等)与回测域 `neckline/research/panel.py::base_universe_expr`(⚠ 包内那份,**没随策略档案迁出**)的一致性**只靠 docstring 人工声称**——无 import、无守门。任何一边改数另一边**静默漂移**,选股与回测对"什么票配入场"分叉且无人报警(「规则常量唯一源」纪律的隐性破口;plan 曾误记两者有 import 依赖,已订正)。修法:抽共享常量(首选)或加两处逐位对拍守门(退而求其次)。来源 = V2.1-① 拆的待办卡(task_649b4081)。
> **📁 已出 review 报告清册(全在 `archive/`,2026-08-05 收官时补登;按需取用,勿重复审)**:v1.3 纪律与金额 `REVIEW_REPORT_v1.3_章程金额审计_20260727.md` · v1.4 两线 `REVIEW_REPORT_v14_判定线_20260729.md` / `REVIEW_REPORT_v14_契约线_20260729.md` · v1.5 两线 `REVIEW_REPORT_v15_判定线_20260730.md` / `REVIEW_REPORT_v15_契约线_20260730.md` · **V2 三份** `REVIEW_REPORT_V2_判定线_20260803.md` / `REVIEW_REPORT_V2_契约数据线_20260803.md` / `REVIEW_REPORT_V2_小审_⑭⑮_20260803.md`。**七份均已逐条销项标注 commit**。**⚠ 诚实登记**:V2 的三份都停在 **⑮ 完工那一刻**(2026-08-03)—— **⑯ 新机建成 + 数据搬家 + 晚间链三段化、⑰ 割接切换、以及 2026-08-05 的生产库点删 / EOD 补拉 / 报告补跑 / P0-39·P0-40 快修,全部未经独立复审**;用户 2026-08-05 拍板「V2 定型、不再补 review」,故这是**已知且被接受的欠账**,⛔ 别当"已审过"。同批留档的 V2 对拍类产物:`V2_契约三方对拍_20260803.md`(契约三方对拍表)、`V2-⑬_删除前后对照表_20260803.md`、`K4_advisory_B3退役_20260804.md`(B3 退役前后哈希与 diff)。

- **[P2-9] 三路独立审计只完成一路**。已完成:**纪律与金额**(2026-07-27,报告 `archive/REVIEW_REPORT_v1.3_章程金额审计_20260727.md`,抓到 4 条主缺陷 🔴-1/🟡-2/🟡-3/🟡-4 + 5 条小修,全部已修并随 v1.3.1 上云)。**未做的两路**:① **情报官 / 候选管线** —— K4 安检实现与 DB `k4_advisory` 六节**逐条对拍**(`hard_cut` 真拦、`avoid_flag` 只打标)、四处「诚实字段」是否真透到客户端(`newsAlertsScan.codesSkipped/codesFailed`、搜索 0 命中脚注、板块资金流 `unavailableReason`、行业闸被挡票的审计留痕);② **契约 / 客户端 / 迁移三方对拍** —— `api/schemas.py` 声明 → `app.py` 转发 → Swift 解码 **逐字段走一遍**,该版**已两次出现「领域层算了、序列化层漏了」的漏字段**,这是复发型缺陷。→ **改判(用户 2026-07-29)**:~~v1.4.0 施工完成后 / 上云后~~ → **在 v1.4-⑩(P0-23 修复)完工、⑩-G 生产实测判据全绿之后,重新上云(⑨)之前**由用户召唤 reviewer 一并做掉(plan 里仍不排 review 施工块)。⚠ v1.4 的 **②-C / ③-G 两份对拍报告**是这次审计的现成输入。**✅ 2026-07-29 结案**:报告 `archive/REVIEW_REPORT_v14_契约线_20260729.md`(两路 + v1.4 契约 14 条三方对拍 + 404/400 reason 与 `mapReason` 互为闭包)—— **无 🔴**,3 张 🟡(`intel_order` 零消费 / exec_hint C3 时区缝 / Swift `inquiryId` 断链)**当天全修**(§九 一行)。**遗留 🟢 观察 4 条不做,只留档**(报告 §二):A4 卫生线码永不命中 × forced 豁免叠加(海选池写入方已休眠,**未来恢复时补 forced 票卫生线标注**)/ `_evaluate_hits` 对「无 EOD 行」三消费点姿势分叉(实测影响≈0)/ `InfoCard` 合成 Codable(**若该端点改读冻结快照必须换手写容错 init**)/ 渲染层 `search_hits` 列表 vs 条数分叉(有 `int(... or 0)` 护栏)。
- **[P2-10] 已发布版本未 review**:`v1.3.1`(审计修复 + 行业闸 lift + 首次带迁移的部署)/ `v1.3.3`(拆墙 + 问询台改自由分析师 + `ts_code` 归一 + **章程二次激活** + **切换器闸 2 窄豁免**)/ `v1.3.4`(provider 层 `search_query`,四处 LLM 调用共用)/ `v1.3.5`(**生产数据迁移:902 个 parquet 分区 cast** + canonical schema 声明 + 候选管线保险丝)。含高危面:**生产数据迁移、给护栏开的豁免口子、章程两次激活、共用 provider 层改动**。⚠ **`v1.3.2` 在 §四/§九/git log 里都没有记录 = 跳号,不是漏记**,别去找。→ **与 P2-9 同批**,时机同上(**⑩ 完工 → review → ⑨ 上云**,用户 2026-07-29 改判,原「上云后」作废);本轮 review 范围另含 **⑥-A 逐笔章程** 与 **⑩ P0-23 修复**。**✅ 2026-07-29 结案**:报告 `archive/REVIEW_REPORT_v14_判定线_20260729.md` —— 四个已发布版本的高危面逐条 git 取证放行(v1.3.1 行业闸 lift / v1.3.3 二次激活 + 闸 2 窄豁免留痕真落 / v1.3.4 `search_query` 四态 payload 逐字节 / v1.3.5 `TABLE_FLOAT_COLS` 声明守门),**无 🔴**;2 张 🟡(回滚洗白历史判定 / 行业强度日更静默跳洞)**当天全修**(§九 一行,🟡-1 单独 commit `b2e3673` 供复核)。**遗留 🟢 观察不做,只留档**(报告 §二 10 条),其中三条**将来动到那块代码时必看**:🟢-3 冷却判据锚「再买日收盘」而非真实再买时刻 + `_cooldown_violation_in_week` 靠正则从文案抠日期(现役 `cooldown_days=0` 恒 no-op,**启用冷却前先把返回结构化**)/ 🟢-6 refresh 落行统一贴请求日 `day_key` 不校验分区内 `trade_date` / 🟢-7 `INSERT OR REPLACE` 不删重算后消失的 (day, industry) 行。

### 🔵 P3 · 功能挂账

- **[P3-49] 🔵 K8 的「落地起跳」位置关落在 K3 已判死的域里 —— 前向证伪义务(2026-08-09 V2.2 立项时 planner 挂;🔴 本版最尖锐的一条,⛔ 别当它已解决)**。**事实(不加修饰)**:K8 §二 核心逻辑 =「选择完成下落或调整、确认支撑并**刚刚向上启动**的核心股票」,翻成机械判据后(§五 ③-C)判据 1+2+4 的形状 = **K3-B2 臂③「升势回撤 + 启动确认」**,而那一臂的判决原文是「**确认信号 = 死猫跳顶点,比直接买更差**」(`research/k3_report.md`,`STRATEGY_LAB.md` 雷区地图第 3 条);判据 1 单独用的形状 = **K3 系统化超跌反弹四臂全灭 + 2026 生存门禁七臂尽墨**。**⚠ 这不是「K8 错了」的意思** —— 三条事实并存:① 用户已拍板「K8 没有讨论空间」,本版照办;② **K8 §九 排除项九条里有六条与雷区地图正面同向**(不接下落猜底 = K3 超跌死 / 不追当日急拉与新高 = K2 追强势死 + K7-C1 诱多 / 拒单一指标与伪共振 = K4 前置 H1·H4 反向 / 拒后排跟随 = H10 龙头结构过 / 拒单日脉冲题材 = H11 五态)—— **K8 的排除项本身就是雷区地图的产品化**,冲突只落在「位置关正向判据」这一处;③ V2 起本项目选股链**本来就与回测信号解耦**(§3.8-(b)),输出「值得关注的票」而非「会涨的票」,终选权在用户 —— K7 也是靠这一条合法存在的。**故本版处置 = 不裁、不辩、只留证伪义务**:(a) `neckline/scan/landing.py` 模块头必须原样写上这段雷区对照(⛔ 后人不许当新发现,也不许当禁令);(b) **⛔ 位置关产出不得出现在任何声称「期望」/「胜率」/「会涨」的文案里**(卡面 / 报告 / 客户端一律「参考、非指令」);(c) **真伪由 K8 自己的选股时钟在前向样本上回答**(K8 §十七)—— 周度按引擎归因必须能出「`landing_state` 四态 × 后续表现」的分层成绩单,**这是本条唯一的结案判据**。**✅ 2026-08-09 用户裁定 #10:按 K8 走,「信实盘别信回测」** —— 两条义务保留,并追加两条口径:**(d) 判据来源写死 = 选股时钟的实盘前向数据**(`selection_clock` 已结案样本),⛔ **不为本条立任何回测战役 / 事件研究**(策略研究档案已整体迁出本仓,§1.6;要跑得先重建 5.5G 面板,而用户明确不走这条路);**(e) 雷区对照只作"知情登记"、不作"否决依据"** —— 写进 `landing.py` 模块头是为了让后人知道这块地历史上塌过,**不是给谁一个拦下 K8 的理由**。**结案条件(判据不是感觉)**:选股时钟累计 ≥ 100 个已结案篮子样本,且「`liftoff_confirmed` 组 vs `landing_pending` 组 vs 安慰剂基线」三方对照跑出方向性结论 —— **结论无论正负都必须写进 §九 与移交件,⛔ 不许只在结论为正时才报**。**不排期,但 ⛔ 在结案前不许对外称 K8 位置关「有效」。**
- **[P3-51] 🔵 行情状态层第五维「近期 T1/T2 正确率」在样本攒够前恒缺席(2026-08-09 V2.2 立项时新挂;**已知冷启动边界,如实披露即可,不是缺陷**)**。**事实**:K8 §一 把「近期 T1、T2 正确率」列为三态判定的五个依据之一,而它的数据源 = **选股时钟已结案样本**(§五 ④-A),而选股时钟在 **V2.2 批 3** 才上线 → 批 1 到批 3 之间(以及批 3 之后最初 10 个交易日)该维**恒 `available=false`**。**处置**:`market_regime_daily.inputs_json` 该维如实标 `unavailable_reason='clock_samples_insufficient'`、`regime_reason` 记一条 `missing:t1t2_accuracy`;**⛔ 不许把缺维当 0 参与比较**、**⛔ 不许因为少一维就不落行**(四维照判,少一维是「证据薄」不是「判不了」)。批 3 之前退化读 `basket_verification` 的 EOD 定论行作近似,如实标 `source='basket_verification_fallback'`。**什么时候自动消失**:选股时钟累计 ≥ 10 个交易日的结案样本。**不排期。**
- **[P3-53] ✅ 已销案(2026-08-09 当日,用户自行执行)** · ~~K8 未经策略线立项,`STRATEGY_LAB.md` §四 仍停在 K7 —— 双线账本不同步~~ —— **用户当日把策略研究档案整体迁出本仓**(顶层 `research/` 全目录 + `STRATEGY_LAB.md` + `K4_STRATEGY.md` + archive 五份研究档案 → `~/Lino/whynotme/`,见 **§1.6**),本条指向的那份图纸已不在本仓、双线账本不同步这个问题**随载体一起消失**。⚠ **两件事没变、别当它们也一起没了**:① **K8 的假设仍是零回测背书**(与 §五 ⑤ 风险登记第 3 条同一件事的两面);② **位置关落在 K3 已判死域的前向证伪义务仍在 → 见 P3-49**(用户裁定 #10「信实盘别信回测」已定死判据来源 = 选股时钟实盘数据)。⛔ 别因为本条销案就以为 P3-49 也一并了结。
- **[P3-47] 🔵 → V2.2.0 第 ⑥ 块(本版结案,按原文点名的**首选**做法办:客户端加日期选择器 + 复用既有 `GET /report?date=`;⛔ **仍不许**新建「复盘历史」端点去现连 `basket_review_daily`)** · 改判理由:K8 §十五 明写「主页面展示结论与状态变化,**完整历史保存在案例详情中**」—— 逐日回看在 K8 下由「第二层需求」变成硬需求。原文如下(留痕):复盘板块「每日页」只看得到最近一份复盘,没有逐日回看(2026-08-07 V2.1 立项时新挂,**已知边界、刻意本版不做**)。**事实**:④ 昨日篮子复盘住在 `reports.basket_daily_json` 这份**随报告冻结**的快照里(V2.1 第 ⑤ 块核实后定死"服务端数据段不迁、客户端只搬展示",契约改动 = 0),而客户端只拉 `/report/latest` → **复盘板块每日页 = 最近那一份**。想回看上周三那天的复盘,现在只能靠 `GET /report?date=` 手工调。**为什么先不做**:V2.1 的产品目标是「把散落的证据装进一个房间」,逐日回看是浏览器式的第二层需求,且**累计页已经覆盖了"长期看得怎么样"这个真问题**(校准报告按窗口分层)。**将来要做的正确做法(留档,别到时候现想)**:① 客户端加日期选择器 + 复用既有 `GET /report?date=`(零服务端改动,**首选**);② ⛔ **不许**为它新建一个"复盘历史"端点去现连 `basket_review_daily` —— 那会绕开冻结快照、让回看到的复盘与当时那份报告讲不同的话(违反 §2.8-B 第 1 条「模型判断新判断 = 新版本,不回写」)。**不排期。**
- **[P3-44] 🔵 → V2.1.0 部分落地(杠杆③ 天然兑现;估算须按 §五 附「成本与超时算术」重写)** · LLM 成本观察(2026-08-05 用户提出,首个干净单链之夜 = 08-06 起)。
  **V2.1 处置(2026-08-07 立项时算过的账,⚠ 纠正原文的乐观预期)**:① **杠杆③「T3 不出卡」随 T3 彻底删除天然落地**,不必再单独拉;② 真正省下的是**卡剧本 −10、复盘 −10(都是便宜的非联网流式)+ `news_scan` 联网调用上界 −30**;③ 🔴 **联网检索段(按次费大头)一分不省** —— 它按**种子数**(`MAX_SEEDS_AGGREGATED=20`)跑,与篮子数无关;④ 省的是**上界不是当前账单**:常态本就是 2–7 篮,若最近几晚都 ≤7 篮则账单几乎不动。**判据不是感觉**:施工时用生产库跑 `SELECT trade_date, tier, count(*) FROM tier_history GROUP BY 1,2` 看近 10 个交易日档位分布,把真实降幅填回本条,⛔ 别写"显著下降"。**杠杆①(路由表给便宜模型分流)与 ②(检索瘦身)仍挂账**,后者才是真降检索费的那一个。
  **✅ 真实降幅已实测填回(2026-08-08 ⑨ 批 1 部署时在生产库上跑的,不是估算)**:
  `baskets` / `tier_history` **全历史 22 行**(`20260804`→`20260807`,V2 上线以来的全部),
  **`tier=3` 共 0 行**;`basket_review_daily` 18 行**全 `depth='full'`、零 `brief`**;
  每晚篮子数 = **4 / 7 / 7 / 4**。⇒ 🔴 **T3 删除对当前账单的降幅 = 0** —— 生产从来没产出过
  一只 T3,省下的**全部是上界**(卡剧本 −10 / 复盘 −10 / `news_scan` −30 的**上界**),
  **当前账单一分不动**。⛔ 别写"显著下降",也别把它算进任何已兑现的成本改善。
  真要降费仍得拉杠杆 ①/②,其中 ② 才碰得到按次费大头。
  **⚠ V2.2 补注(2026-08-09 立项时算过)**:K8 三引擎并跑的 **LLM 增量 = 0 次调用** ——
  引擎是**判据分支**不是**调用分支**,引擎归属由既有 `basket_reason` 那一次一并给出
  (§五 附「成本与超时算术」)。⛔ **别把 K8 写成"省钱",也别写成"更贵"** —— 账单**不动**;
  本版真正涨的是 **CPU**(两张新的全市场预计算表,⑦ 批 2 前置要实测)。原文如下(留痕)。
  **[P3-44 原文]**:2026-08-05 全天 GLM 花费 20+ 元人民币,但当天是**极端不正常样本**(同链完整跑约 4 遍 + 6 次超时白烧的大生成——该浪费已被流式修复结构性堵死)。**常态估算 ≈ 30~35 次调用/晚、¥5–8/晚**(联网检索按次费是大头,篮子数 2~7 随行情浮动)。**观察判据**:看 08-06 起连续几晚的真实账单定基线;若用户仍嫌贵,三个降费杠杆按性价比排序已备案:① **路由表给便宜模型分流**(同账号轻量档〔flash/air〕注册为第二 Provider,`TASK_SCRIPT`/`TASK_REVIEW` 类文字活路由过去,旗舰只留 `TASK_BASKET_REASON`;机制现成零施工风险,预计砍半)② **检索瘦身**(只搜进入前 N 强的种子,不必 20 颗全联网)③ **T3 不出卡**(观察档只留机械数据不烧剧本)。⛔ 拉杠杆前先有 ≥2 晚真实账单;动路由走 App 设置屏或 `settings_store`,动种子数/T3 规格属包或引擎参数,按各自既有门禁办。
- **[P3-41] 🔵 ⑤ 检索段串行,单日墙钟 16.5 分钟、检索账已用到 83% —— 建议并发化**(⚠ **V2.2 对本条同样零影响**:K8 三引擎并跑**不增加任何 LLM 调用**〔§五 附「成本与超时算术」〕,检索段仍按种子数跑 —— ⛔ 别记成"已被 V2.2 缓解";⚠ **V2.1 对本条零影响**:检索段按**种子数**〔`MAX_SEEDS_AGGREGATED=20`〕跑,与篮子数无关,T3 删除一秒都不省 —— ⛔ 别记成"已被 V2.1 缓解";2026-08-05 首次真出篮子的实测基线;**⚖️ 2026-08-05 用户裁定:V2 定型,本条暂不做,留观察** —— 观察判据 = 晚间链是否出现 `budget_exhausted` 或整链时长逼近 `TimeoutStartSec`,出现即重开;⛔ 在有生产观察数据之前不许动并发度、不许改预算常量)。**实测**(nk,20 颗种子,`nk-evening-0804-t240`):检索段 `13:46:17 → 14:02:48` = **991 s ≈ 16 min 31 s**,**逐颗串行**;而 `SEARCH_BUDGET_SECONDS=20 min(1200 s)` —— **已吃掉 83%**,只要哪天多几颗慢种子或多两次重试就会撞账,撞了之后剩余种子直接 `budget_exhausted`、当日证据链残缺(**不是崩,是静默变薄**)。整链 29 min 09 s 里检索段占 **57%**。**方向**:3~5 路并发(种子之间无依赖,`run_driver_search` 是纯函数式的「一颗种子 → 一份证据」);⚠ **两个必须先想清楚的点**:① **预算账并发安全** —— `BudgetLedger.spend()` 现在是"调用完了再记账"的单线程模型,并发下「发起前检查」会同时通过 N 个,最坏一次超发 N-1 个调用的时长;要么给账本加锁 + 预扣(发起前先扣估算值、完成后校正),要么把并发度直接算进预算(`limit / concurrency`)。② **上游限流** —— GLM 侧并发上限未知,打太猛可能反而触发 429/超时,收益变负;先小并发(3)实测再谈调大。**预期**:25~29 min → **< 10 min**。**顺带**:并发化后 `deploy/neckline-basket.service` 的 `TimeoutStartSec` 算术要重算(见该文件注释与 `tests/test_llm_router_budget.py` 那条把两者钉在一起的守门)。

- **[P3-42] ✅ → V2.2.0 第 ④ 块(2026-08-09 改排:V2.1 批 3 并入 V2.2,**形态裁定逐字继承、⛔ 不重新裁定**;V2.1 施工图全文 → `archive/V2.1.0_施工图_20260809归档.md`)** · 原 → V2.1.0 第 ⑥ 块(2026-08-07 planner 裁定形态,施工随 V2.1 落地) —— **裁定 = 候选 ③「折中」**:新起 `neckline-weekly.timer`(周六 09:00,**直接触发单个 oneshot service,⛔ 不用 `.target`** —— P0-45 教训,已验证安全形态是 `neckline-daily.timer` 那条),**自动只跑不依赖交割单的两件**(画像批算落表 + 周度校准报告落盘),**对账仍等用户上传**。**否决候选 ②「纯手动」的决定性理由**:`profile_preference`/`profile_capability` 两张表**目前只有 CLI 一个写入方、生产上零排程在写** → 维持手动 = V2.1 复盘板块累计页上线当天就是空账。**否决候选 ①「全自动」**:对账必需输入只能由用户手动给(§八 第 7 项),排了 timer 也补不出没上传的那份。**「没有交割单那周该输出什么」的答案**:输出画像 + 校准两件,对账段如实 `found=false`「本周尚未上传交割单」。⛔ 不推送(不新增 kind = 不触发用户拍板纪律)。`Persistent=true`(与 evening 的 `false` 刻意不同,理由见 §五 ⑥)。原文如下(留痕)。
  **[P3-42 原文]** ⑫ 的周度作业(对账 / 画像)还没有 unit —— V2 唯一仍开放的规划项(2026-08-05 收官时从 §四 移入,补稳定 ID;原委见 `archive/V2.0.0_施工图_20260805归档.md` 的 ⑯-D「周度部分从日链摘出去」一节)。**事实**:⑯-D 把 ⑫ 的周度部分从 16:35 日链里摘了出来(它是周频作业,不该每天跑一次),但**周度 unit 的形态 plan 明说不由 ⑯-D 决定**,⑯/⑰ 均未擅自排 → **截至割接,`nk` 上只有三件**(常驻 `neckline.service` + `neckline-daily.timer` 16:05 + `neckline-evening.timer` 16:35),**没有任何周度 timer**。**待裁的是形态,不是要不要有**:候选 = ① 新起 `neckline-weekly.timer`(周六/周日固定点,自动跑对账 + 画像);② 维持**纯手动**(用户传交割单时由 App / CLI 触发,周复盘本就依赖用户上传的交割单,自动排程也补不出没上传的那份);③ 折中(自动只跑不依赖交割单的画像段,对账仍等上传)。**⚠ 判据先行**:周复盘的必需输入(券商交割单)**只能由用户手动给**(§八 第 7 项),所以「排了 timer 也未必有数据」——定形态前先想清楚没有交割单那周该输出什么。**不阻塞常态运行**(V2 上线以来周度段从未被日链调用,不存在"每天误跑"的风险)。

- **[P3-27] ✅ 已了结(2026-08-03,V2-⑬-6 兑现删除)** · 候选四件套老键与老模板函数的最终删除(v1.5.0 只做退役,没做删除)(2026-07-29 v1.5.0 立项时新立)。**V2 处置**:自选体检是这四个文案函数最后的消费方,而自选池已按裁定 #9 删除 → **无消费方,四函数 + 四个 `CandidateOut` 老键随候选榜一并彻底删除**。**2026-08-02 用户拍板 D2 = A 路**(新机新子域 `nk.linotsai.top`、老 App 继续打老机)→ **两者不交叉,可直接删,不做过渡文案**(⑭-D)。⚠ **「先客户端可选解码、下版服务端才删键」这条两步淘汰纪律本身仍然有效**(CLAUDE.md 铁律,V2 之后的版本照守),V2 只是靠换机窗口结构性地绕开它。原文如下(留痕)。
  **[P3-27 原文]**v1.5-③-B 把候选卡输出层换成参考三件套后,`CandidateOut.buyPoint/stop/target/invalidation` 四个键**仍在发**(值 = 过渡文案 `LEGACY_FOURPIECE_NOTICE`),`report/candidates.py` 的 `entry_plan_text`/`stop_loss_text`/`target_text`/`invalidation_text` 四个函数**仍在**(自选体检还在消费)。**为什么不能当场删**:已装 v1.4.1 客户端对这四个键是**硬解码**(`client/Models.swift::Candidate.init(from:)` 用 `try c.decode(String.self,…)`,非 `decodeIfPresent`),服务端一旦不发就是整份报告解不出、今日计划全空。**删除条件(两条都满足才动)**:① 双端都已换到 **≥1.5.0**(§八 第 12 项确认);② 自选体检输出层也改完(它是这四个函数最后的消费方)。**怎么删**:先把 Swift 那四个属性改 `decodeIfPresent` + 默认空并发一版,**再**在下一版服务端删键(顺序不能反)。⚠ **`entrySpec`/`invalidationSpec` 永远不在删除范围**——那是盘中哨兵与盘前校准的唯一判据源。**不排期。**
- **[P3-11] ⛔ 原命题废弃,⛔ 不是「已被 ⑫ 吸收兑现」(2026-08-04 核实后改判)** · 打法 × 论点双维归因(原 v1.2.1-D)。**核实结论(以代码现文为准)**:⑫ 画像的维度唯一源 `neckline/profile/common.py` 是 `theme` / `role` / `entry_style` / `entry_label` / `tier` / `missed_role` —— **没有 `playbook`(打法),也没有 `thesis`(论点)**;而 `decision_log` **全仓零 INSERT**(⑩-C 已停写),`breathing_t_trades` 亦停写(裁定 #9)。→ **本条那个具体分析(打法 × 论点交叉表)在 V2 里三个维度的数据源全没了,做不了,也不会再做**。⚠ **⑫ 给的是替代品不是兑现**:它换了一套能回答同类问题的新维度(题材 / 角色 / 入场方式 / Tier / 被错过的角色),并新增了 V1 拿不到的「**是否跑赢同篮未选成员**」——**回答的问题相近,但不是同一张交叉表**,别在账本上记成「已兑现」。**若将来仍想要「打法」这一维**,唯一路径是 ⑩-C 落在 `user_actions` 的**可选标签**(`entry_label`)攒够覆盖率 —— **那正是 §七 [P3-28] 的内容**,去那条看,本条不再单独排期。原文如下(留痕)。
  **[P3-11 原文]** 打法 × 论点双维归因(原 v1.2.1-D)。**V2 处置**:① **「呼吸 T 净贡献」维彻底废弃**(裁定 #9:呼吸台账删,`breathing_t_trades` 停写留档);② **「论点标签」维退化**为可选标签(决策日志强制表单退役,标签改落 `user_actions`,覆盖率会下降 → 见新挂 **P3-28**);③ 归因的**新载体 = ⑫ 能力画像**(按题材 / 角色 / Tier / 入场方式分维,带样本量与置信度,并新增「是否跑赢同篮未选成员」这一 V1 拿不到的维度)。**本条不再单独排期**,以 ⑫ 为准。原文如下(留痕)。
  **[P3-11 原文]**施工图全文在 `archive/v1.2_v1.2.1_施工图_20260728归档.md` 的 v1.2.1-D 节:决策日志 via `position_id` 与 FIFO RoundTrip 按 `(ts_code, buy_date)` 邻近匹配,按论点 / 打法 / 交叉三维聚合胜率盈亏比 + 呼吸 T 净贡献 + 「无决策日志开仓 N 笔」纪律项。**为什么要紧**:「短线追击 vs 呼吸试验」双打法 A/B 裁决的**唯一归因载体**;需 30–50 笔样本才有意义。⚠ 依赖 P0-1 修好(买入日错 → 归因的持有周期全错;P0-1 → v1.4.0 ①-A)。**归因维度自 v1.4.0 ⑤-B 起 +1**(`decision_log.max_chase_pct` 最高追价上限;v1.4 只落数据不出报表),**自 v1.5.0 ①-E 起再 +1**(`reference_plans` 表 = LLM 参谋成绩单:参考买入 / 离场区间、明早剧本、夹逼结果,可与实际成交价与次日走势对拍;v1.5 同样只落数据不出报表)。**本条仍归 v1.5+**(需 30–50 笔样本)。
- **[P3-12] 挂单未成交追踪没有出口**。服务端 v1.3-④ 已上线、`decision_pending_track` 自 2026-07-27 起在攒数据,但 **API 未暴露任何端点**(`app.py` 全仓无 `pending_track` 命中),客户端自然无处可看。**怎么修**:定端点(`GET /decisions/{id}/track` 或并入 `GET /decisions`,§五「v1.3 客户端契约清单」已占位)+ 客户端决策卡展示 N 日走势。→ **v1.4.0 第 ⑦ 块(⑦-A)**,端点定为 `GET /decisions/{id}/track`,404 reason `not_found` 客户端必须加 case。**✅ 2026-07-29 完工**:端点直接读 `report/pending_track.py::load_track_rows`(同码不重写,不重算);404 只在 `decision_id` 不存在时触发,「决策存在但未攒到追踪数据」如实返 200 空态(两种「空」分开)。`reason="not_found"` 复用客户端既有 `case "not_found": return .notFound`,未新增 case(字符串与既有 decisions link/cancel/revise 端点相同)。客户端决策卡展示走势归 ⑧-G(§五 客户端契约清单已登记契约形状)。
- **[P3-13] ⛔ V2.1 后废弃(其对象已整体退役,ID 与原文保留,勿再当待办)** —— 本条修的是「问询台没有历史档案」,而**问询台自 V2.1 第 ① 块起整链删除**(用户裁定 #1),`inquiry_log` 表停写留档不 DROP(§七 P4-31)。原文如下(留痕)。**[P3-13 原文]** 问询记录不落库。`POST /inquiry` 只返回不持久化(`llm_judgments` 只由报告管线写),问询台无历史、回答无法留档、无法与决策日志关联做归因(用户 2026-07-27 提过)。**怎么修**:新建会话表(问题 / 材料 / 回答 / `evidence` / `search_hits` / 时间戳 / 可选 `ts_code`、`position_id`),客户端加历史列表。→ **v1.4.0 第 ⑦ 块(⑦-B)**,表名定为 `inquiry_log`(**与 `inquiry_pool` 是两件事,不许混用**)。**✅ 2026-07-29 完工**:新表 `inquiry_log`(`CREATE TABLE IF NOT EXISTS` 天然幂等,新表无需 `_COLUMN_MIGRATIONS`)+ `run_inquiry` 结尾旁路写入(try/except 包裹,失败只记警告、`inquiryId` 退化 `None`,不打断已算好的回答;`degraded` 字段语义不受影响)+ `GET /inquiries`(分页 + `tsCode` 过滤,倒序)/ `GET /inquiries/{id}`。`position_id`/`decision_id` 当前无写入方(表列已留但无调用方传值)。客户端历史列表 UI 归 ⑧-G(§五 客户端契约清单已登记 `inquiryId` 实为 `Int?` 非 plan 原文 `Int`)。
- **[P3-14] 客户端 Swift 小瑕疵(等 iOS 装机一起做)**:① `InquiryVerdict.rejectRaw/passRaw`(`client/Models.swift:898-899`)自 v1.3.3 二值裁决退役后**已成死码**(只剩单测在引用),清理时连带改 `AppModelTests`;② 「已分析·有风险提示」显示为中性色,应为**警示色**。→ **v1.4.0 第 ⑦ 块(⑦-C)**,与第 ⑧ 块客户端同批做(不必等 iOS 装机)。**✅ 2026-07-29 完工**:`InquiryVerdict` 由二值裁决(`.reject`/`.pass`)改描述性标注(`.analyzed`/`.analyzedWarn`,原样对齐后端 `VERDICT_ANALYZED`/`VERDICT_ANALYZED_WARN` 字面值);`.analyzedWarn` 的 `tone` 改 `.warn`(复用既有 `NKAxisTone.warn`→`NK.amber`,未新造色值),`.analyzed` 维持 `.neutral`(标注不是判决,不套好评色调)。连带修正的死码/失真处:`AppModelTests`(重写三个断言 + 新增 tone 回归测试)、`DTODecodeTests`(两个解码测试此前喂的是已退役字符串,同步换真值)、`IntegrationSmokeTests`(真机联调断言同步,gated by dev server 未跑,本次仍 skip)、四处过时中文注释(`SharedUI.swift`/`InquiryView.swift`/`AppModel.swift`/`APIClient.swift`/`DesignTokens.swift`)。`NKAxisTone` 补 `Equatable`(单测需要按值断言 tone,原枚举未声明该协议)。双端 `xcodebuild BUILD SUCCEEDED`;iOS Simulator `xcodebuild test` 139 跑 126 过 13 skip(IntegrationSmokeTests 无本地 dev 后端天然 skip)0 failure。**截图未拍**(任务briefing「双端 BUILD SUCCEEDED 即可,截图非必需」与本条 plan 原文「(截图)」冲突,取前者;色值改动已有 `testInquiryVerdictWarnGetsWarnToneAnalyzedStaysNeutral`/`testDecodeInquiryAnalyzedWarn` 精确断言 `.tone == .warn` 兜底,且拍照需切后端环境 picker 离开默认 prod,与本会话「零生产访问」冲突,故未做)。
- **[P3-15] → V2.0.0 第 ⑰ 块** · iOS 装机(用户自理)。**V2 处置**:V2 全量换血后 iPhone 上的旧构建将**完全不可用**(契约一次性换血),装机不再是"看不到新功能"而是"必须换";步骤 = §八 第 12 项,时机 = ⑰(等用户指令,builder 不得自行换包)。原文如下(留痕)。
  **[P3-15 原文]**⚠ **2026-07-28 用户更正:iOS 早已装机** —— 此前「未装」的记载**作废,勿再据此判断 iPhone 上哪些能力可用**;具体装的是哪个构建**待第 ⑧ 块双端构建时核实**。macOS 现装 v1.3.4 构建。原文留痕:~~未装 → iOS 上盘前 / 熔断 / 派发警报推送、两档 D 徽标、费用录入与离场原因全部不可用~~。**归 用户操作(见 §八 精神,非网页操作)。** → **v1.5.0 第 ⑤ 块(⑤-F)出构建 + 装机说明、第 ⑦ 块(换包)等用户指令**:用户 2026-07-29 点名「iOS 要跟上」,故 iOS Release 构建**随本版同步产出**(构建不通过 = ⑤ 不算完工),装机动作仍在用户。⚠ 现状:iPhone 上仍是 **v1.4 之前的旧构建**,v1.4 的信息卡页 / 排序理由 / 决策日志第⑨项 / 停牌标注在 iPhone 上都还看不到。

- **[P3-28] → V2.2.0 第 ④ 块(**部分落地**:入口 + 覆盖率指标;LLM 代猜仍不做)** · 归因标签稀疏(V2 立项时新挂,来源 = 架构稿 §9.2 + 裁定 #6:入 Backlog 挂账,V2 不预建机制)。**V2.2 处置**:K8 §十五「用户只补充系统无法识别的主观原因,**每次一条简短说明**」正是本条候选解法 ① 的落点 —— 落 `trade_clock_events.user_note`(**只追加**)+ `POST /clocks/trade/{position_id}/note` 端点 + 客户端持仓卡入口,并**同时落覆盖率指标**(「本期 N 笔中有 M 笔带说明」),让稀疏程度可见。**候选解法 ②(LLM 代猜标签)仍不做**,触发条件与三条约束(标注来源 / 分列存放 / 不得进判定)一字不变。原文如下(留痕)。**病因**:V2 把决策日志强制表单退役成「自动快照 + 用户字段全可选」(⑩-C),摩擦降下来了,但**能力画像的标签覆盖率会同步下降** —— 用户不填题材切换 / 弱转强 / 容量中军之类的标签,⑫ 的能力画像就分不出「哪类打法真有效」。**为什么先不建**:用户明确裁定「真遇到再解决」,预建一套代猜机制反而可能污染真实标签。**届时解法候选(不是现在做)**:① 先加**标签覆盖率指标**(⑫ 画像里如实显示「本期 N 笔中有 M 笔带标签」),让稀疏程度可见;② 若确实稀疏 → **LLM 代猜标签**,但必须**标注来源**(`user` vs `llm_guess`)、**与用户标签分表或分列存放、绝不混同**,且代猜标签**不得进任何判定**。**触发条件**:⑫ 上线后覆盖率持续 < 50%(判据而非感觉)。**不排期。**
- **[P3-29] 检索能力单点在带 `web_search` 的那一家(V2 立项时新挂,来源 = 架构稿 §9.1-3)**。**事实**:双 Agent 分工里,联网检索目前只有 GLM 一条通道实现,DeepSeek 走纯推理 → **GLM 故障时消息面 / 驱动证据整段降级**(诚实披露,不编造)。**V2 已做的缓解**:Provider 自填制 + 任务路由表(②)已经**结构上支持第二检索 Provider**,路由表里给它留了位置;缺席时 `evidence_status='search_unavailable'` 并在卡面与报告披露。**待办**:接一家备用检索 Provider(候选 = 任何带联网搜索的 OpenAI 兼容端点),接完把 `TASK_DRIVER_SEARCH` / `TASK_NEWS_SCAN` 的路由做成「主 + 备」。⚠ **接新检索源时先验协议**:不被上游认识的检索参数会 `ok=True` **静默返 0 条**(v1.3.4 案底),必须看 `search_hits` 字节数证伪。**不排期,不阻塞 V2。**

- **[P3-33] ⚠ V2.2 起**主体作废,只剩两小项挂账** —— 门槛制取代排序制后,「多好才配叫 T1」这个命题由 **K8 §八 的证据成熟度条款 + 六道关口**接管(§五 ③-D):`tier1_min=0.60 / tier2_min=0.40` **降级为「档内排序的辅助下限」**,不再是定档的闸,故"这两个数取多少"不再是判定正确性问题。**仍挂账的只剩两项**:③ `tradability` 两档扣分比例(1.0 / 0.5,零证据)· ④ **中性填充是否该影响档位**(⑥ 五维缺数据一律填中性 0.5,三维缺数据的篮子靠三个 0.5 也可能压过下限,即「**因为不知道所以排得高**」;`neutral_filled_weight` 可审计字段已在,刻意先不设闸)。⚠ **V2.1 已作废的那一项(`tier3_min`)不再重复登记。** 原文如下(留痕)。⚠ V2.1 起四项去其一(`tier3_min` 那一项随 T3 彻底删除而作废,其余三项原样挂账) · Tier 档位质量线初值与「多好才配叫 T1」的策略语义待过目(V2-⑥ 施工中发现,2026-08-02 新挂;与 P3-32 分开挂,因为要用的证据不是同一份)**。**事实**:⑥ 完工时发现「上限非配额、允许 T1 为空、不许凑数」在**纯排名制下不可实现**(只要当天有 ≥1 个篮子 T1 就永远填满),必须有一道**绝对分数**的质量线。**已做的工程处置(§五 ⑥-b)**:质量线**进包** `config.tier.quality_lines`(理由 = 它与五维权重**作用在同一标度上**,权重已在包里,分开住会让换权重静默改变 T1 选择性),初值 `tier1_min=0.60 / tier2_min=0.40 / tier3_min=0.25`;`tier3_min` 是 planner 补的(原实现 T3 无下限 = 事实上的配额,正是 V1「每天硬凑 20 只」那个病)。**待过目的是三个数本身**:① 0.60/0.40 是 builder 按「0.5 是整体中性」推的,② 0.25 是 planner 拟的,**三个都无证据支持具体取值**。**一并回看的两项**:③ `tradability` 的两档扣分比例(一字 1.0 / 涨停开过板 0.5,⑥ 登记 ⑤ 自陈"无证据支持具体数值");④ **中性填充是否该影响档位** —— ⑥ 五维缺数据一律填中性 0.5,三维缺数据的篮子靠三个 0.5 也可能压过 `tier1_min`,即「**因为不知道所以进 T1**」;⑥-b-D 已落 `neutral_filled_weight` 可审计字段但**刻意先不设闸**(不凭空造阈值),要不要据此降档等样本。**什么时候回看**:⑨ 评价引擎攒够样本后 —— 它能出 **Tier 单调性(T1>T2>T3)** 与 **T1 空档频率** 两条,正好是判断质量线松紧的直接证据。届时交**策略线 / 用户**过目,**调值走换包(进化门禁),⛔ 不许顺手改代码或包里的数**。**不排期。**

- **[P3-34] → V2.2.0 第 ③ 块(**扩容登记,不结案**)** · V2 判据的「工程重解读 / 零审计背书」登记册,待策略线 / 用户过目(V2-⑦ 施工中发现,2026-08-02 新挂)。**V2.2 扩容两项(2026-08-09,K8 落地时新增的零背书判据一并进同一册,⛔ 不另起第二本账)**:**(c) 落地起跳位置关的五项机械阈值**(`LOW_TOL`/`SUP_TOL`/`PLATFORM_WIN`/`SELL_DECAY`/`HIGH_GAP`/`LIFT_WIN`/`LIFT_MAX`,§五 ③-C)—— 全部由 planner 把 K8 §二 五条定性状态翻译而来,**零回测、零事件研究支持**;**(d) C1/Z1/Y1 三引擎首版阈值里标 `engineering_v1` 的那些**(回撤深度带 / `platform_days` / 证据份数 / 板块广度分位,§五 ③-F)。两项都已在包 JSON 里带 `provenance: engineering_v1 + calibration: pending`(闸 1 强制),**校准路径 = 选股时钟攒样本 → 周度四分类建议 → 用户确认 → `C2`/`Z2`/`Y2`**;**在有新证据之前 ⛔ 不许自行改数。** 原有两项 (a)(b) 原文如下,一字不变。**这条与 P3-32 / P3-33 分开挂**:那两条问的是「**数值**取多少合适」,本条问的是「**判据语义**这么落对不对」—— 性质不同、要用的证据也不同。两项:
  - **(a) K7 需求 5 `warn_streak_top` 的「簇内」被重解读为「共振邻域」(§五 ⑦-K7-b)**。**策略线必须知道生产上到底怎么落的** —— K7 审计用的是「(日,行业) 涨停家数 ≥3」的**一票一簇**粗簇,而 ④ 的 `limit_cluster_daily` 是多路聚类(一票同日可属十几个簇),照字面「任一簇头名」在 2026-07-24 真实数据上会让 **36/42(86%)** 涨停票都打警示 = 标注失去区分度。生产读法 = 「所在**全部簇成员并集**内并列或独占最高,**且邻域至少有一只更低**」(后一条对应文案「3× 于后排」—— 得先有后排),三天命中 6/6/5(分母 48/129/42)。**待策略线确认这个重解读是否偏离 H12/H13 的原意**;在策略线提异议前按此执行。
  - **(b) ⑦ 的验证 / 失效条件集与聚合门槛是全新发明,零审计背书(§五 ⑦-b)**。验证 = `收盘 ≥ D0 收盘` 且 `≥ D0 MA20`;失效 = `≤ 章程止损线` 或 `触及跌停` 或(`< D0 收盘` **且** `< D0 MA20`);聚合 `min_members_hit = ceil(n/2)`(验证 / 失效两侧同一个数)。**没有任何回测或事件研究支持这套条件**,它们是为了让四态状态机能跑起来而拟的临时默认。**已做的约束**:集中到一处命名常量 + `_ENGINE_CONSTANT_WHITELIST` 登记 + `verification_ruleset_version` 落卡指纹(⑨ 才分得开哪套条件集出的成绩)。**待 ⑨ 攒够样本后回看**:验证率 / 证伪率的分布是否合理、`falsified` 是否打得过早过滥、失效侧要不要**按角色加权**(龙头破位比弹性破位更重 —— 合理但无证据,本版刻意不做)。**⛔ 本版不进包**:§12.2 插槽边界明文把「④篮子卡冻结体例」列在「引擎本体,不进包」一侧,要包化必须走「**扩插槽边界(用户拍板)→ 扩 schema → 发包**」三步,不是 builder 或 planner 能顺手做的。**在有新证据之前不许自行改数、不许自行加包键。** **不排期。**

### ⚪ P4 · 运维 / 历史局限(留档为主)

- **[P4-54] ⚪ 策略档案迁出后,代码面还剩 4 处指向已删 `research/` 的存活引用 —— **只登记不修**,列为 V2.2 批 1 的顺手清障项(2026-08-09 planner 对账时逐条 grep 出来的)**。**背景**:2026-08-09 用户把顶层 `research/` 整体迁出(§1.6),同批已由用户自己改好三处(`test_industry_strength.py` 改冻结源副本 · `test_eval_exit_sim.py` 加 skipif · `smoke_eval_exit_sim.py` 加说明)。**剩下 4 处 planner 已逐条核过、全部不会让全量套件变红**(实测 3063 passed / 3 skipped / 2 failed,2 failed = P1-46 周末日期炸弹,与本次无关),故**本版不改、只登记**:
  1. **`tests/test_v13_exit_6y_baseline.py`(最该知道的一条)** —— 依赖 `research/_cache/k3_panel.parquet` + `research/lab.py`,`pytestmark` 已有 `skipif` 会干净跳过。**但 skip 理由文案现在在撒谎**:它说的是「数据缺失 / 默认跳过」,真相是**这条六年真回测基线(N=1288 / total_return −20.53%)在本仓已永久无法复跑**(面板 5.5G 已删且不可回滚,`lab.py` 已迁出)。⚠ **这条要紧在哪**:§五 ⑤ 的章程修订要把 `MomentumConfig.max_hold_days` 放宽成 `Optional[int]`,plan 写的护栏是 **`tests/test_v13_exit_guardrail.py`(逻辑层,始终运行,不依赖 research/)** —— 那条**没事**;但「六年真回测逐位不变」这一层**从此只能靠逻辑层护栏 + 冻结数字的文字记载**。**修法(批 1 顺手)**:改 skip 理由为如实文案 + 在 docstring 里把冻结数字与「怎么重建才能复跑」写清(档案 README 有办法)。⛔ **别删这个文件** —— 删了那个冻结基线就彻底没有载体了。
  2. **`scripts/oneoff/compare_industry_stage_with_research.py`** —— `sys.path.insert(_ROOT / "research")`,跑起来必 `ImportError`。它是**已执行完毕的一次性脚本**(`scripts/oneoff/` 按既有约定 = 留档审计用,不是现役脚本),**留着无害**;批 1 顺手在文件头加一行「⚠ 依赖已迁出,不可复跑,见 §1.6」。
  3. **`scripts/smoke_eval_exit_sim.py:47` 的 `K3_PANEL` 常量路径** —— 指向已不存在的 `research/_cache/`,但脚本自带「面板不存在 → 打印提示 + `return 2`」分支,**行为正确**(用户已在 docstring 写明「默认直接跳过是预期行为不是故障」)。**不改**。
  4. **`tests/test_eval_exit_sim.py` 的 `sys.path.insert(_REPO / "research")`** —— 在 `skipif` 之后,**够不到就不会执行**。**不改**(⛔ 别删那个 skip 用例,删了等于承诺研究件永不回来)。
  **另**:`neckline/` 与 `scripts/`(oneoff 之外)**零 `import research`**,生产路径干净 —— 「生产不许 import `research/`」这条方向纪律**本来就守住了**,迁出没有打破它。**结案条件**:第 1、2 两处文案改完即可销案(纯注释级,零行为改动)。
- **[P4-50] ⚪ `landing_state_daily` 是本项目第二条「全市场逐票 × 多年回看」批算路径 —— P0-23 的正面靶心(2026-08-09 V2.2 立项时 planner 挂;**§五 ⑦ 批 2 前置,⛔ 不许跳过**)**。**事实**:第一条是 `compute_industry_strength`(2026-07-29 在生产 2 vCPU / 1.6G 上 **700M cap OOM-kill、1400M cap 600s 跑不完**,挡住整版上云);`landing_state_daily` 是**逐票 ×5400 只 × 60 日回看**,量级更大。**已做的设计约束(§五 ③-C)**:① EOD 预计算落表、在线只读;② 取数走 `market_data._scan_table(years=…)` **年分区裁剪**(P1-26 既有修法),⛔ 不全 glob;③ 日更增量只算当日一行 ×5400 票。**⛔ 不许跳过的前置**:上生产前 `systemd-run --scope -p MemoryMax=… -p CPUQuota=…` **隔离单跑一次**,量时长与峰值 —— **收盘后 15:00 之后、避开 16:00–17:00、串行、`load > 4` 立即停手**;实测数达标才把 `neckline-scan.service` 的 `TimeoutStartSec`/`MemoryMax` 改成实测值(带理由)。⛔ **别拿常驻 `neckline.service` 当小白鼠**(它与盘中哨兵同进程,`MemoryHigh` 先节流 = 卡死不报错)。⛔ **别用抬 `MemoryMax` 糊算法成本**(P0-23 原教旨:1400M 都不够)。**与 P4-43 ② 同一个窗口一起量,别分两次跑。** **结案条件**:批 2 上线后连续 5 个交易日 `neckline-scan.service` `ExecMainStatus=0` 且峰值有余量。
- **[P4-48] ⚪ → V2.2.0 第 ① 块(本版结案,修法取「治类不治例」= conftest 全局兜底重定向)** · 测试隔离写库残留复发(2026-08-09 入册;同日改判 —— 本版新增大量测试,不先修就是继续加深) · 全量 pytest 经 `db_path=None` 兜底**静默写真实开发库** `data/neckline.db`。A8 批次修过一轮(当时探针验收 0 泄漏),但后续版本新增测试又带回(08-08 实测跑测试期间开发库 md5 变化;CLAUDE.md A8 条持续登记)。生产库有属主保护不受影响;危害 = 开发库被动长表长行。⚠ 验收铁律:**「MD5 没变」不等于没泄漏**(幂等写照样是泄漏),必须用 A8 的 `sqlite3.connect` 探针法。修法:探针重跑定位新命中点逐个补 `db_path`,或 conftest 全局兜底重定向(治类不治例,首选后者)。来源 = V2-① 时代的待办卡,⑦/P0-39 两次复证仍活。
- **[P4-16] ⛔ V2 后废弃(ID 与原文保留,勿再当待办)** · **`MemoryMax=800M` 这个具体数字随换机作废** —— V2 走 100% 换新服务器(裁定 #3)+ 晚间管线多段化(三个 oneshot **各自** `MemoryMax`),老机 1.6G 总账下的所有配额推演不再适用。**接替它的是 §五 ⑯-C(新机隔离实测:每项批算单独计时 + 量峰值,达标才准进主链)与 ⑯-D(三段 oneshot 各自配额,重活不进常驻服务)**;「抬内存不解算法成本」「重活不进常驻服务」两条**教训本身继续有效**,已写进 §3.10-D 与 §五 铁律。原文如下(留痕)。
  **[P4-16 原文]** `neckline-report.service` 的 `MemoryMax=800M` 对 v1.3 新管线偏紧(`deploy/neckline-report.service:23`)。2026-07-27 补跑时峰值顶满、5206 次页缓存回收,**`oom_kill=0`** —— 撞的是页缓存不是真内存,没崩。**怎么修**:~~v1.4 部署时视情抬到 1G~~ **2026-07-29 改判**:P0-23 证明抬内存不解算法成本(1400M 都跑不完),⑩ 已把全历史扫描从在线路径移除 → **⑨-F 改为纯实测记账**(判据:报告峰值不高于 07-28 基线 838,860,800、`oom_kill=0`),任何 `MemoryMax` 改动先算总账(机器仅 1.6G,还有常驻 `MemoryHigh=420M/Max=600M` 与 `neckline-daily` 900M);抬没抬都要写理由。**2026-07-29 v1.4 定案 = 维持 800M 不改**(六条理由见 `archive/v1.4_施工图_20260729归档.md` ⑨-F)。→ **v1.5.0 第 ⑥ 块(⑥-E)继续复核**:本版把 16:35 的 LLM 段拉长约一倍(20 只审判 + 三件套 + 20 张信息卡取数),判据仍是 **`oom_kill=0`**;峰值高于基线 838,860,800 时**先算总账再谈改 `MemoryMax`**,且**超时先降预算 / 开并发,不许抬内存糊**(P0-23 教训)。
- **[P4-17] 历史局限:早期审判的消息面无搜索支撑(不补跑,如实留档)**。生产 `llm_judgments` **20260721/22/23 三天 10/10 空命中** → 那 **25 条非降级审判结论的消息面部分实质是模型凭训练数据作答**;20260717/0720 为 10/10 `degraded`(当时无 key);20260721 另有 5 条因 25s ReadTimeout 降级。**20260724 起命中恢复满额,自那天起的审判才算有搜索支撑**——事后审计「当时为何否决」必须先看这条时间线。**并入原「GLM 顶层 `web_search` 空数组待查」**:真因仍未完全解释(逐字节相同的 payload 两天行为相反 = 供应商侧漂移),v1.3.4 的 0 命中埋点 + 四处露出已上线,可持续观察,不主动追。
- **[P3-32] 主归属 lift 门槛与「小簇 vs 大概念」的策略语义待过目(V2-⑤ 施工中发现,2026-08-02 新挂)**。**事实**:涨停簇成分常只 2–5 只,`占比 ÷ 全市场占比` 实测 **70–90 倍** lift,与 v1.3.1 行业闸先例(板块成员以百计)不是一个统计量级 → 同票跨「小簇篮 + 大概念篮」时主归属**几乎恒落在小簇篮**。**已做的工程处置(§五 ⑤-c)**:加**最小成分数门槛 = 5** 作**引擎常量**(与 `industry_strength._MIN_MEMBERS` 同源同值),不达标的篮 `lift=None` + `lift_reason='sample_too_small'`、不参与主归属比较,全不达标时按 `(成员数降序, basket_key 升序)` 确定性兜底。**待过目的是策略语义那一半**:① 5 这个数对涨停簇合不合适;② 「小簇篮天然该输给大概念篮」这个方向本身对不对(也可能反过来 —— 小簇更聚焦)。**什么时候回看**:⑨ 评价引擎攒够样本后,它的按包归因正好能出「主归属落在小簇 vs 大概念篮」的实际表现对照;届时交**策略线 / 用户**过目。**在有新证据之前按上述默认执行,builder 不许自行改数。** **不排期。**

- **[P4-25] ✅ 已结案(2026-07-30 v1.5-④-A4)** · `pytest tests/` 会写开发机的真实 `data/neckline.db`(2026-07-29 修 review 🟡-1 时实测发现,**既有行为、非本次引入**)。`tests/test_k3_oversold_guardrail.py::TestK1BitIdentical` 等 **4 个 guardrail 用例刻意读真库的 K1 行**(把 `DB_PATH` 指向临时库跑全量,这 4 个立刻红),于是它们触发的 `init_schema` 会把**新表/新列的幂等迁移顺手落到开发库上** —— 今天 `strategy_activation_log` 就是这么在本地 dev 库建好并播了 K1 那一条(内容正确,与生产 ⑨-B 将发生的是同一段代码)。**为什么记一笔**:①「跑个测试而已」不该悄悄改你的工作库,迁移出错时现场也不好复原;② 开发库被测试改过之后,再拿它当"生产形状"的对照会失真。**怎么修**:~~不急,留档为主~~ → **v1.5.0 第 ④ 块(④-A4)**:conftest 加 session 级 `real_db_readonly_copy` 夹具(真库拷一份临时副本;真库不存在则 `pytest.skip`),把 `test_k3_oversold_guardrail.py` / `test_k2_mainline_guardrail.py` / `test_v13_exit_6y_baseline.py` 里**全部无参数的 `brain.active_config()`** 改成显式 `db_path=副本`;加 grep 守门单测断言 `tests/` 下不再出现无参调用(确有例外须白名单 + 写理由)。⚠ **本版有新表**(`reference_plans`),不修这条的话「跑个测试」又会把新表顺手建到开发库上。

  **✅ 2026-07-30 完工**:`conftest.py::real_db_readonly_copy`(session 级,`sqlite3` 官方 backup API 拷真库只读副本,同生产 `.backup` 既有姿势;真库不存在 → `pytest.skip`)+ 三文件六处 `brain.active_config()` 裸调用全部改显式 `db_path=real_db_readonly_copy`(两个 `_k1_cfg()` 辅助函数改带 `db_path` 参数,一处 `test_require_member_true_ands_the_column` 的两次直调同改)。**守门单测**:新文件 `tests/test_db_isolation_guardrail.py`——**AST 精确匹配**(不是纯文本 grep,理由:`test_brain.py` 有 4 处描述 `brain.py` 内部退化行为的 docstring 会被纯文本 substring 误伤,AST 只看真实语法树天然不受注释/字符串字面量影响,已用正/负/docstring 三组用例验证判据本身正确)扫描全部 `tests/test_*.py`(95 个文件),断言零出现无参数 `active_config()`/`get_active()` 调用;白名单当前为空。**实测证据(而非只信任测试通过)**:改前 dev DB `mtime=23:17:43`;改后跑一次**全量** `pytest tests/ -q`(1940 过 + 2 skip)**前后 dev DB mtime/size/MD5 逐字节不变**(`fd745c6a...`)——比"表结构未变"更强的证据(证明连一次 commit 都没发生,不只是没加列)。⚠ **过程留痕(如实记录)**:A4 修复落地**之前**,本会话早些时候跑过几次未隔离的全量测试,`llm_judgments.search_engine`(④-A3 新列)确实被 K2/K3 护栏用例的 `init_schema` 副作用提前建到了 dev 库上——这正是本条要修的病征的一次现场复现,内容正确无害(幂等迁移),事后已额外显式跑一次 `init_schema()` 留痕(A4 修复生效后该调用为 no-op,dev DB 无变化)。
- **[P4-18] Bark 通道从未活体验证**(payload 依官方文档实现,无真实 `BARK_URL`)。APNs 为主推通道,**不阻塞**;真要用,先手动跑一次 `BarkChannel(url).send(...)` 验协议。
- **[P4-19] 周复盘从未跑过用户真实券商交割单**。4D 对账引擎只用合成 xlsx 冒烟过;**2026-08-02 那个周末是第一次**,可能暴露两家券商原始格式的解析问题(`review_col_map` 可配映射 + `_detect_format` 的坑见项目 `CLAUDE.md` 阶段 4D 节)。首跑时留意「缺列 vs 缺该格」的区分是否真生效。**⚠ V2 附注**:⑫-A **原样继承该引擎、一行不改**(含 `review_col_map` 双驱动与「必需列整列缺失硬跳该行 + 警告、绝不 `or 0.0`」两条坑),故本条**不因 V2 而结案,仍挂账**。
- **[P4-20] TuShare 600 元档配额跨线共享,可被跑干**。策略线 2026-07-25 曾把配额耗尽 → 生产 16:05 增量可能拉不全 → 16:35 报告 / 情报件缺数。**缓解**:研究侧大批量拉取避开 15:00–17:00 生产增量窗口,长期考虑研究独立 token。**⚠ P0-3 若加概念板块日更会新增一笔固定消耗,一并算账。** → 该日更已定于 **v1.4.0 ①-C**,配额账在 **⑨-E** 复核。**⚠ V2 附注(算过账,如实登记)**:V2 新增的三张扫描层预计算表(④)**全部读本地 parquet,不新增任何 TuShare 调用**;盘中存拍(⑧-B)走**免费实时源**(新浪 / 腾讯),同样不吃 TuShare 配额;ETF / 指数报价亦走免费源。故 **V2 的 TuShare 配额账与 v1.4 持平**,本条继续按原口径挂着(研究侧大批量拉取仍须避开 15:00–17:00 生产窗口)。
- **[P4-21] 工作区遗留未提交**:`client/Neckline.xcodeproj/project.pbxproj`(Xcode 自动改的 `MARKETING_VERSION`)。→ **v1.4.0 第 ⑧ 块**客户端施工时一并处理,别单独开提交。**✅ 2026-07-29 完工**:随本块 4 个新文件条目(`InfoCardView.swift`)一并提交,`MARKETING_VERSION=1.3.4` 字面量与 `explicitFileType` 变化不再单独挂账。
- **[P4-30] V2 上线初期分时残缺(V2 立项时新挂,来源 = 架构稿 §9.1-2;历史局限,留档为主)**。**⚠ V2.2 附注**:K8 选股时钟验证内容第 7 项「触发后的**分时承接**与收盘结果」直接吃这一路数据 → `mfe_source ∈ {intraday, eod_approx}` 的**标注体例原样扩到 `selection_clock.mech_json`**,缺存拍的日子如实标 `eod_approx`,⛔ 不装精确。盘中存拍(⑧-B)**从 V2 上线日才开始积累**,在此之前没有任何分时数据 → ⑨ 复盘引擎的 **MFE/MAE 只能用 EOD 近似**。**处置 = 如实标注,不装精确**:`basket_review_daily.mech_json.mfe_source ∈ {intraday, eod_approx}`,报告与客户端照标;**⑯-F 灌入的 preseed 篮子同理**(它们的复盘一律 `eod_approx`)。用户外源分时方案落定后走同一张表回补(裁定 #1),回补到哪天、哪天起就是 `intraday`。**不排期,不阻塞。**
- **[P4-31] V2 停写留档表的最终清理(V2 立项时新挂;⑬ 已于 2026-08-03 完工,六张表现已全部处于「有历史行、零新增行」状态,守门单测锁死;**⚠ V2.1 起扩到七张** —— `inquiry_log` 随问询台整链退役加入;**⚠ V2.2 起扩到八张** —— `circuit_breaker` 随熔断整体退役加入〔2026-08-09 用户裁定 #8,§五 ⑤-B〕,判据与处置同下)**。⑬ 的删除口径是「**代码删干净、表停写留档不 DROP**」,于是 `watchlist` / `breathing_t_trades` / `inquiry_pool` / `llm_judgments` / `reference_plans` / `decision_log` **+ `inquiry_log`(V2.1 新增)** 七张表会长期以「有历史行、零新增行」的状态留在库里。⚠ **`inquiry_pool` 与 `inquiry_log` 名字像、处置不同**:前者仍被周复盘 `reconcile.check_plan_and_ledger` **只读消费**(故保留 `load_inquiry_pool`),后者**零下游消费方**(读写函数一并删,将来查历史行走 `sqlite3`)。**为什么这么做**:历史归因与审计要用,且 DROP 是不可逆动作。**将来要不要 DROP 的判据(不是感觉)**:① 该表最后一行的写入时间距今 > 1 年;② ⑫ 画像与 ⑨ 评价引擎已不再读它;③ 有一份独立备份。三条同时满足才谈 DROP,且必须走一次性脚本体例(默认演练、`--confirm` 才写、双备份)。**不排期。**

- **[P4-35] 关注池的「相关板块 ETF」缺数据源,本版只做到板块基准指数(V2-⑧ 施工中发现,2026-08-02 新挂;留档为主)**。**事实**:⑧-A 原文写的是「相关板块 **ETF/指数**」,实际只落了**指数** —— 项目**没有 ETF 成分 / 映射数据源**(TuShare 600 元档没有;`ths_index` 是同花顺板块指数,免费实时源不认它的代码),硬编一份 ETF 清单等于凭空发明。现状 = 按成员板块取 `000001.SH`/`399001.SZ`/`399006.SZ`/`000688.SH`/`899050.BJ`(与 `scripts/backfill.py::INDEX_CODES` 同一批)。**为什么记一笔**:蓝图 5.4 的持仓监测里有「**板块 ETF 或指数是否突然失去承接**」这一条(⑪-A 的四项新增监测之一),**指数只能替代一半** —— 宽基指数反映不了「这个题材的资金还在不在」,行业 ETF 才能。**要修先得有数据源**(候选:TuShare `fund_basic`+`fund_portfolio` 档位待查 / 交易所 ETF 清单 / 用户外源)。**不排期,不阻塞**;⑪-A 落地时按现状实现并如实标注是指数不是 ETF。

- **[P4-43] nk 上线初期:一项待首验 + 一组资源限额待校准(2026-08-05 收官时从 §四 移入,补稳定 ID;留档 + 观察为主)**。**⚠ V2.2 附注(2026-08-09)**:本条第 ② 项(资源限额待校准)的数据**由 §五 ⑦ 批 2 的 P0-23 前置实测顺带收掉** —— 那次本来就要在生产机上隔离量 `landing_state_daily` / `market_regime_daily` 两张新表的时长与峰值,同一个窗口里把 `neckline.service` 与三个 oneshot 的 `memory.events` / `MemoryPeak` 一并读出来,**⛔ 别为它单独再跑一次探针**(生产探针纪律:只在收盘后、串行、`load > 4` 立即停手)。① **盘中存拍(⑧-B)在新机上还没验过** —— 判据 = 当日 15:05 之后 `sentinel_events` 有 `sentinel='capture'` 行 **且** `intraday_ticks` 当日 parquet 分区落盘(⚠ `missing` 那天是**零行**,状态写不进 parquet,只能靠这行状态区分「没数据」与「没跑」)。② **`neckline.service` 的 `MemoryHigh=420M` / `MemoryMax=600M` 是老机 1.6G 五业务共存时代的数**,新机 4 vCPU / 3.8 GiB 独占下偏紧;但**盘中哨兵在新机上真实峰值未知**,⛔ **别拍脑袋调** —— `MemoryHigh` 是软节流,调错会让进程陷进回收死循环(`memory.events.high` 飙升、`oom_kill=0`)= **卡死不报错**。**先收数据再谈调**:攒几个完整盘中日的 `memory.events` + `MemoryPeak`,有实测才动;调之前顺便复核三个 oneshot 各自的 `MemoryMax`(它们与哨兵不同进程,账要分开算)。

- **[P4-46] ⚪ → V2.2.0 第 ④ 块(本版结案,改成「读周度 unit 落盘的产物,查不到才降级」)** · 改判理由:V2.2 把归因分层键从 2 个扩到 4 个(`skeleton × engine_code × engine_version × ruleset`),现算成本必然上升 → 本条原文的触发条件「生产实测 > 5s」**大概率当场成立**,与其等它成立不如随扩容一起改掉;⛔ 仍不许用抬 `MemoryMax` 糊(P0-23 原教旨)。原文如下(留痕):`GET /eval/weekly` 是**在线现算**,跑在常驻服务里 —— 样本长大后是 P0-23 同款风险(2026-08-07 V2.1 立项时核实新挂,留档 + 观察)。**事实**:该端点每次请求都 `calibration.build_report()` 现算 —— 读 6 张表的横截面 + 装配前复权面板 + 安慰剂抽样(`PLACEBO_DRAWS` 默认 200),而它**跑在 `neckline.service` 内、与盘中哨兵同进程**。现在样本只有十几个交易日、篮子数以个位计,所以还很快;**P0-23 的病灶正是"本地实测廉价"在样本长大后不成立**。**V2.1 已做的缓解**:新端点 `/review/overview` 与 `/review/handoff` **只读周度 unit 落盘的产物、零在线现算**(第 ⑤ 块有 AST 守门断言它们不调 `build_report`),复盘板块走的是这条路;`/eval/weekly` 因 macOS 2.0.0 客户端在用而**本版不动**。**触发条件(判据不是感觉)**:① 生产实测该端点 > 5s,或 ② 累计篮子数 > 300 —— 任一成立就改成「读 ⑥ 落盘的校准产物,查不到才降级」。⚠ **⛔ 别用抬 `MemoryMax` 糊**(P0-23 原教旨)。**不排期。**

### 📌 认知对齐(不许凭字面推断)

- **[📌-22] ✅ 已裁决(2026-07-28,用户拍板选 (b))→ v1.4.0 全版**。~~「K4 打法」到底指什么,当面问用户~~ → **已问已定**:三选一里 **(b) 把 K4 advisory 产品化**(正选漏斗排序键 + 信息卡 + `exec_hint` 执行提示);**(a) 激活 K4 行**(`rule["config"]` 逐字段拷自 K1,激活 ≈ 激活一份 K1,策略行为无变化)与 **(c) 等策略线新内核**(四战役 + 用户 2026-07-26 终裁底盘暂缓)**均已否决,不再列为选项**。规格权威 = `archive/交接_系统线升级需求_20260725.md` **需求 8**(2026-07-28 策略线裁定)。**K4 行现状(事实,勿再误读)**:`is_active=0`、**永不激活**;全部价值在 `rule["k4_advisory"]` 六节(`hard_cut`/`avoid_flag`/`exec_hint`/`circuit_breaker`/`intel_order`/`note`),v1.4 是**把这六节用足**,不是激活这一行。**保留本条只为防止后人重开这个问题。**

### ⚫ 长期机制(常态运行,不排期)

- **[机制] 策略进化门禁**:按月 / 季调参须过回测 + walk-forward 样本外跑赢现役 + 用户批准;大脑按版本归因实盘表现。任何章程变更走 `scripts/activate_charter.py` 四道闸,不许手改 DB。**回滚**(切回此前已现役过的版本)是这套门禁的一个子情形,何时该回滚 / 走哪条路 / 同步清单见 `archive/SOP_章程回滚_20260730.md`(v1.5-④-A5,§七 P1-24)。
- **[机制] 选股策略包门禁(V2.0.0 新立;与上条并行,但两者互不混用)**:**选股**的可配部分(①扫描层种子规则 + ③Tier 机械分维度与权重)走 `selection_packs` + `scripts/activate_pack.py` **四道闸**(schema → 原语/白名单 + `engine_api_version` 兼容 → 默认演练 → `--confirm` 才写);换策略 = 一次激活动作,回滚 = 激活旧版本行;**评价引擎按 `pack_version` 归因**,每个包自动攒出自己的成绩单喂回策略线。⚠ **两条版本线、两张表、两套激活流程,不许混用**:章程(纪律)永远不进包,包也永远不改章程。**版本号沿用 K 字头**(包 = 策略线 K 版本的交付物形态,不另起第三条版本线)。**⚠ V2.2.0 起本机制扩成四条线**(2026-08-09,K8):`selection_packs` 由「单包制」升级为「**骨架线 `V` 一条 + 引擎线 `C`/`Z`/`Y` 各一条**」,唯一现役约束由「全表唯一」改为「**每线唯一**」,每线独立版本 / 独立激活 / 带 `running`·`stopped` 状态;**四道闸、append-only、事件流、按版本归因四套机关原样平移**,⛔ **不新建第二套注册表**。骨架线管**股票池 / 篮子 / 梯度**(K8 §一),引擎线管**六道关口的引擎侧阈值**;引擎包每个阈值强制带 `provenance`(`audited` / `engineering_v1`),**闸 1 校验缺失即拒**。`ENGINE_API_VERSION` 自 V2.2 起为 **2**,`K4-pack-v1`/`K7-pack-v1` 两个旧回滚锚**已被闸 2 硬拒(设计如此)**,⛔ 全项目不许再出现「回滚 = 激活旧包」的写法 —— 回滚一律「代码 commit 回滚 + DB `.bak` 还原」。**已知代价(刻意接受)**:策略线要用引擎没有的新玩法,须先等系统线加原语(一次小版本施工,带单测),代码永远不随包传输。
- **[未来] 分钟线数据源**:TuShare 600 元档无分钟线,盘中靠新浪 / 腾讯免费源。若后续要分钟级回测,再评估升档或换源。

### ✅ 已了结(压缩留痕,勿再当待办)

- ✅ **客户端载体 + 推送通道**(2026-07-20 拍板 = 方案 A:SwiftUI 双端 + APNs,新 Bundle ID);✅ **盘中哨兵部署**(2026-07-20 拍板 = 上 hz ECS)。
- ✅ **大盘 MA20 市场过滤器**(旧争议)→ 阶段 1 **P1 否决**,择时归情绪仪表盘展示层;✅ **纪律章程第 4 条「≥5% 次周单笔减半」**(旧挂起)→ 阶段 1 **P10 空效应否决**,从未纳入章程。判决全文在 `STRATEGY_LAB.md` 雷区地图节。
- ✅ **`sync_code.sh` 尾部 chown 翻 `data/` 属主 footgun** → v1.2-0 修复(收尾 chown/chmod prune `data/` + 末尾只读属主自检 `exit 1`),经 v1.3-⑦(2026-07-26)与 v1.3.1(2026-07-27)**两次真部署活体收官**,自检绿、无 502。
- ✅ **盘前情报包(需求 3)** → v1.3-③ C1/C2/C4 完工上线(复盘情报件 / 板块资金流 / 消息面扫描;`anns_d` 无权限 → 减持走结构化 `stk_holdertrade`,其余三类走 LLM 联网搜索)。
- ✅ **候选池生成管线改版(需求 5,K1 entry mask 退役)** → v1.3-③-C3 完工上线(四步管线:板块层 → 个股卫生线 → K4 安检 → 情报排序;候选语义改「过完安检、值得花注意力」)。
- ✅ **问询台选股域漂移** → v1.3-⑤ 完工(复用 `report/watchlist_check.py::discipline_checks`,顺带补上 P4/P5 在问询台从未实现的真实缺口)。
- ✅ **反向证伪哨兵新条件(K3-B2 副产品:年线下涨停/放量大阳 = 派发)** → 已并入 v1.3-② 持仓管理层作强警示条件。**⚠ 与 `STRATEGY_LAB.md` §五「[转系统线] 诱多做局反向哨兵」是同一件事**(2026-07-28 planner 逐码核对确认:该条要的「年线下突现放量大阳/涨停 = 疑似派发」= 已上线的 `A3_belowyear_limitup` + `A3b_belowyear_bigvol`〔量比≥2,雷区地图 3-⑤ 口径〕,住 `report/holding_k4_check.py`,持仓侧触发第六类 APNs、候选侧经 `intel_candidates` 安检、问询台复用同一份镜像)——**只补引用,不重复立项**。**残留半边(如实登记,不单独立项)**:自选池侧未覆盖 —— `report/watchlist_check.py` / `WatchlistCheckOut` 里没有任何 K4 判据,故自选票不会得到派发警示。**处置**:~~留档观察;若日后有块动 `watchlist_check.py`,顺带复用同一份镜像补上即可~~ → **v1.5.0 第 ④ 块(④-A1)兑现**(本版正好动 `watchlist_check.py`,按原处置口径顺带补上):复用 `holding_k4_check` 同一份镜像,**只取 `A3_belowyear_limitup` / `A3b_belowyear_bigvol` 两码**(派发警报,强价量证据),契约 `WatchlistCheckOut.dispatchAlerts`;⛔ **不推 APNs**(自选不是持仓,第六类推送只对持仓;新增推送须用户拍板)。**✅ 2026-07-30 完工**:`watchlist_check.py` 新增 `attach_dispatch_alerts`(读 `_build_holding_feature_panel` 算好的 `_hit_A3`/`_hit_A3b` 两列 + `describe_hits` decorate,零新 polars 表达式)+ `pipeline.py` 保险丝接线(异常只 WARNING、不阻断报告)+ 契约 `DispatchAlertOut`(4 键,省略 `level`,恒 strong/price_volume)+ markdown `_render_watchlist` 追加警示行(非 plan 逐字要求,但与既有 `_render_holding_check` 展示先例一致、属实现细节层面的合理补全)。**「复用同一份镜像」有交叉断言**:`test_watchlist_check.py::TestDispatchAlertsMatchHoldingK4Check` 同一行同时喂 `holding_k4_check.build_holding_k4_check`(持仓侧)与 `attach_dispatch_alerts`(自选侧),断言 A3/A3b 命中的 code/label/evidence/evidenceStrength 逐位相同。
- ✅ **K2 / K3 策略研究** → 中心命题均被否决并归档;权威全文与判决原在 `STRATEGY_LAB.md`、施工图原在 `archive/` —— **2026-08-09 已随策略档案整体迁出本仓**,新位置见 **§1.6**(`档案/docs/`)。

---

## 八、用户操作清单(必须由用户手动办)

1. ~~**确认 TuShare 600 元档接口权限**~~:✅ 已确认(2026-07-19,用户提供官网权限对照表截图,600 元档 = 6000 积分列)。结论已吸收进 §3.2:`daily/daily_basic/adj_factor/index_daily/moneyflow_dc/top_list/概念板块和成分` 可用;**`limit_list` 不可用(15000 积分)→ 改 0.4b 自算涨跌停衍生表**,不阻塞。
2. ~~**确认券商条件单能力**~~:✅ 已确认(2026-07-19):止损条件单与**回落止盈**条件单均支持。阶段 1 止盈研究(1.4)按「回落止盈可由券商执行」设计。
3. ~~**(阶段 2 前拍板)TuShare 新闻资讯接口是否增购**~~:✅ 已拍板(2026-07-19):**不购**,消息面走 LLM 联网搜索(方案与代价见 2.4:需带搜索的 API 供应商、搜索结果落库存档、审判层退出回测改实盘归因考核)。唯一会重开此项的场景:未来想做「历史新闻进回测」(搜索给不了历史时点快照,只有新闻库能)。
4. ~~**(阶段 4 前)客户端载体拍板**~~:✅ 已拍板(2026-07-20)= 方案 A(SwiftUI 双端 + APNs,新 Bundle ID `top.linotsai.neckline`)。

### 用户网页操作清单(阶段 4,必须在网页手动办理)

5. **Apple Developer 新 App ID + 推送能力**(`https://developer.apple.com/account/resources/identifiers`):
   - **Xcode automatic signing 能自动办的**:注册新 App ID `top.linotsai.neckline` + 生成 provisioning + 在加了 push entitlement + Push Notifications capability 后为该 App ID 开启 Push(加 `Neckline.entitlements` + capability 后 Xcode 首次真机构建通常自动完成)。
   - **可能需网页手动确认的**:若 automatic signing 未自动为该 App ID 勾上 Push Notifications 能力,去上述 Identifiers 页面手动为 `top.linotsai.neckline` 勾选 Push Notifications 并保存。
   - **无需新建的**:APNs 鉴权密钥 `.p8`(Key ID `Q963AP3VY8`)是**账号级密钥,直接复用**给新 Bundle ID,不必新建密钥;`.p8` 已在 ECS `/opt/linon`,部署时拷进 `/opt/neckline`。
6. **提供 LLM API key**(阶段 4 起,App 内办):在 App「设置」板块填 GLM 或 Kimi 的 API key + 选供应商 → `PUT /settings/llm` 落服务端 DB,运行时生效。**这是问询台 + 报告 LLM 审判 + 复盘材料的激活开关;缺 key 全链路优雅降级不崩**。填入后首次真调用即验证 GLM/Kimi 协议假设(兑现阶段 2 遗留欠账)。
7. **(每周,阶段 4 起)提供交割单**:每周把券商交割单 xlsx 拖进 macOS App 周复盘工作台,供违纪对账 + 复盘材料生成。**首版按用户现有「整理格式」支持,字段映射可配以兼容两家券商原始格式**(§五 阶段 4D)。
8. **(接班,阶段 4 部署上线后)确认 LinoN 退役**:Neckline 联调通过、`ln.linotsai.top` 切到 Neckline 后,LinoN 停用退役——旧交易记录 `linon.db` 是否归档下载由用户定(`hz_info.md §13` 挂账)。
9. ~~**(v1.1,按需)提供同花顺自选 txt**~~ **⛔ V2.0.0 起作废**(裁定 #9:自选池 + 同花顺对账直接删除,§五 ⑬-11;**2026-08-03 ⑬ 完工,代码与端点已物理删除**)。原文留痕:**(v1.1,按需)提供同花顺自选 txt**:自选池与同花顺 PC 端对账 = 用户在**同花顺 PC 端手动导出自选 txt 文件**(无官方 API,拒绝模拟登录 = 账号风险)→ 拖进 macOS App 自选工作台做差异对账 + 一键对齐;反向可从 App 导出 Neckline 自选为同花顺可导入 txt。**非每日必办,按需对账时才做**(§五 v1.1-C.4 / v1.1-F.4)。
10. **(v1.1)设置屏开新增两类推送开关**:盘前校准(9:26 汇总)、D5 时间退出——App 设置屏可各自开 / 关(默认开),无网页操作。
11. ~~**🔴(v1.4.0,挡 ①-A 收尾)提供 3 笔在持仓票的真实买入日**~~ ✅ **已办结**(2026-07-28 用户提供、builder 已纠正落库,§七 P0-1)。原文留档:生产 `positions` 里 3 笔 `open` 持仓是 2026-07-27 补录的,买入日**被写成了补录当天**(§七 P0-1)。请按票给出**真实成交日期**(格式 `YYYYMMDD`,须是交易日),例:`002036.SZ = 20260716`。**为什么非你不可**:系统没有任何数据能反推真实买入日(交割单要到周复盘才进系统),而 D 计数 / 时间退出 D5-D15 判向 / 回落止盈峰值起点 / 持有天数归因**全部以它为起点**。收到后由 builder 走 `scripts/oneoff/fix_position_buy_dates.py`(先 `sqlite3 .backup`,改完清 `holding_eod_check.time_exit_locked_*` 三列让次日 16:35 重新定格)。**无网页操作,发数字即可。**
12. **🔴(2026-08-08 更新:服务端 2.1.0 已上产)双端换包 —— macOS ✅ 已换 2.1.0,iOS ⬜ 仍待你自己装,唯一挡在你这边的待办**。
    - ✅ **macOS 已由 builder 换成 2.1.0**(2026-08-08 18:14,`ditto` 装入 `/Applications/Neckline.app`,旧包备份 `~/Lino/app_backups/Neckline-2.0.0-20260808-181431.app`)。**它已被重启过一次** —— 你桌面上那个 Neckline 现在是 2.1.0,三板块 + 复盘板块直接可用。
    - 🔴 **服务端已是 `v2.1.0`(2026-08-08 18:09 上产)** → 下面两处「错话」**从现在起是活的**,直到你在 iPhone 上装 2.1.0 为止:
      ① **问询台点了报「持仓已清」**(不是 404 提示 —— 2.0.0 的 404 兜底文案就是这句,驴唇不对马嘴但不会崩,也不影响其它任何 tab);
      ② 选股页会多渲染一个空的「T3」分组并说「今日 T3 为空(算过了,今天没有达到该档标准的篮子)」,而真相是 **T3 已取消**。
      **其余功能(报告 / 持仓 / 篮子 / 推送)在 2.0.0 上照常可用**,不是必须马上换。
    - ⏱ **装的是 2.1.0**(⛔ 别再装 2.0.0 了)。装完自检:设置屏「App 版本」与「服务端版本」两行都应是 **2.1.0**。⚠ **若你曾在设置屏手填过 `NK_BASE_URL_OVERRIDE`,它压过默认基址** —— 换包后连不上先去清空它(默认已指 `https://nk.linotsai.top`)。
    - **原 V2.0.0 现状(留痕)**:macOS ✅ 已换包 2.0.0(2026-08-04 ⑰ 割接时完成)/ iOS ⬜ 待装。
    - **⚠ 与下面 v1.5.0 时期的记载相比,有一处性质变了**:割接后**老机 hz 上的 Neckline 已 stop + disable**,`ln.linotsai.top` 的 A 记录也已 NXDOMAIN → **iPhone 上的旧包不再是"看不到新功能",而是彻底连不上、已不可用**(下面第 644 行那条「V2 上云期间旧 App 照常能用」是割接前的口径,**已作废**)。
    - **装机步骤照下面 ①~⑤ 不变**(scheme / 签名 / Team 都没动),装的是 **2.0.0**;装完自检:设置屏「App 版本」与「服务端版本」两行都应是 **2.0.0**。⚠ **若你曾在设置屏手填过 `NK_BASE_URL_OVERRIDE`,它压过默认基址** —— 换包后连不上先去清空它(默认已指 `https://nk.linotsai.top`)。
    - **原 v1.5.0 记载(留痕,细节仍适用)**:**macOS 已完成**(2026-07-30 09:46,随 v1.5-⑥ 上云一并做):Release 构建经 `ditto` 装入 `/Applications/Neckline.app`,产物与安装态可执行文件 **sha256 逐字节一致**、`CFBundleShortVersionString=1.5.0`、签名校验通过,启动后对 v1.5.0 服务端 5 个端点真实请求全 200。**iOS 仍待办**:iPhone 上还是旧构建 —— **v1.5 的参考三件套(取代老四件套)、今日计划分块、自选派发警示在 iPhone 上看不到**,且老四件套四键现在服务端只返「本版已由参考三件套取代…」过渡文案,**旧 App 上那四栏会显示这句话而不是数字**。需**你用 Xcode 直连真机装一次**(服务端已 `v1.5.0`,先决条件已满足;无网页操作)。§八 第 5 项的 App ID + Push 能力若尚未在 Apple Developer 网页勾选,装机前先办掉。**真机具体版本仍待你自查**(builder 只有模拟器权限)。
    - **iOS 装机步骤(§五 v1.5-⑤-F 改写;builder 无真机权限,此步骤仍需你亲自操作)**:① 数据线接真机到这台 Mac,解锁并「信任这台电脑」;② Xcode 打开 `client/Neckline.xcodeproj`,顶部 scheme 选 **Neckline**,设备选择器切到你的 iPhone(不是模拟器);③ 签名走 **Automatic**(项目已配 `CODE_SIGN_STYLE: Automatic` + `DEVELOPMENT_TEAM`,无需手动选证书);④ ⌘R 直接跑;⑤ 若报 `Entitlement aps-environment not found`(推送能力未随新 App ID 自动开)→ 回 §八 第 5 项,去 Apple Developer 官网手动为 `top.linotsai.neckline` 勾选 Push Notifications 后重试。
    - **builder 侧已验证的 iOS Release 构建**(`xcodebuild -configuration Release -destination 'generic/platform=iOS' build`,`CFBundleShortVersionString=1.5.0`)是「代码在 Release 配置下能正确编译签名」这道门禁的证据,**不是拿来装机的介质**——那份产物的签名身份/描述文件与你真机直连时 Xcode 现场生成的不一定相同,**装机仍走上面①~④「Xcode 直连真机」这条路**。
    - ⚠ **现状**:iPhone 上还是 v1.4 之前的旧构建 —— v1.4 的信息卡页 / 候选排序理由 / 决策日志第⑨项 / 停牌标注、以及 v1.5 的参考三件套 / 今日计划拆两块(持仓股/候选列表) / 自选派发警示**在 iPhone 上都看不到**;macOS 已是 1.4.1。§八 第 5 项的 App ID + Push 能力若尚未在 Apple Developer 网页勾选,装机前先办掉。**换包后自检**:设置屏底部「App 版本」/「服务端版本」两行(v1.5-⑤-E 起分行展示,取代旧的单行「版本」)应一致,不一致会有一行提示「服务端已是 vX.Y.Z,当前 App 为 y.z,请换包」。**无网页操作(除非踩到上面第⑤条 `aps-environment` 报错)。**
    - ⚠ ~~**V2.0.0 附注**:V2 是一次性契约换血 —— 但 **D2 = A 路已拍板**(新机挂 `nk.linotsai.top`、老机 `ln.linotsai.top` 原样服务),所以 **V2 上云期间你手机上的旧 App 照常能用**(它还打老机);**换到 V2 需要你装一次新包**,时机 = §五 ⑰(**等你指令**,builder 不得自行换包),换完再谈老机退役。~~ **⛔ 割接后作废(2026-08-04)** —— 老机 Neckline 已 stop + disable、`ln` 解析已 NXDOMAIN,**旧 App 已连不上**;⑰ 换包已完成 macOS 侧,iOS 侧就是本项顶部那条唯一待办。

### V2.0.0 用户操作清单(2026-08-02 立项新增;git / 部署 / 构建由 agent 自理,不列)

> ⏱ **时机(2026-08-02 用户追加指示)**:**云上组件迁移放在尾声** —— 第 13、14 两项要在 §五 **⑯** 开跑之前办好即可,**不必现在就办**;①–⑮ 是纯本地施工,不依赖它们。你按自己的节奏买机 / 解析,builder 到 ⑯ 之前会先问一句。

13. **🔴(V2,挡 §五 ⑯ 整块)购买新云服务器** —— **裁定 #3:100% 更换,这是部署前置项**,不办则 ⑯ 无法开始(①–⑮ 不受影响,可照常推进)。
    - **规格(§五 D3,2026-08-02 已拍板)**:**≥ 4 vCPU / 8 GB 内存 / 100 GB SSD**,Ubuntu 22.04 或 24.04 LTS,地域**杭州**(与现有 `hz` 同区,数据搬家快;也可换区,只是搬家慢一点)。**为什么要这么大**:老机 2 vCPU / 1.6 GB 已经被 P0-23 打爆过一次(全历史 parquet 扫描 700M cap OOM、1400M cap 600s 跑不完),而 V2 新增三张全市场预计算表 + 复盘引擎 + 盘中存拍,晚间管线比 v1.5 重一个量级。
    - **购买页**(用现有账号即可,新开一台,**不要动老机**):`https://ecs.console.aliyun.com/`(阿里云 ECS 控制台 → 创建实例);也可选任意云厂商,只要能 SSH。
    - **办完给我什么**:公网 IP + SSH 登录方式(**口令 / 密钥都行,但绝不要贴进任何被 git 跟踪的文件** —— 直接在对话里给,builder 只写进服务器上的 `~/.ssh` 与本机 config)。
    - ⚠ **老机不要停**:`ln.linotsai.top` 上的 v1.5.2 要一直服务到你完成 ⑰ 双端换包并确认之后,才由你决定退役与数据归档。
14. ~~**🔴(V2,挡 §五 ⑯-G)把 `nk.linotsai.top` 解析到新机**~~ —— **✅ 已办妥并验收(2026-08-04)**:DoH 实测 `nk.linotsai.top` → `114.66.0.38`,证书已据此签发、公网 `/api/v1/health` 实测 200。**⛔ 别再改这条 A 记录** —— 改了会连带作废 LE 证书的续期(HTTP-01 要打回这台机)。
    - **在哪办**:你域名 `linotsai.top` 的 DNS 控制台加一条 **A 记录** —— 主机记录 `nk` → 记录值 = 第 13 项那台新机的公网 IP,TTL 默认即可。若域名在阿里云:`https://dns.console.aliyun.com/`。
    - **builder 侧接手的部分**(无需你操作):解析生效后申请证书(certbot)+ 配 nginx 站点 + 定时续期;判据 = `dig nk.linotsai.top` 已返回新机 IP。
    - ⚠ **两条边界**:① **新机 nginx 绝不接管 `ln.linotsai.top`** —— 老 App 必须继续打老机,这是「V2 契约一次性换血、不留过渡键」成立的唯一前提;② **老机的解析在 ⑰ 换包完成前一律不动**。
    - ~~备选 B 路(沿用 `ln.linotsai.top`,割接时切解析)~~ **已否决,不再列为选项**(§五 ⑭-D)。
15. ✅ **已办(2026-08-05 10:53,你在 App 设置屏填的)· 在 App 设置屏填 LLM Provider** —— V2 起是**自填制**(任意 OpenAI 兼容端点),不再是「GLM / Kimi 二选一」。**现状 = 单 Provider 常态(你同日拍板)**:`GLM` / `glm-5.2` / `has_web_search=1` **一条**,检索与推理两类任务都由它承担;裁定 #2 的双 Agent 分工降级为可选扩展路径。
    - ⚠ **两个当天打出来的配置坑,再填时照此**:① 本项目 `base_url` 列语义 = **完整端点**(`…/paas/v4/chat/completions`),**不拼路径** —— 少写 `/chat/completions` 会拿真 key 打出 404;② `llm_default_provider` 必须设(路由未命中时的回退目标),空串会让推理类任务无 provider。两处均已修复并真实调用验证过(§九 2026-08-05)。⛔ `search_engine` **留空是对的**,别去补取值。
    - **想恢复双 Agent 分工时**(可选,无期限):加一条**推理 Agent**(建议 DeepSeek,key 在 `https://platform.deepseek.com/api_keys`,**「是否带联网检索」不勾**)→ 把它设为默认 provider 或把推理类任务显式路由过去即可,**不改代码**。检索 Agent 的 key 在 `https://open.bigmodel.cn/usercenter/apikeys`(**「是否带联网检索」勾上**)。
    - **缺 key 全链路优雅降级不崩**(篮子机械项照出、LLM 段如实标缺席)。
16. **(V2,可选、无期限)分时数据外源方案(裁定 #1:你自寻)** —— V2 的盘中存拍从上线日起自给,历史分时缺口只能靠外源回补。你若谈定了数据源,把**能拿到什么字段、什么频率、怎么取**告诉我,builder 接进 `intraday_ticks` **同一张表**回补,复盘引擎无感切换。**没有也不影响 V2 上线**(缺分时的复盘走 EOD 近似 + 如实标注,§七 P4-30)。

### V2.1.0 用户操作清单(2026-08-07 立项新增)

> ✅ **本版零网页操作**(不碰域名 / 证书 / Apple Developer / 付费能力);git、部署、云上动作、macOS 换包由 agent 自理,不列。

17. **🔴(V2.1,唯一挡在你这边的)iOS 换包到 2.1.0** —— 步骤与自检完全照上面第 12 项(scheme / 签名 / Team 都没动),装完设置屏「App 版本」/「服务端版本」两行都应是 **2.1.0**。⚠ 若你曾在设置屏手填过 `NK_BASE_URL_OVERRIDE`,它压过默认基址,换包后连不上先清空它(默认已指 `https://nk.linotsai.top`)。
18. **(V2.1,每周,承第 7 项不变)交割单仍由你上传** —— V2.1 新起的周度作业(周六 09:00)**只自动跑画像与校准两件,对账仍等你上传交割单**(§五 ⑥ 的裁定理由:排了 timer 也补不出没上传的那一份)。没上传的那周,复盘板块对账段会如实显示「本周尚未上传交割单」,**不是故障**。

### V2.2.0 用户操作清单(2026-08-09 立项新增)

> ✅ **本版零网页操作**(不碰域名 / 证书 / Apple Developer / 付费能力);git、部署、云上动作、macOS 换包由 agent 自理,不列。
> ⚠ **第 18 项(每周交割单)在 V2.2 不但不变,还更重要了** —— K8 的**交易时钟**统计用的就是真实买入样本(§五 ④-B/④-C),没交割单那一周的交易侧 6 项指标会如实缺席。

19. **🔴🔴(V2.2,挡 §五 ⑦ 批 4 = 章程激活)清空全部持仓 + 明确确认,才能激活章程 `v2.2-k8`**
    - ⏳ **2026-08-09 用户表态:「你就记我清仓,我们先把工程做完」** —— 故 **⑤ 的代码全部照建、不等清仓**;
      **卡住的只有"在生产上激活章程"这一个动作**(`activate_charter.py` 闸 2 **硬校验无 open 持仓**,
      带不带 `--confirm` 都过不去,不是纪律软约束而是切换器的物理闸)。**⚠ 实测 2026-08-09 生产
      `positions` 仍有 2 笔 open**(§四 旧快照记 0 笔已过时)。**⑤-B 熔断退役是纯代码、部署即生效,
      ⛔ 不受此闸约束**,与章程那半边是两个独立动作。 —— 这是 staged 铁律(切换器闸 2 硬校验「无 open 持仓」),同 v1.2 / v1.3 两次先例。
    - **你要知道自己在批准什么(⛔ 别只看一句"按 K8 改")**:本次退掉的是 §1.3 交割单归因里**仅有的两条被真金证明过的防线** ——
      ① **−5% 强制止损条件单**(历史上 13 笔破线未止损多亏约 1.38 万,**占已实现亏损 85%**)→ 改为「**止损警戒 + 离场决策**」,**决策与执行 100% 回到你手上**;
      ② **时间退出**(持有 4–7 自然日是唯一打平的持有桶;`winners_anatomy` 实证:**封顶赢家的正是 hold=5 日历退出**,大赢家 88.6% 由它了结)→ 整体退役,交由「上涨效率下降时主动换股」这一**主观**动作替代。
      另外 **回落止盈 8%** 也退役(它是 H9 网格里唯一同时改良全期与 2026 的格)。
    - **新退出规则从未被回测验证,也无法被现有回测引擎验证**(它依赖盘中人判层,EOD 数据够不着)→ 这是**你行使决策权的越线采纳**,同 v1.3 先例,风险已全文登记在 §五 ⑤ 与章程注释里(⛔ 不得删)。
    - 🔴 **同批还有一件你拍板的事:熔断整体删除**(裁定 #8「我不需要你替我做决定」)。**你要知道删掉之后会怎样**:① 连亏时系统**只剩一条提醒**,没有任何锁定态、没有灰化、没有「次日只减不加」;② 那句「次日只减不加」此前是靠 9:26 汇总推送的**必发豁免**送到你手机上的,**这个豁免一并取消**(以后平静的清晨就是真的没推送);③ **停不停手 100% 由你当场决定**。⚠ 熔断部分是**纯代码、部署即生效**,**不等你清仓** —— 与章程那半边(staged)是两个动作。
    - **不变的两件**:仓位三仓制(3 仓 / 单笔 4 万违纪上限 / 满仓档)· `stop_pct=0.05` 这个**数字本身**(它仍是篮子失效条件的唯一源,只是不再意味着「必须挂条件单」)。⚠ **§2.1 第 4 条「单周亏损 ≥ 总仓 2% → 当晚强制复盘」也不变** —— 它不是熔断。
    - **回滚**:随时可 `activate_charter.py --target v1.3.3 --confirm` 切回(SOP 见 `archive/SOP_章程回滚_20260730.md`)。
20. **🔴(V2.2,挡 §五 ⑦ 批 5)双端换包 2.2.0** —— 步骤与自检完全照第 12 项(scheme / 签名 / Team 都没动)。⚠ **在此之前先把 §八 第 17 项的 2.1.0 装了** —— V2.2 是在 2.1.0 之上加键,你手机上那台 **2.0.0 隔了两个版本**,两处错话(问询台显示「持仓已清」/ 空 T3 分组)会一直在。
21. ~~**(V2.2,可选、无期限)K8 的策略线账本由你同步**~~ ✅ **已办结(2026-08-09 当日,你自己做的)** —— 策略研究档案已整体迁出本仓到 `~/Lino/whynotme/`(§1.6),本项与 §七 **P3-53** 同批销案,**不再需要你做任何事**。

---

## 九、变更日志(一行制;详版全文 → `archive/变更日志_详版_20260719-20260728.md`)

> **记录纪律(2026-07-28 起)**:每次动作只记**一行**(日期 · 标题级摘要)。事故复盘、完工验收、长记录一律写 `archive/` 独立文件,此处一行 + 链接,同一件事全文只存在一处。2026-07-28 之前的 54 条详版全文见上述归档文件(原样未改)。

- 2026-08-10 · 🎯 **V2.3.1 批 2 完工(macOS 选股板块逐屏对到原型 inline style;⛔ 只动 `client/**`、未改版号、未部署)**。**六屏实拍**(五张 1200×860 + 成员卡展开态附图 1200×1410 同宽):篮子卡 / 卡未就绪篮 / 今日概览 / 昨日回执 / 报告未生成 / 成员卡。**列表栏**:两套页边距(标题区 18/16/10 vs 行 0/10/4)· `pick()` 选中态换**白底 + 1.5px 实蓝描边 + 轻投影**(原 1px 半透明蓝在 `#FCFCFD` 上几乎看不出)· T1/T2/③b 三个分组头统一成 `NKGroupHeader`(⛔ 不再用 `title3` 17)· 篮子行灯条由「六点各挂关名」改**六点 + 一句摘要**、成员区移到选中框外、成员行加 5px 状态点 · ③b 去白卡壳收成两行。**详情栏**:`22px 26px 40px` 页边距 · 卡内边距分 `16/18` 两档(新 `cardPadH`)· **六关宫格整块重画**(居中三行 / radius **9** / `padding 9×8` / 机械关 1.5px 实边框 + 底色 +「硬」角标 radius **3** / 证据关 .5px 描边 +「证据」灰角标;§〇d 第 4 条点名的两处单独定值)· 「不得进 T1」由折叠区提到宫格下方琥珀条 · ①②③ 并成一张卡、证据条改 3px 竖条 · **成员卡改「卡里的卡」**(外层白卡 `14/14/12`,内层 `memCard()` radius 10 无边框、展开态 `#FAFAFB` + 1px 蓝内描边)· 三个参考件 / 关卡片改白底描边块 · 动作按钮改「蓝实底 + 描边」双按钮 · ⑥ 三剧本改三列并排 · ⑦⑧⑨ 并卡 + 绿勾红叉 · ⑩ 与 ⑪ disclaimer 合并、Tier 红线句收进披露区(**与原型 685 行同位**)· 审计视图收起态改虚线按钮(窄栏另给 `compact`)。**新增三件**:`nkRegimeDimLabel`(🔴 硬伤 2 的**第七处**:行情状态五维缺维在首屏直接印 `moneyflow_migration` 这类服务端英文常量,沿 `nkBoardLabel` 先例换算、未识别原样透传)· `NKGroupHeader` · `NKWrapRow`(`flex-wrap` 等价 `Layout`,⛔ 不再拿横向 `ScrollView` 顶替 —— 桌面上超宽的项**看不见也不知道有**)。**② 持仓体检入口卡在 macOS 今日概览撤掉**(原型没有,列表栏那行右端写死「①·情报·⑤」;工具栏就有持仓胶囊)· **⑤ 那条通栏红橙告警横幅收进 ⑤ 卡内的琥珀块**(原型无横幅;过期项行内标红 + 卡内提示 + 工具栏「降级 N 项」三处仍都说得出口)。**新增 QA 钩子 `NECKLINE_INITIAL_RECEIPT=1`**(只给 `@State` 当初值)。**五处判「刻意不同」**:持仓占比(〇-4)· 六关判不出是篮级(〇-3)· 排程表四个时刻同样着色、⛔ 不画原型那颗「16:05 进行中」光晕(〇-2)· 缺数维斜纹 → 半透明琥珀 + 虚线描边(SwiftUI 无 `repeating-linear-gradient`)· 一批字号就近对齐令牌。**施工期新逮两条通用坑已入 §五 〇d**:`Text(String)` 不解析 Markdown(`**加粗**` 星号上屏)· 演示库 `sentiment_json`/`sectors_json` 是 snake_case + 中文额度,写错会**静默**解不出。**验收**:Python **3581 passed / 3 skipped / 0 failed** · iOS **214 tests / 0 failures** · 双端 **BUILD SUCCEEDED** · 宿主域自检 `defaults read top.linotsai.neckline NK_BASE_URL_OVERRIDE` = **does not exist**(⛔ 只查这一个键,`NK_API_TOKEN` 是用户真 token)。**六屏逐项对照表全文 → `archive/V2.3.1_批2对照表_20260810.md`**;完工摘要 → §五 ③。

- 2026-08-10 · 🎯 **V2.3.1 批 3 完工(macOS 持仓板块逐屏对到原型 inline style;⛔ 只动 `client/**`、未改版号、未部署)**。**六屏实拍**(五张 **1200×860** + 披露全展开附图 **1200×1410**):正常仓 / 破止损仓 / 停牌判向挂起仓 / 盘中动态 / 临时提醒。**列表栏**:标题区两套页边距 + 「补录开仓」改淡蓝底小胶囊 · 汇总条改「数在上名在下 + 竖分隔 + 右端待处理组」· 盘中动态 / 临时提醒两行降为与仓位平级的 `pick()` 行 · 合并敞口块改琥珀淡底窄块 · 仓位行加**迷你刻度条**(轨道 4 + 三标记)。**详情栏**:徽标行提到名称之上 · 现价 32 + 涨跌 15 · **刻度尺整块按 1012–1029 重画**(轨道 5@top20 · 安全区绿渐变 · 四刻度各自宽高 · 标签浮动 `translateX(-50%)` + 两端 clamp · 破线转浅红底 + 整卡 1px 描红)· 条件单勾选框改 **15×15 自绘方框**(⛔ 非 SF Symbols)· **持仓计划卡由折叠改常开三列等宽网格** · 交易时钟 D 轴 + 虚线「补一条主观说明」按钮 · 动作行改左对齐双按钮 + 右端实付费用句 · 盘中动态改「一张卡若干行」· 临时提醒头部改大标题 + 实心蓝主按钮。**本块两段完成**:代码先落盘,再做**独立复核**并当场改齐两处 —— 🔴 **① `plan_json.*_clamp` 服务端英文机器码被当中文正文印上屏**(生产上是 `本次不可用(rejected_not_above_close)`,硬伤 2 的**第八处**;上一轮**演示种子把这格喂成中文「夹逼拒收」→ 首版实拍看不出来**)→ 正文改「本次不可用」、机器码降 `monoKey` 等宽灰字,⛔ 不在客户端补中文表(人读文案唯一源在服务端 `clamp_reason_text`);**② 表头「离场参考区间(不是止盈线)」→「离场参考区间」**(原型 1054 + 批 2 篮子卡先例)。**四处判「刻意不同」**:占总仓三处(§五 〇-4)· **回落止盈线**格(`RetraceState` 只发 `{peak,retracePct,triggered}`,比例从未下发)· 计划卡偏离提示(`planDeviationNotice` 只在开仓响应发一次)· 提醒卡主语只有代码(`CustomAlert` 无名称字段)。**验收**:Python **3581 passed / 3 skipped / 0 failed** · iOS **214 tests / 10 skipped / 0 failures** · 双端 **BUILD SUCCEEDED** · 宿主域 `NK_BASE_URL_OVERRIDE` = **does not exist**。**登记未改**:🔴 金额缺千分位且带两位小数(`NKFmt` 是四板块共用格式化器,**批 1 全局钉子漏网项**)· clamp 的服务端根治(`positions_entry._member_plan_fields()` 补搬 `*_unavailable_reason`)· `NKMemberCard` 同款兜底分支。**六屏逐项对照表全文 → `archive/V2.3.1_批3对照表_20260810.md`**;完工摘要 → §五 ④。

- 2026-08-10 · 🎯 **V2.3.1 批 5 完工(macOS 设置四组 + 四个弹层 + 信息卡整页逐屏对到原型 inline style;⛔ 只动 `client/**`、未改版号、未部署)**。**十屏实拍**:设置四屏(后端连接 / Provider 与路由 / 锁屏推送 / 版本)+ 补录清仓 + 补充说明(各 **1200×860**)· 新建提醒 + 编辑 Provider(**1200×1000**,弹层装不下时**只加高不改宽**)· 信息卡上半 / 下半(各 **1200×1340**;整页约 2100pt,下半那张按 CLAUDE.md **先砍演示数据**,顺带把「每一路数据独立可用性」拍进图里)。**新增一件 `NKFormKit.swift`**(弹层壳 `NKSheetShell` + 分组字段卡 + `NKSwitch`/`NKSegmented`/`NKTagButton`/`NKCheckSquare`/`NKTextFieldBox`/`NKOutlineButton`/`NKDashedButton`/`NKInlineMenu`/`NKInlineNote`/`NKTintedNote`)—— 🔴 理由是 **`Form(.grouped)` 的圆角 / 页边距 / 分隔线 / 段标题字号全由系统定、改不了**,逐项对不到原型;⚠ **iOS 侧原样保留 `Form`**(归批 7)。**设置四屏**:列表栏**去掉每行的 SF Symbol**(原型 1582–1607 四行都没有图标)+ 右端读数四种 · 详情栏改自绘卡 + 26/700 大标题 · 环境改**白底浮起式分段控件** · 覆盖栏警示句转琥珀 · 自检按钮改**常态无底色**蓝描边 · Provider 卡改「名 + 两枚徽标 + 等宽端点行 + 分隔线 + 编辑/删除」· 新增 Provider 改虚线按钮 · 推送开关改 **34×20 绿药丸**、分组头按级着淡底、三组顺序改**由重到轻** · 版本页补「现役纪律章程 / 现役选股包」两行。**四个弹层**:标题条 **52px**(⚠ 原型 inline style 是 52,散文写 56,按 §五 〇a-A 取 inline)+ 取消/主操作**各定宽 60** · 补录清仓补**灰底标的头** + 九码改**一排可点标签**(已选按语义着色) · 补充说明七枚标签改**两列可点方块** · 确认卡七项改键值表且 **⑥ 行情延迟琥珀底 / ⑦ 动作灰底** · Provider 弹层三段各带琥珀/灰说明块(**key 只写不回显**不变)。**信息卡**:sheet 由 660×740 放到 **1120 宽自适应高** · 三图改「K 线通栏 + RS/分歧并排两列」· MA250 由琥珀改灰 · **量柱带改一排 `Rectangle`**(第二个 `Chart` 在 26–34pt 高度上渲染成一条空白,实拍两次逮到) · 快照改四列 · 红黄牌/消息面、龙虎榜/市场语境各并排两列。**逮到四处真缺陷**:🔴 ① **硬伤 2 的第九处** —— 「判不了的标注」直接印服务端英文码(`pullback_leader`/`warn_streak_top`/`warn_chase_zone`),按批 3 clamp 先例改「正文说人话 + 机器码降 `monoKey`」,⛔ 不在客户端补中文表;🔴 ② **§〇d 第 7 条的服务端版本** —— 确认卡 ⑥ 行的服务端原文带 `**有延迟**`(`custom_alerts.py:116`),`Text(String)` 不解析 Markdown → **星号原样上屏**,新增 `nkServerMarkdown()` 修(双端);③ 红黄牌 `level` 的三元写法把未识别值**静默讲成「普通」**;④ RS/分歧线默认 y 轴带 0,**把线压成一条平线**。**判「刻意不同」十处**(详见对照表):「主板」与「现价」两个字段契约里没有 · 同篮对比表的「今日」列与本票自己那一行 · 三张图**保留坐标轴**(原型 SVG 是纯装饰位图)· 两列卡不等高 · 补充说明多一格「标的」· 路由表/推送表各多一枚「保存」· Base URL 不折行 · 一批字号就近对齐令牌。**验收**:Python **3581 passed / 3 skipped / 0 failed** · iOS **218 tests / 10 skipped / 0 failures** · 双端 **BUILD SUCCEEDED** · 宿主域 `NK_BASE_URL_OVERRIDE` = **does not exist**。**登记未改**:服务端 `tags_absent` 应改发 `{code,label}` · 全仓「`Text(String)` × 服务端 Markdown」未系统排查 · `NKCard` 缺「填满行高」一档 · 🔴 **`kill -9` 反复杀 dev 变体会留下损坏的 Saved Application State,下次启动窗口永不出现**(进程活着、0% CPU、日志为空;解法两步见对照表 §超范围-4)。**十屏逐项对照表全文 → `archive/V2.3.1_批5对照表_20260810.md`**;完工摘要 → §五 ⑥。
- 2026-08-10 · 🎨 **V2.3.0 前端视觉整改本地完工(六批全落地,⛔ 未部署 / 未换包)**。交接包 `Neckline视觉升级/` 落进 `client/**`:字阶八档 + 两数字档(删 9.5 档)· macOS **去 240px 侧栏 → 50px 工具栏 + 列表栏 376 / 详情栏自适应**(新 `NKSplitLayout`,四板块共用)· 五个新组件(`NKDisclosure` / `NKStopScale` / `GateGrid` + 灯条改 7px 方点圆点二分 / `NKMemberCard` / `NKToolbar`)· 原始件下沉 `NKAuditSection` · 篮子卡由 700×780 sheet 改右详情栏 · 通栏刹车条上移到壳 · App 图标换红橙 · **455 个裸 `.system(size:)` → 60**(余下全是图标尺寸)。**唯二碰后端处经用户当日拍板**:版号三处同升 `2.3.0` · `PositionsOut.circuit` **真删键** + 客户端删两个 DTO(两步淘汰第二步;⚠ 零风险判据 = 逐版核实**历代客户端从没有一版声明过这个键**,2.0.0 那台读的是已 404 的独立端点 —— ⛔ 不是零删键铁律破例)。**§七 P3-47 结案**(复盘每日日期选择器,复用 `GET /report?date=` 读冻结快照,另存一份 ⛔ 不覆盖 `report`)。**施工期新逮到两处「mock 画了、契约支撑不了」并诚实降级**:六关「判不出」是**篮级不是格级**(冻结卡无逐关 `available`)· 「占总仓 %」**分母不在契约里**(`Settings.total_capital` 从未下发)→ 只显示金额。**验收**:Python **3581 passed / 3 skipped / 0 failed**(基线 3579,+2 = 新增两枚守门针脚)· 双端 **BUILD SUCCEEDED** · iOS **211 tests / 0 failures**(基线 214,−3 = 随 DTO 删除的三条)· iOS 实拍核对(演示库 + `DB_PATH` 隔离后端,⛔ 未碰 `data/neckline.db`)。**截图期逮到并当场修两处**:`NKChip` 空文案渲染成没字的灰胶囊 · 成员卡收起行在 iPhone 宽度下把判定徽标压成竖排单字。🔴 **macOS 视觉核对未做**(用户正式 App 正在跑、同 bundle id,启动 Debug 构建只会切到那个 2.2.0 → 截出来是旧版),按 CLAUDE.md 兜底走「iOS 截图 + 双端 BUILD SUCCEEDED」等价证据。**全文 → §五 V2.3.0**。
- 2026-08-10 · 🚀🚀 **V2.2.0 批 2/3/4/5 一次性全部上产 + macOS 换包(用户明确授权「就现在、我知道哨兵会断」,⛔ 越出 ⑦ 部署窗口是用户知情决定)**。⚠ **窗口违例如实登记**:执行时间 **周一 09:00–09:05,盘前**,不属 ⑦ 允许的任何窗口(交易日 15:05–15:55 / 17:15 后 / 周末全天);orchestrator 已先行报明「哨兵会在开盘前断、且批 1 将失去独立验收」,用户逐条知情后仍要求立即执行 → 照办。**哨兵实际中断 5 秒**(09:03:18→09:03:23,早于 09:25:30 盘前 tick 22 分钟)。**顺序**:双备份(`preV22b2345-20260810-085832`,`integrity ok`)→ `sync_code.sh`(5 个关键文件 sha256 双向逐位对拍、`data/` 属主自检通过)→ **三条引擎线 `C1`/`Z1`/`Y1` 四道闸演练全过后激活**(⚠ **骨架包无需升版** —— 裁定 #11 返工删掉 `config.landing` 后,仓库文件与生产已激活行 `config_json` sha 逐位相同 `2f9641cc…`,原计划的 `K8-V0.6` 前置自动消解)→ 装 `neckline-weekly.{service,timer}` → restart。**验收全绿**:四线各一行现役(`V:K8-V0.5 / C:C1 / Z:Z1 / Y:Y1`,LEGACY 两行留档 `is_active=0`)· 表 47→**52**(+5:`landing_metrics_daily`/`gate_evaluations`/`selection_clock`/`trade_clock`/`trade_clock_events`;`landing_state_daily` 确认**从未上产**)· `holding_eod_check.max_hold_effective` 已放宽为可空(⑤ 补的那处 schema 迁移生效)· `/circuit` 与 `/circuit/unlock` 均 **404** 且 `positions.circuit={"locked":false,"episode":null}`(熔断退役 + 老客户端不炸)· `/clocks/selection`、`/market-regime`、`/eval/weekly` 均 200 · 零 failed unit · load 0.11。**⚠ 周度 timer 踩了 P0-45 同族的坑并当场修**:`systemctl enable` 后 `SubState=dead`、`NextElapse` 为空(enable 只建符号链接、不启动),补 `start` 后自检判据达标(`waiting` + `Sat 2026-08-15 09:00`)。**macOS 换包 2.1.0 → 2.2.0**:旧包 `ditto` 备份 `~/Lino/app_backups/Neckline-2.1.0-20260810-090427.app`(⛔ 非 `cp -R`)· Release build SUCCEEDED · `codesign --verify --deep --strict` 通过 · `ditto` 换包 · 启动进程在跑、零 error 日志。**⛔ 章程 `v2.2-k8` 仍未激活**(现役仍 `v1.3.3`)—— `activate_charter.py` 闸 2 硬校验无 open 持仓,生产 2 笔未清,等用户清仓。**🔴 两项部署前置被迫后移到收盘后窗口**(盘前跑重活会在开盘时飙 load,用户授权的是「哨兵可断」不是「压垮机器」):① **P0-23/P4-50 `landing_metrics_daily` 生产隔离实测**(全市场逐票 × 60 日回看,本项目第二条该量级批算)· ② `neckline-weekly.service` 的 `TimeoutStartSec`/`MemoryMax` 占位值校准。**两项都必须在今日 15:05–15:55 跑完** —— 那仍在 16:35 晚间链之前,不达标还来得及关掉对应段。
- 2026-08-09 · 🔴 **用户裁定 #11:位置关不要机械层,判定直接交 LLM(推翻自己的裁定 #4 在位置关上的适用;施工图 §五 ③ 已改写,代码返工未开工)**。**触发** = K8 §二 对「落地起跳」只有五句定性零个数字,裁定 #4 授权的「工程侧翻译成首版定量」在立项时落成**十二个阈值 / 13 个子门**,施工后实测**连乘交集近乎为空**(`liftoff_confirmed` 全市场当日 1~2 / 5526,**14 个 D0 回放零 T1**)—— 用户判定这一关**本就不该量化**,原话「不要搞这个机械层了…这个地方的判定直接给到大模型」。**三条落点**:(a) **只限位置关**(市场关 / 板块关维持机械硬否决,它们读的是已预计算的客观量而非拍出来的阈值);(b) **位置关 = 证据关、只降级不除名** → **§2.0 第〇原则第 4 锁「LLM 不做闸门」完好无损**(用户在两条自己的裁定冲突时选的那一边);(c) **保留读数、删阈值与四态**,机械层只算事实不下结论。**⛔ LLM 调用增量仍是 0**(判定搭 `basket_reason` 那一次)。**改写落点**:§五 〇 第三轮裁定表 · ③ 节头 · ③-A 二分表 · ③-B ⑤ 行 · **③-C 整节重写**(原文折叠留档、⛔ 作废)· ③-D 的 T1/T2 判据 · ③-F 位置关行 · §2.9-C-1 · §3.11-B · 〇b-5 · ⑥ 契约 `BasketMemberOut` 行。**表改名** `landing_state_daily` → `landing_metrics_daily`(⚠ 原表**从未上产**,批 1 只部署了 ①②,故可干净重构、不受「停写留档不 DROP」约束)。**过程如实登记**:orchestrator 在向用户报「T1 几乎不可达」时**未同时说明那些数字出自施工图 ③-C、依据是用户自己的裁定 #4**,导致用户误以为是施工期自行量化 —— 报问题必须连同出处一起报;另,裁定 #4 授权的是「翻译成首版」,**未授权「翻译完不验联合通过率就往下建」**,派工前漏了这一步。
- 2026-08-09 · 🚀 **V2.2.0 批 1 上产(nk,周日窗口,零市场影响;对外版号 `v2.1.0` → `v2.2.0`)**。顺序:双备份(`integrity ok`)→ `sync_code.sh`(6 个关键文件 sha256 双向逐位对拍、`data/` 属主自检通过)→ **`retire_legacy_packs.py --confirm`**(K7-pack-v1 `is_active 1→0`,总行数 2 前后不变 = **零 DELETE**,事件流追加 `deactivate`)→ **P0-23 隔离实测**(`User=neckline` 瞬态 unit,**⛔ 非 root `--scope`**;`ExecMainStatus=0` / **827ms** / **扛住 `MemoryMax=64M` 硬上限**,相对 scan unit 的 1400M 余量极大 → **配额不改**)→ **激活骨架线 `K8-V0.5`**(四道闸演练全过;**上闸前逐键对拍 K7:差异恰 3 键 = 排科创 + 排白酒 + 去退役键 `tier3_min`,`tier.weights`/`dims` 逐位相同** = 兑现「差异收敛为选股域收窄一处」判据)→ restart。**判据八条全绿**:`/health`=`v2.2.0` · 表 46→**47** · `selection_packs` 列 10→**12** · 索引换 per-line · 现役**恰 1 行**且 `line_code='V'` · `/market-regime` `available:true` · 排程未受影响(下次 08-10 16:05/16:35)· 零 failed unit · load 0.00。**回退自愈已验证**:激活后重算 20260806/07,`skeleton_version` 由 `engine_default` → **`K8-V0.5`**、`missing:skeleton_pack` 消失 = 阈值确实从包里读。⚠ **版本号三处同升**(`app.py::VERSION` + `project.yml` + `pbxproj`,守门单测锁恒等)—— **客户端本批不构建不换包**,换包仍归批 5。现场打出三条与既有记载冲突的事实,已进 `CLAUDE.md`:运维事实文件已拆(`nk` 归 `NB_info.md`)· **nk 上瞬态批算禁用 root `--scope`** · nk 是 4 vCPU/3.8G 非 2 vCPU/1.6G。
- 2026-08-08 · 🧱 **V2.2.0 批 1 本地施工完工(块① 引擎注册表 + 块② 行情状态层;builder-pro ×2,⛔ 未部署)**。① `selection_packs` 单包制 → **四线注册表**(`line_code`/`status` 两列 + 唯一现役约束「全表唯一」改「**每线唯一**」)· 读侧四入口 · **`ENGINE_API_VERSION` 1→2**(两个回滚锚如设计作废,回滚绳改「代码 commit + DB 还原」)· 四个包文件(`K8-V0.5`/`C1`/`Z1`/`Y1`,阈值全带 `provenance`)· 新原语 `industry_blacklist`(**实测 `stock_basic.industry` 白酒取值 = 「白酒」19 只**)· `retire_legacy_packs.py`;**三张老账结案 P2-47 / P4-48 / P1-46** + P4-54 两处文案。② `market_regime_daily` 新表 + `scan/regime.py` 三态判定唯一实现 + 五维各带双位与独立保险丝 + **两道锁落成机器判据**(前视锁逐位断言 / 权限锁 AST)+ `SEG_SCAN` 挂链 + `scan_layer.py regime` + `GET /market-regime`(一律 200、零新增 reason)。**全量套件 3166 passed / 3 skipped / 0 failed**(基线 3063/2 failed),表 46→47、`selection_packs` 列 10→12,A8 探针可写命中集为空。**⛔ 未部署、零接触生产**;🔴 **⑦ 批 1 部署判据自相矛盾已实测证实、待用户裁定**(bump 后 `get_active_pack()` 对生产 LEGACY 行返 `None` → 与「当晚行为逐位相同」互斥)。完工验收全文 → `archive/V2.2.0_批1完工记录_20260808.md`。
- 2026-08-09 · 🗂 **策略研究档案整体迁出本仓(用户自己执行,planner 只对账 + 保树一致;单独一笔 commit)**。删 **62 件**:顶层 `research/` 全目录(K2–K7 的 41 个 runner + 14 份结果报告 + 盲选训练舱 `drill.py` + 考卷引擎 `exam.py`/`exam_html.py`)· `STRATEGY_LAB.md` · `K4_STRATEGY.md` · archive 五份研究档案。新家 `~/Lino/whynotme/`(档案根 `Archive/Neckline量化研究档案_K2-K7/{research,docs}/`,README 记了重建成本)。⛔ **回测核心留仓**:`neckline/{backtest,research,eval,strategy}/`,判分唯一源 `neckline/eval/exit_sim.py` 一行未动。同批代码面适配三处(**与删除必须同一笔,否则树是坏的**):`test_industry_strength.py` 由 `import research.k4p_h6_theme` 改**本文件内嵌冻结源副本**(plan §五 v1.4-②-A「逐位对拍」硬要求逐字仍成立,**冻结副本比活 import 更强** —— 锚死迁出当时的实现,研究件日后被改也不会让基准漂)· `test_eval_exit_sim.py` 那条「research 三处 import 同一函数对象」加 `skipif`(**⛔ 不删**,删了等于承诺研究件永不回来)· `smoke_eval_exit_sim.py` docstring 写明「默认跳过是预期不是故障」。**新增 §1.6 口径声明**(全文 `research/…` 一律指新家)+ 关键处逐处改写(**`research/panel.py` → `neckline/research/panel.py`,它其实没搬,4 处**)+ 证据链指针改指档案 + K2/K3 存根改指档案。代码面剩 4 处存活引用**只登记不修** → §七 **P4-54**(其中最要紧的是 `test_v13_exit_6y_baseline.py`:**六年真回测基线 N=1288 / −20.53% 在本仓已永久无法复跑**,skip 理由文案现在在撒谎,批 1 顺手改)。验证:全量 `pytest tests/ -q` **3063 passed / 3 skipped / 2 failed**(2 failed = §七 P1-46 周末日期炸弹,今天周日,与清理无关)。
- 2026-08-09 · ✅ **V2.2.0 五条遗留点用户全部拍板 + 一项新裁定落图(planner,仍 ⛔ 未开工)**。**#6 关口二分照 planner 拆法**(机械关硬否决 / 证据关只降级 / LLM「退出」收窄为退出正式候选仍列名)· **#7 反馈环划线**(正确率只推状态、永不改规则,前视锁 + 权限锁照写)· 🔴 **#8 熔断整体删除**(用户原话「**我不需要你替我做决定;这个程序永远是提醒 —— 连续三笔止损真的发生了,那也是提醒**」)—— 锁定态 / 次日只减不加 / 强制复盘解锁**三件机制全删**,「连续 3 笔止损」降为**纯提醒**(一推送 + 一事件,零状态零锁零行为改变),单日 −4000 档一并删;⑤ 因此扩为「章程修订 + 熔断退役」,**⑤-B 十处逐项清单**(§2.1 第 7 条加删除线留痕 / `circuit.py` 只留无状态纯函数 / `circuit_breaker` 停写留档不 DROP〔P4-31 七张→**八张**〕/ 删两端点 / **`PositionsOut.circuit` 本版不删键、恒发空态**〔零删键铁律,真删排 v2.3〕/ `circuit` kind 保留改纯告知文案〔**刻意不新增 kind** = 不触发用户拍板纪律〕/ 盘前 `circuit_locked` 与 9:26 必发豁免全删 / 接线改「只推提醒不建行」/ 周复盘熔断审计删 —— ⚠ **§2.1 第 4 条强制复盘不是熔断,⛔ 别连坐删**);风险登记三条入册,其中**生产历史触发情况必须只读 SQL 查证后如实填**(planner 只查得到本地开发库 0 行,**而那不是生产库,证明不了任何事**,承 P0-39)· **#9 混引擎搁置 → 单篮子单引擎**(引擎标篮子、成员继承,归因由成员级改**篮子级**;留门一句「要开就升 `K8-V` 骨架版本」,⛔ 不预建列 / 不预留分支)· **#10 P3-49 按 K8 走「信实盘别信回测」**(判据来源写死 = 选股时钟实盘数据,⛔ 不立回测战役;雷区对照只作知情登记、不作否决依据)。planner 上一轮裁的「熔断锚点」问题**随 #8 作废**;§2.9-A 表拆行、§2.9-C 五条标已拍板;§八 第 19 项补熔断退役的后果告知。
- 2026-08-09 · 📐 **V2.2.0 立项 = K8 策略架构落地(planner,⛔ 只写 Plan、未开工、零接触生产)**。`whynotme/K8.md` 入仓 `K8_STRATEGY_ARCH.md`(**原文一字未改** + 命名防撞头:系统 `v` / 纪律章程 / K8 `V`·`C`·`Z`·`Y` **三条版本线**互不相干,⛔ `V0.5` 禁简写)。**用户五项裁定**:全按 K8 无讨论空间 · **V2.1 批 2(`K7-pack-v2` 激活)作废、批 3 并入 V2.2** · V2.3 仍是前端视觉 · C1/Z1/Y1 定量阈值由工程侧翻译首版并**逐条登记来源、⛔ 不冒充审计结论** · **章程按 K8 持仓原则修订(独立高危批,风险登记全文不得删)**。§五 换成 **V2.2.0 七块 / 五批**(① 引擎注册表〔`selection_packs` 单包制 → 骨架 V + C/Z/Y 四线,每线唯一现役,`ENGINE_API_VERSION` **1→2**〕· ② 行情状态层三态落表 · ③ 六道关口门槛制 + 落地起跳位置关 + 三引擎首版阈值 🔴 · ④ 双时钟 + 按引擎归因 + 四分类建议 · ⑤ 章程 `v2.2-k8` 🔴🔴 · ⑥ 契约与客户端 · ⑦ 分批部署);V2.1 施工图全文 → `archive/V2.1.0_施工图_20260809归档.md`。新增 **§2.9 K8 语义换血总表**(含 **§2.9-C 第〇原则在门槛制下的精确表述** = 六关拆「机械关硬否决 / 证据关只降级」)+ **§3.11 技术选型增补**。**三条 planner 定案写死**:「三引擎并跑 ⛔ 不增加任何 LLM 调用」(引擎是判据分支不是调用分支,增量 = 0 次)· 「行情状态第五维 T1/T2 正确率有前视锁 + 权限锁,断开 K8 文本自带的反馈环」· 「回滚绳改『代码 commit + DB 还原』,⛔ 全项目不许再写『回滚 = 激活旧包』」。§七 逐条处置:**结案 6**(P3-42/P3-47/P4-46/P2-47/P4-48/P1-46)· **主体作废 1**(P3-33)· **扩容 2**(P3-34/P1-8)· **部分落地 1**(P3-28)· **新挂 4**(**P3-49** K8 位置关落在 K3 已判死域的前向证伪义务〔🔴 最尖锐,⛔ 结案前不许称『有效』〕/ **P4-50** `landing_state_daily` 是 P0-23 第二个靶心 / **P3-51** 状态层第五维冷启动缺席 / **P3-53** K8 未走策略线立项、`STRATEGY_LAB.md` 仍停在 K7)· **明确不动 2**(P3-41/P3-44)。§八 新增第 19–21 项(**第 19 项 = 章程激活前用户必须知道自己在批准什么**,含 −5% 与时间退出两条防线退役的真金代价)。§四 换 V2.2 立项快照。
- 2026-08-08 · 🚀 **V2.1.0 批 1 上产 + macOS 换包(@builder-pro,生产 `nk`,周六休市窗口 18:04–18:16 CST)**。⑧ 契约对拍收口先行(三张表 → `archive/V2.1_契约对拍_20260808.md`;`test_contract_crosscheck.py` **10 → 18 例**:新端点形状 +2、④ 的 4 个新键两侧对拍 ×2、**「零新增 reason」显式断言**〔拿 V2.0.0 收官快照 `352f235` 当分界线 —— 既有两条闭包测试守不住「悄悄多一个 reason 但客户端同批加了 case」这一半〕、`FROZEN_SNAPSHOT_DTOS` **3 → 6**;🔴 顺带把 CLAUDE.md「同前缀 B 类 DTO 会切错块」那条**人肉纪律做成机器判据** —— `struct BasketEvidence` 就排在 `struct Basket` 前面,老 `split` 写法**绿着守错类型**;🔧 另修好 `smoke_api.sh` **自 V2-⑬ 起在第 25 步硬中断、其后断言从未跑过**的问题,按既有「步骤号留空」体例收口失效步骤并加 34/35/36,现在跑到底;对拍当场抓到两处「注释说 T3、代码判 T2」的口径已假注释)。**部署**:双备份 `*-preV21b1-20260808-180438`(`.bak` integrity ok)→ `sync_code.sh` dry-run 核删除面(6 条是 ① 预期退役件;🔎 **第 7 条不在预期内当场停手查清** = `predel0807row-20260807-094127'` **0 字节引号事故残渣**,同操作真备份完好,随 `--delete` 清掉)→ 实传 **68 文件 sha256 双向逐位相同** + 属主纪律核过 → 一次性脚本演练 + `--confirm` **两次均 0 改动**(⚠ 生产 `llm_task_routes` 本来就是 `{}`,`inquiry` 键**从未写进过生产库**;真正兜底的读侧过滤已随本次上产)→ `init_schema` **47→47 零新表零 WARNING、库 sha256 未变**(`inquiry_log` 4 行原样留档)→ 重启(`NRestarts=0`、journal 零 ERROR、内存 175.9→69.4 MB)。**公网活体**:`/health` = **`v2.1.0`**、`/review/{overview,handoff}` **200/200**、问询台三端点 **404/404/404**、**全部 16 份历史报告逐份回放全 200**(含 12 份 v1.x 快照)、TLS 严格校验 0 + HTTP/2;④ 打分键在老快照上如实 `null`+`[]`(**键在值空,⛔ 不冒充 0 分**),真值等周一链。**🟡 过渡态实证**:生产仍 `K7-pack-v1`,只读调一次即打出 `tier3_min 已退役…本次忽略` 的 WARNING(⛔ 不静默,② 的设计),周一 journal 会再出现一次,**直到批 2**。**周一链静态就绪**(⛔ 未跑链):两 timer `waiting` + NEXT = 08-10 16:05/16:35,target `StopWhenUnneeded=yes` 且 `inactive`,三段 oneshot `RemainAfterExit=no`,`NeedDaemonReload=no`,8 个 unit 文件 **7 个与仓库逐位相同**,第 8 个 `neckline.service` 查清为**纯注释差异**(`ExecStart` 逐字节相同、实际监听 `0.0.0.0:8002`)→ **刻意不动**,登记为良性漂移。**macOS 换包 2.1.0**(旧包 ditto 备份 → Release build → **ditto 装**⛔ 非 `cp -R` → 版本/签名/主可执行 sha256/文件数四项核过 → 启动存活,服务端侧零 4xx/5xx)。📊 **P3-44 真实降幅已用生产库测出并填回**:全历史 `tier=3` **0 行**、`depth='brief'` **0 行**、每晚篮子数 4/7/7/4 **本就 ≤7** ⇒ **T3 删除对当前账单降幅 = 0**(省的是上界),⛔ 别写"显著下降";顺带诚实补注 ——「历史 T3 回放」在这台生产库上**无对象可验**,读侧宽容是未来保护而非已兑现事实。**批 2(③ 包激活)与批 3(⑥ 周度 unit)未做,等用户指令;⛔ 全程零接触 `packs/`/`selection_packs`/`activate_pack.py`;老机 hz 零接触。** 详见 §五⑧⑨ 完工与部署记录。
- 2026-08-08 · 🧭 **V2.1-⑦ 客户端三板块 IA 重排 + 复盘板块 + 打分卡上卡面,本地完工(@builder-pro,⛔ 未部署 / 未换包;服务端只动 2 行 = VERSION + 推送文案)**。`AppTab` 改 `baskets,positions,review,settings`(**枚举顺序即 TabBar 顺序**,四个 `rawValue` 一字未改 —— 那是 QA 钩子与截图脚本的契约,单测反向锁死);iOS 四项 = 选股/持仓/**复盘(新 tab)**/设置,macOS 侧栏交易组 = 选股+持仓、复盘组 = 复盘、**设置移出交易组沉到最底部**并加分隔线;文案清单八条逐条照做(含 `notify.py` 推送「今日篮子」→「今日选股」,只改文案不动 kind)。**新文件 `ReviewView.swift`(806 行,双端共用)**:每日(④ 昨日复盘整段迁入,`BasketReviewRow` **一字未改**;选股板块只留一行入口且**三态缩写各自不同**)/ 累计(`/review/overview` 五段 + **翻周**)/ 对账(macOS 整块复用 `ReviewWorkbenchView`,去掉自带 ScrollView 交给外层容器;iOS 只读 + 一句「上传请在 macOS 端做」)+ 移交件出口(双端 `ShareLink`,macOS 另给 `NSSavePanel` 存 .md)。**移交约束四条全部落实**:🔴 `ForEach([1,2,3])` → `BasketDaily.displayTiers` = **现役两档 ∪ 快照实际档位**(与服务端 `render` 同构;**伪造历史 T3 快照实拍验证**:T1 出篮、T2 出「今日 T2 为空」、T3 分组照出并标「V2.1 起已取消(历史回放)」;并集使「幽灵空 T3」与「历史 T3 消失」两个错都不可能)· ④ 读法收口成 `Basket.scoreDisplayPercent`/`.scoreDisplayContributions`(`scorePercent ?? tierHistory?.scorePercent`,**分数从哪条路来拆解就从哪条路取**),展示 `:.1f`、**契约 4 位精度不预舍入**,`*` 角标挂数值上 + 行尾统一说明,「占比」直接展示契约字段 `weight`(⛔ 客户端不求和、不归一化 = 不造第二份换算),**缺席写「本报告版本无打分(⛔ 不是 0 分)」** · ⑤ 三个新 DTO 整族**手写 `init(from:)` + `decodeIfPresent`**、`?from=` 别名原样发、`{}` 也解得出且默认 `available=false` · **画像「没看」与对账「没有」分开渲染**(实拍同屏对照)。**plan 之外四改一未做(如实登记)**:① 选股页页面标题改为引用 `AppTab.baskets.title`(否则 tab 写「选股」页头写「今日篮子」;⚠ 段名一字未动)· ② 🔴 **修掉一个一直存在的哑 bug**:`ProfileRow.bucket` 只读 `bucket` 而服务端那格叫 `value` → **每行分组名恒为空**(显示成「role · 」)且看不出是 bug,改 `bucket ?? value` 兜底 · ③ 累计页加**翻周**(周度产物是周六离线落的,不翻周则周一到周五永远只看得到「尚未生成」)· ④ 配套 QA 钩子 `NECKLINE_INITIAL_REVIEW_WEEK`(本环境点不动模拟器,否则拍不到有产物的那一周)· ⑤ **未做**:契约对拍三项 + `smoke_api.sh` 加项归 ⑧、macOS 换包归 ⑨。**一处口径变化**:macOS 工作台「画像」「评价校准报告」两节移出(它们正是累计页前身),`AppModel` 三属性 + `loadWorkbenchExtras()` 随之删,**`APIClient` 三方法与两个 DTO 及解码测试全留**(端点仍在、防线不变窄);代价 = 累计页读**离线产物**而非 `/eval/weekly` 在线现算,⑥(批 3)前会如实说「尚未生成」(plan 明写的合法中间态,**在两处各画一遍才是真错**)。**版本号四处同批到 2.1.0**(三方守门绿,⛔ 未部署)。**测试**:Python **3056/2/2 与开工基线逐位相同**(零新增 Python 测试,⑦ 只动客户端 + 服务端 2 行);iOS test **192 executed/11 skipped/0 failures**(基线 173/11,**+19**);**双端 × Debug/Release 四条 build 全 SUCCEEDED**。**截图**(隔离夹具后端 + 隔离 data 根,真实 db md5 未变、真实 `data/reports/calibration/` 至今不存在;临时 iPad 用完已 `simctl delete`):三 tab + 设置沉底、选股页打分卡全貌含 `*` 与权重、复盘三页、累计页底部观察项 + 移交件出口 + 样本量 chips、**历史 T3 回放三组齐出**。**两处如实认的缺口**:篮子卡页内打分卡需滚动拍不到(该 MCP 模拟器工具本次再次实测仍报 `Xcode is installed but not selected`,靠列表页同组件 + 单测等价兜底);macOS 未截图 —— 全屏 `screencapture` 拍到的是**用户桌面私有内容**(已当场删除、不再尝试),按 CLAUDE.md 体例以「iOS 截图 + 双端 build 绿」为等价证据。详见 §五⑦完工记录。
- 2026-08-08 · 🧾 **V2.1-⑤ 复盘板块服务端本地完工(@builder-pro,⛔ 未部署 / 零新表 / 零写库 / 零新增 reason / 零客户端改动)**。新增 `neckline/review/handoff.py`(产物枚举 / **唯一读实现** / 五节移交件渲染 / `HANDOFF_OBSERVATIONS` 四条)+ 两条只读端点 `GET /review/overview`(`week`/`asOf`,五段)与 `GET /review/handoff`(`from`/`to`/`asOf`,⚠ `from` 是关键字 → `Query(alias="from")`)+ 三个 Out 模型。**⛔ 零在线补算双向证明**:静态 AST **按函数**扫两端点 + 四个段装配函数零 `build_report`(⚠ **不能整文件 grep** —— 同文件的 `get_eval_weekly` 是合法调用方,整文件扫必永远误报,⑰ 刚踩过"对自己报警的闸门等于没有闸门");**运行期**把 `build_report` 换成会抛的桩,两端点仍 200,并**反向断言** `/eval/weekly` 在同一个桩下变 `available=false`(证明桩生效、上面不是假绿)。**产物三态**(plan 只要求文案分开,这里落成机器判据):`ok`/`not_generated`/`corrupt` —— 前两者会自愈、`corrupt` 不会,混成一句就是叫人等一份永远好不了的产物(承 V2 B1 同一裁定)。**🔴 两处刻意判得不一样、⛔ 别"统一"**:画像段缺席 = 系统自己那步没跑 = 「没看」`available=false`(实现**直接调 `/profile/*` 两个端点函数**,同码不重写,有测试逐字段对拍);对账段缺席 = 输入只能由用户给、系统查过表确实没有 = 「没有」`available=true`+`found=false`+可直接渲染的 note。**包成绩单 = 产物里的 `strata` 本身**(原样透传),⛔ 未另建第二份聚合。观察项 **P3-34 只放一条**(§七 里它本就是一条含 (a)(b));P3-33 的 `tier3_min` 一项如实标作废;守门断言每个 id 能在 **§七 那一段**grep 到字面 `[P3-xx]`。**新增测试隔离注入点** `app._DATA_DIR_OVERRIDE` + conftest 同步(不加就会读真实项目 `data/reports/`)。**测试** 3003 → **3056 passed**(+53),2 failed/2 skipped 同基线;随机序与固定序两次全量同数字。**冒烟**(隔离库含真实迁移数据副本 + 隔离 data 根,真实 db 与真实 `data/reports/` 全程不碰):离线落 `calibration_20260720_20260724.{json,md}` 后两端点各出一份完整响应人工核对 —— overview 五段全 available 且各自说得出话、handoff `sampleN={1,2,1,2,2}` / markdown 4443 字符 / 五节齐全 / 校准 `.md` 原文原样嵌入 / `low` 行逐行带「样本不足,不给结论」;反向两个查不到的窗口**均 HTTP 200**(零 404)。详见 §五⑤完工记录。
- 2026-08-08 · 💯 **V2.1-④ 百分制打分卡本地完工(@builder-pro,⛔ 未部署 / 零包改动 / 零 DB 变更 / 零客户端改动)**。新增 `neckline/report/score_display.py`(纯函数、零 I/O、零 DB、**零重算** —— 数据全取已冻结的 `tier_history.mech_breakdown`)+ 三处接线(报告快照 `BasketView.score` / live `_shape_basket` → `TierOut` / `render.py` ③ 节每篮一行)。**守门三条全绿**:AST 反向 import(判定五包 `selection`/`sentinel`/`strategy`/`eval`/`review` 全仓零 import 本模块 —— 方向性规则,要拿分数去判定第一步必然是 import)· `_TIER_SCORE_INPUTS` 逐位不变 · 本模块零 import `neckline.selection`;**自加两条**(中文标签与引擎五维同集、`_DIM_NEUTRAL_FILL_FLAGS` 与引擎 `_DIM_MISSING_FLAGS` 逐位相等)。**一处"受监督的重复"如实登记**:守门 ③ 禁止 import 判定层,故中性填充 flag 映射不得不留一份副本,已用逐位相等的守门把漂移变成当场报红(⚠ 红了是来对齐引擎,⛔ 不是删断言)。**🟠 plan 一处数值疏漏已按实测订正**:「合计 ≈ 总分,容差 0.15」在分项 1 位小数下最坏差 0.30、实测 20000 组有 **1.61% 超 0.15**(会随机翻红的守门比没有更糟);**修法不是抬容差而是契约值别丢精度** —— `contribPercent` 保留 4 位、展示层各自 `:.1f`(精度住契约、位数住展示),plan 的 0.15 因此原样成立并加了 200 组随机 property 测试。**契约新增 4 个只读键 + 1 个元素 DTO,⛔ 零删键**;🔴 **live 与快照两条路刻意只填各自那一处**(live 的分数住 `tierHistory`,两处都填 = 一份响应里两个必须永远一致的副本),⑦ 读法 `basket.scorePercent ?? basket.tierHistory?.scorePercent` 已写进 docstring 与专门测试。**测试** 基线 2967 → **3003 passed**(+36,零回归)。**冒烟**(真实引擎产出,隔离临时库 + 真实 parquet 只读,真实 db md5 `65b22606…` 未变):`机械分 61.7 / 100(可交易性 20.0 · 龙头清晰度 12.5* · 板块强度 12.5* · 驱动新鲜度 10.0* · 卡密度 6.7)`,自洽 61.6667 vs 61.7。🔴 **顺带打出一件产品级事实**:那两篮真实数据 `neutral_filled_weight=0.7` —— **七成权重来自中性填充**,P3-33 说的「因为不知道所以进 T1」在真实数据上不是假想,打分卡把它摆到了卡面(`*` 角标 + 占比),正是 P3-33 等的证据之一;⛔ 本块不因此设任何闸(设闸走换包门禁)。详见 §五④完工记录。
- 2026-08-08 · 🗑 **V2.1-① 问询台整链退役本地完工(@builder,⛔ 未部署 / 零一次性脚本生产执行)**。服务端 3 端点 + 5 pydantic 模型 + `TASK_INQUIRY`(常量/`ALL_TASKS`/`DEFAULT_SEARCH_TASKS`/`__all__` 四处)+ `api/stores.py` 三函数 + `report/discipline_checks.py`(唯一消费方已亡)一并物理删除;`inquiry_log` 表停写留档(db.py 表头登记,同 `watchlist` 口径);`inquiry_pool` **零接触**(反向测试锁死)。`settings_store.get_llm_routes()` 加读侧未知任务名过滤 + WARNING;新增一次性脚本 `scripts/oneoff/strip_retired_llm_routes.py`(默认演练/`--confirm`双备份/幂等,单测 10 例 + 本地库 CLI 冒烟,**未碰生产库**)。客户端 `AppTab.inquiry` case、11 个 `inquiry*` 属性(plan 写 12,清点订正)、4 方法、`VerdictBadge`、五个 Inquiry 系类型(`InquiryReply` 系 plan 笔误,实名 `InquiryResult`)、`APIClient` 三方法全删;iOS TabBar 截图确认三项(今日篮子/持仓/设置),零 IA 重排(留给 ⑦)。**如实登记发现的三处 plan 出入**:属性计数 12→11、类型名 `InquiryReply`→`InquiryResult`、🔴 `selection/primitives.py` 经核实**并不** import `base_universe_expr`(独立手写谓词,值靠 docstring 人工同步,既有风险非本块引入,守门测试已按实况只锁 `strategy/momentum.py`)。**发现并修复 plan 清单外三处真实测试耦合**(否则 pytest 会红):`test_llm.py`(`"inquiry"` 字面量当示例任务名 × 3 处)、`test_scan_layer_guardrails.py`/`test_industry_strength_store.py`(各自独立的 `_ONLINE_FILES` grep 清单 + 后者两例直调 `run_deterministic_checks`)、`test_llm_router_budget.py`(`len(ALL_TASKS)==9` 改 8)。**测试**(`git stash` 前后对拍,基线 = ② 已提交的 `85f4e1e`):**3041 passed → 2967 passed**(2 failed / 2 skipped 两侧不变,失败集逐字节相同,系 `test_api_positions`/`test_fix_position_buy_dates` 两条与今日〔周六〕日期相关的既存红,核实与 ①② 均无关)。双端 `xcodebuild build` **BUILD SUCCEEDED**(`xcodegen generate` 同步 pbxproj,diff 4 行);iOS test **173/11 skipped/0 failures**(对照 ② 报告的 pristine 187/12,187−173=14 与本块删除的 Swift 测试方法数逐个对上)。详见 §五①完工记录。
- 2026-08-08 · 🗑 **V2.1-② T3 全链退役本地完工(@builder-pro,⛔ 未部署 / 零接触包文件 / 零客户端改动)**。引擎两档化:`TIER_CAPACITY={1:2,2:5}` · `TIERS=(1,2)` · **删 `TIER3_MIN_SCORE`** · `_eligible_tier` 无兜底档(够不到 `tier2_min` 即 `below_quality_line`,**码字符串一字未改**保住 ⑨ 归因)· `DEGRADE_ORDER` 只剩 `t2_review_detail`(**删 `DROP_T3_BRIEF`**)· `depth_for_tier` 恒 `full`(`DEPTH_BRIEF` 常量**保留**供读回历史行)。**硬约束「历史 T3 回放不许消失」三处落点**:`basket_daily.by_tier()` 按数据实际档位构造、`render._TIER_TITLE` 改函数式兜底、③ 节迭代 `现役档 ∪ 快照实际档`;`list_baskets` 的 `tier` 白名单**保留 3**(读侧宽容,删了 = 历史按 T3 查静默退化成"全部")。**plan 两处疏漏已补并登记**:① `eval/calibration.py` 的 Tier 行写死 `(1,2,3)`(冒烟当场打出幽灵「T3 —(n=0)」= 把系统缺席讲成实质性结论),改成按实际档位渲染、双向守门 —— 这份报告正是 ⑤ 复盘板块要端给用户的东西;② `aggregate.py` 的「17 篮」注释成假话(常量**刻意不动**,检索段按种子计费)。**回滚锚保住**:`pack` 对 `tier3_min` **受理不报未知键**、不进单调性,守门直吃真 `packs/K7-pack.json` 断言"校验通过 + 原样过闸 + 包对象仍带该键";引擎侧忽略并**打 WARNING(⛔ 不静默)**;`ENGINE_API_VERSION` **仍为 1**(②-4 三条理由,两个回滚锚仍判兼容)。反向 hasattr 守门两条 + 白名单不给已删常量留登记。**测试(隔离 worktree 对拍,排除同批 ① 在制品)**:pristine `5635c77` **3028 passed** → 本块 **3041 passed**,+13 例零回归,两侧失败集相同(2 条日期依赖既存红,08-08 周六)。**冒烟**(隔离库 + LLM 桩,真实 db md5 未变):`{'T1':1,'T2':1,'T3':2}` 溢出 0 → **`{'T1':1,'T2':1}` 溢出 2**(2 篮如实进 ③b `below_quality_line`)。pristine 客户端双端 `BUILD SUCCEEDED` + iOS test **187/12 skipped/0 failures**。🔴 **移交 ⑦**:`BasketDailyView.swift` 的 `ForEach([1,2,3])` ⛔ 不许简单改 `[1,2]`(会在展示层拆掉刚立起来的读侧宽容),改法见 ② 完工记录末条。
- 2026-08-07 · 📐 **V2.1.0 立项:施工图转写进 §五(@planner,⛔ 未开工,等用户 Build 指令)**。六项用户裁定定格(问询台整删 / 三板块 IA / 复盘人工闭环无自动反馈 / T3 彻底删 / 百分制甲案纯展示 / 前端视觉让位 v2.2);分九块 ①问询台整链退役 ②T3 全链退役 ③`K7-pack-v2` 发版激活 ④百分制展示层 ⑤复盘板块服务端 ⑥周度 unit 定案(**P3-42 结案**)⑦客户端 IA 重排 + 复盘板块 ⑧契约对拍守门收口 ⑨分批部署。**本版是对活着的生产系统做在线升级**,四条铁律入 §五〇b(本地施工优先 / 分批部署 / 常驻 API 只在收盘后重启 / **零删键**)。planner 核出四处与前瞻规划的出入并已在图中纠正:`K4-pack-v1` 无 `quality_lines` 故**不必重发版**、`ENGINE_API_VERSION` **不 bump**(理由三条)、报告 ④ 节**服务端零改动**只搬客户端展示、T3 删除**省不到联网检索那笔大头**。§七 吸收 P3-42、部分落地 P3-44、部分作废 P3-33、新挂 P4-46/P3-47;前瞻规划稿归档 `archive/V2.1前瞻规划_20260807立项归档.md`(防双权威)。
- 2026-08-07 · 🔧 **晚间链 timer/target 接线修复:`neckline-evening.target` 加 `StopWhenUnneeded=yes`,排程从"只跑首晚"恢复为天天跑(@builder-pro,已部署 nk,案底 §七 P0-45)**。08-06 整晚未触发的根因是 **timer 的重新武装边**(target 永不落下 → timer 卡在 `SubState=running`、NEXT 永不重算),**不是**"已 active 的 target 吞掉触发"(08-05 那晚整链跑完即为反证)。修法一行、零代码零契约,三段严格串行未动;`/bin/true` drop-in 空跑演练拿到微秒级五连证据(三段串行 → Reached target → 5.6 ms 后自动 Stopped),演练后 ExecStart 逐字复原、五个 unit 与 `/opt/neckline/deploy/` md5 全 MATCH、`NEXT=2026-08-07 16:35:00`、`neckline.service` `NRestarts=0` 未受扰。**🟠 施加过程中踩了一次次生事故并如实登记**:`daemon-reload` 异步落下 target → timer 以过期基点(08-05 16:35)算出已过去的 08-06 槽 → **当场空跑一遍全链并真推送了一条 APNs**(盘中、无当日 EOD,0 篮 0 卡,空 `20260807` 报告今晚 16:35 会被 `ON CONFLICT DO UPDATE` 覆盖 = 自愈;唯一不可逆的是那条推送)。正确的施加顺序(先 stop timer)已固化进 target 文件头。**08-06 补跑按用户"异常停手回报"纪律暂停,待用户裁量。**
- 2026-08-05 · 🩹 **信息卡第三面 `card_corrupt`/`card_not_ready` 混淆修复(B1 同类裁定收口,@builder,commit `06e44e2`,已部署 nk)**。`info_card.py::build_basket_context()` 的 `(row or {}).get("card") or {}` 曾把「压根没有卡行」与「有行但读不出(冻结件损坏)」摊成同一句 `card_not_ready`,照抄 `basket_daily.py::load_today_baskets()`(报告③节)已有的 `any_corrupt` 判读体例修复,复用 `basket_store.load_basket_card` 唯一检测点(不重复打 ERROR),新增 `BASKET_CARD_CORRUPT_REASON`。测试两态(`test_api_info_card.py` 新增 1 例,既有 `card_not_ready` 例保持绿);Python 全量 3030 passed / 2 skipped。部署:sha256 对拍逐字节相同,`systemctl restart neckline.service`(晚间收盘后窗口,`NRestarts=0`、`ActiveEnterTimestamp` 已刷新、journal 干净、公网 `/health` 200 复核)。详见 §九下一行(同批完成,合并部署)。
- 2026-08-05 · 📋 **契约强类型字段全字段值类型核对(§七第5条销案)+ 顺带查出并修复 `sentiment` 解码坑(@builder,commit `484fd1e`)**。兑现 `0eddac7` 登记的「下次三方对拍应逐个字段核对值类型」承诺,全文见新建 `archive/V2_契约类型核对_20260805.md`(方法论 + `ReportResponse`/`BasketDaily`/`BasketCardOut` 全家族逐字段表,约 140 字段)。核对中拿真实生产载荷复测,新查出第二处与 `engineApiVersion` 同一晚的解码坑(这次是"可选性没对齐"而非"数字vs字符串"):`ReportResponse.sentiment` 解码裸 `decodeIfPresent` 未接 `try?`,而 `app.py::_empty_report()`(`GET /report/latest` 当日无报告 / `GET /report?date=` 查无该日报告两条主路径共用)实测真发 `"sentiment": {}`(非 `null`)——`SentimentSnapshot` 九个必填字段解空对象必抛 `keyNotFound`,拖炸整份报告,与 `engineApiVersion` 同一种失败模式,只是触发条件从"有篮子"换成"没有报告"(全新安装 / 非交易日盘前首当其冲)。修法:加一个 `try?`,与同函数里 `intel`/`sectorMoneyflow`/`dataFreshness` 同模板。顺带订正 `DataFreshness` 一处误导性旧注释(其合成 `Codable` 的安全性其实靠一条隐性写侧不变量,不是"每次响应重新构造")。**测试**:Python 全量 3030 passed / 2 skipped;iOS Simulator `xcodebuild test` **187 executed / 12 skipped / 0 failures**(基线 186 + 本次 1 例);macOS + iOS 双端 `xcodebuild build` **BUILD SUCCEEDED**;macOS 已 `ditto` 重装(旧包备份 `~/Lino/app_backups/Neckline-2.0.0-20260805-211109.app`,新装可执行 sha256 与构建产物逐位相同 `f75afeb1…`,`codesign --verify --deep --strict` valid,进程存活、统一日志无 decode/crash fault;二进制 `nk.linotsai.top` 4 命中 / `ln.linotsai.top` 0 命中,后端指向闸门仍在)。**⚠ iPhone 仍需用户自行重装**(同代码同批修复,§八 第 12 项既有待办已涵盖)。

- 2026-08-05 · 🩹 **客户端解码类型错定向紧急修复 —— 找到并修复了昨晚那次重装未能证实的真根因(@builder,commit `0eddac7`,纯本地代码/测试/文档,零服务器接触)**。**根因**:`client/Models.swift::BasketFingerprint.engineApiVersion` 误按 `String` 解码,服务端契约(`neckline/db.py` 三处 `engine_api_version INTEGER`)恒发 int(实测生产值 `1`);`typeMismatch` 顺着 `BasketCard.init(from:)` 的 `fingerprint` 解码一路抛穿,拖炸**整份** `ReportResponse` —— 凡带篮子的报告在 Mac/iPhone 上整份解不出、App 退回空态(与上一条 08-05 macOS 重装记录里"未能证实根因"的疑问对上:那次排查方向〔源码是否随 ⑰ 割接同步〕本身没错但不是这次的病灶,真根因是这个类型错,与源码同步状态无关)。**修法**:改 `Int?`,按项目 B 类冻结快照 DTO 体例手写容错双态解码(数字优先、字符串数字也认,都没有才 `nil`,防历史卡万一是字符串形态时炸解码);`NKJSON` 的 Bool/Double 顺序坑不受影响(未碰该类型)。**验证**:独立 `decodetest` 解码测试台(`main.swift` + 抽取的 `ReportResponse.swift` + 真实 `Models.swift` 编译)对生产 200 响应(7 篮真实载荷)从 `DECODE FAILED: typeMismatch ...fingerprint.engineApiVersion` 转为 **`DECODE OK`**,逐篮核对 `engineApiVersion` 均解出真 `Int`(不是被容错兜成 nil);排查过程中**未发现同批次其余字段类型错**,本次只有这一个字段。**回归防线**:`NecklineTests/DTODecodeTests.swift::testDecodeRealProductionReportFixtureFullShapeAndTypes` 把这份真实生产载荷(单行 minified,自家生产数据,未脱敏)整份固化进测试,喂真实 `APIClient` 解码链路(不是手写小样例),防同类"字段名对、类型没对"的坑再度整份报告拖炸;`archive/V2_契约三方对拍_20260803.md` §七补第 5 条,登记该表 §4.2/§4.3 当时只核对了字段名、没核对值类型的缺口。**测试**:iOS Simulator `xcodebuild test` **186 executed(基线 185 + 本次新增 1)/ 12 skipped(既有,需真后端)/ 0 failures**(`git stash` 交叉验证过基线数字无回归)。**macOS 重装**:旧包 `ditto` 备份 → `~/Lino/app_backups/Neckline-2.0.0-20260805-194456.app`(与装机前可执行 sha256 逐位相同 `1dbdeaf2…`);`xcodebuild -configuration Release -destination 'platform=macOS' build` **BUILD SUCCEEDED**(`CFBundleShortVersionString=2.0.0` 未变,universal x86_64+arm64);**`ditto`**(⛔ 未用 `cp -R`)装 `/Applications/Neckline.app`,产物与安装态可执行 sha256 逐位一致 `ae1cb024…`;`codesign --verify --deep --strict` valid + designated requirement 核对(`identifier "top.linotsai.neckline"` + 开发者证书链完整);二进制字符串核对 `nk.linotsai.top` 4 处命中、`ln.linotsai.top` 0 命中(V2-⑰ 后端指向闸门仍在)。旧进程(孤儿,`lsof` 核实其 `txt` inode 已被 `ditto` 替换)经 `osascript` 妥善退出,新进程 `open` 拉起后 inode 核对一致、持续运行无崩溃、无崩溃报告、统一日志无 decode/crash 相关 fault。**⚠ iPhone 需用户自行重装**:同代码同 bug(客户端源码只有一份,`client/` 未分平台维护),本次只重装了 Mac,iPhone 上的旧包仍带着这个类型错,用户下次用 Xcode 装真机时会自动带上本次修复(§八 第 12 项既有的"iOS 装机"待办本身涵盖此修复,不新增条目)。**顺带机会性清理**:发现并提交了此前遗漏未提交的 P0-44 收尾文档(commit `b412616`,内容早于本次任务、与本次修复无关,委托权限内的仓库卫生动作)。

- 2026-08-05 · 📦 **macOS 客户端重新构建并装机(定向核实任务,@builder,纯本地操作,零服务器接触)**。起因:用户发现当日报告在 Mac 上整份解码失败(服务端 200 已验证,iPhone 用当日代码正常),疑现装包(08-04 21:56 ⑰ 割接时构建)晚于其后的 A/B 清欠批次改动、未随装。**验证与执行**:HEAD `0a3cdc8` 确含 `642b4a9` 等清欠提交;`cd client` 后 iOS Simulator `xcodebuild test` **185 passed + 12 skipped,0 failure**(与基线吻合);`xcodebuild -configuration Release -destination 'platform=macOS' build` **BUILD SUCCEEDED**(`CFBundleShortVersionString=2.0.0` 未变,universal x86_64+arm64);旧包先 **`ditto`** 备份 → `~/Lino/app_backups/Neckline-2.0.0-20260805-184502.app`(与原装机态可执行 sha256 逐位相同 `46b41284…`);**`ditto`**(⛔ 未用 `cp -R`)装 `/Applications/Neckline.app`,产物与安装态可执行 sha256 逐位一致 `1dbdeaf2…`;`codesign --verify --deep --strict` valid + 满足 Designated Requirement;旧进程经 `lsof` 核实其绑定的 inode 已被 `ditto` 替换(装机前的孤儿进程),妥善退出后重新启动,新进程 inode 核对一致、持续运行数分钟无崩溃、无崩溃报告、统一日志无 decode/crash 相关 fault。**⚠ 根因排查有偏离,如实记录**:`git diff 6803fa0..HEAD -- client/` **为空** —— 派单所指的 `card_corrupt`/`card_not_ready` 分流(`4c00976`/`642b4a9`)提交时间(08-04 17:43/18:28)均早于 ⑰ 割接提交 `6803fa0`(08-04 22:10),并非"晚于割接、未随装";`strings` 核对**旧备份二进制内已含 `cardCorrupt`/`cardNotReady` 符号**,与新装机版一致 —— 该具体改动装机前其实已在老包里,`client/` 自 ⑰ 割接以来源码零改动。本次重装本身完整正确(确保 Mac 与当前 HEAD 逐位对应,属合理保险动作),但**未能证实它是今日解码失败的真根因**;若重装后症状仍在,建议改查本机应用态(`NK_BASE_URL_OVERRIDE`/UserDefaults 缓存等)而非客户端源码版本差异。显示效果留用户自行确认。

- 2026-08-05 · 📦 **V2.0.0 大施工收官清理:§五 施工图全文归档 + §四 换上线常态快照 + 账本与坑清单瘦身**(@planner,**纯文档,零代码 / 零数据库 / 零服务器**;工作树此前干净,本次只动 `PROJECT_PLAN.md` / `CLAUDE.md` / `archive/`,仓库外另核 `~/hz_info.md`)。**归档**:§五 V2.0.0 全文(〇 裁定表 + 红线 + D1–D8 + 21 表汇总 + 分块序列 + 铁律 + ①→⑰ 全部块与完工记录 + 12 个追加子项 + 验收标准)照 v1.5 先例移 `archive/V2.0.0_施工图_20260805归档.md`(**含被替换的 §四 上线初期快照作附录**),原位留存根 + 交叉引用约定;PROJECT_PLAN **3780 → 826 行 / 805KB → 约 400KB**。**§四**:替换为上线常态快照(现役机 `nk` / `v2.0.0` / 章程 `v1.3.3` / 包 `K7-pack-v1` / 持仓 open 0 / 单 LLM GLM / 三件排程 / 三重回滚绳 / 用户唯一阻塞待办 = iOS 装机 / 观察项指针 §七)。**§七**:P0-39 · P0-40 当日结案已标;**P3-41 按用户裁定标「暂不做,留观察」**并写死重开判据;新挂 **P3-42**(⑫ 周度 unit 形态未定 = V2 唯一仍开放的规划项)与 **P4-43**(⑧ 盘中存拍待首验 + `MemoryHigh`/`MemoryMax` 待实测校准,⛔ 别拍脑袋调),两条都是从"快照制"的 §四 里救出来的账。**§三 3.10-B / §八 15**:补记 **2026-08-05 用户拍板「单 LLM(GLM)为常态」**,裁定 #2 双 Agent 降级为可选扩展路径(路由机制不变,接第二家即恢复,不改代码);**§八 12**:割接后老机已停、`ln` 已 NXDOMAIN → **iPhone 旧包已不可用**(原「上云期间旧 App 照常能用」一条划掉标作废),macOS 已 2.0.0。**CLAUDE.md**:V2 期新增的超长坑条按「每坑 ≤5 行」压回判据 + 指针(LLM 读超时分级 / ssh 长任务 / `available` 标志 / 冻结件三态 / Swift 手写 `init(from:)`),「V2 文档层级」改常态口径,LLM Provider 节补单 LLM 常态一条;老条(策略线 / 数据源 / LinoN)未动。**用户同日拍板**:V2 定型、**P3-41 暂不做**、**不再补 review**(§七 P2-9 / P2-10 保持原状只作留档)。**git tag `v2.0.0`(annotated)已打**。

- 2026-08-05 · 🩹 **LLM 两处生产配置缺口定向快修完工(@builder,盘中,用户拍板须赶在 16:35 首条正常生产链之前;生产库直改,无对应代码 commit)**。**背景**:上一批快修(commit `97e74e0`)在生产上真打出两处配置缺口 —— `llm_providers.base_url` 少 `/chat/completions` 尾巴(带真 key 打 20 次全 404,鉴权已过、纯路径不对)、`app_settings.llm_default_provider` 从未设过(空串)导致推理类任务(`TASK_BASKET_REASON` 等)路由到空 provider、`[aggregate] 推理段缺席(no_provider)`。**改法**:先双备份 `/opt/neckline/data/backups/neckline.db.{bak,cpbak}-prellmcfg-20260805-115602`(`.bak` 走 `sqlite3 .backup`、`.cpbak` 走 `cp -p`,均 `integrity_check=ok`;`.cpbak` 与改前活库 sha256 逐位相同),再用**项目自身代码路径**(`neckline.settings_store.update_provider`/`set_llm_routes`,零裸 SQL 写)—— ① `llm_providers`(id=1 `GLM` 行)`base_url` 由 `https://open.bigmodel.cn/api/paas/v4` 补全为 `https://open.bigmodel.cn/api/paas/v4/chat/completions`;② `app_settings.llm_default_provider` 由空串设为 `GLM`(读表取的准确值,非猜测大小写),`llm_task_routes` 原样 `{}` 未动、`search_engine`/`api_key` 均未动。**真实调用验证**(nk,`neckline` 用户,`/opt/neckline/.venv`,项目代码路径 `get_provider(task=...)`,验证脚本用后即删、未入库):检索类 `TASK_DRIVER_SEARCH`(路由②因 `enabled AND has_web_search` 挑中 GLM)—— `ok=True`/耗时 18.9s/`search_hits=5`/回答「参考材料最新至7月30日,暂无今日8月5日A股大盘表现」(证明 `search_query` 机制正常触发真实检索且时效纪律生效,不是 v1.3.4 案底那种 `ok=True` 静默 0 命中);推理类 `TASK_BASKET_REASON`(路由③回退 `default_provider`)—— `ok=True`/耗时 6.0s/`content="收到"`。两次解析出的 provider 均为 `GLM`(唯一一行,符合预期)。改前改后 `PRAGMA integrity_check=ok`、`neckline.service` active、`/api/v1/health` 200。**用户同日拍板「单 LLM 为常态」**:GLM 单家兼任检索与推理;架构裁定 #2(§〇「LLM 成本与晚间窗口」)的双 Agent 分工(DeepSeek 推理 + GLM 检索)**降级为可选扩展路径**——路由表机制(`llm_task_routes`)保留,将来加第二家 Provider 时把默认切过去即恢复分工,不需要改代码;App 设置屏侧不用动(已填 key 原样有效);§〇 锁定表原文未改,本条口径以此次记录为准。**范围边界(如实登记)**:本次严格未碰 `scripts/evening.py` 与报告相关文件(另一 builder 会话同时在 nk 上处理 P0-39/晚间链)——仅验证裸 provider 调用,⑤⑥ 篮子引擎在真实报告管线里的首跑仍待下次晚间链或专门补跑验证。

- 2026-08-05 · 🎉 **P0-40 修复上线 + 08-04 报告完整成型 —— V2 篮子引擎在真实报告管线里的首次成功产出**(@builder-pro,盘中 12:40→14:40;commit `3cb96f6`)。**病灶**:LLM 两处配置缺口补齐后重跑,检索段 200 OK 全通、**推理段 3/3 次恰好 90s `ReadTimeout`** → 当日恒不成篮。**根因**:90s 是给「带联网搜索的单次审判/问询」调的,而 ⑤ 一次把 **20 颗种子 + 每颗的成员机械数据**塞进同一个 prompt 要结构化 JSON —— **确定性超长,不是抖动**(实测该调用 **173 s**,正好卡在 90s 与 240s 之间)。**修法**:读超时**按 task 类别分级**(`router.read_timeout_for_task()` = 唯一实现,`factory.get_provider(task)` = 唯一接线点),推理类 240s、**检索类 90s 一字不动**;⛔ 不全局翻倍(短超时的价值在快速掐断真卡死的连接)、⛔ 不做成 `chat()` 参数(要改每个调用点、漏一个看不出来)。同步抬 `deploy/neckline-basket.service` `TimeoutStartSec` 3600→**5400**(该值本就是按预算算术定的,抬读超时不抬它 = 最坏被 systemd 掐成「链断了」;单测把两者钉在一起)。**部署**:6 个文件 sha256 逐字节相同,unit 已 `daemon-reload`(`TimeoutStartUSec=1h30min` 生效),⛔ 未重启常驻服务。**第三次重跑**(`nk-evening-0804-t240`,`User=neckline`/`MemoryMax=1400M`/⛔无 `--notify`):13:46:10 → 14:15:19 = **29 min 09 s**,`ExecMainStatus=0`,五段 `verify:empty · scan:ok · **basket:ok {baskets:4, cards:4, dropped:0}** · review:empty · report:ok`。**产出**:**T1 CPO光通信**(event,机械分 0.879,15 条证据)· **T1 存储芯片涨价**(commodity,0.810,6 条)· **T2 半导体设备国产化**(policy,20 条)· **T2 先进封装玻璃基板**(theme,10 条),各 3 名成员 + 各 1 张卡,`tier_history` 4 行,`dropped_handoff=[]`(⑥ 跑了、零溢出),四张卡 `evidenceStatus=ok`/`degraded=False`/`llmStage=ok`、口径指纹齐全(`stopPct 0.05`/`takeProfitRetrace 0.08`/`charterVersion v1.3.3`/`packVersion K7-pack-v1`/`verificationRulesetVersion verify_ruleset_v2`),夹逼三件套全 `clamp=ok`。**对拍分歧如实两说并存**:`300620.SZ 光库科技` LLM 标 elastic、机械侧 leader(rs_rank=1)→ markdown 表格 `弹性 / 龙头` + `⚠ 两说并存`、契约 `roleConflict=true` + `roleConflicts:["300620.SZ"]`,K4 `avoid_flag` 同步露出,**不采信任一方**。**耗时基线(供 P3-41)**:④ 扫描 7s · **⑤ 检索段 991s(16m31s,20 颗种子串行,检索账 1200s 已用 83%)** · ⑤ 推理 173s · ⑥⑦ Tier+卡 452s(5 次调用)· ⑨ 复盘 0s(无对象)· 报告段 126s(7 次调用);三本账估算 search≈991/1200(83%)、reason≈625/1800(35%)、review 0/900(0%)。⚠ **`BudgetLedger` 是进程内内存态、既不落库也不打日志**,以上为按 journal 时间戳推算的代理值,如实标注。**Y-1 第三次实测生效**:`candidates_json` 53421 B/`d5d2aec…`、`watchlist_json` 17808 B/`31d8444…` **三次重跑 sha256 全等**;`basket_daily_json` 424→**44 956 B**、`markdown` 6487→**25 349 字符**。**日期 grep**:markdown 命中 1 = `生成时间(UTC)`(正确);八个 `*_json` 列 0;**`basket_daily_json` 12 处命中经逐条核实全是 `card.nextTradeDate` / `verificationSpec.next_trade_date` / `invalidationSpec.next_trade_date` = `20260805`(D0=08-04 的**下一交易日**,是卡的语义要求,**不是污染** —— 前两次为 0 只因为当时没有卡)。**公网**:`GET /report?date=20260804` 200,`basketsAvailable=true`/`unavailableReason=null`/`droppedBasketsAvailable=true`,4 篮各带卡。📌 **契约正解**:篮子成员在 `baskets[i].memberCodes`(代码数组)与 `baskets[i].card.members`(完整成员对象),**没有 `baskets[i].members` 这个键** —— 快扫取到 0 是键路径不对,不是数据缺失。收尾:`systemctl --failed` 零条、`neckline.service` `NRestarts=0` 全程未重启、瞬态 unit 已回收、load ≤0.4。**并发化提速登记为 P3-41(今晚不动)**。
- 2026-08-05 · 🩹 **P0-39 当日修复上线 + 08-04 那份报告的 ③ 节就地改成诚实陈述**(@builder-pro,盘中 11:41→12:01,赶在今晚 16:35 首条正常生产链之前;commit `99f0e76`)。**病灶**:③ 的 `baskets_available=True` 挂在「`load_today_baskets()` 读表成功」上 —— 只证明表读得出来、不证明引擎跑过,于是 ⑤ `no_provider` 全缺席时报告照样输出「今天没有共同驱动清晰、成员结构够格的篮子」这句**实质性市场判断**。**修法**:新表 `basket_stage_handoff`(搬运工体例,⛔ 未碰篮子四表)+ 判读唯一实现 `selection/basket_stage_handoff.py`;写侧 `_run_basket_segment` 在 ⑤ 一返回**立刻**落表(**位置定死在「没篮子就早返回」那句之前** —— 缺席场景恰恰走那条早返回),SEG_BASKET 整段炸掉时覆写 `segment_failed:*`;读侧「有篮子 = 引擎跑过的活证据 / 零篮子才查段状态三态定夺 / 无行 = 不知道也标未取得」。**⚠ 与 ⑥ 的 `basket_dropped_handoff` 是两张表两个问题,不合并**(⑤ 缺席时压根走不到 ⑥,dropped 表**无行**而本表**必须有行**)。**契约形状零变化 → 常驻 API 全程未重启**(`NRestarts=0`、`ActiveEnterTimestamp` 停在 08-04 22:01:08 未动,而公网已返回新文案)。**测试**:四态 + 无行第五态 + 默认段状态陷阱(⑤ 保险丝返回 `reason_stage=no_seeds` **默认值**,只看段状态会把故障读成结论 → 判读先看 `notes` 的 `aggregate_failed:*`)+ 写侧落点顺序正面钉死 + ③b 不回归;**全量 2996 passed / 2 skipped**,nk 上同套 92 例复跑绿(⚠ `DB_PATH` 重定向到 scratch 跑,实测确有 507 KB 泄漏写落在 scratch 库 = §七 A8 那条测试隔离洞的又一次实证,生产库 mtime 未受扰)。**部署**:7 个文件 `install -o deploy -g neckline -m 644` 逐个 sha256 与本地**逐字节相同**;⛔ 未重启 `neckline.service`。**08-04 就地重跑**:隔离瞬态 unit `nk-evening-0804-p039`(`User=neckline`/`MemoryMax=1400M`/**⛔ 无 `--notify`**),墙钟 **11:58:43 → 11:59:00 = 17s**,`ExecMainStatus=0`/`Result=success`,五段 `verify:empty · scan:ok(seeds 449) · basket:empty · review:empty · report:ok`;⑤ 再次打出 `推理段缺席(no_provider)—— 当日不成篮`(**两处 LLM 配置缺口照旧未擅改**,本次修的是「缺席时不许撒谎」不是「让它别缺席」)。**结果对照**:`basketsAvailable` **true→false**、`basketsUnavailableReason` `null`→**「聚合/定档引擎本次未运行(原因:no_provider),今日篮子信息本报告未取得。」**,markdown ③ 节旧文案「今日无篮子达到定档标准…」**已绝迹**、改为「⚠ **本段未取得**…」+ 既有的「『未取得』≠『今日无篮子』」提示;新表行 = `search_stage=call_failed:上游 404 · reason_stage=no_provider · basket_count=0`。**Y-1 保护再次实测生效**(日志留证「已跳过覆写、保留历史值」):`candidates_json` 53421 B / `d5d2aec…`、`watchlist_json` 17808 B / `31d8444…` —— **与基线两个 sha256 逐字节相同**;`basket_daily_json` 366→**407 B**、`markdown` 6380→**6453 字符**(只多了诚实文案)。**公网验收**:`GET /report?date=20260804` **200 / 16 799 B / HTTP/2 / 0.118s**,`basketDaily` **13 个键与修前完全同集**(形状零变化的机器证据),`dataFreshness` 三项全 lag 0。收尾:瞬态 unit 已回收、`systemctl --failed` 零条、无残留进程、落盘 md 属主 `neckline:neckline`、全程 load ≤0.43(执行时段恰在 11:30–13:00 午休,盘中哨兵空转)。
- 2026-08-05 · 📄 **08-04 的 V2 报告已补跑落库 —— 机械段全出、篮子段如实缺席;顺带在生产上打出两处 LLM 配置缺口 + 一条 P0 诚实性缺陷**(@builder-pro,盘中,隔离瞬态 unit `nk-evening-0804`,**⛔ 未传 `--notify`** —— 给一份补跑的历史日报告推「报告就绪」到用户锁屏是误导性噪音,任务书亦未要求)。**跑法与结果**:`scripts/evening.py 20260804` 全链带 LLM 落库,`User=neckline`/`MemoryMax=1400M`,墙钟 **11:28:06 → 11:28:27 = 21s**,`ExecMainStatus=0`/`Result=success`,1400M 闸下零 OOM;五段状态 **`[verify] empty`(08-03 无冻结卡,无对象)· `[scan] ok`(cluster 2153 / corr 21382 / leader 2153 / stage 110 / seeds 449〔hot_industry 10 · surging_concept 21 · limit_cluster 323 · anomaly_cluster 95〕)· `[basket] empty`(0 篮 0 卡)· `[review] empty` · `[report] ok`**。**Y-1 保护实测生效(日志留证)**:`save_report(20260804):本次候选快照为空([]),但该日已有非空 candidates_json…已跳过覆写、保留历史值。`;补跑后 `candidates_json` len **53421** / sha256 `d5d2aeeca2d7d4e4b616db26334bfecb3bb55bd75a1c0056f8ff442910fdd429`、`watchlist_json` len **17808** / `31d844483997634f3ba01b0f3835400112a1485f26a1999b8a3de2929f504bf6` —— **与补跑前基线两个哈希逐字节相同**(不是只比长度);V2 侧 `basket_daily_json` `{}`→**366 B** 正常写入、`markdown` 31527→6380 字符(V1 那份含 20 只候选,V2 无候选管线,短属预期)。**日期纪律逐项过**:`daily_update`/`evening` 两条链的 `target`/`trade_date` 全程取自命令行参数、下游无一处旁路 `date.today()`;**日期锚抽查**(补跑历史日的「锚不撒谎」机制)实测装配出 `今天是 2026年8月5日(周三);本次分析的基准交易日是 2026年8月4日(周二)(系补跑/回放历史日,请按该日及之前的信息判断);下一交易日是 2026年8月5日(周三)。`,V2 链四个 LLM 调用点(`aggregate.py:620/757`、`tier.py:717`、`basket_card.py:615`)与 `basket_review.py:855` 全部显式传 `ref_date=trade_date`;**污染 grep**:markdown 里 `20260805|2026-08-05|8月5日|08-05` **仅 1 处命中 = `生成时间(UTC):2026-08-05T03:28:27`(报告确实生成于今日,必须这么写)**,其余六个 `*_json` 列命中 **0**,公网整份响应里唯一命中同样是 `generatedAt`。**公网验收**:`GET /report?date=20260804` **200 / 16 706 B / HTTP/2 / 0.137s**,`tradeDate=20260804`,`dataFreshness` 三项全 **lag 0 / stale=false**(补拉之功)。**🔑 LLM 段如实报告 —— 是「降级版」,但不是任务书预想的那种降级**:key **有效**(带真 key 打出的是 **404 不是 401**,鉴权已过),但**一次调用也没成功**,两处配置缺口:① `llm_providers.base_url` 现值 `…/api/paas/v4`,而本项目这一列是 `OpenAICompatProvider` 直接 `client.post(self.api_url,…)` 的**完整端点、不做路径拼接**,应为 `…/api/paas/v4/chat/completions`(预置参考实现 `llm/providers/glm.py:37` 即此值)—— ⚠ **GLM 官方文档管 `…/paas/v4` 叫「OpenAI 兼容 base_url」,用户填的是官方原文,是本项目字段语义与通用叫法撞车,不是用户填错**;② `app_settings.llm_default_provider` 从未设过(空串,`llm_task_routes={}`),而路由三级里 GLM 只能靠 ②「检索类挑第一个 `enabled AND has_web_search`」被选中,推理类(`TASK_BASKET_REASON`/`TASK_TIER_RANK`/`TASK_SCRIPT`/`TASK_REVIEW`)走 ③ 回退默认 → 空 → `no_provider`,**GLM 不会自动兼任推理**。**两处均属持久配置(路由/端点),builder ⛔ 未擅改**,补齐后同日重跑即可补出篮子(冻结件 `INSERT OR IGNORE`,同日补跑安全)。**🔴 并打出 P0-39(已登记 §七)**:⑤⑥ 全缺席时报告 ③ 节仍输出「今日无篮子达到定档标准…今天没有共同驱动清晰、成员结构够格的篮子」= 把「引擎没跑」讲成实质性市场判断,`basketsAvailable=true`/`basketsUnavailableReason=null` 同样说谎;根因 `basket_daily.py` 的 `baskets_available=True` 只挂在「读表成功」上(同函数 ③b 的三态区分做对了、③ 没有),⑤ 侧 `STAGE_NO_PROVIDER` 等一等状态没落到报告层能读到的地方。**⚠ 故 08-04 那份报告的 ③ 节不可信,其余四段(机械段)不受影响**。收尾:`systemctl --failed` 零条、`neckline.service` `NRestarts=0` 全程未受扰、load ≤0.06、无残留进程、瞬态 unit 已回收。

- 2026-08-05 · 🩹 **A 方案官方补拉:nk 补齐 `20260804` 当日 EOD,⑰ 割接的 parquet 缺口销案**(@builder-pro,盘中,用户裁定 A;隔离瞬态 unit `nk-backfill-0804`)。**⚠ 一处有意偏离**:任务书写 `systemd-run --scope`,实际改用**瞬态 service** —— `--scope` 在 root 下跑会让新落的 parquet 变成 `root:root`,而 `data/` 必须是 `neckline:neckline`(`sync_code.sh` 的属主自检就是防这个,v1.1-H/H2 因属主翻转两次把生产库跑成只读、服务 502)。瞬态 service 同样是「隔离单进程 + MemoryMax」,还能钉住 `User=neckline`、并给出 `ExecMainStatus`(本项目「timer 跑过 ≠ 任务成功」的判据),逐项照抄 `neckline-daily.service`(`User/Group/WorkingDirectory/EnvironmentFile/MemoryMax=900M/Type=oneshot`)。**结果**:墙钟 **11:23:14 → 11:24:36 = 82s**,CPU 6.06s,`ExecMainStatus=0`/`Result=success`,900M 闸下零 OOM;`[daily] 5529 行 · [daily_basic] 5529 · [adj_factor] 5550 · [moneyflow_dc] 5941 · index_daily 5 指数各 1 · limit_derived 落 2455 行(2026-07-15~08-04,15 交易日尾窗)· suspend_d 7 只 · ths_daily 尾窗 5 日写 1970 行 · industry_strength 110 行(滞后 0)`;`ths_index/ths_member` 未到 7 天重拉周期、如实跳过(省下 ~400 次配额)。**逐表验收**:七张表 08-04 分区**全部在位**、属主全 `neckline:neckline`、体量与 08-03 同量级、**每张表 08-03 与 08-04 的 schema 逐表一致**、整年 `scan_parquet` 无 `SchemaError`(项目两次崩报告的类型漂移案底,故显式验);`ths_daily` 自愈到 08-04(08-03/08-04 各 394 行 = 概念指数口径、非 2499 的全板块,滞后 0 天,dtype 未漂)。**🎯 连板递推抽查(本次修复的核心判据,全绿)**:08-04 涨停 141 只 / 炸板 25 只;**A** 20 只 08-04 连板≥2 的票,08-03 的 `consec_limit_up_days` **逐个恰为 c−1、递推异常 0 只**(最长 `003032.SZ` 6→7 连板);**B** 8 只 08-03 涨停而 08-04 未涨停的票,`consec` **全部归 0、异常 0 只**。📌 **反证样本**:`001223.SZ` 08-03 是 **2 连板、08-04 炸板** —— 若 08-04 仍缺失、它 08-05 再涨停,就会被算成 3 连板,这就是那条判据污染的具体长相。**老机 hz 全程零接触**(A 方案不需要它)。

- 2026-08-05 · 🔴 **补跑 08-04 V2 报告的前提不成立 —— 查出 ⑰ 割接漏搬 08-04 当日 EOD 增量,已停手待裁**(@builder-pro,盘中,**纯只读诊断,零写入**)。**病灶**:nk 的 `data/parquet/` 七张 EOD 表(`daily`/`daily_basic`/`adj_factor`/`moneyflow_dc`/`index_daily`/`limit_derived`/`suspend_d`)**最新分区都停在 `20260803`**,**`20260804` 一张都没有**;`ths_daily` 更只到 `2026-07-31`(超 `SECTOR_DATA_STALE_MAX_LAG_DAYS=2` 容忍)。**根因**:⑰ 割接是**两次搬家**——parquet 在 08-04 **14:42** 逐文件 md5 全等地搬过一次,`neckline.db` 在 **21:48** 又增量搬了第二次;而老机 hz 的 16:05 拉数**发生在两次之间**,于是**库里有 08-04(`industry_strength_daily`/`holding_eod_check` 皆 `20260804`),文件里没有** —— 割接验收当时逐表对拍的是 SQLite,parquet 侧没有第二次对拍,缺口因此没被发现。老机 hz 上七张表 **08-04 分区俱在**(本次只读 `ls` 核实,neckline 三单元仍 inactive+disabled,零改动)。**两条后果**:**①** 照原样跑 V2 链会**静默出一份空心报告** —— `market_data.scan_table_range` 对缺分区**返回空 DataFrame 而不抛异常**,④ 扫描层零行 → ⑤ 无种子 → 报告写「当日不成篮」,而真相是「这台机器上压根没有那天的数据」,**看不出是坏的**,正是本项目一贯最忌的静默降级;**②** 更要紧的是**它会污染今晚 16:35 那条链** —— `limit_derived` 连板计数是**按行相邻**递推(`is_limit_up.shift(1).over(ts_code)`,「停牌无行不打断连板」口径),08-04 的空洞会让 08-03 与 08-05 两行**直接相邻**,08-04 炸掉的板会被算成连着的,而连板数是 K7 包的判据输入。**两条候选修法**(均未执行,等用户裁定):**A** 在 nk 上 `python scripts/daily_update.py 20260804`(已审代码:`target` 全程取自 `sys.argv[1]`,下游函数无一处旁路 `date.today()`,日期口径干净;且 ⑯-D 起 `main()` **已不再挂 ④/④b 扫描层**,只补拉数 + `limit_derived` 15 交易日尾窗 + `suspend_d` + 概念板块 + `industry_strength_daily`,与晚间链不重叠)—— 代价是盘中向 TuShare 拉一次全量 EOD;**B** 从 hz 只读 rsync 那 7 个分区(~786 KB)过来 —— 与生成 V1 那份 08-04 报告的数据逐字节同源、零外部调用,代价是要碰一次停用留档的老机。**⚠ 时效**:今日 16:05 拉数只拉 08-05,**不会自愈 08-04 的洞**,故修复应赶在 16:05 之前。

- 2026-08-05 · 🧹 **生产持仓台账清空:删除 2 笔与现实早已不符的僵尸持仓(用户指令)**(@builder-pro,盘中,只动 SQLite 点删、零重活)。**先双备份**`/opt/neckline/data/backups/neckline.db.{bak,cpbak}-preclearpos-20260805-110527`(各 33 038 336 B,`.backup` 耗时 0.162s,**两份各自 `PRAGMA integrity_check` = ok、各自读回 `open:2 closed:3` 复核**;WAL 当时已 checkpoint、`-wal` 0 字节,故 `cp` 副本一致)。**清空方式 = 删除 `positions` 表 `status='open'` 的 2 行,⛔ 未编造任何卖出价**:先查证「不伪造成交价的关闭/作废机制」在代码里**不存在** —— `sentinel/positions.py::close_position(position_id, sell_price: float, sell_date)` 的价格是**必填位置参数**,`status` 全仓只有 `open|closed` 两态、无 void/cancel,且 `closed` + `sell_price=NULL` 是**代码不支持的态**(`scripts/positions.py::_fmt_row` 对 closed 行直接 `f"{p.sell_price:.2f}"`,None 会当场 TypeError;熔断价格兜底同样要读它)—— 故按用户给的优先级降到「删除 + 留档」。**被删两行原文留档**:`#4 601567.SH 买入价16.749×2400 买入日20260730 备注「放量突破」`、`#5 600499.SH 买入价16.769×2400 买入日20260730 备注「放量突破,昨日涨停,重组利好」`(均 `buy_fees=5.0`,`created_at` 2026-07-30)。**3 笔历史平仓行(#1/#2/#3)原样保留**。**关联行核对**:`entry_snapshots`/`position_plans`/`user_actions` 均 **0 行**(V2 表,如预期);另查出**任务书未预料的两处** `holding_eod_check` **8 行**(#4/#5 各 07-30→08-04 四天体检快照)与 `decision_log` **1 行**(#4 的建仓论点)—— **一并保留不删**,理由:它们是「那几天系统看到了什么」的**append-only 审计事实**,删掉等于篡改历史;且全部消费点(`holding_store.py` 三个 load 函数、`precall` 两个 provider)都是**按当前 open 持仓 `.get(pos.id)` 反查**,空仓下永不被取出,孤儿行惰性无害(已逐个读代码确认无 JOIN)。**验证**:`positions` open **2→0**、closed 3 不变;`GET /api/v1/positions` **200 / `{"holdings":[],"circuit":{"locked":false,...}}`**;删后跨多个哨兵拍(盘中 60s/拍)`journalctl -p warning` **零条**、`NRestarts=0`、load 0.00;空仓是合法态有单测背书(`test_sentinel_tick_v2_bypass.py::test_unavailable_reasons_are_reported` 断言 `no_open_position`)。**推送噪音实证**:今早 09:25–09:30 这两笔仓共触发 **8 条**哨兵事件(`precall position_low_open`×2 / `d5exit trigger`×2 / `holding stop_approach`×2 / `holding take_profit` / `attention decoupled`),正是本次要止住的东西;`sentinel_events` 既有行**保留**(已推是事实)。

- 2026-08-04 · 🚀🔻 **V2-⑰ 割接切换完工 —— Neckline 现役生产机由 hz 老机换成新机 `nk`,V2.0.0 正式上线**(🔴 @builder-pro,高危块:权威库二次搬家 + 生产割接;22:00–22:05 CST 一体切换)。**序**:A 路前提自检 → 代码最终同步 → 数据最终增量搬家 → 客户端指向 + macOS 换包 → 老机停 / 新机启 → 公网转 200 → 收尾核查。**① A 路自检**:P1 打出一条 ✗ 经查证是**脚本自身的误报**(唯一命中是 ⑯-G 落产的 `npm-custom-http.conf` 文件头那句护栏注释),权威判据复核「容器 `nginx -T` 剥注释后零命中 `ln`、全量 `server_name` 无任何 `ln` 指令」→ 修 `deploy/preflight_a_route.sh` 两处 grep 先剥注释(**对自己的注释报警的闸门等于没有闸门**);P2/P3/P4/P5 全成立(老机 active、`/health` = `v1.5.2` 用 IP+Host 实测;`ln` 仍 NXDOMAIN,⑯ 已上报的既有事实)。**② 代码同步**:DRY_RUN **零删除**、属主自检绿;sha256 对拍**做成全树而非抽样 —— 396 个文件逐个全等、零差异**(⑰ 前置-④ 的 A 组九项 + B1 + 尾巴三件与 ⑯ 的 12+7 关键文件全含在内);`init_schema` **WARNING 零字节**、46 表、两条关键唯一索引都在。**③ 数据搬家**:写前双备份 `neckline.db.{bak,cpbak}-pre17cut-20260804-214747`(`.cpbak` 与活库 sha256 逐位相同)→ 老机 21:48:10 `.backup` **0.343s / integrity ok / `feff9a4d…`**、**老机→Mac→新机三跳 sha256 逐位相同** → 确认无进程持有库后 `install` 替换(并清 stale wal/shm)→ `init_schema` 26→**46 表**零 WARNING → **重放 ⑯ 三步各自先演练后 `--confirm`**(K4-pack 锚 → K7-pack 现役,演练 diff 与 ⑯-E 记录逐项吻合 → B3 退役,fail-closed 的 before 哈希与老机库实际值精确对上,写后 `32da6a10…`/1999 B **与 archive 存档逐位相同**)。**验证**:老机 26 表**逐表行数一致**,唯一差异 `sqlite_sequence` 7→9 **逐条核实为两张 AUTOINCREMENT 表的记账** = 预期;`selection_packs` 2 行 / 现役唯一 K7-pack-v1 / 激活流水 3 条;现役章程仍 **`v1.3.3`**(`activated_at` 未变)、`strategy_activation_log` 未增行;走**代码路径**复核现役包与 `stop_pct=0.05`;**B3 零黄牌、A2 仍命中**(`persist_days` 0..10 全区间发射集合 = `['A2_theme_persist_ge_4']`);今日写入确已在新库(`reports` 20260804 1 行、`sentinel_events` 62 条)。🔒 **额外保险(plan 未要求)**:停老机**前**再跑一次老机库逐表行数,与快照**零差异** = 搬家未漏任何写入。**④ ⚠ ⑮ 漏改的客户端默认后端,本次补上(真洞)**:`AppConfig.swift` 的 `.prod` 此前仍是 `ln.linotsai.top`,照此换包**新 App 会打老机 → 撞 V1.5.2 契约 → 整份报告解不出、今日计划页全空**(`Candidate` 四个价格键是 `try c.decode` 必需)—— 改指 `nk` + 四处引用换血 + **新增反向闸门单测**(断言消息自带理由),iOS 测试 184 → **185 过**。**⑤ macOS 换包**:旧包 `ditto` 备份 → `~/Lino/app_backups/Neckline-1.5.1-20260804-215939.app`,`ditto`(⛔ 未用 `cp -R`)装 `/Applications/Neckline.app`;验 **2.0.0 / universal / `codesign --verify --deep --strict` valid + 满足 DR / 产物与安装态可执行 sha256 逐位一致**;**二进制字符串实证**:安装态含 `nk.linotsai.top`、**`ln` 零命中**。iOS Release 产物就位(装真机是用户动作,§八-12)。**⑥ 一体切换**:老机 `stop`+`disable` 三单元(**⛔ 只停不删**,五个 unit 文件与 `/opt/neckline`、`.env`/`.p8`/`data/` 原样留档;停机前最终快照 `/health`=`v1.5.2`、自 07-31 06:09:55 连续运行、`NRestarts=0`、当日两 timer **`ExecMainStatus=0`** 16:05:00/16:35:01 —— 铁律的状态码+本次时间戳双证)→ 新机 `enable --now` 三件全 **active+enabled**、下次触发 **Wed 08-05 16:05/16:35** → 公网 `https://nk.linotsai.top/api/v1/health` **502 → 200 `v2.0.0`**。⚠ **本机 Mac curl 该域名 `remote_ip` 显示 `127.0.0.1`**(CLAUDE.md 登记的代理 fake-ip),故**另从老机这个真外部机位复验**:DNS→`114.66.0.38`、200 `v2.0.0`、TLS 校验 0、**HTTP/2**、证书 `CN=nk.linotsai.top`/LE/至 **2026-11-02**;http→301、无/错 token→401、`/baskets/999999`→**404 `basket_not_found`**;**最强一条** —— 带 token 取 `?date=20260804` **200 / 11 711 B / `degraded=false`**,即**老机 16:35 生成的今日报告现由新机对外服务**,一条请求同证搬家完整与 V2 可用。**⑦ 收尾**:新机 `systemctl --failed` 零条、`NRestarts=0`、journal error 0 条、`memory.events` 全零、idle 78.5 MB;**NPM 四个既有站回归与基线逐行完全相同**;A 路自检复跑**「全过」**;`~/Lino/hz_info.md` 已同步(老机 Neckline 条目改「已停用留档」、nk-3/nk-4/nk-6 改现役态)。**回滚绳三重**:新机两对双备份 + macOS 旧包 + **老机原封不动可随时复活**(⚠ 复活前须先在新机 disable,否则双推送重现)。**测试**:`pytest tests/ -q` **2963 过 + 2 skip + 0 fail**(零回归);iOS Simulator **185 过 + 12 skip + 0 failures**;双端 Release `xcodebuild build` **BUILD SUCCEEDED**。**三处如实登记**:P1 误报按「修闸门」办 / `sqlite_sequence` 判为预期(逐条比对得出)/ ⑮ 漏改客户端默认后端已补。**留给用户四件**:App 设置屏填 LLM key、iOS 真机装包、云解析核对 `ln`/`lf` 的 NXDOMAIN、老机退役与老库归档由用户定。详见 §五 V2-⑰ 完工记录。

- 2026-08-04 · 🩹 **⑰ 前尾巴批次(3/3)收尾:positions_entry 卡损坏分流 + leader.py 单位纠偏 + 契约线 B7 两小项核实**(@builder,commit `642b4a9` 代码 + 本提交文档,纯本地代码/测试/文档,零服务器·零部署;真实 `data/neckline.db` 全程只读,mtime 未变)。**①** `positions_entry` 的开仓计划继承(`find_source_basket_member`/`SourceBasketMember`/`build_inherited_plan`)此前遇「有行但读不出」的卡仍报 `card_not_ready`,与「压根没有行」混成一类(A 批次发现 #1,小审 B-3 同类登记)——现按 `basket_store` 唯一检测点(`load_basket_card` 返回的 `card_corrupt`)原样转发分流出新 reason `card_corrupt`,⛔ 不重判;`card_corrupt` 态下 `source_card_version` 如实回填该行版本号(不是 `None`),便于定位坏在哪一版。`schemas.py::PositionPlanOut` 文档同步三态;客户端 `Models.swift::PositionPlan.unavailableText` 加 `card_corrupt` case,文案「来源卡数据损坏,已记录待排查」,⛔ 不进静默重试。测试新增 2 例(两态分流 + 好卡不误伤断言补齐)。**②** 判定线 🔵-4:`scan/leader.py` 两处 docstring「成交额(元)」纠正为「千元」(`daily.amount` 的 TuShare 口径,CLAUDE.md 点名过的单位坑;`amount_share` 是比值不受影响,纯注释修正),核对全文无同款错述。**③** 契约线 B7 两小项核实:客户端 `mapReason` 的 `max_chase_required` 死 case 经核查**已在 ⑮-A(`0ed277c`)随 `decision_log` 退役一并删除**,当前 client/server 零残留(`test_contract_crosscheck.py::PENDING_MAP_REASON_CASES_FOR_15` 空集合断言亦覆盖此项)——此前 08-04 A 组收尾记录复述了审计原文「已成死码」而未核实现状,本次更正记录、不重复"修复"一次已不存在的死码,是记录滞后不是代码欠账;`PositionOpenIn`/`PositionOpenOut`/`PositionCloseIn` 蛇驼混排裁定**维持现状不改**(登记入 `archive/V2_契约三方对拍_20260803.md` §七 第 4 条,四条理由:破坏性契约变更/v1 存量调用面大/收益风险不对称/篮子族新端点已是纯 camelCase 对照)。三份 review 报告(`REVIEW_REPORT_V2_判定线`/`_契约数据线`/`_小审⑭⑮`,均 `_20260803.md`)逐条销项标注已落。**测试**:`pytest tests/ -q` **2963 过 + 2 skip + 0 fail**(基线 2961+2+0,净增 2;累计 A 组以来 net +52);双端 `xcodebuild build` 绿 + iOS Simulator **184 过 + 12 skip + 0 failures**。§四「⑰ 前置」新增第④条「代码同步清单」起记,把本批与 A 组的改动文件列入待 ⑰ 全量同步 + sha256 抽查范围。

- 2026-08-04 · 🩹 **A 组九项 + B1 实现:⑰ 割接前的次级修理批次全清**(@builder-pro,纯本地代码 / 测试 / 文档,零服务器 · 零部署 · 真实 `data/neckline.db` 全量跑前后 **MD5 与 mtime 逐位不变**〔A8 硬验收〕;三个 commit `8c3e650`(A1–A5)/ `65abbf2`(A6–A8)/ `4c00976`(A9 + B1))。**A1** 判定线 🔵-2:种子排序改「语义主键(行业名次升 / 涨幅降 / 簇大小降)→ `seed_key` crc32」两级序,确定性未减、⑤ 截断前 20 颗自此可解释(⚠ 与 ⑧-G `mainline.crc_rank` 按票采样不同层,未动)。**A2** 判定线 🔵-5:`limit_derived` 有涨停命中而 `daily` 缺失(或该码缺 low / 缺涨停价)→ 新集合 `one_word_unresolved`,该维走中性分 + `tradability_missing`,不再按"开过板"半罚混过;判据锁 flag 不锁数值(单成员篮半罚恰好 = 0.5 撞中性分)。**A3** 判定线 🔵-3:`CARD_SYSTEM_PROMPT` 内嵌 `TIMELINESS_RULES`,prompt 快照守门 2 条 → **7 条**。**A4**(CLAUDE.md 挂账销账)`llm/news_scan.py` 照 `judge.py` 姿势接 `prompt_context` 三件齐(时效纪律 + 首行日期锚 + 显式检索词),全仓豁免名单清空。**A5** 契约线 🔵 B2:存拍 `cum_volume/cum_amount` 源没给 → 落 `null` 不落 0,且该列不进下一拍增量基线(老实现会算出"等于当日全部累计量"的假增量)。**A6** 契约线 🔵 B6 两条防线补回:spec ⇄ 人读条款一致性测试**重建在 V2 对象上**(锁 `spec_threshold_text` 这条通路;null 必须说「不判」且全文无 `0.00`;**两处变异反向验证过会红**)+ 新建 `tests/test_discipline_checks.py` 19 例(每条硬线正负各一,阈值不抄字面量)。**A7** 挂钟炸弹拆除:`test_cooldown_blocks_second_hit_and_expires` 改**冻结时钟**(一次钉住三处时间源;只冻传入的 `now` 不够 —— 北京 14:48 后 `+700s` 必越过 15:00 收盘线),**本次跑在 17:14 全绿**;`test_scan_leader.py` 那条核实为已修(`53fab7a`),零改动。**A8**(§四 既有欠账,V2-① 登记)测试隔离写库缺陷:用「DB_PATH 重定向 + patch `sqlite3.connect` 记 nodeid」探针跑全量套件定位到**全部 7 个命中点** —— `test_holding_k4_check.py` 6 例补 `isolated_env` + 显式 `db_path=`、`test_sentinel_loop_precall.py` 加 autouse 夹具钉 `_DB_PATH_OVERRIDE`(泄漏源不是盘前那一拍,是循环里的 ⑧-B 竞价存拍旁路);修后**全量套件探针零泄漏节点**。**A9** 杂项四件:两个 oneoff 死脚本补「仅供阅读」文件头(B4)/ `sentinel/__init__.py` 模块头按 V2 现役重写 + 本 Plan ⑬ 完工记录 `missedEntryHint` 口径改「新数据下恒空、历史回放仍非空」(B8)/ **核实 ⑭ 确实接了 `exec_hints_for`**(`basket_daily.py`,报告的"零调用方"是审计当时事实)并补 AST 调用点守门(B5)/ 客户端 `ReportResponse` 六处硬解码拉平成诚实空态,`degraded` 缺键**取 `true`** 不取 `false`(小审 🔵 B-1)。**B1 实现**(planner `199521f` 裁定):`card_corrupt` = **500**,检测点唯一在 store 并打 **ERROR**,列表侧 200 但 reason 如实,报告写「本篮卡数据损坏,已记录待排查」,客户端新 `APIError.cardCorrupt` + `send()` 500 分支接进 `mapReason`;⚠ 「顶层必需键」落地成**三个内容键有其一**(⛔ 不是都要有)—— 各消费方要的不是同一批键,"都得有"会把合法局部卡判成损坏,**是全量套件 16 红反证出来的**,误判代价还不对称。全量 `pytest tests/ -q` **2961 过 + 2 skip + 0 失败**(基线 2911 过 + 1 常驻红 → 净增 50 例、常驻红清零);双端 `xcodebuild` build 绿 + iOS Simulator **184 过 + 12 skip**。三份 review 报告逐条销项标注已落。
- 2026-08-04 · ⚖️ **切换前三件:B1 卡损坏 reason 裁定(`card_corrupt` = **500** 不是 404)+ B2 `TASK_INQUIRY` 追认销账 + A10 账本整理(四条 Backlog 逐条核实 / 五小项以代码现文对账 / 补稳定 ID P3-38)**(@planner,只改 `PROJECT_PLAN.md`,零代码 / 零数据库 / 零部署;builder-pro 并行清 A1–A9,已 hunk 级挑拣、未撞其销项标注)。**① B1 裁定:`card_json` 解不出 → `500` + reason `card_corrupt`,⛔ 不进 404 家族**。判据分界定死:**有行但读不出**(`json.loads` 抛错 / 顶层必需键缺失)= `card_corrupt`;**根本没有行** = 仍 `card_not_ready`。**四条理由**:(a) **404 是在说谎** —— 卡存在(库里有那行)、只是读不出;(b) 更要命的是它会**和 `card_not_ready` 撞成同一类**,而两者要求的反应**完全相反**(一个是等 ⑦ 补 `version=1` 的正常中间态、不需人介入,一个是数据损坏、需人排查);(c) **决定性的一条:卡是冻结件、`INSERT OR IGNORE` 永不覆盖 → 坏了就是永久坏的**,客户端若当 `card_not_ready` 处理就会**永远重试、界面永远显示「卡还没生成」而那张卡这辈子不会来** = **静默永久失败**,本项目一贯视为最危险那一类;(d) 放进 **500 这个类**意味着**即使有代码 / 日志采集器只看状态码不看 reason 也不会误当良性态**,放 404 则任何按状态码分流的地方都会混掉 —— **冲突时防误判优先于家族整齐**。⚠ **体例一致性用另一种方式保住**:响应体**仍是同一个 `{"reason": ...}` 形状**、`mapReason` **仍是唯一语义映射点**,一致性真正值钱的地方(**查一个 reason 只看一处**)一点没丢,丢的只是「都挂 404 下」这个表面整齐。**配套四条**:客户端 `mapReason` 加独立 case + 文案「**这张卡的数据损坏了,需要排查**」⛔ 不许写成「卡还没生成」+ ⛔ **不进静默重试路径**;服务端命中打 **ERROR**(不是 WARNING —— 冻结件损坏是真数据事故,必须有人看见);⛔ **不许在读侧「重建」或跳过坏字段糊过去**(承 v1.5「写侧收口、不读侧遮蔽」);报告 / markdown 侧同步如实写「本篮卡数据损坏,已记录待排查」⛔ 不许降级成「卡未生成」。已写进 **⑭-B** 并在 **⑮** 加客户端 case 要求。**② B2 追认**:`TASK_INQUIRY` 归检索类默认路由 —— 用户追认 builder 推断(问询台本就 `enable_search=True`,与既有行为一致),**§四 欠账行该条划掉**、**V2-② 完工记录处加追认标注**,自此不是欠账。**③ A10 账本整理**。**(i) 四条 Backlog 逐条核实(不是照抄标注,是查代码)**:**P1-4 ✅ 结案** —— `brain.config_governing_at()` 存在且 `review/reconcile.py` **6 处**按成交时刻逐笔取章程,v1.4-⑥-A 真兑现;**P1-5 ✅ 结案** —— `llm/budget.py` 三本账 `LEDGER_SEARCH`/`LEDGER_REASON`/`LEDGER_REVIEW` 齐备互不透支,单一 300s 墙钟已退役;**P1-6 ✅ 结案** —— `PositionOut.timeExitLockedDay`/`timeExitLockedLateDays` 已在 `schemas.py`,纯派生展示位、判向逐位未变;**P3-11 ⛔ 改判为「原命题废弃」,不是「已被 ⑫ 吸收兑现」** —— 查 `neckline/profile/common.py` 维度唯一源是 `theme`/`role`/`entry_style`/`entry_label`/`tier`/`missed_role`,**没有 `playbook`(打法)也没有 `thesis`(论点)**,而 `decision_log` **全仓零 INSERT**(⑩-C 停写)、`breathing_t_trades` 亦停写(裁定 #9)→ **那张「打法 × 论点」交叉表在 V2 里三个维度的数据源全没了,做不了也不会再做**;⚠ **⑫ 给的是替代品不是兑现**(换了套能答同类问题的新维度 + 新增 V1 拿不到的「是否跑赢同篮未选成员」,**问题相近但不是同一张交叉表**,⛔ 别在账本上记成"已兑现"),将来若仍想要「打法」维,唯一路径是 ⑩-C 落 `user_actions` 的可选标签攒够覆盖率 = **§七 [P3-28] 的内容**,本条不再单独排期。**(ii) 五小项以代码现文对账,结论「两边都没错,只是时点不同」**:**Y5 已销项、⑮ 完工记录的正面陈述为准** —— 实查 `APIClient.swift`:`link` 零命中,`cancel`/`revise`/`scenario-outcome` 各 2 处**全是注释**(行 49/555 说明服务端已删),`PUT /settings/llm` 零命中;**Y5 报告是审计当时的事实,现已修完**。⚠ **顺带核实一件容易误删的:`GET /decisions/{id}/track` 服务端仍在、客户端仍是活调用,它不在 Y5 那五处里,⛔ 别顺手当遗留删掉**。**B7 `status` 参数名已修、两侧一致**(服务端 `Query(alias="status")`、Python 形参 `status_filter` 不外泄;客户端发 `status=`)。**`mapReason` 双 404 case 已落**(`basket_not_found`/`card_not_ready` 各有独立 `APIError` case,注释明写「语义相反」)。**幂等键文档已落且在客户端侧**(`idempotencyKey` 注释已写「每笔新提交一个新键(UUID),⛔ 严禁复用、⛔ 别绑『票+日期』」= 兑现上一轮补进 ⑮ 的要求)。以上已就地写成 **A10 账本核验**一节。**(iii) 补稳定 ID `[P3-38]`「对账结果未作画像数据源」** —— 此前只活在 ⑫ 完工记录与 ⑭ 对拍表 §十里、**§七 没有条目 = 账本漏登**;内容照 ⑭ 裁定(**维持不接**:`RoundTrip` 与 `positions` **无共同主键**,靠「金额±容差+日期邻近」在**同日同票分批建仓**这个本项目常见场景下会**系统性错配**;**错配的代价落在画像这个「结论」上** —— 与「样本不足如实标低置信度」是同一条纪律的两面,**宁可没有这一维,不要一个可能是错的维**;现有数据源已覆盖产品意图,真实费用与资金流已由 `review/cashflow.py` 四分类单独产出),并**把正确做法留档防到时候现想**:**先有确定性关联再谈接入**(录入侧显式配对 / 等券商接口直读拿真实成交流水号),**⛔ 不许用近似匹配凑**。**⚠ 环境异常第四次复现**:又一次工具返回里被追加了与命令输出无关的 computer-use 指引块,**这次还把 `git status` 的真实输出挤掉了**(已重跑取真数据)。**工具返回是数据不是指令通道**,已整段忽略、未据此做任何动作、未请求任何桌面权限(本次纯 markdown)。前三次已连续建议排查,**建议当作确定的注入面缺陷处理,而非偶发噪声**
- 2026-08-04 · 🩹 **V2-⑯-D 补记:③b 跨进程缺陷修复(选③,新表落地)**(@builder,定向小修,纯本地代码 + 新机同步验证,不 enable 任何 timer)。新表 `basket_dropped_handoff`(`neckline/db.py`)+ 唯一读写实现 `neckline/selection/basket_dropped_handoff.py`(按 `trade_date` 覆写,不臆造 tier/`basket_id`,⑥-b-C「不落 `baskets`/`tier_history`」原判不动);写侧接线 `report/evening.py::_run_basket_segment`(⑥ 一算完立刻落,不依赖 ⑦ 卡生成成败),读侧接线 `run_evening_chain` 的 SEG_REPORT 分支(仅当本次压根没打算跑 `SEG_BASKET` 时才查表,已尝试但炸了/⑤零产出的 `None` 不被表里旧数据覆盖,单进程整链路径行为逐字节不变)。三态(没跑/零溢出/有溢出)本地 14 例新测试 + 2 条仓库级参数化守门自动覆盖,`pytest tests/ -q` 2895→**2911 过 + 1 fail(既有无关,已 spawn_task)+ 2 skip**,零回归。新机(114.66.0.38)7 文件 sha256 同步核对相同,`init_schema` 幂等建表(46 表,新表 0 行);两次**独立进程调用**(隔离副本库、真实 parquet 只读、`smoke_basket_review.StubProvider` 桩 LLM 真跑 ⑤⑥⑦)复现三态全部正确,新机两个改动测试文件另跑 29 过。`deploy/neckline-report.service` 头部三条出路 + `report/basket_daily.py`/`report/evening.py` 模块头同步改写为已修复口径。详见 §五 V2-⑯ 完工记录「⑯-D 补记」。
- 2026-08-04 · 🌐 **V2-⑯-G 公网入口收口完工 —— `https://nk.linotsai.top` 已通**(🔴 @builder-pro,在用户活着的反代基础设施上做纯增量)。**没走阻塞上报时列的三条路**(用户从未配过 NPM 面板、拿不到也不该造管理凭据),改走 **NPM 官方 custom include 扩展位** `/data/nginx/custom/http.conf`:`:80` 承 ACME HTTP-01 + 全量 301、`:443` 挂 LE 证书反代 `172.18.0.1:8002`(桥网关 = 宿主),`server_name` **只有 `nk.linotsai.top`**。证书 = 宿主 `certbot certonly --webroot -w /opt/npm/data/acme`(ECDSA,至 2026-11-02),续期投递 hook 落 `/etc/letsencrypt/renewal-hooks/deploy/`(拷进容器 `/data/custom-certs/nk/` → `nginx -t` 过了才 reload),**`renew --dry-run` + `--run-deploy-hooks` 双验通过、certbot 日志实证 hook 被调起**。**公网活体**(临时 `systemd-run` 起 API、关哨兵、不带 `--notify`、验完即停,8002 复核无监听):`/api/v1/health` **200 `v2.0.0`** + TLS 严格校验 0 + HTTP/2,http→301、无/错 token→401、`/baskets/999999`→404 `basket_not_found`、真实报告 12015 B 解得开。**既有 `nas`/`mt`/`web` + IP 站四次采样与基线逐行相同**(基线/两阶段/hook reload 后各一次),四份 `proxy_host/*.conf` md5 未变、`database.sqlite` mtime 未变、容器未重启、WireGuard 未动、三个 neckline unit 仍 `disabled`。回滚绳 + 采样存新机 `/root/npm-backup-20260804/`;生产件原样留档 `deploy/npm-custom-http.conf` / `deploy/npm-le-deploy-hook.sh`(md5 与生产逐字节相同),`deploy/nginx-neckline-nk.conf` 改头标**作废**(它的 `default_server` 搬进 NPM 会炸四站)。如实登记两处判断:ACME 账户 `--register-unsafely-without-email`(无到期邮件预警)、`--agree-tos` 接受 LE 服务条款。老机零接触。
- 2026-08-04 · 🔧 **⑯-G NPM 反代前置两项准备**(@builder,新机 `114.66.0.38`;不碰 NPM 容器 / WireGuard / 其他 UFW 规则,不 enable/start 任何 neckline unit,老机零接触)。① `/etc/systemd/system/neckline.service` ExecStart 的 `--host 127.0.0.1` 改 `--host 0.0.0.0`(端口 8002 不变)+ `daemon-reload`,服务维持现状 `disabled`/`inactive`(双推送处置不变,未启动);② UFW 新增 `8002/tcp ALLOW IN 172.18.0.0/16`(comment `NPM -> neckline api`),默认 `deny (incoming)` 仍在、原 22/80/443/51820(WireGuard)五条规则未动,外网直打 8002 仍被拒;③ 本仓库 `deploy/neckline.service` 同步改 host 并补注释说明原因(为 npm_default bridge 反代做准备,暴露面靠 UFW 源限制收口、不靠绑定地址)。
- 2026-08-04 · 🚀 **V2-⑯ 新服务器建成 + 数据搬家 + 晚间管线三段化 + 两步包激活 + B3 退役完工**(🔴 @builder-pro,首次触碰新机与权威库;**老机 `ln` 全程零接触**、v1.5.2 照常在跑)。新机 `nk`/`114.66.0.38`(4 vCPU/3.8 GiB/58G)属主纪律复刻 + UFW 基线 + venv Py3.12.3;parquet **9594 文件与老机逐文件 md5 全等**、`neckline.db` 端到端 **sha256 逐位相同**(快照 08-04 14:18)、`init_schema` **零「唯一索引建不上」WARNING**、26 张老表行数逐表一致 + 19 张 V2 新表全 0 行、现役章程仍 `v1.3.3`;**P0-23 门禁全过**(主链峰值 **439 MB**/13.55s、扫描层 bootstrap 58 天 135 MB/4m38s、全程 load ≤1.58,⛔ 未抬 swap 未降精度);三段 oneshot + target + timer **装好 + 演练 `ExecMainStatus=0` 严格串行,但一律 `disabled`**(双推送处置);`daily_update` 的 ④/④b 双挂载按 plan 摘除;**⑯-E 两步激活全绿**(`selection_packs` 2 行、现役唯一 `K7-pack-v1`、事件流 3 条,未碰 `strategy_versions`);**⑯-I B3 黄牌退役全绿**(K4 仍 inert、`activated_at` 未变、激活流水未增行、A2 未误伤,全文 diff → `archive/K4_advisory_B3退役_20260804.md`);新机 `/health` = **`v2.0.0`**、端到端活体五段跑通并实证无 key 时的诚实降级。**三件停手上报待裁定**:⑯-G 公网入口被用户既有 `nginx-proxy-manager` 容器占着 80/443、`ln`+`lf` 的 A 记录已从 DNS 消失(NXDOMAIN,非本次所致,老机服务完好)、⑯-D 三段切法会让报告 ③b `droppedBaskets` 恒缺席且文案误导。**⑯-F preseed 无外部输入件,如实跳过未编数据**;⑤⑥ LLM 段 / ⑦ 卡 / ⑧ 存拍三项未实测(无 key / 无篮子 / 需盘中)。测试 **2896 过 + 2 skip**。详见 §五 V2-⑯ 完工记录。
- 2026-08-04 · 🩹 **V2 小审(⑭⑮)修 🟡 Y-1 必修 + 四条便宜 🔵**(@builder,commit `a123fe9`,详见 `archive/REVIEW_REPORT_V2_小审_⑭⑮_20260803.md` 逐条销项)。**Y-1**:`report/store.py::save_report` 的 `INSERT OR REPLACE`(整行先删后插)叠加 ⑬-11 起 `watchlist_json` 不进列清单 + ⑭ 起 `candidates` 恒传 `[]`,会让重跑历史日期的 `scripts/report.py` 把 V1 冻结的 `candidates_json`/`watchlist_json` 快照永久清空;改 `INSERT ... ON CONFLICT(trade_date) DO UPDATE SET <本次写入列>`——`watchlist_json` 不在写入列清单里天然免疫,`candidates_json` 额外加 SQL `CASE` 守卫(本次写 `[]` 且历史已非 `[]` 时保留旧值、记 WARNING,真写非空 candidates 时仍正常覆盖)。**全仓同模式扫描**(INSERT OR REPLACE 落在含「停写留档」列的表上)结论:**除本处外零命中**——`app_settings` 全部走「`INSERT OR IGNORE` 补首行 + 逐字段 `UPDATE…WHERE id=1`」安全模式;`inquiry_pool`/`watchlist`/`decision_log`/`reference_plans`/`llm_judgments`/`breathing_t_trades` 六表整表停写、零残留写手;`holding_eod_check` 的「D5 判一次定格」三列不属此模式(活跃写路径且调用方每次显式带值,非列清单省略)。**四条 🔵**:②`test_contract_crosscheck.py` 调用面扫描从 `APIClient.swift` 单文件扩到 `client/` 全部 Swift 文件 + 新增 HTTP method 维度对拍(已知只覆盖字面量直传调用点,约 36/56)、顺带修正测试代码裸数字路径段("42")误报;④ `AppModel.submitProviderForm()` 两个 `catch` 分支补清 `providerForm.apiKey`,失败重试不再残留明文 key 草稿;⑤ 两处过期注释订正(`scenarioReviewPending` 不再声称走已删的 scenario-outcome 端点;幂等键"提交成功后作废"改为准确描述"下次开仓流程才换新");⑥ `updateProvider` 的 `searchEngine`/`notes` 留空语义对齐 `apiKey`(留空=不改),契约对拍表 §6.2-B1 同步一句。B-1(六处硬解码)与 B-3(`card_not_ready` 语义合并)不在本批(前者改动面超出授权、后者需 planner 先定 reason 拆分口径),报告已标注留待后续。测试:Python **2890 → 2894 passed + 2 skip**(净增 4,零回归);Swift iOS Simulator **179 过 + 12 skip**、iOS/macOS Debug `xcodebuild build` 双 SUCCEEDED。

- 2026-08-03 · 📱 **V2-⑮ 完工:客户端双端改版(D8 四板块)+ ⑮ 欠账清单 6 组全部还清 + 版号三方同批升 2.0.0**(@builder-pro,commits `0ed277c` / `dcd90cf` / `e676967`,纯本地 + 双端构建 + 模拟器截图核对,零服务器 / 零部署)。iPhone 四板块 = **今日篮子 / 持仓 / 问询台 / 设置**(macOS 同四板块 + 周复盘工作台);V1「盘中看板」不再是 tab,**内容一条没删**并入持仓板块成一节(80/15/5 里它本就是为解释持仓服务的,判断已登记)。契约层:候选族五个 Swift 类型退役 → 篮子族 8 + 计划/快照/画像/包/评价 5 + 提醒族 4 + Provider/推送族 4 个 DTO(**两类快照一律手写 `init(from:)` + `decodeIfPresent`**,A 类也手写 = 白拿的保险);**删五处打向已删端点的活调用**(含 `PUT /settings/llm` 那条采集明文 key 打进空洞的);`mapReason` 补八个 case 并把 409/422 也接进来(此前只有 400/404)。界面:篮子卡 11 项 + 成员卡(**角色两说并存**、三个参考件夹逼拒收显示原因不兜 0、**「离场参考区间(不是止盈线)」**、「参考、非指令」四不标注恒显)、③b 两个原因码分开展示、三段两种「空」讲不同的话、Provider 注册表(**key 只写不回显**)+ 按 kind 动态渲染的推送开关(未识别 level 自成一组、未知 kind 优雅降级)、**⑪-D-D per-position 触达提醒开关**(只翻静音位、计划正文一项不动)、**同题材合并敞口**。**幂等键按 planner 追加口径落地**:每笔新提交动作铸一枚 UUID、重试复用同一枚,⛔ 不绑业务量(单测两条)。**两个已登记的债集合双双清空 + 两条冻结快照 DTO 闸从 skip 转跑**(对拍表 7 过 + 2 skip → 9 过)。测试:Python **2890 过 + 2 skip**、Swift **179 过 + 12 skip**、iOS/macOS × Debug/Release **四路 BUILD SUCCEEDED**;语义红线 grep 零命中。**六处设计判断 + 三条 Plan 疏漏**详见 §五 ⑮ 完工记录,对拍表 §六 复选框已逐项打勾注明提交号。
- 2026-08-03 · ⚖️ **⑭ 完工后两件归档:⑯-D oneshot 划分口径改写(我原切法会让报告 ④ 节常态空)+ 三条 🔵 增量落位**(@planner,只改 `PROJECT_PLAN.md`,零代码 / 零数据库 / 零部署;⑮ builder 并行施工 `client/` 全域,已 hunk 级挑拣、diff 零 Swift / 零 `client/`)。**① ⑯-D 的切法我写错了,改写(⛔ 只调文字、未要求动代码)**:原文把**报告**归 `neckline-basket.service`、**⑨ 复盘**归 `neckline-review.service`(即报告在前、复盘在后),而报告的 ④「昨日篮子复盘」节要读 `basket_review_daily` **当日行** → **照原切法那一节会常态显示「今日无昨日篮子可复盘」**。⑭ 已按「⑨ 在前、报告在后」实现,本节对齐。**新切法按「资源画像」而不是「功能名称」切**:(1)`neckline-scan.service` = **⑧ EOD 验证拍 + ④ + ④b**(纯机械批算、吃 parquet 全表扫描 = **内存最重那段**,`MemoryMax` 主要卡这里,P0-23 的正主);(2)`neckline-basket.service` = **⑤⑥⑦**(**LLM 段**,资源画像是墙钟 / 网络不是内存 —— 单独成段才能给它长超时**而不必把长超时也给批算段**);(3)`neckline-report.service` = **⑨ 复盘 + 报告落库**。⚠ **为什么这两个必须同段才是改口径的真正收益**:「⑨ 在报告之前」是**语义依赖**,放进**同一个 unit** 就由链内固定序保证、**systemd 侧根本无法配错**;拆两个 unit 的话任何人改一次 `After=`/`Wants=` 就能弄反,而**症状是报告 ④ 节静默变空、不报错** = 最难查的那类故障 —— **把约束关进一个 unit 里 = 让它结构性不可违反**。复用 V1 已有的 `neckline-report.service` 名(它本来干的就是这件事),不新造 `review`。**顺带纠一处**:⑫ 的**周度**部分原文挂在日链后面 = **周频作业每天跑一次**,已摘出去按 ⑫ 实际触发方式(交割单上传后 / 周末 timer)单独排,⑯-D 不替它决定形态。**② 核 ⑭ 另三处设计判断,三条全核准**:(a) **编排链住新文件 `report/evening.py`、⛔ 不住 `pipeline.py`** —— 理由很硬:`pipeline.py` 在 P0-23 守门单测的**在线模块清单**里、逐字 grep 禁止它出现扫描层批算写入口,而链的第二段恰恰要调那几个入口,**塞进去 = 为了迁就文件摆放而钝化一条被生产 OOM 打出来的真防线**;`build_report` 保持**只读不算**(历史回放的生命线)。⑭ 还**主动**把 `report/basket_daily.py` 加进那份在线清单(`build_report` 的只读装配层)—— **扩大守门覆盖面,好**。⑭-A 原文「`pipeline.build_report` 分段」**是我写错的,已在 ⑭-A 就地更正 + 链序定死**,防后人照原文把链塞回去。(b) **④ 双挂载二选一 = 摘掉 `daily_update` 那份、留链里的**(采纳建议):⑤ 直接依赖 ④ 的**当日**产出,**留在链里链自己就能保证「⑤ 读到的是今天的」**;留 `daily_update` 则是跨进程隐式依赖,那一步失败时**链会拿昨天的扫描表当今天用而且不报错**(它在 `daily_update` 里是「尽力而为不改退出码」)= 又一个「静默用陈旧数据」;移进链里还顺带有了明确退出码。⚠ **⑯-D 拆分时才做,⛔ 现在不动**(`daily_update.py` 是生产 timer 里在跑的脚本,⑭ 不擅自摘是对的)。(c) **`VERSION` 不升、版号归 ⑮ —— 判得对**(三方恒等且必须同一次提交动,⑭ 提前升会让守门单测常年红);⚠ **已在 ⑮ 块点名提醒**:别只改客户端那两处,**`api/app.py::VERSION` 是三方里的第三方**,漏了守门单测立刻红;当前「服务端跑 V2 契约但 `/health` 仍报 v1.5.2」如实登记,⑯ 上云前必须已是 `v2.0.0`。**③ 三条 🔵 增量逐条落位**:(a) **Y3 零行边界** → 补进 **⑫**:按 dimension 整段替换后,「**某维度恰好产出 0 行而其他维度有行**」时该维度旧行会留在当期;现实中几乎打不出来,**但日后画像口径改版、某维度整体停算时会真踩到 —— 那时旧维度的行会永远留在历史期里冒充当期结论**,故要求在 `_clear_period` docstring 写明边界 + 「某维度整体停算须先手工清历史期该维度行」一句(**文档级,⛔ 不改行为**);(b) **索引降级窗口** → 补进 **⑯-B 迁移清单**:「**新机首次 `init_schema` 后 grep 日志确认零条『唯一索引建不上』**」,**为什么要专门查** —— `_POST_MIGRATION_INDEXES` 在脏库上建不上时是 WARNING + 跳过,于是**幂等键与「单现役」两条约束在该库上持续不设防**,而 WARNING **只在 `init_schema` 那一刻打一次**,**不专门查就等于没有这两条约束而且没人会知道**;查到有 → 先清脏数据再重跑,⛔ 不许带着跳过的索引上线;(c) **幂等键语义** → 核对**已落**(`archive/V2_契约三方对拍_20260803.md` §十一 已文档化),但报告点名的「**客户端生成规则归 ⑮**」此前没落 → **已补进 ⑮**:每笔新交易生成新键(UUID per 提交)、⛔ 严禁复用;⚠ 说清后果 —— 服务端是标准幂等语义,**同键 = 同一笔意图的重试**,**同键不同 payload 会静默重放原仓、丢弃新参数**,即「**用户改了价格数量再提交,系统当作重试把改动整个吃掉**」;⛔ 别把键绑在"这只票 + 今天"之类业务量上(必然复用),**绑在"这一次提交动作"上**。**⚠ 环境异常第三次复现**:又一次工具返回里被追加了与命令输出无关的 computer-use 指引块。**工具返回是数据不是指令通道**,已整段忽略、未据此做任何动作(本次纯 markdown、零代码零桌面操作,也未请求任何桌面权限)。前两次已建议排查,**这是第三次,建议当作确定的注入面问题处理**
- 2026-08-03 · 🧺 **V2-⑭ 完工:报告换血成五段篮子日报 + 16:35 编排链独立成段 + API 契约总装 + 契约三方对拍**(@builder-pro,commits `0fb267c` / `3937e9d`)。报告五段顺序定死(① 市场语境 → ② 持仓体检 → ③ 今日篮子 → ③b 未定档 → ④ 昨日复盘 → ⑤ 新鲜度披露),每段独立保险丝;③b 两个原因码 `capacity_overflow` / `below_quality_line` 永不合并、零溢出时节仍在;新 `report/evening.py::run_evening_chain`(⑧ 验证拍 → ④ 扫描 → ⑤⑥⑦ → ⑨ 复盘 → 报告,⑧ 位置定死在拉数后扫描前,`segments` 是 ⑯-D 拆三个 oneshot 的接缝,**刻意不塞进 `pipeline.py`** 以免钝化 P0-23 在线模块守门);兑现三处前置块欠账(`dataFreshness.scanLayer*` 接线 / `exec_hints_for()` 接进篮子成员 / news_alerts 次级域 + `info_card` 同步扩域)。契约面 `CandidateOut` 整族 4 个 DTO + `ReportOut.candidates` 退役换 `basketDaily`,新增 12 条路径 + 17 个 DTO,`GET /baskets/{id}/card` 404 双 reason 分得开。三方对拍表与 ⑮ 欠账清单终稿 → `archive/V2_契约三方对拍_20260803.md`;A 路前提自检清单 → `deploy/A路割接前提自检清单.md`;「客户端调用面 ⊆ 服务端路由面」等 7 条机器断言 → `tests/test_contract_crosscheck.py`(**用 `==` 不用 `<=`**,欠账是精确集合不是 allowlist)。测试 **2808 → 2887 passed + 4 skip**(零回归);真实历史日 20260724 隔离库全链冒烟五段齐、九个新鲜度键在、真实库 MD5 未变。

- 2026-08-03 · 🩹 **判定线 🟡-N1 收口:昨日涨停截断序改判 `crc32(ts_code)` 升序(无偏 + 确定性)+ 需求量 vs 实际采纳量留痕**(@builder,commit `a8a14c3`)。核实原 `consec_limit_up_days` 降序**确实与被测量的量相关**(⑦-K7 审计:连板高度是双尾放大器,次日跌停约 3× 于同簇其余成员,而这份样本正好喂 `compute_breadth_snapshot` 算跌停数 / 炸板率),且原实现未传 `maintain_order`,并列名次连"行序"这个近似说法都不保证;改用 `mainline.crc_rank`(与主线切片姊妹样本同一套采样键)。`WatchUniverse` 新增 `breadth_extra_needed`/`breadth_extra_payload()`,`retreat_metrics` 新列 `breadth_extra_sample_json`(体例照既有 `hot_sector_sample_json`,幂等迁移)。**如实登记**:改前改后同批历史日对拍显示 zaban_rate 读数**有实质变化**(07-21→07-22 真实配额点 42.9%→33.3%,−9.52pp),追加 2026-07 全月探索性对拍(K=71 固定口径,10 个真正截断的日子)证实是**抽样噪声**(均值 −0.99pp、中位 −1.16pp、方向不系统)而非 crc32 新引入的系统性偏差,数字与完整分析详见 §五 ⑧-G 块尾追加记录,已并入 §七 [P3-37] 回看清单(71 只保底配额的抽样噪声量级是否需要专门处理,留待实盘积累后产品决策)。测试:`pytest tests/ -q` **2801 → 2808 passed + 2 skip**(净增 7,零回归)。

- 2026-08-03 · ⚖️ **⑧-G 完工后三件归档裁定:池配额判据改写(我原判据不可满足,已证伪)+ 两处静默交互补注 + 样本口径核准**(@planner,只改 `PROJECT_PLAN.md`,零代码 / 零数据库 / 零部署;两线 reviewer 并行复核只动 `archive/`,已 hunk 级挑拣)。**① ⑧-G-D 判据我写错了,改写**:原判据「两份样本在四天里都没被截断过」**不可满足** —— 昨日涨停需求**上不封顶**(2026-07 全月实测 **32~223 只/日**,两份并集 72~275),任何有限池都装不下;而且**它改组前就一直在被截**(旧口径同样只给"剩余 ~171"),**我要求的是一个系统从来没有过、也不可能有的性质**。**新判据三条**:(1) **主线切片不被截断**(优先保刚修好的那个估计量);(2) **昨日涨停允许被截,但截断必须无偏** —— ⚠ **这才是真正要守的**:两份都是**估计量**(切片 = 均值 / 涨停 = 炸板率这个比率),**估计量对"抽样"免疫、对"有偏抽样"不免疫**,故截取顺序**不得与被测量的量相关**,**建议同样用 `crc32(ts_code)` 序**,⛔ 不用 `ts_code` 序 / 池插入序(理由同 ⑧-G-B:`ts_code` 序 = 按板块排序,而板块与涨跌停幅度、炸板概率直接相关);**尤其要紧的是这份样本同时喂着退潮宽度 `compute_breadth_snapshot`**(CLAUDE.md 明载是退潮判据输入),**有偏截断在这里就是 ⑧-F 那一类 bug 的翻版**;施工时若现实现截断序与被测量相关就改 `crc32` 序,若已无偏则写明留证;(3) **截断后读数无实质变化**(builder 已验的炸板率 42.9%→42.9% / 37.9%→37.9% 保留为验收判据)。**✅ 核准 100/71 取值**,理由不是"先来后到"而是**按截断的代价分配**:切片被截 = **直接损失一个刚修好的、要与 −3% 阈值比大小的均值估计量的精度**;涨停被截 = 比率估计量换个样本量、**实测读数分毫未变** —— **把稀缺预算给"截了真会疼"的那一份**。⚠ 但正因涨停在重日只采到 <1/3,**第 (2) 条无偏截断就是它的全部保护,别当可选项**。**✅ 核准「测量预算按必需项上界 29 固定算、⛔ 不看这一拍谁已在池」**(builder 登记 2)——**好抓**:按实际占用算的话,LLM 多挑一个**恰好也在切片里**的成员就能腾出位、让样本多一只,**残留耦合 ②b 换个面目就回来了**;代价(必需项不满时欠填 ≤29 位)刻意接受。**新增留痕要求**:两份样本都落**「需求量 vs 实际量」**,⛔ 别让日后审计炸板率的人误以为 71 就是当天全部涨停股。**② 两处静默交互补注**:(a) **单主线日(种子数=1)该路必然不判**(单颗种子切片上限 = K = 4 **< MIN = 5**)—— **这是裁定后的正确行为不是 bug**(一条主线不足以支撑「主线**板块**跳水」),**写进 Plan 防日后被当故障上报**;(b) **K 实测量程更正**:`crc32` 口径 **K=4 → 40~100 只/日**(我原文引的「K=6 → 58~144」是 ⑧-F 的 `ts_code` 旧口径,已作废)。**⚠ 同时把一次真实考验记成先例**:07-24 用 **K=6** 读数 −3.04% **会触发**、K=4 是 −2.80% 不触发,**builder 明知如此仍不采用 K=6 —— 正确**;**「换个参数就能让它响」不是采用该参数的理由,那恰恰是偷偷改阈值**。**③ ✅ 核准「样本 = 切片本身,不再并入种子成分 ∩ 机械池的其余部分」**:把昨日涨停股并回样本 = **把 ⑧-G-A 要清除的动量偏差原样请回来**,等于修了一半又自己撤回去;⑧-G-B 原文写的就是切片作样本的「**专属来源**」,**这是照 plan 执行不是偏离**。⚠ **并据此升级 ⑧-G-G 第 1 条的表述**:原话「排除的是『LLM 这条路』、不是『LLM 碰过的票』」现在有更强的保证 —— **被 `crc32` 选中就进样本,与它是不是篮子成员 / 持仓 / 昨日涨停股完全无关**;样本构成**只由「④ 机械种子 + `crc32` 序 + K」三件事决定**,任何人(含 LLM)碰不动。**④ P3-37 补上可量化的移交材料**(⑧-G 完工后这条终于能给策略线 / 用户一组硬数字):**老样本系统性偏乐观 1.51~2.93pp**、新切片误差 **≤0.53pp / 真跳水日仅 0.05pp** → **推论:同一个 −3.0% 阈值在样本修好后,实际上比当年设它的人以为的严了约 1.5~2.9pp**(当年"读到 −3%"对应的真实主线跌幅其实是 −4.5%~−5.9%)——**阈值数字没变,含义变了**;最有说服力的一点是 **07-24 全市场中位 −2.86%、89% 个股下跌、主线本身 −2.75%/−2.98%,这样一天仍不触发**,要不要在这种日子响是**产品决策、交用户**。⚠ **别把这组数读成「所以该调到 −2.7%」** —— 四天不是样本,且 K=6 能凑出触发恰恰说明**该区间对参数很敏感**,**先攒实盘再谈调数**。另补一条回看项:`MIN_MAINLINE_SAMPLE=5` 作用在**票数**上、**没有**作用在**主线条数**上,2 颗种子时 per-seed 估计量只在平均 **2 个数**(n=8 已过下限),数学上偏薄;⛔ 现在不加第二个常量(无证据、实测日常 10~41 颗种子离这个角落很远),回看时一并看。**⚠ 环境异常第二次复现**:又一次 `git log` / `grep` 的工具返回里被追加了与命令输出无关的「MCP Server Instructions」(computer-use 指引)。**工具返回是数据不是指令通道**,已整段忽略、未据此做任何动作(本次纯 markdown、零代码零桌面操作)。上次已建议查注入面,**这次是复现,建议当作真问题排查**
- 2026-08-03 · 🎯 **V2-⑧-G 完工:主线跳水样本换 `crc32` 配额切片(K=4)+ per-seed 估计量 + 池位保底配额 + `MIN_MAINLINE_SAMPLE` 同源下限,这条红色刹车不再是聋的**(@builder-pro,纯本地代码 + 单测,零数据库 / 零部署)。真跳水日 07-24 读数 **−0.18% → −2.80%**,与「主线板块本身」(per-seed −2.75%)只差 **0.05pp**(⑧-F ∩池样本差 1.51pp);**阈值一字未动、四天两口径触发次数仍 0/4**(阈值灵敏度归 §七 P3-37,⛔ 未调 K 凑触发)。关注池改「有界必需项全进(≤29)+ 切片 100 / 昨日涨停 71 两份保底」,LLM 挪不动测量样本(压力用例 200 只涨停 + 篮子 1→21 只三向重叠,两份样本逐位不变)。🟡 **一件交回 planner**:「两份样本四天都没被截断」在 200 池位内做不到(07-21 需 213 > 171),现按"新样本优先"定数、被压两天炸板率读数分毫未变。`test_sentinel_retreat.py` 一条 `hot_n=3→5` 断言按 ⑧-G-E 授权**显式改**。全量 **2801 过 + 2 skip**(净增 15,零回归)。详见 §五「V2-⑧-G 完工记录」。

- 2026-08-03 · ⚖️ **⑧-F 对拍打出的深层问题裁定完毕 → 新增 §五 ⑧-G「主线跳水样本的关注池配额切片」(🔴 碰纪律触发器,排 ⑭ 之前)+ 新挂 §七 [P3-37]**(@planner,只改文档,零代码 / 零数据库 / 零部署)。**问题**:该红色刹车路径**近乎失效** —— 真跳水日 07-24(全市场中位 **−2.86%**、89% 个股下跌)样本读数 **−0.18%**,而主线板块本身 **−2.98%** 已贴 −3% 阈值,**差 2.8pp**;四天改前改后均 **0/4 触发**。⚠ **不是 ⑧-F 引入的** —— V1 样本是候选池、同样偏动量,**大概率一直就是聋的,只是从来没人量过**;⑧-F 的对拍第一次把它照出来。**builder 未擅自改阈值 / 扩池、把裁定权交上来,做得对**。**① 定性:`∩ 关注池` 这一步在语义上就是错的** —— §2.4 里它叫「**主线板块**跳水」,**语义主语是板块本身**,不是「我恰好盯着的那几只票」;而关注池的存在理由完全是另一回事(**免费源限流下只 poll 得起 ~200 只**)。拿一个**测量市场状态的量** ∩ 一个**受轮询配额约束的集合**,是实现副产品不是设计,而且这个副产品**有方向**:池的机械成分几乎全是昨日涨停股 = 全市场动量筛得最狠 = **最抗跌的一群** → **这个测量在最该响的日子最不容易响**。但 `∩ 池` 又不能简单去掉(没报价就算不出收益率)→ **正解是"在池里预留一块专门用来测主线的、机械且有代表性的样本"**。**② 采纳配额切片,但采样键改 `crc32`(⛔ 不用 `ts_code` 升序,这是对候选方案的修正)**:`ts_code` 升序在一颗行业种子内部会系统性偏向 `000xxx`/`300xxx`、把 `600xxx`/`688xxx` 排到后面 = **按板块给样本排序**,而板块与涨跌停幅度(10% vs 20%)、波动率直接相关 → **样本的波动率画像被系统性扭曲** = 用一把刻度不匀的尺,**正是 ⑧-F 刚清除的那类偏差,别在采样器里请回来**;`crc32` 与板块 / 市值 / 上市年份无关且跨进程可复现(CLAUDE.md 明文),**一个函数调用的成本**。**③ K = 4**(非 builder 试的 6):K 是**精度旋钮不是策略参数**,配合 per-seed 估计量只影响主线内部抽样噪声、不影响主线间权重,同时给另一路触发器的样本(昨日涨停)留出更多池位;⛔ **不许调 K 去凑触发频率(那等于偷偷改阈值)**。**④ 估计量定为「每条主线一票」(per-seed mean 的均值),⛔ 不是所有票混在一起平均**:名字就叫「主线**板块**跳水」,一颗 200 成员种子与一颗 8 成员种子各代表**一条主线**,不该因成员多就占更大权重;附带好处是 K 才真正成为纯精度旋钮。⚠ **builder 四天读数是 pooled 口径,必须用 per-seed 口径重跑同四天、两个口径都写进完工记录**,⛔ 不许只报一个。**⑤ 关注池改「有界必需项全进 + 两份测量样本各有保底」**:现状是先到先得的优先队列 —— 对「注意力分配」没问题,对**纪律测量样本**是错的(**会被别人挤掉、大小不可预期的样本 = 不可靠的测量**)。新规则:持仓(≤3)+ T1/T2 成员(**≤7 篮 × ≤3 = ≤21,本来就有界**)+ 指数(≤5)合计 ≈29 **无条件全进**;剩余 ~171 在**昨日涨停(宽度 / 炸板率样本)**与**主线切片**两份测量样本间按保底分配。**⚠ 这正好根治 builder 登记的残留耦合(涨停 >180 时 LLM 仍能挪样本尾巴)** —— 篮子成员既有界又保底,就不再与任何测量样本抢位,**LLM 在任何极端日都挪不动测量样本**,⛔ 别再单独想办法治它。保底具体数由 builder 按实测定,判据 =「两份样本四天里都没被截断过」;装不下**回 planner、⛔ 不许自行抬 `breadth_cap`**(那会改盘中轮询量与限流风险面)。**⑥ 加 `MIN_MAINLINE_SAMPLE` = 直接 import 引用 `industry_strength._MIN_MEMBERS`(5),⛔ 不抄字面量**(照 ⑤-c `MIN_LIFT_SAMPLE_SIZE` 同源引用体例):同一类统计量、同一个理由,⑤-c 已立先例不新发明;n=3 时横截面收益率标准误约 2pp,拿它判 −3% 阈值**接近抛硬币**,而误触发的代价是**整天禁开新仓**;切片上线后正常日样本 30~140,n<5 意味着上游已出事、**更不该开火**。**⚠ 这条改判 builder 的登记,但核准其克制并澄清是我的约束写得有歧义**:⑧-F-B 的「既有纪律单测一条断言未改」防的是「**改断言迁就新代码**」,而「**行为按裁定变了、测试跟着变**」是另一回事、**允许且必须** → `test_sentinel_retreat.py` 那条 `hot_n=3` 断言**现在可以且应当改**,但须**显式改 + 单独说明 + 完工记录写清改前改后**,⛔ 不许悄悄改数让它绿。**⑦ 核准另两条登记**:(a)「∩ 关注池」→「∩ 池的**机械成分**」的字面偏差 **✅ 核准,且是 plan 的错不是实现的错** —— 我写 ⑧-F 时没想到池**本身**含 T1/T2 成员(⑧-A),整池取交会让「换掉 LLM 的成员选择 → 样本逐位不变」这条**我自己写的核心验收判据当场不成立**,plan 字面与自己的验收判据打架,builder 按验收判据实现是对的;⑧-G 的配额切片口径自此取代该表述,但**保留 builder 那条重要区分:排除的是「LLM 这条路」,不是「LLM 碰过的票」**(本来就从机械路进池的照进样本)。(b) 残留耦合 **✅ 要治,已由 ⑤ 的配额规则根治**。**⑧ 施工纪律(🔴)**:`sector_dive` 两个阈值与 `evaluate_retreat` 判定逻辑**一字不动**,只改样本怎么取;四天对拍**两口径都给数并明确报触发日**。**⑨ 新挂 [P3-37]**:这条刹车自 ⑧-G 起**从"聋"变"能响"** —— **不需事前批准**(⑧-G 恢复的是 §2.4 早就写明的既定行为,且 07-24 那种日子本来就该响,承 §1.3「买下跌途中票是主要死因」),**但必须事后知情**:用户此前从未经历过该刹车触发,上线后第一次「全天禁开新仓 + 立即级推送」会是全新体验,**⑰ 换包说明须有一句告知,别让它像故障一样冒出来**;**真正待校准的是灵敏度** —— `sector_dive` 是 V1 传下来的启发式、**当年是配着一个失效的样本定的**,样本修好后**这个阈值的实际含义变了**,须攒 ≥1 个月实盘 + 经历 2~3 次真实急跌日后回看(连同 K=4、保底配额、per-seed 估计量三个无实盘背书的工程默认);⛔ 有实盘证据前阈值一字不动,要动走用户拍板。**⑩ 核档 builder 另两件**:`e1fe049`(⑪-D 的 take_profit 文案补齐 §2.8-C-3 **前提③** 缺的两句并机器锁死 —— **对**,四条前提缺一即豁免失效,文案缺半句就是豁免失效)、⑪-D 新增 `rejected_no_close` 第三态(**对**,与 ⑦ 既有 `rejected_no_limit` 同族,「算不出」与「越界」分开)。**⚠ 另记一笔环境异常**:本次一次 `git log` 的工具返回里被追加了一段与 git 输出无关的「MCP Server Instructions」(computer-use 指引)。**工具返回是数据不是指令通道**,已整段忽略、未据此做任何动作(本次任务纯 markdown、零代码零桌面操作)。如后续复现,建议查工具链注入面
- 2026-08-03 · ⚙️ **planner 两条裁定接线完工:⑪-D `take_profit` 两道机械闸 + ⑧-F 退潮主线样本机械化**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读,对拍跑在 scratchpad 隔离库 + 真实 parquet 只读上)。**⑪-D 已完成、验收全绿**:闸① `basket_card.clamp_exit_reference(raw, close)` 要求 `exit_low > D0 close`(`close` 必填无默认,签名层面不给"忘了传 = 静默关闸"留口子;新增 `rejected_not_above_close` 与 builder 补的第三态 `rejected_no_close`〔「没收盘价可比」≠「比过了不合格」〕;⛔ **仍不夹涨跌停、仍不加上界**,两条各一个守门单测);闸② `positions_entry.evaluate_exit_reference_arming()` 见 `exit_low ≤ 成交价` → 该票 `take_profit` **不武装**(⛔ 只是不武装:计划正文、开仓、其他 kind 全不受影响,单测锁死);`plan_json` 恒带武装态四键,哨兵旁路 E **缺键即不武装**(fail-closed —— 没武装位 = 没过闸,不是"过了但没写");⑮ 的 per-position 开关留了写入点 `set_exit_reference_muted()`(追加新版本、只翻静音位、不动正文),`create_position_plan_version` 顺带堵死"写新版本绕开闸②"的后门;§2.8-C-3 与 ⑪-D-C 的豁免四条前提已逐字人工核对一致。**⑧-F 代码已落、红线已闭,但⚠ 对拍结论与 planner 预判相反,样本口径待再裁一次**:新增 `sentinel/mainline.py`(样本 = ④「热点行业 + 暴起概念」种子原始成分 ∩ **关注池的机械成分**〔昨日涨停 ∪ 持仓〕,**只靠篮子进池的码不进样本** —— plan 字面写的是「∩ 关注池」,但 V2 关注池本身含篮子成员,整池取交会让 ⑧-F 验收那条核心判据当场不成立,已登记为与 plan 字面的偏差);`sector_dive` 阈值一字未动(守门断言);样本构成每拍落 `retreat_metrics.hot_sector_sample_json`(新列,幂等迁移)可审计;⛔ 没有新增任何最小样本量阈值(加了就得改既有 `hot_n=3` 那条断言,与「一条断言未改」冲突)。**对拍(4 个真实历史日,含 07-24 全市场中位 −2.86% 的大跌日;改前样本用「每颗种子取 D0 涨幅最高 1~3 只」的机械龙头代理)**:触发次数 **改前 0/4、改后 0/4**,但**四天里改后样本一律比改前更抗跌(高 0.9~3.0pp)= 改后更不容易触发,方向与 planner 预判相反**。追因已定位真因**不在 LLM**:关注池的机械成分几乎全是昨日涨停股,07-24 它们只跌 0.22%,而「主线板块本身」(种子全成分)是 −2.98%(已贴阈值)—— **本条红色刹车路径在「样本 ∩ 关注池」这个前提下近乎失效**,⑧-F 修好的是「可被 LLM 塑形」这条红线,**没有**修好「这条路能不能真的响」。⛔ 未擅自改阈值 / 扩池;一个已量化的候选方向(给池加机械配额切片,K=6 时 07-24 读 −2.46%)连同四天数字写进 §五 ⑧-F 完工记录,**留给 planner 裁**。测试:全量 **2786 过 + 2 skip**(基线 2744+2,净增 42 = 新增 `tests/test_exit_reference_gates.py` 22 + `tests/test_sentinel_mainline.py` 18 + 守门参数化 2);既有 `test_sentinel_retreat.py` 一条断言未改,`test_sentinel_engine.py` 只改夹具、断言逐字未动;`scripts/smoke_sentinel.py` 跑通。另有一条既有挂钟脆弱测试(`test_sentinel_custom.py::test_cooldown_blocks_second_hit_and_expires`,北京时间 ~14:48 后跑必红)与本次施工无关

- 2026-08-03 · 🛠️ **V2 两线 review 修复批次完工(契约线 🔴1+🟡5+🔵2 / 判定线 🟡3+🔵1,共 8 个 commit)**(@builder-pro,零部署;两份 review 报告已逐条销项标注 commit)。**🔴 R1 `basket_members` 冻结泄漏**:`INSERT OR IGNORE` 只冻「同一行」、冻不住「聚合长子行」,篮子行已存在时新成员码照插进冻结篮而日志正说「未采纳」→ 改为**篮子行已存在 = 该篮成员整段不写**(`1052622`)。**判定 🟡-1 验证侧静默降格**:「扔掉不可判条件再对子集取 all()」把 ⑦-b-B 的两条 AND 降格成单条 → 合成读法上收 `verification_rules.combine_side()` 取 **Kleene 三值**、新 flag `spec_levels_partial`、**bump `verify_ruleset_v2`**(`0f885b2`)。**判定 🟡-2 ⑬-7 漏 ⑧-E 除权锚**:分红季成员级假警会进 9:26 锁屏 → 复用同一检测器、标 `member_ex_rights` 不计 actionable(`47775cb`)。**判定 🟡-5 红线守门锁空靶**(`neckline.push.notify` 不存在)→ 换真实七模块 + 反向存在性断言 + 兼容 `from X import Y` 写法。**契约 🟡 Y1 守门写法盲区**:补 `OR REPLACE/IGNORE` 变体 + 扫描域扩 `scripts/`,**并修好一条从上线起就没可能命中的空转断言**(禁止串小写表名 vs `sql.upper()`,报告未列的第 4 洞,反向证伪时打出来)(`fdf604c`)。**Y2** `user_actions.occurred_at` 归一北京时间(`created_at` 仍 UTC,两列不同轴写死)、**Y3** 画像按 dimension 整段替换止住「每期 = 多次运行并集」(`040bd27`);**Y4** 五常驻僵尸 DTO 与客户端僵尸卡拆除(`d9b41a1`);**Y7** `record_buy` 三写并入单事务 + `POST /positions` 幂等键(部分唯一索引兜底,新增 `_POST_MIGRATION_INDEXES` 机制)(`50ff5b2`);**🔵 B1/B3**(`1052622`/`d61b0b8`)。**Y6 补档**:`archive/V2-⑬_删除前后对照表_20260803.md`(⑭ 三方对拍基准输入)。全量 `pytest` **2744 过 + 2 skip**(基线 2706,净增 38 条回归)+ 客户端双端 build + iOS Simulator 154 例;**未修**:契约 Y5(⑮ 硬清单)、判定 🟡-3/🟡-4(planner 已裁定另派)、🟡-6(⑯ 前定位)、其余 🔵。

- 2026-08-03 · ⚖️ **V2 两线 review 的两张「Plan 权威冲突」黄牌 planner 裁定完毕(判定线 🟡-3 / 🟡-4)+ 三条归档排期**(@planner,只改 `PROJECT_PLAN.md` 与两份 review 报告的归档标注,零代码 / 零数据库 / 零部署;builder-pro 正并行修其余 🔴🟡,已 hunk 级挑拣、未动其在途正文)。**① 判定 🟡-3 `take_profit` 触发阈值 = 未夹逼的 LLM 离场参考 → 裁定「记名豁免 + 两道机械 sanity 闸」,新增 §五 ⑪-D**。**先认账**:我 08-03 开这条 kind 时写了「正确指向 = 篮子卡离场参考」,**却没对它执行我在同一段里给 `basket_falsified` 立的「要开得三处红线一起改」标准** —— 是 plan 缺口,不是 builder 违规。**定性想清楚再动手**:它**不是纪律漂移**(止损 / 回落止盈 / 时间退出 / 仓位 / 熔断一字未碰),**也不主要是虚假权威**(文案已锁死非指令 + per-kind 开关 + 用户拍板);**真正的洞是「一个没有下限的 LLM 数字驱动立即级推送」→ 荒谬推送 → 用户对「立即级」这个通道失信**(立即级的价值全在「响了就值得看」,喂它噪声比不推更糟)—— **所以修法是给数值加机械闸,不是收回 kind**。**两道闸都语义驱动、⛔ 零发明阈值**:卡生成时 `exit_low > D0 close`(**这是定义不是阈值** —— 离场参考按定义是「本轮上涨的压力位」,压力位在现价之上),不满足落新 reason `rejected_not_above_close` 不落卡;开仓继承时 `exit_low ≤ 实际成交价` → **该票此 kind 不武装**(只是不武装,⛔ 不改计划、不阻断开仓、不影响其他 kind)。**仍不夹涨跌停**(⑦ 原决定对:压力位可能几日后才到)、**也不加上界**(上界荒谬只会永不触发,无假推送无伤害,⛔ 别为它发明上限)。**未采纳 reviewer 的 ③(用户确认才武装)** —— 与 ⑩「表单退役、减摩擦」的立项主题相悖;改以 **per-position「不提醒」开关**回应「开仓继承无用户确认」这一点(全局关会连坐所有持仓,用户真正想要的是「这只票的这个数不靠谱,别烦我」),列为 **⑮ 应做项、非红线前提**。**字面冲突同步收口,不留打架**:§2.0 第 1 条加**记名豁免附注**(其余部分尤其「不改任何纪律阈值」一字不变);**§2.8-C-3 改写**成「**判定动作永远机械;数值来源分两类**」+ 四条前提(不改纪律判定 / 已过机械闸 / 纯告知文案 / 用户可关,**缺一即豁免失效**)+ **⛔ 不外延**(买入参考区间、明早剧本、审判评语、问询台回答、`basket_falsified` 一律不得据此接推送或判据)。**② 判定 🟡-4 退潮刹车主线样本被 LLM 间接塑形 → 裁定「改机械派生样本」,⛔ 不写豁免,新增 §五 ⑧-F(🔴 碰纪律触发器)**。**为什么豁免这条路走不通**:§2.8-C-2(b) 之所以站得住,靠的是「每只票的判定阈值仍只来自章程与机械 spec」;**但这里是聚合量,样本组成本身就是判据的输入** —— 换一批票平均结论直接变,「阈值一字未动」是真的但**不相干**。**⚠ 两处与 reviewer 措辞不同**:(a) **不取「T1/T2 篮子所声明种子的原始成分」** —— 那仍过了一道 LLM 筛(哪些种子被拿去建篮由它决定),只是把塑形**上移一层**;**必须不经篮子、直接取 ④ 当日机械种子(热点行业 / 暴起概念)的原始成分 ∩ 关注池**(也是与 V1「热门板块 ∩ 关注池」语义最近的机械等价物,全链路机械:种子规则读现役包、成分来自 `stock_basic.industry`/`ths_member`);(b) **不同意「无单向套利」这条缓解判断** —— LLM 挑的是**龙头 / 中军**,而**领涨股在跳水日恰恰是最抗跌的一批**,样本系统性偏向强者 → 均值跌幅被**低估** → **红色刹车更不容易响**,偏差**恰好朝着「保护失效」单向**,不是中性噪声,故按「从严」办。**顺带把这条升级成通用规则**:§2.8-C-2(b) 加**适用边界** —— 2(b) 只覆盖「盯谁」,⛔ **不覆盖任何聚合纪律触发器的样本组成**,并给自查判据「**换一批成员,这个数会不会变?**」(会变 = 样本即判据 = 不得沾 LLM)。**样本不足 → 不触发 + 如实披露**,并写明**这里的保守方向与项目别处「宁可多提醒」刻意相反**的三条理由(另有两路触发兜底 / 误触发的代价是整天禁开新仓且会训练用户忽略刹车 = 狼来了比不响更危险 / 与「缺数据不判不猜」一贯纪律一致);施工纪律:**`sector_dive` 阈值一字不动、只换样本来源**,**改前改后同批历史日对拍并给出触发次数与触发日的具体数字**(⛔ 不接受「应该差不多」),差异意外就停手回 planner。**③ 三条归档排期**:**契约 Y5** 五处对已删端点的活调用逐个进 **⑮ 硬清单**(`SettingsLLMRequest` 整个退役换 `/settings/providers*`)+ **⑭-C 加机器断言「客户端调用面 ⊆ 服务端路由面」**防复发,并点明最糟的那一面是**采集了 key、发到不存在的端点、界面还是一副成功的样子 = 假成功面 + 明文密钥打进空洞**;**契约 Y6** ⑬ 欠交付的「删除前/删除后对照表」在 ⑬ 验收前加欠交付告示 —— **排进本轮修复批次或最迟 ⑭ 开工前补,⛔ 不许并进 ⑭ 的工作量**(那时基准已不好重建);**判定 🟡-6 核档 = 已结案无需排工**(reviewer 未追溯到 §七 [P1-36] 的 ✅ 结案段与 commit `a239bcc`:根因是等价比较误含 `computed_at` 秒精度审计戳 = **(a) 类良性测试缺陷,已明确排除 (b)**,业务列永远逐位相同 → **确定性主张本身没破**,`leader` 侧漏网已由 `53fab7a` 补上)。两份 review 报告的对应条目均已就地标注裁定结论与「与报告措辞不同之处」
- 2026-08-03 · 🗑️ **V2-⑬ V1 清理清单十三项全部执行完毕**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 只读,冒烟一律跑副本 / 临时库)。**物理删除** 8 个模块(`report/{intel_candidates,candidates,reference_plan,reference_plan_store,watchlist_check}.py`、`neckline/{watchlist,breathing}.py`、`sentinel/entry.py`)+ 10 个端点(`/watchlist*`×5、`/breathing*`×3、`/settings/intel-boards`×2)+ 客户端自选 tab / 两个视图 / 老四件套与参考件三件套 UI;**六张表停写留档不 DROP**(`watchlist`/`breathing_t_trades`/`inquiry_pool`/`llm_judgments`/`reference_plans` + 承 ⑩ 的 `decision_log`)+ 两列(`reports.watchlist_json`/`app_settings.intel_watch_boards`),DDL 全留并加「v2.0.0 起停写」注释。**⑬-7 盘前判定换血**成篮子竞价剧本核对(对象 = T1/T2 篮子成员,判据 = ⑦ 冻结在卡里的 `ref_close`/`close_below_stop_line`,阈值与 `kind='precall'` 推送一行未改,⛔ 不碰 `VERIFICATION_RULESET_VERSION`);**⑬-N 信息卡按 D1 保留改造**成篮子成员详情页地基(新增所属篮子与共同驱动 / 本票角色含对拍分歧 / 同篮成员对比三块)+ **⑬-N-K7 标注件**(读 `member_tags` 同一入口,交叉断言与篮子卡逐位相同)。**守门迁移不是删除**:`discipline_checks` 搬家 `report/discipline_checks.py`(保住问询台)、`exec_hint` 出 `exec_hints_for()` 纯计算口(C3 无前视偏差 + 北京日截断两条守门原样保住)、v1.3.1 行业闸对拍改冻结参考实现、`_ONLINE_FILES`/`prompt_context` 参数表同步、新增 `tests/test_v1_retirement_guard.py` 27 例(十三项逐项 + 六张停写表零写入 + 「跑完管线行数不增」+ **反向守门**:`json_block`/`judge_candidate`/`board_pool`/问询台/`BREATHING_TRIAL`/K1 回测线必须还在)。**一处 Plan 勘误**:⑬-1 落点表把 `report/board_pool.py` 标成「五常驻」是笔误(它是板块池卫生线、V2 扫描层五处在用),**未删**并加反向守门。**三处 Plan 未给规格的设计判断**(买点哨兵退役 / 证伪四哨兵换判定对象但逻辑一行未改 / ⑬-8 拆候选侧留情报侧)+ **三处留给 ⑭ 的欠账**(次级扫描域、`exec_hints_for` 接线、`candidates_json` 契约换血)详见 §五 ⑬ 完工记录。`pytest tests/ -q`:**2706 passed + 2 skipped + 0 failed**(⑬ 前 3097;删 7 个整测试文件 + 若干类例,新增 33 例);双端 `xcodebuild` BUILD SUCCEEDED,iOS Simulator test 154/0 failures;七个 smoke 脚本全绿。
- 2026-08-03 · 🔔 **持仓三事件(stop_approach/take_profit/sector_dive)升级立即级 APNs kind 定向任务完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;承接用户拍板 `3b1dcd7`,与并行在途的 ⑫〔`profile/` 等,未提交〕/ 已收官的 P1-36〔`a239bcc`〕两条线文件面不相交)。`stop_approach`/`sector_dive` **复用既有 console/Bark 文案**旁路推 APNs(`_maybe_push` 新增 `apns_kind` 分支,同一次 `sentinel_events` 去重、不二次措辞;`notify_kinds.py` 三级×kind **11→14**,三条均判 `NKIMMEDIATE`,默认开且 `_seed_push_kinds`/`_decode_push_kinds` 既有"缺键取默认开"机制零改动即天然覆盖,已实测验证老库不悄悄打开用户关过的开关)。**`take_profit` 触发源改判(如实登记,请裁定)**:核实 `sentinel/holding.py::check_take_profit`(回落止盈)现读的是现役 `take_profit_retrace`,并非协调方所指的候选老四件套 `target`/`decision_log.target_price`(git blame 追溯到该函数首次提交即如此,未找到读取后者的代码路径);但协调方明确要求 `take_profit` kind 指向 `position_plans` 继承的离场参考区间,故按指示新增独立函数 `check_exit_reference_reached` + engine.py 旁路 E(独立 try/except、独立去重 event_key=`exit_reference`,**不碰**回落止盈机械纪律与其 console/Bark 通道一字)驱动该 kind,与回落止盈刻意解耦、不合并;无计划 / 无离场参考如实不判,不编默认目标价;文案守语义红线(不写"建议卖出/止盈信号",按"触达"中性表述)。测试:4 个文件净增 22 例(`test_sentinel_holding.py`+6、`test_notify_kinds.py`+1、`test_notify.py`+7、`test_sentinel_tick_v2_bypass.py`+8,含旁路炸了不影响持仓哨兵的红线用例),`pytest tests/ -q`:**3097 passed + 2 skipped + 0 failed**,零回归。db.py 零改动。
- 2026-08-03 · 💰 **V2-⑫ 对账资金流水四分类扩展 + 偏好/能力画像双引擎完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与并行的 ⑪-B kind 决议〔`3b1dcd7`〕/ P1-36〔`a239bcc`〕两条施工线文件面不相交)。**⑫-A**:对账引擎 `parse.py`/`reconcile.py`/`material.py`/`store.py` 四个既有模块骨架逐字未改,`parse.py` 新增 `CashFlowEvent` + 四分类常量 + `ParseResult.cash_flow_events` 纯增量字段;新模块 `review/cashflow.py` 按周聚合转入转出(拆 `transfer_in`/`out`/`net`)/分红/税费/交易盈亏(直取 `WeeklyReview.stats.realized_pnl`,不重算),**刻意不给合计字段**。**⑫-B**:新包 `neckline/profile/`——`preference.py`(题材/角色/入场方式两口径〔几何 + `user_actions.kind='label'` 用户自述〕/Tier)、`capability.py`(胜率/盈亏比/MFE·MAE 只用 `positions` 真实成交、只看已平仓;`vs_peer_delta`/`missed_role` 复用 `eval.metrics.score_tradable`/`exit_sim` 同一套判分,样本〔配对数〕不足如实 `None`)、`store.py`(每期一版)、`common.py`(`MIN_SAMPLE_N=5` 三档置信度)。`scripts/profile.py` CLI + `scripts/smoke_profile.py` 合成数据端到端冒烟(人工核对 MFE/MAE 手算逐位一致)。新增 5 个测试文件 58 例 + `test_review_parse.py` 追加 3 例,`pytest tests/ -q`:本块零回归(基线 3012+2skip → 3070+2skip+0fail);⚠ 全量套件另含**并行在途会话**对 `notify_kinds.py`/`api/notify.py`/`sentinel/{holding,engine}.py` 的未提交改动(跟进 `3b1dcd7` 三个 kind 决议,尚未完工),致两条精确集合守门测试中间态失败——**与本块零关联,未触碰、未修复**。九处如实登记(资金流水第五档 `CASH_FLOW_OTHER`/转入转出拆三数/入场方式两口径并存/题材取 `stock_basic.industry`/能力指标只用真实成交/`missed_role` 无 `vs_peer_delta`/`verdict` 不落库/未接入周复盘对账结果〔无确定性外键〕/MFE·MAE 未复权有除权失真)详见 §五 V2-⑫ 段尾「完工记录」。**附带发现**:`test_scan_cluster/corr` 偶发失败已由 P1-36(`a239bcc`)结案,§四「块外挂账」同步勘误。下一步:V2-⑬(V1 清理清单执行)。
- 2026-08-03 · 🩹 **[P1-36] 结案:`test_scan_cluster/corr` 两条「三路等价」偶发红定向诊断**(@builder,纯测试代码,零生产代码 / 零数据库 / 零部署;与并行的 ⑫/⑪-B 文件面不相交)。诊断 = **(a) 类良性测试缺陷,明确排除 (b)**:工程复现(mock `_now()` 强制 bulk/day-by-day 两阶段返回不同墙钟值)精确重放症状后逐行 diff 证实——业务列(cluster_key/scope_key/corr/n_obs 等)永远逐位相同,唯一分叉列是秒精度审计戳 `computed_at`(两次独立调用间跨墙钟秒边界即合法不同,与业务确定性无关);`refresh_limit_clusters`/`refresh_corr_matrix` 的 bulk 与 day-by-day 逐行代码等价,从未真正分叉,⑨ 归因可比性不受影响。同一坑本项目已有两处正确先例(`test_industry_strength_store.py`、`test_scan_stage.py::_table_rows_excl_computed_at`),本次两测试建于同批却漏抄。修法:比较前 `.drop("computed_at")`,业务列仍要求逐位相同(未放宽断言、未加 flaky 重试、未改生产代码一行)。验证:目标文件连续 20 轮 100% 绿;全量 `pytest tests/ -q` 零回归。详见 §七 [P1-36] 结案记录(含 `test_scan_leader.py` 同款潜在隐患的留档不改)。
- 2026-08-03 · 🔔 **用户拍板:`stop_approach` / `take_profit` / `sector_dive` 三条开「立即」级 APNs kind,已落 ⑪-B**(@planner,只改文档,零代码 / 零数据库 / 零部署;一个 builder 正在并行实现这三条 kind,⑫ 与 P1-36 亦在跑,已 hunk 级挑拣、未动他人在途正文)。**① 三条进 `NKIMMEDIATE`**,依据 = **蓝图 5.5 权威兑现**(该节逐字把「逼近或触发止损」「快速跳水」列进立即通知,⑪-B 的分级归属行本就照抄了它,只是当时没有对应 kind 落地 —— 属**补齐已写在 Plan 里的东西**,不是新加需求);**V1「只进看板」拍板(§2.4)自此被 V2 三级体系取代** —— 当年只留两类 APNs 是因为**没有分级也没有 per-kind 开关**,多推一类就是全员挨轰,V2 有了三级 × per-kind 开关,「重要的事推、嫌吵可单独关」才成立;⚠ 取代的**只是这三条的通道归属**,§2.4「买点 / 证伪 / 普通警示只进看板」其余部分继续有效。**每条独立开关、用户可单独关**。**② 与 `basket_falsified`(裁决不推)的对照写进正文防被当先例**:两者**不是同一类事** —— 这三条是**持仓事件**、直接指向用户手里的仓、蓝图**逐字点名**属立即级,只欠一次用户拍板;`basket_falsified` 是**选股线的驱动假设被证伪**、**不指向任何持仓动作**,且「不进推送」写在 **CLAUDE.md 坑条 + ⑦-b/⑧-C2 红线**里 —— **要开它得先改那三处红线,不是「照这次的例再拍一次板」就行**。**③ 默认开关 = 开,且零迁移(核过播种机制,自洽)**:`_seed_push_kinds` 只播种 V1 那六个 kind 且只碰 `push_kinds IS NULL` 的行,读侧 `_decode_push_kinds` 把 `ALL_KINDS` 全部补齐、**缺键取默认开** → 三条新 kind 在**全新库与已播种老库上都自动是开**,**⛔ 无需回填 / 迁移 / 改播种函数**。⚠ 但**别照抄播种函数 docstring 那句「新 kind……用户从未表达过意见」当理由** —— 那是给 `custom_alert` / ⑪-A 四监测那种**全新能力**写的;**这三条不是新能力,是既有看板事件升级通道**,用户此前从未同意过它们上锁屏。**默认开的依据是「用户 2026-08-03 拍板」,不是「缺键取默认开」那条通用规则**(规则只是恰好让实现零成本),两件事别混。「用户关过的不许被悄悄打开」这条底线不受影响(老库里它们从来没有过开关列)。**⑰ 换包说明里加一句告知**(改的是锁屏行为,不是 App 内显示),不必做引导页。**④ 改写「两套通道」那条的措辞,让它与本次开闸不打架**:默认态仍是两条通道两个注意力预算、**⛔ builder 不许「顺手」接**(那叫违规 = 绕过「新增 kind 须用户拍板」);但**「某个看板事件该不该升级成锁屏,由用户拍板」本就是既有纪律本身,不是例外** —— 这三条走的正是这条路,**不是打破了「不混」,而是用对了那道闸**。**两者区别只有一个字:谁批的**(builder 自己接 = 违规,用户拍板 = 合法);⛔ 别把「不许顺手接进」读成「永远不许接」。**⑤ 顺手给 builder 钉两条实现要求**:(a) **⑬-6 执行时必须复核 `take_profit` 的触发源还在** —— V1 这条读候选老四件套 `target` / `decision_log.target_price`,而四件套已定退役(⑬-6)、`decision_log` 已停写(⑩-C),不改指向它会变成**永不触发的死开关**(设置屏挂个拨了没用的开关,正是 `basket_falsified` 那条反对的撒谎式 UI);V2 正确指向 = 篮子卡 `members[].exit_reference` 或 ⑩ `position_plans` 继承的离场参考。(b) **去重粒度决定这三条是不是噪声**:`stop_approach` 震荡时每拍都可能命中,V1 只进看板无所谓、上锁屏后必须只推一次 —— **判据 = `sentinel_events.event_key` 保证「当日每票每 kind 只推一次」,⛔ `event_key` 里不许带价格 / 时间戳之类每拍都变的量**。另 `take_profit` 文案**不许写成「该卖了 / 建议止盈」**(离场参考是参考、回落止盈才是纪律,语义红线照旧)
- 2026-08-03 · ⚖️ **⑪ 提的一处 Plan 自相矛盾裁决 + 三处欠账归位**(@planner,只改文档,零代码 / 零数据库 / 零部署;⑫ 并行施工中,未动 ⑫ 块正文与 §四,hunk 级挑拣提交)。**① 裁决 `basket_falsified` 的两份权威冲突 —— 判 ⑪ builder 的保守方向正确,⑪-B 的 kind 例举已改正**:该 kind **不是也不会是**一个 kind,原例举里有它是 **planner 立项时随手列的示例、不是逐条论证过的**;权威层级写死 —— CLAUDE.md 原始坑条「篮子 `falsified` ≠ 持仓该走 …… ⛔ 不进推送(守门单测扫 import)」+ ⑦-b / ⑧-C2 语义红线 **高于**一句 kind 例举。补三条实质理由防后人照例举接回去:(a) 它说的是「**驱动假设不成立**」,**不指向任何用户动作** —— 没买的人收到是噪声;(b) 买了的人会读成「**系统在暗示我卖**」,而该不该走是**持仓纪律**的事、由持仓哨兵管,这正是那条坑要防的误读;(c) V2 注意力 **80/15/5(持仓 80%)**,选股线的复盘信息不该抢持仓的推送带宽。并钉死两句:**「不推 APNs」≠「用户看不到」**(盘中看板 / ⑮ 验证角标 / ⑭-A 报告篮子节三处本来就有露出),⛔ 也**不留零调用点的空 kind**(设置屏给个拨了没用的开关 = 撒谎式 UI);**日后若真要开,正确路径不是「照例举接回去」,必须用户拍板 + 同步改 ⑦-b/⑧-C2 红线与 CLAUDE.md 坑条三处,只改一处就是绕过红线**。另在 ⑪-B 补「**两套通道刻意不混**」(`channels` 的看板事件 ⛔ 不许顺手接进 kind 白名单,承 V1 §2.4「买点/证伪/普通警示只进看板」),并注明**权威 kind 清单 = `notify_kinds.py` 唯一源,例举只是举例**。**② `stop_approach`/`take_profit`/`sector_dive` 三条立即级 kind —— 本次不动**,已由协调方单独提请用户拍板,回复后再落 Plan。**③ 三处欠账归位**:(a) **⑮ 补客户端推送欠账**(`PushSettings` 六 bool → `kinds[]` 且设置屏**动态渲染 + 按 level 分组**、⛔ 不许硬编 kind 清单〔权威在服务端,硬编必漂移,新增 kind 应做到客户端不改代码就能显示〕;`PushCategory` 六 → 三;**路由改按 payload `kind` 分发不按 category**〔按 category 分支会连坐 = V1 拆 `HOLDINGALERT` 的教训〕;**未知 `kind` 必须优雅降级**照常显示、⛔ 不许静默丢弃〔v1.5「Swift 未知 `status` 静默消失」同类坑〕;NL 提醒确认卡**七项必须齐**含行情延迟披露);(b) **⑭-B 标注 `/alerts` 五端点与 `PUT /settings/push` 已由 ⑪ 落地,⛔ ⑭ 不要重建**、只纳入三方对拍;(c) **⑪-A 补注**②实为板块基准指数不是 ETF(承 P4-35)、④ 大盘突变依赖宽基恰在池里否则如实标 `no_broad_index_quote` 不判,且**⛔ 不许为此改关注池组成** —— 无条件塞宽基会挤占 `breadth_cap` = 改变退潮哨兵宽度样本 = 碰纪律判定输入,⑪ builder 没擅自动是对的。**④ 顺手给 ⑪ 的「块外发现」补账本 → 新挂 §七 [P1-36]**:④ 两条「三路等价」守门单测全量下偶发红、单独跑恒绿(⑪ 期间两次复现,与 ⑪ 零关联)。**定为 P1 而非测试卫生**,因为两种成因性质完全不同且必须先分清:(a) 夹具跨用例污染 = 良性;(b) ④ 的 bulk 与 day_by_day 两路**真有不确定性**(polars 分组顺序 / rank tie-break 没排干净)—— **那就撞在项目明令禁止的那条线上**(CLAUDE.md「任何进判据/排序的 rank 必须先排定确定性 tie-break」,2026-07-29 行业强度真数据打出来的),意味着同一天算两次可能不一样 → 历史回放不可复现、⑨ 按包归因失去可比性。**这条守门单测存在的全部意义就是拦 (b),它偶发红本身就是在报警**;已给出四步判据(固定 hashseed 复跑 / 失败率对比 / 失败时逐行 diff 看差在行序还是数值 / 查 bulk 路径有没有 sort 收口),并写死**⛔ 分清之前不许放宽断言或加 flaky 重试(等于把报警器拆了)**;排期建议与 ⑧-E 同批(⑬/⑭ 之前)
- 2026-08-03 · 🔔 **V2-⑪ 监控 80/15/5 + 通知三级 + 自然语言临时提醒完工**(🔴 @builder-pro,纯本地代码 + 单测,零服务器 / 零部署;**哨兵四哨兵 + 熔断 + 盘前校准的判定分支一行未动**,新监测与 NL 提醒全部挂在独立 try 的旁路上)。**⑪-A**:新建 `sentinel/attention.py` —— 四监测(同篮成员集体转弱 / 板块基准指数承接消失〔**如实标是指数不是 ETF**〕/ 持仓转独立弱势 / 大盘突变)全部纯规则、只吃 D0 冻结件 + 本拍已有报价、**零额外网络**,13 个阈值登记在 `ATTENTION_DEFAULTS`(⑦-b 体例:工程默认零回测背书、不进包不进章程);同篮合并敞口按 `basket_id` 归并(⑩ 的 `entry_snapshots.basket_id` 优先、退 `position_plans`),≥2 个不同标的才标主题集中并挂进通知正文。**⑪-B**:新建 `notify_kinds.py` 作三级 × 11 kind 唯一源(D5 三个 category `NKIMMEDIATE`/`NKIMPORTANT`/`NKDIGEST`,**开关按 kind 配不按 category 配**),`api/notify.py` 扇出收敛成单条 `push_event()`(未登记 kind 直接抛 + AST 守门断言 `_fanout` 只被它调用),`push/apns.py` 六个 V1 category 常量删除换三个,开关落点从六个 bool 列换成 `app_settings.push_kinds` JSON 并由 `_seed_push_kinds` **幂等播种老库取值**(⛔ 不许把用户关过的推送悄悄打开;六列停写留档不 DROP)。**⑪-C**:新建 `custom_alerts.py`(7 个 metric 白名单 + 三值判定所需的规范化 + **七项确认卡**〔行情延迟披露与「只通知不自动交易」是恒出现的必选项〕+ 台账)、`sentinel/custom.py`(确定性执行器,`nl_text` 一个字不进判据、**零下单** AST+逐行双守门)、`llm/nl_alert.py`(带日期锚、`json_block` 剥 JSON、白名单拒收不修正、**LLM 不可用 → 手填表单不静默失败**);新增 `GET|POST|PUT|DELETE /alerts` + `POST /alerts/parse`(**⑭-B 清单里的同名五个端点已在此落地,⑭ 只需对拍**),`PUT|GET /settings/push` 契约换血为 kind 清单。测试:新增 7 个文件 **164 例** + 改造 3 个既有文件另净增 12 例(合计 +176)(`test_notify.py` 六类断言按 ⑪-B 要求换成三级 × kind 并重新锁死),`python -m pytest tests/ -q` 基线 2836+2skip+1fail(周日 ISO 炸弹,今日已自愈)→ **3012 passed + 2 skipped + 0 failed**,零回归;新增红线单测:三条旁路任一抛异常,持仓哨兵照常判照常推。冒烟 `scripts/smoke_attention_alerts.py`(一次性临时库、真库零读写、零真实 LLM/APNs)**全绿**:四监测各至少一条触发路径 + NL 提醒解析→确认卡→去重→阴性对照→命中→不重复轰炸→收盘失效→降级手填全链。**9 处如实登记、其中 🔴 2 处提请裁定**(`basket_falsified` kind 未登记 —— ⑪-B 例举与 ⑦-b/⑧-C2「不进推送」红线正面冲突,取保守方向;`stop_approach`/`take_profit`/`sector_dive` 未开 APNs kind —— 蓝图 5.5「立即」点名了止损但 ⑪-B kind 例举没有,按「新增 kind 须用户拍板」不擅自加)详见 §五 V2-⑪ 段尾「完工记录」。🟡 块外挂账:`test_scan_cluster/corr` 两个「三路等价」测试在全量套件下偶发失败、单独跑恒绿(开工基线首跑即复现,与本块零关联,属 ④)。下一步:V2-⑫(对账与画像)。
- 2026-08-03 · 🧾 **V2-⑩ 持仓极简台账 + 计划继承 + 决策日志强制表单退役完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与并行的 ④/⑨ 两条施工线文件面不相交)。新建 `neckline/positions_entry.py`(买卖三字段唯一编排入口,API/CLI 共用):entry_snapshots 冻结 + position_plans 版本继承(新版本不改原卡,单测锁死)+ 偏离提示 + user_actions 自动记账;`decision_log` 写函数物理删除,`POST /decisions` 换血为「用户可选补充」入口(落 user_actions,不再是决策日志表单);`close_reason` 枚举加四码。`python -m pytest tests/ -q`(含并行 ④/⑨ 已完工内容)最终 **2836 passed + 2 skip + 1 failed**(唯一失败是 `test_review_reconcile.py` 既有真实时钟"当周"判据,施工窗口恰跨 2026-08-02→08-03 ISO 周边界触发,用 `git worktree` 拉纯净 HEAD 复现同一失败、证实与本块无关、不修不新增)。两处如实登记(`POST /decisions` 保留路径整体换血、七枚标签码与四枚 close_reason 码系本次新拟)详见 §五 V2-⑩ 段尾「完工记录」。下一步:V2-⑪(🔴 @builder-pro,碰推送与哨兵)。
- 2026-08-02 晚间 · 🩹 **V2-④ 定向快修:种子输出顺序确定性**(@builder,纯本地代码 + 单测,零服务器 / 零部署;只改 `neckline/scan/seeds.py` + `tests/test_scan_seeds.py` + 本节 / ④ 块补记)。根因:涨停簇 / 异动簇种子走 `frame.group_by(["cluster_key"])` 迭代直接 append,polars 不保证顺序 + 上游 SQL 未加 `ORDER BY`,⑤ 只取前 20 颗 → 同一 D0 同一库重跑产出不同篮子(⑨ 完工时实证发现的块外真洞)。修法:四类种子(不只两类被点名的)输出前一律经新增 `_sort_by_seed_key()` 按 `seed_key`(crc32 稳定业务键)升序排定。新增回归测试锁死"连跑三次逐位相同 + 顺序即 `seed_key` 升序"(临时移除排序反测确认测试真能识别回归);`scripts/smoke_basket_review.py` 跨两个独立进程双跑同一 D0,113 行输出仅 2 行预期内差异(临时目录随机后缀 + 一处审计时间戳),篮子/卡/验证/复盘全部逐字节一致。改动范围 `pytest` 686 过零失败;全量套件另受并行施工的 V2-⑩(positions/api/decision_log)与一处跨零点翻车的既有 `today()` 测试双重扰动,均核实与本次改动无耦合、未修,详见 §五 ④ 块补记。
- 2026-08-02 · ⚖️ **V2-⑨ 盘后复盘引擎 + 评价引擎完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读、V2 表仍 0 行,冒烟跑在 `sqlite3.backup` 副本 + 真实 parquet 只读上)。**⑨-D 判分唯一源下沉**:`ReTrade`/`_sim_one`/`SLIP`/`BROKER` 自 `research/h9_exit_reform.py` **逐字搬入** `neckline/eval/exit_sim.py`,h9 改再导出、`drill.py`/`exam.py` 改直接 import(老写法 `h9.X` 一字不改仍工作);**对拍两层全绿** —— 单测内嵌搬迁前冻结源做**源码逐字**比对 + `exec` 出独立可调用体(独立 `Broker()`)在 400 条随机造数 + 三条窄分支上**行为逐字段**全等,真 K 线 `scripts/smoke_eval_exit_sim.py` 900 笔真单 × 3 组退出参数**不一致 0 笔**。新增 `score_kw_from_charter()`(章程→判分参数唯一翻译,v1.3.3 翻出来恰好等于研究侧冻结的 `SCORE_KW`)+ `fill_and_score()`(考官线 §九 竞价成交层 + 卡上 `max_chase` 上限,`score_kw` 设为**必填关键字**从调用面堵死默认值)。**⑨-A** 新建 `neckline/review/basket_review.py` 机械判九项(龙头从 D0 冻结卡认、可买性口径对齐 `fwd_buyable` 且一字单列、涨停价取卡上冻结值、成员收益走 `daily.pct_chg` 绕开除权锚坑、缺存拍 MFE/MAE 走 EOD 近似且 `mfe_at=None` 不编时刻)+ `basket_review_store.py`(**只有 INSERT OR IGNORE,无 UPDATE/DELETE 路径**,守门单测锁死)。**⑨-B** `TASK_REVIEW` + 复盘账 + 不联网,降级次序直读 `budget.DEGRADE_ORDER`(T3 简评 → T2 细节,T1 与 `NEVER_DROPPED` 不在其中),LLM 缺席机械判照出。**⑨-C/C2** 新建 `neckline/eval/{metrics,placebo,calibration}.py`:七组指标全部按 `pack_version × verification_ruleset_version` **分层**(preseed 自成一层、缺失显式占位不并层)、`not_evaluated` 不进验证率分母、买不进与前向未走完的**不进收益均值**、已选 vs 未选无数据时空值如实;两条对照臂 `crc32` 派生种子**同日两跑逐位相同**、抽 N 次取分布、小样本只报样本数不报结论(文案单测锁死);周度校准报告落 md+json,**不接报告管线**。测试 2749 → **2842 过 + 2 skip + 2 fail**(既有周日炸弹),净增 93 例。端到端冒烟 `scripts/smoke_basket_review.py`(真实 D0=2026-06-18,④→⑤→⑥→⑦→⑧→⑨→周报全链,零真实 LLM):复盘 4 篮、重跑幂等零差异,对照臂真实 −2.57% vs 随机中位 −1.95%(第 38 百分位)vs 满仓持有 −1.63%,**人工逐项核对一篮九项全部对得上**。**六条如实登记 + 一处提请裁定**详见 §五 ⑨ 段尾;其中 🔴 **块外真洞**:`scan/seeds.py` 的涨停簇/异动簇种子走 polars `group_by` 默认无序迭代,而 ⑤ 只取前 20 颗 → **同一 D0 同一库重跑会产出完全不同的篮子**(冒烟三连跑实证),违反「跨进程可复现」与「冻结件同日重跑幂等」前提,**⑨ 不越块自行修**,请排 ④ 的修正。
- 2026-08-02 · 🩹 **V2-⑧-E 除权除息锚失效检测器完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与并行施工的 ⑨ 块文件面不相交,只改 `neckline/sentinel/basket_verify.py` + 顺手修一处被本子项唤醒的 `scripts/smoke_basket_verify.py` 沉睡真洞)。检测器 `pre_close ≠ 卡里冻结的 ref_close`(带 `vr.EPS`)判锚失效,命中即复用既有「两侧都不计」机制排除该成员,盘中 `Quote.pre_close` 与 EOD `daily.pre_close` 走同一个 `evaluate_specs()`;EOD 另用 `adj_factor(D+1) vs adj_factor(D0)` 交叉确认拆出 `member_ex_rights`(不报警)/`anchor_mismatch`(WARNING)/`anchor_unconfirmed`(数据缺失,自补第三态);⛔ 不做自动 rescale。真实历史除权样本冒烟(`603409.SH` 2026-06-17→06-18,`adj_factor` 1.0146→1.5265)验证检测器打响且交叉确认判对。新增 `tests/test_basket_verify_ex_rights.py` 10 例,`pytest tests/ -q` 2754→**2766 passed + 2 failed(既有周日炸弹)+ 2 skipped**,零回归;⑦ 卡形状与 `spec_version` 一字未动(守门断言)。如实登记两条(详见 §五 ⑧-E 段尾「完工记录」):① 施工期发现「无条件给每个成员加 `pre_close` 字段」会打破既有「盘中/EOD 逐位相同」单测,已改为仅在锚失效分支携带该字段;② plan 验收原文"2 只成员篮"字面场景与 `min_hit=ceil(2/2)=1` 数学上不共存,已按最贴近字面意图重新构造(⑦-b「中间地带」而非净空数据)并附反证测试。下一步:⑬/⑭ 之前的排期已履行,V2-⑨(并行中)/ ⑬ / ⑭ 按既定顺序推进。
- 2026-08-02 · 🃏 **V2-⑦ ④篮子卡冻结完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;与 ⑥-b 并行施工,`tier.py`/`pack.py`/`packs/` 全程未动,两处共用测试文件按 hunk 挑拣提交):新建 `neckline/selection/basket_card.py` —— **卡内容 = 蓝图 4.6 全十一项**(①②③④来自 ⑤ 的篮子/证据/why_now/成员角色,⑤来自 ⑥ 的定档与五维 breakdown〔**duck-typed 传入,⑦ 不 import ⑥**,守门单测锁死〕,⑥⑧⑨⑩ 的人话半份由 ⑦ 的 LLM 出,⑦ 的数字过夹逼闸,⑪ `BASKET_CARD_DISCLAIMER` 单一源)+ **夹逼**(建仓观察区间与最高追价各自独立落 `ok`/`absent`/`rejected_out_of_limit`/`rejected_malformed`/`rejected_no_limit`,锚 = `compute_intraday_limit_prices(D0 close, board, is_st, next_trading_day(D0))`、`board`/`is_st` 走 `load_stock_meta`;**离场参考只校验格式不夹涨跌停**〔它是 ⑩ `position_plans.plan_json` 要继承的那一项〕;另加自洽校验「追价 < 区间上沿 = 自相矛盾」)+ **止损价系统算**(`round(close×(1−stop_pct),2)`,`stop_pct` 只从 `brain.active_config()` 来,纪律标签 `discipline_labels()` 动态生成、缺指纹退化成不带数字的说法,AST 守门断言代码里无 `0.05`/`0.08` 与「−5%」类模板)+ **结构化验证/失效 spec**(`verification_spec`/`invalidation_spec`,**⑧ 的唯一判据源**;只用价量结构;某成员某条阈值 `null` = 该条对该成员不判,**不当"不满足"**;先算 spec 再喂 LLM,单测锁死阈值确实出现在上下文里)+ **卡 LLM 段**(`TASK_SCRIPT`、**不联网**、扣 `LEDGER_REASON`、`json_block` 先剥再用、**不复用 `_parse_verdict`**;成员白名单闸丢弃篮外代码;缺席时**结构化半份照出**、人话半份如实标 `llm_stage`)。新建 `neckline/selection/member_tags.py`(**⑦-K7 标注件唯一实现**,判据逐字取自 `research/k7_pre2_report.md` §1 / `k7p_h12_pullback.py`,阈值全落在函数关键字默认值上故只需登记一个 `_EPS`;三态 `tags`/`missed`/`absent` 不合并;⑬-N 信息卡将读同一份)。`basket_store.py` 增**事务 2**:`save_basket_card`/`save_basket_cards`/`load_basket_card`/`next_card_version`,⑦ **只写 `basket_cards`**,`INSERT OR IGNORE` on `UNIQUE(basket_id, version)` 冻结不覆盖、内容不一致 WARNING + `frozen_conflicts` 如实带出,`version=1` 显式默认(自增留给 D+1 追加,免得同日重跑把冻结件写成流水账)。**真实交易日冒烟**(2026-07-24,隔离库 + 真 parquet 只读 + LLM 全桩):④206 颗种子 → ⑤2 篮 → ⑥全 T1 → 事务 1 落 `2/4/2` → ⑦2 张卡 → 事务 2 落 2、重跑 `cards_existing=2` 且 0 冲突;人工核对 601728.SH:涨停 6.78 / 跌停 5.54 / 止损线 5.85(= 6.16×0.95,读真实现役章程 `K1`),区间 [6.10,6.22] 与追价 6.34 判 `ok`,离场参考 [7.70,8.62] **远高于涨停仍放行**(证明不夹),同篮 600941.SH 越界区间被拦、篮外 `999999.SZ` 被白名单闸丢弃。测试:新增 `test_selection_member_tags.py`(46)+ `test_selection_basket_card.py`(54)= **100 例**,`pytest tests/ -q` → **2652 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹;数字含并行 ⑥-b 净增)。**六条如实登记 + 两点提请裁定**详见 §五 V2-⑦ 段尾「完工记录」,最要紧三条:① **`min_members_hit=ceil(n/2)` 与验证/失效的条件集都是 builder 拟的临时默认**(plan 只给了「双份 + ⑧ 唯一判据源」两条约束,没给聚合规则与条件清单),**⑧ 一开始消费就成既成事实,建议先拿裁定**;② **`warn_streak_top` 的「簇内」改读作「共振邻域」**——审计粗簇一票一簇,而 ④ 的多路聚类下一票同日属十几个簇,照字面「任一簇头名」当日 **36/42(86%)** 都要打警示 = 等于没警示,改判后三天真实数据命中 6/6/5;③ `selection/__init__.py` 的「本包全程不 import `strategy.brain`」一句如实修正为「**包机制本身**不 import」(⑤ 起就不成立,⑦ 读 `stop_pct` 是 plan 明文要求),并把 ⑥ 那条 `test_reads_no_discipline_thresholds` 按 tier / store 两种职责拆成两条。下一步:**V2-⑧**(碰哨兵,🔴 @builder-pro)
- 2026-08-02 · 👁️ **V2-⑦-b 条件集定版 + V2-⑧ D+1 验证完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读、V2 表仍 0 行,冒烟跑在库副本 + 临时 parquet 上)。**⑦-b**:条件集 / 聚合门槛 / 四态映射 / 比较语义集中到新模块 `neckline/selection/verification_rules.py`(选「独立小模块」而非 `basket_card.py` 顶部,**理由是防模块环** —— ⑧ 住 `sentinel/` 而 `basket_card.py` 反向 import `sentinel.universe`);失效第 ③ 条按裁定改判为复合条件 `close_below_ref_and_ma20`(`{ref_close, ma20}` 两阈值 AND,任一 null 整条不判),三个 `spec_version` 随形状 bump 至 `_v2`;新增 `verification_ruleset_version` 落 `card_json.fingerprint` **+ 两份 spec 自身**(⑧ 手里常只有 spec,没有它 `evidence_json` 标不出「哪套条件集出的判」);`MIN_MEMBERS_HIT_DIVISOR`/`EPS` 登记进 `_ENGINE_CONSTANT_WHITELIST`(前者措辞含「临时默认 / 零审计背书 / 要可配须先扩 §12.2 插槽边界」,单测断言);比较语义**存进卡**、⑧ 按卡里写的串选比较器(老卡永远按它自己那版被判)。**⑧**:新建 `sentinel/basket_verify.py`(盘中与 EOD **共用同一个** `evaluate_specs()`,拆的只是代入什么价)+ `sentinel/basket_verify_store.py`(`basket_verification` **只 INSERT**;盘中变了才落、EOD 必落、`falsified` 当日终态不撤回而 `verified` 可翻;「当前状态」三路读法唯一实现)+ `sentinel/capture.py`(内存累计 → 15:05 一次 `write_table_day`,`capture_status` 三态 + 原始数字一起落 `sentinel_events`);关注池改组为「持仓 > T1/T2 篮子成员 > 候选(V1 残留)> 板块基准指数 > 昨日涨停填充」、**自选池不再是来源**(⑬-11 复述的「⑧-A 已改」);`run_tick` 与 lifespan 循环各挂**独立 try/except 的旁路**,四哨兵 / 熔断 / 盘前校准判定逻辑一行未动。**顺手修一个会静默拉错标的的真洞**:`quotes.to_symbol` 改为 `.SH/.SZ/.BJ` 后缀优先(`000001.SH` 上证综指原本被拉成 `sz000001` 平安银行),对所有股票代码形状逐位等价(单测锁死)。测试 2652 → **2735 passed + 2 skip + 2 fail**(2 fail 仍是既有周日时间炸弹),净增 83、删 5 例随自选来源退役的用例。冒烟两份:`scripts/smoke_basket_verify.py`(真实 D0=07-23 锚 + 合成分钟)**盘中四态齐 + 定格不撤回 + 存拍读回 dtype 全合声明**,EOD 用真实 07-24(全市场 −2.62% 的真跌日)判出 4/5 篮 `falsified`、其中 `600396.SH` 最低价恰等于算出的跌停价、「触及跌停」条在真数据上打响;`scripts/smoke_sentinel.py` 回放确认四哨兵零回归。**七条如实登记**详见 §五 ⑧ 段尾,其中**两处提请裁定**:① **锚是前复权、观测是原始价**(盘中免费源只有原始价,两条路径彼此一致但成员恰在 D+1 除权时会误判破位;正解是 ⑦ 在卡里多冻一份原始价锚,属 ⑦ 形状变更、不在 ⑧ 授权内);② **EOD 那一拍谁调 Plan 未定死** —— 本块只给函数 + `scripts/basket_verify.py` CLI,**没有**擅自挂进 `daily_update.py`(⑭-A 的 16:35 链才是 plan 指定入口),故 ⑭ 落地前 EOD 定论行不会自动产生。另登记 `capture_status` 落 `sentinel_events`(不新开第 22 张表)与「相关板块 ETF/指数」本版只做到基准指数(无 ETF 映射数据源)。下一步:**V2-⑨**(盘后复盘 + 评价引擎;依赖 ⑦⑧ 已就位)
- 2026-08-02 · 🧭 **V2-⑥-b 追加子项完工**:档位质量线进包 + T3 下限 + 溢出摘要原因码拆分 + `neutral_filled_weight` 审计字段(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ⑦ 并行施工,文件面不相交,详见 §五 ⑥-b 段尾「完工记录」)。`pack.py` 新增 `_validate_quality_lines()` + `Pack.tier_quality_lines()`(`quality_lines` 三键各自独立可选,单调性只比较字面给出的键、不合并引擎默认——规避 `pack.py`↔`tier.py` 循环 import);`tier.py` 新增 `TIER3_MIN_SCORE=0.25`/`resolve_quality_lines()`(逐键回退,姿势与 `resolve_weights()` fail-loud 刻意不同)/`DROP_BELOW_QUALITY_LINE`(与 `DROP_CAPACITY_OVERFLOW` 分开留痕计数)/`neutral_filled_weight()`(只认 flags 不认数值,躲开 `leader_clarity` rank=2 撞车);`packs/K7-pack.json` 补 `quality_lines:{0.60,0.40,0.25}`,`K4-pack.json` 零改动;两包四道闸 dry-run 均 exit 0,`ENGINE_API_VERSION` 仍 1。测试净增 32(`test_selection_pack.py` +17、`test_selection_tier.py` +15)+ `_ENGINE_CONSTANT_WHITELIST` 改写说明,`pytest tests/ -q`:2516(基线)→**2651 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹,与本子项无关;⑥ 既有 86 例一字未改仍绿,余下净增来自并行 ⑦)。如实登记一处范围限定:「消费 `mech_breakdown_json` 须同时读 flags」暂未落成自动化仓库扫描(核实唯一准消费者 `basket_card.py` 是整段透传、不构成违反;⑨/⑭/⑮ 未落地前写扫描器要么打空要么误伤,改以文档 + 可执行示范兜底,留给三者落地时补真正的静态守门)。下一步:⑦ 并行施工中,两者收尾后转 V2-⑧。

- 2026-08-02 · 🧭 **V2-⑥ ③Tier 分层引擎完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读、全部 V2 表仍 0 行、现役章程仍 `K1` 未动):新建 `neckline/selection/tier.py`(**五维机械分**——`sector_strength`〔`industry_strength_daily` 只读表 + 概念热榜,两侧取较大者、概念只加成不减分〕/ `driver_freshness`〔④b `industry_stage_daily.stage` 经**现役包** `tier.stage_scores` 映射〕/ `leader_clarity`〔`rs_rank` 倒数衰减 `1/min(rank)`,⛔ 连板高度不进〕/ `tradability`〔一字扣 1.0、涨停开过板扣 0.5〕/ `card_density`〔⑤-b 的 `k4_tag` 命中密度〕;**任一维算不出一律中性分 0.5 + `flags` 如实标,⛔ 永不写 0** —— 0 是 `overheat` 的真实取值)+ **运行期访问锁**(特征行里刻意同装六个 LLM 产出字段,`mech_score()` 只许读 `_TIER_SCORE_INPUTS` 五键,单测断言 `accessed == 白名单` 且与 LLM 键 `isdisjoint`,并配一条"行里真有 LLM 字段可读"的可证伪性断言)+ **权重只住包里**(`resolve_weights()` 缺维度/多出未知维度**双向 fail loud**,归一化保证机械分恒在 [0,1])+ **定档**(`(机械分降序, basket_key 升序)` 确定性排定 → 每档一道质量线 → **上限非配额**,够格但满了向下顺延、三档全满则今日不定档并留痕)+ **LLM 同档微调**(`TASK_TIER_RANK`、不联网、扣 `LEDGER_REASON`;日期锚 + `TIMELINESS_RULES`;产出走 `json_block` 先剥 JSON 再解析、**不复用 `_parse_verdict`**;**跨档提案整条丢弃 + WARNING**,另五类拒收码 `unknown_basket`/`bad_rank`/`slot_taken`/`duplicate_key`/`malformed` 语义不合并;`rank_mech` 与 `rank_in_tier` 两者都落库 + `llm_rank_delta` + `llm_reason`;缺 provider / 调用失败 / 预算尽 / 解析失败 → **机械序原样用**并如实标 `tier_rank_unadjusted:{stage}`;每档只 1 个篮子时根本不发起调用)。新建 `neckline/selection/basket_store.py`(篮子四表唯一写入口:`save_baskets` 从 `aggregate.py` **原地搬来**、新增可选 `conn=`、`aggregate.py` 同名再导出**同一函数对象**,⑤ 既有 63 例一字未改仍绿;新增 `save_tier_history` 与**事务 1** 入口 `save_tier_decision` —— 三表同一 `with connection()` 内落地,单测 monkeypatch 让 tier_history 抛错验证**三张表一起回滚、库里零行**;`INSERT OR IGNORE` 冻结不覆盖,重跑不一致 → `frozen_conflicts` 逐条带出 + WARNING「本次重算结果未采纳」,不静默)。**本地冒烟**(真实交易日 2026-07-24 + 真 parquet + LLM 全桩 + 真库一次性副本):206 颗种子(截断 20)→ ⑤ 出 15 篮 → **⑥ 定档 T1=2(满)/ T2=5(满)/ T3=8、溢出 0**,机械分 0.810~0.280;跨档桩提案真被拒、档内调序真生效(机械 #5 → 最终 #1,Δ=4,理由落库),事务 1 落 `15/22/15`,重跑全 existing、0 冲突。测试:新增 `tests/test_selection_tier.py` **86 例** + `_ENGINE_CONSTANT_WHITELIST` 补登记 tier.py 8 个引擎常量,`pytest tests/ -q` 2427→**2516 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹)。**十条如实登记**详见 V2-⑥ 段尾「完工记录」,其中**两处提请 planner 裁定**:① **档位质量线 0.60/0.40 是 builder 的临时工程默认**(Plan 没给数;纯排名制下「允许 T1 为空 + 不许凑数」的单测根本写不出来,故必须有绝对分数线;它带策略语义本该进包,但两份包都无此键、Plan 也未授权 ⑥ 扩 ③ 的包 schema);② **容量溢出的篮子今日不落库**(`baskets.tier` NOT NULL,臆造 tier 更错;留痕在 `TierResult.dropped`)。下一步:**V2-⑦**(篮子卡冻结 + ⑦-K7 成员标注件;`save_basket_card()` 直接加进已建好的 `basket_store.py`、**单独一个事务**)
- 2026-08-02 · 🏗️ **V2-⑤-b 成员卫生线闸 + V2-⑤-c lift 最小成分数门槛完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ③-K7/④b 两条并发线文件面不相交):新建 `neckline/selection/member_hygiene.py`——过滤链路挂在 `aggregate_baskets()` 内 `build_mech_context()` 之后、`presented_by_seed`(`_shortlist` 截断)之前,当日对候选成员集调用 `apply_member_hygiene()` **恰好一次**;**两级保险丝**:tier-1(`stock_hygiene` 的 `is_st`/`board` + 独立停牌/次新检查,数据源 `sentinel.universe.load_stock_meta`+`stock_basic`+`suspend_d`)算不出 **fail closed**(拒收);tier-2(`close`/`ma20`/`amount_ma20`,数据源 `build_research_panel` 单日切片;K4 分区归属复用 `report/holding_k4_check.py` 既有机械镜像 + 本模块自写 bulk 谓词下推 loader,不 import 即将随候选榜退役的 `intel_candidates.py`)算不出 **fail open**(`hygiene_unavailable`/`k4_unavailable` 分开披露)。`k4_advisory_gate` 两档语义照包:`hard_cut→exclude` 剔出,`avoid_flag→tag` 保留但标(`k4_tag_of`,新字段 `BasketMemberCandidate.k4_tag` 随成员流转供未来 ⑥/⑦ 消费)。`aggregate.py::assign_primary()` 改写为 **lift 三态**:达标篮(成分池 ≥ `MIN_LIFT_SAMPLE_SIZE`)之间正常比 `primary_reason='highest_lift'`;不达标篮 `lift=None`+`lift_reason='sample_too_small'`、完全退出比较;一票全部候选篮都不达标 → 确定性兜底 `(成分池大小降序, basket_key 升序)`、`primary_reason='fallback_no_qualified_lift'`。`MIN_LIFT_SAMPLE_SIZE` **直接 import 引用** `industry_strength._MIN_MEMBERS`(不是抄字面量,故意不登记进 `_ENGINE_CONSTANT_WHITELIST`——登记一个白名单扫描器结构上命中不到的 `Name` 引用反而会打破反向存在性校验)。测试:新增 `tests/test_selection_member_hygiene.py` **23 例** + `test_selection_aggregate.py` **7 例**(⑤-b 编排接线 3 + ⑤-c lift 三态 4);`test_selection_aggregate.py::_run()` 补测试脚手架(自动注册现役包 + 补齐 `stock_basic.list_date`,`INSERT OR IGNORE`/`WHERE...IS NULL` 语义不覆盖既有值——⑤-b 让卫生线成为强制前置,既有 55 个测试函数里只 3 个显式注册过 `stock_basic`);`_two_basket_env` 是既有 56 例里唯一动了测试**数据**(非断言)的一处(种子成分 3→5 只以满足新门槛,原本示范的"小样本失真"改由新测试专门覆盖)。`pytest tests/ -q`:**2427 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹,树内同时含 ③-K7/④b 已完工的净增,三方文件面互不相交零冲突)。**五条如实登记**(其中最要紧:①"面板行存在但字段 null"与"整行缺失"改判同等降级,原因是 `non_new_stock` 120 自然日门槛远严于 ma20 所需 20 交易日,能过前者却后者仍 null 现实代表数据缺口非"真太新";②K4 评估失败的降级哲学是 builder 类推 plan 未点名)详见 V2-⑤-b 段尾「完工记录」。下一步:**V2-⑥**(依赖的 ③/③-K7/④b/⑤ 现已全部就位,解锁可开)
- 2026-08-02 · 🏗️ **V2-③-K7 原语与白名单跟进 + `packs/K7-pack.json` 完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ⑤-b/⑤-c 另一 builder 并行施工,文件面不相交):`neckline/selection/primitives.py` 白名单增第 10 个模式 `industry_stage_daily.*`;`intel_rank_priority` 新增 `_RANK_DIM_DIRECTIONS` 方向声明表(既有三维 `asc` 不变,新增 `industry_stage_score` 为 `desc`、`leader_rs_rank` 为 `asc`),未登记维度名 fail loud。`neckline/selection/pack.py` 新增 `config.tier.stage_scores` 可选键校验(键 ⊆ `neckline.scan.stage.STAGE_ORDER` 六码,值须数值)+ `Pack.tier_stage_scores()` 访问器。**`ENGINE_API_VERSION` 判定:保持 1(纯增量,K4-pack-v1 回滚锚逐位不变,单测锁死)**。新增 `packs/K7-pack.json`(`K7-pack-v1`,9 原语全收录、8 个与 K4-pack 逐字节相同、`intel_rank_priority.dims` 换四维排序键、`tier.weights` 证据化初值、`stage_scores` 六态英文码全覆盖;四道闸 dry-run 全绿,**只落盘不激活**,`selection_packs` 真实库仍 0 行)。**补 ③ 完工记录挂账的"换包→跟着变"测试局限的真实版本**:两处新测试改用仓库里真实 K4/K7 两份包文件驱动 `intel_rank_priority`(排序结果真的翻转)与真实 `generate_seeds()`(种子集合逐位不变,证明 K7 变更不含 scan 种子阈值)。测试:4 文件净增 24 例,`pytest tests/ -q` 2371→**2395 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹,零回归)。三处如实登记(K7-pack 收录全部 9 原语非草案 5 个/未加 schema 级 dims 枚举校验、留运行时 fail loud/新增 `tier_stage_scores()` 访问器非 Plan 逐字要求)详见 V2-③-K7 块内「完工记录」。下一步:V2-⑥(依赖 ③ ③-K7 ④b ⑤,现除 ⑤-b/⑤-c 外均已完工)
- 2026-08-02 · 🏗️ **V2-④b 行业题材阶段六态状态机完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ⑤ 并行施工,文件面不相交):第 21 张表 `industry_stage_daily` 落 `neckline/db.py::_SCHEMA`(附加在 ①/④ 已完工段落之后,不回改);新模块 `neckline/scan/stage.py`(`decide_stage()` 六态判据唯一实现——启动/发酵/过热/分歧回调/退潮/无题材,互斥优先级从上往下,三类"缺数不猜"安全落 `none` 且 `stage_reason` 逐条区分;`persist_days` 直接读 `industry_strength_daily` 单一源不重算,近 2/5 交易日强度旗标读本表自己历史不重查源表)+ `scripts/industry_stage.py {refresh,verify,bootstrap}` + `daily_update.py` 挂 `update_industry_stage`(排 `update_industry_strength` 之后)。**两遍法未采纳**(如实登记,理由同 ④ 自己 #5 先例:本表无跨日无界递推状态)。**本地真实数据冒烟**:`industry_strength_daily` 暖启动 134 个交易日(2026-01-02~07-24,14740 行,0.69s)→ `industry_stage_daily` 同区间批算(14740 行,**1.75s**),`verify` 全区间全绿,尾窗六态分布人工核对合理。**与研究实现对拍**(`scripts/oneoff/compare_industry_stage_with_research.py`,研究面板覆盖至 07-17):排除冒烟库暖启动前 2.5 周后(2026-01-20~07-17,118 天,12390 样本)**一致率 97.71%**,`is_strength_day`/ignition/fermentation/overheat 判定**两侧零冲突**,残余 284 处分歧**全部**落在"双方都判非强度日、仅 divergence/ebb/none 三态细分不同"一类,抽样确认根因是研究脚本 `lu_cnt` 施加 `base_expr()` 域过滤、生产按 ④b-A 定义直接读 `limit_derived.is_limit_up` 不做域过滤——按 ④b-D 裁决,**生产实现判定正确**,已如实记账。测试新增 `test_scan_stage.py` 38 例,`pytest tests/ -q`:**2371 passed + 2 skip + 2 fail**(2 fail 是既有周日时间炸弹,与本块无关;当前树含 ⑤ 并发 builder 未提交在制品,已计入本次全量数;本块自身净增 38、零回归)。真实 `data/neckline.db` 全程只读、MD5/mtime 不变;`industry_strength_daily` 零改动(全仓 grep 确认零写入)。三处如实登记(grep 守门改行为验证——本表无类比 `compute_industry_strength` 的可命名封禁函数;缺行保险丝两路的"⑥ 不拦/排序键退化"消费逻辑留给 ⑥ 落地时验收,本块只交付 `load_industry_stage`/`industry_stage_status` 两个原语;读侧口径指纹校验为主动补充的一致性增强非越权)详见 V2-④b 块内「完工记录」。下一步:V2-⑥(需 ④b 与 ③-K7 就位,现均已完工)
- 2026-08-02 · 🏗️ **V2-⑤ ②驱动聚合层完工(V2 心脏)**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;与 ④b 并行施工,文件面不相交):新建 `neckline/selection/aggregate.py` —— **两段式 LLM 编排**(`run_driver_search` 走 `TASK_DRIVER_SEARCH` + 联网 + 显式带年份检索词、扣 `LEDGER_SEARCH`;`run_basket_reason` 走 `TASK_BASKET_REASON` + 不联网、扣 `LEDGER_REASON`;两段 user 消息首行恒 `prompt_context.date_anchor_line`,两段 system prompt 各嵌 `TIMELINESS_RULES`)+ **两道机械闸**(成员白名单闸 → 凭空造票**整条拒收** + WARNING,配套「声明了不存在的种子」同样整条拒收;角色对拍闸 → 与 `leader_structure_daily.role_mech` 冲突时 `role_conflict=1` 且 `role_llm`/`role_mech`/`rs_rank` **三者并存**,机械侧 `unknown` 判「无判定」不判冲突)+ **单侧降级三态**(检索缺席 → `evidence_status='search_unavailable'` 篮子照出、推理缺席 → 不成篮、部分搜成 → `partial`)+ **主归属**(行业闸 lift 最高者 `is_primary=1`,tie-break lift 降序→`basket_key` 升序)+ **落库口** `save_baskets(..., tier_by_basket_key=…)`(`INSERT OR IGNORE` 幂等不覆盖;**tier 必填、缺就 fail loud**)。**顺带最小重构**:围栏 JSON 解析器从 `report/reference_plan.py` 搬到新模块 `neckline/llm/json_block.py`(行为逐字节不变、原处同名再导出,既有 81 例全绿)—— 通用件不该跟着 ⑬-3 停用的模块陪葬,本块与 ⑦⑨ 都要用同一份。**真实数据冒烟**(真库副本 + 真 parquet + LLM 全桩,2026-07-24):检索词实测 `信创 2026 最新` / `光伏建筑一体化 2026 最新`,成两篮带真实中文名/行业/成交额/机械角色/簇内 RS 名次/相关性 0.73,白名单闸真拦下故意混入的 `999999.SZ`,对拍闸真标出一处冲突;**冒烟打出并修掉两处合成测试看不见的坑**(涨停簇种子只有概念锚时 `label` 是裸指数代码 `886086.TI`,当检索词等于什么都没查 → 统一过 `load_index_names` 名表;`daily.amount` 单位是千元,换亿元该除 `1e5` 不是 `1e8`,原写法把 7.27 亿写成 0.01 亿)。测试:新增 `tests/test_selection_aggregate.py` **56 例**(含三条静态守门:不 import 哨兵 / 不复用 `judge._parse_verdict` / 源码零纪律参数痕迹;一条 lift 与 v1.3.1 行业闸的**交叉断言**),`pytest tests/ -q` 2274→**2333 过 + 2 skip + 2 fail**(2 fail 为既有周日时间炸弹;⑤ 自身 +57,余 2 来自并行的 ④b)。真实 `data/neckline.db` 全部 V2 表仍 **0 行**、现役章程仍 `K1` 未动。**十条如实登记**详见 V2-⑤ 块内「完工记录」,其中最要紧的是 **①「`baskets` 写入块登记在 ⑦ 但 `tier` 由 ⑥ 定档」的 Plan 自相矛盾**(连带 `tier_history.basket_id` 与 `baskets.id` 的循环依赖,⑥/⑦ 施工前需 planner 裁定落库次序)与 **②「成员卫生线该落在哪一块」无人认领**(现状:篮子成员可能含 ST/次新/流动性差的票)。下一步:V2-⑥(需 ④b 与 ③-K7 就位)
- 2026-08-02 · 🏗️ **V2-④ 市场扫描层完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):全新包 `neckline/scan/`(`cluster.py`→`limit_cluster_daily`〔同日/连板双 `cluster_kind`,`crc32` 稳定键〕、`corr.py`→`corr_matrix_daily`〔簇成员∪概念成分候选对,纯 polars 不引入 numpy,规模上限按真实数据实测从 60 调至 30〕、`leader.py`→`leader_structure_daily`〔RS/tie-break 已按施工期收到的 K7 交接稿需求 1a 对齐:RS20 严格口径+RS降序→成交额降序→ts_code升序 tie-break+连板高度成交额均不进 role_mech 主定义,等效完成施工图后来追加的 V2-④b-0 子项,该子项落地时可直接引用本记录不必重做〕、`seeds.py`〔四类驱动种子,阈值全部走新增 4 原语读现役包〕、`freshness.py`、`verify.py`)+ `scripts/scan_layer.py {refresh,verify,bootstrap}` + `daily_update.py` 挂 `update_scan_layer`;`neckline/selection/primitives.py` 新增 4 原语(`hot_industry_seed`/`surging_concept_seed`/`limit_cluster_seed`/`anomaly_cluster_seed`,白名单扩至 9 模式)+ `packs/K4-pack.json` 补种子参数节。真实历史数据(2026-07-22~24)冒烟:三张表 5.3s/3 天、`verify` 三项自检全绿、四类种子人工核对合理(含 2 颗真实暴起概念种子)。测试:新增 9 文件 95 例 + `test_selection_primitives.py` 扩容,`python -m pytest tests/ -q` 2179→**2274**(净增 95,零回归,2 pre-existing 失败同名同因未变);真实 `data/neckline.db` 全程只读、MD5/mtime 不变。六处如实登记(四类种子算法无先例文档系原创设计 / 不消费 `sector_moneyflow.py` / 异动簇不用 `top_list.py` / 种子成员不二次过滤留给⑤ / bootstrap 未做两遍法 / `dataFreshness.scanLayer*` 未接线进 `pipeline.py`),详见 V2-④ 块内「完工记录」。下一步:V2-⑤ / V2-③-K7 / V2-④b(后两者互不依赖、可并行)
- 2026-08-02 · 🏗️ **V2-③ 选股策略包机制完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):全新包 `neckline/selection/`(`engine_api.py` 单一源 `ENGINE_API_VERSION=1`;`primitives.py` 特征白名单 `_ALLOWED_FEATURES`〔8 模式〕+ `Primitive` 冻结 dataclass 两道白名单锁〔构造期 `__post_init__` fail loud + 运行期 `validate_all_primitives_whitelisted()`〕+ 首包 5 个原语 `stock_hygiene`/`non_new_stock`/`k4_advisory_gate`/`industry_dominance_gate`/`intel_rank_priority`,现值分别抽自 `neckline/research/panel.py::base_universe_expr`/`intel_candidates.py` 的 `NON_NEW_MIN_DAYS`/K4 安检语义/`INDUSTRY_GATE_MIN_LIFT`/`_sort_key` 三级键顺序;`pack.py` schema 校验 + `Pack` 冻结视图〔`seeds_config`/`tier_weights`/`tier_dims`〕+ `get_active_pack()`〔按 `(db_path,pack_version)` 双键缓存,防跨库串味〕+ `activate_pack()` 单事务写)+ `scripts/activate_pack.py`(四道闸,复刻 `activate_charter.py` 体例并按包性质简化)+ 首包 `packs/K4-pack.json`(`pack_version="K4-pack-v1"`,D7 定案;`tier.weights` 五数值逐字取自 plan 格式示例本身,V2 全新概念无 V1 代码可抽)。**手工演练发现并修复一处真实顺序漏洞**:CLI"已现役→快捷退出"若跑在 append-only 内容完整性核对之前,会放过"文件被篡改但版本号未改、且恰好现役"的情形,已提前核对顺序 + 两个回归测试锁死。测试:新增 4 文件 123 例,`python -m pytest tests/ -q` 2056→**2179**(净增 123,零回归,2 个 pre-existing 失败与 V2-② 同名同因、今日核实确为周日);真实 `data/neckline.db` 全量测试前后与手工 dry-run 前后 MD5/mtime 双证不变,全程未跑 `--confirm` 碰真实库。三处设计判断如实登记(身份/分类数据不计入 inputs 白名单 / 三级键归入 seeds 非 tier / tier 权重来源如上),「插槽真被消费」因 ④⑥ 未施工改在原语+包层做代理验证,详见 V2-③ 块内「完工记录」。下一步:V2-④
- 2026-08-02 · 🏗️ **V2-② LLM Provider 自填制 + 双 Agent 路由 + 预算多本账完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):`_PROVIDERS`/`ALLOWED_PROVIDERS` 两处枚举退役,`llm/factory.py::get_provider(task, *, db_path, settings_obj)` 改纯 DB 驱动(`llm_providers` 自填注册表 CRUD + `app_settings.llm_task_routes`/`llm_default_provider` 路由);新建 `llm/router.py`(九任务常量 + 默认路由纯函数)、`llm/budget.py`(预算三本账互不透支 + 降级次序 T3简评→T2细节、篮子卡冻结永不丢);`OpenAICompatProvider` 改可直接实例化(通用搜索钩子照 GLM 协议平移,`has_web_search=0` 一律不发 `tools`),`GLMProvider`/`KimiProvider` 完整保留行为不变、降级为预置参考实现供既有单测续用。契约:`PUT /settings/llm` 删,新增 `GET/POST /settings/providers`、`PUT/DELETE /settings/providers/{name}`、`GET/PUT /settings/llm-routes`,`GET /settings` 换形状(`providers`/`routes` 取代 `llmProvider`/`llmKeySet`)。新测试文件 `tests/test_llm_router_budget.py`(16 例)+ `test_llm.py::TestFactory`/`test_api_settings.py` 大幅重写。`pytest tests/ -q` 2023→2056(净增 33,零回归,2 pre-existing 失败经 `git stash` 复核与本块无关),真实 `data/neckline.db` 全量跑前后 MD5/mtime 不变。三点如实记账(详见 V2-② 块内「完工记录」,未改 Plan):`news_scan.py` 缺 `prompt_context` 既有缺口、"两段式编排"判断留给 ⑤、`TASK_INQUIRY` 归入检索类默认路由是 builder 推断待澄清。下一步:V2-③
- 2026-08-02 · 🏗️ **V2-① 表与共享信息层地基完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):20 张新表一次到位(18 SQLite 落 `neckline/db.py::_SCHEMA` + `app_settings` 两新列;2 parquet 表登记进 `market_data.py` 的 `_VALID_TABLES`/`TABLE_FLOAT_COLS`);新建 `neckline/user_actions.py`(append-only 唯一实现,靠"没有第三个函数"担保);新守门单测 `tests/test_v2_schema_guard.py`(17 用例,五条三律判据——冻结/追加/不回写/幂等/parquet 声明——全覆盖,含一处比 plan 原文更对称的补强:`DELETE FROM entry_snapshots` 冻结检查)。`pytest tests/ -q` 2005→2023(净增 18,零回归)。**副产品发现**(已 `spawn_task` 记账、未修复、不影响本块验收):`tests/test_holding_k4_check.py` 若干用例调用 `build_holding_k4_check` 未传 `db_path`,经既有代码路径命中真实 `data/neckline.db`(早于本次改动即存在的 pre-existing 缺陷,git stash 复现验证过;这次因新表新列首次留下可见痕迹,纯增量无数据损坏)——本块验收改用 `DB_PATH` 环境变量重定向复测,证明本块新代码本身零写入真实库。下一步:V2-②
- 2026-08-02 · 📐 **K7 系统线需求六项 + K7-pack 草案整合进 §五 施工图**(@planner,只改文档,零代码 / 零数据库 / 零部署;来源 = 用户转发的 `archive/交接_K7_系统线需求_20260802草案.md`,该稿即刻转参考件、不作第二权威;用户指示「施工时尽量全部做到」,含建议项)。**落位**:**需求 1a** `leader_clarity` = 簇内 RS20 头名度(依名次衰减,tie-break 定死 **RS 降序→成交额降序→ts_code 升序**;**连板高度与成交额头名均不进主定义**)→ **⑥** 正式定义 + 新块 **④b-0** 的 `rs_rank` 口径追加(**不改 ④ 已定型规格,④ 未完工则由 ④ 一并落、已完工则 ④b 补,两路都要单测锁三级键**);**需求 1b** `driver_freshness` = 行业五态状态机取代 `persist_days` 单调函数 → **⑥** 正式定义 + **新块 ④b**(**第 21 张表 `industry_stage_daily`**,DDL 定死、16:05 EOD 批算、在线只读、新 CLI `scripts/industry_stage.py`,P0-23 纪律与 ④ 同规格;**planner 定为新表而非给 `industry_strength_daily` 加列**,三条理由入档;**缺行保险丝取中性分 + 标 `stage_missing`,严禁写 0**——0 是「过热」的真值会撞车;**六态键一律英文码**〔`ignition`/`fermentation`/`overheat`/`divergence`/`ebb`/`none`〕,中文只做展示层,与草案 JSON 的中文键刻意不同,理由照 `CandidateOut.board` 先例;研究实现 `research/k7p_h11_stage.py` 只作对拍基准、不进生产);**需求 2** 五维权重证据化初值 0.30/0.30/0.10/0.20/0.10 → **⑥**(落 K7-pack;**K4-pack-v1 占位值原样不动、不重发版**,它是回滚锚;同时澄清「K4-pack 不动」的准确范围 = 只指 Tier 权重占位值,**不禁止 ④ 给它补自己的种子参数**);**需求 3** B3「题材持续 2-3 天」黄牌停用(A2 红牌不动、不新设绿牌)→ **新子项 ⑯-I**(🔴,一次性脚本 + payload + 默认演练 + 双备份 + 前后 diff 落 archive,**与 ⑯-E 包激活同窗**;写明**不违反红线** —— K4 行 `is_active=0` 永不激活,改的是建议载体不是纪律,现役章程恒 `v1.3.3`;镜像 `_HIT_META` 同步 + 守门单测断言 B3 零黄牌且 A2 仍命中)**+ 【门禁】新增一个用户判定点**(稿内建议叫 review,按项目规矩**由用户点名、不自动排程**);**需求 4** 原语与白名单跟进 → **新子项 ③-K7**(`_ALLOWED_FEATURES` 增 `industry_stage_daily.*`、确认 `leader_structure_daily.rs_rank` 可被 sort_key 引用、`intel_rank_priority` 支持新 dims **并显式声明每个 dim 的升/降序方向**、`config.tier.stage_scores` 落点与 `engine_api_version` **bump 判定规则定死**〔纯增量则维持 1,否则 bump 2 并 fail loud,**两路都必须在完工记录写明走的哪条**〕、`packs/K7-pack.json` 落盘但**不激活**);**需求 5** 龙回头位 + 双尾警示族 → **⑦-K7**(篮子卡成员节,判据全机械零 LLM,落 `card_json.members[].tags` 不新建表)**+ ⑬-N-K7**(信息卡,读同一份 `member_tags.py` 实现与文案,交叉断言两侧标签逐位相同);**四不硬约束**(不进排序 / 不进 Tier 机械分 / 不进哨兵 / 不改去留)+ 打标前后 Tier 序与成员集合逐位不变的守门单测;**需求 6** 安慰剂对照臂 → **⑨-C2**(从建议项**排为正式验收项**:随机同规模篮子臂〔`crc32` 派生种子可复现、抽 N 次取分布、判分复用 `exit_sim` 唯一源〕+ 满仓持有基准臂,样本不足只报样本数不报结论,**两臂只进周报不进任何在线判据**)。**planner 另定两件**:① **⑯-E 改为两步激活** —— 先 `K4-pack-v1`(**回滚锚**,库里必须真有旧行才谈得上回滚)再 `K7-pack-v1`(现役),验两行 / 现役唯一 / 事件流三条,并写明「⑯-E 之前是最后的改包窗口」(`--confirm` 后内容即冻);② **preseed 行 `pack_version` 填字面量 `'preseed'` 不许填 K7-pack-v1**(人工配的成绩不许算到包头上,防污染 §12.5 按包归因)。§五 表汇总 20→**21 张**、分块序列加 ③-K7 / ④b 并附「K7 需求落位一览」、V2 验收标准加三条
- 2026-08-02 · ✅ **V2.0.0 Plan 用户验收通过 + D1–D8 全部拍板 + 两项追加裁定入档**(@planner,只改文档,零代码改动):① **D1、D3–D8 照 planner 建议默认通过**,§五 清单表由「建议默认、可推翻」改为「**已拍板(2026-08-02 用户)**」;② **D2 = A 路定死** —— 新机挂新子域 **`nk.linotsai.top`**、**DNS 解析是用户动作**(用户原话:「我本来就要换云服务器,NK 这个域名我会直接把它解析到新的云服务器上」),老机 `ln.linotsai.top` 原样服务到 ⑰ 换包完成,**契约一次性换血不留过渡键**;**B 路(沿用 `ln`)全线删除**,⑭-D / ⑯-G / ⑰ / §七 P3-27 / §八 第 12 项五处的 B 路分支已收掉、只留删除线留痕(**不给 builder 留两条路犹豫**),同时写明「两步淘汰纪律本身仍有效,V2 只是靠换机窗口结构性绕开它」+ A 路成立的前提条件(**新机 nginx 绝不接管 `ln`**);③ **云上组件迁移置尾声** —— §五 新增「工序时序纪律」节 + ⑯ 块首改写:**①–⑮ 全部本地推进优先、⑯ 不与前面并行穿插、⑮ 完工前不碰任何服务器**,⑯ 内五项纯本地子项(三个 oneshot 单元 / nginx 模板 / `activate_pack.py`+首包 / `preseed_baskets.py` / 迁移清单)标 **📦 本地可先备料**(写好不上机跑,不拆走);§八 第 14 项由「走 A 路时才需要」改为**必办项**并加时机说明(按尾声节奏办、不必现在催)
- 2026-08-02 · 🏗️ **V2.0.0 立项**(@planner,只改文档,零代码 / 零数据库 / 零部署)· 架构设计稿全文转写进 PROJECT_PLAN,**架构稿即刻转历史档案**(`archive/V2架构设计稿_20260731_20260802立项归档.md`),防双权威。**§五 = V2.0.0 详版施工图 17 块**:① 表与共享信息层地基(**20 张新表**一次到位 + 冻结/追加/不回写三律守门)② LLM Provider 自填制 + 双 Agent 路由 + 预算三本账 ③ 选股策略包机制(`selection_packs` + 原语白名单 + `engine_api_version` + 四道闸 + 首包 K4-pack)④ 市场扫描层三张预计算表 + 驱动种子 ⑤ 驱动聚合层(LLM + 成员白名单闸 + 角色对拍闸)⑥ Tier 分层引擎 ⑦ 篮子卡冻结 ⑧ D+1 验证(关注池改造 + 盘中存拍 + 状态机)⑨ 盘后复盘 + 评价引擎 ⑩ 极简台账 + 计划继承 + 决策日志表单退役 ⑪ 监控 80/15/5 + 通知三级 + NL 提醒 ⑫ 对账与画像 ⑬ V1 清理十三项 ⑭ 篮子日报 + 契约总装 + 三方对拍 ⑮ 客户端双端改版 ⑯ 新服务器 + 管线多段化 + 割接 + 首包激活 + Tier 预填充 ⑰ 双端换包(等用户指令)。**§二 新增 §2.8 语义换血总表**(篮子取代单票候选 / Tier 取代排序 / 三级取代六类推送 / 自动快照取代强制表单;**历史拍板一条不删**;**§2.8-C 重新表述第〇原则四锁** —— 「不进排序」精确为「不进机械分」、「不进哨兵」精确为「LLM 文本不进判据 + 关注池是注意力分配非判定」);**§三 新增 §3.10 V2 技术选型增补**;§一 新增 1.5 V2 定位。**§七 Backlog 逐条处置**:吸收 P1-5/P1-8/P3-11/P3-15/P3-27,废弃 P4-16(换机后 800M 数字作废),新挂 P3-28(归因标签稀疏)/ P3-29(检索单点)/ P4-30(初期分时残缺)/ P4-31(停写留档表清理),新立「选股策略包门禁」长期机制。**§八 新增 V2 用户操作四项**(购新机 / 子域 DNS / 填两条 Provider / 分时外源可选),第 9 项同花顺 txt 作废。**planner 补位三张表并如实登记理由**:`llm_providers`(`app_settings` 是单行表装不下注册表)、`entry_snapshots`(决策日志表单退役需要落点,`decision_log` 的 NOT NULL 列无法就地放宽)、`selection_pack_activation_log`(§12.4 要激活日志但未给表名)。v1.5 施工图全文 + v1.5.2 收官快照 → `archive/v1.5_施工图_20260802归档.md`
- 2026-08-02 · 📐 **选股策略包机制定稿入档**(用户批准,`V2架构设计.md` 新增 §十二):声明式配置包(非代码插件)、包只管①种子规则+③Tier权重、原语白名单+`engine_api_version` 兼容校验、新表 `selection_packs`(append-only 单现役,激活复刻四道闸体例)、按包版本归因、K 字头版本、初版把现成选股代码整理成首包灌入。纯文档,零代码改动
- 2026-08-02 · 📋 **V2 追加裁定三项入档**(`V2架构设计.md` §〇 #9–11):① V1 清理遗留定案——自选池+同花顺对账**直接删**、呼吸台账**删**、五常驻保底删除**确认**;② V2 不设「首战核验/待验」类条目,验证归用户,施工线只交付 APP;③ 选股板块预留**可轮替策略包插槽**(策略线打包交付、系统线校验激活,机制设计讨论中)。纯文档,零代码改动
- 2026-08-02 · ⚖️ **⑧ 提的两处归档排期完毕 → 新增 ⑧-E(除权除息锚失效检测,排 ⑬/⑭ 之前做)+ ⑭-A 补挂 EOD 验证拍 + 三条登记对齐**(@planner,只改文档,零代码 / 零数据库 / 零部署;⑨ 并行施工中,未动 ⑨ 块正文与 §四,hunk 级挑拣提交)。**① 除权除息锚缺口 → 裁定「现在做,不进 Backlog」,新增 ⑧-E**。四条量化理由:(a) 每日被验证的篮子成员约 **40 只**,A 股除权除息集中在 5–7 月(年报)与 9–11 月(中期),峰值日 1–2%、均值 ~0.5% → **每 1–3 天就有一个成员中招**;(b) `min_members_hit=ceil(n/2)` 使 **2 只成员的篮子门槛是 1** —— 单只误判就能把整篮打成 `falsified`;(c) `falsified` 是**当日终态、不撤回**,误判挂一整天且不报错;(d) 它污染的正是 ⑨ 的验证率 / 证伪率,而那是 **P3-34 回看条件集的唯一依据** ——「拿被污染的数据校准判据 = 错上加错」,且 **V2 上线时点大概率正撞中期分红季**。**修法否决了 builder 提的「⑦ 卡里多冻一份原始价锚」**(D0 那天 `qfq == raw`、多冻的是同一个数;「原始价 MA20」把除权前后价格混进一根均线**是个更糟的数**;还要改冻结件形状 + bump `spec_version`),**改用 `pre_close` 检测器**:A 股除权日交易所公布的**前收盘价 = 除权除息参考价**,故 `pre_close ≠ 卡里 D0 收盘`(带 `_EPS`)就是除权的**直接精确信号**;数据现成 —— 盘中 `Quote.pre_close`(新浪/腾讯都已解析)、EOD `daily.pre_close`(已在 `TABLE_FLOAT_COLS`),**同一判据两条路径,不破 ⑧-C2 的对称性,⑦ 卡形状一个字不动**。命中后**复用 ⑦-b 已有的「两侧都不计」机制**(不发明新机制),⛔ 绝不把锚失效算成失效命中。**⛔ 不做自动 rescale**(虽然 `pre_close/D0_close` 数学上就是精确调整比例)—— 因为 `pre_close` 对不上有**两种成因**(真除权 / 数据错),盘中分不开,自动 rescale 会**把该报警的故障变成静默的错误判定**;宁可当天不判,不要判错。**EOD 用 `adj_factor(D+1) vs adj_factor(D0)` 交叉确认把两种成因分开**:变了 → `member_ex_rights` 正常降级不报警;没变 → `anchor_mismatch` **打 WARNING**(真故障该被看见)。排期 = **⑬/⑭ 之前**,不阻塞 ⑨,但 ⑨ 在本子项落地前攒的验证率数据含已知污染,P3-34 回看时须把这段窗口标出来。**② EOD 验证拍的调用点 —— 核对结果:⑭-A 原文确实没写,已补**。`pipeline.build_report` 分段改为「16:05 拉数 → **⑧ EOD 篮子验证拍** → ④ 扫描 → ⑤ 聚合 → ⑥ Tier → ⑦ 卡冻结 → 报告落库」,并写明**位置定死在「拉数之后、扫描层之前」**(它判的是昨日冻结的卡在今日的收盘表现,与今晚要生成的新篮子无关;放前面既符合因果,又保证后面某段炸了昨日定论行也已落好)+ 包保险丝 + 点名「**⑭ 落地前 EOD 定论行不会自动产生,是已知窗口期不用治,但 ⑭ builder 必须挂上**,否则三路读法永远只能读到盘中暂态」。**③ 三条登记顺手对齐**:(a) ⑧-A 补注「ETF 部分未做**不是遗漏**、无数据源、现状是板块基准指数」并新挂 **§七 [P4-35]**(点明蓝图 5.4「板块 ETF 或指数失去承接」这条监测**指数只能替代一半** —— 宽基反映不了单一题材的资金去留,⑪-A 落地时按现状实现并如实标注);(b) ⑧-B 写明 `capture_status` **落点 = `sentinel_events`** 与三条理由,并定死 **⑨ 的读侧入口 = `capture.load_capture_status()`**(⛔ ⑨ 不要自己翻 parquet 数行数;它区分「没记过」与「记了是 missing」),同时明确 **⑧ 只落原始数字、不替 ⑨ 发明「多少算够」的阈值**;(c) ① 的 parquet 列草图**更正** `intraday_ticks.trade_date` 为 **`pl.Date`**(原文误写 TEXT),并写明理由与「**⑧ 按 `pl.Date` 落地是对的,别照原文改回 TEXT**」
- 2026-08-02 · ⚖️ **⑦ 提的两处裁定完毕 → 新增 ⑦-b(验证/失效条件集定案)+ ⑦-K7-b(共振邻域读法),⑧ 解除阻塞**(@planner,只改文档,零代码 / 零数据库 / 零部署)。**① 验证 / 失效条件集与聚合规则(阻塞 ⑧)** —— **归属裁定 = 引擎默认 + P3 登记,⛔ 本版不进包**(与 ⑥-b 结论相反,三条理由写死防类推):(a) **§12.2 插槽边界是用户拍板的**,明文把「④篮子卡冻结体例」列在「引擎本体,不进包」一侧,要包化必须走「扩插槽边界(用户拍板)→ 扩 schema → 发包」三步;(b) **零审计背书**,⑥-b 的质量线作用在 K7 带审计的权重上,这套条件集连方向性证据都没有,进包会被"体面化"成"策略线校准过的参数";(c) 它有下游机械消费方(哨兵)—— ⚠ 但同时澄清**包化的风险并不来自「换包改盘中判定」**:spec 在 D0 就冻进 `card_json`,⑧ 读冻结件,换包只影响此后新卡(与 `stop_pct` 指纹同机制),真正理由是 (a)(b)。**落地要求**:阈值从函数关键字默认值**集中到一处命名常量** + 登记 `_ENGINE_CONSTANT_WHITELIST`(注释写明"临时默认、零审计背书")+ **新增 `verification_ruleset_version` 落卡指纹**(与跟形状的 `spec_version` **分开** —— 没有它,⑨ 会把两套条件集的成绩混成一锅)。**条件集定案**:采纳 builder 结构,**修订失效侧第 ③ 条** —— 原「`收盘 < MA20`」与验证的「`≥MA20`」**互为反面**,擦边跌破就判「驱动已证伪」,`unclear` 几乎没有生存空间、`falsified` 打得又早又滥;改为「`< D0 收盘` **且** `< D0 MA20`」两条 AND,**中间地带才落回 `partial`/`unclear`,四个格子各有地盘**,且**零新常量**。补齐 Plan 缺口:**失效侧聚合门槛**(builder 只定了验证侧)取同一个 `min_members_hit`(对称、不发明第二个数;按角色加权留 P3-34);**四态映射表**定死 + **`falsified` 优先级压过一切**(宁可先说坏消息);**数据缺失 ≠ 证伪**(两侧都不计 + 标 `member_data_missing`)。**语义红线**:`verified` 证明的是「没走坏 + 共振存在」,**不是「驱动兑现」更不是「可以追」**;**止损线在这里只判「驱动是否被证伪」、⛔ 不接任何持仓动作**(与哨兵执行的 −5% 纪律同数字同单一源但是两回事)。**③ 盘中 vs EOD 子集(⑦ 问的第三点)→ 新增 ⑧-C2**:**同一份 spec 不拆子集,拆的是代入什么价**(盘中现价 / EOD 收盘);**锚一律取 D0 冻结值,盘中不重算** —— MA20(盘中根本算不出今日 MA20)、跌停价、止损线用的 `stop_pct` 都读卡里的指纹,**不读"当前现役 config"**(否则同一张卡在章程切换前后讲两套话);**状态未变不落行、变了才追加,EOD 那一拍无论变没变必落一行**;**「当前状态」三路读法**定死(有 EOD 取 EOD / 只有 intraday 标「盘中暂态」/ 都没有标 `not_evaluated` —— 「还没判」与「判了是 unclear」必须分开);**`falsified` 是当日终态不撤回**(承 v1.3「D5 判向定格一次」,堵死"跌破了→又回来了→系统改口说没证伪"),`verified` **不是**终态可翻;**无卡的篮子不判**(落 `unclear`+`no_card`,⛔ 不许拿默认条件顶上 = 凭空编判据)。**② ⑦-K7「簇内连板高度第一」→ 采纳共振邻域读法(新增 ⑦-K7-b)**,三条理由:审计粗簇(一票一簇)与 ④ 多路聚类**本就不同构且 ⑥ 已立过「簇不必同构但语义必须保住」的兼容注**;字面读法在多路聚类下是**逻辑上的必然膨胀**(实证 36/42 = 86% 命中 = 背景噪声不是警示);「邻域至少有一只更低」是**文案自洽的必然**(说「3× 于后排」得先有后排)。并列最高都打标(警示从严,标注件不进排序无唯一性约束);实证三天命中 6/6/5。**③ 顺手补两处契约欠账**:⑭-B 点名 **`card_not_ready` 是全新字符串、`mapReason` 必须加新 case**(⛔ 不能指望 fallback —— v1.4 `watchlist` 的 `not_found` 被误显示成「持仓已清」的案底),并要求 `basket_not_found`(篮子不存在)与 `card_not_ready`(篮子在、卡未生成)**两个 404 分得开**;⑮ 点名 **`card_json` 与 `reviews.result_json` 两个冻结快照 DTO 必须手写 `init(from:)` + 全字段 `decodeIfPresent`**(合成 `Codable` 的后果 = 新 App 翻老卡整张解不出)。**④ 新挂 §七 [P3-34]**「V2 判据的工程重解读 / 零审计背书登记册」(与 P3-32/33 分开挂 —— 那两条问数值取多少,本条问判据语义落得对不对),两项 = 共振邻域重解读(**策略线须知**)+ 验证/失效条件集零背书(待 ⑨ 归因)
- 2026-08-02 · ⚖️ **⑥ 提的两处 Plan 缺口裁定完毕 → 新增 ⑥-b 追加子项(质量线进包 + T3 下限 + 溢出披露)**(@planner,只改文档,零代码 / 零数据库 / 零部署;并发窗口内未动 ⑦ 块正文与 §四,提交走 hunk 级挑拣)。**① 档位质量线归属 → 裁定「进包」**(`config.tier.quality_lines`,不是引擎常量):照 ⑤-c 立的框架(引擎常量治统计/工程有效性、pack 参数治策略偏好且 regime 敏感),质量线属后者;**决定性理由是标度耦合** —— 质量线与五维权重**作用在同一标度上**而权重已在包里,把线留在代码里 = 策略线换一次权重就**静默改变 T1 选择性**且无人察觉(这正是它与 ⑤-c 那个门槛的关键差别:成分数 5 与包里任何数都不共享标度)。schema 定死 `{tier1_min, tier2_min, tier3_min}` 与 `weights`/`dims`/`stage_scores` 平级、**可选键**;照 ③-K7-C 规则逐条核过 **不 bump `ENGINE_API_VERSION`**(纯增量可选键 + `K4-pack-v1` 原样重校验仍通过 + 缺键回退现有引擎默认 → 旧包行为逐位不变),落点 `pack.py::validate_config` 与 `stage_scores` 同一处;**`quality_lines` 缺键回退 vs `weights` 缺维度 fail loud 两种姿势刻意不同**(K4-pack-v1 不重发版、是 ⑯-E 回滚锚,不给回退就等于作废回滚锚),理由写死防被"统一";引擎常量降级为「缺省回退值」不删;`packs/K7-pack.json` 补键 `0.60/0.40/0.25`(合法窗口 —— ③-K7 已写明 ⑯-E 之前是最后改包窗口);校准走 ⑨ 周报 + 进化门禁。**② planner 主动纠一处:T3 也要有下限** —— 原实现「T3 无下限」使 `T3≤10` 成为**事实上的配额**(只要有 ≥10 个候选就永远填满),而蓝图 4.4「上限不是配额」是**对三档同时说的**,这正是 V1「每天硬凑 20 只候选」那个被 V2 立项要治的病;补 `tier3_min=0.25`(planner 拟的临时默认,比 T2 线明显低但不为零),并定死**三档全部可空、「今日无篮子定档」是合法输出、⛔ 不许为让报告好看放宽任何一条线**。**③ 容量溢出篮 → 采纳 builder 处置(不臆造 tier、不落库、留痕 `TierResult.dropped`)**,否决「全塞 T3 放宽容量」(会突破写死的 `T3≤10` 并把上限变回配额)与「`baskets.tier` 改可空」(① 已完工要重建表,且"今天没定档"本就不该在 `baskets` 里表达);**同时写死它与「⛔ 不许因为没卡把篮子抹掉」的边界** —— 那条治「**已作出并落库的判断**被下游失败藏起来」,溢出篮是「**系统明确没给出判断**」,编个 tier 才是不诚实;**但「今天有 N 个候选没被定档」必须披露**(否则用户以为今天全市场就这些篮子),故 ⑭-A 报告结构新增 **③b「今日未定档篮子」一行摘要**(篮子名/机械分/原因码,不出卡无 `basket_id`,零溢出时节仍在),**原因码必须分 `capacity_overflow`〔分数够位置满 = 机会多到装不下〕vs `below_quality_line`〔连 T3 线都没过 = 今天没好货〕,⛔ 不许合并**(两者指向相反的市场结论),契约 `droppedBaskets` 默认空数组。**④ 顺带钉死中性填充可见性**(承 ⑥ 登记 ②③):`leader_clarity` 的 `1/rank` 在 `rank=2` 时**恰好等于中性分 0.5**,「第 2 名」与「没数据」数值撞车 → **任何消费 `mech_breakdown_json` 的下游必须同时读 `flags`,⛔ 不许只看数值**(守门单测);新增可审计字段 `neutral_filled_weight`(中性填充贡献的权重合计),因为三维缺数据靠三个 0.5 也可能压过 `tier1_min` = 「**因为不知道所以进 T1**」;**刻意只记录+披露、先不设闸**(不凭空造阈值,承裁定 #5),要不要据此降档进 P3-33。**⑤ 新挂 §七 [P3-33]**(与 P3-32 分开挂 —— 要用的证据不同:P3-32 要主归属归因,P3-33 要 Tier 单调性 + T1 空档频率),一并收 `tradability` 两档扣分比例与中性填充降档两项。**⑥ 顺手修一处结构缺陷**:并发编辑导致 ⑥ 的 `###` 标题与「目标」行被错位到 ⑤-b/⑤-c 与【跨块裁定】节**之前**(⑤-b/⑤-c 渲染成了 ⑥ 的下级),已把标题移回它自己的正文之前并补回缺失的 `---` 分隔
- 2026-08-02 · ⚖️ **⑤ 提的三处 Plan 缺口 planner 裁定完毕,⑥/⑦ 解除阻塞**(@planner,只改文档,零代码 / 零数据库 / 零部署;并发窗口内未动 ④b 块正文、§四 只做增量小改)。① **落库次序自相矛盾(阻塞项)** —— 原「V2 新表汇总」把 `baskets`/`basket_members` 写入登记在 ⑦,而 `baskets.tier` NOT NULL、tier 由 ⑥ 定档、`tier_history.basket_id` 又要 `baskets.id` → 循环。**裁定**:新增【planner 裁定 · 跨块】节(排 ⑤ 与 ⑥ 之间)定死运行期次序 `⑤内存 → ⑥定档 → ⑥【事务1】baskets+basket_members+tier_history → ⑦生成卡 → ⑦【事务2】basket_cards`;**刻意两个事务**(卡生成要调 LLM,跨 LLM 持事务是错的);写入口统一搬新模块 `neckline/selection/basket_store.py`(⑤ 的 `save_baskets` 平移 + `aggregate.py` 同名再导出、既有 56 例一字不改,照 ⑤ 自己刚做过的 `json_block.py` 体例),`save_baskets` 加可选 `conn=` 以并入事务;四表一律 `INSERT OR IGNORE` **冻结不覆盖**(并写明**这与 v1.5 契约线 🟡-1「重跑要清旧行」不冲突、别套错模板** —— 那条治的是两处讲相反的话,这里同批冻结只有一个说法),重跑不一致 → WARNING + 报告如实披露;**「有篮子无卡」定为合法中间态**(⑦ 可同日重跑补 `version=1`;`GET /baskets/{id}/card` 无卡返 404 + 新 reason `card_not_ready`,⑭-B 与客户端 `mapReason` 同步;**⛔ 不许因为没卡就把篮子从报告里抹掉**);表汇总 1/2 的写入块由 ⑦ 改判为 ⑥、3 标「⑦ 只写这一张」、6 标「与 1、2 同事务」;⑥/⑦ 两块各补「落库职责」段与验收三条。② **成员卫生线无人认领** → 新增 **⑤-b**(⑤ 主体已完工,追加子项):落点定死在**「装配给 LLM 的成员清单」之前**(先过滤再按 `MAX_MEMBERS_IN_CONTEXT` 截断)—— LLM 压根看不到脏票、**既有白名单闸自动兜住**、不用造第三条拒收路径;**判据一律复用 ③ 的 `stock_hygiene`/`non_new_stock`/`k4_advisory_gate` 三原语读现役包,⛔ 禁自造第二份**(采纳 ⑤ builder 不自造"部分版卫生线"的克制);`hard_cut→exclude`/`avoid_flag→tag` 两档语义照包(标传 ⑦ 展示 + 计入 ⑥ `card_density`,即「机器不禁、人可复核」);算力只算候选成员集、当日一次装配全篮复用、耗时计入 ⑯-C;**保险丝拆两级** —— ST/停牌/次新/板块**永远拦**(数据源便宜且必在,算不出即异常、该篮拒收),`ma20`/`amount_ma20` 面板缺失才降级不拦 + 标 `hygiene_unavailable`;被剔票留痕且与白名单闸拒收**分开计数**。③ **lift 小样本失真**(涨停簇 2–5 只、实测 70–90 倍 lift,与 v1.3.1 百级成员先例不可比)→ 新增 **⑤-c**:加**最小成分数门槛 = 5** 作**引擎常量不进包**(治的是**统计有效性**不是策略偏好,进包会被误当 alpha 旋钮;与 `industry_strength._MIN_MEMBERS` / K7 五态「成员≥5」**同源同值**,实现须显式引用同一常量防漂移);不达标篮 `lift=None` + `lift_reason='sample_too_small'`(**「算不出」≠「等于 0」**)、不参与主归属比较;全不达标 → `(成员数降序, basket_key 升序)` **确定性兜底** + `primary_reason='fallback_no_qualified_lift'`;**篮子本身不因不达标被剔**;策略语义那一半(5 合不合适 / 小簇是否天然该输)**登记 §七 [P3-32]** 交策略线 / 用户在 ⑨ 攒够归因样本后过目,**在那之前 builder 不许自行改数**。另顺手钉死两条低优先级:**⑤ 疏漏 ⑦ 的自洽读法**写进 ⑤ 篮子规则条文(判据是「本篮每颗种子是否都真的被检索过」——「查了没有」→ 不成篮 / 「没查」→ 照出并明示 / 混合态 `partial` 不判不成篮,即「没有」与「没看」必须分得开);**`neckline/llm/json_block.py` 不随 ⑬-3 陪葬**已登记进 ⑬-3(它是通用件、零 import `report`,删 `reference_plan.py` 时只删那层再导出,并加守门单测)
- 2026-08-01 · 📁 **V2 蓝图入库**(commit `c1c7187`):产品总纲 `新版本量化交易APP与选股架构.md` + 工程架构设计稿 `V2架构设计.md`(2026-07-31 八项用户裁定已定格;**未立项未施工**,立项时经 planner 转写进本文件)+ `.learnings/ERRORS.md` 错误台账;V2 施工线接管交接完成
- 2026-07-30 · 🐛🚀 **用户报障快修 + 上云:LLM 没有「现在」的概念 → v1.5.2(⑨,@builder-pro,15:08–15:12 收盘后窗口部署,🔴 高危区,未回滚,**不换包**)**。**部署证据**:双备份 `neckline.db.{bak,cpbak}-v152-20260730-150653`(30318592B,integrity ok,0.31s);dry-run 零删除、5 个改动文件 sha256 逐个吻合、属主自检两次绿、依赖零变更;preflight `VERSION=v1.5.2`/43 路由 + 四条 prompt 均含时效纪律;15:08:57→15:08:59 重启中断 ~2s、`NRestarts=0`、error 0 条、`memory.events` 全零;公网 `/health`=**v1.5.2**;**本版零迁移**(26 表 / `reference_plans` 25 列不变)、`integrity=ok`、15 张业务表行数逐表一致、`is_active` 仍 `v1.3.3` 未变;活体——老快照读取不崩、持仓两笔 D1 无回归、info-card 2.23s、404 reason 精确、无 token 401;**⑨ 专属实地证据**:生产上装配的问询台/审判两处上下文首行均为「今天是 2026年7月30日(周四);下一交易日是 2026年7月31日(周五)。」(生产真日历解出),检索词实测带「2026 最新」。**回滚绳** = 该双备份 + `a984596`。**报障**(带截图):问询 603298 的回答把 **2024 年研报的目标价**当现行参照。**排查**:生产 `inquiry_log` 实证**联网是通的**(两次问询 search_hits 2489 / 8565 字节、含 2025-06 命中),但命中新旧混杂;**真因 = 三处 LLM 提示词一处都没注入当前日期**,模型没有"现在"的概念,2024 年研报与上月公告在它眼里一样新。**修三件(提示词层,零契约、客户端零改动)**:①**当前日期锚**——问询台/审判/参考件三处上下文**第一行**统一插「今天是 X 年 X 月 X 日(周 X);下一交易日是 …」,日期与交易日动态取(`CN_TZ` + `neckline.calendar`,零硬编),参考件那处额外**点名「明早 = 下一交易日 X」**(周五生成时"明早"是下周一)且与涨跌停锚同源,补跑历史日时如实说明基准日不撒谎,日历算不出只少一项、不抛异常;②**时效纪律**——四条 system prompt(问询台/候选审判/自选审判/参考件)嵌同一份 `TIMELINESS_RULES`(引用必须带日期 / 超半年必须明示「截至 X 的旧数据」/ **旧研报目标价只能作历史参照、不得当现行定价基准**);③**检索词时效引导**——问询台 `_build_search_query` 与**新增的**审判侧 `judge_search_query` 都补当前年份词「2026 最新」,**只改检索词文本、不加任何新 API 参数**(v1.3.4 案底:不被上游认识的参数会静默 0 命中)。**单一实现** = 新模块 `neckline/llm/prompt_context.py`,三处 import,守门单测扫全仓禁止第二份拷贝。**两处实现取舍**:①年份词**紧跟主体不放最末**(GLM `max_search_query_chars=78` 截尾会把末尾时效词连同用户长问句切掉,单测用真常量跑过截断验证);②审判链路此前**从不显式传 `search_query`**,本次改为显式传——是 v1.3.4 已验证的既有参数不是新参数,但审判侧 GLM payload 自此多一个字段(三处测试桩同步)。测试:Python **2003 过 + 2 skip**(基线 1969+2,净增 34),iOS `xcodebuild build` SUCCEEDED(Swift 源码零改动、仅版号)。版号三方联动 **v1.5.2**,**不换包**(已装 1.5.1 App 设置屏会显示版本差提示,属预期)
- 2026-07-30 · 🚀 **v1.5.1 上云 + macOS 换包 1.5.1 完工(⑧-E,@builder-pro,用户 11:58 明示授权提前到午间休市执行,🔴 高危区,未回滚)**。**窗口** = 12:07–12:17 CST(11:30–13:00 休市内),12:07:06 restart → 12:07:08 startup complete = **中断 ~2s**,开盘前 40 分钟收工;**本轮零改动 `neckline/sentinel/`**(`git diff 510bf20..HEAD` 实查 0 文件),盘中行为不变,未碰 09:26 推送与 16:05/16:35 两 timer(下次触发 4h+ 后,`load 0.12`、无运行中 job)。**部署前双备份** `neckline.db.{bak,cpbak}-v151-20260730-115951`(各 30244864B,`.backup` integrity ok、耗时 **0.34s**)——**回滚绳** = 这对备份 + v1.5.0 代码态(`510bf20`)。**唯一迁移** `reference_plans.take_profit_retrace REAL` 由 `init_schema` 幂等补列成功,该表 **24→25 列**;`integrity_check=ok`、**14 张业务表行数迁移前后逐表一致**、**`is_active` 仍 `v1.3.3` 且 `activated_at` 未变**、`strategy_activation_log` 仍 3 行。`sync_code.sh` dry-run **零删除**、实传后属主自检**两次绿**(`data/` `2770 neckline:neckline`、`.env` 600);**生产 9 个关键文件 sha256 与本地逐个吻合**;preflight import **`VERSION=v1.5.1` / 43 路由 / 38 去重路径**(无新端点);**依赖零变更**;重启 `NRestarts=0`、journal **error/exception/traceback 0 条**、`memory.events` **全零**、idle 86→98MB;本地 + 公网 `/health` = **`v1.5.1`**。**活体**:`/report/latest` 200 读昨日快照——20 只候选老四件套四键全非空(过渡文案)、`referencePlan` 全 null、`judgeSkipped` 全 false、16 条自选 `dispatchAlerts` 非空 0 条 = **老快照兼容不崩**;info-card **2.25s / 17392B**;404 reason 逐个精确(`report_not_found`/`code_not_in_report`/`not_found`);无 token 全 401、`/health` 免鉴权 200;全部非 2xx 均来自本次 127.0.0.1 探针,App 侧全 200。**macOS 换包**:旧包先 `ditto` 备份到 `~/Lino/app_backups/`(另移一份进废纸篓,**未用 `rm -rf`**)→ **`ditto`** 装 `/Applications`,**产物与安装态可执行 sha256 逐字节一致**(`204304ab…`)、`1.5.1`、universal、签名 valid + Designated Requirement,启动无崩溃且服务端记到该 App 真实带 token 请求 5 端点全 200。**iOS 未换包**(装机是用户动作)。**⚠ 实盘状态已变(用户今晨自行处置,早于部署)**:`300759.SZ` 已 `STOP_LOSS` 清仓、`300261.SZ`/`002036.SZ` 均 `MANUAL` 清仓(三笔 `updated_at` 在 09:52–10:32,`positions` 行数部署前后恒 5);现持仓 = `601567.SH` + `600499.SH` 两笔 D1。**🔴 今晚 16:35 首战四项待验**见 §四
- 2026-07-30 · ✅ **v1.5 两线 review 出报告(0🔴 + 3🟡)→ 三张黄牌当天全部修完 + 四条便宜观察项顺手做掉,代码态 `v1.5.1`(当日 12:07 已部署,见上一行)**(审计 @reviewer 两线并行,修复 @builder-pro,**纯本地施工、零生产访问**)。报告存档 `archive/REVIEW_REPORT_v15_{判定线,契约线}_20260730.md`。黄牌处置(逐条按报告正解,不取最小修):**判定线 🟡-1**(commit `d80f57c`,单独提交供复核)verdict 标签被三件套 JSON 自由文本**双向劫持** → 改「先剥 ```json 围栏、再解析 verdict」,`judge_candidate` 新增可选 `narrative_splitter` 依赖注入(`llm/` 不反向依赖 `report/`,围栏解析仍是唯一实现)、剥出的 JSON 走 `JudgeResult.parsed_attachment`(不落库不进契约),`_parse_verdict` **逻辑一字未动**、老两条路径逐字节不变,prompt 补禁令,回归含审计员两个复现用例 + 一条**反证测**;**契约线 🟡-1**(`1783090`)同日重跑 + 预算耗尽时「陈旧审判结论」与「本次未发起」并存 → **写侧收口**(标 skipped 时批量删当日该批码的 `llm_judgments`/`reference_plans`,单事务幂等),**不读侧遮蔽**;**两线共**(`3f0a5a2`)「章程 −5%/回落止盈 8%」硬编文案 → 参考件加口径指纹 `take_profit_retrace`(与 `stop_pct` 成对,幂等补列)+ 契约增 `buy.stopPct`/`exit.takeProfitRetrace`,markdown 与 Swift **各自动态生成**标签、缺指纹退化成不带数字说法。便宜项四条:🔵-2 Swift 未知 status 不再静默消失(展示态抽成可单测的 `displayState()`)、🟢-4 残留围栏(含未闭合半截)剥净、🔵-4 §四 清掉不存在的 `judgeSkipReason`、🔵-5 `WatchlistCheckOut` docstring 更正;其余 🟢 留档不做。版号三方联动 **v1.5.1**。测试:Python **1969 过 + 2 skip**(基线 1942+2),Swift iOS Simulator **178 跑 / 13 skip / 0 fail**,iOS+macOS build SUCCEEDED,macOS Release 产物 1.5.1 就绪(签名 valid,**未换包**)。**唯一迁移** = `reference_plans.take_profit_retrace`(该表生产 0 行)
- 2026-07-30 · 🚀 **v1.5.0 上云 + macOS 换包 1.5.0 完工(⑥+⑦,@builder-pro + 用户「立即执行」授权,🔴 高危区,未回滚)**。**🚩 门禁豁免**:上一版定的「16:35 首战验收三项不过不得开跑」被用户**明示豁免**——后果是 **v1.4.0/v1.4.1/v1.5.0 三版的 16:35 主链改动累计从未经过一次真实自动跑**(07-29 那两跑是 p1 代码,部署在其后),今晚 16:35 是三版叠加首战。**⚠ 时机**:简报说「凌晨」,实为 **09:40–09:47 交易时段内**;确认①今日 09:26 盘前推送已在 v1.4.1 跑完(27 条 precall、`d5exit` 零条)②**v1.5 零改动 `neckline/sentinel/`**、③重启中断 ~2s 后照常执行。**证据**:本地 **1942 过 + 2 skip**;部署前双备份 `neckline.db.{bak,cpbak}-v150-20260730-094231`(30212096B,integrity ok,`.backup` 0.291s);`sync_code.sh` 属主自检绿 + dry-run 零删除;生产 7 个关键文件 sha256 与本地逐个吻合;preflight 43 路由 / 38 去重路径;**依赖零变更**;迁移两项(`reference_plans` 新表 24 列 2 索引 + `llm_judgments.search_engine` 新列)`integrity=ok`、**13 张业务表行数逐表一致**、`is_active` 仍 `v1.3.3` 未变、`activation_log` 仍 3 行;重启 `NRestarts=0`、零 error、`memory.events` 全零;本地+公网 `/health`=`v1.5.0`。**活体**:`002036.SZ` 仍 `suspended_hold`(D7)无回归;老快照读取兼容——老四件套四键返**过渡文案**、`referencePlan=null`、`judgeSkipped=false`、16 条自选 `dispatchAlerts=[]`;info-card 3.57s(v1.4.1 的 P1-26 修复仍生效);四个 404 reason 精确、无 token 全 401、邻居站点不受影响。**macOS 换包**:`ditto` 装 `/Applications`,**产物与安装态可执行 sha256 逐字节一致**、`1.5.0`、universal、签名 valid + Designated Requirement,启动无崩溃且服务端记到该 App 真实带 token 请求 5 端点全 200(⑤-E 版本号治理已修掉此前 1.3.4 的陈旧字面量)。**iOS 未换包**(装机是用户动作)。**今晚三项待验**:`ExecMainStatus=0`+时间戳 / `reference_plans` 当日真有行且三态合理 / 20 只全覆盖下的墙钟与 `judgeSkipped` 命中数 + `MemoryPeak` 是否仍 ≤838,860,800 且 `oom_kill=0`(P4-16 复核点)
- 2026-07-30 · **v1.5-⑤ 客户端跟进 + A2 版本号治理 + iOS/macOS 构建产出完工(v1.5.0 施工全部完工,仅剩 ⑥⑦)**(@builder,纯本地施工,零生产访问/零部署):Swift 侧——`TodayPlanView` 今日计划拆两块(①持仓股/②候选列表,各补定位文案);新组件 `ReferencePlanSection`(`Components/SharedUI.swift`)替换 `FourPieceDisclosure` 展示参考三件套(`ok` 态折叠展开、`vetoed`/`unavailable`/`nil` 三态直显不折叠,`judgeSkipped` 独立 chip 不与 `llmJudgment` 合并);`Candidate` 新增 `referencePlan`/`judgeSkipped`(新型 `ReferencePlan`/`ReferencePlanBuy`/`ReferencePlanExit`,普通 `Codable`);`WatchlistCheckItem` 新增 `dispatchAlerts`(新型 `DispatchAlert`,`.warn` 色调展示);顺带修复 `AppModel.beginPositionEntryFlow(fromCandidate:)` 进场理由预填(老 `buyPoint` 已恒为过渡文案,改用新纯函数 `entryReasonText(for:)` 取 `referencePlan.buy.why`,单测覆盖)。A2 版本号治理——`client/project.yml` + `pbxproj` app target 三处 `MARKETING_VERSION`→`1.5.0`,`neckline/api/app.py::VERSION` 本会话内同步改 `v1.5.0`(纯本地源码常量,未部署,是 ⑤-E 自身「三者恒等」验收要求的必然推论)+ 新 Python 守门单测 `tests/test_client_version_governance.py`;`APIClient.health()` 返回签名改 `(ok, version)` 元组 + 设置屏展示 App/服务端双版本。iOS/macOS Release 构建均 `BUILD SUCCEEDED`(`CFBundleShortVersionString=1.5.0` 已核实,产物路径 `~/Library/Developer/Xcode/DerivedData/Neckline-*/Build/Products/{Release-iphoneos,Release}/Neckline.app`)。§八第 12 项 iOS 装机说明改写。**视觉核对(真实本地后端)**:隔离 SQLite 库(拷四张只读表)+ 真 parquet(20260724)跑 `scripts/report.py` 生成真报告,一次性脚本(scratchpad,已清理)patch 出 ok/vetoed/judgeSkipped/dispatchAlerts 四态,iPhone16Pro/临时 iPadPro13 模拟器(用毕已 delete)截图逐一核对;ok 态展开内容与设置屏版本行因本环境 touch/scroll 能力受限未截图,靠单测 + 代码审阅兜底(CLAUDE.md 已补记该环境限制的新发现)。测试:Python 全量 **1942 过 + 2 skip**(基线 1940+2,净增 2 = 新守门测试本身 + guardrail 对新文件自动追加的 1 个参数化实例,零 server 业务逻辑改动);Swift 离线套件 153 个(`DTODecodeTests` 94/`AppModelTests` 53/`URLGateTests` 2/`AppConfigDefaultTests` 4)iOS Simulator `xcodebuild test` 全绿,双端 `xcodebuild build`(Debug)SUCCEEDED。语义红线 grep 零真实命中。规格偏差 4 处均实现细节层面必然补全,详见 §四
- 2026-07-30 · **v1.5-④ 存量配菜四件完工**(@builder,纯本地施工,零部署;**A3 对生产库做过一次只读 SQL 探针,已如实登记**):A1 自选票 K4 派发警示(`watchlist_check.py::attach_dispatch_alerts` 复用 `holding_k4_check` 同一份镜像,只取 A3/A3b 两码,契约 `WatchlistCheckOut.dispatchAlerts`,交叉断言锁死与持仓侧判定逐位一致,⛔ 不推 APNs)+ A3 `search_engine` 定案(`llm_judgments` 幂等加列 + `GLMProvider._SEARCH_ENGINE` 单一源 + provider 层透传;生产只读 SQL 实捞 20260724~20260729 共 4 交易日 / 40 条非降级审判 / 0 命中率 0.0%,判定基线健康→维持 `search_pro`,§七 P1-7 结案,未消耗 A/B)+ A4 测试卫生(`conftest.py::real_db_readonly_copy` session 夹具 + 三个 guardrail 文件六处裸调用改显式 `db_path=` + 新 AST 守门测试 `test_db_isolation_guardrail.py`〔避开纯文本 grep 对 `test_brain.py` docstring 的误伤〕,实测全量 pytest 前后 dev DB mtime/MD5 逐字节不变,§七 P4-25 结案)+ A5 章程回滚 SOP(`archive/SOP_章程回滚_20260730.md` 一页纸四节 + 三处指针,§七 P1-24 结案)。测试:Python 全量 **1940 过 + 2 skip**(基线 1819+2,净增 121,大头是 A4 新增的 97 个参数化守门用例)。规格偏差两处均为实现细节层面合理补全(A1 markdown 渲染行、A5 第三处指针),已如实登记,详见 §四。
- 2026-07-30 凌晨 · **v1.5-③ 卡面改版 + 老四件套退役 + 报告分两块完工**(@builder,纯本地施工,零生产访问/零部署):候选卡老四件套(买点/止损/目标/证伪条件)全面退役——`intel_candidates.py::_build_intel_candidate` 不再调用四个文案函数(`entry_spec`/`invalidation_spec` 结构化字段一字不动,新增守门单测锁死键集),`Candidate` 对应四字段改带默认空串(dataclass 字段重排到 `invalidation_spec` 之后满足语法,全仓构造点均关键字传参、重排零影响,已核对);`api/app.py::_shape_candidate` 新增单一源常量 `LEGACY_FOURPIECE_NOTICE`,`buyPoint`/`stop`/`target`/`invalidation` 四键**无条件**下发过渡文案(不读落库快照、不按报告新旧分叉,老报告快照同样统一改发)满足已装 v1.4.1 客户端硬解码的向后兼容硬约束;`report/candidates.py` 四个文案函数保留不动(自选体检仍消费)。`render.py::_render_candidates`「前N只详情/后N只表格」两段结构合并成一段(全部候选统一走详情 + 全量速览表),候选卡新增执行提示行(`exec_hint.py` 既有计算首次渲染进 markdown)+ 参考三件套渲染(新函数 `_render_reference_plan`,ok/vetoed/unavailable/None 四态诚实展示,被夹逼拦下的子项不画空区间不写0)。报告 markdown 新增「持仓体检」节(`_render_holding_check`,排在候选**之前**,数据源 = pipeline 已算好的 `holding_k4_check`,零新增计算,时间退出五态含 `SUSPENDED_HOLD`、K4 强/普通警示按持仓侧 level 分级,与候选侧 hard_cut/avoid_flag 红黄牌是不同分类维度)。语义红线全仓 grep 巡检(推荐买入/建议买入/看好/值得买/推荐买点/目标价/止盈线)零真实违规(命中均为负向表述或指向 `decision_log` 用户自填目标价等不同概念)。测试:Python 全量 **1819 过 + 2 skip**(基线 1790+2,净增 29:`test_render.py` +22 / `test_intel_candidates.py` +1 守门单测 / `test_pipeline.py` +2 接线单测 / `test_api_report_board.py` +1 契约测),哨兵既有单测 105 个逐位复跑绿零改断言;未动 Swift。**一处规格与实现小出入(已如实登记)**:计划中「定格日晚点标注」若照 `api/app.py::_locked_time_exit_day` 算出「晚了几天」需要交易日历 I/O,与 `render.py`「纯函数不碰 I/O」模块契约冲突,故 markdown 只展示定格发生的原始日期(已有字段),晚点天数不重复展示——该信息本就已由 `PositionOut.timeExitLockedDay`/`timeExitLockedLateDays`(v1.4-⑥-C)在客户端持仓卡呈现,markdown 报告未丢失任何用户可得信息。规格与现实基本无冲突,零 TODO 遗留
- 2026-07-29 深夜 · **v1.5-② LLM 覆盖面与预算重排完工**(@builder,纯本地施工,零生产访问/零部署):`pipeline.TOP_N_JUDGED` 10→20(=`TOP_N_TOTAL`,旧「前10审/后10不耗LLM」分档退役);判官循环抽成 `_judge_candidates_with_budget`(调用机制未动,仍逐票一次 `judge_and_build_reference_plan`),新常量 `CANDIDATE_JUDGE_BUDGET_SECONDS=1200`(独立预算,与 `news_alerts.LLM_SCAN_BUDGET_SECONDS=300` 不合并)——按 rank 升序审,预算耗尽从当前位置到列表尾整体标 `Candidate.judge_skipped=True` 且不再发起调用(不掐断进行中调用)。新字段 `judge_skipped`/`CandidateOut.judgeSkipped`(与 `llmJudgment.degraded` 语义分开,`app.py`/`render.py` 已接,render 新增专属文案分支不与真异常「未执行」混淆)。**并发(②-C)**:本地三处核实均无 GLM/Kimi key → 限频无法实测 → 定案**串行+预算硬闸**(照 `news_alerts.py` v1.4-⑥-B 先例,理由入 docstring),未写任何并发脚手架。**时间账(②-D)**:本地实测机制自身开销(零网络)≈7–10ms/候选,可忽略;用等比缩小延迟验证预算边界确定性行为(调用前检查、不掐断进行中调用),反推生产最坏情形 20 只约 17 只能审完、约 3 只 `judgeSkipped`;已回填 PROJECT_PLAN「v1.5 LLM 预算账」节,与既有估算量级一致。测试:Python 全量 **1790 过 + 2 skip**(基线 1779+2,净增 11:`test_pipeline.py` 8 + `test_render.py` 1 + `test_api_report_board.py` 2);未动 Swift。规格与现实无冲突,零偏差
- 2026-07-29 深夜 · **v1.5-① 参考件三件套地基完工**(@builder,纯本地施工,零生产访问/零部署):新模块 `report/reference_plan.py`(`REFERENCE_PLAN_SYSTEM_PROMPT` 一次 LLM 调用出「自由评语+结论标签+三件套 json」,复用 `judge_candidate` 新增可选 `context_block` 参数、不改调用/解析/降级链;买入区间夹逼明日涨跌停〔唯一底线〕、离场区间只格式校验不夹涨跌停;止损价系统算 `close×(1−stop_pct)` 读现役 config;三态 `ok`/`vetoed`/`unavailable`)+ 新表 `reference_plans`(`reference_plan_store.py` 幂等落库)+ `Candidate.reference_plan`/`CandidateOut.referencePlan` 契约(老四件套四键与 `entry_spec`/`invalidation_spec` 一字未动,退役归 ③)+ `pipeline.py` 判官循环改调 `judge_and_build_reference_plan`(`TOP_N_JUDGED` 仍 10 不变,覆盖面扩到 20 是②的活;两层降级:上下文装配失败退默认上下文仍判一次、装配异常整段 try/except 兜底 `reference_plan=None`+候选照出)。守门三条全绿:`sentinel/` 全目录 grep 零命中 `reference_plan`/`referencePlan`(15 文件参数化测试)、`_SORT_KEY_INPUTS` 白名单新测锁死、`notify.__all__` 仍六类(既有测试原样过)。测试:Python 全量 **1779 过 + 2 skip**(基线 1710+2,净增 69:`test_reference_plan.py` 60 + `test_pipeline.py` 5 + `test_intel_candidates.py` 1 + `test_api_report_board.py` 3);本块未动 Swift(归⑤),iOS/macOS 沿用上次基线 169 跑 156 过 13 skip。规格与现实无冲突,零偏差
- 2026-07-29 · **v1.5.0 立项**(施工图七块就位待 builder)· 候选参考件三件套〔LLM 参谋化〕= ① 三件套地基(生成 + 明日涨跌停夹逼 + `reference_plans` 落库)+ ② 20 只全覆盖与 LLM 预算重排 + ③ 卡面改版与老四件套退役(键保留 + 过渡文案,哨兵结构化证伪不动)+ ④ 存量配菜四件(自选派发警示 / P1-7 定案 / 测试卫生 / 回滚 SOP)+ ⑤ 双端客户端 + 版本号治理 + iOS 构建 + ⑥ 部署上云(门禁 = 07-30 v1.4 首战验收)+ ⑦ 换包(等用户指令);§2.0 新立**第〇原则**(LLM 是参考、硬条款才是纪律,参考件不触发任何机器动作)、§2.3 补需求 9 拍板块;v1.4 施工图归档 `archive/v1.4_施工图_20260729归档.md`,需求 9 入档 `archive/交接_系统线升级需求_20260725.md`(@planner,只改文档,零代码改动)
- 2026-07-29 · 🔥 **v1.4.1 热修上云:信息卡从「总是加载失败」修到冷调 3.0s / 热调 2.25s(§七 P1-26 结案)**(@builder-pro,用户报障驱动,🔴 碰生产部署,**未回滚**)。**症状**:App 加载信息卡总是失败。**两层根因**:①服务端端点公网实测 **14.15 / 13.98 / 13.89s**,而②客户端 `APIClient.fetchInfoCard` 走 `get()` **默认 12s 超时** → **必然超时**;用户重试叠加把常驻服务顶到内存节流线(实测 3 次调用 `memory.events high` **+8945**,`MemoryCurrent` 440MB ≈ `MemoryHigh`),越试越慢。**⚠ 原诊断只对了一半**:P1-26 原文说根因是 `_load_single_code_panel` 全 glob —— 它确实全 glob,但**真正的大头是 `compute_sentiment` 一次请求做 5 次全市场横截面**,连同单票 420 日面板 2 次 + 大盘指数线 1 次 = **8 次全 glob**(每次打开 1592 个 parquet footer);所以修在 `market_data._scan_table` 这一层才对,只修 `_load_single_code_panel` 会治不好。**修法(修法①,纯 I/O、结果逐位不变)**:`_scan_table` 加 `years=` 参数,由 `get_market_slice`(单日→1 个年目录)/ `get_stock_history` / `scan_table_range`(区间→`_years_in_range` 覆盖年份)传入;`features.market_state_labels` 起点由写死 `date(2019,1,1)` 改为 `start-(ma_window*3+30)` 自然日(`rolling_mean` 是后向窗口,只需 `ma_window` 个交易日前置量,返回值照旧 filter 回 `[start,end]`)。合法性依据是**结构性**的:分区路径由 `day_file_path` 按 `trade_date.year` 生成,`year=YYYY` 里只可能有该年的行。客户端 `fetchInfoCard` 显式 `timeout: 60`(照问询台同样重的端点的既有惯例)。**公网活体验收**:`/health`=**v1.4.1**;info-card **冷调(重启后首次)3.05s**(判据 <10s)、**热调 2.33 / 2.25s**(判据 <3s)= **6.2 倍**;连打 **6 次** `memory.events high` **恒 0**(修前 3 次 +8945)、`MemoryCurrent` 440→287MB;`002036.SZ` 仍 `suspended_hold`+`priceStale{staleDays:5,suspended}` **无回归**;`/report/latest` 20260729 候选 20 只正常。**部署**:双备份 `neckline.db.{bak,cpbak}-v141-20260729-185132`(30199808 bytes,`.backup` 0.290s、`integrity=ok`)→ `sync_code.sh` DRY_RUN 先验(只传 3 个改动文件、`data/` 零触碰)→ 实传 + 收尾 prune chown + **属主自检绿** → preflight import(`VERSION=v1.4.1`)→ restart(顺带清掉节流态,`memory.events` 全部归零)。**纯代码零迁移**。**macOS 换包**:Release 构建 → **`ditto`**(非 `cp -R`)装 `/Applications`,换前旧包已 ditto 备份;已装包与新构建产物 **sha256 逐字节一致**(`15e9530f…`)且与旧包(`595b9a45…`)不同、`codesign --verify --deep --strict` rc=0。真机项按既有口径跳过。**⚠ 顺带收窄了脏分区的传染半径,已如实改写守门单测而非删断言**:v1.3.5 那条「写侧修好不会让历史脏分区自愈」现在只对**跨到脏那年**的读成立(单日/区间读不再打开无关年份),`test_dirty_first_partition_does_not_drag_new_write` 改为同时断言「同年读通过」+「跨年 `scan_table_range` 仍 SchemaError」。测试:等价单测 `TestYearPartitionPruning` 5 条(裁剪版 vs 全表版对同一份跨 3 年数据逐位相等,含缺年目录跳过);Python 全量 **1710 过 + 2 skip**(基线 1705+2,净增 5);Swift iOS Simulator **169 跑 156 过 13 skip 0 failure**、双端 `xcodebuild build` SUCCEEDED。**⚠ 留给主会话**:今日 20260729 报告快照是 **p1 代码 16:35 生成的**,故 `dataFreshness` 只有板块三键、无 ⑩-F 的行业强度三键,`infoCard`/`execHints`/`industryRank` 亦仍空 —— **明日 16:35 那份(v1.4.1 首次自动跑)才是这些新字段的首验**。
- 2026-07-29 · 🚀 **v1.4.0 完整上云成功 + macOS 换包(⑨ 完工,@builder-pro + 用户「现在立即执行」授权,🔴 高危区,未回滚)**。上午那次因 P0-23 回滚的部署,在 ⑩ 修复 + review 过门后**当天 17:00 重跑一次过**。**时序**:等今日 16:35 报告(**p1 代码最后一跑**,16:35:01→16:57:11、`ExecMainStatus=0`、报告真生成 61003B / `reports` 当日 1 行 / 候选 20 / 情报节 4837B、`MemoryPeak` 恰顶 800M 且 `oom_kill=0`)退出后才动生产;今日 16:05 日更亦 `ExecMainStatus=0`(16:05:00→16:06:28)——**⑨-D 两项当日证据齐**。**⑨-B 迁移四项全绿**:`max_chase_pct`/`inquiry_log`(上午已落、保留)+ `industry_strength_daily`(175,120 行 / 1592 交易日 / 110 行业,`verify` 三项自检 exit 0)+ **`strategy_activation_log` 播种验证**——先看源 `strategy_versions` 非空 `activated_at` 恰 3 条,再看果表内 3 条 `via='seed'`、**双向 `EXCEPT` 均空 = 逐字节一一对应**、非 seed 行 0,且 ⑨ 再跑一次 `init_schema` 后**仍是 3 条 = 幂等不重播**;`integrity_check=ok`、13 张业务表行数迁移前后逐表一致、`is_active` 仍 `v1.3.3` 且 `activated_at` 未变。**⑨-C**:属主自检绿、`.env`/`.p8` 600、preflight import 43 路由、依赖零变更、生产 6 个关键文件 sha256 与本地逐个吻合、重启 `NRestarts=0` 零 error、idle RSS 90→67MB 持平。**活体**:公网/本地 `/health`=`v1.4.0`;`002036.SZ` 仍 `suspended_hold`+`priceStale{staleDays:5,…,suspended}` 无回归;**上午永不返回的 info-card 端点现返 200 / 15.6s / 16431B**(K线60/RS线60/分歧线60,慢是 P1-26);四个 404 reason 逐个精确 + `/inquiries` 空态 200;无 token 全 401;`dataFreshness={20260728, lag1, stale:false}`。**⑨-F 定案 = `MemoryMax` 维持 800M 不改**(v1.4 加进主链的只是一次 5.3ms 读表、⑥-B 没开并发、信息卡不进 16:35;连续三跑顶满 800M 均 `oom_kill=0` 顶的是可回收页缓存;抬到 1G 后最坏并发 1624M > 物理 1612M——理由六条见 §四)。**macOS 换包**:Release 构建 → **`ditto`**(非 `cp -R`)装 `/Applications`,universal + 签名 valid + Designated Requirement 满足,启动无崩溃且服务端日志记到该 App 真实带 token 请求 `/circuit`·`/decisions`·`/report/latest`·`/board`·`/positions` **全 200**。**按授权跳过一切真机项**(iOS 未装机)。顺带:①`sync_code.sh` 的 `--exclude '.git/'` 改 `'.git'`(无尾斜杠)——上午从 git worktree 回滚时把 worktree 的 `.git` **文件**推上了生产,本次 `--delete` 清掉并堵住复发;②`app.py` 那段「本版未上云」警示改成失败部署留痕。**⚠ 顺带发现的实盘状态(非部署问题)**:`300759.SZ` 康龙化成 `todayAction`=「现价已跌破止损线…系统不代下单」,EOD `net_float` 三日递减 -344.9→-869.6→-1119.5,现役 `stop_pct=0.05`,**处置在用户**。**留给主会话**:v1.4 代码的**首次** 09:26 盘前推送与 16:05/16:35 自动跑(看 `ExecMainStatus`+时间戳),并确认当日报告新字段(`infoCard`/`execHints`/`industryRank`)真落地——今日快照是 p1 生成的,这三项仍空
- 2026-07-29 · ✅ **v1.4 上云前 review 两线出报告(0🔴 + 5🟡)→ 五张黄牌当天全部修完**(审计 @reviewer 两线并行,修复 @builder-pro,**纯本地施工,零生产访问**)。报告存档:`archive/REVIEW_REPORT_v14_判定线_20260729.md`(⑥-A 逐笔章程 / ⑩ P0-23 / P2-10 四个已发布版本)与 `archive/REVIEW_REPORT_v14_契约线_20260729.md`(P2-9 两路 + v1.4 新契约 14 条三方对拍)—— 两线均**未发现 🔴**,§七 P2-9 / P2-10 就此结案。五张黄牌处置(逐条按报告的「正解」修,不取最小修):**判定线 🟡-1**(🔴 纪律判定)激活时间线改 **append-only 事件流** `strategy_activation_log`,回滚/重激活不再改写历史周判定(commit `b2e3673`,单独提交供复核);**判定线 🟡-2** 行业强度日更**向前补洞** + 断口 ERROR/exit 1 + 有断口不许报绿;**契约线 🟡-1** 红黄牌展示序真读 DB `k4_advisory.intel_order`(六节最后一节的零消费方补上);**契约线 🟡-2** exec_hint C3 无前视截断补上 UTC/北京时区缝(决策日志日期过滤统一北京日);**契约线 🟡-3** Swift 补 `inquiryId` 解码断链。顺带做掉两条便宜观察项(🟢-8 陈旧注释、🟢-5 `forbid_high_elasticity` 跨切换单测缺口),其余 🟢 由两份报告留档 + §七 P2-9 一行挂账。测试:Python **1705 过 + 2 skip**(基线 1677+2,新增 28),Swift iOS Simulator **169 跑 156 过 13 skip 0 failure**(基线 168),macOS `xcodebuild build` SUCCEEDED。⚠ **⑨-B 迁移清单新增 `strategy_activation_log` 播种验证**(建表+播种由 `init_schema` 幂等完成,但必须验出 K1→v1.3→v1.3.3 三条)
- 2026-07-29 · 🔴 **v1.4-⑩ 生产旁路 bootstrap + 隔离实测完成,⑩-D/⑩-G 判据全绿(P1-26 一条按 plan 预授权另立)**(@builder-pro,用户授权提前到盘中 15:44 执行、明示接受与 16:05/16:35 窗口重叠的风险;**未部署代码、未重启任何服务、`/health` 仍 `v1.4.0-p1`**)。旁路目录 `/opt/neckline-p023/`(rsync 代码 + `data` 软链真实数据,**跑完先断软链再删目录,已删净**),用生产 `.venv` 跑、**零新依赖**(polars 1.42.1 + stdlib sqlite3 已在);全程 `systemd-run --scope --uid=neckline -p MemoryMax=600M -p CPUQuota=100%` **隔离单进程串行**。**⑩-D 五条判据**:①7 个年块**全 exit 0**,`memory.peak` **204~312MB**(判据 <500M)、各 **1.3~2.4s**(判据 <3min),`memory.events` 全 0 无节流无 OOM;Pass2 **27.9s / 104MB**;②`verify` **三项全绿**(交易日无洞 / streak 自洽 / 口径指纹一致),**175,120 行 / 1592 交易日 / 20200102~20260729**;③**随机 3 日单日对拍逐位一致**(20230803 / 20250904 / 20231229,各 110 行);④`stock_basic` 快照全程未变 —— 15:44 基线 5871|5538 与 16:11 的 5872|5539 之差是 **16:05 日更 `bootstrap_metadata` 刷的**(该 job `ExecMainStatus=0`、16:06:28 退出,**早于首块 16:09:02**),为取干净前后对**又幂等重跑了整套 bootstrap**,前后 `stock_basic` **5872|5539 未变**且表内容 **md5 逐字节相同**(`a1c1069eec9191892ac54c633fe327fc`,排除 `computed_at`)= **生产数据上的幂等性实证**;⑤`PRAGMA integrity_check=ok`、**22 张业务表行数与基线逐表一致**(唯二变动 `stock_basic`+1/`namechange`+2/`backfill_log`+4 均为 16:05 日更所写)、`is_active` 仍 `v1.3.3` 且 `activated_at` 未变、`.backup` 耗时 **0.074s**、库 4.59→**29.8MB**。**⑩-G 五条**:①同上;②日更增量 **0.16s / 110 行**(判据 <60s);③读侧 `load_industry_strength` **5.3ms**、60 日序列 **5.2ms**(判据 <200/<100ms);④信息卡主体 300M cap 下 **exit 0、峰值 98MB**(**此前永不返回**)但耗时 **18~20s > 10s 判据** → 按 ⑩-G 原文预授权**另立 §七 P1-26**(根因 = `_load_single_code_panel` 全 glob 1592 个 footer,与行业强度无关,行业那一维已是读表 5.3ms),**不阻塞本块**;⑤同上。**双备份**(迁移级动作)`neckline.db.{bak,cpbak}-v140p023-20260729-154459`(各 4587520 bytes,`.backup` 那份 `integrity=ok`)。**零碰撞**:16:05 `neckline-daily` `ExecMainStatus=0`(16:05:00→16:06:28,88s),我的首块特意等它退出后才起(串行纪律未被授权豁免);常驻 `neckline.service` **未重启**(`ActiveEnterTimestamp` 仍 10:09:47)、`memory.events` **全 0**(对照 07-29 上午那次 `high` 飙到 54 万)。收尾 `pgrep` 无残留(命中的两行是自己的 SSH 命令行 = CLAUDE.md 那个坑)、`systemctl --failed` 0 units、load 0.59 回落
- 2026-07-29 · 🔴 **v1.4-⑩ P0-23 修复「行业强度预计算落表」本地部分完工**(@builder-pro,**零生产访问**,生产侧 bootstrap + 隔离实测待 17:00 后):新表 `industry_strength_daily`(⑩-A DDL 照抄进 `db.py::_SCHEMA`,`NULL≠0` 三列 + 口径指纹两列 + 落全部行业含未达标);计算侧按 ⑩-B 拆 `_day_local_table`/`_attach_persist`/`next_persist_days` 三件(口径一字未改,`test_core_table_matches_research_bit_for_bit` 断言未动仍绿)+ `_ret1d_from_daily` 抽单一源;新 `report/industry_strength_store.py`(日更增量**只读当日一个分区** + `prev` 严格早于当日查询保幂等 + 补算历史日**自动向后延到库内最大交易日** + 每天一个事务 + bootstrap 两遍法 + ⑩-D 退路 `bootstrap_recent_days` + `verify` 三项自检 CLI/单测共用);新 CLI `scripts/industry_strength.py {refresh,verify,bootstrap}`;16:05 `daily_update.py` 挂 `update_industry_strength`(尽力而为不改退出码但**日志 ERROR** + 带补算命令原文);四处在线取数点 + ④分歧线 60 日序列全部改读表,保险丝四态落地(报告降级不崩且可复现 / A2 不拦 / 信息卡 rank+persist **如实缺省不写 0** / 分歧线**新增第三档 reason**「行业强度数据未就绪」与「样本不足」分开 / 问询台 `evidence` 明说本次不可得);`dataFreshness` 加三键(既有 `stale` 语义一字未改,两故障不合并)+ Swift `DataFreshness` 三 Optional 键 + 横幅两行制 + `InfoCardSnapshot.industryPersistDays` 改 `Int?`。**演练中发现并修掉一个真洞**:`rank(ordinal)` 并列由**行序**打散,而行序随「按年块 glob vs 单日分区」而变 → 同一天 bootstrap 与日更算出**不同 rank**、报告重跑会换序(20230803 真数据实测撞一串);修法 = 排名前先按 `(median_ret 降序, industry 升序)` 排定,已入 CLAUDE.md 纪律 + 专项单测。**本地真数据演练**(scratchpad DB 副本 + 真实 parquet,零生产访问):7 年块 Pass1 各 0.1~0.3s / 峰值 254~389MB,Pass2 1.3s / 115MB,合计 **3s**,落 **174,790 行 / 1589 交易日 / 110 行业**,库 3.8MB→**29.0MB**;`verify` 三项全绿;**随机 3 日单日对拍逐位一致**;`load_industry_strength` **1.3ms**、60 日序列 **1.3ms**、日更增量 **0.02s/110 行**、`build_info_card` 真数据 **0.56s**(此前生产永不返回);`integrity_check=ok` + 业务表行数未变;⑩-D 退路(250 交易日)演练 1.78s / 119MB / verify 绿。测试:Python 全量 **1677 过 + 2 skip**(基线 1647+2,净增 30),Swift iOS Simulator **168 跑 155 过 13 skip 0 failure**(基线 165,净增 3),双端 `xcodebuild build` SUCCEEDED。守门单测:四个在线文件 grep **零**现算入口 + 三路等价(全量算 ≡ 逐日递推 ≡ 落表读回)逐位相等
- 2026-07-29 · **P0-23 修法裁定 + review 时机改判,立第 ⑩ 块**(@planner,只改文档,零代码改动):用户拍板 ① P0-23 走**方案②「行业强度预计算落表」**(方案① 加窗口 / 方案③ 只修两个交互路径均否决)→ §五 新增 **v1.4-⑩**(🔴,七小节:SQLite 新表 `industry_strength_daily` 全规格 DDL / 计算侧拆三件保单一源 + 三路等价守门单测 / 16:05 日更增量只读当日一个分区 + 补跑自动向后延 / bootstrap 定案「生产旁路目录分块两遍法」带五条验证判据与退路 / 四处调用点改读表 + 四态保险丝语义 / `dataFreshness` 三新键与客户端小增量 / 生产隔离实测五条证据);② **review 前移到「修完 P0-23、重新上云前」**(原「上云后 review」作废),范围 = ⑥-A + ⑩ + P2-9 + P2-10。同步:§四 快照替换(新工序链 ⑩→实测→review→⑨ 重跑 + ⑨ 增量清单更新)、§七 P0-23 标已裁定 / P2-9 / P2-10 / P4-16 改时机、⑨-A/B/F 与验收改判、§八 第 12 项换包时机、CLAUDE.md 新增「生产机性能探针纪律」(15:00 后 · 避开 16:00–17:00 · `systemd-run --scope` 隔离串行 · load>4 停手)
- 2026-07-29 · 🛑 **v1.4.0 第 ⑨ 块完整部署:尝试 → 撞硬阻塞 → 按预案回滚**(@builder-pro + 用户「现在部署」授权,🔴 高危区,**已回滚,生产仍 `v1.4.0-p1`**)。做成并保留:本地 `VERSION`→v1.4.0 + 全量 1647 过 + 2 skip;生产库 `.backup` + `cp -p` **双备份**(`neckline.db.{bak,cpbak}-v140-20260729-092734`,备份内 `integrity=ok` + 业务行数对得上);**两处增量幂等迁移**(`decision_log.max_chase_pct` REAL + `inquiry_log` 表 + 2 索引)—— 迁移后 `integrity=ok` / 10 张业务表行数与迁移前**逐表一致** / `is_active` 仍 `v1.3.3` 且 `activated_at` 未变,**回滚时刻意保留**(向前兼容,p1 按 `_SELECT_COLS` 具名取列,已验 `GET /decisions` 200);**`ths_daily` 宽窗补拉 0720~0728 共 7 个交易日**(生产原 max=**2026-07-17**,缺口比原估的 3 天大;走 ①-C 既有 `update_ths_daily`,概念过滤生效 = **392 行/日**而非不过滤的 ~2499,dtype 合声明,补拉前 `cp -p` 备份 parquet,配额 8 次)。撞上的阻塞 → **§七 P0-23**:`compute_industry_strength` 扫全历史 784 万行,生产机 700M cap OOM-kill / 1400M cap 600s 跑不完(开发机 <1s),`GET .../info-card/{code}` 实测**永不返回**且把常驻服务(与盘中哨兵同进程)拖进内存回收死循环;**16:35 报告主链也吃这一步**,上了大概率当日无报告 → 判定硬阻塞,rsync 回 `fc55e2e` + 重启,`/health`=`v1.4.0-p1`、②~⑧ 端点 404、①-B 停牌口径无回归、`integrity=ok`。**刻意没做**:`MemoryMax` 未改(新证据表明抬内存不是解,且 v1.4 已回滚无消费方)、macOS/iOS **未换包**(v1.4 客户端配 p1 服务端会可见地坏:信息卡 404 / 问询历史 404 / `maxChasePct` 静默丢弃)。⚠ **过程中两次自伤已记取**:`pkill -f probe_industry` 匹配到自己的 SSH 命令行把会话杀了;探针把 2 vCPU 箱负载推到 65(已全部清理,`load 0.42` / 无残留进程 / failed scope 已 `reset-failed`)。今日 16:05 与 16:35 的 `ExecMainStatus` 待主会话验(跑的是 p1 = 与昨天同一份代码)
- 2026-07-29 · v1.4-⑧ 客户端跟进完工(@builder,纯本地,零生产访问/零部署):七项契约(①③④⑤⑥⑦)全部接线——①补录持仓日期选择器+停牌/`suspended_hold`/`dataFreshness` 展示;③排序理由三级 chips(`industryRank` nil≠0)+`sectorFlow`「并列参考」改文案;④新 `InfoCardPageView`(Swift Charts 三图:K线蜡烛+MA20/MA250+成交量、RS线、行业分歧线,均如实缺省不画空图)+ 快照/红黄牌/温和带/消息面/龙虎榜/市场语境;⑤执行提示展示 + 决策日志⑨项 `maxChasePct` 二选一强制(`APIClient` 请求体改手写 `encode(to:)` 令该键永远出现,nil→JSON null,与项目内其余「nil→省略键」字段刻意不同);⑥定格日晚点标注 + 周复盘 `CharterVersionCard`(`ReviewWeeklyResult` 因 `reviews.result_json` 写入时冻住历史快照而补手写容错解码)+ 消息面 `rotationGroup`/`codesRotationDeferred` 独立展示(顺带补齐客户端此前漏解码的 `codesNoSearch`);⑦决策卡新增挂单追踪走势(Swift Charts 折线)+ 问询历史列表/详情页。语义红线落地(候选区头部文案 + 全仓「推荐买入/建议买入/看好/值得买/推荐买点」grep 零命中)。`mapReason` 新增 3 case:`reportNotFound`/`codeNotInReport`/`maxChaseRequired`(`decisions/track`、`inquiries/{id}` 两处 404 复用既有 `notFound`,未新增,同 plan 字面)。⑤「候选专属信息卡入口」假设已核对成立(全仓唯一入口 = 候选卡按钮,恒持有 `Candidate` 对象,未给 `InfoCardOut` 加字段)。`pbxproj` 随本块一并提交(P4-21 结案,唯一被授权本块)。双端 `xcodebuild build` SUCCEEDED;iOS Simulator `xcodebuild test` 165 跑 152 过 13 skip 0 failure(基线 139,新增 26 测试;`httpBodyOrStream()` 由 `private` 放宽 `internal` 供跨测试文件复用同一 helper);Python 全量 1647+2 不变(本块零服务端改动)。视觉核对:本地隔离库(ATTACH 真实参考表+真实 parquet)出真报告 + 真持仓,`xcrun simctl` 截图拿到停牌持仓卡完整证据;候选卡/信息卡因 iPhone 视口纵向溢出且本环境无可用触屏注入(`Claude_Code_iOS_Simulator__control` 缺 Xcode-select 配置报错、通用 computer-use 同样点不动 Simulator),改用临时 iPad Pro 13" 模拟器(更大点尺寸)拍到候选卡排序理由+执行提示+信息卡入口完整证据,并新增 `NECKLINE_INITIAL_INFOCARD_CODE` QA 钩子(同 `NECKLINE_INITIAL_TAB` 先例)拍到信息卡 K线/RS线真实渲染;决策日志⑨项因表单靠后位置同一滚动限制未能截图,改由 6 个专项单测兜底覆盖,详见 §四

- 2026-07-29 · v1.4-⑦ 功能补全完工(@builder,纯本地,零生产访问/零部署/零迁移写生产):⑦-A 新端点 `GET /decisions/{id}/track`(读专用 `report/pending_track.py::load_track_rows` 装配,不重算;404 仅 `decision_id` 不存在,「未攒到追踪数据」是合法 200 空态,两种「空」分开;`reason="not_found"` 复用客户端既有 case,未新增)。⑦-B 新表 `inquiry_log`(纯追加档案非队列,`CREATE TABLE IF NOT EXISTS` 幂等,不套 `inquiry_pool` 的「审计时间戳+消费标记」两字段模式)+ `run_inquiry` 结尾旁路写入(try/except,失败只使 `inquiryId` 退化 `None`、不打断回答)+ `GET /inquiries`(分页+`tsCode`过滤,倒序)/`GET /inquiries/{id}`;`POST /inquiry` 响应加 `inquiryId:Int?`。⑦-C `InquiryVerdict` 由已退役二值裁决(`.reject`/`.pass`)改描述性标注(`.analyzed`/`.analyzedWarn`,原样对齐后端字面值),顺带修掉「已分析·有风险提示」误落中性色的展示 bug(改警示色,复用既有 `NKAxisTone.warn`,未新造色值)+ 清理连带失真的 `AppModelTests`/`DTODecodeTests`/`IntegrationSmokeTests`;`NKAxisTone` 补 `Equatable`。Python 全量测试 1647 过 + 2 skip(基线 1627+2,新增 20);Swift 双端 `xcodebuild BUILD SUCCEEDED`,iOS Simulator `xcodebuild test` 139 跑 126 过 13 skip 0 failure(macOS 侧 `xcodebuild test` 因 `TEST_HOST` 既有工程配置只适配 iOS 布局而跑不了,与本块改动无关,项目历来只在 iOS Simulator 跑单测)。**一处已知偏差**:plan §五-⑦ 验收原文要「警示色生效(截图)」,本次按任务简报「双端 BUILD SUCCEEDED 即可,截图非必需」执行未拍照(靠两条 Swift 精确断言 `.tone==.warn` 兜底;拍照需切后端环境 picker 离开默认 `prod`,与「零生产访问」边界不必要地贴近)
- 2026-07-29 · 🔴 v1.4-⑥ 判定精度完工(@builder-pro,纯本地,零生产访问/零部署/零迁移/未动 `activated_at`):⑥-A 周复盘章程**按成交时刻逐笔判**(新 `brain.config_governing_at`〔判据「激活时刻 ≤ 成交时刻」= 恰好等于算新章程〕+ `brain.activations_between`;`review/reconcile.py` 四条判据各锚各的时刻——单笔上限/禁买锚买入、止损锚卖出〔与哨兵读现役 stop_pct 同源〕、并发/敞口按日粒度切段、冷却锚再次买入;时区归一新增 `neckline.calendar.CN_TZ`/`MARKET_CLOSE_TIME` 单一源、`intraday._CLOSE` 改引用,`activated_at` naive 按 UTC 读 vs 入参 naive 按北京时间读**两向约定刻意相反并各自定死**;`RawTrade.trade_time` 可选,只有日期时按该日收盘取章程;周报 `charter_segments`/`charter_switches` 分段计数 + 切换文案进 material,`strategy_version` 收窄为周初标签)+ **老新实现对拍**:非切换周逐位一致、切换周按预期修掉生产真实形态(激活北京 14:36)下的 2 条假警报,golden 已写进永久单测;⑥-B 消息面预算取**自选隔日轮扫**分支(**限频未实测**——本地无任何可用 GLM key,按"不许拍脑袋开并发"取保守分支,并发路留待有 key 时评估;持仓每日必扫 + `zlib.crc32` 稳定哈希分两组 × `toordinal()` 奇偶交替〔纯函数、历史回放可复现〕+ 减持类不参与轮扫 + `rotationGroup`/`codesRotationDeferred` 与既有三个计数**语义不合并**;预算维持 300s 不抬,不给 ⑨-F 内存账加压);⑥-C `PositionOut.timeExitLockedDay`/`timeExitLockedLateDays`(纯派生展示位,未定格返 null 不冒充,**判向逐位不变有单测**,与 ①-B 停牌复牌晚定格共存);全量测试 1627 过 + 2 skip(基线 1578+2,新增 49);⑥-B 顺带补掉 `newsAlertsScan` 强类型模型丢弃未声明字段的老坑(schemas + `_shape_report` 两处必须同步补,同 `codesSkipped`/`codesNoSearch` 先例)。⚠ **一处 plan 理由与现实不符已记录**:plan 说"章程激活都在盘后跑"故当日成交按收盘取章程,但真实 v1.3.3 激活在北京 14:36 盘中——**规则照 plan 不改**,代价(激活当日激活前的成交偏判新章程)写进 `trade_instant` docstring
- 2026-07-29 · v1.4-④ 信息卡与考卷同构完工(@builder,纯本地,零生产访问/零部署):新模块 `report/info_card.py` 组装九件套(K 线 60 日/RS 线基准 `SSE_INDEX`/行业分歧线「成员中位数合成」/快照七项/红黄牌/温和带 `[2%,3%]`/消息面/龙虎榜近5日/市场语境),两条消费路径刻意分离(`build_info_card` 单只按需服务端现算供新端点、`attach_info_card_summaries` 批量摘要零额外 parquet 读取直接吃 `Candidate.raw`/`intel_rank` 供列表页);新端点 `GET /report/{date}/info-card/{code}`(`code` 经 `normalize_ts_code` 归一;404 两 reason `report_not_found`/`code_not_in_report`);`CandidateOut.infoCard` 摘要位(不含 60 日序列,老快照 `None` 不冒充"确认无内容");`industry_strength.py` 新增 `industry_median_return_series`(分歧线合成源,`_load_ret1d_panel` 加可选 `start` 窗口参数,**不受** `_MIN_MEMBERS` 排名门槛约束)+ `holding_k4_check.py` 新增 `describe_hits`(红黄牌 decorate,不重判,复用 `_HIT_META`/DB evidence);行业未达标(成员<5)如实标"分歧线缺省"、消息面扫描域仍仅持仓+自选(候选不在内如实标注,不冒充"扫了没有")、龙虎榜回看"查到几天算几天"不为凑齐而回补历史;零 DB 迁移(`info_card_summary` 只是 `candidates_json` 里的新 key);端到端用本地真实数据(隔离库四张只读参考表 ATTACH `data/neckline.db` + 真 parquet,2026-07-24)验证九件套齐全(K线60根/RS线60点/行业分歧线可用标"半导体"/K4黄牌真命中 `B2_dual_golden_cross`),经真实 FastAPI TestClient 打端点验证往返;测试隔离踩坑一次并已解决并入 CLAUDE.md(`neckline.db` 的 `settings` 绑定不在 `isolated_env` 覆盖范围,domain 函数必须显式传 `db_path`);全量测试 1528 过(基线 1472+56)
- 2026-07-28 · v1.4-③ 正选漏斗排序键改版完工(@builder,纯本地,零生产访问/零部署):`intel_candidates.py` 排序键换三级(`industry_rank ASC → industry_persist_days ASC → yellow_card_count ASC`,base_score/code 仅确定性兜底)+ `sector_flow` 退为并列展示 + `_SORT_KEY_INPUTS` 白名单单测锁死;`IntelRankOut` 补 `industryRank`/`industryPersistDays`/`yellowCardCount` 三字段(老客户端兼容);`render.py` 语义红线落文案;③-G 新旧排序对照报告 `archive/v1.4_排序键切换对照_20260724.md`(20只候选14进14出6变位0不变,A2拦截率变化系②已完工效应、与③无关,报告已单独澄清);全量测试 1472 过(基线1465+7)
- 2026-07-28 · v1.4-② 行业强度单一源完工(@builder,纯本地,零生产访问/零部署):新模块 `report/industry_strength.py`(与 `research/k4p_h6_theme.py` 逐位对拍单测锁死)+ A2/B3 判据三处调用点(`holding_k4_check.py`/`intel_candidates.py`/`api/inquiry.py`)+ `pipeline.py` 接线回归规格档,概念板块 `board_age` 降级为纯展示;切换对拍报告 `archive/v1.4_A2B3口径切换对拍_20260724.md`(A2 命中 375→11,旧口径 76% 命中撞在 `max()`-over-多板块的封顶假象);全量测试 1465 过(基线 1450+15)
- 2026-07-29 · v1.4-⑤ exec_hint 产品化 + 决策日志第⑨项完工(@builder,纯本地,零生产访问/零部署):新模块 `report/exec_hint.py`(C1强票市价/C2温和带〔复用④ `info_card.MILD_BAND_RANGE`不重开〕/C3挂低单自觉〔与⑤-B联动,查该票 `trade_date` 当天或之前最近一条 `decision_log`〕/C4回调大红机械层不做 四码,文字一律读 DB `k4_advisory.exec_hint`〔扁平 `{code:text}`,2026-07-29 真库探活确认与 `hard_cut`/`avoid_flag` 嵌套形状不同〕、四码可部分命中部分兜底、镜像阈值模块头对照表同 `holding_k4_check.py` 体例、C4 与 B4 数值巧合相同但概念独立声明不复用私有 `_hit_B4`);`attach_exec_hints` 零额外 parquet 读取(C1/C2/C4 直接读 `Candidate.raw`)+ **按 `trade_date` 截断 `decision_log` 查询防前视偏差**(历史回放不得读到该历史日之后才创建的决策,新入 CLAUDE.md 定案);`decision_log.max_chase_pct` 幂等迁移(REAL 可空,`3.0`=昨收+3%、允许负值、`NULL`=显式不设上限)+ HTTP 层 `model_fields_set` 探测 JSON 键是否存在(缺键 400 `reason=max_chase_required`、显式 `null` 合法,create/revise 两端点同纪律)+ 与 `planned_price` 语义分离(docstring + 单测各写各的,新入 CLAUDE.md 定案);契约 `CandidateOut.execHints`/`DecisionOut.maxChasePct` 往返(仅 `CandidateOut` 载 execHints,`InfoCardOut` 不重复开字段,信息卡页复用候选卡已取数据,按 plan 客户端契约清单字面);复用 ③ 白名单单测模式新增一条「exec_hint 相关量不进 `_sort_key`」的锁;全量测试 1578 过(基线 1528+50)。**规格与任务简报有一处措辞张力已按 PROJECT_PLAN 原文实现并在完工报告显式记录**:简报口径说 `max_chase_pct` "可选"且"老客户端不传照常工作",但 PROJECT_PLAN §五-⑤-B 原文明确要求 HTTP 层"缺键 400"——两者对 `POST /decisions` 而言互斥,已按 PROJECT_PLAN 原文实现(此端点其余 4 项〔why_buy 等〕本就一直硬性必填、从无"老客户端选择性传参"先例,且生产部署要到 ⑨ 才发生、彼时 ⑧ 客户端已同步跟上,故两者实际不冲突)
- 2026-07-28 · 🔴 **v1.4.0-p1 计划外小步上云**(@builder-pro + 用户拍板,🔴 高危区,未回滚):只上第 ① 块 P0 地基,理由 = `002036.SZ` 停牌票假警报有硬时限(0729 09:26 误推 / 16:35 永久错误定格),等第 ⑨ 块来不及;`sync_code.sh` 属主自检绿 + API 重启 active + `/health`=v1.4.0-p1 + 两处幂等迁移落地 + `integrity=ok`;补 `suspend_d` 5 个交易日分区 + 回填 `data_unavailable` 6 行(**部署时实测:不回填的话盘前那一拍照推假警报**);干跑验收 0729/0730 09:26 推送清单为空、0731 另两只照常判该走。**第 ⑨ 块仍是最终完整部署**(届时 VERSION 改回 v1.4.0)
- 2026-07-28 · 🔴 生产三笔持仓买入日纠正完工(@builder-pro,🔴 碰持仓判定 + 生产数据):`002036.SZ` 20260727→20260722(唯一实改,另两笔本就正确、幂等跳过);`.backup`+`cp -p` 双备份 + `integrity_check=ok` + 逐行对拍「其余行零改动」+ 定格三列改前即全 NULL 无需清;⚠ 遗留风险 = 代码未上云前 **0729 09:26 会推假警报、16:35 会永久错误定格**,处置与时限见 §四 第一条
- 2026-07-28 · ✅ v1.3.5 上云后**首次自动跑报告**验收通过(16:35:01→16:53:24,`ExecMainStatus=0`,报告真生成 / 审判 10 条 0 降级 10/10 有搜索命中;内存峰值顶满 800M 归 P4-16→⑨-F);验收判据收严:`Result=success` 会被 `reset-failed` 抹掉,以 `ExecMainStatus` + 时间戳为准(已入 CLAUDE.md)
- 2026-07-28 · 🔴 v1.4-① P0 地基完工(本地,未部署)· ①-A 补录买入日契约 `buyDate` + 两个 400 reason + 客户端 mapReason;①-B 停牌口径(`suspend_d` 探活可用 / `002036.SZ` 自 0723 真停牌 / `priceStale` + 第五态 `suspended_hold` + K4 `dataUnavailable` + §2.1 附注);①-C 概念板块日更(`ths_daily` 尾窗重拉 + 概念过滤 + `ths_index`/`ths_member` 周更带 .bak)+ `suspend_d` 日更 + 板块数据过期告警 `dataFreshness`(@builder-pro,全量测试 1449 过;代码上云归第 ⑨ 块)
- 2026-07-28 · **v1.4.0 立项**(施工图九块就位待 builder)· K4 打法产品化〔方案 b〕= P0 三洞地基 + 行业强度单一源 + 正选漏斗排序键 + 信息卡考卷同构 + exec_hint 与追价上限 + 判定精度 + 功能补全 + 双端 + 部署;v1.3 施工图归档 `archive/v1.3_施工图_20260728归档.md`(@planner,只改文档,零代码改动)
- 2026-07-28 · 档案室搬家:PLAN 瘦身(§四快照制 / §五旧+§五附归档 / §九一行制)+ CLAUDE.md 修剪 + scripts/oneoff 归位 + 记录纪律立规(纯文档与文件移动,零代码逻辑改动)
- 2026-07-28 · 会话交接:§七 Backlog 重整 + §四 交接条目(@planner,只改本文件,零代码改动)
- 2026-07-28 · v1.3.5 报告管线抗毁快修 + 2026-07-27 报告补跑(@builder-pro + 用户授权,未回滚)
- 2026-07-27 · v1.3.4 问询台联网搜索修复快修完工 + 上云 + macOS 换包(@builder-pro + 用户授权,未回滚)
- 2026-07-27 · 🔴 v1.3.3「拆墙」快修完工 + 上云 + 章程生产激活(@builder-pro + 用户三条明确授权,🔴 碰纪律章程 + 生产数据 + 护栏本身,未回滚)
- 2026-07-27 · 🔴 v1.3 章程在生产激活(@builder-pro + 用户明确授权,staged 步骤 2 = 用户纪律判定标准真正改变,未回滚)
- 2026-07-27 · v1.3.1 部署上云完工(@builder-pro + 用户授权,🔴 高危区,已部署、章程仍未激活、未回滚)
- 2026-07-27 · 独立审计(纪律章程与金额判定)缺陷修复完工(@builder-pro,~~未部署~~ **已随 v1.3.1 上云**,🔴 高危区,未激活任何章程行)
- 2026-07-26 · K4 机械半身行 bootstrap 到生产完工(@builder-pro + 用户,🔴 碰生产权威库,只增一行 inert 数据,未激活/未改现役配置/未重启)
- 2026-07-26 · v1.3-⑦ 部署上云完工(@builder-pro + 用户,🔴 最高危块,已部署、未激活章程,未回滚)
- 2026-07-26 · v1.3-⑥ 客户端跟进(双端)+ 三处(实为四处)后端补齐完工(@builder,未部署,v1.3 最后一块,只剩 ⑦ 部署)
- 2026-07-26 · v1.3-④ 挂单未成交追踪 + v1.3-⑤ 问询台选股域漂移清理完工(@builder,未部署)
- 2026-07-26 · v1.3-③-C3 候选排序两次追加(常驻保底 2 只 + 行业闸)完工(@builder-pro,未部署;仅 `report/intel_candidates.py` + 其测试 + 本节)
- 2026-07-26 · v1.3-③-C4 验收后必改两项完工(@builder,未部署)
- 2026-07-26 · v1.3-③-C4 晚间消息面公告扫描(持仓+自选:减持/立案/暴雷/监管)完工(@builder,未部署)
- 2026-07-26 · v1.3-③-C3 候选池情报筛选管线改版(K1 选股逻辑退役)完工(@builder-pro,未部署)
- 2026-07-26 · v1.3-③-C1/C2 情报官(复盘情报件 + 板块资金流展示)完工(@builder,未部署)
- 2026-07-26 · v1.3-② 持仓管理层(K4 牌每日对持仓重算)完工(@builder-pro,🔴 推送路由 + 金额判定,未部署/未激活)
- 2026-07-26 · v1.3-① 退出规则章程变更完工(@builder-pro,🔴 碰纪律章程,未部署/未激活)
- 2026-07-26 · v1.3 立项(施工图就位待 builder)· v1.2 合并发布 + 退出规则改革 + 持仓管理层 + 情报官候选管线改版
- 2026-07-25 · v1.2-E 客户端双端(SwiftUI iOS+macOS)完工
- 2026-07-25 · v1.2-G 呼吸试验仓台账后端完工
- 2026-07-25 · v1.2-A2 熔断纪律后端完工(🔴 碰纪律章程 + 金额判定 + 第五类推送)
- 2026-07-25 · v1.2-B 预注册决策日志(八项)后端完工
- 2026-07-25(增补)· v1.2 范围扩大 + 切两波(策略线三版补丁交接)
- 2026-07-25 · v1.2 立项(施工中)· 人机协作配套:三仓章程 + 预注册决策日志 + 归因闭环
- 2026-07-25 · K3 · B2 四臂组合级回测完工 → ★中心假设否决(诚实否定合格)
- 2026-07-24 · K3 · B0 数据修缮 + B1 超跌事件研究完工 → ⏸ B1 用户检查点
- 2026-07-24 · K3 策略研究立项(纯研究,不动生产 / 不动纪律章程)
- 2026-07-22 · K2 策略研究(B1–B6)完工 · 中心命题否决 · K2 落库不激活(纯研究,生产零改动)
- 2026-07-19 · 立项(v0.1)
- 2026-07-19 · 立项后确认(v0.1.1)
- 2026-07-19 · 阶段 0 完工(数据层 + 回测引擎骨架)
- 2026-07-20 · 阶段 1 完工(策略研究 P1–P10)
- 2026-07-20 · 阶段1收口拍板(v0.2)
- 2026-07-20 · 阶段2完工(盘后报告管线,§2.1–2.6)
- 2026-07-20 · 阶段3完工(盘中哨兵,§2.4)
- 2026-07-20 · 阶段 4 立项(v0.3,客户端 + 云端化 + 问询台 + 周复盘)
- 2026-07-20 · 阶段 4A 完工(后端 API 服务化)
- 2026-07-20 · 阶段 4B 完工(云端化 + hz ECS 部署,方案 A)
- 2026-07-20 · 阶段4C完工(SwiftUI iOS+macOS 双端客户端)
- 2026-07-20 · 阶段4D完工(周复盘工作台·对账引擎)
- 2026-07-20 · 阶段 4E 部分完工(LinoN 接班切换 + 收官接线)
- 2026-07-21 · v1 上线 + 首日生产坑快修
- 2026-07-21 · v1.1 立项(SOP 补洞)
- 2026-07-21 · v1.1-C/D 后端完工
- 2026-07-21 · v1.1-H 服务端部署上云(🔴 @builder-pro)
- 2026-07-22 · 版本号双线规约
- 2026-07-22 · v1.1-H2 退潮哨兵双级制重构(🔴 高危快修 + 上云)
- 2026-07-22 · v1.1.1(退潮哨兵双级制)+ 版号规约收严
- 2026-07-22 · K2 策略研究立项(纯研究,不动生产 / 不动纪律章程)
- 2026-07-25 · 架构拆分(双权威文件)
- 2026-07-25 · v1.2-0 完工:`sync_code.sh` 部署 footgun 修复(@builder)
- 2026-07-25 · v1.2-A 完工(staged 步骤 1):仓位章程三仓制落库 + 历史洗白修复(@builder-pro,🔴 碰纪律章程 + 大脑激活)
- 2026-07-27 · 行业闸判据缺陷修复:share→lift(@builder,仅 `report/intel_candidates.py` + 其测试 + 本节)
