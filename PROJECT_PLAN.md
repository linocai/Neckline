# Neckline · V2.5.1 Build 11 施工控制面

> 当前生产：V2.5.0 · Build 10　　当前阶段：V2.5.1 Build 11 RC 全门禁通过，已授权发布　　更新：2026-08-23
>
> 本文件是唯一现行计划；细化施工记录见
> [V2.5.1 Build 11 执行计划](archive/施工图/V2.5.1_Build11_执行计划_20260823.md)。

## 1. 愿景与版本边界

K9 已发布的机械引擎不变。本版把它交付成一份手机/桌面都能读懂、十分钟可读完、
离线可回看的每日报告：说明市场在发生什么、每只票是什么、收盘价与明日预案是什么，
但不替用户做买卖决定。

- 版本固定 **V2.5.1 · Build 11**；直接在 `main` 工作，不建分支。
- 本版补齐：事实层方向解读旁路、两层报告与整段复制、报告离线缓存、覆盖率可见性、
  现价/价位体验、全 App 文案收口、用量账和真实端到端验收。
- 不改 K9 机械策略、参数包、四通道、排序、名额、D1 两拍或清单成绩口径；不恢复 K8、
  篮子、持仓、旧校准、旧通知；不重做复盘业务逻辑。
- 本轮规划和施工都不得触发正式报告重跑、部署、发布或换包；这些只在后续明确授权后执行。

## 2. 已裁定且继续生效的契约

1. 排序使用上方机械空间；预案使用第一压力位，两者不混名。
2. 行业强度为申万二级成员当日涨跌幅中位数；只排全天停牌成员，不用概念板块。
3. 放量倍数一处共享；P1 为 `≥ V`、P3 为 `< V`，`V` 不分档。
4. 参数包没有默认值；缺包/非法必须报告“今天没跑成 · 参数未配置”，不得出清单。
5. 9:26 只能“已触发放弃 / 待开盘后观察”；10:00 是三分支终值唯一权威。
6. `report_date` 管标题、推送、可见身份；`trade_date` 管行情、清单、预案、审计键。
7. LLM 不进机械策略；联网统一 Tavily。V2.5.1 新增的方向解读仅为事实层报告背景。
8. 退役能力物理删除；Git 历史承担追溯，不保留 K8 或兼容壳。
9. 通知仅 `report_ready`、`precall`；排程为周一至周四与周日 19:00，周五留至周日。
10. B1–B22 的具体口径（含明确价格、无默认值、26 张现行表、B20 一周观察）继续有效，
    不在本版重新裁定；V2.5.0 发布记录见下方归档索引。

## 3. 现状与本版缺口

- V2.5.0 Build 10 已于 2026-08-23 部署宁波云；K9 facts → 四通道 → 清单 → 解释 →
  预案 → 次日核对 → 成绩链已上线，当前生产 schema 为 26 张现行表。
- 方向解读模块尚未建立；报告虽有 `structured`/`markdown`，App 未真正交付两层阅读、复制或
  本地缓存；覆盖率仅在成绩页可见。
- App 仍泄漏 Markdown、K9/架构章节、HTTP 状态、接口路径和本地存储术语；个股详情重新出现
  价格刻度，且遗漏收盘价数字。
- Provider 返回 Token 与 Tavily credits，但长任务只靠 stdout 汇总会丢失，尚无持久账。

## 4. V2.5.1 施工切片与顺序

1. **契约与迁移（已完成）**：定义方向、报告展示、收盘价、用量账的增量 DTO；schema 从 26 张增至
   28 张，仅新增 `fact_direction_briefings` 与 `llm_usage_events`。不回填历史、不改冻结报告。
2. **方向旁路与计量（已完成，复审修复）**：facts 冻结后以原子 claim 执行一次方向解读；每份事实包最多
   一条 Tavily Basic 检索和一次 DeepSeek 调用，崩溃/并发后不自动重复收费。四个 LLM 岗位全部落账。
3. **报告/API（已完成）**：从冻结事实、方向 sidecar、覆盖率、解释和预案装配默认阅读数据、完整资料与
   可复制文本；把 D0 收盘价和一句话画像批量加入清单/详情响应。
4. **macOS/iOS App（已完成，复审修复）**：语义化默认报告 + 默认折叠完整版 + 复制；离线快照仓仅在明确断网
   回退；价格数字化；日期人话化；设置默认页与高级诊断分层。
5. **验收与修复（Builder 已完成，待复审）**：自动化、临时库 smoke、双端三构建均已重跑通过；独立复审接手
   并核验并发成本、离线状态机和设置分层。视觉取证仍需可控环境补齐。

## 5. 产品验收口径

- 每份冻结事实包只产生一次方向背景；它永远不读取 K9 输出、不改召回/排序/名额，失败只显示
  “方向解读暂未生成”。
- 选股首页默认依次可读：报告状态、方向背景、市场事实与覆盖率、每票一句话画像、收盘价、
  明日三价位和预案入口；`not_run` 仍尽可能展示已冻结市场事实和覆盖率。
- 完整资料默认折叠，可一键复制一段可读中文；屏幕上不展示原始 Markdown 或 JSON。
- 断网后可打开最近成功下载的同一环境/同一账号报告，并明显标明“离线浏览”和缓存时间；
  无缓存时如实提示，绝不把网络错误说成空清单。
- 所有用户页面没有 raw Markdown、K9/架构章节号、`append-only`、HTTP 数字、GET 路径、
  `UserDefaults`、`git` 等工程词；必要的服务地址/模型/密钥输入只放 macOS 高级/诊断区。
- 个股不再有价格刻度或百分比替代价格；首要数字为 D0 收盘价（明确标截至收盘），三个预案价位
  都直接显示金额。
- 设置高级诊断可查看连续 3–5 个交易日的每日任务数、真实输入/输出/总 Token、未回传次数和
  Tavily credits；不展示密钥、prompt、搜索原文，也不凭空换算人民币费用。

## 6. 数据、离线与安全边界

- 新表只在 API 启动、明确写命令或发布迁移中建；所有读 helper 和 GET 继续零 DDL。迁移先在
  生产备份副本演练，确认 28 张表和 `integrity_check` 后才可进入部署门禁。
- 方向 briefing 与用量事件均为事实/审计记录：不含 API key、Authorization、完整 prompt、
  完整搜索响应或股票机械排名；Token 未回传写 `null + unavailable`，不得估算。
- App 缓存仅保存已成功解码的报告快照，以服务地址和访问凭据的不可逆指纹隔离；原子写入、
  文件保护、配置变更清理。在线有效响应覆盖同日快照；鉴权失败不使用旧缓存掩盖问题。
- 新 API 字段均 additive/optional：旧 App 忽略；新 App 读旧报告时明确“当日未生成/资料未保存”。
  不回写 V2.5.0 冻结事实、预案或报告。

## 7. 验证、Review 与发布前门禁

```bash
cd Backend
.venv/bin/python -m pytest -q
bash scripts/smoke_api.sh
bash scripts/smoke_review.sh

cd ../App
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'platform=macOS' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'generic/platform=iOS Simulator' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline build-for-testing -destination 'generic/platform=iOS Simulator'
```

- 任何 Swift 改动三条 `xcodebuild` 一条都不能省；缓存、旧 payload、三态、方向失败、
  用量未回传和用户文案都要有自动化覆盖。
- Builder 全量通过后，独立 reviewer 审 diff、迁移、LLM 调用/成本账、离线安全、双端实际截图。
  所有确认的 P0/P1 修复并重复全门禁；报告存入 `archive/review报告/`。
- 发布候选才更新 API `v2.5.1`、App `2.5.1`、Build `11`，运行版本一致性门禁、签名与归档。
  没有新的用户授权前，止步于“部署发版前”。

## 8. 持续观察（不重新裁定）

- 上线后连续 3–5 个交易日比对用量账与 DeepSeek/Tavily 控制台；只根据真实账单讨论下一步成本，
  不在本版预设 Token 上限或隐含策略阈值。
- 延续 B20：记录一周 9:26 双源 `open` 分布后再决定质量状态。
- 首份真实生产 K9 报告必须完成 macOS 全链视觉验收；iOS 设备安装仍由用户执行，builder 提供
  签名包并完成模拟器验收。

## 9. 归档索引与状态

- [V2.5.0 Build 10 发布事实](archive/施工图/V2.5.0_Build10_发布记录_20260823.md)
- [V2.5.1 Build 11 执行计划](archive/施工图/V2.5.1_Build11_执行计划_20260823.md)
- [V2.5.1 Build 11 独立审查](archive/review报告/V2.5.1_Build11_独立审查_20260823.md)
- Build 复验：后端全量 `1356 passed`、两套临时库 smoke、macOS/iOS 三构建与离线状态机单测均通过；
  复审发现的 2 个 P1 与 2 个 P2 已修复，具体对照追加在独立审查报告末尾。
- 第二轮独立复审：原 4 项全部 closed，未发现新的 P0/P1/P2；RC 已将普通设置页最后一处
  `token 已填/未填` 改成“访问码已填/未填”。
- RC 元数据已统一为 API `v2.5.1`、App `2.5.1 (11)`；后端 1356 项、API/复盘隔离冒烟、
  macOS、iOS Simulator 与 iOS `build-for-testing` 全部门禁通过。生产发布已获用户授权，
  仍不得借发布或验收之名手动重跑报告。
- 状态：**独立复审通过，等待 RC 版本收口与可控环境视觉补验；未部署、未发版、未重跑报告。**
