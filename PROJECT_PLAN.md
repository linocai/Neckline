# Neckline V3 · 3.0.5 / 双端 Build 37 · K10-v1.4

本文件是唯一工程控制面。策略行为以 [`whynotme/K10.md`](../whynotme/K10.md) 为准，生产不得导入研究仓；本轮详细施工契约见 [archive/v3.0.5-b37_execution.md](archive/v3.0.5-b37_execution.md)。

## 概述

3.0.4 / Build 36 已发布，但 2026-09-08 的夜间恢复暴露了错误的资讯处理方式：2472 篇资料几乎逐条交给 V4 Pro。用户已要求停止该运行，并授权 3.0.5 / 双端 Build 37 重做“先程序筛减、后小型理解、再重点核验”的调用控制。本轮只施工和离线验证，不恢复生产任务、worker、timer、模型或搜索调用，也不部署。

## 稳定技术决定

- 生产继续停机：所有入口（定时器、CLI enqueue、worker、恢复）均须受持久暂停开关约束；暂停/预算缺失/预算耗尽时零模型与零搜索调用，既有历史任务不可被暗中续跑。
- 保持 K10-v1.4：全市场来源可映射创业板；创业板、排除 ST/*ST 与申万白酒Ⅱ `801125.SI`；正式候选上限、公司比较、跨日延续、D1/D2、重叠样本与成绩口径均不改变。
- 来源处理固定为：增量采集 → 原文/版本去重 → 显式模板审计筛分 → 同事项资料包 → 小型理解 → 事件优先级 → Tavily 核验 → 公司比较/全局 30 家排序。栏目、利好词、文本长度或创业板代码命中均不能充当硬排。
- 没有已批准的模板规则或数值预算即为 `not_configured`：资料进入可见的待处理/待核计数，不允许空筛后全量送模，也不产生正式候选。未知条目、取消/否认/更正和重大反证受保护，不能静默丢弃；未知资料可在显式额度内接受轻量分诊，预算不足时保持待处理。
- 每轮冻结可审计的规则版本、资料包、优先级、预算预留/实耗/未知用量与缓存命中。并发、重试、全文补读均须先原子预留预算；用量未知不退还预留，缓存不收费。
- 同源稳定版本可跨 scan 复用已验证的原始事实：来源正文/版本、规则、模型与提示输入不变且资料在当次 cutoff 前可见时，后来的 cutoff 不单独使纯事实缓存失效；价格背景、两日判断、证据时效和事实变化必须重核。旧事件普通更新不重开机会或观察窗口。
- DeepSeek V4 Pro 仍是唯一模型。全文只能由轻量理解明确请求且有独立的全文预算时进入；“空 events”不再触发全文。

## 当前状态

- 生产仍为 `v3.0.4-b36`，源码 `6017a0c`、Schema 4；worker 与晚/晨 timer 停用。历史与停机证据见 [B36 记录](archive/v3.0.4-b36_execution.md)。本轮没有调用真实模型/搜索，没有部署或恢复生产。
- 本地 3.0.5 / 双端 Build 37 施工完成：Schema 5、获批模板、资料包、逐请求整轮/阶段预算、持久暂停、事实缓存、优先级和 API/Swift 状态已接通；待生产规则与额度批准前保持关闭。
- 整合基线为 `931850b`。后端完整回归 **794 passed**；包括真实 CLI→worker→生产 handler 的隔离恢复、重复来源只理解一次，以及具体 HTTP 适配器在 MockTransport 下的预算/缓存验证。
- 冻结 2472 篇离线审计：原文重复 16、提纯重复 26、2446 个资料包、2472 个引用全部保留；中断恢复不重放完成包。全部测试数值仅属 fixture，零真实模型/搜索调用。
- 实际 FastAPI 响应由当前 Swift DTO 解码通过；macOS/iOS 填充、空态和 B37 暂停/预算专属画面已验证。最后 Swift 编辑后的三条构建全部通过，同一临时 QA 实例已关闭、卸载。
- 模板和数值预算尚未批准，不写入生产默认配置。代码完成不等于生产可恢复，真实付费小样本另需用户授权。

## 当前 Plan

本轮已授权的施工与离线验收全部完成，无剩余施工项。代码保存在本地 main，未推送、未发布。下一步由用户确定生产模板与数值预算，再决定受控付费小样本及恢复；不自动续跑。

## 待用户决定

- 模板规则的批准内容及版本：哪些可明确降级、哪些必须保留为未知/重大反证；在此之前运行配置保持不可用。
- 每轮/各阶段的模型调用、输入/输出/总 token、全文、重试和 Tavily 搜索额度。必须给出具体整数；代码不填生产默认值。
- 完成施工后的生产恢复与发布不随本次工作流自动发生，须在审阅配置和离线测量后另行明确授权。

## Backlog

- 预算与模板获批后，以受控小样本核对真实 DeepSeek/Tavily 用量、筛分漏检和证据时效，再决定是否恢复夜间任务。
- 仅在实际运行恢复后评估 3.0.5 的 APNs、夜扫、晨扫及 D1/D2 生产链路；不得以离线测试替代。

## 里程碑索引

- 3.0.4 / Build 36：已发布后因全量逐篇模型处理被用户叫停；历史、停机与发布边界见 [archive/v3.0.4-b36_execution.md](archive/v3.0.4-b36_execution.md)。
- 3.0.5 / Build 37：资讯筛分、预算与持久暂停本地施工及离线验收完成，未发布；契约与验收见 [archive/v3.0.5-b37_execution.md](archive/v3.0.5-b37_execution.md)。
