# Neckline · 当前控制面

> 当前生产：V2.5.1 · Build 12
> 现行引擎：K9
> 当前阶段：观察期
> 更新：2026-08-23

本文件只记录当前事实、已裁定契约和下一步；历史实现、施工过程、复审流水与旧版本事实
由 Git 保留，不在工作树重复存档。

## 1. 当前目标

Neckline 是面向 A 股盘后研究与次日核对的生产应用。它应在十分钟内回答：市场发生了什么、
哪些方向值得看、每只票是什么、收盘价与次日预案是什么。系统提供研究参考，不代替用户下单。

客户端当前只保留“选股 / 成绩 / 复盘”和设置入口；不展示或跟踪持仓。研究、回测、标定和
实验历史在 `/Users/linotsai/Lino/whynotme`，生产仓不反向依赖研究仓。

## 2. 已裁定契约

1. K9 主链固定为：事实包 → 四通道机械召回 → 排序与名额 → 解释 → 预案 → 次日两拍 → 成绩。
2. 退役能力物理删除。K8、篮子、六关、旧校准、持仓、旧通知与兼容壳不得恢复；Git 历史负责追溯。
3. 策略参数没有默认值。参数包缺失或非法时报告“今天没跑成 · 参数未配置”，不得输出可信清单。
4. 排序使用上方机械空间；预案使用第一压力位，两者不得混名。
5. 行业强度使用申万二级成员当日涨跌幅中位数；只排全天停牌成员，不使用概念板块。
6. 放量倍数只有一份共享值；P1 为 `≥ V`、P3 为 `< V`，不分档。
7. 消息面剔除后持续按机械名次读取后备池，直到恢复原目标数量或后备池耗尽；不存在轮数上限。
8. 成立率为 `confirmed / (confirmed + rejected)`；`observed` 与未结算行不进入分子或分母。
9. 9:26 只能输出“已触发放弃 / 待开盘后观察”；10:00 是成立 / 放弃 / 观察终值的唯一权威。
10. `report_date` 管标题、推送和可见身份；`trade_date` 管行情、清单、预案与审计键。周日报告
    使用周日作为 `report_date`，使用前一个周五作为 `trade_date`，并纳入周末消息。
11. LLM 不进入机械策略。联网只使用 Tavily；推理通过用户配置的 OpenAI 兼容 Provider 完成。
12. 每份事实包的方向背景最多一条 Tavily Basic 检索和一次模型调用；失败不得改变机械结果。
13. 通知只保留 `report_ready` 与 `precall`。发布、验收和排障不得擅自重跑正式报告。
14. 读 helper 与 GET 不执行 DDL；`init_schema()` 只允许在 API 启动、明确写命令或确认过的发布迁移中执行。

## 3. 当前产品状态

- 生产 API 与 macOS App 已发布 V2.5.1 Build 12；iOS Build 12 签名归档由用户自行安装。
- App 默认阅读层使用中文语义、明确价格、两层报告、整段复制与离线快照，不展示原始 Markdown、
  JSON、内部状态码、价格刻度或工程术语。
- 方向背景、市场事实、覆盖率、逐票画像、D0 收盘价、三价位预案、次日核对与成绩均已落地。
- 生产数据库为 28 张现行表，发布后完整性检查正常。
- 现役参数包为 `k9-params-20260823-r2`；机械标定值没有随 Build 12 改动。
- 晚间单元为 `facts → strategy → report`；旧 `scan/basket` 单元已删除。

## 4. 日常排程

| 时刻 | 任务 |
|---|---|
| 16:05（周一至周五） | 更新行情；休市日跳过 |
| 19:00（周一至周四） | 生成报告；休市日跳过 |
| 19:00（周日） | 使用前一周五盘面并纳入周末消息 |
| 9:26–9:29（D1） | 第一拍核对并推送 |
| 10:00–10:05（D1） | 零 LLM 终值结算，不推送 |

周五不直接生成报告；该周五休市时，周日不回退重跑周四。

## 5. 数据与安全边界

- `Backend/data/`、`.env`、密钥、生产数据库与行情文件都是本地或运行状态，不提交 Git。
- 测试只能使用临时数据库或明确的只读快照，绝不能回落到工作数据库。
- 方向背景与用量账不保存密钥、Authorization、完整 prompt、完整搜索响应或机械排名。
- Token 未回传时记录 `null + unavailable`，不得估算；设置页不凭空换算人民币费用。
- App 离线缓存按服务地址和凭据指纹隔离；鉴权失败不得使用旧缓存掩盖。

## 6. 当前观察项

1. 连续 3–5 个交易日对照 App 用量账、DeepSeek 与 Tavily 控制台，再讨论成本参数。
2. 观察首份 Build 12 正式报告：成立率、消息剔除持续补位、方向背景、清单数量、逐票详情与复制。
3. 记录一周 9:26 双源 `open` 分布，再决定是否调整数据质量状态。
4. iOS 真机安装与视觉验收由用户执行。

## 7. 发版硬门禁

```bash
cd Backend
.venv/bin/python -m pytest -q

cd ../App
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'platform=macOS' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'generic/platform=iOS Simulator' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline build-for-testing -destination 'generic/platform=iOS Simulator'
```

## 8. 下一版本边界

- 影响使用的问题立即快修；不影响使用的改进先记入本节，集中进入下一版本。
- 任何策略阈值、参数包或 LLM 成本边界变化必须先由用户裁定，不得以清理、重构或默认值名义带入。
- 清理完成后仓库只保留当前运行代码、测试、三份现行文档与空的 `archive/` 预留目录。
