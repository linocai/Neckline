# Neckline · V2.6.0 / K9-v2 升级计划

> 当前生产：V2.6.0 Build 16 / K9-v2 / fp-3 / d2-v1
>
> 最终后端标签：`v2.6.0-b16-r3`；发布代码：`676d20e`
>
> 当前阶段：一条龙发布完成；K9-v2 Day 1 已进入观察，状态 `continue-observing`
>
> 策略方向文件：`/Users/linotsai/Lino/whynotme/K9.md`
>
> 正式参数包：`k9-params-20260824-v2-r1`，原包 SHA-256
> `718bf7876d69936937edfdc7432bbea88ec1cd3e6e6107501acd325b7f1098df`

## 1. 目标与当前边界

本轮把 Neckline 的生产链路从 K9-v1 升级为 K9-v2，覆盖事实包、参数契约、四通道、
排序配额、解释、作战卡、次晨复核、D+2 成绩线、API 与双端 App。

- Neckline 只实现稳定运行合同，不导入或调用 `whynotme`。
- `whynotme/K9.md` 是本轮策略方向源；其中的非数值规则必须先被翻译成可验证的机械合同。
- 当前线上已经完整切换到 V2.6.0 / K9-v2；正式参数、fp-3、D2 成绩合同、API、App 与生产 schema 作为一个版本集合运行。
- 系统施工期不生成真实 K9-v2 参数，不借用 K9-v1 数值，不植入样例或临时默认值。
- 测试只从正式 schema 原包派生隔离夹具；生产加载器找不到合格 K9-v2 参数包时，
  必须返回“今天没跑成 · 参数未配置”，不得产出清单。

## 2. 已确定规则

1. 系统版本为 **V2.6.0**，策略身份为 **K9-v2**；K9 只是策略家族名，不能代替版本字段。
2. 结果观察窗由 D+4 改为 **D+2**；主成绩只使用 D0 / D1 / D2。
3. 先通过**有效活跃度边界**划定可比市场，再运行四条相互独立的召回通道：
   - P1：活跃股首次放量启动，且近期不具备 P3 持续热门身份。
   - P2：个股性超跌反弹；多日绝对超跌与同期跑输所属申万二级行业缺一不可。
   - P3：热门强博弈；它完全替换 K9-v1 的旧 P3。
   - P4：资金领先价格，且只能在活跃度门内运行。
4. 每条通道保留严格层与宽松层；信号必须能由冻结事实机械复现，策略层禁止联网和调用 LLM。
5. 排序继续采用机械合成、跨通道接力、10–20 只动态配额；同一股票多通道命中本身不加分。
6. LLM 只做市场背景读取、解释与受封闭输入约束的次晨判断，不参与召回、排序、阈值选择或指标计算。
7. 09:26 次晨判断、10:00 作战卡时序不因本轮升级改变；P3 必须获得独立的新作战骨架。
8. 每日活跃预测队列最多维护 60 条 D0 / D1 / D2 记录；滚动成绩不受 60 条限制。五项主指标为：
   D+1—D+2 上涨触达率、D+2 收盘胜率、D+2 行业超额、D+1—D+2 最大回撤、最终清单提升，
   并同时提供全清单与 P1 / P2 / P3 / P4 口径。
9. 不把“数据不可用”解释成否定信号；缺失事实、源未覆盖和确实未命中必须是不同状态。
10. K8 已退役，本轮不得恢复任何 K8 运行合同、兼容层、字段、页面或文案。
11. 切换后不并行保留 K9-v1 运行入口；历史如何保留只服务审计，不能形成旧策略兼容路径。
12. 参数包必须完整声明策略版本、事实包版本、来源、审批信息和全部策略选择；加载器不得补默认值。
13. P3 的涨停、跌停、龙虎榜、控异动、反包和二波结构都是可用时的附加证据，不是入池硬门；
    缺失时按剩余分项归一，跌停等负向结构必须增加风险标记。
14. 行业基准固定为 SW2021 二级分类的当日冻结成员涨跌幅中位数，只排除全天停牌成员；
    不使用行业指数、概念板块或今天的成员关系回填历史。
15. 跨股票活跃度比较使用成交额，不直接比较成交量。P1/P2 共享“当日量 ÷ 前 20 日均量”的读数但门槛独立；
    P3 不再与 P1 共用 V，P4 的盘后量比仍是“当日量 ÷ 前 5 日均量”。

### 既有生产护栏

- 现有数据就绪检查、交易日历、任务幂等和“缺数据就明确 not_run、不回退旧日报”继续成立。
- 恢复任务只补当前 K9 所需且最多 60 个交易日的数据前置，不自动补跑报告或 APNs。
- 通知仍只保留 `report_ready` 与 `precall`；9:26—9:29 只冻结竞价放弃/待观察，
  10:00 是三分支唯一终值，之后不改判。
- 联网继续只走既定检索与用户配置的 OpenAI 兼容 Provider；Provider 级并发固定为 3，安全降级和用量账不因换策略放宽。
- HTTPS、Keychain、上传限额、GET 零 DDL、临时数据库隔离和部署前可恢复备份均是继承门禁，不在本轮重开。
- 可选 S3 异机备份保持未配置、未启用，不作为 V2.6.0 发布门禁。

## 3. 暂不纳入本轮

- 不在 Neckline 内做参数搜索、回测调优或人工挑选阈值。
- 不改写研究侧批准的正式 K9-v2 原包；后续调参必须生成新的不可变包版本。
- 不改变数据供应商、网络安全边界、报告日期口径或既有定时任务时刻，除非新合同确有必要。
- 不在未经确认、备份和回滚设计的情况下修改生产数据库、线上配置或已部署服务。
- 不在旧 schema、旧参数或未完整部署的混合状态下运行报告；完整切换后按用户裁定立即用最新完整交易日生成 K9-v2 Day 1。
- V2.6.0 的不可变构建序列确定为 **Build 16**。

## 4. 已冻结的实现合同

非数值算法形状与正式数值均已封闭；运行只接受批准原包逐项给出的窗口、阈值和权重。

| 项目 | 冻结实现 | 参数包职责 |
|---|---|---|
| 有效活跃度 | 滚动中位成交额 + 参与密度横截面分位加权；参与密度闭集为换手率或成交额/流通市值 | 两个窗口、参与密度枚举、两项权重、淘汰分位线 |
| P2 绝对超跌 | 同一窗口同时计算复合跌幅与距近期高点回撤；使用方式为 cumulative / drawdown / either / both 闭集 | 窗口、口径枚举、绝对门槛、行业跑输门槛 |
| P2 支撑/释放 | lowRecovery / downsideDeceleration / effectiveTurnover 三项闭集，声明启用项与最低满足数 | 启用项、最低满足数及各项阈值 |
| P3 热门强博弈 | 持续热门 + 近 5 日强博弈 + 当前活跃为硬门；涨跌停、龙虎榜、控异动与反转二波仅作可用时加证，跌停同时加风险 | 热门、博弈、活跃、风险阈值及证据权重 |
| D+2 标签 | D0 收盘为主账起点；U/L 必填；D1 辅助账使用 10:00 前最后一笔有效成交；行业匹配基准固定为申万二级中位数；同日同时触线且无分时顺序时标记路径不明 | 原包固定 U=+3%、L=-4% |
| K9-v1 历史 | 新读写路径只认 K9-v2，不会混入新分母 | 两次活动结果标记 `invalidated / superseded_by_k9-v2` 后归档并从活动库清除；K9-v1 与 `k9-params-20260822-r1` 正式历史不删除 |

## 5. 版本与数据架构

### 5.1 身份隔离

- 新增并贯穿 `strategy_version = K9-v2`，覆盖运行、清单、报告、预测、成绩线与审计记录。
- `strategy = K9` 只作家族聚合，任何成绩查询都必须按版本过滤。
- K9-v1 参数包、旧事实包或缺少版本声明的包，对 K9-v2 加载器一律无效。
- API、缓存和 App 模型必须显式携带版本；不得靠报告文案推断。

### 5.2 事实包

- 冻结现有 fp-2，不原地改变其语义；K9-v2 所需新事实进入 fp-3。
- fp-3 至少覆盖成交额、换手率、流通市值、涨跌停状态、连续强势状态及 P3 最终合同要求的机械事实。
- 若采用龙虎榜或其他非全覆盖源，事实中同时记录“有数据 / 确认未命中 / 源不可用”。
- 日更、回补、完整性检查、研究快照导出共用一套字段语义和来源审计。

### 5.3 参数包

- K9-v2 参数结构独立版本化，所有数值、枚举、窗口、权重、阈值及交叉约束均为必填。
- 必须验证策略版本、事实包版本、字段全集、数值范围、窗口可满足性、权重和、阈值顺序及来源审批。
- 仓库保存故意不可运行的占位结构示例；生产包只认 `Backend/config/k9-params.json` 的完整正式 schema。
- 正式包已经按原始字节接入并锁定上述 SHA-256；禁止人工改写后投入生产。

### 5.4 成绩隔离

- 新预测记录保存策略版本、参数包版本、事实包版本、生成日和标签合同版本。
- K9-v2 聚合只读取 K9-v2 / D+2 记录；K9-v1 / D+4 记录绝不能进入新分母。
- 不可交易、停牌、行情缺失与观察窗尚未结束使用不同状态，不以零收益代替。

## 6. 施工顺序

### P0 · 合同冻结

- 将 `whynotme/K9.md` 的 K9-v2 方向逐条映射为工程合同，列明公式、闭集枚举、输入、输出和缺失语义。
- 清理 `whynotme/Neckline新架构_20260818.md` 中仍与 K9-v2 冲突的 D+4、旧 P3/P4 和旧成绩口径；
  保留当前用户未提交改动，不覆盖研究目录中的其他工作。
- 定稿 strategy / fact pack / parameter pack / label contract 的版本关系。

验收：工程合同中不存在 K9-v1 语义残留；未决策略项被明确拦截，没有隐含默认值。

### P1 · fp-3 事实链

- 扩展事实声明、构建、持久化、日更、回补、完整性检查和研究快照导出。
- 为 P3 数据源实现覆盖状态与实际可得时间约束，防止使用在当次运行时尚不可见的信息。
- 为每个新字段建立来源、日期、空值和确定性测试。

验收：相同输入生成字节级稳定的事实；策略执行期间不联网；缺失和否定信号可区分。

### P2 · K9-v2 参数合同

- 建立无默认值的 K9-v2 参数模型、加载器、校验器与可读错误报告。
- 隔离测试夹具和运行配置；明确拒绝 K9-v1、fp-2、未知枚举、漏项和越界组合。
- 无合格参数时整条日报链路安全停止，并输出既定未运行文案。

验收：删除任意必填项都会失败；测试夹具不能被生产路径发现；不存在 V1 回退。

### P3 · 活跃度门与四通道

- 先计算有效活跃度边界，再分别实现 P1、P2、P3、P4 的 strict / relaxed。
- P3 重新实现，不复用旧 P3 的业务语义；P4 增加活跃度硬门。
- 每个候选保留门槛轨迹、强度、证据、风险、缺失归一化与淘汰原因。

验收：四通道可独立重放；同一事实和参数产生相同结果；所有方向与层级和 K9-v2 合同一致。

### P4 · 排序、配额、持久化与报告

- 接入 K9-v2 机械排序、跨通道接力和 10–20 动态配额。
- 在运行、清单、候选、报告与审计中持久化完整版本身份及证据/风险结构。
- 解释回填继续保持幂等，解释失败不改变机械名单。

验收：多通道命中不被额外奖励；最终数量与接力规则可解释；重跑不产生版本混算或重复记录。

### P5 · Explain、作战卡与次晨判断

- 为 K9-v2 更新封闭输入合同、提示词、输出解析和缓存键。
- 给 P3 建立热门强博弈专属作战骨架；同步 P1/P2/P4 新语义与风险提示。
- 保持 09:26 判断和 10:00 作战卡时序，验证审计事件、降级状态和内容安全。

验收：LLM 无法改变召回或排序；四通道文案不串型；离线重开仍能读到完整结果。

### P6 · D+2 成绩线

- 建立版本化预测与 D0/D1/D2 结果计算，每日活跃队列最多维护 60 条记录，历史聚合采用滚动多日样本。
- 实现 K9-v2 五项主指标、分通道切片、D1 辅助观察和匹配基准。
- 删除运行中的 D+4 聚合、旧字段依赖与旧 App 占位；历史处置遵循迁移裁定。

验收：分母、不可用状态、观察窗和基准都可追溯；任何查询都不会混入 K9-v1。

### P7 · API 与 iOS/macOS App

- 更新报告、清单、详情、成绩线、设置与审计 DTO。
- 更新双端通道名称、卡片层级、风险/证据标识、D+2 成绩文案和版本展示。
- 升级离线缓存并处理旧缓存失效；移除旧 P3/P4 与 D+4 的运行文案和页面合同。

验收：API 与 App 对版本、空状态和缺失状态理解一致；macOS 与 iOS 均通过完整构建。

### P8 · 集成验收与参数包接入门

- 使用显式测试夹具完成端到端重放、确定性、并发幂等、版本隔离与恢复路径验证。
- 在临时数据库验证 V2.6.0 当前 schema、旧 schema 只读安全和 GET 零 DDL；生产前滚脚本在 P9
  根据用户对 K9-v1 历史的裁定定稿；本轮已经完成精确作废归档与业务队列清空脚本。
- 完成无参数生产形态验收：服务和 App 可运行，但策略不出清单。

验收：系统已具备接收真实参数包的能力；该门已由正式原包通过。

### P9 · 正式参数包验证与发布候选

- 对用户提供的原始包执行哈希、来源、审批、版本和全量约束校验。
- 用冻结事实快照做隔离重放，核对研究侧与生产侧的候选、排序、配额和成绩计算一致性。
- 裁定 K9-v1 活动记录处置，针对生产目标制作迁移前备份、前滚与成对回滚材料并演练。
- 确定不可变 Build 号，更新 V2.6.0 运维入口、发布说明和回滚材料。

验收：参数包未经修改即通过；候选版满足发布门禁；用户已明确授权一条龙部署。

## 7. 验证矩阵

所有后端测试使用临时数据库或显式只读快照，禁止触碰 `Backend/data/` 工作库。

```bash
cd /Users/linotsai/Lino/Neckline/Backend
.venv/bin/python -m pytest -q

cd /Users/linotsai/Lino/Neckline/App
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'platform=macOS' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline -destination 'generic/platform=iOS Simulator' build
xcodebuild -project Neckline.xcodeproj -scheme Neckline build-for-testing -destination 'generic/platform=iOS Simulator'
```

补充门禁：

- 参数合同：缺项、错版本、错事实包、未知枚举、非法窗口、非法权重全部拒绝。
- 事实合同：时间点、覆盖状态、空值、复权、行业对齐和快照哈希测试。
- 策略合同：边界、四通道 strict/relaxed、排序、接力、配额、解释隔离和确定性金样测试。
- 成绩合同：D0/D1/D2、60 条上限、不可用状态、匹配基准、全局/分通道与版本隔离测试。
- 运行合同：无参数不出清单；日报、次晨与作战卡任务幂等；重启后缓存和状态一致。
- 数据库合同：临时库迁移、备份恢复、前滚/回滚成对验证。
- 若修改 `whynotme` 的 Python 代码，只运行与本轮合同相关的定向测试，并单独记录仓库既有失败；
  纯方向文档同步不以修复研究仓库历史测试为前置条件。

## 8. 发布与回滚边界

1. P0–P9 已完成本地施工和验收；本轮发布授权覆盖提交、标签、签名、服务部署、数据库迁移和首份 Day 1 报告。
2. 发布前确认服务、数据库、定时器、App 签名与参数包的真实目标，并建立可校验备份。
3. 数据库迁移、服务版本和参数包必须作为一个版本集合前滚；不得只切其中一项。
4. 回滚必须恢复相互匹配的服务、schema、参数包与已签名客户端；旧包不能被新加载器误用。
5. 切换后观察健康检查、名单数量、版本身份和任务审计；完整部署后立即生成最新完整交易日的 K9-v2 Day 1。
6. 正式切换已完成；任何回滚必须恢复发布前服务、数据库、参数包和匹配客户端，不允许形成混合版本。

## 9. 当前状态与下一动作

- [x] 完成 K9-v1 生产链、K9-v2 方向文件、事实包、参数包、成绩线与 App 影响面盘点。
- [x] 确定 V2.6.0、参数后置、无默认值和版本隔离边界。
- [x] 建立 P0–P9 施工、验收、发布和回滚顺序。
- [x] P0–P2：同步方向架构，完成 fp-3、龙虎榜覆盖状态、有效活跃度与零默认 K9-v2 参数合同。
- [x] P3–P5：完成四通道、P1/P3 身份互斥、排序配额、证据/风险、报告与 P3 独立 D1 预案骨架。
- [x] P6–P7：完成 D0/D1/D2 预测队列、D2 五指标、D1 辅助账、API、缓存及 iOS/macOS 展示。
- [x] P8/P9：正式原包 SHA-256 与研究批准记录完全一致；最终后端全量 **1132 passed**。
- [x] macOS build、iOS Simulator build、iOS build-for-testing 三条强制门禁全部成功。
- [x] 在生产 SQLite 一致性副本上完成作废前滚与成对回滚演练：12 张 K9-v2 活动表归零，61 份事实包记录不变，回滚后两次旧运行与报告原样恢复。
- [x] 发布候选确定为 **V2.6.0 Build 16**；结构示例故意不可运行，正式包缺失或无效时仍不出清单。
- [x] `main` 已提交并推送；保留 `v2.6.0-b16`、`v2.6.0-b16-r2`，最终后端使用不可变标签 `v2.6.0-b16-r3`。
- [x] macOS 与 iOS 签名产物已生成；macOS 分发包通过 Developer ID 严格验签，iOS IPA 通过开发签名和描述文件验签。
- [x] 发布前完整服务器备份位于 `/opt/neckline-release-backups/v2.6.0-b16-pre-20260824-230400/`，数据库备份 SHA-256 为 `e5da985c01c1778af9ed4365c6b58e6d9b4ee1c86865b5991cee1c9665587894`。
- [x] 两次 K9-v1 活动结果已作废并归档到 `/opt/neckline/data/archive/k9-v1-invalidated-20260824/`；候选、D1/D2、待结算和成绩状态已清空，原始行情快照及正式历史保留。
- [x] fp-3 已完成最近 60 个交易日冻结；迁移前 61 条事实包记录未改动，生产当前事实包记录共 121 条。
- [x] 最新完整交易日 2026-08-24 已生成 **K9-v2 Day 1**：运行 `bcce862a80fb409e8d6fcf02daeaec97`，最终严格清单 20 只，观察队列 20、已结算 0、成绩分母 0。
- [x] 公开 HTTPS、参数与事实包版本、服务重启次数、数据库完整性及三项 timer 已核验；本机 macOS 真实清单页和空成绩基线已核验。
- [x] macOS V2.6.0 Build 16 已换装并保留可恢复的 Build 15 备份；iOS IPA 已交付目录，最终安装由用户完成。
- [ ] 继续观察 Day 1 的 D1/D2 演进；在出现正式 D2 样本前，实验状态保持 `continue-observing`，不宣称 K9-v2 优于 K9-v1。
