# Neckline V3 · 3.1.0 / 双端 Build 39 · K10-v1.4

本文件是工程控制面。详细施工契约见 [archive/v3.1.0-b39_execution.md](archive/v3.1.0-b39_execution.md)；策略行为以 [`whynotme/K10.md`](../whynotme/K10.md) 及用户本轮最新决定为准，生产不得导入研究仓。

## 概述

3.1.0 将 B38 的“冻结文章→一次事件搜索→核验→比较”改为可检查、可恢复的命题调查：原文先拆关键命题，复用适用证据，围绕具体缺口受控搜索／全文，再按证据收口并完整比较公司。目标是让推荐、待核与执行失败各自有真实含义，且不重置已发布机会的 D1/D2 成绩。

## 稳定技术决定

- 目标为 `3.1.0 / 双端 Build 39 / Schema 7`，策略仍为 `K10-v1.4`；执行配置继续显式绑定 DeepSeek V4 Pro、Tavily、80／40 和无整轮资源总上限。
- Schema 7 以既有 source-document versions、external attempts、checkpoints、article admissions 与发布 `comparison_json` 为基础，只新增最小研究快照、问题路径和证据关联；不为每个 Prompt 阶段建立一套表或进程。读 helper 绝不执行 DDL。
- 研究状态与执行状态分开。`task completed`、`scan partial` 或零正式候选均不得表示“比较完成”或“无机会”；协议、引用、模型或程序错误保留定位与检查点，投影为执行失败。
- 每个输入公司的比较结果必须是 `primary`、`alternative`、`tied`、`pending` 或 `excluded` 之一；仅前三类组成发布派生集并按既有名额／机会规则发布。`pending`／`excluded` 仍持久化并可查看，不为通过校验而漏公司或硬排。
- 未核传闻可正常参与比较、正式推荐、晚间 30 家名额及 D1/D2 成绩。它必须显示“未核实”、传闻性质、真实源头或“来源未知”、未证实环节和条件化分析；不得伪造成已核事实、来源编号、公司身份或硬排序。未核本身不是 `pending` 的自动理由。
- 80／40 仍限制实际正文深读。冻结正文及受控补读均走既有 admission；不预留核验全文名额，额外全文只使用当时实际剩余且不得挤占冻结名单。搜索摘录不计全文，Extract／全文交模型前必须获准。
- 事实缓存以来源版本、内容哈希、调查契约／Prompt／模型参数版本键控；稳定来源事实可复用。问题答案、市场判断、比较和发布始终绑定消息截止、实际查证截止与上下文版本，不能回填旧报告。
- 本轮只授权本地实现、离线和隔离 native 验收。线上 `v3.0.4-b36 / Schema 4` 的 worker、晚／晨 timer 继续停用；不部署、不改生产库、不复活旧任务，也不把 B38 的一次真实窗口重跑为 B39 付费验证。

## 当前状态

- Git 代码基线为 `0c8a143`；本地 B38（`3.0.6 / Build 38 / Schema 6`）未发布，线上仍是 B36。
- B38 的 1,430 标题、37 篇正文、9 次 Tavily／45 条摘录实验止于公司比较 `model_execution_invalid`；原响应未保存，根因未知，不能称已修复或将零候选解释为无机会。
- B38 已有标题全量审计、冻结准入、80／40、usage ledger、缓存、pause／retirement 与双端进度投影，可复用但须适配新研究契约。
- B39 已接通一次正文理解提取命题、动态问题搜索／全文、证据审读与完整公司比较；正式推荐链路支持带披露的未核传闻，待核／排除保留记录，执行失败阻止发布。
- 恢复复用原任务、冻结文章与已完成调用；研究模型输入不因运行状态／快照流水版本改变而重复计费。未知结果阻止重发，已知语义错误可受控恢复，错误缓存不会永久冒充成功。
- API→Swift 实际隔离响应解码、三条双端构建和实际 iOS 填充／空态已通过；白卡蓝色风格及可展开的研究／比较状态已对图验证。
- 历史离线验证覆盖 1,431 条原始审计（精确去重后 1,430）、37 篇冻结正文，新增付费调用和生产写入均为 0；这不证明 B39 真实模型判断质量。
- 本地实现提交 `4bcfd5b`；后端 **905 passed**，4／6→7 带历史数据的备份／恢复验证通过。无新增付费外呼、部署、生产写入或 worker／timer 恢复。
- 用户本轮已覆盖策略资料中“传闻独立推荐”和“预留全文名额”的旧待定项，二者均不是待用户决定事项。

## 当前 Plan

- 本轮正式施工及本地验收完成，无剩余施工块；详细实现、回归与 native 证据见当前版本记录。未进行独立 review、新付费验证或发布。

## 待用户决定

无。

## Backlog

- 工程和本地验收完成后，如需新的一次真实隔离验证或发布，主会话先给出明确最新窗口、凭据／数据库隔离、最大正文范围与回执方案；该授权不由本 Plan 推定。
- 发布前需从线上 B36 到目标版本审查累计差异、Schema 4→7 迁移、备份恢复与停机状态；本轮未请求 review 或发布。

## 里程碑索引

- 3.0.4 / Build 36：线上已发布，后因全量逐篇处理停机；见 [archive/v3.0.4-b36_execution.md](archive/v3.0.4-b36_execution.md)。
- 3.0.5 / Build 37：未发布预算／资料包方案，已替代；见 [archive/v3.0.5-b37_execution.md](archive/v3.0.5-b37_execution.md)。
- 3.0.6 / Build 38：标题初筛与 80／40 已实现，真实比较中断且未发布；见 [archive/v3.0.6-b38_execution.md](archive/v3.0.6-b38_execution.md)。
- 3.1.0 / Build 39：本地施工／验收完成，未发布；见 [archive/v3.1.0-b39_execution.md](archive/v3.1.0-b39_execution.md)。
