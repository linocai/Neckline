# Neckline — A 股短线量化交易系统 · PROJECT_PLAN

> 本文件是本项目唯一权威施工件。全局规范见 `~/.claude/CLAUDE.md`。
> **铁律:任何 token / key 绝不写进任何被 git 跟踪的文件,一律从 `.env` 读。**
> 项目名 Neckline(颈线)= 技术形态的分水岭,寓意「过线才动手」的纪律内核。

---

## 一、概述

### 1.1 项目定位

Neckline 是一套**只审计、不代下单**的 A 股短线量化决策系统。它把用户的短线交易从「拍脑袋 + 情绪驱动」重构为「回测验证过的规则 + 盘后计划 + 盘中执行哨兵 + 事后违纪对账」。核心信条:**研究先于产品,规则先于直觉,系统给纪律与证据、下单与止损由人和券商执行。**

系统三态同码(同一套信号代码):喂历史 = 回测,喂今日 = 盘后报告,喂单票 = 问询台。回测引擎是常驻地基,不是一次性脚本。

### 1.2 用户画像与约束(全部写死为设计前提)

- **资金**:总资金约 12–13 万,**不再入金,固定分母**(历史越亏越入金是死因之一,新系统按固定盘子做风控)。
- **时间(v1.2 修正,2026-07-25 策略线 register)**:~~本职工作忙,盘中约每 20–30 分钟看一次手机~~ → **用户非上班族、时间充裕可全天盯盘**;交易制度 T+1 不变。**盘中人判层(集合竞价 / L2 盘口)从「不可用」变为「可用」**(此前按上班族注意力设计,盘中只做提醒不做人判;现用户可亲自做盘中强度 / 竞价 / 盘口判断)。**边界(必读,防误用)**:① 本条只改「系统线当前设计前提」这一处;`STRATEGY_LAB.md` 雷区地图信封定义里「上班族注意力」是**历史口径、策略线明确保持原文不改**——此修正**不复活**任何已判死的信封(三场战役死因是 alpha 灭失、非注意力不足),**不得据此宣称信封判决失效**。② 因此可**重新讨论**的既有设计仅限「盘中人判层能力」(如 v1.2.1 呼吸打法的竞价 / 盘口择 T 由人判承担期望假设);**本次不扩范围**,回测审计仍受 EOD 数据边界约束。
- **执行分工**:止损已改用**券商条件单**执行(在系统之外),每笔开仓必挂 -5% 止损。系统只做盘后计划、盘中提醒、盘后对账,**永不自动下单**。
- **实盘节奏**:2026-05 开始实盘,阶段 0–1(数据层 + 策略研究)期间用户继续实盘,按下方「纪律章程」**手动**执行,系统尚不介入。

### 1.3 交割单归因(立项依据,核心发现)

对 2026-05-12 → 07-17 全量交割单做了归因(源表 `/Users/linotsai/Downloads/2026年股票交割单整理.xlsx`;归因脚本在会话 scratchpad,仅参考,**不入仓库**):

- 账户级:净投入 15.28 万,已实现亏损约 2.51 万(**-16.4%**,约 10 周)。
- 闭合回合 37 笔,**胜率 40.5%**、**盈利因子 0.47**(总盈 / 总亏),典型「小赚扛大亏」。
- **13 笔破 -5% 未止损**多亏约 1.38 万 → **占已实现亏损 85%**。→ 结论:止损纪律是第一死因,故 -5% 条件单强制化。
- 买「绿盘大阴线 / 下跌途中票」是主要死因;**追强势票反而 67% 胜率** → 母战法定为强势股短线。
- **7 笔买入次日跌停**,集中在高弹性题材 / 次新票型 → 该类票型入黑名单或强制减仓。
- **持有 4–7 自然日**是唯一打平的持有桶 → 时间退出思想(D4)有据。
- 开仓日上证在 **MA20 下方 28 笔合计 -1.95 万、上方 9 笔 +0.32 万** → 市场过滤器有强信号,但作为「争议项」交回测定生死。
- **越亏越重仓越入金**的加仓行为与亏损正相关 → 仓位纪律(单笔上限、总敞口上限)写死。

### 1.4 与前作 LinoN 的关系(继承与切割)

前作 LinoN(`/Users/linotsai/Lino/LinoN`,FastAPI + SQLite + SwiftUI 双端 + ECS 部署)被用户判定为「**交易记录系统而非量化**」——选股拍脑袋、无回测、LLM 深判是模板腔,已弃用。

- **切割**:Neckline 以**回测引擎为地基**,一切规则参数须过回测才落地;LLM 一律自由对话体,**禁枚举模板卡**。
- **继承**:LinoN 的**工程资产**(尤其大量已踩平的数据源坑)必须吸收,详见 §3.7。LinoN 的坑清单权威在 `/Users/linotsai/Lino/LinoN/CLAUDE.md`(builder 落地对应模块前必读对应小节)。

### 1.5 V2.0.0 定位(2026-08-02 立项,重心转向)

V1(v1.0 → v1.5.2)把系统建成了一套**纪律 / 回测 / 记录 / 归因**都完善的减损系统,但用户实测出四个真问题:开仓前填写内容过多、大量字段反复给出相同理由、**选股能力没有成为产品中心**、每日 20 只候选高度集中于同一板块(相关性高、信息重复)。

**V2 的总体目标**(蓝图 §一):

> **用轻量纪律保护账户,把主要计算、数据和 LLM 能力投入选股;系统从严格的家教老师转为选股雷达与持仓助手。**

- **选股单位由单票改为股票篮子**(同一主要驱动、次日方向相似或明显联动的 1–3 只),按 **Tier-1/2/3** 分层、D0 冻结篮子卡、D+1 验证与复盘、周期性校准 —— 语义换血总表见 **§2.8**。
- **持仓由「严格家教」改为「全能助手」**:极简台账 + 监控 + 通知分级 + NL 提醒 + 计划继承 + 资金对账 + 交易画像;**纪律外壳(§2.1)一行不动**。
- **继承关系总原则**:**换心脏(候选管线 → 篮子引擎)+ 减负(表单 → 自动快照)+ 装新感官(盘中存拍 + 复盘评价),骨架(数据、纪律、对账、哨兵、推送)全部继承 V1。**
- 施工图 = **§五 V2.0.0**(17 块);产品语义总纲 = 仓库根 `新版本量化交易APP与选股架构.md`(**用户蓝图,非施工权威**);工程推演原稿已归档 `archive/V2架构设计稿_20260731_20260802立项归档.md`(**已转历史档案,不得作为第二权威**)。

---

## 二、设计共识与领域规则(方向已定案,不得擅改)

> 本章是产品圣经。build 照此实现;**任何改动方向须回 planner**。参数值凡标「回测定」的,由阶段 1 过堂后写死,不由 build 拍。
>
> ⚠ **2026-08-02 V2.0.0 立项**:本章 §2.2~§2.6 中有若干条款被 V2 语义取代(单票候选 → 篮子、排序 → Tier、六类推送 → 三级、决策日志表单 → 自动快照等)。**换血对照总表 = §2.8**,该节凌驾被它标注取代的条款;**历史拍板全文一律保留不删**。**§2.0 第〇原则与 §2.1 纪律章程在 V2 全文有效**(四锁的 V2 精确表述见 §2.8-C,**必读**)。

### 2.0 第〇原则:LLM 是参考,硬条款才是纪律(2026-07-29 用户口述定案,v1.5.0 立项)

**LLM 的作用永远是参考,硬条款才是纪律。** 这条凌驾于本章其余各节对 LLM 的一切描述,新增任何 LLM 产出物前先过这条:

1. **参考件不触发任何机器动作**。LLM 产出的买入 / 离场参考区间、明早证伪剧本、审判评语、问询台回答——**一律不进哨兵判据、不进推送、不进排序键、不改候选去留、不写决策日志、不改任何纪律阈值**。它们只出现在"给人看"的位置。
   > ⚠ **2026-08-03 记名豁免一处(V2 review 判定线 🟡-3 收口)**:本条「**不进推送**」自 V2 起有**且仅有**一个例外 —— `take_profit` kind 的**触达告知**(用户 2026-08-03 拍板开该 kind)。**豁免的边界、四条前提与机械 sanity 闸全文见 §2.8-C-3 与 §五 ⑪-D**;本条其余部分(尤其「**不改任何纪律阈值**」)**一字不变、无任何豁免**。⛔ **不得据此把买入参考区间 / 明早剧本 / 审判评语 / 问询台回答接进任何推送或判据。**
2. **纪律只住章程**。−5% 止损 / 回落止盈 8% / 时间退出两档 / 仓位三仓 / 熔断阈值的唯一事实源仍是现役 `strategy_versions` config(+ 熔断的命名常量),**LLM 说什么都不改它**。「参考在上、纪律兜底」:参考件负责让用户少等几个 8%,纪律负责在用户不动手时兜住。
3. **机器不禁、人可复核**。LLM 判风险大 → 只收回参考件(不给区间、只给不买理由),**票与信息卡照留**;不许把 LLM 的判断做成闸门(§2.5 v1.3.3 拆墙的同一条精神)。
4. **参考件必须自带"参考、非指令"标注**,终选权在用户(与「排序 = 注意力优先级,不是收益预测」并列为语义红线两条)。
5. **参考件必须落库**并可与实际走势 / 成交对拍(LLM 参谋成绩单)——参考的价值要靠事后归因证明,不靠语气。

> ⚠ **勿与 v1.4-④ 的「第〇原则」混淆**:那一条说的是**考卷信息集 ≡ 实盘情报包信息集(同构)**,是信息供给的原则;本节说的是 **LLM 产出物的权限边界**。两条同时成立,互不替代。

### 2.1 纪律章程 v0.1(系统只审计,不执行)

1. **每笔开仓必挂 -5% 止损条件单**,只许设、不许撤、不许下调;系统盘后对账查单完整性。
2. **止盈 / 时间退出(v1.3 退出规则改革,2026-07-25 用户知情越线采纳,staged 生效)**:① **-5% 止损不变**;② **回落止盈 5%→8%**;③ **时间退出仅对非浮盈单**——D5(第 5 个持有交易日)收盘扣双边费后**净浮盈 ≤0 → 次日退出**;④ **浮盈单豁免时间退出**,交回落止盈 8% + -5% 止损管到浪停,**硬上限 15 个交易日**(防"无限期"名义下变相长线)。**⑤ 判向在 D5 定格一次,此后不再重判(2026-07-27 用户拍板,方案 A)**:D5 那天 16:35 用**当日 EOD 收盘**净浮盈判一次向并持久化,③④ 两态自那一刻起**写死**——D6 起无论净浮盈怎么变,豁免的仍豁免、判该走的仍该走,**只有 ④ 的 D15 硬上限例外**(始终按持有交易日数无条件触发)。理由(两条,勿删):**(一)回测验证过的规则才是能守的规则**——回测引擎就是「D5 判一次、豁免后一次性续命到硬上限」,逐日重判等于实盘执行一套从未被回测验证过的规则;**(二)堵死违纪被事后合法化**——「D5 判该走 → 用户没走 → D6 转浮盈 → D7 系统改口豁免」这条路必须封死,否则纪律形同虚设。配套:周复盘新增**时间退出违纪审计**(系统说该走、台账显示没走 → 记违纪),§2.1 第 2 条自此有周线兜底。唯一事实源 = 现役 `strategy_versions` config(`take_profit_retrace=0.08` / `max_hold_days=5`〔非浮盈时间退出档〕/ `max_hold_days_profit=15`〔浮盈硬上限〕/ `time_exit_only_if_unprofitable=True`);哨兵/回测读库自动跟随,**生效于 v1.3 激活**(staged,同第 3 条,激活前仍按 K1 现役值〔回落 5%/hold≤5〕执行)。**风险登记(不得删,原样入 charter 注释)**:② 回落 8% 系 H9 V0 网格观察免测采纳(六格网格唯一同时改良全期与 2026 的格);③④ 组合版**未整体回测**(最接近的 H9-V3 差 724 元未过 2026 生存门禁);**用户知情行使决策权越线采纳(2026-07-25)**,风险已当面告知——组合退出规则的真实期望未经样本外验证,误差主要体现在"刚好在盈亏平衡线附近"的单子判向。证据链:`research/h9_exit_reform.md` + `research/winners_anatomy.md`。**附注(v1.4-①-B,2026-07-28,§七 P0-2 口径落地,不改上文任何阈值)**:持仓票**当日无 EOD 行**(停牌 / 数据缺口)且**尚未定格**时,时间退出**判向挂起**(第五态 `suspended_hold`)——不定格、不推 D5、不推硬上限;`d_count` 照常按交易日累计并展示(纪律口径是「持有交易日数」,不因停牌暂停计数);**复牌当日 16:35 用复牌当日 EOD 正常定格**(届时若 `d_count` 已越过 `max_hold_days`,就在复牌当日定格)。**已定格的判向不因停牌撤回**(⑤ 的「判一次定格」优先)。**这是对既有保守兜底的定向收窄,不是放宽**:只针对「当日无 EOD 行」这一情形(原行为会催用户去卖一只根本卖不掉的票),其余无定格情形(EOD 管线断跑等)仍保守判 `time_exit_next_day`,一字不改。~~止盈不设固定线:回落止盈或时间退出(D4 思想),参数回测定(K1:回落 5%/hold≤5,v1.3 前)~~
3. **仓位纪律(v1.2 章程修订 = 三仓制,staged 生效)**:**最多持 3 只**(注意力约束「只做 3 仓」,不会更多);**单笔金额不定死**——由用户视股价与当时想控仓位当场在区间内自定,系统只把 **4 万**作为**违纪判定上限**(`single_cap`,**非推荐值**)、不替用户拍单笔金额;总敞口设满仓档(`max_exposure_frac`≈1.0)。**-5% 止损不变;止盈与时间退出按上方第 2 条 v1.3 新规**(~~回落止盈 5%、hold=5 不变~~ —— 本行 v1.2 立项时的写法已被第 2 条取代,勿按旧值实现)。唯一事实源 = 现役 `strategy_versions` config;**与第 2 条同批、生效于 v1.3 章程行激活**(staged:用户清掉现有持仓 + 确认后由切换器 `activate_charter.py --target v1.3 --confirm` 激活;激活前仍按 K1 现役值执行。⚠ 库里那行过时的 `v1.2` 章程行〔回落 5%/hold=5〕**保留但永不激活**,见 §五 v1.3-①)。~~单笔仓位上限 2 万;最多持 5 只;总敞口 ≤60%(K1 原值,2026-07-25 前;v1.2 激活后由三仓制取代)~~ **章程回滚 SOP(v1.5-④-A5,§七 P1-24)**:何时该回滚 / 走 `activate_charter.py` 哪条路 / 同步清单 / 要不要重跑受影响周复盘,见 `archive/SOP_章程回滚_20260730.md`(未经实战演练,首次真回滚后按实际经历修订)。
4. **单周实现亏损 ≥ 总仓 2%** → 当晚**强制复盘**(材料由系统生成)。~~「≥5% 次周单笔减半」挂起项~~ → **已否决**(2026-07-20 用户拍板:回测显示纯空效应 + 用户明确不要,永久关闭)。
5. **同票割肉后冷却**:冷却期天数参数回测定(初值 10 个交易日)。
6. **违纪审计并入周复盘**:用户每周提供交割单,系统对账(实际成交 vs 当周报告、条件单完整性)。
7. **熔断纪律(v1.2 新增,2026-07-25 用户批准,数字已定;落库即生效,不随三仓章程 staged)**:**连续 3 笔止损** 或 **单日实现亏损 ≥ 4000 元**(约总仓 3%)→ **当日停止开新仓、次日只减不加**,**完成一次强制复盘后解锁**(解锁复用第 4 条 / 4D 强制复盘机制,不另造)。系统**无法物理阻止下单** → 以**哨兵强提醒 + 客户端状态标记 + 日志留痕**实现;**熔断是纯提醒层,绝不代下单 / 撤单**(§3.8)。触发与解锁事件都落库(归因用)。**诚实边界**:熔断只能基于**用户已补录进台账**的成交判定,漏录则失灵——判定所依据的数据与时效须显式呈现给用户(看板 / 报告卡注明「基于台账 N 笔已补录成交」)。**阈值单一源 = 命名常量**(住熔断模块,**非** `strategy_versions` config;理由同第 4 条强制复盘线 `FORCED_REVIEW_LOSS_FRAC`——政策值非回测参数,不进大脑);连续止损判据用到的 `stop_pct` 仍读现役 config(不硬编 -5%)。**与第 3 条 staged 章程的关系**:第 3 条三仓制改的是 `strategy_versions` config 仓位字段、须 staged 激活才生效;熔断不读那三个字段、只读命名常量阈值 + `stop_pct`,故**代码部署即生效、不等章程激活**。**触发链口径补记(2026-07-27 审计,原为未写明的既有行为,实盘触发时不要当 bug)**:① **连续止损链无时间窗** —— 横跨数月的 3 笔止损同样触发(链只看「尾部连续」,不看间隔);② **解锁后链不重置** —— 解锁后第 1 笔新止损即可能再次熔断(尾部仍连着旧链)。两者都是**保守方向**(宁可多提醒),是刻意保留的口径。**「次日只减不加」的落点**:9:25:30 盘前 tick 查锁定态,锁定则 9:26 汇总推送**必发**(即便当日零判定也不被「平静清晨不轰炸」的门槛吞掉)并前置「熔断中:今日只减不加」;客户端横幅是拉取式,不能替代这条推送。

~~「买点 / 止损 / 目标 / 证伪条件」四件套~~ 的止损口径 **-5.0** 是全系统单一常量,只在一处定义,禁止各处漂移(继承 LinoN「规则常量唯一源」教训)。**v1.5.0 起候选卡四件套由「参考三件套」取代**(§2.3 需求 9 拍板 / §五 v1.5-①),**但这条常量纪律一字不变**:参考买入区间那一行里显示的止损价 = `close × (1 − stop_pct)`,`stop_pct` 仍**读现役 config**,不许硬编 0.05、不许由 LLM 产出。

### 2.2 母战法(策略内核)

- **风格**:强势股短线,持有 **2–5 个交易日**。
- **两类买点**:① 强势票**回调低吸**;② 平台**放量突破**。
- **禁买规则(第一版,阈值均回测调)**:
  - 当日跌幅 ≤ **-3%** 的绿盘大阴线禁买(初值)。
  - 距 20 日高点过远的**下跌途中票**禁买(阈值回测定,初值 -15%)。
  - **次新股 / 易跌停高弹题材票型**入黑名单或强制减仓。
- **强势的定义交回测赛马**(见 §6):涨停基因 / 20 日涨幅分位 / 量价结构 三选一或组合。

> **⚠ 阶段 1 判决(2026-07-20,以 SQLite 大脑 `strategy_versions.v1` 为唯一现行内核,本节以上文字保留作历史假设记录)**:日线 2–5 日窗口 A 股均值回归,「追强势」无正净期望——强势三候选**全否决**(`strength=none`);绿盘大阴线禁买、距前高禁买、同票冷却期均**被回测否决删除**;次新剔除否决、~~**高弹题材剔除采纳(风控属性)**、限主板~~(**v1.3.3 起解除,见下条**);买点走 pullback,hold=5,回落止盈 5%。规则 v1 定位:**减损纪律系统,非正 alpha 策略**;正 alpha 开放路径 = 情绪信号(阶段 2)+ LLM 消息面(实盘归因考核)。全文见 `research/stage1_report.md`。

> **⚠ 高弹题材剔除(P6 / 限主板)自 v1.3.3 起解除 —— 拆墙(2026-07-27 用户拍板)。** 章程 `v1.3.3` 把 `forbid_high_elasticity` **True → False**(承 v1.3 全部字段,只改这一个)。**为什么拆**:用户实操域就是高弹板块(五常驻里创新药 / 机器人 / 储能全是创业板 / 科创板大户),而 v1.3 起候选情报管线(§2.3 需求 5)**已刻意含全板块 MAIN/GEM/STAR**;`forbid_high_elasticity=True` 却仍在**四处**生效——周复盘违纪判定(`review/reconcile.py`)、问询台(`api/inquiry.py`)、自选体检(`report/watchlist_check.py`)、回测 entry mask(`strategy/momentum.py`)——**同一系统两套口径**,用户每买一笔创业板都会被周复盘误标违纪。本次把纪律判定域与生成域对齐。**风险登记(不得删)**:① **此 config 与任何回测基线都不同** —— K1 六年回测里高弹是被**剔掉**的,当年作为**风控**采纳(§1.3 第二死因:20% 涨跌幅品种与 -5% 止损不匹配、易一字跌停),关掉它 = 入场域扩到**从未被该 config 回测过**的范围,**冻结基线 N=1288 / −20.53% / 95361.4988 不适用于 v1.3.3**;② 拆墙后高弹的风险**改由 -5% 止损 + 三仓纪律(3 仓 / 单笔 4 万违纪上限 / 回落止盈 8%)+ K4 安检(hard_cut 拦、avoid_flag 标)承担,不再由「不许碰」承担**;③ 这是 §2.3「生成域刻意含高弹、止损频率代价已在策略线审计定价」(2026-07-25)的自然延伸。**四处代码零硬编**——全部由 `cfg.forbid_high_elasticity` 驱动,拆墙不需改一行代码(已逐处核对);`v1.3` 保留在切换器白名单作**唯一合法回退目标**,回退后墙立刻回来。

### 2.3 盘后报告(每交易日 16:00,CLI/文件形态先行)

> ⚠ **V2.0.0 起本节大部分被取代**:候选 20 只 / 排序 / 候选卡 / 自选体检 → **篮子 + Tier + 篮子卡**,报告重构为**篮子日报**。对照见 **§2.8-A**,施工见 §五 V2-⑤⑥⑦⑬⑭。以下原文保留作历史拍板留痕。
>
> 定 16:00 是因 A 股有盘后交易,收盘数据 16:00 后稳定。

- **情绪仪表盘**:涨停家数 / 跌停家数 / 连板最高高度 / 炸板率 / 昨日涨停股今日平均溢价 → 输出**明日仓位额度**(满额 / 半额 / 休息)。
- **强势板块**:**加权不圈死**——板块只加分,全市场强势形态票均可入池;必须含**板块年龄因子**(启动第几天、涨停梯队扩缩、龙头是否掉队;启动早期加分、连续主线 4–5 天后衰减降权)。
- ~~**候选 20 只带评分**,每只**四件套:买点 / 止损 / 目标 / 证伪条件**~~(v1.5.0 起由三件套取代,见下条)。
  - ~~**前 10 只**过 **LLM 逻辑审判**(读新闻 / 题材 / 公告,判催化持续性,**有一票否决权**)~~(v1.5.0 起 20 只全覆盖)。
  - ~~**后 10 只**只给分数与形态标签,不耗 LLM~~(v1.5.0 起退役)。

> **⚠ 候选输出层变更拍板(2026-07-29 用户口述定案,需求 9,落地 §五 v1.5-①②③)——四件套 → 参考三件套,20 只全覆盖。** 承 §2.0 第〇原则(LLM 是参考,硬条款才是纪律):
> - **候选卡输出层 = 三件 LLM 参考件**:① **买入参考区间**(基于 60 日 K 线 + EOD 数据推"若明日买入,适合的价格区间";**唯一底线 = 数字必须落在明日涨跌停价区间内,出界不显示**;具体止损价 `0.95×` 并进这一行一笔带过)② **离场参考区间**(本轮上涨的压力位区间,**明示参考、非止盈线**;回落止盈 8% 纪律一字不动、仍是被动兜底)③ **明早证伪剧本**(面向 T−1 晚阅读的开盘行动指引,带分支;生成时把哨兵的结构化阈值喂给 LLM,使剧本与盘中自动警报同频)。**LLM 其他评语保持现状。**
> - **止损独立条目删除**(−5% 是章程常量,不再每卡说教);**哨兵的机械结构化证伪条款保留在水下**(`invalidation_spec`/`entry_spec` 仍是盘中自动警报的唯一判据源,**只是卡面不再展示原始条款**)。
> - **20 只全覆盖**:每票**或出三件套、或出不买理由**(~~前 10 只审、后 10 只不耗 LLM~~ 退役);**风险否决 → 三件套全无、只出不买理由,但票与信息卡保留**(机器不禁、人可复核)。
> - **今日计划拆两块**:**持仓股** / **候选列表(每日 20 只)**。
> - **语义红线 +1**:参考件必须标注**「参考、非指令」**,终选权在用户;原「排序 = 注意力优先级,不是收益预测」不变。

> **⚠ 候选语义变更拍板(2026-07-25 用户,需求 5,落地 §五 v1.3-③-C3)**:自 v1.3 起,**候选列表不再是「系统认为会涨的票」,改为「过完安检、值得用户花注意力的票」,终选权在用户**。生成源从 **K1 entry mask 退役**——改走「情报筛选管线」四步(板块拥挤度 top〔五板块常驻 + 当日暴起〕→ 全板块 MAIN/GEM/STAR 只过卫生线 + 趋势向上、**不套 K1 主板 only 与回调买点** → K4 安检〔`hard_cut` 拦 / `avoid_flag` 标,读 DB `K4.k4_advisory`〕→ 情报排序〔资金流强度 + 题材持续天数**反用**〕)。**生成域刻意含高弹板块**(贴用户实操,与 K1 哲学相反,用户知情拍板,止损频率代价已在策略线审计定价)。候选卡文案须跟上此语义(不再标「推荐买点」)。**§3.8 铁律「同码三跑道」已相应重述**(候选生成解耦、纪律核对仍同码)。~~候选 = 系统认为会涨的票(v1.3 前 K1 口径)~~
- ~~大盘 MA20 市场过滤器 = 争议项~~ → **已否决定案**(2026-07-20 用户拍板,与阶段 1 回测结论一致:回顾性分层相关真实,但做成实时闸门样本内外双双更差——滞后指标不可交易)。**市场择时职责归情绪仪表盘**(其阈值第一版为启发式、标注未回测,靠实盘归因迭代)。

> **⚠ 自选体检拍板(2026-07-21 用户,v1.1 新增报告节)**:16:35 报告新增「**自选体检**」节——对用户自选池(§2.4 v1.1)每只票**用同一套评分管线**跑当日评分 + 形态标签 + 纪律红绿灯(禁买线 / 票型黑名单 / ST / 板块限制核对)+ 形态触发买点条件的给完整四件套。**LLM 只审「当日状态较上一份报告发生变化的」∪「用户点名(pinned)的」**(控成本 / 控时长),其余确定性输出。自选体检**不改候选 20 评分逻辑、不进候选榜**,是独立一节(同码复用评分,只扩输入范围到自选池)。落地 §五 v1.1-C。

### 2.4 盘中哨兵(轮询免费实时源,1 分钟一拍)

> ⚠ **V2.0.0 改动两处、不动判定**:① 关注池组成换成「持仓 + T1/T2 篮子成员 + 相关板块 ETF/指数 + 昨日涨停」;② 推送白名单**六类 → 三级 × N kind**。**四哨兵判定逻辑一字不动**,新增篮子验证状态机与盘中存拍。对照见 **§2.8-A**,施工见 §五 V2-⑧⑪。
>
> **铁原则:盘中不产生任何新决策,只执行前晚计划,永不盘中推荐新票。**

1. **买点哨兵**:候选触达预设买点**且**确认条件成立(量能折算、站稳 VWAP)→ 进盘中看板。
2. **退潮哨兵**:盘中情绪恶化(炸板率飙升 / 跌停扩大 / 主线板块跳水)→「**今日计划作废、禁开新仓**」红色刹车。
3. **持仓哨兵**:持仓票放量跳水逼近止损线 / 触达目标区间 / 所属板块跳水预警。
4. **证伪哨兵**:候选分时走坏(低开不回 / 全天 VWAP 下方 / 量能异常 / 板块梯队瓦解,证伪条件**前晚写死**)→「剔除勿进」。**盘中主力资金流免费源不可靠,证伪只用价量结构**(VWAP / 量能折算 / 高低开)。

> **⚠ 推送路由拍板(2026-07-20 用户,推翻阶段 3「四哨兵各自推送」的默认设计)**:APNs 锁屏推送**只留两类**——① 每日 16:00 盘后报告就绪;② 退潮红色刹车(默认开、设置屏可关)。**其余哨兵事件(买点触发 / 证伪剔除 / 持仓预警)一律不推 APNs,只进 App「盘中看板」板块**(打开即看)。每类事件的推送开关在设置屏可配。理由:比行情速度必输同花顺,系统价值是把前晚计划执行成判决(看板),不是抢报新闻。上文四哨兵的**判定逻辑一字不改**,变的只是「触发后去哪」——看板(全部四类)vs APNs(仅退潮 + 报告)。阶段 3 已建的 `sentinel/channels.py` 通道抽象保留、阶段 4 新增 APNs 通道(复用 LinoN `push/apns.py`),**Bark 降为备用通道**。

> **⚠ 盘前校准 tick 拍板(2026-07-21 用户,v1.1 新增第 5 类哨兵动作 + 推送白名单扩到四类)**:交易日 **9:25:30** 加一次「**盘前校准 tick**」(集合竞价快照 9:25 后即含开盘价 / 竞价量)——**纯规则判定、零 LLM**:候选高开超阈→「买点已变形今日失效」;低开踩证伪线→「开盘即证伪预警」;竞价量能异常标注;持仓大幅低开→「止损条件单今日可能触发」。**9:26 一条汇总推送 + App 内明细**。这仍是 §2.4 铁原则的题中之义——**盘前不产新决策,只判前晚计划是否已被集合竞价作废**(执行层,不是选新票)。哨兵进程(FastAPI lifespan asyncio 单 unit)加盘前窗口分支,**现有 9:35 起 intraday 判逻辑一字不改**。
>
> **推送白名单(v1.1 四类、v1.2 扩五类、v1.3 扩六类,推翻更早「只两类」)**:① 16:35 报告就绪、② 退潮红色刹车、③ **9:26 盘前校准汇总(v1.1 新)**、④ **D5 时间退出(v1.1 新)**、⑤ **熔断提醒(v1.2 新,§2.1 第 7 条;默认开、与退潮刹车同级)**、⑥ **K4 持仓派发警报(v1.3-② 新,用户 2026-07-26 拍板独立 category `HOLDINGALERT` + 独立开关 `push_holding_alert`,默认开)**——各自独立开关(设置屏可配)、各自独立 APNs category。**D5 时间退出**是规则 v1 `hold=5` 时间退出纪律的**执行器**(此前无人触发 = SOP 最大裸奔点):D5 当天看板置顶 + 推送。**熔断提醒**是 §2.1 第 7 条熔断纪律的执行器(连续 3 笔止损 / 单日亏损 ≥4000 触发即推 + 客户端状态标记)。**K4 持仓派发警报**只推**强价量证据**命中(年线下涨停 / 放量大阳派发 / 换手 >10%);题材持续天数=概念板块成分弱证据,只进看板不推(守铁律「证伪只用价量结构」)。买点 / 证伪 / 普通警示仍只进看板不推 APNs(不变)。落地 §五 v1.1-A(盘前校准)/ v1.1-B(D5)/ v1.2-A2(熔断)/ v1.3-②(K4 派发警报)。
>
> **⚠ K4 持仓牌强警示推送(v1.3 需求 7;用户 2026-07-26 拍板 = 第六类,推翻 planner「复用 D5EXIT」默认)**:持仓牌每日重算的**强警示级别**(年线下涨停 / 放量大阳 = 派发警报、题材持续 ≥4 天、换手 >10%)推 APNs;**普通警示**(题材 2-3 天等)只进盘中看板。**用户拍板独立第六类推送**(不复用 D5EXIT):理由 = K4 派发警报说的是「你的票可能在被派发」,与 D5 的「持有到期了」是两回事,合并后关一个会连坐另一个 → **独立 category `HOLDINGALERT` + 独立开关 `push_holding_alert`(默认开)**;`notify.__all__` 白名单结构守护五→六、apns category 五→六、`app_settings` 幂等加列、`GET/PUT /settings/push` 契约扩六字段。落地 §五 v1.3-②。守铁律「证伪只用价量结构」:换手/年线下涨停/放量大阳=强价量证据(触发 APNs)、题材天数=概念板块成分弱证据标「参考」(**不单独触发强警示**)。

### 2.5 问询台(LLM 问询板块)

用户丢外部消息源的票进来 → 系统先跑**确定性材料**(同一评分管线跑分 + 纪律/硬线核对 + 板块年龄 + K4 安检)→ LLM **带工具调用**(实时取数 / 重算 / 联网搜索)自然语言回答。

> **⚠ 定位变更拍板(2026-07-27 用户,v1.3.3「拆墙」同批)——审判员 → 自由分析师。** 用户实测拿创业板票(300759 康龙化成)问,只得到一句「未通过硬性纪律核对,不予放行」,**问不出任何东西**;问询台的价值本该是"把牌摊开",却退化成一道闸。自 v1.3.3 起:
>
> - **硬栏杆全拆**:**任何票都走完整流程、拿到实质回答**,不再有「纪律不过 → 直接终止、不劳 LLM」的短路。
> - **裁决二值退役**:`不符合` / `初审通过进海选池` 两个枚举值**删除**。`verdict` 字段保留(客户端契约不破),但取值改成**纯描述性标注**`已分析` / `已分析·有风险提示` —— **它不是判决,不授权也不禁止任何操作**。
> - **纪律降级为警告标注**:真硬线(ST / 退市风险 / 流动性太差 / 次新 / 停牌)与 **K4 安检命中**(如「年线下涨停,K4 标为派发域」)仍在回答里**如实提示并解释含义**,**但一律不拦**;prompt 里明写「不要因此拒绝分析这只票」。
> - **软护栏(保留,但改成软形式)**:**不下「买 / 卖」指令** —— 可以把走势、逻辑、风险、赔率分析得很透,但不产出买卖指令。**软 = 只在 prompt 层约束**,刻意**不做**枚举强校验、**不做**输出后处理拦截(旧的"三重保险"连同二值裁决一并拆除)。理由(定案,勿改回):**LLM 的买卖判断没有回测支撑,不该塞进用户的决策链;分析归系统,扣扳机归用户。** 强校验换来的是把模型逼进模板腔(违背 §2.7),而真正的护栏本就是 §3.8「系统永不自动下单」这条铁律,不是在文本里 grep「买」字。
> - **「初审通过进海选池」退役**:问询台**不再自动写 `inquiry_pool`**。想让一只票进当晚报告,由用户在客户端**一键加自选**(「+自选」按钮早已在问询卡上,且不看裁决值)——自选池本就进当晚**自选体检** + 哨兵**关注池**,路径更短也更符合"终选权在用户"。`inquiry_pool` 表与报告侧消费逻辑(`load_pending_inquiry_codes`/`mark_inquiry_pool_consumed`)**保留不动**(向后兼容,空池 noop,历史待消费行仍正常消费),只是不再有自动写入方。
>
> ~~**裁决只有两种**:「不符合 + 依据」或「初审通过,进当晚海选池」~~(v1.3.3 退役)。**「永不产出『现在就买』」仍然成立**,只是实现方式由"代码级二值裁决 + 枚举 + 强校验"改为"prompt 层软护栏 + §3.8 系统永不下单"。

> **拍板落地(2026-07-20,部分被上条取代)**:阶段 4 把问询台做成 App「问询台」板块(自由对话体聊天,继承 LinoN `/chat` 无状态、客户端持有上下文的对话工程)。~~「初审通过进当晚海选池」= 落 `inquiry_pool` 表(当日),`report.py` 生成当晚报告时把海选池的票强制纳入候选评分 universe~~(v1.3.3 退役,改一键加自选;表与消费侧保留)。~~裁决二值 = 硬约束,system prompt guardrail + verdict 枚举只两值双保险~~(v1.3.3 退役,见上条)。

### 2.6 回测引擎(系统地基,带笼子的策略进化)

- **同一套信号代码三跑道**:喂历史 = 回测、喂今日 = 报告、喂单票 = 问询台。
- **回测必须正确处理**:前视偏差、T+1、涨停买不进 / 跌停卖不出、停牌、复权(前复权)、滑点、手续费。
- **版本号双线规约(2026-07-22 用户定)**:**系统版本走 `v` 字头**(v1.1、v1.2……,产品/工程升级);**策略版本走 `K` 字头整数**(K1、K2……,`strategy_versions` 表标签,回测过堂的大脑)。两线独立演进互不挂钩;现役策略 = **K1**(原名 v1,2026-07-22 双端 DB 已改名)。任何文档/报告/客户端展示的策略号一律 K 字头。
- **策略进化带笼子**:
  - 按月 / 季调参;任何调整须过**同一道门**——回测 + walk-forward **样本外跑赢现役版本**才可上线。
  - 大脑带**版本号 + 变更日志 + 实盘表现按版本归因**。
  - **调参最终批准权在用户。**

### 2.7 LLM 输出风格(全系统硬约束)

- 一律**自由对话体、自然叙述**;**禁止结构化模板卡 / 枚举腔**(用户明确厌恶 LinoN 三轴卡)。
- 语气自然但**立场铁**:纪律不过,不放行。
- ⚠ **V2 附注(2026-08-02)**:第二句「纪律不过,不放行」的**语气**保留,但**问询台自 v1.3.3 起已是自由分析师、不拦票**(§2.5),V2 延续;**篮子卡与剧本同理 —— 参考件不做闸门**。禁的是模板腔,不是禁止一个机器可读收尾标签 / 一段供夹逼校验的 JSON(`llm/judge.py` 模块头已论证)。

### 2.8 V2.0.0 语义换血总表(2026-08-02 立项;**本节凌驾 §2.2~§2.5 中被标注取代的条款**)

> **读法**:§2.1(纪律章程)与 §2.0(第〇原则)**继续全文有效**;§2.2~§2.6 中被下表标「**取代**」的条款,自 V2.0.0 起以本节为准,**原文一律保留作历史拍板留痕,不删**(照 §2.1 / §2.3 既有的删除线 + 附注体例)。施工细节全部在 §五 V2.0.0。

#### A. 逐条换血对照

| V1 条款 | V2.0.0 处置 | 落地块 |
|---|---|---|
| §2.3 **候选 20 只单票**(含五常驻板块保底 + 竞争位) | **取代** → **股票篮子**:1–3 只/篮,必须说明共同驱动与「为何是这几只」;**仅历史相关性不足以成篮**;同票多篮时主归属唯一(行业闸 lift 最高者) | ⑤⑦⑬-1 |
| §2.3 **候选排序(排序键三级)** | **取代** → **Tier 分层**:T1≤2 / T2≤5 / T3≤10,**上限非配额,允许 T1 为空**;机械分定档、LLM 只在同档内微调 | ⑥ |
| §2.3 **候选卡参考三件套 / 老四件套** | **取代** → **篮子卡 11 项**(蓝图 4.6);夹逼 / 冻结 / 口径指纹 / disclaimer 体例**原样移交** | ⑦⑬-3⑬-6 |
| §2.3 **自选体检节** | **删除**(裁定 #9:自选池 + 同花顺对账直接删) | ⑬-11 |
| §2.4 **哨兵关注池 = 候选 + 持仓 + 昨日涨停** | **取代** → **持仓 + T1/T2 篮子成员 + 相关板块 ETF/指数 + 昨日涨停**;仍是**代理样本不是全市场** | ⑧-A |
| §2.4 **四哨兵判定逻辑** | **不动**(纪律外壳一行不动);新增**篮子验证状态机**(四态)与**四项持仓监测**,与纪律分支并列不替代 | ⑧-C⑪-A |
| §2.4 **推送白名单六类** | **取代** → **三级 × N kind**(立即 / 重要不紧急 / 盘后汇总;开关按 `kind` 配)。「新增推送须用户拍板」这条纪律**保留** | ⑪-B |
| §2.5 **问询台 = 自由分析师** | **保留**;但「+自选」写回通道随自选池删除而消失 → 问询台成**纯分析入口** | ⑬-11 |
| §2.6 **同码三跑道 / 情报筛选管线** | **取代** → 选股主链 = **扫描 → 聚合 → Tier → 卡 → 验证 → 复盘**,可配部分读**现役策略包**;§3.8-(a)(纪律与回测同码、参数单一源)**不变** | ③④⑤⑥ |
| §2.6 **策略进化门禁** | **扩** → 章程走 `activate_charter.py`(不变)+ **选股策略包走 `activate_pack.py`**;两条版本线、两张表、两套激活流程,**互不混用** | ③ |
| §2.1 **纪律章程 v0.1 全文** | **一行不动**(现役 `v1.3.3`);红黄牌**降格为风险信息与模型特征,不等同禁买**(蓝图 7.2) | 全域 |
| §2.3 **决策日志强制表单(论点五必填 + 追价上限)** | **取代** → **开仓自动快照(`entry_snapshots`)+ 用户字段全部可选**;`decision_log` 停写留档 | ⑩-C |

#### B. V2 新增的产品级设计共识(V1 无对应条款)

1. **事实 / 用户行为 / 模型判断三类分存,互不覆盖**(蓝图 2.4):事实 = parquet 分区 + 冻结快照表(只增不改);用户行为 = `user_actions`(只追加);模型判断 = 篮子 / 卡 / Tier / 复盘 / 画像(**新判断 = 新版本,不回写**)。
2. **选股与持仓职责独立、信息互通**(蓝图 2.3):可互读、可追加,**不得回头修改对方已冻结的历史信息**;选股日报可追加用户行为观察,**但不得因此修改原始 Tier 与篮子判断**。
3. **外松内紧**(蓝图 2.1):只对可能伤害账户生存的事项保留硬约束(单笔与账户风险上限、券商止损条件单状态、连亏与单日亏损熔断、停牌退市重大公告行情失效);其余改为提示 / 自动记录 / 可选输入,**不再用长表单阻断交易**。
4. **不吸收的做法(蓝图 7.3,写死为禁令)**:满仓一击;跌停后用「格局」代替证伪;把一次成功修复机械复制到其他股票;为增强选股而无节制叠加指标;用用户偏好直接替代市场证据;**因持仓盈亏回头修改原始选股结论**。
5. **画像初期不得反向影响客观 Tier**(蓝图 4.4);解禁路径只能是展示层个性化,且须另行立项。

#### C. 第〇原则四锁的 V2 精确表述(**必读 —— V1 原文逐字照搬会与 V2 直接冲突**)

§2.0 的四锁原文是「参考件不进排序键 / 不进哨兵 / 不进推送 / 不改候选去留」。V2 里有两处**表面冲突**,必须按下面的精确表述执行,**不许各自理解**:

1. **「不进排序」→ 精确为「不进机械分」**。V2 明确允许 **LLM 在同档内微调排序**(架构裁定,第〇原则的 Tier 变体),但:**机械分五维的输入白名单 `_TIER_SCORE_INPUTS` 里不得出现任何 LLM 产出字段**;LLM 只能改**档内序**、**不得跨档**;`rank_mech` 与最终 `rank_in_tier` **两个都落 `tier_history`**,微调理由必须留痕。
2. **「不进哨兵」→ 精确为两条**:(a)**LLM 产出的自由文本与数字一律不进哨兵判据** —— 哨兵只读**机械生成或过了机械闸的结构化 spec**(`verification_spec` / `invalidation_spec`)与现役章程 config;(b)**关注池成员选择受篮子影响 = 注意力分配,不是纪律判定** —— 盯谁可以由篮子决定,盯到之后每只票的判定阈值仍只来自章程与机械 spec。
   > ⚠ **2(b) 的适用边界(2026-08-03 收紧,V2 review 判定线 🟡-4 打出来的;这是通用规则,不只针对退潮刹车)**:**2(b) 只覆盖「盯谁」,⛔ 不覆盖任何「聚合纪律触发器」的样本组成。**
   > **为什么必须分开**:2(b) 能成立,靠的是「**每只票的判定阈值仍只来自章程与机械 spec**」这句 —— 对**逐票判定**,盯谁与判什么是两件事,换样本不改判据。但对一个**聚合量**(如「主线板块均值跌幅 ≤ 阈值」),**样本组成本身就是判据的输入** —— 换一批票平均,结论直接变;「阈值没动」是真的,但不相干。**⛔ 凡进入纪律触发器的聚合量,其样本必须是机械派生的**,不得由 LLM 的选择塑形(哪怕只是间接塑形)。
   > **判据(自查用)**:问一句「**换一批成员,这个数会不会变**?」—— 会变 = 样本即判据 = 不得沾 LLM;不会变(纯逐票比阈值)= 才落在 2(b) 的豁免里。
3. **「不进推送」→ 精确为「判定动作永远机械;数值来源分两类」(2026-08-03 改写,V2 review 判定线 🟡-3 收口;原文「触发条件永远是机械的」在 `take_profit` 上与用户拍板正面打架,故在此收口而非留字面冲突)**:
   - **默认(无豁免)**:参考件本身不推 APNs;推送由机械事件触发,文案里可以引用参考件的结论,**触发所用的数值也必须是机械的**。
   - **记名豁免(目前有且仅有一个)**:**`take_profit` kind 的触达告知**(2026-08-03 用户拍板)。参考件的数值可以作为**触发位置**,但**四条前提必须同时成立,缺一即豁免失效**:
     1. **不改变任何纪律判定** —— 止损 / 回落止盈 / 时间退出 / 仓位 / 熔断**一字不动**,不产生任何持仓动作;
     2. **该数值已过机械 sanity 闸**(⑪-D 的两道),未过 → **该票此 kind 不武装**;
     3. **纯告知型文案**,禁指令词,必须点明「这是你计划里的参考位、不是止盈线,纪律仍是回落止盈」;
     4. **用户可关**(per-kind 开关)。
   - ⛔ **豁免不外延**:买入参考区间、明早证伪剧本、审判评语、问询台回答、篮子 `falsified` —— **一律不得**据此接推送或判据。要再开一个,走 `basket_falsified` 那条路(用户拍板 **且** 三处红线同步改)。
4. **「不改去留」→ 精确为「不做闸门」**:LLM 判风险大 → 只影响卡面内容(不给区间、只给不买理由),**篮子与成员照留**;`hard_cut` 之类的机械闸仍由 K4 advisory 与卫生线承担,**不由 LLM 承担**。
5. **不变的两条**:参考件**必须自带「参考、非指令」标注**;参考件**必须落库**并可与实际走势 / 成交对拍(参谋成绩单)。

---

## 三、技术选型(在此定死,不留给 build 猜)

### 3.1 语言与运行环境

- **Python 3.11+**(本地研究期,不受 LinoN 的 3.9 系统解释器约束;polars / pyarrow 在 3.11 上性能与内存更优)。阶段 0–3 全本地跑。
- 依赖装库走**阿里云 PyPI 镜像**(继承 LinoN 坑:直连公网 PyPI 易超时)。虚拟环境 `.venv`,`requirements.txt` 钉死版本。

### 3.2 数据源

- **主源 TuShare 600 元档 = 6000 积分**(token 从 `.env` 的 `TUSHARE_TOKEN` 读;权限已于 2026-07-19 按官网权限对照表逐项确认,限频 **500 次/分钟**)。
  - **可用**:`daily`(日线)、`daily_basic`(换手 / 量比 / 市值 / PE-PB)、`adj_factor`(复权因子)、`index_daily`(指数日线)、`stock_basic`(代码→行业)、`namechange`(ST 状态历史)、`trade_cal`(交易日历)、`moneyflow_dc`(东财个股资金流,`net_amount` = 主力净额,万元)、**`top_list`(龙虎榜)**、**概念板块和成分**(板块层 / 板块年龄因子的数据源)、融资融券、财务三大报表、沪港通列表、**`stk_holdertrade`(股东增减持,2026-07-26 v1.3-③-C4 探活新确认,2000 积分档,`in_de`=IN增持/DE减持结构化字段,消息面扫描「减持」类数据源,见 §五 v1.3-③-C4)**。
  - **不可用(档位不够,方案已绕开)**:**`limit_list`(涨停榜单,15000 积分)→ 涨跌停全部自算**(见 0.4b 衍生表:涨停价 = `round(pre_close×(1+幅度),2)`,幅度按板块 10%/20%/ST 5%/北交所 30%,`close==涨停价` 判涨停、`high==涨停价且close<涨停价` 判炸板、连续涨停日计连板;新股上市首日无涨跌幅按 `stock_basic.list_date` 剔除);筹码分布 / 量化因子(8000 积分,不依赖);游资数据 / 个股及行业热板(15000 积分,不依赖);**新闻资讯为单独权限 1000 元/年,未购**(见 §8 决策项);**`anns_d`(公告接口)同样为单独权限 1000 元/年、未购**(2026-07-26 v1.3-③-C4 真实 token 探活确认:调用返回「无接口访问权限」,与新闻资讯是 TuShare 两个不同的独立付费产品——立案 / 暴雷 / 监管三类消息面扫描因此走 LLM 联网搜索兜底,不依赖 `anns_d`)。
  - **无分钟线。**
- **盘中实时源(免费)**:新浪 `hq.sinajs.cn`(主)/ 腾讯 `qt.gtimg.cn`(备)。仅供盘中哨兵,分钟级轮询。解析坑见 §3.7。
- **TuShare 调用姿势铁律**:`ts.pro_api(token)` **直传**,**禁用 `ts.set_token()`**(会往家目录写缓存,继承 LinoN 坑 5)。全市场批量接口只带 `trade_date`(不带 `ts_code`)一次返回全市场。限频:分批 + sleep(阶段 0 落地脚本按 TuShare 每分钟调用上限退避)。

### 3.3 存储(选型论证)

**规模估算**:A 股约 5400 只 × 2020-至今约 1600 交易日 ≈ **850 万行/表**;`daily` + `daily_basic` + `adj_factor` + `moneyflow_dc` 四张大表合计约 **3000–4000 万行** numeric;自算涨跌停衍生表(0.4b)只存涨停/炸板/连板行,约百万行;`index_daily` 极小。回测「喂历史」是**按交易日顺序迭代、每日取全市场横截面**的列式扫描负载。

**选型结论:Parquet(行情大表)+ SQLite(元数据 / 业务台账)混合。**

- **Parquet + polars(回测主力)**:`daily / daily_basic / adj_factor / index_daily / moneyflow_dc / limit_derived(自算)` 落 `data/parquet/<表>/year=YYYY/…`,**按年分区**。列存 + snappy 压缩后全量预计 **< 2 GB**;polars `scan_parquet` 惰性扫描 + 谓词下推,按 `trade_date` 切片近乎零拷贝,反复回测调参内存友好。→ **回测数据层用 polars lazy,选它就是为吃这个负载。**
- **SQLite(`data/neckline.db`)**:交易日历、股票 / 行业元数据、回测结果与净值曲线、盘后报告与候选、持仓审计、纪律台账、大脑版本与变更日志、问询会话。这些要事务、点查、关系查询,且与 LinoN 工程一致、后期客户端好接。
- **pandas 仅用于边缘衔接**:TuShare 返回的是 pandas DataFrame,落地时用 pandas 承接后转 polars / 写 Parquet;回测核心不碰 pandas。
- **为何不全 SQLite**:千万行横截面反复全表扫描,行存聚合慢、库文件臃肿。**为何不全 Parquet**:业务台账要事务与点更新,Parquet 不擅长。故混合,各取所长。

### 3.4 LLM

> ⚠ **V2.0.0 起本节的「GLM/Kimi 枚举」被取代为 Provider 自填制 + 双 Agent 分工**(裁定 #2),见 **§3.10-B**;降级链 / 短读超时重试 / 带搜索 90s 读超时 / `MockTransport` 免联网单测等**工程姿势一字不变**。以下原文保留作历史拍板留痕。

- **多供应商可切换(2026-07-19 用户拍板,不用 DeepSeek)**:候选 **GLM 5.2 / Kimi K3**(两家 API 均原生带联网搜索工具,正好覆盖 2.4 消息面方案)。设计约束:① `llm/` 层做**供应商抽象**(统一 chat + 工具调用 + 搜索接口,provider 实现可插拔);② key 与 provider 选择从配置读(研究期 `.env`:`LLM_PROVIDER` / `LLM_API_KEY`;**阶段 4 客户端里必须有「填 API key + 切换供应商」的设置入口**,运行时可切不重启)。**阶段 4 拍板(2026-07-20)**:App 可改 key/provider → 走 `PUT /settings/llm` 落服务端 `app_settings` DB 单行(**高危区**:key 服务端存取,DB 文件 600、gitignored、rsync 永不同步覆盖);`get_provider()` 解析优先级改为 **DB 覆盖 → `.env` 兜底**,每次调用现读故运行时生效;③ key 用户后续自填,**缺 key 时全链路优雅降级不崩**(继承 LinoN 降级链思想)。
- 继承 LinoN 的**降级链**(缺 key / 超时 / 非 200 / 非法 JSON → 优雅降级占位,全链路不崩)与**短读超时 + 每次全新连接重试**(`_READ_TIMEOUT=12` / `_CONNECT_TIMEOUT=6` / `_MAX_ATTEMPTS=3`,治 EdgeOne CDN 偶发单连接卡死)。
- **对话工程**:问询台的工具调用(实时取数 / 重算)沿用 LinoN v1.2.1 `/chat` 的姿势重写。

### 3.5 客户端载体(**已拍板 = 方案 A**,2026-07-20)

阶段 0–3 **完全不需要客户端**,CLI + 本地文件(markdown / HTML 报告)即可跑通全部研究与报告逻辑。载体决策推迟到阶段 4 前:

| 选项 | 优点 | 代价 |
|---|---|---|
| A. 复用 LinoN SwiftUI 双端 + APNs | 工程资产在、锁屏推送体验最好 | 最重;要维护 Xcode / 签名 / 真机 |
| B. Bark(推送)+ 轻量 Web(报告 / 问询台) | 最省;盘中哨兵推送即插即用,报告 / 问询台用浏览器看 | 推送样式简单,无原生交互 |
| C. 纯 Web(FastAPI + 简单前端) | 单端维护、跨设备 | 盘中推送需另配通道(可 + Bark) |

~~**推荐 B**:盘中哨兵推送走 Bark + 报告/问询台走轻量 Web。~~ → **已否决**。

> **⚠ 客户端载体拍板(2026-07-20 用户定案 = 方案 A)**:复用 LinoN 的 SwiftUI **iOS + macOS 双端**工程底子 + **APNs 推送**;**新 App、新 Bundle ID `top.linotsai.neckline`**(不改 LinoN 旧 App)。上表 A/B/C 保留作决策留痕。选 A 理由:锁屏推送体验最好、LinoN 工程资产(xcodegen 双端 / DesignTokens / APIClient / PushManager / 平台分叉)可整块搬、绿涨红跌沿用。**信息架构全新**(iPhone 四板块 = 今日计划 / 盘中看板 / 问询台 / 设置;macOS 四板块 + 周复盘工作台),非照搬 LinoN 今日台。复用清单与坑吸收见 §五 阶段 4C。

配色偏好(阶段 4 落地时遵循):用户沿用 LinoN 的**绿涨红跌**(与 A 股本地相反,是用户明确选择,勿「纠正」)。

### 3.6 部署

> ⚠ **V2.0.0 起本节被取代**:hz 单机(1.6G 内存硬约束)→ **100% 更换更强云服务器 + 晚间管线多段化**(裁定 #3),见 **§3.10-D** 与 §五 V2-⑯。以下原文保留作历史拍板与老机事实留痕(老机在用户完成换包前仍在跑)。

- **研究期(阶段 0–3)本地跑。**
- ~~盘中哨兵上不上 ECS = 后期决策项。~~ → **已拍板(2026-07-20)= 上 hz ECS,复用 LinoN 基建**。

> **⚠ 部署拍板(2026-07-20 用户,阶段 4 落地)**:后端 + 哨兵跑 **hz 杭州云 ECS**(`deploy@118.178.122.194`),照搬 LinoN 的 **FastAPI + systemd + nginx 反代**模式。复用 LinoN 资产:nginx 站点 + 证书 **`ln.linotsai.top`**(证书 ECDSA 到 2026-09-20 cron 自动续期健康,DNS 已在,**零新增用户网页操作**;域名 `ln` 语义留痕于 LinoN 是可接受的表面不符,不值得为改名新开子域 + 新证书——后者还要碰本机 certbot 已知问题,见 `hz_info.md §13`)、APNs `.p8`(Key ID `Q963AP3VY8` / Team `HX73DFL88G`,**账号级密钥可直接给新 Bundle ID 用**,APNs topic 换成新 Bundle ID 即可)。
>
> **端口/账户/目录**:Neckline 用**独立端口 8002**(⚠ 拍板文字说的「8001 空闲」已过时——`hz_info.md` 2026-07-20 记 LinoN 07-20 晨已恢复运行、仍占 8001 作记录工具,**8001 未空闲**;Neckline 用 8002 与 LinoN 共存,不抢端口)、独立 nologin 用户 `neckline`、独立目录 `/opt/neckline`(`deploy:neckline` setgid 2770,`.env`/`.p8`/`neckline.db` 600 `neckline:neckline`)。**接班切换(阶段 4 部署上线并联调通过后)**:nginx `ln.linotsai.top` upstream 由 `127.0.0.1:8001` 切到 `:8002` → `linon.service` stop + disable(LinoN 退役,兑现 `hz_info.md` 决策)→ 视需归档 `linon.db` / 清理旧站点。
>
> **内存硬约束(hz 仅 1.6G + 2G swap,还跑着 lf / fiscal / 主页 / postgres,v1.4.1 实测 used ~853M / available ~759M)是本阶段第一约束**。polars 全市场重活(backfill / 报告生成)能否在 ECS 上跑,**必须实测门禁定生死**(见 §五 阶段 4B),两套架构方案都写清,以实测定,回退路径 = Mac 定时落数 + 报告生成 → rsync 产物上云,ECS 只管 API + 哨兵轮询(轻量,关注池 ≤200 只)+ APNs。**哨兵不另起进程**——折进 FastAPI 单 unit 的 lifespan asyncio 任务(照搬 LinoN 单 unit 省内存),`scripts/sentinel.py` 独立脚本留本地。

### 3.7 继承资产(从 LinoN 搬什么,搬前必读其 CLAUDE.md 对应节)

代码可搬可重写,由 build 定;但**下列已踩平的坑必须吸收**(权威原文见 `/Users/linotsai/Lino/LinoN/CLAUDE.md`):

- **`tushare_client`**(LinoN `backend/app/data/tushare_client.py`):`TushareResult{ok,data,reason}` 永不抛异常的封装;`pro_api(token)` 直传;字段单位表——`daily.amount`=千元、`daily.vol`=手;`daily_basic.total_mv`=万元;`moneyflow_dc.net_amount`=万元(= 超大单 + 大单,东财主力口径,与用户同花顺一致)。**Neckline 需新增全市场批量拉取 + 落 Parquet。**
- **交易日历**(LinoN `app/calendar/`):静态兜底表(官方休市日 + 调休补班周末股市仍休)+ `trade_cal` 比对告警。**Neckline 需把日历从 2020 起补全**(回测六年)。注意包名勿与标准库 `calendar` 冲突(LinoN 坑:包内绝对导入)。
- **实时源解析**(LinoN `app/data/realtime.py`):新浪**必带 `Referer: https://finance.sina.com.cn`**(否则 `Kinsoku jikou desu!` 无数据)、GBK 解码、volume 股→手;腾讯 GBK、volume=手、amount=万元、bid/ask「价先量后」(**与新浪相反,易写反**)。归一目标:`volume` 单位手、`amount` 单位元。
- **盘中 VWAP 纯函数**(LinoN `app/data/intraday.py`):`VWAP = amount/(volume×100)`(元/股),不自拉价、不联网。
- **LLM 层工程姿势**(LinoN `app/llm/deepseek.py`,供应商换了但姿势保留):降级链 + 短读超时重试 + `MockTransport` 可注入免联网单测。
- **黑名单口径**(LinoN 教训):按**板块整段正则** `^(30|688|689|8|4|920)` 挡创业板 / 科创 / 北交所,**勿枚举精确子段**(会漏新增子段);白酒用 `stock_basic.industry` 精确归类,非名称关键词。

### 3.8 铁律(全项目硬约束,build 逐条守)

- **token / key 绝不入 tracked 文件**:一律从 `.env` 读;`.env` 已在 `.gitignore`。**scratchpad 里的归因脚本 `entry_profile.py` 硬编了一个 TuShare token —— 严禁复制进仓库,重写时一律从 `.env` 读。**
- **系统永不自动下单 / 撤单 / 改止损**;一切成交由人 + 券商执行。
- **同码 = 纪律与回测同码,情报筛选独立(v1.3 重述,取代旧「同码三跑道」)**:旧表述「回测/报告/问询共用同一套信号代码」在 v1.3 需求 5(K1 entry mask 退役)后与候选生成直接冲突,故拆成两条**同时成立**的表述——
  - **(a) 纪律 / 退出规则的参数单一源 + 回测与实盘同码(不变,是「减损纪律系统」的地基)**:止损 / 回落止盈 / hold 两档 / 仓位 / 熔断阈值各有唯一源;退出规则的模拟(回测引擎 `strategy/momentum.py` + `backtest/`)与实盘哨兵读同一现役 config,**回测口径 = 实盘口径**,不得各写一份(否则报告与回测漂移)。**问询台 / 自选体检的纪律判定仍与报告同码**(选股域 `research/panel.py::base_universe_expr` + config 禁买过滤,同一个 `report/watchlist_check.py::discipline_checks`,见 §五 v1.3-⑤)。**⚠ v1.3.3 起「同码」只到判定为止,消费方式两处刻意不同**:自选体检把命中项当**红灯**(`green_light=False`),问询台把同一批命中项当**警告标注**(提示不拦,§2.5 自由分析师)——**同一份判定、两种展示**,是刻意取舍,不是漂移;禁止任何一方另起一份阈值。
  - **(b) 候选生成自 v1.3 起改为「情报筛选管线」,与回测信号解耦**:候选生成(拥挤度 → 卫生线 → K4 安检 → 情报排序)**不再套 K1 entry mask、不再声称是回测过的 alpha 信号**——它输出「值得关注的票」而非「会涨的票」,终选权在用户(三场战役判死「机器自动出信号」)。**故不要求(也不可能)与回测同码**;回测引擎仍是纪律参数的过堂载体与「减损系统」验证地基,与情报管线解耦。落地 §五 v1.3-③-C3。**⚠ V2.0.0 附注**:情报筛选管线自 V2 起由**篮子引擎**(扫描 → 聚合 → Tier → 卡)取代,可配部分改读**现役策略包**;本条 (a)(b) 的**分工原则一字不变** —— 纪律与回测仍同码同参数源,选股链仍与回测信号解耦、仍输出「值得关注」而非「会涨」。见 §2.8-A 与 §五 V2-③④⑤⑥。
- **规则常量单一源**:-5.0 止损、仓位上限、各阈值只在一处定义,禁止漂移(继承 LinoN 教训)。
- **无前视偏差**:回测第 T 日任何计算都不得读到 > T 的数据;数据访问层强制截断 + 单测守护。
- **LLM 一律自由对话体**,禁模板卡。

### 3.9 建议仓库布局(build 可微调,大方向照此)

```
Neckline/
  .env                      # gitignored,TUSHARE_TOKEN / LLM_PROVIDER / LLM_API_KEY
  .gitignore                # 已含 data/*.db、data/parquet/
  PROJECT_PLAN.md           # 本文件,唯一权威
  requirements.txt
  neckline/                 # 主包
    config/                 # 读 .env、路径常量
    data/                   # tushare_client / realtime / intraday / 数据访问层(polars)
    calendar/               # 交易日历(勿撞标准库名)
    backtest/               # 引擎、撮合、组合、walk-forward
    strategy/               # 母战法信号(同码三跑道的源)、大脑版本
    report/                 # 盘后报告管线(情绪仪表盘 / 板块 / 候选评分)
    sentinel/               # 盘中四哨兵
    llm/                    # 供应商抽象(GLM/Kimi 可插拔)/ 深判 / 问询台对话与工具
    review/                 # 周复盘 / 违纪对账
  scripts/                  # 落地脚本、每日增量、回测 CLI、报告 CLI
  data/                     # Parquet + SQLite(gitignored)
  archive/                  # 历史 plan 全文、超长记录
  tests/
```

### 3.10 V2.0.0 技术选型增补(2026-08-02 立项;在此定死,不留给 build 猜)

> 只写 V1 之上的**增量与变更**;未提及的一律沿用 §3.1~§3.9。

**A. 存储与数据层**
- 存储路线**不变**(Parquet + polars 跑行情大表 / SQLite 跑元数据与业务台账)。V2 新增 **18 张 SQLite 表 + 2 张 parquet 表**,清单与 DDL 要点在 §五 V2-① 定死。
- **`intraday_ticks` / `auction_snapshots` 走 parquet**(按日分区,`write_table_day` 铁律),不进 SQLite:它们是「只增不改的高频事实」,量级 ≈ 关注池 200 只 × 240 分钟/日,列存 + 按日分区最合身。**必须同步进 `_VALID_TABLES` + `TABLE_FLOAT_COLS` 声明**。
- **盘中存拍落盘时机 = 内存累计 + 15:05 一次性落盘**(§五 **D4 已拍板**);理由:`write_table_day` 的语义是「一日一文件整段替换」,逐分钟 append 会反复重写整日文件并拖累哨兵主循环。
- **分时数据双轨**(裁定 #1):自给存拍从 V2 上线日起积累;用户外源方案落定后**走同一张表回补**(`source` 列区分),复盘引擎无感切换。

**B. LLM 层**
- ~~多供应商可切换 = GLM / Kimi **枚举**(§3.4)~~ → **取代:Provider 自填制**。`llm_providers` 注册表(name / base_url / model / api_key / `has_web_search` / `search_engine` / enabled),**任意 OpenAI 兼容端点可配**,App 设置屏增删改;`openai_compat` 仍是唯一传输底座;GLM / Kimi 两个类降级为**预置模板行**。
- **任务 → Provider 路由表**(`app_settings.llm_task_routes` JSON),缺路由回退 `llm_default_provider`。**默认双 Agent 分工**(裁定 #2):**GLM = 检索型任务**(联网检索目前只有它这一条通道)、**DeepSeek = 推理型任务**;两段式流水(检索产物带来源+日期 → 喂推理),**单侧故障单侧降级并诚实披露**。
- **预算三本账**(检索 / 推理 / 复盘)独立不合并;**降级次序定死**:先丢 T3 简评 → 再丢 T2 复盘细节 → **绝不牺牲篮子卡冻结与纪律外壳**。
- **`prompt_context`(日期锚 + 时效纪律 + 检索词年份引导)对所有 Provider 一律注入**,唯一实现不许有第二份。
- ⚠ **`has_web_search=0` 的 provider 一律不发 `tools` / `search_query`**(不给上游不认识的参数 —— v1.3.4 案底:会 `ok=True` 静默返 0 条)。

**C. 选股引擎与策略包**
- 新包 `neckline/selection/`(`engine_api.py` / `primitives.py` / `pack.py` / `aggregate.py`)+ `neckline/scan/`(三张预计算表)+ `neckline/eval/`(评价与判分)+ `neckline/profile/`(画像)。
- **策略包 = 声明式配置包,不是代码插件**(§12.1 定案,勿重开):包里只装参数与规则声明,执行引擎永远住系统线仓库。原语白名单制,**特征只能来自预计算表**;`engine_api_version` 兼容不过**拒绝激活、fail loud**。
- **判分引擎唯一源下沉**:`research/h9_exit_reform.py::_sim_one` + `SLIP` / `BROKER` 常量搬进 `neckline/eval/exit_sim.py`,research 三处改 import 它(**生产不许 import `research/`**,方向恒为 research → neckline),搬迁前后**逐位对拍单测**锁死。

**D. 部署与运行环境(取代 §3.6 的 hz 单机方案)**
- ~~后端 + 哨兵跑 hz 杭州云 ECS(1.6G 内存硬约束)~~ → **取代:100% 更换更强云服务器**(裁定 #3,部署前置项)。建议规格 ≥ 4 vCPU / 8 G / 100 G SSD(§五 D3,用户可推翻);属主纪律、`.env` / `.p8` / `neckline.db` 600、`sync_code.sh` / `sync_data.sh` 体例**原样复刻**。
- **晚间管线多段化**:扫描 / 聚合+Tier+卡+报告 / 复盘+评价 拆成**三个 oneshot 服务串行**、各自 `MemoryMax`;**重活一律不进常驻 `neckline.service`**(它与盘中哨兵同进程,`MemoryHigh` 先节流 = 卡死不报错)。
- **域名与割接(§五 D2 = A 路,2026-08-02 用户拍板定死)**:新机挂新子域 **`nk.linotsai.top`**(**DNS 解析由用户自己做**),老机 `ln.linotsai.top` 原样服务到用户完成 ⑰ 双端换包 → **老 App 打老机、新 App 打新机不交叉,契约一次性换血、不留过渡键**。~~沿用老域名的 B 路~~ **已否决,不是选项**(§五 ⑭-D)。
- **P0-23 纪律平移**:所有新批算必须 **EOD 预计算落表、在线只读**,且**在新机上单独计时 + 量峰值后才准进主链**;探针只在收盘后隔离串行跑,`load > 4` 立即停手。

---

## 四、当前状态

**2026-08-03 · 🏗️ V2.0.0 施工中 —— V2-①→⑮ 全部完工,只剩【门禁】用户判定点 + ⑯⑰ 两个尾声块**。①–⑭ 详见各块「完工记录」。**⑮ 客户端双端改版完工:D8 信息架构落地 + 契约欠账清单 6 组全部还清 + 版号三方同批升 2.0.0** —— iPhone 四板块换成 **今日篮子 / 持仓 / 问询台 / 设置**(macOS 同四板块 + 周复盘工作台);V1 的「盘中看板」不再是 tab,**内容一条没删**、并入持仓板块成一节(80/15/5 里盘中动态本就是为解释持仓服务的)。全量测试 **Python 2890 过 + 2 skip**(两条 skip 是需 `NECKLINE_RUN_6Y=1` 的六年真回测;⑭ 时 4 skip 里那两条冻结快照 DTO 闸**已转跑**)、**Swift 179 过 + 12 skip**(skip = 未起 dev 后端的真实联调冒烟)、**iOS/macOS × Debug/Release 四路 `xcodebuild` 全 BUILD SUCCEEDED**。**哨兵纪律阈值自 ⑧ 起一行未动;现役章程恒 `v1.3.3`,V2 全程未新建章程行、未跑 `activate_charter.py`**。

- **V2-⑮ 完工内容**(详见 §五 V2-⑮ 段尾「完工记录」,含**六处设计判断**):`Models.swift` 候选族五个类型退役 → 篮子族 8 个 + 计划/快照/画像/包/评价 5 个 + 提醒族 4 个 + Provider/推送族 4 个 DTO;`APIClient` 删五处「打向已删端点」的活调用(含**采集明文 key 打进空洞**的那条)、接 V2 新端点、`mapReason` 补八个 case 并把 409/422 也接进来;新建 `BasketDailyView`/`BasketCardView`/`PositionsView`,重写 `SettingsView`(Provider 注册表 + 任务路由 + **按 kind 动态渲染**的推送开关)、`PushManager`(三级 category + **按 `kind` 路由**)、`PositionExtras`(计划继承卡 + **⑪-D-D per-position 触达提醒开关**)。
- **🔴 ⑭ 打出的三处「服务端已改、客户端硬解码」——已全部改完**:`ReportResponse.candidates`(整份报告解不出)/ `PushSettings` 六个具名 bool(整个 `/settings` 解不出)/ `SettingsSnapshot.llmKeySet`。**⑮ 之后客户端与 V2 服务端契约对得上**,⑰ 换包的前提已具备。
- **两个「已登记的债」集合双双清空**(`tests/test_contract_crosscheck.py`):`PENDING_CLIENT_CALLS_TO_BE_REMOVED_IN_15` 与 `PENDING_MAP_REASON_CASES_FOR_15`。⚠ **空集合不是「闸松了」,恰恰是最严的状态** —— 两条断言都用 `==`,此后任何新增的「打不到的调用 / 没 case 的 reason」立刻红。对拍表 §六 的 6 组复选框已逐项打勾并注明落在哪一笔提交(另加 6.6 两条 ⑮ 施工中追加项)。
- **版号三方已恒等 `2.0.0`**:`api/app.py::VERSION` = `v2.0.0`、`project.yml` + pbxproj `MARKETING_VERSION` = `2.0.0`(守门单测 `test_client_version_governance` 锁死)。⚠ **本地代码已是 v2.0.0,生产 `/health` 仍报 v1.5.2** —— 因为**至今零部署**,⑯ 上云后才会对上。
- **生产现状(未动)**:公网 `/health` = **v1.5.2**,现役章程 **v1.3.3**,macOS App **1.5.1**,iPhone 仍是 v1.4 之前的旧构建。**V2 施工期间不改动 v1.5.2 生产行为**;至今全部改动**纯本地代码 + 单测 + `xcodebuild`**,未碰任何服务器/DNS/部署。真实 `data/neckline.db` 里全部 V2 表**仍是 0 行**(⑮ 的界面核对跑在 scratchpad 的隔离临时库上,真实库只读拷了 `trade_cal`/`strategy_versions`/`stock_basic`/`namechange` 四张参考表)。
- **🟡 交回 planner 的一件(⑭ 提出,仍挂着,不拦 ⑯)**:**⑨ 复盘与报告落库的先后**在 Plan 两处描述不一致(⑭-A 未明说前后 vs ⑯-D 的服务划分)。现按「**⑨ 在前**」实现(报告 ④ 节要读当日 `basket_review_daily`);`segments` 参数让两种切法都跑得动,**改口径只需调 ⑯-D 的服务划分、不必动代码**。
- **V2-⑭ 完工内容**:`report/basket_daily.py`(三段视图模型 + **`card_json` snake→camel 唯一转换点**)、`report/evening.py`(16:35 链)、`scripts/evening.py`、`scripts/smoke_evening.py`;`reports` 新列 `basket_daily_json`。**兑现三处前置块欠账**:④ 的 `dataFreshness.scanLayer*` 三键接线(与板块三键、行业强度三键**并列九个键**)、⑬-4 的 `exec_hints_for()` 接进篮子成员、⑬ 的 `news_alerts` 次级扫描域接篮子成员。
  - ⚠ **③b 的两个原因码 `capacity_overflow` / `below_quality_line` 永不合并**(相反的市场结论),**零溢出时这一节仍在**;⑮ 的界面已按此实现并截图核对过。
  - ⚠ **编排链刻意住 `report/evening.py` 而非 `pipeline.py`**(后者在 P0-23 在线模块守门清单里)。`build_report` 自此**只读不算**。
- **V2-⑬ 完工内容**:V1 十三项清理全部执行完毕 —— 单票候选管线整链、自选池 + 同花顺对账整链、呼吸台账、问询台 forced 海选池通道全部物理删除;盘前 9:26 判定换血成**篮子竞价剧本核对**;信息卡改造成**篮子成员详情页地基**(⑮ 已把 ⑬-N 的三块接上 UI)。删除前后对照表 → `archive/V2-⑬_删除前后对照表_20260803.md`。
- **V2-⑫ / ⑪ / ⑩ 完工内容**:`review/cashflow.py` + `neckline/profile/`;`notify_kinds.py`(三级 × 14 kind 唯一源)+ `custom_alerts.py` + `sentinel/attention.py` + `llm/nl_alert.py`;`positions_entry.py`(买卖三字段唯一编排入口),`decision_log` 停写留档。
- **V2-⑧ / ⑦-b / ⑧-E / ⑧-F / ⑧-G 完工内容**:`selection/verification_rules.py`、`sentinel/basket_verify.py`、`sentinel/capture.py`、`sentinel/mainline.py`。**关注池(⑧-G 起)= 【有界必需项无条件全进】持仓 > T1/T2 篮子成员 > 板块基准指数(上界 29)+【两份纪律测量样本各有保底】主线切片 100 / 昨日涨停 71**(合计 = `breadth_cap` 200,守门单测锁死)。⚠ **主线跳水样本 ≠ 关注池**(⑧-G-A)。
- **V2-⑨ / ⑦ / ⑥ / ⑤ / ④ 系列完工内容**:`review/basket_review.py` + `neckline/eval/`;`selection/basket_card.py` + `member_tags.py`;`selection/tier.py` + `basket_store.py`;`selection/aggregate.py` + `member_hygiene.py`;全新包 `neckline/scan/` 与 `neckline/selection/` + `scripts/activate_pack.py` + 两个 pack。
- **如实登记的设计判断汇总**(各块「完工记录」尾段):③ 三处、④ 六处、⑤ 十处、③-K7 三处、④b 三处、⑤-b/⑤-c 五处、⑥ 十处、⑥-b 一处、⑦ 六处、⑦-b 一处、⑧ 七处、⑧-E 两处、⑨ 六处、⑩ 两处、⑪ 九处、⑪-D 一处、⑧-F 三处、⑧-G 六处、⑫ 九处、⑬ 一处勘误 + 三处判断 + 三处欠账(**⑭ 已全部兑现**)、⑭ 五处判断 + 一件交回 planner、**⑮ 六处判断**。均未改 Plan,如与规划意图不符请澄清。
- **仍未修复的三点既有欠账**(与各新块均无关):V2-① 的 `test_holding_k4_check.py` 未传 `db_path`(会写开发库)、V2-② 的 `news_scan.py` 缺 `prompt_context`、`TASK_INQUIRY` 归入检索类默认路由待澄清。
- **⑭ 顺带发现的两条已在 ⑮ 处理掉**:`ReportSnapshot` 的死代码 `Decodable` 已收(改成纯 `Equatable`);`InfoCard` 的合成 `Codable`(每个属性都必需)已改手写 `init(from:)` 全字段 `decodeIfPresent`。
- **下一步** = 【门禁·非施工块】**用户判定点:是否召唤 @reviewer**(建议范围:⑥⑦⑧⑪ 判定线 + ⑭⑮ 契约线两线并行)。⚠ **⑮ 碰了鉴权相关面(Provider key 写入路径)与全部客户端契约,建议叫一次 review**。**⑯ 的两条硬前置仍是 ①–⑮ 全部完工(✅ 已满足)+ 用户已购机并解析好 `nk`(⏳ 待用户)**;在用户交付新机之前,**不碰任何服务器**。
- **待办总入口 = §七 Backlog**。策略假设 / K 字头版本权威在 `STRATEGY_LAB.md`。

> 📁 **本节自 2026-07-28 起为快照制**:每次会话交接**替换**本节全文,不追加;历史价值内容归 §九 一行 + `archive/` 详版。v1.0 → v1.3.5 的历史交接账本 → `archive/当前状态_历史账本_20260719-20260728.md`;v1.4 → v1.5.2 的收官快照 → `archive/v1.5_施工图_20260802归档.md` 文末附录。

---

## 五、当前版本 Plan(**V2.0.0** = 换一颗篮子引擎的心脏 · 减负的持仓台账 · 存拍复盘的眼睛 · LLM 双 Agent 参谋部)

> **一句话定义**:**V2 = 在 V1 的骨架(数据、纪律、对账、哨兵、推送)上换一颗篮子引擎的心脏,配一双存拍复盘的眼睛,和一个不再挡路的持仓台账;LLM 双 Agent 是参谋部,章程仍是唯一的宪法。**
>
> **本节是 V2.0.0 唯一权威施工图**。上游文献:产品总纲 `新版本量化交易APP与选股架构.md`(用户蓝图,**产品语义以它为准**,已保留在仓库根,非施工权威);工程推演原稿 `archive/V2架构设计稿_20260731_20260802立项归档.md`(**已转历史档案,施工不再读它**)。
>
> 🔴 = 高危块(碰纪律判定 / 生产部署 / 数据迁移),点名 **@builder-pro**,一律**改前备份 + 逐项证据**。

### 〇、已拍板的设计前提(用户逐条裁定,**施工时不得重开**)

| # | 议题 | 裁定 |
|---|---|---|
| 1 | 分时数据冷启动 | 用户自寻数据源方案;架构按「盘中存拍自给 + 外源可插拔」双轨设计,存拍从 V2 上线日起自动积累 |
| 2 | LLM 成本与晚间窗口 | **双 Agent 分工出报告(DeepSeek + GLM)**;Provider 层必须改为**自填制**(任意 OpenAI 兼容端点可配,不再是 GLM/Kimi 枚举) |
| 3 | 生产机容量 | **100% 更换更强云服务器**,列为部署前置项,不作为架构约束 |
| 4 | Tier 冷启动 | **预启动填充**:真启动前在外部把初始篮子/Tier 配好灌入,之后系统自转(⑯-F) |
| 5 | LLM 编驱动幻觉 | 不设专项防线(用户判定近期模型已足够可靠);篮子卡的证据链字段(来源+日期)按蓝图 4.6 保留 |
| 6 | 表单可选化→归因标签稀疏 | **入 Backlog 挂账**(§七 P3-28),真遇到再解决,V2 不预建机制 |
| 7 | 契约换血 | 工程侧自理(两步淘汰等坑清单纪律照旧),用户只验成果 |
| 8 | 建设方式 | **一步到位**,无影子系统、无分阶段过渡 |
| 9 | V1 清理遗留三项 | **自选池 + 同花顺对账直接删除**(不采纳降级保留建议);**呼吸台账删**;**五常驻板块保底删除确认** |
| 10 | 施工与验收分工 | V2 全程**不设「首战核验 / 首战待验」类条目** —— 验证由用户侧负责,施工线只负责把 APP 做出来交付。**边界**:单测 / 守门单测 / `xcodebuild` 构建 / 部署当次必须留的证据,是**工程门禁**、属施工的一部分,照旧写 |
| 11 | 选股策略包插槽 | 选股板块初版用现成选股代码,但必须**预留可轮替的策略包插槽**:策略包由策略线打包交付,系统线负责校验入库与激活轮替(机制见本节 ③) |

> **另:「LLM 永远是参考,硬条款才是纪律」第〇原则(§2.0)在 V2 全域继续有效** —— Tier、篮子卡、剧本、画像全部是参考件;纪律只住章程。**四锁在 V2 的精确表述见 §2.8-C(必读,V1 原文逐字照搬会与 V2 直接冲突)。**

### ⛔ 红线(任何一处违反即本版验收不过)

1. **不改现役章程**:现役 `strategy_versions` 恒为 `v1.3.3`,V2 全程不新建章程行、不跑 `activate_charter.py`、不改任何阈值。**纪律外壳一行不动**(§3.6 原文:章程 / `strategy_versions` + append-only 激活历史 / 四道闸 / 逐笔章程判定 / 熔断 / 券商条件单核查提醒 / 周对账)。
2. **不授权自动交易**:系统永不下单 / 撤单 / 改止损(§3.8)。NL 提醒只通知不交易。
3. **第〇原则四锁在 V2 全域重挂**,表述以 §2.8-C 为准(不是 V1 原文逐字)。
4. **P0-23 纪律平移**:任何「扫全市场多年历史」的量若成了判据 / 排序输入,**必须 EOD 预计算落表、在线只读**,缺行走保险丝(降级方向 = 不拦 + `dataFreshness` 显式披露),**不许在线现算自愈**;**新批算必须在新机上单独计时 + 量峰值后才准进主链**(⑯-C)。
5. **语义红线**:排序 / Tier = **注意力优先级,不是收益预测**;T1 ≠ 最会涨;终选权在用户。UI 禁「推荐买入 / 建议买入 / 看好 / 值得买」类表述;参考件每处出现都带「参考、非指令」标注;离场参考区间**不许**被表述成止盈线。
6. **策略包不装可执行代码**(§12.1 定案,勿重开);**纪律章程不进包**(两条版本线、两张表、两套激活流程)。

### D1–D8 定案清单(**2026-08-02 用户逐条拍板,施工时不得重开**;原为 planner 建议默认,现已全部转为定案)

| # | 事项 | **已拍板取值(2026-08-02 用户)** | 定这个的理由 |
|---|---|---|---|
| D1 | **信息卡**(V1 清理 §8.2 唯一遗留项) | ✅ **不删,保留改造**为篮子成员详情页地基(⑬-N) | 九件套现成,删了成员详情页要另起地基 |
| D2 | **割接域名路线** | ✅ **A 路定死:新机启用新子域 `nk.linotsai.top`**(**DNS 解析由用户自己做**,§八 第 14 项),老机 `ln.linotsai.top` 维持到 ⑰ 双端换包完成 → **契约一次性换血,不留过渡键**。~~B 路(沿用 `ln`,割接时切解析)~~ **已否决,不再是选项** —— 详见 ⑭-D | 用户原话:「我本来就要换云服务器,NK 这个域名我会直接把它解析到新的云服务器上」。老 App 打老机、新 App 打新机,两者不交叉,**契约换血最干净** |
| D3 | **新机规格** | ✅ **≥ 4 vCPU / 8 G / 100 G SSD**(按 ⑯-C 实测复核) | 老机 2 vCPU/1.6G 已被 P0-23 打爆过一次;V2 晚间管线重一个量级 |
| D4 | **盘中存拍落盘时机** | ✅ **内存累计 + 15:05 收盘后一次性落盘** | `write_table_day` 是「一日一文件整段替换」,逐分钟 append 会反复重写整日文件并拖累哨兵主循环 |
| D5 | **通知三级的 APNs 落法** | ✅ 三个 category `NKIMMEDIATE` / `NKIMPORTANT` / `NKDIGEST`,事件带 `kind`,**开关按 kind 配** | 按 category 配开关会连坐(V1 拆 `HOLDINGALERT` 的教训) |
| D6 | **新表命名** | ✅ `entry_snapshots` / `llm_providers` / `selection_pack_activation_log` / `profile_preference` / `profile_capability` | 三张补位表 + 画像两表,命名一次定死,后续不再改 |
| D7 | **首包命名与位置** | ✅ `packs/K4-pack.json`,`pack_version = "K4-pack-v1"` | 沿用 K 字头,包 = 策略线交付物形态 |
| D8 | **iPhone 信息架构** | ✅ 四板块 = **今日篮子 / 持仓 / 问询台 / 设置**(macOS 加周复盘工作台) | 加 tab 会稀释「打开就看今天做什么」 |

### V2 新表汇总(**21 张**,DDL 要点在各块定死;1–20 建表统一在 ① 一次到位,**第 21 张随 ④b 追加**)

> 算术:架构稿 §四 列了 **16 张**(含 2 张 parquet)+ §十二 的 `selection_packs` **1 张** + **planner 补位 3 张** + **K7 需求 1b 追加 1 张**(理由逐条写在下表「来源」列)。
> ⚠ 第 21 张(`industry_stage_daily`)是 ① 完工**之后**才立项的,DDL 落在 **④b** 块自己那里(新表 `CREATE TABLE IF NOT EXISTS` 天然幂等、**不进 `_COLUMN_MIGRATIONS`**,同 ① 的既有体例),不回头改 ① 的完工记录。

| # | 表 | 载体 | 建表块 / 写入块 | 三律 | 来源 |
|---|---|---|---|---|---|
| 1 | `baskets` | SQLite | ① / **⑥** | 冻结 | 架构稿(⚠ 写入块 2026-08-02 由 ⑦ 改判为 ⑥,见「篮子四表落库次序」) |
| 2 | `basket_members` | SQLite | ① / **⑥** | 冻结 | 架构稿(⚠ 同上,由 ⑦ 改判为 ⑥) |
| 3 | `basket_cards` | SQLite | ① / ⑦ | 冻结 + 追加版本 | 架构稿(⑦ **只写这一张**) |
| 4 | `basket_verification` | SQLite | ① / ⑧ | 追加,不回写 | 架构稿 |
| 5 | `basket_review_daily` | SQLite | ① / ⑨ | 每日一行幂等 | 架构稿 |
| 6 | `tier_history` | SQLite | ① / ⑥ | 每日一行幂等 | 架构稿(与 1、2 **同一事务**) |
| 7 | `corr_matrix_daily` | SQLite | ① / ④ | EOD 预计算,在线只读 | 架构稿 |
| 8 | `limit_cluster_daily` | SQLite | ① / ④ | 同上 | 架构稿 |
| 9 | `leader_structure_daily` | SQLite | ① / ④ | 同上 | 架构稿 |
| 10 | `position_plans` | SQLite | ① / ⑩ | 追加版本,不改原卡 | 架构稿 |
| 11 | `custom_alerts` | SQLite | ① / ⑪ | 可改(用户自己的规则) | 架构稿 |
| 12 | `user_actions` | SQLite | ① / ⑩⑮ | **append-only** | 架构稿 |
| 13 | `profile_preference` | SQLite | ① / ⑫ | 每期一版 | 架构稿(画像两表之一) |
| 14 | `profile_capability` | SQLite | ① / ⑫ | 每期一版 | 架构稿(画像两表之二) |
| 15 | `selection_packs` | SQLite | ① / ③ | append-only + 单现役 | 架构稿 §12.4 |
| 16 | `selection_pack_activation_log` | SQLite | ① / ③ | **append-only 事件流** | **planner 补位**:§12.4 明写「+ 激活日志」但未给表名;照 `strategy_activation_log` 先例单列一表,不把「事件」塞进「版本」表 |
| 17 | `llm_providers` | SQLite | ① / ② | 可改(用户自填) | **planner 补位**:§5.1 说「`app_settings` 扩为 Provider 注册表」,但 `app_settings` 是 `id=1` 单行表(CHECK 约束),装不下 N 个 provider;注册表落新表,**路由**留在 `app_settings` 新列 |
| 18 | `entry_snapshots` | SQLite | ① / ⑩ | **冻结** | **planner 补位**:§3.1「决策日志强制表单退役 → 自动快照表」需要落点,而 16 表清单里没有;`decision_log` 的 4 个 `NOT NULL` 无默认列无法就地放宽(SQLite 要重建表),故新表冻结 + `decision_log` 停写留档 |
| 19 | `intraday_ticks` | **parquet** | ① / ⑧ | 只增不改 | 架构稿 |
| 20 | `auction_snapshots` | **parquet** | ① / ⑧ | 只增不改 | 架构稿 |
| 21 | `industry_stage_daily` | SQLite | **④b** / ④b | EOD 预计算,在线只读 | **K7 需求 1b**(2026-08-02 用户转发):`driver_freshness` 改行业五态状态机,需要一张按日落表的阶段表;交接稿给了「新表 or `industry_strength_daily` 加列」二选一,**planner 定为新表**,理由见 ④b |

### 分块序列(依赖一眼看清)

```text
地基层  ① 表与共享信息层地基
        ② LLM Provider 自填制 + 双 Agent 路由 + 预算多本账
        ③ 选股策略包机制(含首包 K4-pack)
        ③-K7 原语与白名单跟进(K7 需求 4)             ← ③,可与 ④/④b 并行
选股层  ④ ①市场扫描层(三张预计算表 + 驱动种子)      ← ①③
        ④b 行业题材阶段表 industry_stage_daily(K7 需求 1b)
             + ④ 的 rs_rank tie-break 口径追加(K7 需求 1a)  ← ①,**可与 ④ 并行**
        ⑤ ②驱动聚合层(LLM + 两道机械闸)             ← ②④
        ⑥ ③Tier 分层引擎(K7 需求 1a/1b/2 的机械分正式定义)← ③ ③-K7 ④b ⑤
        ⑦ ④篮子卡冻结                                ← ⑥
        ⑧ ⑤-a D+1 验证:关注池改造 + 盘中存拍 + 状态机 ← ⑦
        ⑨ ⑤-b 盘后复盘引擎 + 评价引擎                 ← ⑦⑧
持仓层  ⑩ 极简台账 + 计划继承 + 决策日志表单退役       ← ①⑦
        ⑪ 监控 80/15/5 + 通知三级 + NL 临时提醒        ← ⑧⑩
        ⑫ 对账与画像                                  ← ⑨⑩
换血层  ⑬ V1 清理清单执行(先建后拆,不许边建边拆)     ← ④⑤⑥⑦⑧⑩⑪
        ⑭ 篮子日报 + API 契约总装 + 三方对拍(✅)       ← ①~⑬
        ⑮ 客户端双端改版                              ← ⑭
        【门禁·非施工块】用户判定点:是否召唤 @reviewer
交付层  ⑯ 新服务器 + 晚间管线多段化 + 迁移割接 + 两步包激活 + B3 退役 + Tier 预填充(🔴)
             ← ①~⑮ + 用户已购机
        ⑰ 双端换包(末块,等用户指令)
```

> 📌 **K7 需求整合位置一览(2026-08-02 用户转发 `archive/交接_K7_系统线需求_20260802草案.md`,已全文整合,原稿转参考件)**:
> 需求 **1a** leader_clarity → **⑥** 正式定义 + **④b-0** 的 `rs_rank` 三级 tie-break 口径追加;
> 需求 **1b** driver_freshness 五态 → **⑥** 正式定义 + 新块 **④b**(`industry_stage_daily`);
> 需求 **2** 五维权重证据化初值 → **⑥**(数值落 K7-pack,**K4-pack-v1 原样不动、不重发版**);
> 需求 **3** B3 黄牌退役 → **⑯-I**(🔴 advisory 载体变更,与 ⑯-E 包激活同窗)+ **【门禁】** 的用户判定点;
> 需求 **4** 原语 / 白名单 / schema 扩展 + `packs/K7-pack.json` → **③-K7**,激活时机 → **⑯-E**;
> 需求 **5** 龙回头位与双尾警示标注件 → **⑦-K7**(篮子卡成员节)+ **⑬-N**(信息卡);
> 需求 **6** 安慰剂对照臂 → **⑨-C**(用户「尽量都做到」,已从建议项排进正式验收)。

> ⏱ **工序时序纪律(2026-08-02 用户追加指示,写死)**:**云上组件迁移放在尾声。①–⑮ 全部本地推进优先,⑯ 的云上动作(购机验收 / 数据搬家 / 新机实测 / 割接 / 首包激活 / preseed 灌入)是尾声块,不与前面任何块并行穿插。**
> - **判据**:在 ⑮ 完工之前,**不碰任何服务器**(新机老机都不碰)、不搬数据、不改 DNS、不部署。①–⑮ 期间的一切验证走本地隔离库 + 本地 parquet + 单测 + `xcodebuild`。
> - **⑯ 内部允许「本地可先备料」的纯本地子项**(标注在块内,**不拆走**):`deploy/` 三个 oneshot 单元文件与 timer 的编写、nginx 站点模板、`scripts/activate_pack.py` 与 `packs/K4-pack.json`(③ 已产出)、`scripts/oneoff/preseed_baskets.py` 与其校验逻辑、迁移检查清单脚本。**写好即可,一律不在服务器上跑**,到 ⑯ 才上机执行。
> - **用户侧的 §八 第 13/14 项(购机 + `nk` 解析)也按尾声节奏办**,不必在 ①–⑮ 期间催办;builder 在 ⑯ 开跑前问一次即可。

### 铁律(承 §3.8 + §2.0 + §2.8;builder 逐条守,每块都适用)

- 纪律参数全读现役 `brain.get_active().rule["config"]`(禁硬编 0.05 / 0.08);涨跌停一律走 `data/limit_derived.py`;板块分类读 `data/board.py`;交易日 / 收盘时刻 / 时区读 `neckline.calendar`(`CN_TZ` / `MARKET_CLOSE_TIME`);总仓读 `Settings.total_capital`;题材持续天数读 `report/industry_strength.py::stock_persist_days`;行业强度读 `report/industry_strength_store.py`(**不现算**);K4 红黄牌文字读 DB `k4_advisory`。
- **所有 LLM 调用点必须 import `llm/prompt_context.py`**(日期锚 + 时效纪律 + 检索词年份引导,唯一实现;守门单测扫全仓禁止第二份拷贝)。
- **标签后面还挂内容的调用方,必须先剥掉那段内容再解析 verdict**(`narrative_splitter` 依赖注入体例)。
- **落 parquet 一律 `write_table_day`**;新表必须同步进 `_VALID_TABLES` + `TABLE_FLOAT_COLS` 声明(守门单测会挂)。
- **跨进程 / 跨天要复现的分组一律 `zlib.crc32`,禁内置 `hash()`**;轮转用纯日期函数;**任何进判据 / 排序的 `rank` 必须先排定确定性 tie-break 再 `ordinal`**。
- **纪律阈值比较一律加 `_EPS=1e-9` 容差**;「上市第几天」一律 `sentinel.universe.is_new_stock_exempt`;polars 链式赋值右侧一律用 `pl.col(...)`。
- **测试隔离**:经 `neckline.db.connection` 访问 SQLite 的模块调用时**必须显式传 `db_path=`**(`isolated_env` 不覆盖它,`db_path=None` 会静默读真实开发库)。
- **「没有」与「没看」必须能分开**:禁 `or 0.0` 静默兜底、禁空数组冒充「生成过了没内容」;降级必须诚实披露。
- **核心管线对可选情报输入的调用必须包保险丝**:任何一段(扫描 / 聚合 / Tier / 卡 / 复盘 / 画像)异常都不许让当日无报告。
- **新表 / 新列幂等迁移 + 改 schema 前 `cp -p` 备份**;删表一律**停写留档,不 DROP**。

---

### V2-① · 表与共享信息层地基(@builder)✅ 完工(2026-08-02)

**目标**:把 V2 全部 20 张新表 / 新分区一次建齐,并把「事实 / 用户行为 / 模型判断三类分存 + 冻结 / 追加 / 不回写三律」锁成机器判据。此后每块只写读写逻辑,不再动 DDL。

**涉及模块 / 文件**
- 改造 `neckline/db.py::_SCHEMA`(18 张 SQLite 新表 DDL;**新表走 `CREATE TABLE IF NOT EXISTS` 天然幂等,不进 `_COLUMN_MIGRATIONS`**);`app_settings` 新增两列走 `_migrate_columns`(`llm_default_provider TEXT` / `llm_task_routes TEXT NOT NULL DEFAULT '{}'`)。
- 改造 `neckline/data/market_data.py`:`_VALID_TABLES` 加 `intraday_ticks` / `auction_snapshots`;`TABLE_FLOAT_COLS` 同步补两张表的 canonical 数值列声明(**漏了守门单测会挂,这是 v1.3.5 血训**)。
- 新建 `neckline/user_actions.py`:`record(kind, *, ts_code=None, basket_id=None, position_id=None, payload=None, occurred_at=None, db_path=None)` + `list_actions(...)`。**只有 INSERT,没有 UPDATE / DELETE 函数**(append-only 靠"没有那个函数"来保证,不靠自觉)。
- 新建 `tests/test_v2_schema_guard.py`(三律守门 + 幂等 + parquet 声明)。

**新表 schema 要点**(列名以此为准,builder 不用猜;`created_at` 一律 ISO8601 字符串,`trade_date` 一律 `'YYYYMMDD'`)

```sql
-- 模型判断:篮子本体(D0 冻结)
CREATE TABLE IF NOT EXISTS baskets (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  trade_date         TEXT NOT NULL,              -- D0
  basket_key         TEXT NOT NULL,              -- crc32(trade_date|driver_slug) 十六进制,跨进程可复现
  name               TEXT NOT NULL,
  driver             TEXT NOT NULL,              -- 共同驱动(一句话,LLM 产出)
  driver_kind        TEXT NOT NULL,              -- theme|policy|event|commodity|overseas|rotation|limit_cluster
  tier               INTEGER NOT NULL,           -- 1|2|3
  pack_version       TEXT NOT NULL,              -- 现役策略包版本(归因用)
  engine_api_version INTEGER NOT NULL,
  charter_version    TEXT NOT NULL,              -- 生成时的现役章程(口径指纹)
  via                TEXT NOT NULL DEFAULT 'auto',   -- auto | preseed
  evidence_status    TEXT NOT NULL DEFAULT 'ok',     -- ok | search_unavailable | partial
  created_at         TEXT NOT NULL,
  UNIQUE(trade_date, basket_key)
);

-- 模型判断:成员与角色(含角色对拍分歧)
CREATE TABLE IF NOT EXISTS basket_members (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  basket_id       INTEGER NOT NULL,
  ts_code         TEXT NOT NULL,
  role_llm        TEXT NOT NULL,          -- leader | core | elastic
  role_mech       TEXT,                   -- 来自 leader_structure_daily;NULL=机械侧无判定
  role_conflict   INTEGER NOT NULL DEFAULT 0,   -- 1 = 两侧冲突,分歧入卡不静默采信
  reason          TEXT NOT NULL,          -- 为何是这只而不是同题材其他票
  is_primary      INTEGER NOT NULL DEFAULT 1,   -- 主归属篮(同票多篮时唯一 1)
  created_at      TEXT NOT NULL,
  UNIQUE(basket_id, ts_code)
);

-- 模型判断:篮子卡(冻结 JSON,追加版本制,写入即不可改)
CREATE TABLE IF NOT EXISTS basket_cards (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  basket_id          INTEGER NOT NULL,
  version            INTEGER NOT NULL,          -- 1 = D0 原判;D+1 追加 2,3…
  card_json          TEXT NOT NULL,             -- 蓝图 4.6 全项 + 结构化 spec + disclaimer
  stop_pct           REAL,                      -- 口径指纹(生成时现役 config)
  take_profit_retrace REAL,
  charter_version    TEXT,
  pack_version       TEXT,
  engine_api_version INTEGER,
  created_at         TEXT NOT NULL,
  UNIQUE(basket_id, version)
);

-- 模型判断:验证状态流水(append-only,状态追加不回写)
CREATE TABLE IF NOT EXISTS basket_verification (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  basket_id     INTEGER NOT NULL,
  trade_date    TEXT NOT NULL,          -- D+1
  observed_at   TEXT NOT NULL,          -- ISO8601 北京时间
  state         TEXT NOT NULL,          -- verified | partial | unclear | falsified
  source        TEXT NOT NULL,          -- intraday | eod
  evidence_json TEXT NOT NULL DEFAULT '{}',   -- 命中了哪条结构化条件
  created_at    TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_basket_verif ON basket_verification(basket_id, trade_date);

-- 模型判断:每日复盘(机械判 + LLM 解释)
CREATE TABLE IF NOT EXISTS basket_review_daily (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  basket_id     INTEGER NOT NULL,
  review_date   TEXT NOT NULL,          -- 被复盘的那个交易日(= D+1)
  depth         TEXT NOT NULL,          -- full(T1/T2) | brief(T3)
  mech_json     TEXT NOT NULL,          -- 九项机械判 + 数据来源标注(存拍/EOD近似)
  llm_text      TEXT,                   -- NULL = 未生成(预算或降级),不冒充"没内容"
  llm_skip_reason TEXT,
  degraded      INTEGER NOT NULL DEFAULT 0,
  created_at    TEXT NOT NULL,
  UNIQUE(basket_id, review_date)
);

-- 模型判断:Tier 定档留痕(可完整复现)
CREATE TABLE IF NOT EXISTS tier_history (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  trade_date      TEXT NOT NULL,
  basket_id       INTEGER NOT NULL,
  tier            INTEGER NOT NULL,
  mech_score      REAL NOT NULL,
  mech_breakdown_json TEXT NOT NULL,    -- 五维分项 + 权重(来自现役包)
  rank_in_tier    INTEGER NOT NULL,     -- 最终序
  rank_mech       INTEGER NOT NULL,     -- 机械序(LLM 微调前)
  llm_rank_delta  INTEGER NOT NULL DEFAULT 0,
  llm_reason      TEXT,
  pack_version    TEXT NOT NULL,
  created_at      TEXT NOT NULL,
  UNIQUE(trade_date, basket_id)
);

-- 事实(EOD 预计算):板内/簇内滚动 20 日相关
CREATE TABLE IF NOT EXISTS corr_matrix_daily (
  trade_date  TEXT NOT NULL,
  scope_key   TEXT NOT NULL,      -- 簇 or 板块的稳定键
  code_a      TEXT NOT NULL,
  code_b      TEXT NOT NULL,      -- 约定 code_a < code_b,不存两遍
  window      INTEGER NOT NULL,   -- 20
  corr        REAL,               -- NULL = 样本不足,禁写 0
  n_obs       INTEGER NOT NULL,
  computed_at TEXT NOT NULL,
  PRIMARY KEY (trade_date, scope_key, code_a, code_b, window)
);

-- 事实(EOD 预计算):涨停共振簇
CREATE TABLE IF NOT EXISTS limit_cluster_daily (
  trade_date       TEXT NOT NULL,
  cluster_key      TEXT NOT NULL,   -- crc32 稳定键
  ts_code          TEXT NOT NULL,
  cluster_kind     TEXT NOT NULL,   -- same_day | consecutive
  cluster_size     INTEGER NOT NULL,
  consecutive_days INTEGER NOT NULL,
  anchor_industry  TEXT,
  anchor_concept   TEXT,
  computed_at      TEXT NOT NULL,
  PRIMARY KEY (trade_date, cluster_key, ts_code)
);

-- 事实(EOD 预计算):簇内龙头结构
CREATE TABLE IF NOT EXISTS leader_structure_daily (
  trade_date   TEXT NOT NULL,
  cluster_key  TEXT NOT NULL,
  ts_code      TEXT NOT NULL,
  rs_rank      INTEGER,          -- NULL = 算不出,禁写 0
  limit_height INTEGER,
  amount_share REAL,
  role_mech    TEXT NOT NULL,    -- leader | core | elastic | unknown
  computed_at  TEXT NOT NULL,
  PRIMARY KEY (trade_date, cluster_key, ts_code)
);

-- 用户行为:唯一落点,append-only
CREATE TABLE IF NOT EXISTS user_actions (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  occurred_at  TEXT NOT NULL,     -- ISO8601 北京时间
  kind         TEXT NOT NULL,     -- view | select | buy | sell | alert | label | voice_note
  ts_code      TEXT,
  basket_id    INTEGER,
  position_id  INTEGER,
  payload_json TEXT NOT NULL DEFAULT '{}',
  created_at   TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_user_actions_kind_time ON user_actions(kind, occurred_at);

-- 事实:开仓自动快照(决策日志强制表单的替代;冻结,一笔持仓一行)
CREATE TABLE IF NOT EXISTS entry_snapshots (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  position_id    INTEGER NOT NULL,
  ts_code        TEXT NOT NULL,
  trade_date     TEXT NOT NULL,
  basket_id      INTEGER,          -- NULL = 非篮子来源(手动补录)
  card_version   INTEGER,
  tier           INTEGER,
  role           TEXT,
  snapshot_json  TEXT NOT NULL,    -- 机器可知的一切:价量/涨幅/换手/量比/板块强度/资金流/红黄牌/竞价表现…
  created_at     TEXT NOT NULL,
  UNIQUE(position_id)
);

-- 模型判断:持仓计划(版本化;新版本不改原始篮子卡)
CREATE TABLE IF NOT EXISTS position_plans (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  position_id         INTEGER NOT NULL,
  version             INTEGER NOT NULL,   -- 1 = 从篮子卡继承
  source_basket_id    INTEGER,
  source_card_version INTEGER,
  plan_json           TEXT NOT NULL,      -- 建仓区间/最高追价/离场参考/验证失效/主要风险
  note                TEXT,
  created_at          TEXT NOT NULL,
  UNIQUE(position_id, version)
);

-- 用户行为:自然语言临时提醒(结构化规则)
CREATE TABLE IF NOT EXISTS custom_alerts (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  ts_code          TEXT,               -- NULL = 大盘级
  nl_text          TEXT NOT NULL,      -- 用户原话(留痕)
  rule_json        TEXT NOT NULL,      -- 结构化规则(哨兵唯一判据)
  active_from      TEXT,               -- 'HH:MM' 生效窗起
  active_to        TEXT,
  expires_at       TEXT,               -- NULL 且 persist=0 → 收盘自动失效
  persist          INTEGER NOT NULL DEFAULT 0,   -- 1 = 显式长期有效
  cooldown_seconds INTEGER NOT NULL DEFAULT 0,
  max_fires        INTEGER NOT NULL DEFAULT 1,
  fired_count      INTEGER NOT NULL DEFAULT 0,
  status           TEXT NOT NULL DEFAULT 'active',  -- active | expired | cancelled
  created_at       TEXT NOT NULL,
  updated_at       TEXT NOT NULL
);

-- 模型判断:偏好画像 / 能力画像(两张账分开,每期一版)
CREATE TABLE IF NOT EXISTS profile_preference (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  as_of_date   TEXT NOT NULL,
  dimension    TEXT NOT NULL,     -- theme | role | entry_style | tier | auction_habit
  value        TEXT NOT NULL,
  share        REAL NOT NULL,     -- 占比
  sample_n     INTEGER NOT NULL,
  window_start TEXT NOT NULL,
  window_end   TEXT NOT NULL,
  confidence   TEXT NOT NULL,     -- low | medium | high(样本不足一律 low 并标注)
  computed_at  TEXT NOT NULL,
  UNIQUE(as_of_date, dimension, value)
);
CREATE TABLE IF NOT EXISTS profile_capability (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  as_of_date    TEXT NOT NULL,
  dimension     TEXT NOT NULL,
  value         TEXT NOT NULL,
  sample_n      INTEGER NOT NULL,
  win_rate      REAL, profit_factor REAL, avg_mfe REAL, avg_mae REAL,
  vs_peer_delta REAL,              -- 相对同篮未选成员
  window_start  TEXT NOT NULL, window_end TEXT NOT NULL,
  confidence    TEXT NOT NULL,
  computed_at   TEXT NOT NULL,
  UNIQUE(as_of_date, dimension, value)
);

-- 策略包(append-only + 单现役)与激活事件流
CREATE TABLE IF NOT EXISTS selection_packs (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  pack_version       TEXT NOT NULL UNIQUE,
  name               TEXT NOT NULL,
  engine_api_version INTEGER NOT NULL,
  manifest_json      TEXT NOT NULL,
  config_json        TEXT NOT NULL,
  evidence_ref       TEXT,               -- research/*.md 指针
  is_active          INTEGER NOT NULL DEFAULT 0,
  created_at         TEXT NOT NULL,
  activated_at       TEXT
);
CREATE TABLE IF NOT EXISTS selection_pack_activation_log (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  pack_version TEXT NOT NULL,
  action       TEXT NOT NULL,      -- activate | deactivate
  via          TEXT NOT NULL,      -- cli | seed
  note         TEXT,
  at           TEXT NOT NULL       -- UTC(同 brain._now() 口径)
);

-- LLM Provider 注册表(自填制;key 服务端存,DB 600)
CREATE TABLE IF NOT EXISTS llm_providers (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  name           TEXT NOT NULL UNIQUE,   -- 用户自取,如 deepseek / glm
  base_url       TEXT NOT NULL,
  model          TEXT NOT NULL,
  api_key        TEXT,                   -- 绝不出现在任何 HTTP 响应里
  has_web_search INTEGER NOT NULL DEFAULT 0,
  search_engine  TEXT,                   -- 带检索时的引擎取值(沿用 v1.5 埋点口径)
  notes          TEXT,
  enabled        INTEGER NOT NULL DEFAULT 1,
  created_at     TEXT NOT NULL,
  updated_at     TEXT NOT NULL
);
```

**parquet 两表**(`write_table_day` 铁律,每日一分区 `year=YYYY/YYYYMMDD.parquet`)
- `intraday_ticks`:`ts_code TEXT` / **`trade_date` = `pl.Date`**(⚠ **2026-08-02 更正,原文误写 TEXT**:本项目**全部 parquet 表的 `trade_date` 都是 `pl.Date`**,而 `get_market_slice` 用 `pl.col("trade_date") == <date>` 过滤 —— 落成字符串会让 ⑨ 用标准取数路径读这张表时**类型不匹配**。⑧ 已按 `pl.Date` 落地,**这是对的,别照原文"改回" TEXT**)/ `ts TEXT`(`HH:MM:SS` 北京时间)/ `price` / `volume`(手)/ `amount`(元)/ `cum_volume` / `cum_amount` / `source TEXT`(`sina|tencent|external`)。
- `auction_snapshots`:`ts_code` / `trade_date` / `auction_price` / `auction_volume` / `auction_amount` / `pre_close` / `gap_pct` / `captured_at`。
- 两张表的数值列**必须**写进 `TABLE_FLOAT_COLS` 声明(**对齐向声明看齐,永不向"第一个文件"看齐**)。

**三律守门单测(缺一不可)**
1. **冻结**:`basket_cards` / `entry_snapshots` 同 key 二次写 → `IntegrityError`;`grep` 断言 `neckline/` 全仓不出现 `UPDATE basket_cards` / `UPDATE entry_snapshots` / `DELETE FROM basket_cards`。
2. **追加**:`user_actions` / `basket_verification` / `selection_pack_activation_log` 三表的 store 层**只有 INSERT**(AST 扫描,不用纯文本 grep —— 纯文本会被 docstring 误伤,照 `test_db_isolation_guardrail.py` 先例)。
3. **不回写**:造一张 D0 卡 → 跑一次 D+1 验证与复盘 → 断言 D0 那行 `card_json` **逐字节不变**、`baskets` 行未被 UPDATE。
4. **幂等**:`init_schema()` 在空库 + 真实生产库只读副本上各跑两遍,表数 / 列数 / `integrity_check` 前后一致。
5. **parquet 声明**:两张新表写-读往返 dtype 与 `TABLE_FLOAT_COLS` 声明逐列一致;**全空列不漂 String**(照 `test_concept_data.py` 体例)。

**依赖**:无(第一块)。
**验收**:上述 5 条守门单测全绿;`python -m pytest tests/ -q` 零回归;跑一次全量 pytest 前后开发库 `mtime`/MD5 不变(P4-25 的隔离夹具仍生效)。

**完工记录(2026-08-02,@builder)**:18 张 SQLite 新表全部落 `neckline/db.py::_SCHEMA`(`CREATE TABLE IF NOT EXISTS`,天然幂等)+ `app_settings` 两新列(`llm_default_provider`/`llm_task_routes`)走 `_COLUMN_MIGRATIONS`;`intraday_ticks`/`auction_snapshots` 两张 parquet 表登记进 `neckline/data/market_data.py` 的 `_VALID_TABLES`+`TABLE_FLOAT_COLS`;新建 `neckline/user_actions.py`(`record`/`list_actions` 两函数,append-only 靠"没有第三个函数"担保)。新守门单测 `tests/test_v2_schema_guard.py`(17 个用例)覆盖 5 条三律判据:①冻结(`basket_cards`/`entry_snapshots` 撞键 `IntegrityError` + 全仓 grep 四短语,补了 plan 原文没列的 `DELETE FROM entry_snapshots` 对称项)②追加(AST 扫描 `execute`/`executemany`/`executescript` 调用点,已用合成用例验证不误伤 docstring)③不回写(直接 SQL 模拟 D+1 追加、断言 D0 篮子/卡逐字节不变)④幂等(空库 + `real_db_readonly_copy` 各跑两遍)⑤parquet 声明(两表往返 dtype + 全空列不漂 String)。全量 `pytest tests/ -q`:2005(基线,2 failed pre-existing+2001 passed+2 skipped)→2023(2 failed 同名同因+2019 passed+2 skipped),净增 18(17 新用例 + 1 条既有参数化守门测试自动多扫到本文件)、零回归。**发现一个与本块无关的既有缺陷(未修,已 `spawn_task` 记账)**:`tests/test_holding_k4_check.py` 的 `test_build_has_strong_only_price_volume` 等四个用例调用 `report/holding_k4_check.py::build_holding_k4_check(...)` 时未传 `db_path`,经 `holding_store.locked_time_exit_map(db_path=None)`→`init_schema(None)` 命中 `neckline/db.py` 自己未被 `isolated_env` 覆盖的真实 `settings.db_path`——该缺陷在改动前的原始代码上同样复现(已用 `git stash` + `DB_PATH` 环境变量重定向双重验证），只是这次因为本块新增了表/列才第一次在真实开发库 `data/neckline.db` 上留下可见痕迹(纯增量:新表+新列,无数据被改写或删除)；本块验收所用的"前后 mtime/MD5 不变"数据来自把 `DB_PATH` 重定向到 scratch 路径后的干净复测,不是默认调用。

### V2-② · LLM Provider 自填制 + 双 Agent 路由 + 预算多本账(@builder)✅ 完工(2026-08-02)

**目标**:退役「GLM/Kimi 枚举」,任意 OpenAI 兼容端点可配;任务→Provider 路由表可配;检索与推理两段式解耦;预算分三本账、降级次序定死。

**涉及模块 / 文件**
- 改造 `neckline/llm/factory.py`:`_PROVIDERS` 枚举**退役**;新签名 `get_provider(task: str | None = None, *, db_path=None, settings_obj=None) -> LLMProvider | None`,按 `app_settings.llm_task_routes` 找 provider 名 → 查 `llm_providers` 行 → 实例化**统一的 `OpenAICompatProvider`**(base_url / model / key / `has_web_search` / `search_engine` 由行给)。缺路由 → 回退 `llm_default_provider`;无 provider / 无 key → 返回 `None`(**全链路优雅降级不崩**这条铁律一字不动)。
- `neckline/llm/providers/glm.py` / `kimi.py`:**降级为「预置模板行」**(建库时可选播种 base_url / model / `has_web_search` 默认值),代码里不再是唯一合法值;`_SEARCH_ENGINE` 常量搬进模板行的 `search_engine` 列(**单一源随之下移,不许两处**)。
- `neckline/llm/openai_compat.py`:保留唯一传输底座(降级链 + 每次全新连接重试 + 带搜索 `read_timeout=90` 不动);**`has_web_search=0` 的 provider 一律不发 `tools` / `search_query`**(不给上游不认识的参数 —— v1.3.4 案底:会 `ok=True` 静默返 0 条)。
- 新建 `neckline/llm/router.py`:任务常量单一源 —— `TASK_DRIVER_SEARCH`(驱动证据检索)/ `TASK_NEWS_SCAN`(消息面)/ `TASK_BASKET_REASON`(篮子逻辑与角色比较)/ `TASK_TIER_RANK`(同档排序理由)/ `TASK_SCRIPT`(剧本)/ `TASK_REVIEW`(复盘解释)/ `TASK_PROFILE`(画像总结)/ `TASK_INQUIRY`(问询台)/ `TASK_NL_ALERT`(NL 提醒解析)。**默认路由**(路由表可改):检索类 → 带 `has_web_search` 的 provider(GLM);其余 → 推理 provider(DeepSeek)。
- 新建 `neckline/llm/budget.py`:三本账 `SEARCH_BUDGET_SECONDS` / `REASON_BUDGET_SECONDS` / `REVIEW_BUDGET_SECONDS`(**独立、不共享、不合并** —— 一个吃光另一个是最难查的那类故障,承 v1.5-② 定案);**降级次序定死**:先丢 T3 简评 → 再丢 T2 复盘细节 → **绝不牺牲篮子卡冻结与纪律外壳**。
- 改造 `neckline/settings_store.py`:provider CRUD + 路由 get/set;安全视图**逐行只回 `keySet: Bool`,绝不回明文**。

**两段式流水(定死)**:检索 Agent 先出「证据条目列表(每条带**来源 + 日期**)」→ 作为上下文喂推理 Agent。**单侧故障单侧降级**:检索段失败 → 篮子仍出,`evidence_status='search_unavailable'` 并在卡面 / 报告诚实披露「本次消息面缺席」;推理段失败 → 该篮子无 LLM 叙述,机械项照出。**两段都不许静默补一个编的**。

**契约变更**
- 新增 `GET /settings/providers`(列表,不含 key)/ `POST /settings/providers` / `PUT /settings/providers/{name}` / `DELETE /settings/providers/{name}` / `GET|PUT /settings/llm-routes`。
- **`PUT /settings/llm` 直接删**(D2 = A 路已拍板,老 App 打老机、不会撞到新服务端,**不做 `legacy` 兼容层**,见 ⑭-D)。
- `GET /settings` 的 `llmProvider` / `llmKeySet` 两字段由 `providers: [{name, model, hasWebSearch, keySet, enabled}]` + `routes: {task: name}` 取代。

**测试与守门**
- 路由解析四态单测:有路由 / 缺路由回退默认 / provider 被禁用 / 无 key(返 `None` 且调用方走降级)。
- `has_web_search=0` 时 payload **逐字节**不含 `tools` / `search_query`(锁死)。
- 预算三本账互不透支单测(把检索预算调到 1s,断言推理与复盘预算不受影响)。
- 降级次序单测:预算不足时被丢的顺序恒为 T3 简评 → T2 细节,**篮子卡冻结永不被丢**。
- 全仓守门:任一 LLM 调用点未 import `prompt_context` → 单测挂。
- key 不外泄:`GET /settings*` 全部响应 grep 断言不含 `api_key` 值。

**依赖**:①。
**验收**:两条 provider(一带检索、一纯推理)可在 App 侧增删改并即时生效(不重启);任一 provider 故障时报告主体仍产出且缺席段诚实披露(单测两向);pytest 零回归。

**完工记录(2026-08-02,@builder)**:`_PROVIDERS` 枚举与 `ALLOWED_PROVIDERS` 白名单全部退役,`neckline/llm/factory.py::get_provider(task=None, *, db_path=None, settings_obj=None)` 改为纯 DB 驱动——读 `neckline/settings_store.py` 新增的 Provider 注册表 CRUD(`list_providers`/`get_provider_record`/`create_provider`/`update_provider`/`delete_provider`,均含 `ProviderRecord`〔内部,含明文 key〕/`ProviderPublic`〔安全视图〕两套 dataclass)+ 路由存取(`get_llm_routes`/`set_llm_routes`,写 `app_settings.llm_task_routes`/`llm_default_provider`),交给新建 `neckline/llm/router.py::resolve_task_provider_name()`(纯函数,九个任务常量 `TASK_*`/`ALL_TASKS` + 默认路由 `DEFAULT_SEARCH_TASKS`)决定 provider 名,查到行后直接构造裸 `OpenAICompatProvider`(不再实例化 `GLMProvider`/`KimiProvider`)。`neckline/llm/openai_compat.py` 的 `OpenAICompatProvider` 从"抽象钩子必须被子类覆盖"改为**可直接实例化**——新增 `name`/`api_url`/`has_web_search`/`search_engine` 四个可选构造参数,四个搜索钩子(`_search_tools`/`_handle_tool_call`/`_extract_top_level_search_hits`/`_search_engine_value`)各自有了通用默认实现(协议照搬 GLM 的 `web_search` 工具形状,`has_web_search=0` 时 `_search_tools` 直接返回 `None`);`GLMProvider`/`KimiProvider` 两个类**完整保留、行为逐字节不变**(降级为"预置参考实现",不再被 `get_provider()` import,仅供既有单测继续当"真实可用 provider"的测试替身)。新建 `neckline/llm/budget.py`:`BudgetLedger`(三本账独立字典,`spend`/`remaining`/`exhausted`)+ `DEGRADE_ORDER=(t3_brief, t2_review_detail)` + `NEVER_DROPPED` 机器判据。契约:`PUT /settings/llm` 已删;新增 `GET/POST /settings/providers`、`PUT/DELETE /settings/providers/{name}`、`GET/PUT /settings/llm-routes`;`GET /settings` 的 `llmProvider`/`llmKeySet` 由 `providers:[{name,model,hasWebSearch,keySet,enabled}]`+`routes:{task:name}` 取代(`neckline/api/schemas.py` 新增 `ProviderOut`/`ProvidersListOut`/`ProviderCreateIn`/`ProviderUpdateIn`/`SettingsProviderOut`/`LLMRoutesOut`/`LLMRoutesIn`,删 `SettingsLLMIn`)。既有 `api/inquiry.py` 问询台调用点接入 `TASK_INQUIRY`(**builder 推断**:一并归入 `DEFAULT_SEARCH_TASKS` 检索类默认路由——问询台现有实现本就 `enable_search=True`,不归入会在配好双 provider 后静默丢失搜索能力;plan 原文只举了 driver_search/news_scan 两个检索类例子,未提问询台,如与规划意图不符请澄清);`report/pipeline.py`(V1 候选审判,⑬ 将被篮子引擎取代)调用点保持 `task=None` 不变、未接入任务语义,判断理由见下方「发现的疏漏」。测试:新建 `tests/test_llm_router_budget.py`(16 例:路由纯函数边界 / 预算三本账互不透支 / 降级次序 / 全仓 `prompt_context` 守门含 `news_scan.py` 既有缺口的豁免登记 / key 不外泄);`tests/test_llm.py::TestFactory`、`tests/test_api_settings.py` 大幅重写(旧断言测的是已退役的 `.env`/枚举语义)。`python -m pytest tests/ -q`:2023(基线)→2056(**净增 33**,2 fail 同名同因未变——`git stash` 复核过与本块无关),真实 `data/neckline.db` 全量跑前后 MD5/mtime 双证不变。**发现的疏漏(如实登记,未改 Plan)**:① `neckline/llm/news_scan.py::scan_news_for_code` 调用 `provider.chat(...)` 但从未 import `prompt_context`(早于本块存在的既有缺口,已 `spawn_task` 记账,不在本块顺手修);② Plan 原文"两段式流水"的检索→推理编排与"单侧故障→`evidence_status='search_unavailable'`"披露逻辑,经核对 `evidence_status` 是 `baskets` 表(① 已建)的列、实际编排代码理应长在产生篮子的 ⑤ 块——本块只交付了使其可实现的路由/预算原语,未构造一个提前的、脱离篮子上下文的编排函数(避免在 ⑤ 定型篮子卡形状前臆造接口);③ `settings_store._default_settings` 现已无任何函数消费,仅为不破坏 `tests/conftest.py::api_env` 的 `monkeypatch.setattr` 而保留。

---

### V2-③ · 选股策略包机制:`selection_packs` + 原语白名单 + 四道闸激活 + 首包 K4-pack(@builder)✅ 完工(2026-08-02)

**目标**:§十二 全文落地。**包里装参数与规则声明,不装可执行代码**;执行引擎永远住系统线仓库。选股主链**从第一天就读现役包**(插槽不是空架子)。

**涉及模块 / 文件(全新包 `neckline/selection/`)**
- `engine_api.py`:`ENGINE_API_VERSION = 1`(**单一源**)+ `is_compatible(manifest) -> bool`;不兼容 → **拒绝激活、fail loud**(不静默降级)。
- `primitives.py`:原语注册表 `PRIMITIVES: Dict[str, Primitive]`。每个原语声明 = `name` / `kind`(filter | feature | sort_key)/ `inputs`(**只能引用白名单特征**)/ `params_schema` / `impl`。**特征白名单 `_ALLOWED_FEATURES`**:只允许来自预计算表与 EOD 面板的列(`industry_strength_daily.*` / `corr_matrix_daily.*` / `limit_cluster_daily.*` / `leader_structure_daily.*` / `limit_derived.*` / `daily*` / `moneyflow_dc.*` / `k4_advisory.*`),**LLM 产出的任何字段一律不在白名单内**(`_SORT_KEY_INPUTS` 体例平移,单测锁死)。
- `pack.py`:manifest / config 的 JSON Schema 校验 + 装载 + `get_active_pack(db_path=None) -> Pack`(读 `selection_packs` 现役行,**缓存按 `pack_version` 失效**)。
- `scripts/activate_pack.py`:**复刻 `activate_charter.py` 四道闸** —— ① JSON Schema 校验;② 原语 / 白名单核对 + `engine_api_version` 兼容;③ **默认演练模式**(打印 diff:哪些参数变了、影响哪些块);④ `--confirm` 才写(单事务:旧行 `is_active=0`、新行 `is_active=1`、`selection_pack_activation_log` 追加两条事件)。**回滚 = 激活旧版本行**(历史行只读、不改写)。

**包格式(定死)**
```json
{ "manifest": { "pack_version": "K4-pack-v1", "name": "K4 机械半身首包",
                "date": "2026-08-XX", "engine_api_version": 1,
                "evidence_ref": ["research/k4_assembly_report.md"] },
  "config":   { "seeds":  { "<primitive_name>": {…params…} , … },
                "tier":   { "weights": { "sector_strength": 0.25, "driver_freshness": 0.20,
                                          "leader_clarity": 0.25, "tradability": 0.20,
                                          "card_density": 0.10 },
                            "dims": ["sector_strength", …] } } }
```

**插槽边界(定死,勿扩)**:包只管 **①扫描层的驱动种子生成规则**(用哪些过滤器、什么阈值)与 **③Tier 的机械分维度选择与权重**。②驱动聚合的两道机械闸、④篮子卡冻结体例、⑤复盘与评价引擎 = **引擎本体,不进包**;**纪律章程不进包**(两条版本线、两张表、两套激活流程,防双权威漂移)。

**首包 K4-pack**(⑬ 拆 V1 之前,把「现成选股代码」的可配部分抽出来):从 `report/intel_candidates.py` 抽 —— 个股卫生线阈值、`NON_NEW_MIN_DAYS=120`、K4 安检开关(`hard_cut` 拦 / `avoid_flag` 标)、行业闸 `INDUSTRY_GATE_MIN_LIFT=2.0`、情报排序三级键的权重表示。**五常驻板块保底与竞争位不进包**(裁定 #9 已删,不给它留复活入口)。

**已知代价(如实登记,勿当 bug)**:声明式的表达力受限于引擎原语集 —— 策略线每想用全新玩法,须**先等系统线加原语**(一次小版本施工,带单测),代码永远不随包传输。此代价刻意接受:它把「策略变更必须过门禁」从流程约束变成**结构约束**。

**测试与守门**
- Schema 正 / 负例;引用不存在的原语 → 拒绝;引用白名单外特征 → 拒绝;`engine_api_version=2` 的包在 `ENGINE_API_VERSION=1` 的引擎上 → 拒绝且退出码非 0。
- 演练模式**零写库**(断言库文件 MD5 不变);`--confirm` 后 `is_active` 唯一 + 日志两行。
- **插槽真被消费**:同一份输入数据,换一个包(改一个阈值 / 改一组权重)→ ④ 的种子集与 ⑥ 的 Tier 序**跟着变**(单测锁死;这条是「插槽不是空架子」的机器判据)。
- 全仓 grep:`neckline/selection/` 下**不出现**可配阈值的模块级字面量(白名单里显式列出确属引擎常量的项 + 写理由)。

**依赖**:①。
**验收**:`python scripts/activate_pack.py --file packs/K4-pack.json` 演练输出可读 diff 且零写库;加 `--confirm` 后现役唯一;`get_active_pack()` 被 ④⑥ 真实消费(换包单测绿);pytest 零回归。

**完工记录(2026-08-02,@builder)**:全新包 `neckline/selection/`(`__init__.py`/`engine_api.py`/`primitives.py`/`pack.py`)+ `scripts/activate_pack.py` + 首包 `packs/K4-pack.json` 一次到位。`engine_api.py`:`ENGINE_API_VERSION=1` 单一源,`is_compatible()` 显式排除 `bool`/`float`(`True==1`/`1.0==1` 两个 Python 陷阱会让裸 `==` 误判兼容,已用单测锁死)。`primitives.py`:`_ALLOWED_FEATURES` 八模式逐字照抄 plan 原文;`Primitive` 冻结 dataclass 两道白名单锁——①构造期 `__post_init__` 核对 `inputs⊆白名单`,违反即 `ValueError`(fail loud);②运行期 `validate_all_primitives_whitelisted()`(`activate_pack.py` 闸 2 防御性复核 + 单测用合成坏注册表验证有牙齿)+ 各原语单测用"记录实际访问键"的字典子类证明 `impl` 运行时真的不读 LLM 产出字段(`_SORT_KEY_INPUTS` 体例平移);参数 schema 校验手写(未引入 `jsonschema`,§3.1 钉死依赖清单没有它)。首包 5 个原语与来源:`stock_hygiene`(← `research/panel.py::base_universe_expr` 的 `close≥2.0`/`amount_ma20≥20000`/`ma20`非空 + `intel_candidates.py::_ALLOWED_BOARDS`)、`non_new_stock`(← `NON_NEW_MIN_DAYS=120`)、`k4_advisory_gate`(← K4 安检语义:`hard_cut`拦截出池/`avoid_flag`打标保留)、`industry_dominance_gate`(← `INDUSTRY_GATE_MIN_LIFT=2.0`)、`intel_rank_priority`(← `_sort_key` 三级键顺序,**顺序即权重**——用有序 `dims` 列表而非发明数值权重,因为原函数是字典序比较而非加权和,数值权重会歪曲原语义)。`pack.py`:`validate_manifest`/`validate_config`/`validate_pack_doc` 三段式校验;`Pack` 冻结视图 + `seeds_config`/`tier_weights`/`tier_dims` 访问器;`get_active_pack()` 缓存**按 `(db_path, pack_version)` 双键失效**(不是只按版本号——测试隔离下多个临时库可能恰好用同一版本号字符串,单键缓存会跨库串味,已用专项单测锁死);`activate_pack()` 单事务(append-only 落新行或识别到内容相同的既有行幂等复用 + 激活切换 + 事件追加,已现役重复调用 0 事件、切换 2 事件〔先 deactivate 旧、后 activate 新〕、首次激活 1 事件)。`scripts/activate_pack.py` 四道闸对齐 `activate_charter.py` 体例但按包的性质简化(无"清仓"闸、无"核心值核对"闸——包没有章程那种固定拍板核心值概念)。**手工演练中发现并修复一个真实顺序漏洞(单测已固化回归)**:最初实现里"目标已现役→直接返回"的快捷退出跑在 append-only 完整性核对**之前**,导致"文件被篡改但版本号没改、且该版本恰好当前现役"这种情况被快捷退出直接放过、完整性问题静默过关;修法 = 完整性核对提到快捷退出之前,新增 `test_confirm_rejects_tampered_content_even_when_currently_active`/`test_confirm_rejects_tampered_content_when_not_currently_active` 两个回归测试锁死两种触发路径。测试:新增 4 个文件共 123 例(`test_selection_engine_api.py` 5、`test_selection_primitives.py` 67、`test_selection_pack.py` 36、`test_activate_pack_script.py` 15),另把 `get_active_pack` 补进 `test_db_isolation_guardrail.py::_BANNED_BARE_CALLS`(同 `brain.get_active`/`active_config` 一样有"裸调用命中真实开发库"的坑)。`python -m pytest tests/ -q`:2056(基线)→**2179**(净增 123,零回归;2 fail 同名同因未变——`test_api_positions.py::test_buy_date_historical_trading_day_written_through`/`test_fix_position_buy_dates.py::test_unchanged_position_does_not_bump_updated_at`,今日 2026-08-02 实系周日,与既有"周日触发的时间炸弹"描述吻合,不修不新增)。真实 `data/neckline.db` 双重验证零写入:①全量 pytest 前后 MD5/mtime 逐字节不变;②手工 `python scripts/activate_pack.py --file packs/K4-pack.json`(无 `--confirm`,默认指向真实库)dry-run 前后 MD5/mtime 逐字节不变,输出可读 diff、exit=0——**真实库当前仍无任何 `selection_packs` 行,首包灌入留给 ⑯ 尾声**,全程未跑 `--confirm` 碰真实库。红线自查:全程未 import `neckline.strategy.brain`、未碰 `strategy_versions`、`packs/K4-pack.json` 不含 stop_pct 等章程参数;纯本地,零服务器/DNS/部署改动。

**如实登记三处设计判断(均未改 Plan,如与规划意图不符请澄清)**:① **身份/分类数据不计入白名单 `inputs`**——`board`(`data/board.py` 代码前缀纯函数分类)/`is_st`/`industry`(均出自 `stock_basic`)是全局参考数据而非"判断特征",不在 plan 给的 8 个模式内,判断为设计上的合理排除而非遗漏,原语 `impl` 内部仍可用它们过滤,只是不计入受白名单约束的 `inputs` 声明。② **"情报排序三级键"放进了 `config.seeds` 而非 `config.tier`**——plan「插槽边界」原文只点名两个可配区(①扫描层过滤器/阈值、③Tier 权重),但「首包 K4-pack」的五项清单把三级键与其余四个 seeds 类阈值并列同列,故按后者归入 `seeds.intel_rank_priority`;④ 市场扫描层落地时如判断它更该属于别处,是那个块可以调整的实现细节,不是本块结论。③ **`config.tier.weights` 五个数值(0.25/0.20/0.25/0.20/0.10)逐字取自 plan 本节"包格式定死"示例本身,不是从现有代码抽取**——五维机械分是 V2 全新概念,V1 代码里没有对应实现可抽,plan 示例里的具体数值经核对不见于 `research/` 任何研究文档,应是 planner 拟定该格式定义时一并给出的初始值,已如实沿用而非另行拟定。**已知的一条测试局限(未违反验收,如实登记)**:「插槽真被消费」的验收原文点名"④ 的种子集与 ⑥ 的 Tier 序跟着变",但分块序列里 ④⑤⑥ 依赖 ③(不是反过来),本块落地时这两个模块尚不存在;已在 `tests/test_selection_pack.py::test_slot_is_really_consumed_not_an_empty_shell` 与 `test_selection_primitives.py` 的多个 "*_params_change_outcome"/"*_dims_param_changes_outcome" 用例里,在唯一已存在的层面(原语 `run()` + `Pack` 访问器)证明换包真会改变输出,待 ④⑥ 落地后应在各自块内用真实种子集/Tier 序重跑一遍这条验证。

---

### V2-③-K7 · 原语与白名单跟进 + `packs/K7-pack.json`(K7 需求 4;@builder,**③ 主体已完工,本子项是 K7 追加施工项**)✅ 完工(2026-08-02)

> **来源**:`archive/交接_K7_系统线需求_20260802草案.md` 需求 4(2026-08-02 用户转发)。**跨线协议照旧:原语先进引擎,包才能引用** —— 本子项是「先进引擎」那一步,K7-pack 只是它的下游产物。
> **⛔ 边界**:本子项**只加不改**,③ 已完工的 5 个原语语义一字不动。
> ⚠ **「K4-pack 原样不动」的准确范围(别理解过宽,否则 ④ 没法干活)**:需求 2 说的是 **Tier 五维权重的占位值**(`0.25/0.20/0.25/0.20/0.10`)**不因 K7 而改、不为此重发 K4 版本** —— 证据化初值只落 K7-pack。它**不禁止** ④ 在自己的施工里给 `packs/K4-pack.json` 补 ④ 种子原语所需的参数节(那是 ④ 本来就要做的事,与 K7 无关)。**③-K7 自身则一行都不改该文件**(基准 = ③-K7 开工时的文件状态)。
> ⚠ 另:`selection_packs` 表当前**真实库里还是 0 行**(③ 完工记录:首包灌入留到 ⑯),所以现在改包文件不触发 `activate_pack.py` 的 append-only 完整性闸;**一旦某个版本被 `--confirm` 写进库,它的文件内容就冻了**(篡改会被闸拒),届时任何改动都必须换新版本号。

**③-K7-A 白名单扩容**(`neckline/selection/primitives.py::_ALLOWED_FEATURES`)
- 新增模式 **`industry_stage_daily.*`**(④b 产出的阶段表)。
- **确认** `leader_structure_daily.*` 已在白名单内(③ 完工记录称八模式逐字照抄 plan,含此项)**且其 RS 排名列 `rs_rank` 可被 `sort_key` 类原语引用** —— 若现实现只允许 `filter`/`feature` 引用它,本子项补齐 `sort_key` 通路。
- ⚠ 白名单只收**预计算表与 EOD 面板的列**;**LLM 产出字段永远不在内**(单测锁死,`_SORT_KEY_INPUTS` 体例)。

**③-K7-B `intel_rank_priority` 的 `dims` 取值扩容**
- `params_schema` 支持新 dims:**`industry_stage_score`**(读 `industry_stage_daily`)、**`leader_rs_rank`**(读 `leader_structure_daily.rs_rank`)。
- 既有三个取值(`industry_rank` / `industry_persist_days` / `yellow_card_count`)**保留可用**(K4-pack-v1 还在引用它们,是回滚锚,不许失效)。
- **顺序即权重**的既有语义不变(③ 完工记录:有序 `dims` 列表 = 字典序比较,不是加权和)。K7-pack 的排序键 = `["industry_rank", "industry_stage_score", "leader_rs_rank", "yellow_card_count"]`。
- **方向必须显式声明,不许靠猜**:`industry_rank` / `leader_rs_rank` 是**名次(升序为优)**,`industry_stage_score` 是**分数(降序为优)**,`yellow_card_count` **升序为优**。现实现若把 dims 一律当同向处理 → 本子项必须把每个 dim 的方向做成原语声明的一部分(单测:同一份数据下 `industry_stage_score` 高的排前面)。

**③-K7-C `tier` 节的 schema 扩展 + `engine_api_version` 判断**(需求 4 末条,**fail loud 不静默兼容**)
- K7-pack 的 `config.tier` 需要携带**五态打分映射**(需求 1b:「打分映射必须做成 pack 可配参数」)。
- **判定规则(定死)**:若新增字段能以**纯增量、旧包不受影响**的方式加入(即 `K4-pack-v1` 原样重新校验仍通过、`get_active_pack()` 对旧包行为逐位不变)→ **`ENGINE_API_VERSION` 保持 1**;若为容纳它必须改动既有字段的形状 / 必填性 / 语义 → **bump 到 2 并 fail loud**(旧包在新引擎上拒绝激活并给出可读原因,退出码非 0)。**两条路都要在完工记录里写明走的是哪条、依据是什么**,不许含糊。
- **落点定死**:`config.tier.stage_scores`(与 `weights` / `dims` 平级),**新增可选键**;缺省时 `driver_freshness` 维度走「阶段表缺行」的保险丝路径(见 ④b),**不静默给 0 分**。

**③-K7-D 字典键一律英文枚举码(planner 定案,与交接稿草案 JSON 的中文键**刻意不同**)**
- 交接稿草案里 `stage_scores` 用中文键(`"发酵"` / `"启动"` …)。**改为英文枚举码**:`ignition`(启动)/ `fermentation`(发酵)/ `overheat`(过热)/ `divergence`(分歧回调)/ `ebb`(退潮)/ `none`(无题材)。
- **理由(照项目既有纪律,不是口味)**:①`CandidateOut.board` 先例 —— **库与契约用英文枚举码、中文只做客户端展示层换算**(项目 CLAUDE.md 明文);②中文键在 JSON / SQLite / 日志 / grep 守门里易踩编码与不可见字符的坑;③`industry_stage_daily.stage` 列也用同一套码,**配置键与库列值同源**才能写交叉断言。
- **中文名只出现在展示层**(报告 markdown / 客户端 / 卡面文案),映射表单一源住 `neckline/scan/stage.py`。

**③-K7-E `packs/K7-pack.json` 落地**
- 按交接稿附录草案落盘,**四处按引擎实际 schema 定稿**:`stage_scores` 键换英文码(③-K7-D)、`date` 填**原语落地当日**、`engine_api_version` 填 ③-K7-C 的实际结论、`evidence_ref` = `["research/k7_pre_report.md", "research/k7_pre2_report.md"]`。
- `config.seeds` 的卫生线 / 非次新 / K4 安检 / 行业闸四项**承 K4-pack 不变**(交接稿明文:K7 的变更集中在排序键、Tier 权重与两个新维度)。
- `config.tier.weights` = 需求 2 的证据化初值(见 ⑥);`dims` 五项照旧。
- ⚠ **本子项只产出文件 + 演练通过,不激活**;激活排在 **⑯-E**(尾声块,守「⑮ 完工前不碰服务器」)。

**测试与守门**:白名单新模式的正负例;`leader_rs_rank` 可被 sort_key 引用的用例;新 dims 的方向性单测(升序 / 降序各一);**K4-pack-v1 逐字节重新校验仍通过 + `get_active_pack()` 对它的输出逐位不变**(回滚锚不许被本子项打坏 —— 这条是硬判据);`K7-pack.json` 过四道闸演练(**零写库**,断言库文件 MD5 不变);若 bump 了 `engine_api_version`,补「旧包在新引擎上被拒 + 退出码非 0 + 原因可读」单测。

**依赖**:③(已完工)。**可与 ④ / ④b 并行**(不同文件)。⚠ ③-K7-A 的 `industry_stage_daily.*` 白名单项**可以先加**(白名单是声明,不要求表已存在),但引用它的原语单测要等 ④b 的表 DDL 就位。
**验收**:上述守门全绿;`packs/K7-pack.json` 演练 exit 0 且输出可读 diff;**③-K7 自身对 `packs/K4-pack.json` 零改动**(以本子项开工时的文件状态为基准做 diff,不是与 git HEAD 比 —— ④ 可能已合法地往里加过种子参数);**K4-pack-v1 的 Tier 五维权重仍是占位值** `0.25/0.20/0.25/0.20/0.10`(单测断言,防被 K7 初值顺手覆盖);pytest 零回归。

**完工记录(2026-08-02,@builder)**:`neckline/selection/primitives.py`:`_ALLOWED_FEATURES` 增第 10 个模式 `industry_stage_daily.*`;`intel_rank_priority` 新增 `_RANK_DIM_DIRECTIONS`(5 个合法维度 → `asc`/`desc` 显式声明表:既有三维 `industry_rank`/`industry_persist_days`/`yellow_card_count` 全 `asc`,新增 `industry_stage_score` 为 `desc`〔分越高越优先〕、`leader_rs_rank` 为 `asc`〔名次越小越优先〕);`_intel_rank_priority` 改为按方向表取值(`asc` 原样、`desc` 取负号再入元组字典序比较),缺值仍恒 `+inf` 排最后(与方向无关);引用未登记方向的维度名直接 `ValueError`(fail loud,不猜);`inputs` 元组补 `industry_stage_daily.stage`/`leader_structure_daily.rs_rank` 两条来源声明。`neckline/selection/pack.py`:新增 `_validate_stage_scores()`(`config.tier.stage_scores` 可选键,键必须 ⊆ `neckline.scan.stage.STAGE_ORDER` 六个英文码、值必须数值,不要求六态全覆盖)接入 `validate_config()`;`Pack` 新增 `tier_stage_scores()` 访问器(镜像既有 `tier_weights`/`tier_dims`,缺省空字典)。**跨包引用**:`pack.py` 新增 `from neckline.scan.stage import STAGE_ORDER`(单一源,不复抄六码元组)——`neckline/scan/seeds.py` 早已反向依赖 `neckline/selection/pack.py`,这是同一对包之间的另一条边,已核实 `neckline/scan/__init__.py` 无预加载子模块、`stage.py` 自身零 import `neckline.selection`,不构成循环。

**`ENGINE_API_VERSION` 判定(③-K7-C,定死走的哪条、依据是什么)**:**保持 1,不 bump**。依据:本子项三处改动(白名单加一个模式、`intel_rank_priority.dims` 新增两个可选取值、`tier` 新增可选键 `stage_scores`)对 K4-pack-v1 全部是纯增量——① 白名单只加不减;② K4-pack-v1 的 `dims` 默认值与既有三维方向全部是 `asc`,与扩容前"直接取值参与字典序比较"数值等价(`3 == 3.0` 逐位相等,单测 `test_k4_pack_v1_still_validates_and_activates_identically_after_k7_extension` 锁死);③ `stage_scores` 完全不存在于 K4-pack-v1,`validate_config` 对没有这个键的包连分支都不进。`pack.validate_pack_doc(K4-pack.json)` 与 `get_active_pack()` 对它的输出逐位不变,回滚锚成立。

**`packs/K7-pack.json` 最终形状**:`pack_version="K7-pack-v1"`,`date="2026-08-02"`,`engine_api_version=1`,`evidence_ref` 两篇研报路径照抄交接稿。`config.seeds` **收录全部 9 个已注册原语**(与 K4-pack.json 的完整性惯例一致——`test_real_k4_pack_file_...` 早有"九个原语一个不多一个不少"的单测先例;交接稿附录草案只列了 5 个是因为起草时 ④ 尚未落地);其中 `stock_hygiene`/`non_new_stock`/`k4_advisory_gate`/`industry_dominance_gate`/`hot_industry_seed`/`surging_concept_seed`/`limit_cluster_seed`/`anomaly_cluster_seed` 共 8 个与 K4-pack.json **逐字节相同**(交叉断言单测锁死),唯一改动的是 `intel_rank_priority.dims`(换成四维排序键)。`config.tier.weights` = 需求 2 证据化初值(`0.30/0.30/0.10/0.20/0.10`),`dims` 五项照旧,`stage_scores` 六态全覆盖、英文码(`ignition:0.6 / fermentation:1.0 / overheat:0.0 / divergence:0.4 / ebb:0.2 / none:0.2`,与需求 2 表格逐字对应)。**四道闸演练**:`validate_pack_doc` 零错误、`primitives.validate_all_primitives_whitelisted()` 零错误、`engine_api.is_compatible()` 为真、`scripts/activate_pack.py --file packs/K7-pack.json`(隔离临时库,不带 `--confirm`)exit 0、闸 1/2 通过、diff 逐项列出 9 个 seeds 键 + 5 个 tier 权重 + `stage_scores` 全部标"← 改动"、dry-run 后现役仍为空——全部经自动化单测复现(`tests/test_activate_pack_script.py` 新增 3 例),另在会话中手工跑过一次核对输出可读性(临时 db,已删除,未碰真实 `data/neckline.db`)。

**③ 完工记录挂账的"换包→跟着变"测试局限,本子项补真实版本**:③ 当年只能在原语 + 包层面用合成迷你包代理验证(④/⑥ 未施工)。现 ④ 已完工,`tests/test_selection_pack.py::test_real_k4_pack_vs_k7_pack_intel_rank_priority_dims_and_ranking_differ` 改用仓库里两份真实文件装载 + 激活,证明 `intel_rank_priority.dims` 真实配置不同、且同一份候选数据在两包下排序结果真的翻转(不是配置字符串表面不同);`tests/test_scan_seeds.py::test_generate_seeds_identical_under_real_k4_and_k7_pack_files` 同样用两份真实文件驱动真实 `generate_seeds()`,证明四类种子资格判断参数按设计保持逐位不变(K7 的变更不含 scan 种子阈值)。**"Tier 序"那半仍留给 ⑥** 落地后用真实 Tier 引擎重跑,不在本子项范围。

**测试**:四文件净增 24 例(`test_selection_primitives.py` +7:白名单正例 1 + `leader_rs_rank`/`industry_stage_score` 方向性 2 + 缺值恒排最后 1 + K7 四维端到端排序 1 + 未登记维度 fail loud 1 + K4-pack-v1 回滚锚 1;`test_selection_pack.py` +13:`stage_scores` 校验正负例 8 + K4 回滚锚 1 + K7 真实文件 schema 1 + K4/K7 交叉断言 1 + `tier_stage_scores()` 访问器 1 + 真实包驱动排序翻转 1;`test_activate_pack_script.py` +3:K7 四道闸 dry-run 三例;`test_scan_seeds.py` +1:真实包驱动 `generate_seeds()` 一致性)。`python -m pytest tests/ -q`:2371(基线)→**2395 passed + 2 skipped + 2 failed**(净增 24,零回归;2 fail 仍是同名同因的既有周日时间炸弹——`test_api_positions.py::test_buy_date_historical_trading_day_written_through`/`test_fix_position_buy_dates.py::test_unchanged_position_does_not_bump_updated_at`,与本子项文件面完全不相交,今日 2026-08-02 实系周日)。

**如实登记(均未改 Plan,如与规划意图不符请澄清)**:① **K7-pack.json 的 `config.seeds` 收录全部 9 个原语而非交接稿草案的 5 个**——交接稿附录写"按引擎实际 schema 定稿"且明写"四处"具体修正(均不含新增 seeds 键),本子项判断"完整收录、值与 K4-pack 逐字相同"比"沿用草案的部分收录"更贴合项目"不静默留白"的一贯纪律、也与 K4-pack.json 自身的完整性惯例(既有单测断言"九个原语一个不多一个不少")一致,两种写法运行时行为逐位等价(缺省即取 `Primitive` 自身 schema 默认值,与 K4-pack 显式值相同)。②**未新增 `params_schema` 层面的 `dims` 取值枚举校验**(如给 `intel_rank_priority.dims` 加一个"合法维度白名单"结构化校验、让 `activate_pack.py` 闸 1 就能拦下拼写错误的维度名)——Plan 原文只要求"每个 dim 带显式方向声明",未要求 schema 级 enum;现方案用 `_RANK_DIM_DIRECTIONS` 在 `impl` 运行时 fail loud(未登记维度名直接 `ValueError`),与既有 `dims` 参数"运行时才发现拼写错误"的既定深度一致,未额外加码,判断为合理的最小改动而非遗漏。③ **新增 `Pack.tier_stage_scores()` 访问器**——Plan 未逐字点名要求这个方法,但它与既有 `tier_weights()`/`tier_dims()` 是同一族读法(⑥ 落地时必然需要一个读 `stage_scores` 的入口),照既有体例顺手补齐,不算越权新增功能。

**并发说明**:施工期间 `neckline/selection/aggregate.py`(⑤-b/⑤-c 另一 builder 在制品)与新出现的未跟踪文件 `neckline/selection/member_hygiene.py` 全程未碰、未读、未 add。git 提交只包含本子项自己改动的文件。

---

### V2-④ · ①市场扫描层:三张预计算表 + 驱动种子(@builder)✅ 完工(2026-08-02)

**目标**:16:05 数据更新后批算,产出**驱动种子集**(热点行业 / 暴起概念 / 涨停簇 / 异动簇)交 ⑤。**全部 EOD 预计算落表、在线只读**(P0-23 纪律)。

**继承不改(明确写出,防误删)**:全市场特征面板(`research/panel.py`)、行业强度表(`report/industry_strength_store.py`,**只读表不现算**)、概念板块强度(`report/sectors.py` + `data/concept_data.py` 的 `ths_daily` 日更自愈)、资金流(`report/sector_moneyflow.py`)、涨停梯队(`data/limit_derived.py` + `data/top_list.py`)、个股卫生线、K4 红黄牌镜像(DB `k4_advisory`)、行业闸 lift。

**新造(新包 `neckline/scan/`)**
- `corr.py` → `corr_matrix_daily`:滚动 20 日收益相关。**只算簇内 / 板内对**(候选对集合 = `limit_cluster_daily` 的簇成员 ∪ 概念成分),**不算全市场 N²**;`n_obs` 不足窗口 → `corr=NULL`(禁写 0)。
- `cluster.py` → `limit_cluster_daily`:同日涨停 + 连日涨停,按 (行业 ∪ 概念成分) 聚类;`cluster_key = crc32(trade_date|anchor)` 十六进制(**跨进程可复现**)。
- `leader.py` → `leader_structure_daily`:簇内 RS 排名 / 连板高度 / 成交额占比 → `role_mech`。⚠ **rank 必须先排定确定性 tie-break 再 `ordinal`**(体例:`(指标降序, ts_code 升序)`;`rank(method="ordinal")` 的并列由行序打散 = 不确定性,2026-07-29 真数据打出来过)。
- `seeds.py`:四类种子的生成 —— 过滤器选择与全部阈值 **一律 `get_active_pack().config["seeds"]` 读取**,禁模块级字面量。
- `scripts/scan_layer.py {refresh,verify,bootstrap}`:照 `scripts/industry_strength.py` 体例(日更增量**只读当日一个分区**;`bootstrap` 分块两遍法;`verify` 三项自检 = 交易日无洞 / 键自洽 / 口径指纹一致)。
- `scripts/daily_update.py` 挂载 `refresh`(尽力而为不改退出码,但失败打 **ERROR** + 打印补算命令原文)。

**保险丝(四态,语义不合并)**:缺行 → ⑤ 的种子集为空 → **当日无篮子是合法输出**(报告如实写「今日未形成篮子」),不许现算自愈、不许拿昨日的凑数;`dataFreshness` 新增 `scanLayerDate` / `scanLayerLagDays` / `scanLayerStale` 三键(**与既有板块三键、行业强度三键不合并**)。

**依赖**:①③。
**验收**:三表 `bootstrap` + 单日 `refresh` + `verify` 三项自检全绿;**三路等价单测**(全量算 ≡ 逐日递推 ≡ 落表读回)在随机 3 日上逐位一致;确定性单测(同一天跑两次 `cluster_key` / `rs_rank` 逐位相同);换包 → 种子集跟着变;grep 守门:在线模块(`api/` / `report/pipeline.py` / `api/inquiry.py`)零现算入口;pytest 零回归。

**完工记录(2026-08-02,@builder)**:全新包 `neckline/scan/`(`__init__.py`/`cluster.py`/`corr.py`/`leader.py`/`seeds.py`/`freshness.py`/`verify.py`)+ `scripts/scan_layer.py`(`{refresh,verify,bootstrap}`)+ `scripts/daily_update.py` 挂 `update_scan_layer`(排在 `update_industry_strength` 之后,WARNING 级"尽力而为",理由见下)。`neckline/selection/primitives.py` 新增 4 个原语(`hot_industry_seed`/`surging_concept_seed`/`limit_cluster_seed`/`anomaly_cluster_seed`)+ 白名单扩至 9 模式(`ths_daily.*`);`packs/K4-pack.json` 的 `config.seeds` 补 4 个新键(承 ③ 完工记录已预告"④ 可以给它加自己的种子参数,不算破坏 K4-pack 原样不动的范围")。

**三张事实表算法要点**:`cluster.py` 把当日 `limit_derived` 命中行按 (行业∪概念成分) 聚类,`same_day`(当日涨停)与 `consecutive`(`consec_limit_up_days>=2`)两种 `cluster_kind` 各自算,`cluster_key=crc32(trade_date|cluster_kind:anchor_type:anchor_value)`;`MIN_CLUSTER_SIZE=2`(工程常量,孤身涨停不算共振)。`corr.py` 候选对 = 当日簇成员(读 `cluster.py` 产出)∪ 概念成分(`board_pool.apply_hygiene` 过滤宽基标签),`code_a<code_b` 每对只存一次,`corr=NULL` 覆盖"样本不足"与"常数序列标准差为0"两种"算不出"情形(纯 polars 实现,不引入 numpy 作新的直接依赖)。`leader.py`:**RS 口径与 tie-break 已按施工期收到的策略线 K7 交接稿需求 1a 对齐**(见下「施工期间收到的中途修正」)。

**四类种子生成(`seeds.py`)**:阈值全部经新增 4 原语从 `get_active_pack().config["seeds"]` 读取,不落表(读的三张事实表 + `industry_strength_daily` + `ths_daily` + `daily_basic` 均已够便宜,现算即可);`member_codes` 是**未经二次筛选的原始成分**(行业/概念/簇全部成员),成员精选与角色标注留给 ⑤。`generate_seeds()` 无现役包时返回 `None`(如实披露,不造默认阈值,是 `get_active_pack()` docstring 原文预留的分支)。

**施工期间收到的中途修正(K7 交接稿需求 1a,已落地并入本次完工记录)**:施工中段收到策略线 K7 交接稿(`archive/交接_K7_系统线需求_20260802草案.md`)转发,定死 `rs_rank` 口径与 tie-break:①RS 口径 = **RS20**(`ret_20d`,窗口不满 20 个交易日观测 → 该票 `rs_rank=None`,不用近似值顶替,`MIN_OBS_FOR_RS` 从最初的 2 改为等于 `PRICE_WINDOW_DAYS`);②tie-break 定死为 **RS 降序→成交额降序→ts_code 升序**(最初实现只有"RS 降序→ts_code 升序"两级,已补成交额中间级);③**连板高度与成交额头名均不进 `role_mech`/`rs_rank` 主定义**——最初实现曾把连板高度当综合序主键判定 leader/core/elastic,现已改正为**纯粹从 `rs_rank` 派生**(`rs_rank==1→leader`,`rs_rank<=1+n_ranked//2→core`,其余 `elastic`,`n_ranked`=簇内有效排名成员数不含 `unknown`),`limit_height`/`amount_share` 两列继续照常产出(供未来"双尾警示标注"等场景使用),只是不再喂进头名判断。此修正等价于施工图后来追加的 **V2-④b-0** 子项(K7 需求 1a 的具体化);Plan 原文写明"若④尚未完工→由④一并落地",本块完工时确认已满足④b-0 的全部要求(含其点名的"RS 相同断成交额、成交额也相同断 ts_code"两级单测),④b 后续落地时**不需要重做这一段**,只需引用本记录。

**规模控制的实测调参**:`corr.py` 的 `MAX_SCOPE_MEMBERS_FOR_CORR` 最初拟定 60,用 2026-07-22~24 三个真实交易日冒烟后发现单日近 10 万行(多个概念板块贴着 60 人上限,`C(60,2)=1770` 对);按同一批真实数据重算,**调到 30 后单日降到约 4.3 万行、3 天批算耗时从 34s 降到 5.3s**,遂改为 30(不是性能门禁下的强制修正,是拿到真实数据后顺手做的合理调参,如实记账;详见 `corr.py` 模块 docstring)。

**如实登记(均未改 Plan,如与规划意图不符请澄清)**:① **"热点行业/暴起概念/涨停簇/异动簇"四类种子的具体判定算法(读哪张表、用哪个指标、什么阈值)在任何现存文档里都没有给出**(已逐字 grep 用户蓝图 `新版本量化交易APP与选股架构.md` 与 K7 交接稿,均无这四个复合词的具体定义)——四类各自的判定方式(热点行业=`industry_strength_daily` 排名+强度日;暴起概念=`ths_daily` 单日涨幅**而非** `sectors.py::board_age`/20日动量,后者已退役为判据数据源见项目 CLAUDE.md;涨停簇=直接读自己产出的 `limit_cluster_daily`;异动簇=`daily_basic.volume_ratio` 量比逐票判定后按行业/概念聚类)均为本块原创设计,已在各自函数 docstring 详述理由。②**`sector_moneyflow.py`(资金流)未被本块消费**——该模块明文"不参与任何评分/候选筛选"(STRATEGY_LAB K2 判决:板块资金无次日领先性),即便种子生成理论上算"识别当前热点"而非"预测",仍判断不宜在未经用户确认的情况下重新启用一个明确写着"不进筛选"的模块。③**异动簇未使用 `top_list.py`(龙虎榜)**——已考虑但改用 `daily_basic.volume_ratio`,理由:后者已在 16:05 日更主链必然就绪、无需额外网络请求,前者是"现拉现落盘"语义,引入会让扫描层意外依赖一次盘后网络抓取的成败。④**`seeds.py` 未复用 ③ 首包 5 个原语**(`stock_hygiene`/`non_new_stock`/`k4_advisory_gate`/`industry_dominance_gate`)**给 `member_codes` 做二次过滤**——判断"成员选择"字面写在 ⑤ 的职责范围(⑤ 目标句"合并驱动+…+成员选择+角色标注"),本块的种子 `member_codes` 是原始成分候选池,不是最终候选名单。⑤`scripts/scan_layer.py bootstrap` 未做 `industry_strength.py` 那样的"两遍法"——三张表均无跨日递推状态(与 `persist_days` 不同),`refresh`/`bootstrap` 是同一份"逐日循环"实现,已用三路等价单测证明批量与逐日结果逐位相同;Plan 原文"照 `scripts/industry_strength.py` 体例…`bootstrap` 分块两遍法"这半句字面上未采纳两遍法,如认为仍需要为大区间历史回填单独优化性能,留作 ⑯ 或未来专项。⑥`dataFreshness.scanLayer*` 三键(`freshness.py::scan_layer_status`)已实现函数本身,**未接线进 `report/pipeline.py::ReportOut`**——block 文件范围明确是新建 `neckline/scan/`,不含 `report/pipeline.py`,接线留给未来实际消费扫描层数据的块(⑤ 或 ⑭)。

**本地真实数据冒烟(只读 `data/neckline.db`+`data/parquet`,业务表写入隔离 SQLite 副本;真实库全程 MD5/mtime 双证不变)**:2026-07-22~07-24 三个真实交易日(43~129 只涨停/日),`scripts/scan_layer.py bootstrap --db <scratch> --parquet-dir data/parquet`:`limit_cluster_daily` 2323 行(同日簇 420 个、连板簇 100 个),`corr_matrix_daily` 43309 行(667 scope),`leader_structure_daily` 2323 行,`verify` 三项自检全绿,3 天总耗时 **5.3s**;`generate_seeds` 单独验证(2026-07-23,`industry_strength_daily` 未预填故热点行业=0,属预期)找到 2 颗真实暴起概念种子(可燃冰 +5.68%、柔性直流输电 +5.28%)+ 224 颗涨停簇种子 + 52 颗异动簇种子,人工核对均为合理结果。

测试:新增 9 个文件共 95 例(`test_scan_cluster.py` 9、`test_scan_corr.py` 8、`test_scan_leader.py` 8、`test_scan_seeds.py` 7、`test_scan_freshness.py` 3、`test_scan_verify.py` 12、`test_scan_layer_script.py` 7、`test_scan_layer_guardrails.py` 7、`test_daily_update_scan_layer.py` 3)+ `test_selection_primitives.py` 扩容(新增 4 原语行为单测 22 例 + 2 条既有"数量锁"断言随白名单/注册表增长同步改数)。`python -m pytest tests/ -q`:2179(基线)→**2274**(净增 95,零回归;2 fail 同名同因未变,今日 2026-08-02 实系周日,与既有"周日时间炸弹"描述吻合)。真实 `data/neckline.db` 全程只读:全量 pytest 前后 + 本地真实数据冒烟前后,MD5/mtime 均不变。红线自查:未碰任何服务器/DNS/部署;`packs/K4-pack.json` 新增键均是种子阈值,不含 stop_pct 等章程参数;`selection_packs` 真实库仍是 0 行(冒烟用的是隔离库内 `activate_pack(..., via="seed")`,未碰真实库)。

#### 🩹 2026-08-02 晚间定向快修:四类种子输出顺序确定性(⑨ 完工时实证发现的块外真洞,✅ 已修复)

**问题**(详见 V2-⑨ 段尾「一处提请裁定 + 一个块外真洞」):`_limit_cluster_seeds`/`_anomaly_cluster_seeds` 原实现按 `frame.group_by(["cluster_key"])` 迭代直接 append 进列表,而 **polars `group_by` 官方不保证顺序**(定向复现:同一 6 组的 DataFrame 连调 20 次 `group_by`,19 次顺序互不相同);其上游 `cluster.load_limit_clusters`/`cluster_members_by_anchor` 的 SQL `SELECT` 也未加 `ORDER BY`,同样不能当确定性的基础(`maintain_order=True` 治标不治本)。`⑤` 的 `_select_seeds()` 只取 `SeedSet.all_seeds()` 前 `MAX_SEEDS_AGGREGATED=20` 颗,前提是"每类内部各自有序"——该前提被违反后,同一 D0 同一库连跑会产出不同篮子(⑨ 冒烟实证)。

**修法**(仅改 `neckline/scan/seeds.py`):新增 `_sort_by_seed_key()`,四类种子(热点行业 / 暴起概念 / 涨停簇 / 异动簇,**全部核过,不只核被点名的两类**)的输出列表在交给 `SeedSet` 前一律按 `seed_key`(crc32 十六进制串,由行业名 / 概念代码 / 簇 anchor 等稳定业务标识派生,`cluster.make_cluster_key` 同手法,跨进程无随机盐)升序排定,不依赖任何上游行序——热点行业的 SQL `ORDER BY industry_rank`、暴起概念的 parquet 读回顺序目前看似已经稳定,但那是"恰好如此"而非契约保证,同样收口。

**验证**:新增回归测试 `tests/test_scan_seeds.py::test_generate_seeds_multi_item_categories_stay_ordered_across_repeated_calls`(四类各 ≥4 个合格项、DB 写入 / 构造顺序刻意打乱,同一天连跑三次断言逐位相同 + 顺序恰好等于 `seed_key` 升序);临时移除排序逻辑重跑此测试确认会炸(hot_industry 因未按 `seed_key` 排定而失败),证明测试真的识别回归而非巧合通过。**跨进程双跑**(`scripts/smoke_basket_review.py --d0 20260723`,两次独立 `python` 进程、各自 `sqlite3.backup` 出全新临时库、真实 parquet 只读):113 行 stdout 里仅 2 行不同(临时目录随机后缀;⑧ EOD 验证的审计时间戳 `first_falsified_at`——两者都与篮子数据无关,是预期差异),其余逐字节相同——种子 278 颗(hot_industry 0/surging_concept 2/limit_cluster 224/anomaly_cluster 52)、篮子 3 个(T1=1/T2=2/T3=0)、最大篮 `basket_key=dc92c25e`「柔性直流输电」两次相同、卡数 / EOD 验证判定 / 复盘计数 / 幂等重跑结果全部一致。`pytest tests/ -q` 改动范围内(`tests/test_scan_*.py`+`tests/test_selection_*.py`+`tests/test_eval_*.py`+`tests/test_basket_review.py` 共 21 个文件)686 过、0 失败。全量套件此刻另受两处与本次改动无关、逐一核实零耦合的扰动:并行施工的 V2-⑩(`neckline/positions.py`/`neckline/api/*`/`neckline/decision_log.py` 区域,32 个测试失败 + 4 个文件收集失败)、以及一处用真实 `datetime.now()` 的既有测试(`test_review_reconcile.py::TestRollbackDoesNotWhitewashHistory`)恰好在本次会话跨过 2026-08-02→08-03 零点时翻车(与"周日时间炸弹"同类但非同一条)——均未修,不在本次职责范围。

### V2-④b · `industry_stage_daily` 六态状态机 + `rs_rank` 口径追加(K7 需求 1b / 1a;@builder,**可与 ④ 并行**)✅ 完工(2026-08-02)

> ⚠ 排版修复(2026-08-02,@builder):本标题此前误粘在 ④ 完工记录段落末尾、缺失独立
> `###` 标题行(施工时发现的纯排版缺陷,不影响内容,顺手补齐,不改一个字的实质内容)。

> **来源**:`archive/交接_K7_系统线需求_20260802草案.md` 需求 1a/1b(2026-08-02 用户转发,证据链 `research/k7_pre_report.md` H10/H11)。
> **为什么单开一块、不塞进 ④**:① **④ 正在施工中,其规格已定型,插改会返工**;② 本表的数据源(`daily` + 现役 `industry_strength_daily` + `limit_derived` 涨停家数)与 ④ 三表的**簇 / 相关口径无耦合**,可并行;③ 它有**两个消费方**(⑥ 的 `driver_freshness` + K7-pack 排序键的 `industry_stage_score`),不专属 ⑥,埋进 ⑥ 会让 ④ 的种子排序拿不到它。
> **依赖**:①(建表体例)+ 现役 `industry_strength_daily`。**不依赖 ④**。

#### ④b-0 · ④ 的 `leader_structure_daily.rs_rank` tie-break 口径追加(K7 需求 1a,**口径级要求**)

- ④ 原文只给了 tie-break **体例**(`(指标降序, ts_code 升序)`);K7 需求 1a **把 `rs_rank` 的 tie-break 具体化为三级、定死**:**RS(`ret_20d`)降序 → 成交额降序 → `ts_code` 升序**。
- **谁做**:**若 ④ 尚未完工** → 由 ④ 一并落地(它本来就在写 `leader.py`);**若 ④ 已完工** → 作为 ④b 的第一件小改动补齐。**两种情况都要有单测锁死三级键**(造两只 RS 相同的票 → 断言成交额大的在前;再造成交额也相同的 → 断言 `ts_code` 小的在前)。
- **这不是对 ④ 的推翻,是它的具体化**:确定性 tie-break 本就是 ④ 的硬要求,K7 只是把中间那一级从「无」定成「成交额降序」(理由:成交额是簇内相对强弱的天然次序,比直接掉到 `ts_code` 字典序更有语义)。
- ⚠ 连带:③-K7-B 的 `leader_rs_rank` 排序键读的就是这一列,口径不定死则排序键不可复现。

#### ④b-A 五态状态机(定义定死,per 行业 per 交易日,**互斥,优先级从上往下**)

| 码(库与配置唯一键) | 中文名(仅展示层) | 判据 |
|---|---|---|
| `ignition` | 启动 | 强度日 且 `persist == 1` |
| `fermentation` | 发酵 | 强度日 且 `persist ∈ [2, 3]` |
| `overheat` | 过热 | 强度日 且 `persist ≥ 4` |
| `divergence` | 分歧回调 | 非强度日 且 近 **2** 个交易日内有强度日 且 当日该行业涨停家数 **≥ 1** |
| `ebb` | 退潮 | 非强度日 且 近 **5** 个交易日内有强度日 且 当日该行业涨停家数 **= 0** |
| `none` | 无题材 | 其余 |

- **强度日定义(与现役 `industry_strength_daily` 同族口径,不另起一份)**:行业当日成员 `ret_1d` **中位数进全行业 top 20%**(`q = 0.80`),且**成员数 ≥ 5**。⚠ **`persist` 直接读 `industry_strength_daily` 的既有 `persist_days` 递推列,不重算**(单一源纪律;那张表的口径指纹与 `verify` 自检已在生产跑了几个月)。
- **「近 N 个交易日」一律按交易日数**(`neckline.calendar`),不是自然日。
- **当日行业涨停家数**读 `data/limit_derived.py` 口径(唯一源,禁自己乘 1.1);行业归属读 `stock_basic.industry`(与 `industry_strength_daily` 同口径,不混用概念板块 —— 项目 CLAUDE.md 明文:行业一对一 vs 概念多对多是两个量)。
- **`persist=0` 或强度日判据缺数** → `stage = 'none'` 且 `stage_reason` 如实写原因,**不猜**。
- **中英映射唯一源** = `neckline/scan/stage.py::STAGE_LABELS`;报告 / 客户端只读它,**禁在别处抄第二份中文表**。

#### ④b-B 新表 `industry_stage_daily`(第 21 张;DDL 定死)

```sql
CREATE TABLE IF NOT EXISTS industry_stage_daily (
  trade_date     TEXT NOT NULL,          -- 'YYYYMMDD'
  industry       TEXT NOT NULL,          -- stock_basic.industry 口径
  stage          TEXT NOT NULL,          -- ignition|fermentation|overheat|divergence|ebb|none
  is_strength_day INTEGER NOT NULL,      -- 0/1,留痕供复现
  persist_days   INTEGER,                -- 读自 industry_strength_daily,NULL=该表当日无行
  limit_up_count INTEGER,                -- 当日该行业涨停家数,NULL=算不出(禁写 0)
  member_count   INTEGER,                -- 参与中位数的成员数(<5 时强度日恒 0)
  stage_reason   TEXT,                   -- 落到该态的判据留痕(可读)
  spec_fingerprint TEXT NOT NULL,        -- 口径指纹:q/成员数下限/近N日窗口 的序列化
  computed_at    TEXT NOT NULL,
  PRIMARY KEY (trade_date, industry)
);
CREATE INDEX IF NOT EXISTS idx_industry_stage_date ON industry_stage_daily(trade_date);
```
- **为什么新表而不是给 `industry_strength_daily` 加列**(交接稿给了二选一,planner 定案):① 那张表**已在生产跑着 17.5 万行**,加列要走 `_migrate_columns` 且要**回填历史**,风险与工作量都更大;② 五态判据额外吃 `limit_derived` 的涨停家数,**与那张表的口径指纹不同源** —— 混一张表会让「口径指纹一致」这条 `verify` 自检语义模糊;③ 分表后两张表各自的 `verify` 三项自检互不牵连,缺行保险丝也能各自独立降级。
- **落全部行业**(含 `stage='none'`),不只落有题材的 —— 同 `industry_strength_daily` 的既有取舍(缺行 ≠ 无题材,两者必须能分开)。

#### ④b-C P0-23 纪律(硬要求,与 ④ 同规格)

- **16:05 EOD 批算落表、在线只读**;日更增量**只读当日一个分区** + 读 `industry_strength_daily` 的当日行 + 前 5 个交易日的既有 `industry_stage_daily` 行(算「近 N 日有强度日」),**不扫全历史**。
- 新 CLI `scripts/industry_stage.py {refresh,verify,bootstrap}`(照 `scripts/industry_strength.py` 体例:`bootstrap` 分块两遍法;`verify` 三项 = 交易日无洞 / 五态判据自洽〔强度日与 stage 不矛盾〕/ 口径指纹一致)。
- `scripts/daily_update.py` 挂 `refresh`(尽力而为不改退出码,失败打 **ERROR** + 打印补算命令原文);**排在 `update_industry_strength` 之后**(依赖它的 `persist_days`)。
- **新批算在新机上单独计时 + 量峰值后才准进主链**(⑯-C)。
- **缺行保险丝(定死)**:`driver_freshness` 拿不到当日阶段 → **降级方向 = 不拦**(该维度按「中性分」处理**且在 `mech_breakdown_json` 里如实标 `stage_missing`**,**不写 0 分**——0 分是「过热」的真实取值,与「没数据」撞车);排序键 `industry_stage_score` 缺行 → 该级键退化为并列(不改变其余级次序),**并在 `dataFreshness` 新增 `industryStageDate` / `industryStageLagDays` / `industryStageStale` 三键显式披露**(与 ④ 的 `scanLayer*` 三键、既有板块三键、行业强度三键**一律不合并**)。

#### ④b-D 研究参考实现的边界

- 研究侧参考实现 = `research/k7p_h11_stage.py`。**生产实现归系统线重写 + 带守门单测,研究代码不直接进生产**(交接稿明文,也是本项目 research → neckline 单向依赖的既有纪律)。
- 允许做的是:拿研究实现当**对拍基准** —— 在若干随机交易日上跑「研究实现 vs 生产实现」逐位比对(比对脚本可放 `scripts/oneoff/`,**不进生产链路**)。口径若有出入,**以本块 ④b-A 的表格为准**(它才是定义),并把差异写进完工记录。

**测试与守门**:五态各一条正例 + 每条边界(`persist=1/3/4`、近 2 日 / 近 5 日窗口两端、涨停家数 0 与 1)各一条;`stage='none'` 与「表里没这一行」两种「没有」分开的单测;确定性(同日跑两次逐位相同);`verify` 三项;**三路等价**(全量 bootstrap ≡ 逐日 refresh ≡ 落表读回)在随机 3 日逐位一致;缺行保险丝两路(⑥ 不拦 + 排序键退化);grep 守门:在线模块零现算入口;`industry_strength_daily` **零改动**(本块不碰它,只读)。

**依赖**:①。**可与 ④ 并行。** 是 **⑥** 与 **③-K7-B** 的前置。
**验收**:上述守门全绿;`bootstrap` + 单日 `refresh` + `verify` 三项自检绿;与研究实现的随机 3 日对拍结论已写进完工记录(一致 / 不一致各自说明);pytest 零回归;真实 `data/neckline.db` 零写入(演练走临时库)。

**完工记录(2026-08-02,@builder)**:新表 DDL 逐字落 `neckline/db.py::_SCHEMA`(附加在 ①/④ 已完工段落**之后**,不回改那两块);全新模块 `neckline/scan/stage.py`(`decide_stage()` 六态判据唯一实现 + `refresh_industry_stage()`/`load_industry_stage()`/`industry_stage_status()`/`verify_industry_stage()`,"计算+落表读写+新鲜度+自检"一体自包含,同 `report/industry_strength_store.py` 先例,不拆进 ④ 的 `freshness.py`/`verify.py`——那两个文件既定范围明确是三张 ④ 表)+ `scripts/industry_stage.py {refresh,verify,bootstrap}`(体例照 `scripts/scan_layer.py`)+ `scripts/daily_update.py` 挂 `update_industry_stage`(排在 `update_industry_strength` 之后、`update_scan_layer` 之前,ERROR 级尽力而为)。

**六态判据落点**:`decide_stage()` 纯函数,输入 `is_strength_day`(读自 `industry_strength_daily` 当日原始值,三态 `Optional[bool]`)+ `persist_days`(同表直接搬运,不重算)+ `limit_up_count`(`data/limit_derived.py` 当日分区聚合,行业归属用 `report/industry_strength.py::load_industry_map` 同一份映射)+ 近 2/5 交易日强度旗标(**读本表自己**已落的 `is_strength_day` 历史,不重查 `industry_strength_daily` 多日窗口)。互斥优先级严格按 ④b-A 表格实现;缺数分三层不猜(强度日判据本身缺失/涨停家数算不出/persist 与 is_strength_day 反常)一律安全落 `none` 且 `stage_reason` 逐条区分,不与"评估完毕确实无题材"的 `none` 混为同一句话。`is_strength_day` 列 NOT NULL 0/1 的有损合并(`None`/`False` 均存 0)是刻意简化,已在模块 docstring 说明理由与影响范围。

**"两遍法"未采纳,如实登记(理由同 ④ 自己 #5 的既有先例)**:本表没有 `persist_days` 那种跨日无界递推状态,唯一跨日依赖是固定 `EBB_LOOKBACK_DAYS=5` 日回看窗口且读本表自己历史,按升序逐日处理天然满足"批==逐日"(三路等价单测证明),故 `refresh_industry_stage()` 不做两遍法,CLI `bootstrap` 与 `refresh` 共用同一实现。**另一处局限如实登记**:事后回填/纠正历史某日不会像 `industry_strength_store._resolve_targets` 那样自动向前级联重算(blast radius 有界,至多 5 天,量级远小于 `persist_days` 的无界传播);`verify_industry_stage()` 的②自洽检查能发现这种滞后不一致,运维手动对受影响的后续几天重跑 `refresh` 即可,本块未自动化这一步。

**本地真实数据冒烟(只读 `data/neckline.db`+`data/parquet`,业务表写入隔离 SQLite 副本;真实库全程 MD5/mtime 双证不变)**:先用真实 `daily` parquet 跑一遍生产写入路径把 `industry_strength_daily` 暖启动至 134 个交易日(2026-01-02~07-24,14740 行,0.69s),再跑 `stage.refresh_industry_stage` 同一区间(14740 行,**1.75s**),`verify_industry_stage` 三项自检全区间全绿;07-15~07-24 尾窗人工核对六态分布均合理(如 07-24:ignition 18/fermentation 3/overheat 1/divergence 6/ebb 36/none 46,共 110 个行业),抽样非 none 行的 `stage_reason` 文案与判据吻合(如"煤炭开采"当日零涨停但近 5 日曾强势 → ebb)。确定性:同一天重跑逐字节相同(排除 `computed_at`)。真实 `data/neckline.db` 全程只读,冒烟前后 MD5/mtime 均不变。

**与研究实现对拍(④b-D,`scripts/oneoff/compare_industry_stage_with_research.py`)**:研究面板缓存(`research/_cache/panel_full.parquet`)覆盖至 2026-07-17,与本地冒烟库重叠 129 个交易日(2026-01-02~07-17)。**首次直接对拍仅得 67.6% 一致**,排查后发现是冒烟库本身的暖启动假象(临时库从 2026-01-02 起跑,`persist_days` 缺更早历史支撑,而研究侧用全历史 2020-2026 计算)——补上更长暖启动窗口后跳到 **96.9%**(13545 样本);进一步分析发现剩余分歧仍集中在暖启动最初 2~3 周(逐日衰减,01-05 当天 56 处 → 01-09 仅 7 处),**排除暖启动前 2.5 周后(2026-01-20~07-17,118 个交易日,12390 样本)一致率 97.71%**,且**逐条分类**证实:`is_strength_day` 判定与 ignition/fermentation/overheat 细分**两侧零冲突**(强度日判据 100% 一致),**全部 284 处残余分歧都落在"双方都判非强度日,只是 divergence/ebb/none 三态细分不同"这一类**;抽样核实根因是**当日涨停家数口径不同**——研究脚本 `lu_cnt` 在 `base_expr()` 域内过滤后计数,生产按 ④b-A 定义直接读 `data/limit_derived.py::is_limit_up`**不做域过滤**(plan 原文明写"唯一源,禁自己乘系数",未提域过滤)。按 ④b-D「口径若有出入,以本块 ④b-A 的表格为准」的裁决,**判定生产实现正确、无需改动**,此差异属已知、可解释、可归因的方法论差异,如实记账。

**如实登记(均未改 Plan,如与规划意图不符请澄清)**:①"grep 守门:在线模块零现算入口"——本表设计上没有一个类似 `compute_industry_strength` 的"可被误用的全历史现算函数"可供命名封禁(结构性避免了这个反模式:`_strength_today_rows`/`_limit_up_counts_for_day` 天生只读当日),故守门测试改为行为验证(`test_single_day_refresh_does_not_need_other_years_partitions`:目标年份之外的 parquet 分区目录整个不存在也能正常工作)+ 文本守门(`industry_strength_daily` 零写入断言),未字面照抄"grep 两个禁用函数名"的形式,精神一致。②"缺行保险丝两路(⑥ 不拦 + 排序键退化)"——⑥ 尚未开工,这两路消费逻辑本就不在 ④b 文件范围内(block 文件范围是新建 `neckline/scan/stage.py`);本块只负责把消费方需要的两个原语做对(`load_industry_stage` 缺行返回空 DataFrame、`industry_stage_status` 三键如实披露 unavailable/stale),两路保险丝本身留给 ⑥ 落地时验收。③读侧口径指纹校验(`load_industry_stage` 对 `spec_fingerprint` 不匹配的行视同缺行 + WARNING)是参照 `industry_strength_store.load_industry_strength` 先例主动补充的防御,plan 原文只要求 `verify()` 检查口径指纹一致,未明确要求读侧也拦,判断为合理的一致性增强,不算越权。

测试:新增 1 个文件 `test_scan_stage.py` 共 38 例(六态判据正例/边界/三类缺数 15、近 N 日回看窗口边界 5、涨停家数三态 4、端到端生命周期叙事 2、"没有"两种语义分开 1、确定性 1、三路等价 1、新鲜度 2、口径指纹漂移 1、`verify` 四项(绿+三红)4、守门 2)。`python -m pytest tests/ -q`:**2371 passed, 2 skipped, 2 failed**(2 fail 是既有"周日时间炸弹"——`date.today()` 落在 2026-08-02 周日导致的既有已知失败,同名同因未变,与本块无关;当前 git 树另有 ⑤ 块并发 builder 的未提交在制品,其净增测试数已计入这次全量结果,本块自身净增 38 例、零回归)。真实 `data/neckline.db` 全程只读:全量 pytest 前后 + 本地真实数据冒烟前后 + 对拍脚本运行前后,MD5/mtime 均不变。红线自查:未碰任何服务器/DNS/部署;`industry_strength_daily` 零改动(全仓 grep 确认无写入语句);仅新增文件 + `neckline/db.py`/`scripts/daily_update.py`/`neckline/scan/__init__.py` 三处追加式编辑,未改动 ④/①/③ 已完工代码一行。

---

### V2-⑤ · ②驱动聚合层:LLM 合并驱动 + 两道机械闸(V2 心脏,@builder)✅ 完工(2026-08-02)

**目标**:把驱动种子变成**篮子候选** —— 合并同驱动异名题材、命名共同驱动 + 证据链(来源 + 日期)、「为什么是现在」、成员选择 + 角色标注。

**涉及**:新建 `neckline/selection/aggregate.py`;LLM 走 ② 的路由(`TASK_DRIVER_SEARCH` → 检索 Agent;`TASK_BASKET_REASON` → 推理 Agent),两段式。

**LLM 输入**:驱动种子 + 成员机械数据(来自 ④ 三表 + 面板)+ 联网检索产物。**日期锚与时效纪律走 `prompt_context` 唯一实现**(排查「模型引用旧研报」类问题先看 prompt 有没有日期,别一上来怀疑搜索没通)。

**两道机械闸(定死,守门单测)**
1. **成员白名单闸**:LLM 只能从**扫描层给出的簇 / 板块成员集合**里选票。凭空造票 → **整条建议拒收**(不静默丢那一只票,因为成员集被污染意味着这条驱动的成员选择整体不可信)+ WARNING 落日志。
2. **角色对拍闸**:LLM 标注的龙头 vs `leader_structure_daily.role_mech` 冲突 → `basket_members.role_conflict=1`,**两说并存写进卡**,**不静默采信任何一方**。

**篮子规则(蓝图 4.1 / 4.2)**
- 1–3 只 / 篮;必须说明共同驱动与「为何是这几只而不是同题材其他票」。
- **仅历史相关性不足以成篮** —— 机械判据:`driver` 文本为空 → **不成篮**(相关性只作辅助证据)。
- **⚠「零证据不成篮」与「检索缺席仍出篮」两条的自洽读法(2026-08-02 planner 钉死,承 ⑤ 完工记录疏漏 ⑦;不这么读会直接打架)**:判据是**「本篮的每颗种子是否都真的被检索过」**,不是「证据链有没有条目」——
  - **检索跑过、空手而归**(检索段执行了、返回 0 条)→ **不成篮**。这是「查了,没有」。
  - **压根没搜成**(检索 provider 缺席 / 失败 / 预算耗尽)→ **篮子照出**,`evidence_status='search_unavailable'`,证据链留空**并在卡面与报告明示**。这是「没查」。
  - **混合态**(一篮合并的多颗种子里只搜成一部分)→ **不判不成篮**,`evidence_status='partial'`。
  - 一句话:**「没有」与「没看」必须分得开**(§五 铁律),判「不成篮」的只有前者。
- **主归属规则**:同一票可保留多个题材标签,但 `is_primary=1` 唯一 —— 取**行业闸 lift 最高**的那篮(复用 v1.3.1 行业闸 lift 先例,防「挂靠票占位」重演)。**⚠ lift 的小样本失真与最小成分数门槛见下方 ⑤-c(2026-08-02 planner 裁定)。**
- 不为凑数加入弱相关股票;优先覆盖不同角色(龙头 / 中军 / 弹性)但**不强制凑齐三角色**。

**诚实缺省**:检索段缺席 → `evidence_status='search_unavailable'`,证据链留空并明示,**禁编造来源**;推理段缺席 → 该驱动不成篮(不拿机械数据硬凑一个"驱动")。

**依赖**:②④。
**验收**:白名单闸单测(喂一只不在成员集里的码 → 整条拒收 + WARNING);对拍闸单测(LLM 龙头 ≠ 机械头名 → `role_conflict=1` 且卡里两说并存);无驱动文本 / 零证据 → 不成篮;主归属唯一性单测(一票两篮 → `is_primary` 恰一个 1);检索段故障 → 篮子仍出且 `evidence_status` 如实;pytest 零回归。

**完工记录(2026-08-02,@builder-pro)**:新建 `neckline/selection/aggregate.py`(唯一编排入口 `aggregate_baskets()`,**永不抛异常**——顶层保险丝把任何失败降级成"当日无篮子 + 如实 `notes`",承 §五铁律「任何一段异常都不许让当日无报告」)。

**两段式编排落点**:`run_driver_search()`(逐颗种子,`TASK_DRIVER_SEARCH` 路由 + `enable_search=True` + **显式 `search_query`** 带年份,扣 `budget.LEDGER_SEARCH`)→ `run_basket_reason()`(全部种子一次调用,`TASK_BASKET_REASON` 路由 + `enable_search=False`,扣 `budget.LEDGER_REASON`)。**单侧降级三态分得开**:检索段缺席/失败/预算耗尽 → `evidence_status='search_unavailable'`、证据链留空并在推理段上下文里**明写「本次未取得」**、篮子照出;推理段缺席 → 不成篮;一篮合并的多颗种子里只搜成一部分 → `partial`。两段 user 消息**第一行恒是 `prompt_context.date_anchor_line(ref_date=D0, name_tomorrow=True)`**,两段 system prompt 各自内嵌 `TIMELINESS_RULES`(单一实现,守门单测扫全仓)。

**两道机械闸落点**(均在 `_gate_proposal()`,闸序刻意是「先查出处 → 再查成员 → 最后才谈证据」):① **成员白名单闸** → `REJECT_FABRICATED_MEMBER`,**整条拒收 + WARNING**;配套 `REJECT_UNKNOWN_SEED`(声明了不存在的种子 = 出处被污染,同样整条拒收)。② **角色对拍闸** → `_resolve_mech_role()` 读 `leader_structure_daily`,`role_conflict=1` 时 `role_llm`/`role_mech`/`rs_rank` **三者并存**;机械侧 `unknown` 或簇里没这只 → `role_mech=NULL` + `role_conflict=0`(**「没判定」不是一种角色**,不能拿它去判冲突)。一只票落在多个簇时取值可复现:优先本篮声明的簇 → 否则 `rs_rank` 最小 → 再 `cluster_key` 升序。

**篮子产出形状**:`BasketCandidate`(= `baskets` 一行**减去 `tier`**)+ `BasketMemberCandidate`(= `basket_members` 一行),外带 `RejectedProposal` 留痕(拒了什么/为什么/原样)与 `DriverEvidence`(每颗种子的检索产物,`ok` 与 `search_unavailable` 两态严格分开)。`basket_key = crc32(trade_date|driver_slug)` 十六进制,`driver_slug` = 篮子名归一化(去标点/空白转小写,保留中日韩表意字与字母数字);同日撞键 → 后一条 `REJECT_DUPLICATE_KEY`,不静默丢。**主归属** `assign_primary()`:lift = 该票行业在**本篮所声明种子的原始成分并集**里的占比 ÷ 全市场占比,取 lift 最高的那篮 `is_primary=1`,tie-break **lift 降序 → `basket_key` 升序**;lift 算不出时字段仍是 `None`(「算不出」≠「等于 0」)。

**落库** `save_baskets(result, *, tier_by_basket_key, db_path)`:`INSERT OR IGNORE` 幂等(同日同 `basket_key` 重跑 = no-op,**不覆盖既有行**);**`tier` 必须由调用方显式给出、缺一个就 `ValueError` fail loud**——`baskets.tier` 是 `NOT NULL` 而 tier 由 ⑥ 定档,本层绝不臆造。⚠ **`aggregate_baskets()` 不自动调它**(理由见下「疏漏 ①」)。

**测试**:新增 `tests/test_selection_aggregate.py` **56 例**(白名单闸 4 / 对拍闸 5 / 篮子规则 8 / 主归属 5 / 两段式降级 9 / 编排与预算 11 / 结构化解析 3 / 落库 5 / 架构守门 6),含 plan 验收逐条 + 三条静态守门(不 import `neckline.sentinel` / 不复用 `judge._parse_verdict` / 源码零纪律参数痕迹)+ 一条**交叉断言**(本模块 lift 与 v1.3.1 行业闸 `intel_candidates._market_industry_shares`/`_dominant_industries` 同口径,同 v1.5 K4 双侧镜像体例)。`python -m pytest tests/ -q`:2274(基线)→ **2333 过 + 2 skip + 2 fail**(2 fail = 既有周日时间炸弹,同名同因未变);其中 ⑤ 自身贡献 57(56 新例 + `test_selection_primitives.py` 的数值守门参数化多扫到 `aggregate.py` 1 例),余 2 例来自并行施工的 ④b 在树内的改动。真实 `data/neckline.db`:全部 V2 表仍 **0 行**、现役章程仍 `K1` 未动;首次全量跑后 MD5 变化经核实是 ① 已登记的 `test_holding_k4_check.py` 未传 `db_path` 既有缺陷(与本块无关,本块新代码单跑两次 MD5 逐位不变)。

**真实数据冒烟(只读真库副本 + 真 parquet,LLM 全桩)**:真库 `.backup` 到 scratch → `scan_layer.py bootstrap 20260722~24`(5.4s)→ 激活 `packs/K4-pack.json` → `generate_seeds(2026-07-24)` 得 144 涨停簇 + 52 异动簇种子 → `aggregate_baskets(max_seeds=6)`:检索词实测 `信创 2026 最新` / `光伏建筑一体化 2026 最新` / `抽水蓄能 2026 最新`,成两篮(成员带真实中文名/行业/涨幅/成交额/机械角色/簇内 RS 名次/两两 20 日相关性 0.73),白名单闸真的拦下了故意混入的 `999999.SZ`,对拍闸真的标出了一处 `role_conflict=1`,`save_baskets` 落库 1 篮 2 成员。

**冒烟中发现并在本块修掉的两处**(都由真实数据打出来,合成测试看不出):① ④ 的涨停簇/异动簇种子在只有概念锚时 `label` 是**裸指数代码**(`886086.TI`),直接当检索词等于什么都没查(v1.3.4「检索词没身份信息 → 命中泛泛新闻 → 模型退回训练数据」的同类病)—— 本层统一过一遍 `report/sectors.py::load_index_names` 名表(`MechContext.label_for()`),`886086.TI` → `西部大开发`,查不到就原样保留;② `daily.amount` 单位是**千元**,换亿元该除 `1e5` 而不是 `1e8`,原写法把 7.27 亿的票写成「0.01 亿」。两条各有单测锁死。

**顺带做的一处最小重构**(行为逐字节不变,既有 81 例 `test_reference_plan.py` 全绿):`split_narrative_and_reference_json` 及其三条围栏正则从 `report/reference_plan.py` 原地搬到**新模块 `neckline/llm/json_block.py`**,`reference_plan.py` 改为 import 并同名再导出(既有调用方与单测逐字不动)。两条理由:它是**通用 LLM 输出解析件**而本块要用同一份(CLAUDE.md「唯一实现,不许各抄一份」),而 `reference_plan.py` 按 ⑬-3 将停用 —— 通用件不该陪葬。**这不是 `llm/` 反向依赖 `report/`**(新模块零 import `report`),`judge_candidate(narrative_splitter=…)` 的依赖注入体例保持不变。

**如实登记的疏漏与设计判断(均未改 Plan,如与规划意图不符请澄清)**:
1. **Plan 自相矛盾一处(最要紧)**:「V2 新表汇总」把 `baskets`/`basket_members` 的**写入块登记在 ⑦**,而 `baskets.tier` 是 `NOT NULL`、tier 由 **⑥** 定档,⑥ 又依赖 ⑤ —— 于是「⑤ 落表」在 DDL 层面不可能成立(除非臆造一个 tier)。**本块处置**:⑤ 产出内存态 `BasketCandidate`(字段与两张表一一对应、只缺 `tier`),并提供 `save_baskets(..., tier_by_basket_key=…)` 写入口**要求调用方显式给 tier、缺就 fail loud**;`aggregate_baskets()` 本身不落库。**另一处连带**:`tier_history.basket_id`(⑥ 写)需要 `baskets.id`,而 `baskets` 由 ⑦ 写、⑦ 又依赖 ⑥ —— **⑥/⑦ 施工时必须先定死运行期落库次序**(建议:⑦ 一次事务内写 `baskets`+`basket_members`+`basket_cards`,⑥ 的 `tier_history` 在拿到 `basket_id` 之后补写),请 planner 裁定。
2. **成员卫生线未在本块施加**:③ 的 `stock_hygiene`/`non_new_stock` 两原语需要 `ma20`/`amount_ma20`/`days_since_listing`(出自 `research/panel.py` 的全市场滚动面板,是一次不便宜的计算),而 ⑤ 块规格只点名两道机械闸、未提卫生线;§2.8-C 第 4 条说「机械闸仍由 K4 advisory 与卫生线承担」但没说落在哪一块。**现状 = 篮子成员可能包含 ST / 次新 / 流动性差的票**。不自行补一个"部分版卫生线"(那会造出第二个口径源),请 planner 裁定是补在 ⑤、⑥ 还是 ⑦。
3. **lift 在小样本下会失真**:涨停簇种子的成分常只有 2–5 只,`占比 ÷ 全市场占比` 会算出 70~90 倍的 lift(真实冒烟实测),与 v1.3.1 先例(板块成员数以百计)不是一个统计量级。后果:同票同时出现在「小簇篮」与「大概念篮」时,主归属几乎恒落在小簇篮。**未加最小成分数门槛**——那是一个 plan 没有的新参数,属产品决策。
4. **`driver_slug` 的口径是 builder 选的**:DDL 注释只写了 `crc32(trade_date|driver_slug)`,没定义 slug 怎么算;本实现取**篮子名**归一化(不取整句 `driver`——一句完整中文的标点/语气一变键就漂)。
5. **`driver_kind` 非法值走机械兜底而不是拒收**:分类标签写错 ≠ 成员选择不可信(与白名单闸的性质不同),按种子类型映射兜底(`limit_cluster→limit_cluster` / `surging_concept→theme` / `hot_industry`·`anomaly_cluster→rotation`,依据蓝图 4.1「共同驱动可以来自」清单)并置 `driver_kind_fallback=True` 留痕。**映射表 plan 未给,是 builder 拟的。**
6. **`role` 非法值则整条拒收**(`REJECT_BAD_ROLE`):角色是三值枚举、prompt 逐字给过,写不对就没法跟机械侧对拍 = 对拍闸失效,故不猜一个塞进去。
7. **两条 plan 条文的自洽读法**(不这么读会直接打架):「证据链条目为 0 → 不成篮」管的是**检索跑过、空手而归**;「检索段缺席 → 证据链留空并明示、篮子仍出」管的是**压根没搜成**。判据落在 `_evidence_for()` 的第三个返回值「本篮每颗种子都真的被检索过」。混合态(部分种子没搜成)下**不判不成篮**。
8. **白名单闸的白名单 = 系统实际展示给 LLM 的那份清单**(`MAX_MEMBERS_IN_CONTEXT=20`,按当日成交额降序截断),不是种子的全体原始成分 —— 热点行业种子成分上百只,全列既撑爆上下文也没意义;取"展示过的"当白名单更严、更可审计,也不会出现「它猜中了一只我们没给它看的真成员」这种无法与幻觉区分的情形。
9. **四个引擎常量登记进 `test_selection_primitives.py::_ENGINE_CONSTANT_WHITELIST`**(`MIN_MEMBERS`/`MAX_MEMBERS`/`MAX_SEEDS_AGGREGATED`/`MAX_MEMBERS_IN_CONTEXT`)—— 依据「插槽边界」原文「②驱动聚合的两道机械闸 = 引擎本体,不进包」,前两者是蓝图 4.2 的产品硬规则、后两者只约束上下文规模不参与任何判据。
10. **`dataFreshness.scanLayer*` 三键仍未接线进 `report/pipeline.py`**(④ 完工记录已登记):⑤ 块规格的「涉及」只列 `neckline/selection/aggregate.py`、不含报告管线,故按块内文字留给 ⑭。

---

#### ⑤-b · 成员卫生线闸(2026-08-02 planner 裁定;**⑤ 主体已完工,本子项是追加施工项**)✅ 完工(2026-08-02)

> **背景**:⑤ 完工记录疏漏 ② —— §2.8-C 第 4 条说「机械闸仍由 K4 advisory 与卫生线承担」,但 ⑤ 原规格只点名白名单闸与对拍闸,**没人认领卫生线** → 现状篮子成员可能含 ST / 次新 / 流动性差的票。⑤ builder **刻意没自造"部分版卫生线"**(防第二口径源),这个克制是对的。**planner 裁定:补在 ⑤,复用 ③ 的现成原语,不发明第二份判据。**

- **落点定死 = 「装配给 LLM 看的成员清单」之前**(即 `MechContext` 构建期,`MAX_MEMBERS_IN_CONTEXT` 截断**之前**先过滤再截断)。**为什么是这里而不是事后拒收**:① LLM 压根看不到脏票,不用再造第三条拒收路径;② **既有的白名单闸自动兜住** —— 它只能从展示过的清单里选(⑤ 疏漏 ⑧ 的白名单定义正好使这条成立);③ 一次过滤、全篮复用。
- **判据一律复用 ③ 的三个原语,读现役包参数,⛔ 禁自造第二份**:`stock_hygiene`(`close_min` / `amount_ma20_min` / `require_ma20` / `allowed_boards` / `exclude_st`)、`non_new_stock`(`min_days`)、`k4_advisory_gate`。
- **`k4_advisory_gate` 两档语义照包,不许改**:`hard_cut_action='exclude'` → **剔出成员清单**;`avoid_flag_action='tag'` → **保留但打标**,标随成员传到 ⑦ 的成员节展示、并计入 ⑥ 的 `card_density` 维度。这正是「机器不禁、人可复核」(§2.0 第 3 条)。
- **算力边界**:只对**候选成员集**(本日全部种子成分的并集,量级几百只)算,**不是全市场**。若原语实现形态要求全市场面板 → 允许「先算面板、再按码取子集」,但必须**当日一次装配、全篮复用**(⛔ 不许每篮各算一遍),且耗时**计入 ⑯-C 的新机实测**。
- **保险丝拆两级(定死,别一刀切)**:
  - **便宜的硬风险永远拦**:ST / 停牌 / 次新 / 板块不在 `allowed_boards` —— 数据源是 `sentinel/universe.load_stock_meta` + `stock_basic` + `suspend_d` 口径,便宜且必在,**算不出就是异常,该篮整体拒收并留痕**,不放行。
  - **贵的趋势 / 流动性线算不出才降级**:`ma20` / `amount_ma20` 面板缺失 → **降级方向 = 不拦**(该项放行)+ 篮子 `notes` 与报告如实标 `hygiene_unavailable`(承 P0-23「降级=不拦 + 显式披露」定案)。**⛔ 不许静默当作"都合格"。**
- **被剔票要留痕**:剔了谁、因为哪条原语、参数是什么 → 进 `RejectedProposal` 同族的留痕结构(与白名单闸拒收分开计数,两种"没进来"语义不同)。

**⑤-b 验收**:造一只 ST 票 / 一只次新票 / 一只 `amount_ma20` 不达标票 → 三者都不出现在喂给 LLM 的成员清单里(单测);`hard_cut` 命中被剔、`avoid_flag` 命中**仍在清单里且带标**(单测两路,断言语义没混);面板缺失 → 趋势线放行 + `hygiene_unavailable` 如实标,而 ST 票**仍被拦**(单测);**交叉断言**:同一票同一天,⑤-b 的卫生线判定与 ③ 原语 `run()` 的结果**逐位相同**(禁两处口径漂移,同 v1.5 K4 双侧镜像体例);当日装配次数 = 1(单测计数,防每篮重算);pytest 零回归。

**完工记录(2026-08-02,@builder)**:新建 `neckline/selection/member_hygiene.py`(唯一实现,`aggregate.py` 不自造第二份判据)。**过滤链路位置**:`aggregate_baskets()` 内,`build_mech_context()`(拿到 `ctx.industry_of`/`ctx.close_of` 复用)之后、`presented_by_seed` 装配(`_shortlist` 截断)之前,对 `all_codes`(本日全部种子成分并集)调用 `apply_member_hygiene()` **恰好一次**,`result.kept` 过滤每颗种子的 `member_codes` 后才截断——单测 `TestMemberHygieneWiring::test_filtering_happens_before_truncation_not_after` 用「最高成交额那只是 ST」的构造反证顺序没错(旧序会让排名靠后但干净的票永远没机会露面)。

**两级保险丝落点**:tier-1(`stock_hygiene` 的 `is_st`/`board` 两项 + 独立的停牌 / 次新检查)数据源 = `sentinel.universe.load_stock_meta`(`stock_basic`)+ `get_market_slice(..., "suspend_d")`,任一码查无 meta 或停牌名单本身读取失败 → **fail closed**(该码拒收,原因码分别是 `meta_missing`/`suspended`)。tier-2(`close`/`ma20`/`amount_ma20`)数据源 = `build_research_panel(d, d, with_forward=False)` 单日切片(同 `intel_candidates.py` 既有调用姿势,45 自然日缓冲非全历史扫描);K4 分区归属额外复用 `report/holding_k4_check.py` 既有机械镜像(`_build_holding_feature_panel` + `_evaluate_hits` + `load_k4_sections`/`_load_k4_evidence`),I/O 换成本模块自写的 `_bulk_scan_codes_range`(同 `intel_candidates.py::_bulk_load_codes_table` 手法,**不 import 那个即将随候选榜退役的模块**,`aggregate.py::market_industry_shares` 已有不 import 的先例)。两者任一整体读取失败 / 该码查无该行 → **fail open**(放行 + `hygiene_unavailable`/`k4_unavailable` 如实披露,两个 flag 分开计数)。

**常量共享方式(⑤-c 用)**:`aggregate.MIN_LIFT_SAMPLE_SIZE` 是对 `industry_strength._MIN_MEMBERS` 的**直接 import 引用**(`from ... import _MIN_MEMBERS as _INDUSTRY_STRENGTH_MIN_MEMBERS; MIN_LIFT_SAMPLE_SIZE = _INDUSTRY_STRENGTH_MIN_MEMBERS`),不是抄一份字面量 `5`。

**lift 三态行为(⑤-c)**:① 成分池(该篮所声明种子的成员并集)≥5 → 正常参与比较,`industry_lift` 照算、`primary_reason='highest_lift'`;② 成分池 <5 → `industry_lift=None` + `lift_reason='sample_too_small'`,**完全退出比较**(不是"降权"——同票若有 ≥1 个达标篮,主归属只在达标篮之间比,不达标篮的原始 lift 数值再高也不参与);③ 一票的全部候选篮都不达标 → 确定性兜底 `(该篮成分池大小降序, basket_key 升序)`,`primary_reason='fallback_no_qualified_lift'`。

**测试**:`tests/test_selection_member_hygiene.py` 新增 **23 例**(tier-1 五路 / tier-2 降级五路 / K4 两档语义六路 / 与原语 `run()` 交叉断言参数化 5 组);`test_selection_aggregate.py` 新增 **7 例**(⑤-b 编排接线 3:过滤先于截断、当日装配恰一次、降级 notes 透出;⑤-c 4:达标/不达标单篮、回归场景〔小簇 vs 大概念〕、全不达标确定性兜底 + 常量同源交叉断言)。`python -m pytest tests/ -q`:**2427 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹,与本子项无关;树内同时含 ③-K7/④b 并发完工的净增,三方文件面不相交、零冲突)。

**如实登记的设计判断(均未改 Plan,如与规划意图不符请澄清)**:
1. **"面板里有该行但字段是 null"改判与"整行缺失"同等降级(不拦)**,与初稿设计(该情形按原语既有语义正常判不通过)不同——真数据/真实测试跑起来发现:`non_new_stock` 的 `min_days=120` 自然日门槛远严于 `ma20` 只需的 20 个交易日,一票能通过 `non_new_stock` 却 `ma20` 仍是 `None`,现实里几乎只可能是**数据缺口**(该码窗口内交易日不足但已上市 120+ 自然日),不是"真的太新"(真太新的票已被 `non_new_stock` 拦下)。按数据缺口处理更贴合 P0-23「降级=不拦」的立法原意,也避免"测试只喂了一两天历史"这类夹具局限被误判成"真的不达标"。
2. **K4 分区归属评估失败(`k4_unavailable`)的降级哲学是 builder 类推,非 plan 逐字要求**——plan 原文「保险丝拆两级」只点名 ma20/amount_ma20 这一档,未提 K4;K4 判据本身同样"贵"(需 ~420 自然日窗口的价量特征镜像),按同一 P0-23 doctrine 类推为"算不出→降级不拦+披露",与 ma20/amount_ma20 的 `hygiene_unavailable` **分开计数**,便于日后核实是否要单独处理。
3. **`MIN_LIFT_SAMPLE_SIZE` 未按"⑤ 既有四个常量的体例"登记进 `_ENGINE_CONSTANT_WHITELIST`**——那四个常量都是模块级数值**字面量**,白名单扫描器（`ast.literal_eval`）能命中、也必须登记;本常量是对 `industry_strength._MIN_MEMBERS` 的**直接引用**(`Name` 节点),扫描器结构上无法/不会命中它,若仍登记一个扫不到的键会让反向存在性校验 `test_engine_constant_whitelist_entries_are_still_present` 失败。直接引用比"字面量+白名单登记+人工确保两处一致"更强的防漂移(不可能漂移,不是"漂移了会被测试抓到"),判断为比字面遵照体例更贴合"防两处漂移"这一意图的实现。
4. **既有 56 例测试里唯一动了测试数据(非断言逻辑)的一处**:`_two_basket_env`(`TestPrimaryAttribution`)的种子 `s-a` 从 3 只成分扩到 5 只——⑤-c 门槛生效后原 3 只成分池会判"不达标"从而退出比较,与该测试"两篮都合法参与 lift 比较"的原意冲突;扩容后两篮成分池都 ≥5(仍保持 A 篮 lift > B 篮 lift 的原有关系),测试断言**一字未改**。该测试原本恰好示范的"小样本 lift 失真"场景,现由新测试 `test_primary_falls_on_qualified_basket_despite_smaller_baskets_higher_raw_lift` 专门覆盖(⑤-c 验收原文点名的"正是失真场景的回归")。
5. **`test_selection_aggregate.py::_run()` 新增测试脚手架**(`_ensure_hygiene_defaults`):⑤-b 之前 `aggregate_baskets()` 从不需要一个真的注册在 DB 里的策略包与完整的 `stock_basic` 参考数据;⑤-b 起三原语要读包参数、`non_new_stock` 要读 `list_date`,而既有 55 个测试函数里只有 3 个显式 `insert_stock_basic`(且均未给 `list_date`)、也从未注册过任何包。`_run()` 因此在真正编排前用 `INSERT OR IGNORE`/`UPDATE ... WHERE list_date IS NULL` 语义补齐两处默认值(现役包 + `stock_basic.list_date`),**不覆盖测试已显式给出的值**——这是让 56 例保持"断言一字不改仍绿"的必要前提,不是取巧放水(默认值照抄 `packs/K4-pack.json` 现值,ma20/amount_ma20/K4 三项在多数测试里仍然因缺真实价量历史而走"降级不拦",不是被人为放宽阈值"造假通过")。

#### ⑤-c · lift 主归属的最小成分数门槛(2026-08-02 planner 裁定;**追加施工项**)✅ 完工(2026-08-02,随 ⑤-b 同批,完工记录合并写在 ⑤-b 段尾)

> **背景**:⑤ 完工记录疏漏 ③ —— 涨停簇成分常只 2–5 只,`占比 ÷ 全市场占比` 实测算出 **70~90 倍** lift,与 v1.3.1 先例(板块成员以百计)**不是一个统计量级**;后果是同票同时出现在「小簇篮」与「大概念篮」时,主归属**几乎恒落在小簇篮**。

- **裁定:加最小成分数门槛,作为引擎常量(⛔ 不进包)**。
  - **为什么不进包**:它治的是**统计有效性**(样本太小,lift 这个估计量本身就没意义),**不是策略偏好**。进包会让策略线误以为它是可调的 alpha 旋钮 —— 实际上调它只是在调「愿意接受多不可靠的估计」。
  - **同族先例**:`report/industry_strength.py` 的 `_MIN_MEMBERS=5`(成员 <5 不参与行业排名)、K7 五态的「成员数 ≥5」—— **本门槛默认值取 5,与它们同源同值**;实现时**显式引用同一个常量或至少在 docstring 里指名同源**,防两处漂移。
- **未达标时的行为(定死,不是"丢弃篮子")**:
  - 成分数 < 门槛的篮子,其 lift **不参与主归属比较** —— 记 `lift=None` + `lift_reason='sample_too_small'`(**「算不出」≠「等于 0」**,承 ⑤ 既有姿势)。
  - 主归属在**达标篮之间**按 lift 比(tie-break 仍是 lift 降序 → `basket_key` 升序,⑤ 已实现)。
  - **一票的全部候选篮都不达标** → 退化到**确定性兜底**:按 `(成员数降序, basket_key 升序)` 取一个,标 `primary_reason='fallback_no_qualified_lift'`。**可复现、不随机、不拍脑袋。**
  - **篮子本身不因不达标而被剔** —— 门槛只影响「主归属归谁」,不影响「成不成篮」。
- **⚠ 策略含义部分登记为待过目,不悬空**:「门槛取 5 是否合适」「小簇篮是否天然该输给大概念篮」带策略语义。本 plan 给的是**临时工程默认**;**已登记 §七 [P3-32]** 交策略线 / 用户在有归因样本后回看(⑨ 的按包归因正好能出「主归属落在小簇 vs 大概念篮」的实际表现对照)。**在有新证据之前,按上面的默认执行,不要自行改数。**

**⑤-c 验收**:3 只成分的簇篮 lift 记 `None` + reason(单测);一票跨「小簇篮 + 大概念篮」→ 主归属落在**达标的那个**(单测,正是失真场景的回归);全部候选篮都不达标 → 兜底可复现(同输入两次跑结果逐位相同)+ `primary_reason` 精确;门槛常量在 `_ENGINE_CONSTANT_WHITELIST` 里登记(同 ⑤ 既有四个常量的体例);pytest 零回归。

---

### 【planner 裁定 · 跨块】篮子四表的运行期落库次序(2026-08-02;**⑥⑦ 施工前必读,消除原 Plan 的循环依赖**)

> **背景**:⑤ 完工记录「疏漏 ①」报了一处 Plan 自相矛盾 —— 原「V2 新表汇总」把 `baskets`/`basket_members` 的写入登记在 **⑦**,但 `baskets.tier` 是 `NOT NULL` 而 tier 由 **⑥** 定档;`tier_history.basket_id`(⑥ 写)又要 `baskets.id`(⑦ 写)→ **循环**。**planner 采纳 ⑤ 的既成处置为前提**(⑤ 产出内存态 `BasketCandidate`〔= `baskets` 一行减 `tier`〕,写入口 `save_baskets(..., tier_by_basket_key=…)` 强制要 tier、缺就 `ValueError`,`aggregate_baskets()` 不自动落库),在此把次序定死。

**A. 运行期次序(定死,不许改序)**

```text
⑤ aggregate_baskets()            → 内存:BasketCandidate[] + BasketMemberCandidate[](无 tier)
⑥ score_and_tier()               → 内存:tier / mech_score / breakdown / rank_mech / LLM 同档微调
⑥ 落库【事务 1,单事务】          → INSERT baskets(拿 id) → basket_members(用 id) → tier_history(用 id)
⑦ build_card()                   → LLM 生成剧本等(慢、可失败)
⑦ 落库【事务 2】                  → INSERT basket_cards(version=1)
```

- **为什么 ⑥ 是 `baskets`/`basket_members` 的写入方**:tier 在它手里、`tier_history` 也在它手里,三张表**同一事务**落地才不会出现「有篮子没定档」或「有定档没篮子」;⑦ 只剩一张 `basket_cards`,职责干净。
- **为什么不把四张表塞进一个事务**:⑦ 的卡生成要**调 LLM**(剧本/理由),跨 LLM 调用持 SQLite 事务是错的(慢、易超时、锁库)。**两个事务是刻意的。**
- **写入口统一住 `neckline/selection/basket_store.py`**(新模块):把 ⑤ 已实现的 `save_baskets` **搬进来**、`aggregate.py` 保留同名再导出(**行为逐字节不变,既有单测一字不动** —— 照 ⑤ 自己刚做过的 `json_block.py` 搬迁体例),并在此新增 `save_tier_history(...)` 与 `save_basket_card(...)`。**理由**:四张表一个家,事务边界才管得住;这也是项目既有体例(`report/store.py` / `reference_plan_store.py` / `industry_strength_store.py` / `review/store.py` 都是「一族表一个 store」)。
- **事务 1 内部允许 `save_baskets` 复用外部 connection**:给它加可选 `conn=`(给了就不自己开事务,照 `neckline/db.py::connection()` 既有体例),**签名其余部分与 fail-loud 语义一字不改**。

**B. 幂等与冻结(定死)**

- 四张表**一律 `INSERT OR IGNORE`**;**同日重跑 = no-op,绝不覆盖既有行**(它们是 **D0 冻结件**)。
- ⚠ **这与 v1.5 契约线 🟡-1「同日重跑要在写侧清掉旧行」不冲突,别套错模板**:那条治的是「快照上的标」与「响应时现连的表」讲相反的话;这里四张表**同批冻结、只有一个说法**,不存在两处打架。**冻结件的正确姿势就是不覆盖。**
- **重跑发现不一致必须留痕、不许静默**:若本次算出的 tier / 成员集与库里已冻结的行不同 → **WARNING 落日志 + 报告如实写「今日已有冻结篮子,本次重算结果未采纳(差异 N 处)」**。藏起来不是诚实。

**C. 中间态是合法状态,必须能表达(不许隐藏、不许回删)**

- **「有篮子、无卡」**(事务 1 成功、⑦ 失败 / LLM 不可用 / 预算耗尽):**合法**。⑦ 可在**同一 D0 内重跑补 `version=1`**(仍是 D0 原判,不违反冻结)。
- **契约与展示层必须能表达它**:`GET /baskets/{id}/card` 在无卡时返 **404 + reason `card_not_ready`**(⑭-B 端点清单据此补一个 reason,客户端 `mapReason` 加对应 case —— CLAUDE.md 坑:新增会返 404 的端点必须检查要不要加新 case);报告的篮子节如实写「本篮卡未生成({reason})」。**⛔ 不许因为没卡就把篮子从报告里抹掉。**
- 反向的「有卡无篮子」在本次序下**不可能发生**(卡的 `basket_id` 来自事务 1),无需处理。

**D. 谁调用谁(编排归属)**

- 全链路编排入口在 **⑭-A 的 `report/pipeline.py`**(⑤→⑥→⑦ 依次调用、每段包保险丝);**⑥⑦ 自身只提供纯函数 + 写入口,不互相 import、不各自开编排**。在 ⑭ 之前,⑥⑦ 各自用自己的 CLI / 单测驱动。

---

### V2-⑥ · ③Tier 分层引擎:机械分定档 + LLM 同档微调 + `tier_history`(@builder)✅ 完工(2026-08-02)

**目标**:把篮子候选定档 T1/T2/T3,**定档可完整复现**。

**机械分五维(**K7 需求 1a/1b 已把第 2、3 维从「方向描述」升级为正式定义**,施工前采纳、免返工;维度选择与权重读现役包)**

> 来源:`archive/交接_K7_系统线需求_20260802草案.md` 需求 1a/1b/2(2026-08-02 用户转发;证据链 `research/k7_pre_report.md` H10/H11)。

1. **`sector_strength` 板块强度**(`industry_strength_daily` + 概念板块强度,**只读表**);
2. **`driver_freshness` 驱动新鲜度 = 行业五态打分(K7 需求 1b,取代 persist_days 单调函数)**
   - 读 **④b 的 `industry_stage_daily.stage`**(六态英文码),经**现役包的 `config.tier.stage_scores`** 映射成分值。
   - ~~`stock_persist_days` 反用(天数越大越减分)~~ **已被取代**:H11 审计判定它是**非单调**的 —— 2-3 天(发酵态)在当前 regime 是最优注意力段,而单调减分函数会把它和「启动」压成同一方向。原写法保留在此只作留痕。
   - **打分映射必须是 pack 可配参数,不许写死在代码里**(H11 结论:**regime 敏感** —— 样本内「启动>发酵」,样本外 / 2026「发酵>启动」)。当前 regime 的**证据化初值**(落 K7-pack):`fermentation 1.0` / `ignition 0.6` / `divergence 0.4` / `ebb 0.2` / `none 0.2` / `overheat 0.0`。
   - **前向校准走 ⑨ 评价引擎周报 + 进化门禁(= 换包),不许顺手改。**
   - **缺行保险丝**:阶段表当日无该行业行 → 该维度取**中性分**并在 `mech_breakdown_json` 标 `stage_missing`,**绝不写 0**(0 是 `overheat` 的真实取值,与「没数据」撞车 —— 「没有」与「没看」必须分得开)。
3. **`leader_clarity` 龙头结构清晰度 = 簇内 RS 头名度(K7 需求 1a)**
   - 定义:成员在其**涨停簇内**按 **`ret_20d`(RS20)** 排名的**头名度** —— 头名最高分,**依名次衰减**(衰减函数是引擎常量,不进包;若日后要可配再走加原语流程)。数据源 = **`leader_structure_daily.rs_rank`**(④ 规划三列之一)。
   - **tie-break 定死(可复现铁律)**:**RS 降序 → 成交额降序 → `ts_code` 升序**(落点在 ④b-0,本块只消费)。
   - ⛔ **连板高度不进头名主定义**:十二格审计判定它是**双尾放大器**(涨停触达 1.5× 的同时,**次日跌停 3×**);它的用途在 **⑦-K7 的双尾标注**,不是加分项。⛔ **成交额头名审计否决,也不进**(它只在 tie-break 里当次级键)。
   - **簇定义兼容注(不必同构,但语义要对)**:审计用的是「(日, 行业) 涨停家数 ≥3」的粗簇(阈值 3/5 双档方向全稳);④ 的 `limit_cluster_daily` 聚类簇与它**不必同构**,但 `rs_rank` 的语义必须保持**「簇内相对强弱头名」**。完工记录里写明本块用的是哪个簇。
4. **`tradability` 可交易性**(一字比例与涨停占比 —— 买不进的涨停不算机会,承蓝图 4.9;可交易性审计:龙头组可买率显著更低);
5. **`card_density` 红黄牌密度**(`k4_advisory` 命中数;**降格为风险信息与模型特征,不等同禁买** —— 蓝图 7.2)。

**五维权重(K7 需求 2:证据化初值,由 K7-pack 携带)**

| 维度 | K7-pack 初值 | 依据 |
|---|---|---|
| `sector_strength` | **0.30** | 行业强度方向已审计(H6 / 现役) |
| `leader_clarity` | **0.30** | H10 十二格全正、2026 递增,两役里最稳的量 |
| `driver_freshness` | **0.10** | H11 方向真实但 **regime 敏感** → 低配,交前向校准 |
| `tradability` | **0.20** | 一字 / 涨停占比语义(龙头组可买率显著更低) |
| `card_density` | **0.10** | 红黄牌密度,K4 案底 |

- ⚠ **`K4-pack-v1` 的占位值 `0.25/0.20/0.25/0.20/0.10` 原样不动、不为此重发版**(需求 2 明文)—— 它是**回滚锚**,证据化初值只活在 K7-pack。
- ⚠ **权重是包里的数,代码里不许有第二份**:⑥ 读 `get_active_pack().tier_weights`,缺维度 → fail loud(不静默补默认值)。

**LLM 的权限边界(第〇原则 Tier 变体,定死)**:**只能在同档内微调排序并留痕,不得跨档**。跨档请求一律丢弃 + WARNING(守门单测);`llm_rank_delta` 与 `llm_reason` 落 `tier_history`,`rank_mech` 与最终 `rank_in_tier` **两个都存**(可复现、可归因)。

**容量**:T1 ≤ 2 / T2 ≤ 5 / T3 ≤ 10,**全部是上限非配额,允许 T1 为空**(市场混沌时不许凑数 —— 单测锁死)。

**白名单锁**:`_TIER_SCORE_INPUTS` 白名单 + 运行期访问锁(`_SORT_KEY_INPUTS` 体例平移):**LLM 产出的任何字段不得进机械分**。

**权重校准路径**:走 ⑨ 评价引擎的周度报告;**调参 = 换包**(过 §12 四道闸 + 用户批准),**不许顺手改代码里的数**。

**依赖**:③、**③-K7**、**④b**、⑤。
**验收**:定档复现单测(同输入两次运行 → tier 与序逐位相同);跨档微调被拒(单测);T1 空态是合法输出;容量上限不被突破;`_TIER_SCORE_INPUTS` 白名单单测锁死;换包(改权重)→ 序变化且 `mech_breakdown_json` 能解释变化;**K7 追加四条** —— ① 六态各喂一条数据 → `driver_freshness` 分值等于包里 `stage_scores` 对应值(单测,禁硬编);② 阶段表缺行 → 该维取中性分 + `stage_breakdown` 标 `stage_missing`,**断言不是 0**;③ `leader_clarity` 头名度随 `rs_rank` 单调衰减 + 三级 tie-break 生效(造 RS 并列、成交额并列两组数据);④ 用 K4-pack 与 K7-pack 各跑一遍同一份输入 → **权重差异真的改变了 Tier 序**(「插槽不是空架子」在 ⑥ 层面的兑现,补上 ③ 完工记录里登记的那条测试局限);**落库追加三条** —— ⑤ 三张表单事务落地(单测:故意让 `tier_history` 插入失败 → 断言 `baskets`/`basket_members` **一起回滚、库里零行**);⑥ 同日重跑 no-op 且**不覆盖**既有行(单测:改 tier 后重跑 → 库里仍是首跑的值 + WARNING 已发出);⑦ `save_baskets` 搬进 `basket_store.py` 后 **⑤ 既有 56 例逐条仍绿、一字未改**;pytest 零回归。

---

**落库职责(2026-08-02 planner 裁定,详见上方【跨块】节;**这是对原 Plan 的改判,以此为准**)**
- **⑥ 是 `baskets` / `basket_members` / `tier_history` 三张表的写入方**(原 Plan 把前两张登记在 ⑦,已改判 —— 因为 `baskets.tier` NOT NULL 而 tier 在 ⑥ 手里)。
- **单事务**:`INSERT baskets`(拿 `id`)→ `basket_members`(用 `id`)→ `tier_history`(用 `id`)。三张一起成功或一起回滚,不留半截。
- **复用 ⑤ 已实现的写入口**:`save_baskets(result, *, tier_by_basket_key, db_path|conn)` —— ⑥ 是它的**调用方**,把定档结果按 `basket_key` 组成 `tier_by_basket_key` 传进去。**⑥ 不自己再写一份 INSERT**(缺 tier 就 `ValueError` 的 fail-loud 语义原样保留)。
- 写入口统一搬进新模块 `neckline/selection/basket_store.py`(`aggregate.py` 同名再导出、行为逐字节不变),`save_tier_history(...)` 也住那里。
- **`INSERT OR IGNORE` 幂等 + 重跑不覆盖**(D0 冻结件);重跑结果与已冻结行不一致 → WARNING + 报告如实披露,**不静默**。

**完工记录(2026-08-02,@builder-pro)**

新建两个模块,**不碰 ⑦ 的任何东西**(`save_basket_card` 留给 ⑦ 自己加):

- **`neckline/selection/tier.py`** —— 五维机械分 + 定档 + LLM 同档微调,唯一编排入口
  `score_and_tier(result, trade_date, *, pack, db_path, parquet_dir, use_llm=False, provider, ledger)`。
  **`use_llm` 默认 False**(纯机械、零 LLM 调用),`use_llm` 与 `provider` 刻意是两个开关
  ——注入个桩不会意外走上 LLM 路径。
- **`neckline/selection/basket_store.py`** —— 篮子四表唯一写入口。`save_baskets` 从
  `aggregate.py` **原地搬来**(新增可选 `conn=`,不传时逐字节等价),`aggregate.py` 保留
  `save_baskets = _basket_store.save_baskets` 同名再导出(单测断言"是同一个函数对象");
  新增 `save_tier_history(...)` 与**事务 1** 入口 `save_tier_decision(...)`。

**五维机械分与访问锁的落点**

| 维度 | 数据源 | 聚合规则 | 算不出时 |
|---|---|---|---|
| `sector_strength` | `industry_strength_daily`(只读表)+ `sectors.compute_sector_strength` 概念热榜(既有在线路径) | 成员行业名次 → `1-(rank-1)/total`;两侧**取较大者**,概念只加成不减分 | 中性 + `sector_strength_missing` |
| `driver_freshness` | ④b `industry_stage_daily.stage` → **现役包** `tier.stage_scores` | 成员各行业分值取 max | 中性 + `stage_missing`(K4-pack 无该段 → `stage_scores_absent`;码不在映射里 → `stage_unmapped`) |
| `leader_clarity` | `BasketMemberCandidate.rs_rank`(⑤ 从 `leader_structure_daily` 解析) | `1/min(rank)` 倒数衰减(MRR 形态,无额外参数) | 中性 + `leader_clarity_missing` |
| `tradability` | 当日 `limit_derived` + `daily` 两个分区 | 一字扣 1.0 / 涨停开过板扣 0.5,`1-加权比例` | 中性 + `tradability_missing` |
| `card_density` | ⑤-b 带过来的 `member.k4_tag` | `1-命中数/成员数` | ⑤ 报 `k4_unavailable` → 中性 + `card_density_missing` |

**中性分 = `NEUTRAL_DIM_SCORE=0.5`,任何一维算不出都走它 + `flags` 如实标,⛔ 永不写 0**
(plan 只对 `stage_missing` 点名,这里统一到全部五维——理由同一条:0 是 `overheat` 的真实
取值)。**访问锁**:`build_tier_row()` 产出的特征行**同时装着**五个机械维度与六个 LLM 产出
字段(`llm_name`/`llm_driver`/`llm_why_now`/`llm_member_reasons`/`llm_roles`/
`llm_evidence_claims`),`mech_score(row, weights)` 只许读 `_TIER_SCORE_INPUTS` 五键——单测用
记录访问键的 dict 子类在**运行期**断言 `accessed == _TIER_SCORE_INPUTS` 且
`isdisjoint(_LLM_PROVENANCE_KEYS)`,配一条"行里真的有 LLM 字段可读"的可证伪性断言(否则
"没读到"只是因为压根没有)。权重 `resolve_weights(pack)` **缺维度/多出引擎不认识的维度
双向 fail loud**,并做**归一化**(K4/K7 权重和本就为 1,是恒等变换)——归一化是让机械分恒在
[0,1],档位线才谈得上可比。

**定档与容量**:`(机械分降序, basket_key 升序)` 排定(⛔ 不靠行序,CLAUDE.md rank 确定性
铁律)→ 每档一道**质量线**,够格才进,够格但满了**向下顺延**,三档都满 → 今日不定档
(`capacity_overflow` 留痕、不落库,`baskets.tier` NOT NULL 不许臆造)。

**LLM 同档微调闸与留痕形状**:`TASK_TIER_RANK` + 不联网 + 扣 `LEDGER_REASON`;user 消息首行
恒 `prompt_context.date_anchor_line`,system prompt 内嵌 `TIMELINESS_RULES`。产出走
`llm/json_block.split_narrative_and_reference_json` **先剥 JSON 再谈解析**(v1.5.1 标签劫持案),
**不复用 `_parse_verdict`**(静态守门单测)。提案形状
`{"adjustments":[{"basket_key","tier","rank_in_tier","reason"}]}`,六类拒收码语义不合并:
`cross_tier`(**跨档一律整条丢弃 + WARNING**)/ `unknown_basket` / `bad_rank` / `slot_taken`
(名次先到先得)/ `duplicate_key`(同一篮子被提两次,放行会让它占两个坑、剩余篮子填空位时
直接崩)/ `malformed`。未被有效提案指定名次的篮子按机械序填剩余空位 → "只动一个"时其余
相对次序不变、可复现。留痕 = `rank_mech` 与 `rank_in_tier` **两个都落库** +
`llm_rank_delta = rank_mech − rank_in_tier`(正 = 被往前提)+ `llm_reason`。
LLM 缺席 / 调用失败 / 预算耗尽 / 解析失败 → **机械序原样用**,`llm_stage` 五态如实 +
`notes` 加 `tier_rank_unadjusted:{stage}`;每档只有 1 个篮子时**根本不发起调用**。

**事务 1**:`save_tier_decision()` 在**同一个 `with connection()`** 里
`INSERT baskets`(拿 id)→ `basket_members`(用 id)→ `tier_history`(用 id);回滚靠
`connection()` 只在正常退出才 `commit()` 这一既定语义(单测 monkeypatch 让
`_save_tier_history_on_conn` 抛错 → 三张表**库里零行**)。四表 `INSERT OR IGNORE` 冻结不覆盖;
**重跑不一致不静默**:`frozen_conflicts` 逐条人读描述随 stats 返回 + WARNING「今日已有冻结
篮子,本次重算结果未采纳(差异 N 处)」,由报告层披露。

**本地冒烟(真实交易日 2026-07-24 + 真 parquet + LLM 全桩 + 真库只读副本)**:
K7-pack 激活于副本 → 18 天行业强度 1980 行 / 阶段表 1980 行 / 涨停簇 490 行 / 相关 12650 行 /
龙头结构 490 行 → **206 颗种子(截断 20 颗)→ ⑤ 出 15 个篮子**(卫生线剔 59 只,拒收 0)→
**⑥ 定档 T1 = 2(满)/ T2 = 5(满)/ T3 = 8,溢出 0**,机械分 0.810 ~ 0.280;LLM 桩的**跨档提案被
拒**(`cross_tier`)、档内调序**生效**(`fdef7327` 机械 #5 → 最终 #1,Δ=4,`llm_reason` 落库);
事务 1 落 `baskets 15 / basket_members 22 / tier_history 15`,**重跑 = 全 existing、0 冲突、
库里逐位不变**。真实 `data/neckline.db` 全部 V2 表**仍 0 行**、现役章程仍 `K1` 未动。

**测试**:新增 `tests/test_selection_tier.py` **86 例**(五维各自的算得出/算不出两路 + 六态
参数化 + 三级 tie-break 端到端〔造 RS 全并列 + 成交额并列两组数据,过 ④ 的
`compute_leader_structure_for_day` 再喂本维〕+ 访问锁 + 容量四路 + LLM 十一路 + 事务 1 七路 +
静态守门五条);`tests/test_selection_primitives.py::_ENGINE_CONSTANT_WHITELIST` 补登记 tier.py
的 8 个引擎常量。`python -m pytest tests/ -q`:2427 → **2516 passed + 2 skip + 2 fail**
(2 fail 仍是同名同因既有周日时间炸弹,与本块无关)。⑤ 的既有 63 例**一字未改、逐条仍绿**。

**如实登记的设计判断(均未改 Plan,如与规划意图不符请澄清)**

1. **⚠ 最要紧一条:Plan 没给「档位质量线」的数,是 builder 定的临时工程默认**
   (`TIER1_MIN_SCORE=0.60` / `TIER2_MIN_SCORE=0.40`,T3 无下限)。**为什么必须有它**:
   「上限非配额、允许 T1 为空、市场混沌时不许凑数 —— 单测锁死」这句话在**纯排名制下不可
   实现**(只要当天有 ≥1 个篮子,T1 就永远填满,"T1 为空"这条单测根本写不出来)。故必须有
   一道**绝对分数**的质量线。取值理由:五维各自归一到 [0,1]、权重归一后加权和亦在 [0,1],
   0.5 是"整体中性"的自然基准,T1 取明显好于中性、T2 取不明显差于中性。**它带策略语义**
   (多好才配叫 T1),按 ⑤-c 的先例本该进包交策略线校准,但 K4-pack / K7-pack 都没有这个
   键、Plan 也没授权 ⑥ 扩包 schema(那是 ③ 的地盘 + 换包四道闸),故先做引擎常量并在
   `_ENGINE_CONSTANT_WHITELIST` 里写明"临时默认、要可配须走包扩容"。**请 planner 裁定是否
   转为包参数**;在那之前⛔不许在代码里顺手改数。
2. **`leader_clarity` 的衰减函数选了倒数衰减 `1/rank`**(plan 只说"依名次衰减、衰减函数是
   引擎常量",没给函数形态)。选它的理由:MRR 的标准形态、单调、上界 1.0、**零额外参数**
   ——任何带底数的几何衰减都要再发明一个常量。副作用如实登记:`rank=2` 恰好等于
   `NEUTRAL_DIM_SCORE=0.5`,即"第 2 名"与"这一维没数据"**数值上撞车**;两者靠
   `flags` 里的 `leader_clarity_missing` 区分(与 `stage_missing` 同一机制),但如果日后要让
   数值本身也能分开,得换衰减函数或挪中性分。
3. **中性分统一到全部五维**(plan 只对 `driver_freshness` 的缺行点名)。理由是同一条:任何
   一维用 0 表示"没数据"都会与该维的真实低分撞车;`flags` 是唯一可靠的区分手段。
4. **`sector_strength` 的概念侧只加成不减分**(取行业侧与概念侧的**较大者**)。plan 只写
   「`industry_strength_daily` + 概念板块强度」没给合成规则;取 max 的理由是"不在今日概念
   热榜上 ≠ 弱,只是这一路没有证据"——取平均会让"没有证据"变成"扣分",正是「没有」与
   「没看」混为一谈。概念侧走 `report/sectors.py::compute_sector_strength`,它是 v1 报告
   **既有的在线路径**(`intel.py`/`intel_candidates.py` 都在调),不是本块新开的全历史扫描。
5. **`tradability` 的两档扣分比例(一字 1.0 / 涨停开过板 0.5)是 builder 定的**;plan 只给了
   语义(「一字比例与涨停占比」「买不进的涨停不算机会」)。一字 = 全扣有硬依据(当日全天
   最低价没低于涨停价 = 物理上买不进);0.5 是"有过成交机会但不好买"的折中,**无证据支持
   具体数值**,与档位线同属"日后可校准"那一类。
6. **簇定义如实交代(plan 要求写明)**:`rs_rank` 来自 ④ 的 `leader_structure_daily`,其簇 =
   `limit_cluster_daily` 的聚类簇(同日簇 + 连板簇),**与 K7 审计用的「(日,行业) 涨停家数
   ≥3」粗簇不同构**;语义按 plan 要求保持「簇内相对强弱头名」。一票同属多簇时由 ⑤ 的
   `_resolve_mech_role(prefer_cluster_keys=...)` 择一,⑥ 只消费其结果、不另挑簇。
7. **`rank_mech` / `rank_in_tier` 都定义为「档内 1-based」**(DDL 只写了"最终序/机械序"未
   言明范围)。理由:`llm_rank_delta = rank_mech − rank_in_tier` 只有同一坐标系下才有意义;
   跨档比较本就被禁,全局序在本块无用武之地。
8. **容量溢出的篮子今日不落库**(plan 未明说溢出怎么办)。`baskets.tier` NOT NULL,给它们
   臆造一个 tier 才是错的;溢出留痕在 `TierResult.dropped` + `notes`,由报告层披露。⚠ 这与
   「⛔ 不许因为没卡就把篮子从报告里抹掉」不是一回事——那条治的是"有篮子无卡",这里是
   "今天根本没给它定档"。**若 planner 认为溢出篮也该有个档,请裁定**(可选方案:全部塞
   T3 并放宽 T3 容量,但那会突破 plan 写死的 `T3≤10`)。
9. **`save_basket_card()` 没写**——跨块裁定说它也住 `basket_store.py`,但那是 ⑦ 的写入口,
   本块不越块;⑦ 直接往这个模块里加即可。
10. **`_save_baskets_on_conn` 的日志前缀保留 `[aggregate]`**(而不是改成 `[basket_store]`)
    ——⑤ 的既有单测 `test_replay_is_idempotent_and_does_not_overwrite` 按 message 断言
    「幂等跳过」,前缀虽不在断言里,但"搬迁后行为逐字节不变"的承诺覆盖日志文本;改前缀
    属于无收益的行为变更。

#### ⑥-b · 档位质量线进包 + T3 下限 + 溢出篮披露(2026-08-02 planner 裁定;**⑥ 主体已完工,本子项是追加施工项**)✅ 完工(2026-08-02)

> **来源**:⑥ 完工记录登记 ① 与 ⑧ 两问。builder 的两处临时处置**都是对的**(有质量线才谈得上「T1 可空」、溢出篮不臆造 tier),本子项只做归属裁定与缺口补齐。

**⑥-b-A 裁定:档位质量线 → 进包(`config.tier.quality_lines`),不是引擎常量**

- **判据照 ⑤-c 立的框架**:引擎常量治**统计 / 工程有效性**,pack 参数治**策略偏好且 regime 敏感**。质量线属后者 —— 「多好才配叫 T1」是注意力分配的策略选择,且**市场整体强弱会平移分数分布**(强市遍地 >0.6、弱市可能一个都没有),与 `stage_scores` 同族。
- **决定性理由(比 regime 敏感更硬,写下来防以后重开)**:**质量线与五维权重作用在同一个标度上** —— 权重一改,加权和的分布就跟着变,而**权重已经在包里**。把线留在代码里 = 策略线换一次权重就会**静默改变 T1 的选择性**,而且没人会注意到。**两个数必须住在同一个地方,才可能一起校准。** 这是把它从"像 ⑤-c 那样留在引擎里"拽出来的关键差别:⑤-c 的门槛(成分数 5)与包里任何数都不共享标度,质量线共享。
- **schema 形状(定死)**:`config.tier.quality_lines = {"tier1_min": 0.60, "tier2_min": 0.40, "tier3_min": 0.25}`,与 `weights` / `dims` / `stage_scores` 平级,**可选键**。
- **`engine_api_version` 判定 —— 照 ③-K7-C 的规则核过:不 bump,维持 1**。依据逐条:① 纯增量的**可选**键;② `packs/K4-pack.json` 原样重新校验仍通过(它没有这个键);③ 缺键时回退引擎默认值 = ⑥ 现有的 `TIER1_MIN_SCORE`/`TIER2_MIN_SCORE`,故**旧包行为逐位不变**。实施落点 = `neckline/selection/pack.py::validate_config`(与 ③-K7 的 `stage_scores` 同一处,不另起校验)。
- **⚠ 缺键回退 vs 权重的 fail loud,两种姿势刻意不同,别"统一"**:`weights` 缺维度 → **fail loud**(每个包 schema 都必须有它,缺了就是包坏了);`quality_lines` 缺 → **回退引擎默认**。原因只有一个:**`K4-pack-v1` 原样不动、不重发版**(需求 2 明文,它是 ⑯-E 的回滚锚),而它没有这个键 —— 不给回退路径就等于把回滚锚作废。**两条各自的理由已写死在这里,谁都别去"修正"成一致。**
- **引擎常量不删,降级为「缺省回退值」**:`TIER1_MIN_SCORE` / `TIER2_MIN_SCORE` 留在 `_ENGINE_CONSTANT_WHITELIST` 里,但白名单注释改写成「**pack 未给 `quality_lines` 时的回退默认**」,不再是权威。
- **`packs/K7-pack.json` 补键**:`quality_lines` 三个值填 `0.60 / 0.40 / 0.25`。⚠ **现在补是合法的** —— ③-K7 已写明「**⑯-E 之前是最后的改包窗口**」(`--confirm` 写库那一刻内容才冻),K7-pack 尚未激活。
- **前向校准路径,与 `stage_scores` 同一句话**:走 **⑨ 评价引擎周报 + 进化门禁(= 换包)**,**⛔ 不许顺手改代码里的数、也不许顺手改包里的数**。

**⑥-b-B 裁定:T3 也要有下限(纠正「T3 无下限」)**

- **为什么必须补**:`T3 ≤ 10` 在**没有下限**时是**事实上的配额** —— 只要当天有 ≥10 个候选,T3 就永远填满 10 个。**这正是 V1「每天硬凑 20 只候选」那个被 V2 立项要治的病**(§1.5 第 4 条:每日 20 只高度重复);蓝图 4.4 的「以上均为**上限,不是必须填满的配额**」是**对三档同时说的**,不只对 T1。builder 把「T1 可空」做成了单测,但 T3 这一档漏了同一条原则。
- **`tier3_min` 初值 = 0.25(planner 拟的临时工程默认)**。理由:比 `tier2_min=0.40` 明显低(承认 T3 是「观察与储备级」,门槛本就该松),但**不为零**(为零就退回配额)。**没有证据支持这个具体数字**,与 0.60/0.40 同批交校准(见 §七 P3-33)。
- **三档全部可空,极端情况「今日无篮子定档」是合法输出**:报告必须能如实写出来(「今日无篮子达到定档标准」),**⛔ 不许为了让报告好看而放宽任何一条线**。单测:构造一批全部低于 `tier3_min` 的篮子 → 三档皆空、不抛异常、报告照出。

**⑥-b-C 裁定:容量溢出篮不落库(采纳 builder 处置),但**必须在报告里露脸****

- **落库口径定死(采纳登记 ⑧)**:溢出篮**不臆造 tier、不落 `baskets`、不落 `tier_history`**,留痕在内存态 `TierResult.dropped`。~~全部塞 T3 并放宽 T3 容量~~ **否决**(会突破 plan 写死的 `T3≤10`,且把「上限」变回「配额」,与 ⑥-b-B 正相反)。~~改 `baskets.tier` 为可空~~ **否决**(① 已完工、要重建表,代价大而收益只是表达一个"今天没定档"的状态 —— 那本来就不该在 `baskets` 里表达)。
- **与「⛔ 不许因为没卡就把篮子从报告里抹掉」的边界(builder 问的就是这条,在此写死)**:两者**不矛盾,治的是两件事** ——
  - 那一条治的是「**系统已经作出并落库的判断**(已定档的篮子)因为下游失败而被藏起来」→ **藏已有的判断 = 不诚实**;
  - 溢出篮是「**系统今天明确没有给出判断**」→ 不落库是**如实**,给它编一个 tier 才是不诚实。
- **但「今天有 N 个候选没被定档」这个事实本身必须披露** —— 否则用户会以为"今天全市场就这 15 个篮子",而真相是被容量截断了。这是「**没有**」与「**没看**」纪律在容量维度上的兑现。
- **露脸的形状(定死,只到摘要级)**:报告与契约给一行摘要 + 可选清单 —— 篮子名 / 机械分 / **未定档原因码**;**不出卡、不进 Tier 表、不进 `baskets`、无 `basket_id`**(它没有身份,别给它造一个)。
- **⚠ 原因码必须区分两种「没进来」,混为一谈会让用户读错市场**:
  - `capacity_overflow` —— **分数够、位置满**(说明今天机会多到装不下);
  - `below_quality_line` —— **分数不够,连 `tier3_min` 都没过**(说明今天没什么好货)。
  - 这两个指向相反的市场结论,**合并成一个"未入选"是信息损失**。
- **⑭-A 的报告结构据此补一行**(见 ⑭-A);契约侧挂在篮子日报响应里(`droppedBaskets: [{name, mechScore, reason}]`,默认空数组),**不新建表**。

**⑥-b-D 顺带钉死:中性填充必须可见(承 ⑥ 完工记录登记 ②③)**

- ⑥ 把**五维缺数据一律填中性分 0.5**,而 `leader_clarity` 的 `1/rank` 在 `rank=2` 时**恰好也等于 0.5** —— 数值上「第 2 名」与「这一维没数据」撞车,只能靠 `flags` 区分。
- **纪律(定死)**:**任何消费 `mech_breakdown_json` 的下游(⑨ 评价引擎 / ⑭ 报告 / ⑮ 客户端)必须同时读 `flags`,⛔ 不许只看数值**。守门单测:⑨/⑭ 里凡读 breakdown 数值处必伴随 flags 读取(grep 或结构断言)。
- **新增一个可审计字段**:`tier_history.mech_breakdown_json` 里落 **`neutral_filled_weight`**(本篮加权和中由中性填充贡献的权重合计)。**为什么要它**:一个三维缺数据的篮子,靠三个 0.5 的填充也可能压过 `tier1_min=0.60` —— 那就成了「**因为不知道所以进 T1**」。
- **本子项只记录 + 披露,⛔ 先不设闸**(不凭空造一个"缺数据超过 X 就降档"的阈值 —— 承裁定 #5「不设专项防线」与「先观测再立规」)。**要不要据此降档,一并进 §七 P3-33**,等 ⑨ 攒出样本再说。

**⑥-b 验收**:`quality_lines` 三键的 schema 正负例(缺键回退引擎默认 / 非法值拒绝 / 三线不单调〔如 `tier1_min < tier2_min`〕→ **fail loud 拒绝激活**);**`packs/K4-pack.json` 零改动且重新校验仍通过、⑥ 对它的输出逐位不变**(回滚锚不许被打坏 —— 硬判据);`packs/K7-pack.json` 三键已补且演练 exit 0;`ENGINE_API_VERSION` **仍是 1**(单测断言,并在完工记录里按 ③-K7-C 逐条写明为什么不 bump);T3 下限生效(单测:一批分数 0.10~0.20 的篮子 → **T3 为空**而不是被塞满);三档皆空是合法输出且报告照出;溢出摘要两种原因码分得开(单测各一);`neutral_filled_weight` 落 breakdown 且数值正确(单测:三维缺数据 → 该值 = 那三维权重之和);⑥ 既有单测**一字不改逐条仍绿**;pytest 零回归。

**⑥-b 依赖**:③(pack schema)、⑥。**不依赖 ⑦**,可与 ⑦ 并行(文件面不相交:⑥-b 动 `pack.py` / ⑥ 的定档模块 / `packs/K7-pack.json`,⑦ 动卡生成)。

**完工记录(2026-08-02,@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ⑦ 并行施工,文件面不相交)**:`neckline/selection/pack.py` 新增 `_validate_quality_lines()`(挂 `validate_config`,与 `stage_scores` 同一处)+ `Pack.tier_quality_lines()` 访问器;schema 定死 `config.tier.quality_lines={tier1_min,tier2_min,tier3_min}`,三键**各自独立可选**(同 `stage_scores` 纪律);非数值 / `bool` 值拒绝(同 `_validate_stage_scores` 陷阱防线);**单调性检查只比较字面给出的键**(`_QUALITY_LINE_ORDER` 顺序逐对相邻比较、靠传递性覆盖跨档缺省键的组合),**不合并引擎默认值再比较**——决定性原因是 `pack.py`⛔不能 import `tier.py` 的具体默认数字(方向相反会成环,`tier.py` 已反向 import `Pack`/`get_active_pack`),此为如实登记的**范围限定**:一个"只给 `tier3_min` 一个键、且该值与另两档隐藏的引擎默认组合后其实不单调"的畸形配置不会被这条校验拦下(概率低——两份现役包要么整段不给要么三键齐给),留档供 ⑨/审计参考。`neckline/selection/tier.py`:`TIER1_MIN_SCORE`/`TIER2_MIN_SCORE` 白名单注释按裁定改写为"缺省回退值"说明,新增 `TIER3_MIN_SCORE=0.25`;新增 `resolve_quality_lines()`(逐键回退,与 `resolve_weights()` 的 fail-loud 姿势刻意不同,docstring 写死两条理由防被"统一");`_eligible_tier()` 返回 `Optional[int]`(够不到 `tier3_min` → `None`);`assign_tiers()` 新增 `quality_lines` 形参(默认引擎值,既有调用方零改动)+ 新原因码 `DROP_BELOW_QUALITY_LINE`(与 `DROP_CAPACITY_OVERFLOW` 分开留痕,`score_and_tier()` 的 `notes`/WARNING 日志也逐原因码计数、不再把两种"没进来"揉成一句话);新增纯函数 `neutral_filled_weight(flags, weights)` + `_DIM_MISSING_FLAGS` 映射(只认 flags 不认数值,专门躲开 `leader_clarity` 在 `rank=2` 时与"缺数据"中性分数值撞车的陷阱),落 `mech_breakdown_json.neutral_filled_weight`;`TierResult` 新增 `quality_lines` 字段(本次实际生效的三线,审计快照,同 `weights` 字段性质)。`packs/K7-pack.json` 补 `quality_lines:{0.60,0.40,0.25}`(唯一改动,`K4-pack.json` 零改动);两包 dry-run 均 `exit=0`(`--db` 显式指向隔离临时库,真实 `data/neckline.db` 全程只读,mtime 核对不变)。**「消费 `mech_breakdown_json` 必须同时读 flags」未落成自动化仓库扫描**(如实登记:排查确认目前唯一的准消费者 `basket_card.py`〔⑦ 并行新建〕是整段 dict 原样透传〔`flags` 随 `dims` 一起进卡〕不是取值判断,不构成违反;⑨/⑭/⑮ 尚未落地,此刻写一个 repo-wide 文本/AST 扫描器要么扫不到真目标要么误伤合法透传代码,故改以 docstring 明文纪律 + `TestNeutralFilledWeightEndToEnd::test_rank_two_is_not_mistaken_for_missing_in_the_full_pipeline` 可执行示范兜底,**留给 ⑨/⑭/⑮ 落地时补一条真正打在它们身上的静态守门**)。测试:`test_selection_pack.py` +17(`quality_lines` 校验正负例 12 + 访问器 2 + 真实包文件 2 + `ENGINE_API_VERSION` 断言 1)、`test_selection_tier.py` +15(`resolve_quality_lines` 4 + T3 下限与原因码拆分 3 + 三档皆空端到端 1 + notes 双原因码端到端 1 + `neutral_filled_weight` 纯函数 4 + 端到端含 `rank=2` 撞车 2)、`test_selection_primitives.py::_ENGINE_CONSTANT_WHITELIST` 改写 `TIER1_MIN_SCORE`/`TIER2_MIN_SCORE` 说明 + 补 `TIER3_MIN_SCORE` 条目。`pytest tests/ -q`:2516(基线)→**2651 passed + 2 skip + 2 fail**(本子项净增 32、零回归;2 fail 仍是同名同因既有周日时间炸弹`test_api_positions.py::test_buy_date_historical_trading_day_written_through`/`test_fix_position_buy_dates.py::test_unchanged_position_does_not_bump_updated_at`,今日 2026-08-02 实系周日;余下净增来自并行 ⑦ 的新文件,文件面不相交)。⑥ 既有 86 例**一字未改、逐条仍绿**。下一步:⑦ 已在并行施工(篮子卡冻结),⑥-b 与它均不互相阻塞;两者收尾后系统线下一个未开工块是 **V2-⑧**(D+1 验证:关注池改造 + 盘中存拍 + 验证状态机)。

---

### V2-⑦ · ④篮子卡冻结:`baskets` / `basket_members` / `basket_cards`(@builder)

**目标**:D0 盘后为每个篮子冻结一张不可回溯修改的卡。**每个数字要么机械算出、要么过机械闸(夹逼 / 白名单 / 对拍),无裸奔 LLM 数字。**

**卡内容 = 蓝图 4.6 全项(逐项,缺一不可)**
1. 篮子名称与共同驱动;2. 驱动证据与信息来源(**每条带日期**);3. 为什么是现在;4. 成员、角色与比较结果(含对拍分歧);5. Tier 及分层理由;6. 次日**强 / 平 / 弱三种竞价剧本**;7. **建仓观察区间 + 最高追价**;8. 篮子验证条件(**结构化 + 人话双份**);9. 篮子失效条件(**结构化 + 人话双份**);10. 主要风险;11. `disclaimer`(固定文案,**单一源常量**,客户端原样透传不改写)。

**工程体例继承 v1.5 参考件(逐条,不重新发明)**
- **夹逼**:建仓观察区间与最高追价的每个数字**必须落在次日涨跌停闭区间内** —— `limit_derived.compute_intraday_limit_prices(pre_close=D0 close, board, is_st, trade_date=next_trading_day(D0))`;`board` / `is_st` 走 `sentinel/universe.load_stock_meta`(唯一源,不自己判 ST 前缀 / 不自己分板块);`next_trading_day` 走 `neckline.calendar`。**越界不显示** + 三态分开落行:`rejected_out_of_limit`(越界)/ `rejected_malformed`(缺数、非数、low>high)/ `absent`(LLM 压根没给)。算不出涨跌停 → `rejected_no_limit`。
- **离场参考只做格式合法性校验,不夹涨跌停**(压力位可能几个交易日后才到,拿明日涨跌停夹它是错的)。
- **止损价系统算、不由 LLM 给**:`round(close × (1 − stop_pct), 2)`,`stop_pct` **读现役 config**(禁硬编 0.05)。
- **口径指纹落行**:`stop_pct` / `take_profit_retrace` / `charter_version` / `pack_version` / `engine_api_version`;**卡上的纪律标签动态生成**(缺指纹就退化成不带数字的说法,禁把「−5%」「8%」写进模板)。
- **验证 / 失效条件双份**:`card_json.verification_spec` / `invalidation_spec` = **结构化,是 ⑧ 哨兵的唯一判据源**;人话剧本 = LLM 生成,**只给人看**。生成剧本时把结构化阈值喂给 LLM,使剧本与盘中自动警报**同频**(v1.5-①-A 体例)。
- **D+1 只能追加**:`basket_cards` 追加版本制(`version=1` 是 D0 原判,冻结);新信息写 `version=2,3…`,**D0 行一字不改**。

#### ⑦-K7 成员标注件:「龙回头位」+ 双尾警示族(K7 需求 5;**只标注,不进排序、不加分**)

> 来源:`archive/交接_K7_系统线需求_20260802草案.md` 需求 5(2026-08-02 用户转发,证据链 `research/k7_pre2_report.md` H12/H13)。**用户已裁定:只标注。**
> **⛔ 四不(硬约束,守门单测)**:**不进 `_TIER_SCORE_INPUTS`**、**不进排序键 `dims` 白名单**、**不进哨兵判据**、**不改篮子 / 成员去留**。它们是**纯展示位**,落在卡的「成员、角色与比较结果」节里,**每处带「参考、非指令」标注**。

**落库职责(2026-08-02 planner 裁定,详见上方【跨块】节;**这是对原 Plan 的改判,以此为准**)**
- **⑦ 只写 `basket_cards` 一张表**(原 Plan 把 `baskets`/`basket_members` 也登记在本块,已改判给 ⑥ —— 那两张要 tier,tier 在 ⑥ 手里)。本块块名里的三张表名**保留作历史留痕**,实际写入以本条为准。
- **⑦ 进来时 `baskets.id` 已经存在**(⑥ 的事务 1 已落),卡直接挂 `basket_id`;写入口 `save_basket_card(...)` 住 `neckline/selection/basket_store.py`,**单独一个事务**(不与 ⑥ 合并 —— 卡生成要调 LLM,跨 LLM 调用持事务是错的)。
- **「有篮子、无卡」是合法中间态**:LLM 不可用 / 预算耗尽 / 生成失败 → **不回删篮子、不抛异常**,如实留 reason;**同一 D0 内可重跑补 `version=1`**(仍是 D0 原判)。契约侧 `GET /baskets/{id}/card` 无卡返 **404 + reason `card_not_ready`**(⑭-B 补 reason + 客户端 `mapReason` 加 case),报告如实写「本篮卡未生成({reason})」。**⛔ 不许因为没卡就把篮子从报告里抹掉。**

- **落库**:标注结果进 `basket_cards.card_json.members[].tags`(冻结快照的一部分,与卡同生共死),**不新建表、不新建列**;`basket_members` 表结构不动。
- **判据全部机械算、零 LLM**(它们是价量结构判定,不是叙述):

| 标签码 | 中文名(展示层) | 机械判据 |
|---|---|---|
| `pullback_leader` | 龙回头位 | **强势资格**(近 20 日涨停 **≥2** 且 `ret_20d ≥ +25%`)× **回调态**(距 20 日高 **−25% ~ −8%** 且 `close > ma20`)× **企稳日**(当日缩量 且 涨跌幅 **−3% ~ +2%**) |
| `warn_streak_top` | 双尾警示 · 连板头名 | 簇内**连板高度第一**(`leader_structure_daily.limit_height` 头名) |
| `warn_chase_zone` | 双尾警示 · 强势追入位 | **强势资格**(同上)且 **距 20 日高 > −3%** |

- **展示文案要点(照交接稿,数字必须带来源与「参考」标注,禁写成收益承诺)**:
  - `pullback_leader`:「机会密度约为市场的 **1.8×**,但 3 日跌停率也是市场的 **4–6×**(**双尾**);同为强势票,回调位的跌停率约为追入位的 **1/3**。—— 参考、非指令。」
  - `warn_streak_top`:「簇内连板高度第一:涨停触达约 1.5×,**次日跌停约 3× 于后排**。—— 参考、非指令。」
  - `warn_chase_zone`:「强势且贴着 20 日高:3 日跌停率 **15–19%**,三段样本一致。—— 参考、非指令。」
- **不重复建标(交接稿明文)**:**过热态由 A2 红牌承接**、**年线下放量族由 A3 / B1 现有牌承接**,⑦-K7 不再造第二套。
- **口径单一源**:`neckline/selection/member_tags.py`(判据 + 码 + 中文名 + 文案模板同一处);篮子卡与 ⑬-N 信息卡**读同一份**,禁两处各写一遍(v1.5 自选 / 持仓两侧 K4 镜像的交叉断言体例平移)。
- **缺数据 → 不打标**(不猜):`ret_20d` / 20 日高 / `ma20` / 连板高度任一算不出 → 该标签 `absent` 且不显示,**不写 false 冒充「已判定为否」**。

**依赖**:⑥。
**验收**:卡 11 项齐全的 golden 快照单测;夹逼四态各一单测(界内通过 / 越涨停被拦 / low>high 被拦 / 算不出涨跌停被拦),被拦时该项为 `null` 且 reason 精确;止损随现役 `stop_pct` 变而变(改测试库 config → 输出跟着变);冻结单测(同 `(basket_id, version)` 二次写 → 拒);结构化阈值确实出现在喂给 LLM 的上下文里(单测);`disclaimer` 单一源;**⑦-K7 追加三条** —— ① 三个标签各一条命中 / 不命中单测 + 缺数据 `absent` 单测;② **四不守门**:grep 断言 `member_tags` 的标签码不出现在 `_TIER_SCORE_INPUTS` / 排序键 dims 白名单 / `neckline/sentinel/` 全目录,且打标前后 **Tier 序与成员去留逐位不变**(同一份数据跑两遍,一遍关标注一遍开标注);③ 文案每处带「参考、非指令」(渲染层 grep);pytest 零回归。

#### ✅ V2-⑦ 完工记录(2026-08-02)

**落地**:新建 `neckline/selection/basket_card.py`(卡装配 + 夹逼 + 结构化 spec + 卡 LLM 段,
`build_cards()` 是编排入口、`use_llm` 默认 False 可完全离线)与
`neckline/selection/member_tags.py`(⑦-K7 标注件**唯一实现**,⑬-N 信息卡将读同一份);
`basket_store.py` 增**事务 2** 三个入口 `save_basket_card` / `save_basket_cards` /
`load_basket_card` + `next_card_version`。⑦ **只写 `basket_cards`**,与 ⑥ 的事务 1 全程分开。

- **`card_json` 形状(⑮ 客户端契约的源头,snake_case —— plan 原文写的就是
  `card_json.verification_spec` / `members[].tags`,且 ⑧ 哨兵是 Python 直接消费方;
  转 camelCase 归 ⑭ 契约层)**:34 个顶层键 + 26 个成员键,由
  `test_card_json_key_shape_is_stable` 逐键锁死。蓝图 4.6 十一项落点 = `name`/`driver`/
  `driver_kind`(①)、`evidence[]`+`evidence_status`(②)、`why_now`(③)、`members[]`+
  `role_conflicts`(④)、`tier`/`rank_in_tier`/`rank_mech`/`mech_score`/`tier_breakdown`/
  `tier_reason`/`tier_note`(⑤)、`scripts.{strong,flat,weak}`(⑥)、`members[].entry_zone`+
  `max_chase`(⑦)、`verification_spec`+`verification_text`(⑧)、`invalidation_spec`+
  `invalidation_text`(⑨)、`risks[]`(⑩)、`disclaimer`(⑪);另加 `fingerprint` 五项 +
  `discipline_labels` + `llm_stage`/`degraded`/`notes`。**`spec_version` 恒随形状变化而变**。
- **冻结 / 版本制**:`INSERT OR IGNORE` on `UNIQUE(basket_id, version)`;同版本二次写 = no-op
  且**不覆盖**,内容不一致时 WARNING + `frozen_conflicts` 带出给报告层如实披露(不静默)。
  `card_json` 序列化用 `sort_keys=True`,冻结件才逐字节可比。`version=1` 显式默认,
  **自增留给 `next_card_version()`**(默认自增会把「同一 D0 重跑补 v1」变成流水账)。
- **夹逼**:建仓区间 / 最高追价各自独立落四态(`ok`/`absent`/`rejected_out_of_limit`/
  `rejected_malformed`/`rejected_no_limit`),锚 = `compute_intraday_limit_prices(D0 close,
  board, is_st, next_trading_day(D0))`,`board`/`is_st` 走 `load_stock_meta`。**离场参考只校验
  格式、不夹涨跌停**(它是 ⑩ `position_plans.plan_json` 要继承的那一项)。另加一条自洽校验:
  最高追价 < 区间上沿 = 自相矛盾 → 拦下追价、区间照留。
- **止损与指纹**:`stop_price = round(close × (1 − stop_pct), 2)`,`stop_pct` 只从
  `brain.active_config()` 来;纪律标签 `discipline_labels()` 动态生成,缺指纹退化成不带数字
  的说法。守门单测用 AST 扫**非 docstring** 常量,断言代码里没有 0.05/0.08 与「−5%」类模板。
- **LLM 段**:路由 `TASK_SCRIPT`、**不联网**、预算走 `LEDGER_REASON`;输出走
  `json_block` **先剥再用**,本链路没有结论标签也不复用 `_parse_verdict`。降级时
  **结构化半份照出**(spec / 机械锚 / 指纹 / 标注 / disclaimer 全在),人话半份缺席并在
  `llm_stage` 标明原因。成员白名单闸:`entries` 里出现篮外代码 → 整条丢弃 + WARNING。
- **⑦-K7**:三标签判据逐字取自 `research/k7_pre2_report.md` §1 / `k7p_h12_pullback.py`,
  阈值全部落在 `evaluate_member_tags()` 的**关键字默认值**上(白名单扫描认可的两种合规形态
  之一,故 `member_tags.py` 只需登记一个 `_EPS`)。三态 `tags`/`missed`/`absent` 语义不合并。
- **冒烟(真实交易日 2026-07-24,全桩零真实 LLM,隔离库)**:④ 206 颗种子(截断 20)→ ⑤ 2 篮
  → ⑥ 全 T1 → 事务 1 落 2/4/2 → ⑦ 2 张卡 → 事务 2 落 2,重跑 `cards_existing=2` 且 0 冲突。
  人工核对 601728.SH:close 6.16 / MA20 5.7135 / 涨停 6.78 / 跌停 5.54 / 止损线 5.85
  (= 6.16×0.95,读真实现役章程 K1 的 `stop_pct=0.05`),建仓区间 [6.10, 6.22] 与追价 6.34
  在闭区间内 → `ok`,离场参考 [7.70, 8.62] **远高于涨停仍放行**(证明不夹);同篮 600941.SH
  的越界区间被 `rejected_out_of_limit` 拦下;篮外 999999.SZ 被白名单闸丢弃。
- **测试**:新增 `tests/test_selection_member_tags.py`(46)+ `tests/test_selection_basket_card.py`
  (54)= **100 例**;全量 `pytest tests/ -q` = **2652 passed + 2 skip + 2 fail**(2 fail 仍是
  既有周日时间炸弹,未修未新增;本次数字含并行施工的 ⑥-b 用例)。

**如实登记的设计判断(六处,均未改 Plan;如与规划意图不符请澄清)**

1. **`min_members_hit = ceil(n/2)`(篮子级聚合门槛)是 builder 的临时工程默认** —— plan 只说
   「结构化 + 人话双份、是 ⑧ 的唯一判据源」,**没规定几只成员命中才算整篮成立**。取 `ceil` 而非
   「严格过半」是因为 2 只篮要求两只同时命中会让「被验证」几乎不可能发生,而四态里本就有
   `partial` 承接「只对了一半」。已做成 `build_verification_spec(min_members_hit=…)` 可覆盖。
2. **验证 / 失效的具体条件集是 builder 拟的**(验证 = 收盘 ≥ D0 收盘 **且** ≥ D0 MA20;失效 =
   收盘 ≤ 章程止损线 **或** < MA20 **或** 触及跌停)。plan 未给条件清单,只给了「只用价量结构」
   与「结构化是 ⑧ 唯一判据源」两条约束。⚠ 失效条件里的止损线**用途是判「驱动是否被证伪」,
   不触发任何交易动作**,与哨兵对真实持仓执行的止损纪律是两回事(同一个数字、同一个单一源)。
3. **`warn_streak_top` 的「簇内」读作「共振邻域」而不是「逐簇各判一次」** —— 审计用的粗簇是
   「(日, 行业) 涨停家数 ≥3」、**一票一簇**;而 ④ 的 `limit_cluster_daily` 是多路聚类,
   2026-07-24 真实数据 42 只涨停票摊在 144 个簇 490 行,照字面「任一簇头名即头名」当日
   **36/42(86%)**都会被打警示 = 等于没有警示。改判为「在它所在全部簇的成员并集里连板高度
   并列或独占最高,**且邻域里至少有一只更低**(文案说的『3× 于后排』得先有后排)」,三天真实
   数据命中 6/6/5(分母 48/129/42)。**并列最高都打标**(警示从严,标注件不进排序、没有
   「名次必须唯一」的约束)。
4. **卡上多了一项「离场参考」(蓝图 4.6 十一项之外)**:plan 的「工程体例继承」节明文写了
   「离场参考只做格式合法性校验、不夹涨跌停」,且 ⑩ 的 `position_plans.plan_json` 注释列的
   继承项里就有它 —— 判定为在范围内,故落在 `members[].exit_reference`。
5. **`neckline/selection/__init__.py` 的「本包全程不 import `strategy.brain`」一句已如实修正**:
   ⑤ 起就不成立(它读 `charter_version` 落指纹),⑦ 又要读 `stop_pct`(plan 明文要求)。改成
   「**包机制本身**(`pack.py`/`primitives.py`/`engine_api.py`)不 import」,并写明两处只读用途。
   真正要防的「纪律参数写进策略包」一步没退。
6. **`tests/test_selection_tier.py::test_reads_no_discipline_thresholds` 拆成两条**:原测试把
   `tier.py` 与 `basket_store.py` 一起扫「`stop_pct` 不许出现」,而 `basket_cards` 表**本来就有**
   `stop_pct`/`take_profit_retrace` 两列(① 建表时定的口径指纹列),列名必然出现在 INSERT 里。
   store 那一条改为 AST 断言「**不去读**」(不 import brain、不调 `active_config`/`get_active`、
   无 0.05/0.08 字面量、两个指纹只作为 `save_basket_card` 的参数进来),`tier.py` 那一条原样不动。

**提请 planner 裁定 / 留给下游的两点**

- **上面第 1、2 条(聚合门槛 + 条件集)带策略语义**,按 ⑤-c / ⑥ 先例本该有个归属(引擎常量 or
  进包 or plan 明写)。当前是引擎内的函数默认值,**⑧ 一旦开始消费就等于既成事实**,越早裁定
  越省返工。 → **✅ 2026-08-02 planner 已裁定,见下方 ⑦-b。**
- **⑭-B 待补**:`GET /baskets/{id}/card` 无卡返 404 + reason **`card_not_ready`**,客户端
  `mapReason` 需加新 case(这是新字符串,不能指望 fallback);读侧可直接用
  `basket_store.load_basket_card()`(`version=None` 取最新版本)。**⑮ 客户端解 `card_json`
  必须手写 `init(from:)` + `decodeIfPresent`** —— 它是「写入当时冻住」的那一类快照,不会因
  服务端升级补全新键(CLAUDE.md「落库快照两类论」)。 → **✅ 2026-08-02 已分别补进 ⑭-B 与 ⑮。**

#### ⑦-b · 验证 / 失效条件集与聚合规则(2026-08-02 planner 裁定;**⑦ 主体已完工,本子项是追加施工项;⑧ 的判据源以本节为准**)✅ 完工(2026-08-02)

> **来源**:⑦ 完工记录登记 ①②。Plan 原文只约束了「结构化 + 人话双份、是 ⑧ 唯一判据源、只用价量结构」,**没给条件清单** → builder 拟了临时默认。**本节把它定死,消除「⑧ 一消费就成既成事实」。**

**⑦-b-A 归属裁定:引擎默认 + P3 登记,⛔ 本版不进包**

- **判据照 ⑤-c / ⑥-b 立的框架**,但**这次结论与 ⑥-b 相反**,三条理由(逐条写死,防以后凭「⑥-b 都进包了」类推):
  1. **§12.2 插槽边界是用户拍板的,planner 无权单方扩**。原文:包只管「①扫描层种子规则」与「③Tier 机械分维度与权重」;**「④篮子卡冻结体例」明文列在「引擎本体,不进包」那一侧** —— 验证 / 失效 spec 正是 ④ 的产物。要包化,**必须先走「扩 §12.2 插槽边界(用户拍板)→ 再扩 schema → 再发包」三步**,不是 builder 或 planner 能顺手做的。
  2. **零审计背书,不该被包"体面化"**。⑥-b 的质量线虽也无证据,但它作用的**权重**是 K7 带 H10/H11 审计过来的;本节这套条件集是**全新发明的**,连方向性证据都没有。塞进包会让它看起来像「策略线校准过的参数」,实际是占位值 —— 这比留在引擎里更危险。
  3. **它有下游机械消费方(哨兵)**,与 Tier 权重那种「纯排序、无机械后果」的量不同。⚠ **但要说清:包化的风险并不来自「换包会改盘中判定」** —— spec 在 D0 就被**冻进 `card_json`**,⑧ 读的是冻结件,换包只影响此后新生成的卡,老卡逐字节不变(与 `stop_pct` 指纹同一机制)。真正的理由是上面 1、2 两条。
- **落地要求(⛔ 不许继续躺在函数关键字默认值里)**:
  - 全部阈值与条件集**集中到一处命名常量**(`neckline/selection/basket_card.py` 顶部的 `_VERIFICATION_DEFAULTS` 或独立小模块),登记进 `_ENGINE_CONSTANT_WHITELIST`,注释写明「**临时默认、零审计背书;要可配须先扩 §12.2 插槽边界**」。
  - **新增 `verification_ruleset_version`** 落进 `card_json.fingerprint`(与既有 `spec_version` **分开**:那个跟的是**形状**,这个跟的是**条件集与阈值**)。**条件或阈值一改就 bump**。**为什么要它**:⑨ 评价引擎按它分层,才谈得上「这套条件集的验证率是多少」;没有它,日后回看会把两套条件集的成绩混成一锅。
- **升级路径写死**:⑨ 攒够样本、若证明条件集需要按 regime 校准 → 才谈包化,且必须走上面那三步。**在那之前 builder 不许自行改数、也不许自行加包键。**

**⑦-b-B 条件集定案(V2 初版;采纳 builder 结构,**修订失效侧一条**)**

- **成员级 · 验证条件(两条 AND)**:`收盘 ≥ D0 收盘` **且** `收盘 ≥ D0 MA20`。
  - **采纳原样。** 单看很弱(「没跌」而已),但**篮子级聚合才是它的意义所在** —— 「过半成员都没跌且都站上 MA20」是**共振**的直接证据,远强于任何单只票的表现。不要为了让单条看起来"够格"而加阈值。
  - ⚠ **语义红线(文案必须守)**:`verified` 证明的是「**没走坏 + 共振存在**」,**不是「驱动兑现」、更不是「可以追」**。卡面 / 报告 / 客户端一律不得把 `verified` 写成买入信号或收益暗示,**每处带「参考、非指令」**。
- **成员级 · 失效条件(三条 OR,**第 ③ 条已修订**)**:
  1. `收盘 ≤ 章程止损线`(= `D0 收盘 × (1 − stop_pct)`,**读现役 config 的同一个单一源**);
  2. `触及跌停`(盘中 `low ≤ 跌停价` 即算,**不要求收在跌停**);
  3. ~~`收盘 < MA20`~~ → **改为 `收盘 < D0 收盘` 且 `收盘 < D0 MA20`(两条同时成立)**。
  - **为什么必须改这一条(builder 原版有个真洞)**:验证要求 `≥MA20`、失效原版只要 `<MA20` —— 两者**互为反面**,于是「擦边跌破 MA20」立刻等于「驱动已证伪」,四态里 `unclear` 几乎没有生存空间,`falsified` 会被打得又早又滥。改成 AND 之后,**中间地带**(高于 D0 起点但丢了均线 / 低于起点但守住均线)落进 `partial`/`unclear`,**四个格子才真的各有各的地盘**。
  - **本修订零新常量** —— 只是把两个已有条件用 AND 连起来,不发明任何数。
  - ⚠ **止损线在这里的用途(承 ⑦ 登记 ②,钉死)**:判「**驱动是否被证伪**」,**不触发任何交易动作、不进推送、不改任何持仓判定**。它与哨兵对真实持仓执行的 −5% 止损纪律**是两回事**(同一个数字、同一个单一源,但一个是"这个假设还成不成立"、一个是"这笔仓该不该走")。**⛔ 不许把篮子 `falsified` 接成任何持仓动作。**
- **篮子级 · 聚合门槛**:`min_members_hit = ceil(n / 2)`(n = 篮子成员数;n=1→1,n=2→1,n=3→2)。
  - **采纳 builder 的 `ceil` 而非「严格过半」**,理由照他写的:2 只篮要求两只同时命中会让「已验证」几乎不可能发生,而四态里本就有 `partial` 承接「只对了一半」。
  - **失效侧用同一个门槛**(builder 只定了验证侧,这是 Plan 缺口,在此补齐)。理由:对称、可解释、**不发明第二个数**;「按角色加权(龙头破位比弹性破位更重)」是合理的日后改良方向,**但没有证据,本版不做**,登记 P3-34。
- **四态映射(定死,⑧ 照此实现)**:
  | 态 | 判据 |
  |---|---|
  | `falsified` | 失效命中数 **≥ `min_members_hit`** |
  | `verified` | 验证命中数 ≥ `min_members_hit` **且** 未达失效门槛 |
  | `partial` | 有验证命中但 **<** `min_members_hit`,且未达失效门槛 |
  | `unclear` | 零验证命中且未达失效门槛(含数据缺失) |
  - **优先级:`falsified` 压过一切**(同一拍两侧都达门槛时判 `falsified`)。理由:证伪是风险信息,**宁可先说坏消息**;与「参考在上、纪律兜底」的保守方向一致。
  - **数据缺失 ≠ 证伪**:某成员当日无行情(停牌 / 数据缺口)→ 该成员**两侧都不计命中**,并在 `evidence_json` 如实标 `member_data_missing`;**⛔ 不许把「查不到」算成失效**(「没有」与「没看」必须分得开)。若缺数据成员多到使两侧都够不着门槛 → 落 `unclear` 并标明原因。

**⑦-b 验收**:四态各一单测(含 n=1/2/3 三种篮子规模下的 `min_members_hit` 边界);**修订后的失效第 ③ 条**专项单测 —— 「高于 D0 收盘但跌破 MA20」与「低于 D0 收盘但站上 MA20」**都不判 `falsified`**(这正是修订要治的),而两者同时成立才判;`falsified` 优先级单测(两侧同时达门槛 → `falsified`);缺数据成员两侧都不计 + `member_data_missing` 如实标(单测);止损线随现役 `stop_pct` 变而变(改测试库 config → spec 里的数跟着变,禁硬编);`verification_ruleset_version` 落进 fingerprint 且改条件即 bump(单测);`_ENGINE_CONSTANT_WHITELIST` 已登记且注释含「临时默认、零审计背书」;⑦ 既有 100 例**一字不改逐条仍绿**;pytest 零回归。

**⑦-b 依赖**:⑦。**是 ⑧ 的硬前置**(⑧ 消费它)。

**✅ ⑦-b 完工记录(2026-08-02,@builder-pro)**

- **新建 `neckline/selection/verification_rules.py`** = 条件集 / 聚合门槛 / 四态映射 / 比较语义的**唯一定义处**(选了 plan 给的「独立小模块」那个选项而不是塞进 `basket_card.py` 顶部,**理由是防模块环**:⑧ 住 `sentinel/`,而 `basket_card.py` 反向 import `sentinel.universe` 查 board/is_st —— 生产者与消费者要吃同一份定义,就必须放在一个**零项目内 import** 的小模块里)。内含 `VERIFICATION_RULESET_VERSION` / 五个条件码 / `VERIFY_REQUIRE_ALL` / `INVALIDATE_ANY_OF` / `MIN_MEMBERS_HIT_DIVISOR` / `EPS` / 四态常量 + `min_members_hit()` / `decide_state()` / `evaluate_condition()`。`basket_card.py` **再导出**同名 `COND_*`(同 `aggregate.py` 再导出 `save_baskets` 的搬迁体例),⑦ 既有调用点与单测按符号名引用、不必改字。
- **失效第 ③ 条按裁定改判**:`COND_CLOSE_BELOW_MA20`(单条)**退役** → `COND_BELOW_REF_AND_MA20`(复合,`{ref_close, ma20}` 两个子阈值,比较语义 `close<all_levels`,**任一子阈值 null 则整条不判**)。**形状变了,三个 `spec_version` 全部 bump**:`basket_card_v2` / `basket_verify_v2` / `basket_invalidate_v2`。
- **`verification_ruleset_version` 落两处**:`card_json.fingerprint`(plan 明文要求)**+ 两份 spec 自身的 `ruleset_version` 键** —— 后者是**追加的**,理由:⑧ 手里常常只有 spec(不是整张卡),没有它就没法在 `evidence_json` 里如实标「这一判是哪套条件集出的」,⑨ 归因会缺一环。
- **白名单登记**:`("verification_rules.py", "MIN_MEMBERS_HIT_DIVISOR")` + `("verification_rules.py", "EPS")` 进 `_ENGINE_CONSTANT_WHITELIST`,前者理由写明「临时默认、零审计背书 / 要可配须先扩 §12.2 插槽边界 / ⛔ 不许自行改数」(单测断言这三个词都在)。条件码与 `require`/`any_of` 是**字符串元组**,`ast.literal_eval` 的数值扫描命中不了它们,**故刻意不登记**(登记扫不到的键会让反向存在性校验挂 —— 同 `aggregate.py::MIN_LIFT_SAMPLE_SIZE` 那处已登记过的先例)。
- **比较语义存进卡、由卡说了算**:`conditions[].compare` 是机器可用的串(`close>=level` / `close<=level` / `low<=level` / `close<all_levels`),⑧ **按卡里写的串**选比较器;卡里没写才退回引擎当前声明;两者都没有 → 该条如实「判不了」(`None`),**⛔ 不猜**。这样老卡永远按它自己那版语义被判,换引擎不改口。
- **新增 `tests/test_selection_verification_rules.py` 31 例**,逐条对齐 ⑦-b 验收:四态 + n=0..5 门槛边界;修订第 ③ 条两个阴性方向(高于收盘但破 MA20 / 低于收盘但守住 MA20 都**不判**证伪)+ 一个阳性;`falsified` 优先级;null 阈值与缺观测一律 `None` 不是 `False`;止损线随现役 `stop_pct` 变;ruleset 版本落指纹;**「改条件即 bump」快照守门**(锁住条件集全貌,改了不 bump 就挂);白名单登记与措辞;`pack.py` / 两个 pack.json 里不许出现条件集键(⛔ 本版不进包的机器判据)。
- ⚠ **验收原文「⑦ 既有 100 例一字不改逐条仍绿」与 ⑦-b 自身的两条要求直接冲突**(加 fingerprint 键会撞 `assert j["fingerprint"] == {…}` 的全等断言;改第 ③ 条会撞 `assert iv["any_of"] == […]`)。按施工交办口径处理:**⑦ 既有测试按新形状最小修正、语义断言一字不动**(实际改动 = 2 个断言各加一行/换一个常量名),`spec_version` 的存在就是为这种形状变更兜底。**如与规划意图不符请澄清。**
- 全量 `pytest tests/ -q`:2652 passed → 2683(+31),2 skip / 2 fail(既有周日时间炸弹)不变。

#### ⑦-K7-b · `warn_streak_top`「簇内」= 共振邻域(2026-08-02 planner 裁定:**采纳 builder 重解读**)

> **来源**:⑦ 完工记录登记 ③。

- **裁定:采纳**「在**所在全部簇的成员并集(共振邻域)**内连板高度**并列或独占最高**,**且邻域里至少有一只更低」**,取代字面的「任一簇内头名」。
- **三条理由(写死,防以后按字面回退)**:
  1. **审计粗簇与生产簇本就不同构,Plan 早有兼容注**。K7 审计用的是「(日, 行业) 涨停家数 ≥3」的**一票一簇**粗簇;④ 的 `limit_cluster_daily` 是**多路聚类**(同日簇 + 连板簇,一票同日可属十几个簇)。⑥ 的「簇定义兼容注」已经立过同一条规矩:**簇不必同构,但语义必须保住**。本条是那条规矩在 ⑦ 的同一次适用。
  2. **字面读法在多路聚类下是逻辑上的必然膨胀** —— 簇越多,一票越容易在**某个**簇里当头名。实证:2026-07-24 真实数据 42 只涨停票摊在 144 个簇 490 行,照字面 **36/42(86%)** 都要打警示。**一个 86% 命中的「警示」不是警示,是背景噪声。**
  3. **「至少有一只更低」这条附加要求是文案自洽的必然** —— 警示文案说的是「次日跌停约 **3× 于后排**」,**得先有后排**,才谈得上「你在后排前面」。邻域里全员同高度时打标是无意义的。
- **并列最高都打标**(不做唯一化):警示**从严**;标注件不进排序,没有「名次必须唯一」的约束。
- **实证区分度**(完工记录):三天命中 **6 / 6 / 5**(分母 48 / 129 / 42),量级合理。
- ⚠ **登记为待策略线过目**:这是 **K7 交接稿的判据**,生产侧做了工程重解读,**策略线有权知道生产上到底怎么落的** → **§七 [P3-34]**。本裁定在策略线未提异议前有效,builder 照此执行。

---

### V2-⑧ · ⑤-a D+1 验证:关注池改造 + 盘中存拍 + 验证状态机(🔴 @builder-pro,碰哨兵)✅ 完工(2026-08-02)

**目标**:哨兵骨架保留、纪律分支一行不动;换的是**关注谁**与**记录什么**。

**⑧-A 关注池改造**(`sentinel/universe.py`)
- 新组成 = **持仓 + T1/T2 篮子成员 + 相关板块 ETF/指数 + 昨日涨停**,容量与现状同量级(≤ 200)。**自选池退役后不再是来源**(⑬)。
  - ⚠ **「ETF/指数」本版实为「板块基准指数」,ETF 部分未做且不是遗漏**(2026-08-02 对齐完工记录登记 ②):项目**没有 ETF 成分 / 映射数据源**(TuShare 600 元档没有;`ths_index` 是同花顺板块指数,免费实时源不认它的代码),硬编一份 ETF 清单等于凭空发明。落地 = 按成员板块取 `000001.SH`/`399001.SZ`/`399006.SZ`/`000688.SH`/`899050.BJ`(与 `scripts/backfill.py::INDEX_CODES` 同一批),板块判定唯一源 `load_stock_meta`。**要真 ETF 得先有数据源** → §七 [P4-35]。
- ⚠ 保留既有认知:**关注池是代理样本,不是全市场**(免费源限流取舍);不够灵敏的备选是低频全市场轮询叠加,**不是提主循环频率**(改前先读 `sentinel/retreat.py` docstring)。
- 「上市第几天」一律 `is_new_stock_exempt`;polars 链式赋值右侧一律 `pl.col(...)`。

**⑧-B 盘中存拍(新)**
- 关注池分钟报价 → 内存累计;**15:05 收盘后一次性落 `intraday_ticks` 当日分区**(**D4 已拍板**);09:25 竞价快照 → 同样收盘后落 `auction_snapshots`。
- 落盘走 `write_table_day`;失败只 WARNING、**绝不影响哨兵主循环**;当日存拍不完整 → 落一个 `capture_status`(`full` / `partial` / `missing`)供 ⑨ 如实标注。
  - **`capture_status` 的落点 = `sentinel_events`**(2026-08-02 对齐完工记录登记 ①;`sentinel='capture'`,`event_key='intraday'|'auction'`,payload 带 `covered_minutes`/`expected_minutes`/`empty_ticks`/`rows`/`codes` 全部原始数字)。**为什么不落 parquet / 不开第 22 张表**:① `missing` 恰恰是「一行都没有」的那天,状态**写不进 parquet 行里**;② ① 已把表建齐并写明「此后每块只写读写逻辑、不再动 DDL」,为一个日级标记开新表不划算;③ `sentinel_events` 本就是哨兵当日事件台账、已有非推送用法。
  - **⑨ 的读侧入口 = `capture.load_capture_status()`**(唯一实现,**⛔ ⑨ 不要自己翻 parquet 数行数**);它明确区分「**没记过**」(`recorded=False`)与「**记了是 missing**」——「没有」与「没看」照旧必须分得开。**⑧ 只落原始数字、不替 ⑨ 发明「多少算够」的阈值**,⑨ 要用哪条线自己定并在 ⑨ 里写明。
- **外源可插拔**(裁定 #1):用户外源方案落定后**走同一张表回补**,`source` 列区分;⑨ 复盘引擎无感切换。

**⑧-C 篮子验证状态机**
- 判据 = ⑦ 的 `verification_spec` / `invalidation_spec`(**唯一判据源**,不另写一份阈值);盘中每拍判 + EOD 收盘再判一次。
- 四态:`verified` / `partial` / `unclear` / `falsified`;**状态追加不回写**(`basket_verification` 只 INSERT)。
- 证伪只用价量结构(承 §2.4 铁律:盘中主力资金流免费源不可靠)。
- **⚠ 条件集与四态映射的权威 = 【⑦-b】**(2026-08-02 planner 裁定),本块**只消费、不另立**:成员级验证两条 AND / 失效三条 OR、篮子级 `min_members_hit = ceil(n/2)`(两侧同一个数)、`falsified` 优先级、缺数据两侧都不计。⛔ **本块不许再写一份阈值或另定门槛。**

**⑧-C2 盘中判定 vs EOD 判定:同一份 spec 的哪个子集(2026-08-02 planner 裁定,⑦ 问的第 ③ 点)**

- **同一份 spec,不拆子集 —— 拆的是「代入什么价」**:盘中把**现价**代入 spec 里「收盘」那个位置,EOD 代入**真实收盘价**。**⛔ 不许为盘中另写一套更松 / 更紧的条件。**
- **锚一律取 D0 冻结值,盘中不重算**(这条最容易踩错):
  - **`MA20` 用卡里冻结的 D0 MA20** —— 盘中根本算不出「今日 MA20」(它要今日收盘),且卡是冻结件、锚必须与 D0 判断同源;
  - **跌停价用卡里冻结的那个**(⑦ 用 `compute_intraday_limit_prices` 按次日算好的),**盘中不重算**;
  - **章程止损线用卡里冻结的 `stop_pct` 指纹算出的那个数**,不读"当前现役 config"(否则同一张卡在章程切换前后会讲两套话 —— 卡是冻结件,`stop_pct` 已在指纹里)。
- **`触及跌停` 是盘中语义**:用当日 `low ≤ 跌停价` 判(**触及即算,不要求收在跌停**);EOD 用当日最低价同判,两者一致。
- **落行规则(防一天几百行流水)**:**状态未变不落行**,**只在状态发生变化时追加一行**;每行带 `source ∈ {intraday, eod}` 与 `observed_at`。**EOD 那一拍无论状态是否变化都必须落一行**(`source='eod'`)—— 它是当日的定论记录,不能因为"和盘中最后一拍一样"就省掉。
- **「当前状态」怎么读(⑧/⑭/⑮ 都要用,在此定死)**:取该篮该日 `source='eod'` 的行;**没有 EOD 行则取最近一条 `intraday` 行并标注「盘中暂态、未收盘定论」**;两条都没有 → `unclear` 且标 `not_evaluated`(**「还没判」与「判了是 unclear」必须分得开**)。
- **`falsified` 是当日终态,不撤回**:一旦判定 `falsified`,当日后续拍**不再改写、也不再落新行**(EOD 那一行照落,内容仍是 `falsified`)。承 v1.3「D5 判向定格一次」的同一条纪律 —— **堵死「跌破了 → 又回来了 → 系统改口说没证伪」这条路**,否则证伪形同虚设。⚠ 反向不成立:`verified` **不是**终态(盘中验证了、尾盘走坏,照样能翻 `partial`/`falsified`)。
- **卡缺席时的行为**:某篮**有篮子无卡**(⑥ 落库成功、⑦ 未生成,见【跨块】节 C)→ **没有 spec 就不判**,如实落 `unclear` + `no_card`,**⛔ 不许拿默认条件顶上**(那等于凭空给它编一份判据)。

**⑧-D 纪律外壳一行不动**:`sentinel/holding.py` / `retreat.py` / `entry.py` / `invalidation.py` / `precall.py` / `circuit.py` 的**判定逻辑不改**;本块只改「关注谁」与「新增记录什么」。⚠ 纪律阈值比较一律带 `_EPS`。

#### ⑧-E · 除权除息锚失效检测(2026-08-02 planner 裁定;**追加施工项,排在 ⑬/⑭ 之前做掉,不进 Backlog**)

> **来源**:⑧ 完工记录登记 ⑤。锚是 D0 价格标度(`qfq(D0) == close(D0)`,v1.5-①-C 已确认)、观测是 D+1 原始价,两侧本来一致 —— **但成员恰在 D+1 除权除息时,D+1 原始价按除权比例跳水,而锚还停在 D0 标度 → 静默误判破位。**

**⑧-E-A 裁定:现在做,不登记 Backlog 拖到上线后。四条理由(量化过,不是感觉)**

1. **频率不低到可忽略**:每日被验证的篮子成员约 **40 只**(T1≤2 + T2≤5 + T3≤10,每篮 1–3 只);A 股除权除息集中在 **5–7 月(年报分红)** 与 **9–11 月(中期分红)**,峰值日全市场 1–2%、均值约 0.5% → **每 1–3 天就有一个成员中招**。
2. **一个成员就能翻整篮**:`min_members_hit = ceil(n/2)`,**2 只成员的篮子门槛是 1** —— 单只误判破位直接把整篮打成 `falsified`。
3. **错误是「静默 + 粘住」**:`falsified` 是**当日终态、不撤回**(⑧-C2),误判会挂一整天,而且没有任何地方会报错。
4. **它污染的正是我们要用来校准判据的那份证据**:⑨ 的验证率 / 证伪率是 **P3-34** 回看条件集的唯一依据 —— **拿被污染的数据去校准判据 = 错上加错**。⚠ 且 **V2 上线时点(⑯ 之后)大概率落在中期分红季**,「等上线后处理」正好撞上。

**⑧-E-B 修法:`pre_close` 检测器 + 复用已有的「不计」机制(⛔ 不改 ⑦ 的卡形状,不 bump `spec_version`)**

- **⚠ 否决 ⑧ builder 提的「⑦ 在卡里多冻一份原始价锚」**:① D0 那天 `qfq == raw`,多冻一份 close 是同一个数;② **「原始价 MA20」是个更糟的数**(它把除权前后的价格混在一根均线里,本身就没有可比性);③ 它要改冻结件形状 + bump `spec_version`,代价大而并不解决问题。
- **检测器(零新数据源,盘中 / EOD 同一套)**:`pre_close ≠ 卡里的 D0 收盘`(带 `_EPS` 容差)即判**锚已失效**。
  - **依据**:A 股除权除息日,交易所公布的**前收盘价 = 除权除息参考价**(已按 `(前收盘 − 现金红利) / (1 + 送转比例)` 调整),涨跌停也按它算。所以 `pre_close != D0 close` 是除权除息的**直接、精确**信号。
  - **数据现成**:盘中 `sentinel/quotes.py::Quote.pre_close`(新浪 / 腾讯**都已解析**);EOD `daily.pre_close`(已在 `TABLE_FLOAT_COLS` 声明)。**两条路径同一个判据,不破 ⑧-C2 第 1 条的对称性。**
- **命中后的行为 = 复用 ⑦-b 已有的「缺数据两侧都不计」机制,不发明新机制**:该成员**验证侧与失效侧都不计命中**,`evidence_json` 标原因码;若因此两侧都够不着门槛 → 整篮落 `unclear` 并写明原因。**⛔ 绝不把「锚失效」算成失效命中**(承「数据缺失 ≠ 证伪」)。
- **⛔ 不做自动 rescale**(虽然 `pre_close / D0_close` 数学上就是精确的调整比例):`pre_close ≠ D0 close` 有**两种成因** —— 真除权 / 数据错 —— 盘中无法区分;自动 rescale 会把**数据故障也悄悄修正掉**,**把一个该报警的故障变成一个静默的错误判定**。宁可当天不判(信息少一点),不要判错(信息是错的)。
- **EOD 多一份交叉确认,把两种成因分开(这正是「没有」与「没看」纪律的延伸)**:EOD 时查 `adj_factor(D+1) vs adj_factor(D0)` ——
  - 变了 → 确认真除权,原因码 `member_ex_rights`,**正常降级、不报警**;
  - 没变 → 这不是除权,是**真故障**,原因码 `anchor_mismatch` + **打 WARNING**(该被看见,别静默吞掉)。

**⑧-E 验收**:造一只 D+1 `pre_close ≠ D0 close` 的成员 → 两侧都不计 + 原因码精确(单测);2 只成员篮里一只锚失效、另一只正常破位 → **整篮不判 `falsified` 而是 `unclear`**(单测,正是本子项要治的回归场景);EOD 交叉确认两路(`adj_factor` 变 → `member_ex_rights` 无 WARNING / 未变 → `anchor_mismatch` 有 WARNING);盘中与 EOD 用同一个检测器(单测:同一份数据两条路径判定逐位相同);`_EPS` 容差生效(浮点毛刺不误触发);**⑦ 的 `card_json` 形状与 `spec_version` 一字未动**(守门断言);⑧ 既有单测一字不改逐条仍绿;pytest 零回归。

**⑧-E 依赖**:⑧。**排期:⑬ / ⑭ 之前**(⑨ 正在并行施工,不阻塞它;但 ⑨ 攒的验证率数据在本子项落地前含已知污染,回看 P3-34 时要把这段窗口标出来)。

#### ⑧-F · 退潮红色刹车「主线板块跳水」样本改机械派生(2026-08-03 planner 裁定,V2 review 判定线 🟡-4 收口;**追加施工项,🔴 碰纪律触发器**)

> **背景**:V1 该样本 =「关注池里命中今日热门板块标签的**候选**」(候选机械生成,LLM 无权改去留);V2 换成 **T1/T2 篮子成员**(⑤ 里 LLM 在白名单内**挑选**出来的)。`hot_sector_avg_chg ≤ sector_dive` 是**红色刹车三路触发之一** —— 红色 = **全天禁开新仓 + 立即级推送 = 纪律层动作**。

**⑧-F-A 裁定:改机械派生样本(reviewer 选项 ①),⛔ 不写豁免**

- **为什么不能靠写豁免解决(这条比 reviewer 说得更重)**:§2.8-C-2(b) 的豁免之所以站得住,靠的是「**每只票的判定阈值仍只来自章程与机械 spec**」;但这里是**聚合量**,**样本组成本身就是判据的输入** —— 换一批票平均,结论直接变。「阈值一字未动」是真的,但**不相干**。已把这条升级成 §2.8-C-2(b) 的**通用适用边界**(附带一句自查判据:「**换一批成员,这个数会不会变?**」)。
- **⚠ 我不同意 reviewer 的「无单向套利」缓解判断 —— 这里的偏差是单向的**:LLM 挑成员时是奔着**龙头 / 中军**去的,而**领涨股在跳水日恰恰是最抗跌的那一批**。样本系统性偏向强者 → 均值跌幅被**低估** → **红色刹车更不容易响**。也就是说这个偏差**恰好朝着"保护失效"的方向**,不是中性噪声。碰纪律触发器,按「从严」办。
- **样本定义(定死)= 当日 ④ 扫描层机械产出的「热点行业 / 暴起概念」种子的原始成分 ∩ 关注池。**
  - ⚠ **⛔ 不要照 reviewer 措辞取「T1/T2 篮子**所声明种子**的原始成分」** —— 那仍然过了一道 LLM 筛(哪些种子被 LLM 拿去建篮由它决定),只是把塑形上移了一层。**必须直接取 ④ 的种子集,不经篮子。**
  - 这也是与 V1 语义**最接近**的机械等价物(V1 = 热门板块 ∩ 关注池;④ 的种子生成规则读**现役包**,阈值是包参数,成分来自 `stock_basic.industry` / `ths_member`,**全链路机械**)。
- **样本不足 → 不触发该路 + 如实披露**(不拿 LLM 成员补足、不用小样本硬判)。
  - ⚠ **这里的「保守方向 = 不触发」与项目别处「宁可多提醒」的取向刻意相反,理由三条**:① 退潮刹车**另有两路触发**兜底,不是唯一防线;② 误触发的代价是「**整天不能开新仓**」,对只做短线的账户是实打实的机会成本,且会训练用户**忽略刹车**(狼来了比不响更危险);③ 与项目一贯的「缺数据不判、不猜」一致。
- **留痕**:每次评估(触发与否)都落**样本构成**(codes + 来源标签 + 样本量),让「样本没被 LLM 塑形」这件事**事后可审计**,而不是靠代码读一遍来相信。

**⑧-F-B 施工纪律(🔴 碰纪律触发器)**
- **`sector_dive` 阈值一字不动** —— 本子项只换**样本来源**,不碰任何阈值。
- **改前改后对拍**:同一批历史日跑两版,**给出触发次数与触发日差异的具体数字**(⛔ 不接受「应该差不多」这种说法);差异大到意外时**停手回 planner**,别自行调阈值找齐。
- 哨兵既有纪律单测**全绿且不许改断言**。

**⑧-F 验收**:样本来自 ④ 机械种子成分(单测:换掉 LLM 的成员选择 → 样本**逐位不变**,这是本子项的核心判据);样本不足 → 不触发 + 如实披露(单测);`sector_dive` 阈值未变(守门断言);改前改后历史对拍数字已写进完工记录;留痕行含样本构成;哨兵既有纪律单测一条断言未改;pytest 零回归。

**⑧-F 依赖**:④、⑧。**排期:与 ⑧-E 同批**(都碰 ⑧ 且都在 ⑬/⑭ 之前)。


#### ⑧-G · 主线跳水样本的关注池配额切片 —— 让这条红色刹车真的能响(2026-08-03 planner 裁定,⑧-F 对拍打出来的;**追加施工项,🔴 碰纪律触发器**;**✅ 2026-08-03 完工,记录见下方「V2-⑧-G 完工记录」**)

> **背景**:⑧-F 修好了「样本可被 LLM 塑形」这条红线(已闭、可审计),但对拍**顺带量化出一个更深的问题** —— 这条路**近乎失效**。真跳水日 07-24(全市场中位 **−2.86%**、89% 个股下跌),样本读数 **−0.18%**,而「主线板块本身」(④ 种子全成分)是 **−2.98%**、已贴着 −3.0% 阈值。**差 2.8 个百分点。** 四天改前改后都是 **0/4 触发**。⚠ **这不是 ⑧-F 引入的** —— V1 时代样本是候选池,同样是动量筛出来的一群,**大概率一直就是聋的**,只是从来没人量过。⑧-F 的对拍第一次把它照出来。**builder 未擅自改阈值 / 扩池,把裁定权交上来 —— 做得对。**

**⑧-G-A 定性:`∩ 关注池` 这一步在语义上就是错的(先想清楚再动手)**

- §2.4 里这条叫「**主线板块跳水**」,它的**语义主语是板块 / 主线本身**,不是「我恰好盯着的那几只票」。
- 而关注池的存在理由完全是另一回事:**免费实时源限流下我们只poll得起 ~200 只**。拿一个**测量市场状态的量**去 ∩ 一个**受配额约束的轮询预算**,是实现产生的副产品,不是设计。
- 更糟的是这个副产品**有方向**:池的机械成分几乎全是**昨日涨停股** = 全市场动量筛得最狠的一群 = **最抗跌的一群**。于是这个测量在**最该响的日子最不容易响**。
- **但 `∩ 池` 又不能简单去掉** —— 没有报价就算不出收益率。**所以正解不是"不 ∩ 池",而是"让池里预留出一块专门用来测主线的、机械且有代表性的样本"。** ⑧-F 完工记录里量化的那个候选方向(配额切片)方向是对的,本节把它定死并修两处。

**⑧-G-B 采纳配额切片,但采样键改 `crc32`(⛔ 不用 `ts_code` 升序)**

- **做法**:④ 的每颗机械种子(热点行业 / 暴起概念)按**确定性序取前 K 只**,并入关注池,作为主线跳水样本的**专属来源**。
- ⚠ **采样键定为 `zlib.crc32(ts_code)` 升序,⛔ 不用 `ts_code` 本身升序**(这是对 builder 候选方案的一处**修正**,理由要紧):
  - `ts_code` 升序在一颗**行业**种子内部会系统性偏向 `000xxx`(深主板)/ `300xxx`(创业板),把 `600xxx`(沪主板)/ `688xxx`(科创板)排到后面 —— 也就是**按板块给样本排了个序**。而板块与**涨跌停幅度(10% vs 20%)、波动率**直接相关。对一个「主线跌了多少」的测量来说,**样本的波动率画像被系统性扭曲**,等于用一把刻度不匀的尺。这正是我们刚花一整个 ⑧-F 要清除的那类偏差,**别在采样器里把它请回来**。
  - `crc32` 与板块 / 市值 / 上市年份**无关**,是无偏抽样;且**跨进程跨天可复现**(项目 CLAUDE.md 明文:要复现的分组一律 `crc32`,禁内置 `hash()`)。**零额外成本,一个函数调用的事。**
- **K 的定法**:**K 是精度旋钮,不是策略参数**。定 **K = 4**(而非 builder 试的 6),理由:配合下面 ⑧-G-C 的**按种子取均值**估计量,K 只影响每条主线内部的抽样噪声,不影响主线之间的权重;而**给昨日涨停股(另一路触发器的样本)留出更多池位**。⚠ **K 与阈值是两件事** —— ⛔ 不许通过调 K 去凑触发频率(那等于偷偷改阈值),K 只按"样本量够不够稳"来定。
  - **实测量程更正(2026-08-03,`crc32` 口径)**:**K=4 → 40~100 只/日**(我原文引的"K=6 → 58~144"是 ⑧-F 那次 `ts_code` 口径的旧数,已作废)。
  - ⚠ **⛔ 这条 K 纪律经受住了一次真实考验,记下来当先例**:07-24 用 **K=6** 时读数 −3.04% **会触发**,而 K=4 是 −2.80% 不触发;**builder 明知如此仍不采用 K=6** —— 正确。**「换个参数就能让它响」不是采用该参数的理由**,那恰恰是偷偷改阈值。
  - **单主线日(种子数 = 1)该路必然不判**:单颗种子的切片上限 = K = 4 **< `MIN_MAINLINE_SAMPLE` = 5**。**这是裁定后的正确行为,不是 bug** —— 一条主线不足以支撑「主线**板块**跳水」这个判断。**在此写明,防日后被当故障上报。**(生产日常 10~41 颗种子 / 40~100 只样本,离下限很远。)

**⑧-G-C 估计量定为「每条主线一票」(per-seed mean 的均值),⛔ 不是把所有票混在一起平均**

- **理由**:名字就叫「**主线板块**跳水」,主语是**主线**。一颗 200 成员的种子和一颗 8 成员的种子,各代表**一条主线**,不该因为成员多就占更大权重。
- **附带好处**:这样 K 变成**纯精度旋钮** —— 每条主线多抽几只只让它自己的均值更稳,**不改变主线之间的相对权重**。⑧-G-B 里「K 不许用来凑触发频率」这条才真正站得住。
- ⚠ **builder 的四天读数是 pooled 口径算的**,与本条不同 → **必须用 per-seed 口径把同四天重跑一遍,两个口径的数字都写进完工记录**。若两者差异显著,那本身就是要知道的信息;⛔ 不许只报一个。

**⑧-G-D 关注池改成「有界必需项全进 + 两份测量样本各有保底」(顺带根治残留耦合 ②b)**

- **现状问题**:池是一条**先到先得的优先队列**(持仓 > T1/T2 成员 > 指数 > 昨日涨停填剩余到 200)。对「注意力分配」这没问题;但对**纪律测量样本**是错的 —— **一个会被别人挤掉、大小不可预期的样本,产生的是不可靠的测量**。
- **新配额规则(定死)**:
  1. **有界必需项无条件全进**:持仓(三仓制 ≤3)+ **T1/T2 篮子成员(≤7 篮 × ≤3 只 = ≤21)** + 板块基准指数(≤5)。**合计上界 ≈29,本来就有界**,不必让它们参与抢位。
  2. **两份纪律测量样本各设保底名额**:**昨日涨停(宽度 / 炸板率样本)** 与 **主线切片(本节)**。剩余 ~171 个位按保底分配,谁不够用谁的实际量,余量归对方。
  3. ⚠ **这正好根治 builder 登记的残留耦合(⑦「涨停 >180 时 LLM 仍能挪样本尾巴」)** —— 篮子成员既然是**有界且保底**的,就不再与任何测量样本抢位,LLM **在任何极端日都挪不动测量样本**。⛔ 别再单独想办法治那条,**它是这条规则的推论**。
- ~~**判据是「两份样本在四天里都没被截断过」**~~ → **❌ 这条判据我写错了,2026-08-03 改写(builder 实测证伪)**。昨日涨停的需求**上不封顶**(2026-07 全月实测 **32~223 只/日**,两份并集 72~275),**任何有限池都装不下**;而且**它改组前就一直在被截**(旧口径给它的同样是"剩余 ~171"),我要求的是一个**系统从来没有过、也不可能有**的性质。
- **✅ 新判据(三条,取代上面那条)**:
  1. **主线切片不被截断**(它是刚修好的那个估计量,优先保它);
  2. **昨日涨停允许被截,但截断必须无偏** —— ⚠ **这才是真正要守的东西**:两份样本都是**估计量**(切片 = 均值,涨停 = 炸板率这个比率),**估计量对"抽样"是免疫的、对"有偏抽样"不免疫**。故截取顺序**不得与被测量的量相关**;**建议同样用 `crc32(ts_code)` 序**,⛔ 不用 `ts_code` 序、不用池插入序 / 先到先得(理由同 ⑧-G-B:`ts_code` 序 = 按板块排序,而板块与涨跌停幅度、炸板概率直接相关)。**施工时若现实现的截断序与被测量相关就改成 `crc32` 序;若已无偏,写明并留证据。**
     - ⚠ **这一条尤其要紧**:昨日涨停这份样本**同时喂着退潮宽度 `compute_breadth_snapshot`**(CLAUDE.md 明载它是退潮判据输入)—— 有偏截断在这里就是 ⑧-F 那一类 bug 的翻版。
  3. **截断后读数无实质变化**(builder 已验的炸板率对照即此条:07-22 121→71 只 **42.9%→42.9%**、07-24 129→125 只 **37.9%→37.9%**),**保留为验收判据**。
- **✅ 核准 `MAINLINE_SLICE_QUOTA_FLOOR=100` / `PREV_LIMIT_UP_QUOTA_FLOOR=71`(合计 171 = 200 − 29)**。理由(不是"先来后到",是**按截断的代价分配**):**切片被截 = 直接损失一个刚修好的、要与 −3% 阈值比大小的均值估计量的精度**;**涨停被截 = 比率估计量换个样本量,实测读数分毫未变**。**把稀缺预算给"截了真会疼"的那一份**,这是对的。⚠ 但正因为涨停在重日只采到 <1/3,**第 2 条的无偏截断要求就是它的全部保护** —— 别把它当可选项。
- **✅ 核准「测量预算按必需项上界 29 固定算、⛔ 不看这一拍谁已在池」**(builder 登记 2)。**这是个好抓** —— 若按实际占用算,LLM 多挑一个**恰好也在切片里**的成员就能腾出一个位、让样本多一只,**残留耦合 ②b 换个面目就回来了**。代价(必需项不满时池子欠填 ≤29 个位)刻意接受。
- **留痕要求**:两份样本都要落**「需求量 vs 实际量」**,让日后审计炸板率的人**一眼知道它是抽样不是普查**(⛔ 别让人误以为 71 就是当天全部涨停股)。
- 若 200 仍装不下,**回 planner,⛔ 不许自行抬 `breadth_cap`**(那会改盘中轮询量与限流风险面)。

**⑧-G-E 加最小样本量下限(⚠ 这一条改判 builder 的登记,理由见下)**

- **裁定:加 `MIN_MAINLINE_SAMPLE`,值 = 直接 import 引用 `report/industry_strength.py::_MIN_MEMBERS`(5),⛔ 不抄字面量**(照 ⑤-c 的 `MIN_LIFT_SAMPLE_SIZE` 同源引用体例,builder 已实现过一次)。
  - **为什么是同一个 5**:同一类统计量(横截面均值)、同一个理由(样本太小则估计量本身无意义)。**⑤-c 已经立过这个先例,这里照搬,不新发明。**
  - **为什么必须有**:n=3 时横截面收益率的标准误约 2pp,拿它去判 −3% 阈值**接近抛硬币** —— 而误触发的代价是**整天禁开新仓**。配额切片上线后正常日样本 30~140,n<5 意味着种子几乎为空 = 上游已经出事,**这种时候更不该开火**。
- **⚠ 核准 builder 当时不加的克制,但那条约束我写得有歧义,在此澄清**:⑧-F-B 的「哨兵既有纪律单测一条断言未改」防的是「**改断言去迁就新代码**」;而**行为按裁定变了、测试跟着变**是另一回事,**是允许且必须的**。故 `test_sentinel_retreat.py` 里那条 `hot_n=3` 触发断言**现在可以且应当改**,但要求:**显式改、单独说明、在完工记录写清改前改后**,⛔ 不许悄悄改数让它绿。

**⑧-G-F 施工纪律(🔴)**:`sector_dive` 阈值(−3% / 早盘 −4%)**一字不动**;`evaluate_retreat` 判定逻辑不动;本节只改**样本怎么取**。同四天历史对拍**两个口径(pooled / per-seed)都给数**,并**明确报出触发日**;哨兵其余纪律单测断言不动。

**⑧-G 验收**:采样键是 `crc32` 且与板块无关(单测:构造同一行业内跨四个板块的成员,断言抽样不集中在某一板块 + 同输入两跑逐位相同);per-seed 估计量(单测:一颗大种子 + 一颗小种子 → 两条主线等权,pooled 口径会给出不同数值,两者都断言到);池配额(单测:造 200 只涨停的极端日 → **持仓 / 成员 / 指数 / 两份测量样本各自的保底量一个不少**,且篮子成员数量变化**不改变任一测量样本**——这是残留耦合 ②b 的回归判据);`MIN_MAINLINE_SAMPLE` 生效且是同源 import(单测 + AST 守门);`sector_dive` 两个常量守门断言仍绿;四天对拍两口径数字与触发日已写进完工记录;`breadth_cap` 未改(守门);pytest 零回归。

**⑧-G 依赖**:④、⑧、⑧-F。**排期:⑭ 之前**(它改的是纪律触发器的读数,不该带着一个"已知是聋的"的刹车进 ⑭)。

**⑧-G-G 核准 / 归档 builder 的三条登记**

1. **「∩ 关注池」→「∩ 关注池的**机械成分**」的字面偏差:✅ 核准,且是 plan 的错不是实现的错**。我在 ⑧-F 写「种子成分 ∩ 关注池」时没想到池**本身**含 T1/T2 成员(⑧-A),整池取交会让「换掉 LLM 的成员选择 → 样本逐位不变」这条**我自己写的核心验收判据当场不成立** —— plan 字面与自己的验收判据打架,builder 按验收判据实现是对的。**⑧-G 的配额切片口径自此取代 ⑧-F 的「∩ 池」表述**,但**保留其精神**:样本只取机械来源。⚠ 同时保留 builder 那条重要区分 —— **排除的是「LLM 这条路」,不是「LLM 碰过的票」**:一只票若本来就从机械路进池(本身就是昨日涨停 / 是持仓),**照进样本**。
2. **池 200 截断时篮子成员挤占涨停股的残留耦合:✅ 要治,已由 ⑧-G-D 根治**(见上,是配额规则的推论,不单独立项)。
3. **不加最小样本量的理由:部分核准 → 已改判**,见 ⑧-G-E(克制本身对、约束被我写得有歧义、现按裁定加下限并允许显式改那条断言)。
4. **✅ 核准「样本 = 切片本身,⛔ 不再并入种子成分 ∩ 机械池的其余部分」**(⑧-G 完工记录登记 1)。**判得对**:把昨日涨停股并回样本 = 把 ⑧-G-A 定性要清除的**动量偏差原样请回来**,那等于修了一半又自己撤回去。⑧-G-B 原文写的就是切片作样本的「**专属来源**」,故这是**照 plan 执行,不是偏离**。
   - ⚠ **而且它让 ⑧-G-G 第 1 条更彻底、表述该升级**:原话是「排除的是『LLM 这条路』,不是『LLM 碰过的票』」;现在的保证更强 —— **被 `crc32` 选中就进样本,与它是不是篮子成员 / 持仓 / 昨日涨停股完全无关**。样本的构成**只由「④ 的机械种子 + `crc32` 序 + K」三件事决定**,任何人(含 LLM)碰不动。**以此表述为准。**

---

#### ✅ V2-⑧-G 完工记录(2026-08-03,@builder-pro;**这条红色刹车不再是聋的**;⚠ 一条 plan 判据在 200 池位内做不到,已如实登记交回 planner)

**落点**:`sentinel/mainline.py` 重写派生(样本 = ④ 每颗机械种子按 `crc32(ts_code)` 升序取前 `MAINLINE_SAMPLE_PER_SEED`=4 只的**配额切片**,⛔ 不再 ∩ 关注池)+ 新增 per-seed 估计量 `estimate()` + `MIN_MAINLINE_SAMPLE`(**同源 import** `industry_strength._MIN_MEMBERS`)+ `MainlineSample.restrict()`(池配额压缩,**逐颗种子轮转**);`sentinel/universe.py` 关注池按「有界必需项全进 + 两份测量样本各有保底」重排(三个新常量 `MANDATORY_POOL_RESERVE`=29 / `MAINLINE_SLICE_QUOTA_FLOOR`=100 / `PREV_LIMIT_UP_QUOTA_FLOOR`=71,合计恰为 `DEFAULT_BREADTH_CAP`=200,守门单测锁死);`sentinel/engine.py` 改读 `wu.mainline_sample` + 用 per-seed 读数判(`_hot_sector_peer_returns` 改返回 `{code: ret}`,不再自己求均值);`sentinel/retreat.py` **只改一处准入判断**(`hot_sector_sample > 0` → `>= MIN_MAINLINE_SAMPLE`)。**`sector_dive` 两个阈值(−3% / 早盘 −4%)与 `evaluate_retreat` 判定逻辑一字未动**(守门断言仍绿);**`breadth_cap` 一字未动**(新增守门断言)。

**四天对拍(⑧-G-F 要求的两个口径都给数)**。真实 parquet 只读;隔离对拍库 = 参考四表 + K4-pack 现役 + `industry_strength_daily` 2026 全年(14740 行 / 20260105–20260724)+ `scan_layer` 7 月事实表(18 天)。收益率用 D 日 `close/pre_close−1`(EOD 代理一个尾盘拍,本地无分钟线,与 ⑧-F 同口径)。**改后一列是走生产代码路径跑出来的**(`derive_mainline_sample` → `restrict` → `estimate`);**改前一列按 ⑧-F 定义解析计算,pooled 数字与 ⑧-F 完工记录逐位一致**(+0.94/+1.34/+4.98/−0.18,n=27/93/7/45),以此自证对拍库与 ⑧-F 那次等价。

| D(盘中日) | D0 | 全市场中位 | 改前 pooled | 改前 per-seed | 改后 pooled | **改后 per-seed(判定口径)** | 改后 n / 种子 | 触发(改前 / 改后) |
|---|---|---|---|---|---|---|---|---|
| 20260721 | 20260720 | +0.59% | +0.94%(27) | +0.65%(6 颗) | −1.31% | **−1.31%** | 40 / 10 颗 | 否 / 否 |
| 20260722 | 20260721 | −1.28% | +1.34%(93) | +1.51%(38 颗) | −0.77% | **−0.88%** | 100 / 41 颗 | 否 / 否 |
| 20260723 | 20260722 | +1.82% | +4.98%(7) | +4.74%(5 颗) | +1.28% | **+1.28%** | 40 / 10 颗 | 否 / 否 |
| 20260724 | 20260723 | **−2.86%**(89% 个股下跌) | −0.18%(45) | −1.24%(9 颗) | −2.77% | **−2.80%** | 46 / 12 颗 | 否 / 否 |

**触发次数:改前 0/4 天、改后 0/4 天,触发日差异 = 无。** 两口径差:改后四天 0.00 / 0.11 / 0.00 / 0.03 pp(切片下每颗种子样本量相同 = K,两口径天然贴近);改前 0.29 / 0.17 / 0.24 / **1.06** pp(∩ 池后各种子命中数悬殊,pooled 被大种子拖走)。

**⚠ 最要紧的一张表 —— 切片准不准(对照「主线板块本身」= 种子全成分,不 ∩ 任何池)**:

| D | 全成分 pooled | 全成分 per-seed | 切片 per-seed 的误差 | ⑧-F ∩池 per-seed 的误差 |
|---|---|---|---|---|
| 20260721 | −1.60%(210 只) | −1.31% | **0.00pp** | 1.96pp |
| 20260722 | −1.54%(1743 只) | −1.29% | **0.41pp** | 2.80pp |
| 20260723 | +1.89%(174 只) | +1.81% | **0.53pp** | 2.93pp |
| 20260724 | **−2.98%(506 只)** | **−2.75%** | **0.05pp** | 1.51pp |

**结论(与 ⑧-F 相反,方向与 planner 预期一致)**:配额切片是「主线板块本身」的**无偏且够准**的估计 —— per-seed 最大误差 0.53pp,真跳水日 07-24 只差 **0.05pp**;而 ⑧-F 的 ∩池样本那天差 1.51pp(per-seed)/ 2.57pp(pooled,−0.18% vs −2.75%…−2.98%)。**这条路不再是聋的**。⚠ **07-24 仍不触发,但这已经不是样本的问题** —— 主线板块本身(per-seed −2.75% / pooled −2.98%)就在 −3.0% 阈值**之上**,是**阈值灵敏度**问题,归 §七 [P3-37];⛔ **未动阈值、未调 K 去凑触发**(K=6 时该日读数 −3.04% 会触发,**刻意不采用** —— 那正是 ⑧-G-B 明令禁止的"调 K 凑频率")。

**⚠ 保底配额定数 + 一条 plan 判据做不到(如实登记,交回 planner)**。实测(2026-07 全月 17 个交易日,真实 parquet):**主线切片(K=4)40 ~ 100 只/日**、**昨日涨停 32 ~ 223 只/日**(planner 引的 43~129 是 ⑧-F 那四天窗口的范围)、**两份并集 72 ~ 275 只/日**。
- **⑧-G-D 的判据「两份样本在四天里都没被截断过」在 200 池位内做不到**:07-21 两份合计需要 **213** 个不同代码(切片 100 + 涨停 121 − 重叠 8)> 可用 **171**;更极端的 07-01 光昨日涨停就 **223** 只。⚠ **昨日涨停这一份本来就一直在被截**(改组前给它的额度同样是"剩余 ~171"),不是 ⑧-G 引入的。
- **故按「新样本吃满实测上界、老样本拿余量且不低于保底」定数:切片 100 / 涨停 71**(合计 171)。四天里**切片一次没被截**;昨日涨停 07-22 被压到 71(需求 121)、07-24 压到 125(需求 129),另两天一只没少。
- **被压的那两天,炸板率读数没变**(EOD 代理:D 日 `high ≥ 涨停价` 且 `close < 涨停价`):07-22 全量 121 只 → 71 只,炸板率 **42.9% → 42.9%**(涨停 12/炸板 9 → 8/6);07-24 129 → 125,**37.9% → 37.9%**(18/11 不变)。**⛔ 未抬 `breadth_cap`。** 若 planner 认为该换个分法(如切片 71 / 涨停 100,代价是宽种子日切片的 K 掉到 ~2),改两个常量即可,两份样本的口径与判定逻辑都不必动。

**⑧-G-E 授权的那条断言改动(显式、单独说明)**:`tests/test_sentinel_retreat.py::TestEarlySessionStricter::test_sector_dive_gradient` **`hot_n=3` → `hot_n=5`**,改前 `_eval(_snap(), hot_avg=-0.035, hot_n=3, NORMAL).triggered == [COND_SECTOR_DIVE]`、改后同一断言 `hot_n=5`;**两个阈值与该用例要证的"常规触发 / 早盘不触发"梯度语义一字未动**,改的**只是**样本量入参 —— 因为 ⑧-G-E 把准入下限从 `>0` 抬到了 5,n=3 起该条件不再准入(行为按裁定变了、测试跟着变,不是改断言迁就代码)。测试内已写清改前改后与裁定依据(`351ce56`),并新增 `test_sample_below_min_mainline_sample_is_not_judged` 把 n=0…4 一律不判、n=5 触发锁死。`tests/test_sentinel_engine.py` **只改夹具与数据规模**(`_seed_mainline_sample` 改成按 ≤K 切多颗种子、不再伪装涨停;`x_codes` 3 → 5 只),**断言一字未改**。哨兵其余纪律单测断言全部未动。

**测试**:`tests/test_sentinel_mainline.py` 18 → **32 例**,新增覆盖 ⑧-G 验收逐条 —— crc32 采样(前 K 逐位可复现 / 输入乱序结果同 / **板块无偏的分布断言**:100 颗种子 × 四板块各 10 只,crc32 下每个板块拿到 ≥12% 名额,**反证 `ts_code` 升序会把 100% 名额给同一个板块**)+ K=4 守门 + per-seed 估计量(大种子 200 成员 vs 小种子 8 成员**等权**,pooled 同时断言到且两者确实不等 / 无报价的种子不投票 / 空样本 → `None` 不是 0)+ `MIN_MAINLINE_SAMPLE`(**值等同 + AST 守门**:赋值右侧必须是 `ast.Name` 且确实 `from neckline.report.industry_strength import _MIN_MEMBERS`)+ 池配额(**200 只涨停的压力日**:持仓/成员/指数全在、两份保底一个不少、样本 ⊆ 池、30 颗种子一颗没掉队;**篮子成员 1 只 → 21 只〔且故意与切片、涨停名单、第三个板块三向重叠〕,两份测量样本逐位相同** = 残留耦合 ②b 的回归判据)+ `breadth_cap` 守门 + 四个原因码 + 当日冻结与失败不冻结 + ⑧-F 两条核心判据(签名无篮子入口 / 整拍级换篮子成员样本逐位不变)。全量 `pytest tests/ -q`:**2801 过 + 2 skip**(基线 2786 过 + 2 skip,净增 15,零回归);另有既有挂钟脆弱测试 `test_sentinel_custom.py::test_cooldown_blocks_second_hit_and_expires` 1 条(北京时间 ~14:48 后必红,**已 `git stash` 在改动前的工作树上复现,与本块无关**)。`scripts/smoke_sentinel.py` 跑通(对拍库无现役包 → 该路如实"不判",其余退潮条件照常)。

**如实登记的设计判断(六处,均未改 Plan)**
1. **样本 = 切片本身,⛔ 不再并入「种子成分 ∩ 机械池」的其余部分**。plan 写切片是样本的「**专属来源**」,故本实现里昨日涨停 / 持仓**不再**给样本供码(它们仍在池里、仍被别的哨兵判)。理由:把昨日涨停股并回样本 = 把 ⑧-G-A 定性要清除的**动量偏差**原样请回来。⚠ ⑧-G-G 第 1 条「排除的是 LLM 这条路、不是 LLM 碰过的票」仍然成立且更彻底:一只票被 `crc32` 选中就进样本,**与它是不是篮子成员/持仓/涨停股无关**(单测锁死)。
2. **两份测量样本按「必需项上界 29」这个固定预算分配,⛔ 不按"这一拍实际占了几个"**;`restrict()` 也**只看额度、不看谁已在池里**。若按实际占用算,LLM 多挑一个恰好也在切片里的成员就能让样本多出一只 —— **残留耦合 ②b 换个面目回来**。代价:必需项不满 29 时池子欠填 ≤29 个位(少 poll 几只),**刻意接受**;`mainline_codes`(新占池位)与 `mainline_sample.size`(样本量)因此可以不等,差值就是重叠数。
3. **池配额压缩走「逐颗种子轮转」**(先给每颗种子留第 1 只,再第 2 只……),⛔ 不整颗整颗地砍 —— 整颗掉队 = 少一票 = 直接改掉 per-seed 估计量的权重构成,比"每条主线少抽几只"失真大得多。
4. **`MIN_MAINLINE_SAMPLE` 作用在 `evaluate_retreat` 的准入判断上**(`> 0` → `>= 5`),不是在派生侧砍样本 —— 这样"样本多小"与"判不判"分开留痕,也正是 plan 点名那条 `hot_n=3` 断言必须改的原因。
5. **只有一颗主线的日子,该路必然不判**(单颗种子的切片上限 = K = 4 < MIN = 5)。这是裁定后的正确行为(一条主线不足以支撑"主线板块跳水"这个判断),不是夹具凑数;生产日常 10~41 颗种子、样本 40~100 只,离下限很远。
6. **三个原因码 / 来源标签随口径退役**:`REASON_NO_OVERLAP`(∩ 池的产物)→ `REASON_NO_POOL_QUOTA`(池一个位都腾不出来);`SOURCE_PREV_LIMIT_UP` / `SOURCE_POSITION` → 只剩 `SOURCE_MAINLINE_SLICE`(留痕的 `allowed_sources` 审计锚仍在,出现别的标签就是有人接了 LLM)。`retreat_metrics.hot_sector_sample_json` 的 payload 相应扩了 `seed_slices` / `per_seed_avg` / `pooled_avg` / `per_seed_k` / `min_sample` / `restricted_from`(**该列无任何读侧消费方**,纯审计留痕,形状变更不影响任何端点)。

**依赖**:④、⑧、⑧-F。**⚠ 交回 planner 的一件**:上面「保底配额」那条 —— 判据做不到,现按"新样本优先"定数并给了代价数字,若要换分法只需改两个常量。

---

#### 🩹 判定线 🟡-N1 收口(2026-08-03,@builder;commit `a8a14c3`):昨日涨停截断序改判 `crc32` 升序 + 需求量/实际量留痕(⑧-G-D 第②条同一件事)

**核实结论(权威规格:⑧-G-D 第②条,`88e1e17` 修订)**:review 判定线 🟡-N1 点名的 `_load_prev_limit_up_codes` 原排序键(`consec_limit_up_days` 降序,且未传 `maintain_order` —— 并列名次的相对顺序连"parquet 行序"这个近似说法都不保证,是 polars 排序实现的**未文档化行为**,比"不确定性"更差)**核实为真有偏,不只是不确定**。本项目 ⑦-K7 审计已实证连板高度是双尾放大器(簇内连板高度最高的成员,涨停触达概率约 1.5×、次日跌停概率约 3× 于同簇其余成员),而这份样本正好喂 `retreat.compute_breadth_snapshot` 算跌停数 / 炸板率 —— 池位不够时优先保留连板高的等于把跌停风险系统性更高的一批塞进分子,是 ⑧-F 那类有偏抽样的翻版。全仓未找到「截断优先保连板高」的注意力语义依据(该排序自 v1 阶段3〔`baff9e5`〕引入起就只服务于这一份宽度代理样本,原始提交未附带任何展示 / 注意力用途说明;`report/sentiment.py::max_consec_limit_up` 是独立的全市场 EOD 扫描,不消费这份顺序)。**故整个截断序换成 `crc32(ts_code)` 升序**(复用 `mainline.crc_rank`,不抄第二份)——不是像原 review 建议那样只给 `consec_limit_up_days` 降序加 `ts_code` 二级 tie-break(那样仍会把有偏的 consec 留作主键)。

**落点**:`WatchUniverse` 新增 `breadth_extra_needed`(需求量)与 `breadth_extra_payload()`(形状照 `mainline.MainlineSample.payload()` 的 `codes`/`size`/`restricted_from` 同名同义);`retreat_metrics` 新列 `breadth_extra_sample_json`(体例照既有 `hot_sector_sample_json`,幂等迁移),`RetreatMetrics`/`record_retreat_metrics`/`engine.run_tick` 三处打通,**每拍都落**(触发与否都落)。

**双载入确定性单测**:重写 `limit_derived` 分区(行序反转,模拟数据修缮 / 回填)后重读,结果逐位相同;另有一条直接断言排序结果恒等于 `sorted(codes, key=crc_rank)` 且不等于 consec 降序排列的结果(反证有偏主键已被换掉)。

**⚠ 改前改后同批历史日炸板率对照(顺带量化 🔵-N2 的 limit_down 一路)—— 如实登记,读数有实质变化,交回知悉**:用真实 parquet 复算 ⑧-G-D 完工记录已发布的两个真实配额点:

| D0(限)  | D(检查日) | 配额 | OLD(consec 降序)zaban_rate | NEW(crc32)zaban_rate | Δ(NEW−OLD) | limit_down 变化 |
|---|---|---|---|---|---|---|
| 20260721 | 20260722 | 71  | 42.9%(6/14)  | 33.3%(5/15)  | **−9.52pp** | 0 |
| 20260723 | 20260724 | 125 | 37.9%(11/29) | 37.9%(11/29) | 0.00pp | 0 |

07-21→07-22 这一天,换一种同样确定、同样无偏的截断方式,zaban_rate 从 42.9% 移到 33.3%——**不是「分毫未变」**,是实打实的 9.5 个百分点。为判断这是系统性偏差还是抽样噪声,追加用 2026-07 全月 17 个可用交易日、固定配额 K=71(不管当天真实配额是多少,统一口径看摆动量级)做探索性对拍:**真正发生截断(need>71)的 10 天**里,Δzaban_rate(NEW−OLD)均值 **−0.99pp**、中位 **−1.16pp**、样本标准差 **7.08pp**、绝对值最大 **12.11pp**(20260723→20260724 在 K=71 口径下摆动到 +12.11pp,与该日**真实配额 125** 下的 0.00pp 形成对照,说明摆动幅度随截断力度〔need 相对 K 的比例〕增大);limit_down 计数变化(NEW−OLD)为 `[1,2,2,0,0,1,2,0,0,0]`,全部 ≥0(未出现 NEW 更低的情形,方向性观察但 n=10 样本小,不作定论)。

**这说明什么、不说明什么**:①摆动均值贴近零、10 天里有正有负,与 crc32 本身"和板块 / 连板高度 / 涨跌停幅度均无关"的性质一致——**不是新引入的系统性偏差**;②但摆动幅度不小(标准差 ~7pp、单日最高 12pp),暴露的是更根本的事实:**把 121~223 只的总体压到 71 只,本身就会给 zaban_rate 这个小样本比率估计量带来可观的抽样噪声,与用哪条确定性规则去挑这 71 只无关**——旧的 consec 降序 + 未定义行序 tie-break 同样在承受这份噪声,只是此前不可复现、从未被量出来过;⑧-G-D 完工记录报的"42.9%→42.9% / 37.9%→37.9% 分毫未变",准确说是那两天在**旧规则的那个特定配额点**上恰好没变,不是"任何确定截断都不改变读数"的普遍性质。③不影响本次修复的必要性——crc32 换掉的是"有偏且不可复现"这两个真问题,换来的是"无偏但仍有抽样噪声"这个所有子采样方案都逃不掉的性质,是纯粹的改进而非新增风险。④**建议**:这组数字并入 §七 [P3-37](该项本就在追踪退潮哨兵灵敏度校准);71 只保底配额下 zaban_rate 的抽样噪声量级(±7~12pp)是否需要专门处理(如展示时带误差区间、或抬高保底配额),是需要实盘积累后另议的产品决策,本次不擅自处理。

**测试**:`tests/test_sentinel_universe.py`(+4)/ `test_sentinel_mainline.py`(+2)/ `test_sentinel_retreat_store.py`(+2)。全量 `pytest tests/ -q` **2801 → 2808 passed + 2 skip**(净增 7,零回归;唯一 fail 为既有挂钟脆弱测试 `test_sentinel_custom.py::test_cooldown_blocks_second_hit_and_expires`,已在改动前的 `main` 上独立复现,与本次改动无关)。

---

#### ⚠ V2-⑧-F 完工记录(2026-08-03,@builder-pro;**代码已落、红线已闭,但对拍结论与 planner 预判相反,样本口径本身待 planner 再裁一次** → **✅ 2026-08-03 已裁定,见上方 ⑧-G;样本口径已被 ⑧-G 取代,本记录留痕**)

**落点**:新增 `neckline/sentinel/mainline.py`(样本派生唯一源,模块头写死"为什么不经篮子");`sentinel/engine.py` 的 `_hot_sector_peer_returns` 由「吃 `wu.targets`(T1/T2 篮子成员)」改成「吃一串 code」,样本由 `mainline.derive_mainline_sample()` 现给;`retreat.RetreatMetrics` 加 `hot_sector_sample_detail`(默认空 dict,老调用点不炸);`retreat_store` + `db.py` 加 `retreat_metrics.hot_sector_sample_json` 列(幂等迁移,默认 `'{}'`)。**`sector_dive` 阈值(−3% / 早盘 −4%)与 `evaluate_retreat` 判定逻辑一字未动**(守门单测锁死两个常量值)。

**⚠ 与 plan 字面的一处偏差(请 planner 复核)**:plan 写「种子成分 **∩ 关注池**」,实现取的是「种子成分 ∩ **关注池的机械成分**」= `昨日涨停股 ∪ 持仓`,**只靠篮子进池的码不进样本**(它仍在关注池里、仍被别的哨兵盯)。理由:V2 的关注池**本身**含 T1/T2 篮子成员(⑧-A),而篮子成员 ⊆ 种子成分 —— 若整池取交,LLM 换一批成员就换一批样本,**⑧-F 验收里那条核心判据「换掉 LLM 的成员选择 → 样本逐位不变」当场不成立**。plan 的字面与它自己的验收判据在这一点上打架,按验收判据实现。⚠ 一只票若**同时**从机械路进池(本来就是昨日涨停)照进样本 —— 排除的是"LLM 这条路",不是"LLM 碰过的票"。

**⛔ 没有新增任何最小样本量阈值**:「样本不足」= 派生不出(无现役包 / 种子生成失败 / 当日无热点行业与暴起概念种子 / 与机械池无交集,四个原因码分开落)或交集为空;既有 `hot_sector_sample > 0` 这道门槛原样保留。曾考虑加 `MIN_SAMPLE=5`(plan 有「不用小样本硬判」一句),**放弃** —— 既有单测 `test_sentinel_retreat.py` 有一条 `hot_n=3` 触发的断言,加了就得改它,与 ⑧-F-B「哨兵既有纪律单测一条断言未改」直接冲突;plan 的两条要求在这里也是自洽的(不足 = 0)。

**改前 / 改后同批历史日对拍(真实 parquet 只读;隔离对拍库 = 参考四表 + K4-pack 现役 + `industry_strength_daily` 2026 全年 + `scan_layer` 7 月事实表)**。收益率用 D 日 `close/pre_close−1`(**EOD 代理一个尾盘拍**,本地无分钟线);改前样本(T1/T2 篮子成员)需 LLM 才建得出,本地无 key,**用「每颗种子取 D0 涨幅最高 1~3 只」的机械龙头代理**(与 planner 判词「LLM 挑龙头 / 中军」同向,故对"改后是否更易触发"这个结论是保守方向)。

| D(盘中日) | D0 | 全市场中位 | 改前 n / 均值 / 触发 | 改后 n / 均值 / 触发 | 改后−改前 |
|---|---|---|---|---|---|
| 20260721 | 20260720 | +0.59% | 18 / −0.68% / 否 | 27 / **+0.94%** / 否 | +1.62pp |
| 20260722 | 20260721 | −1.28% | 18 / −1.57% / 否 | 93 / **+1.34%** / 否 | +2.91pp |
| 20260723 | 20260722 | +1.82% | 18 / +1.95% / 否 | 7 / **+4.98%** / 否 | +3.02pp |
| 20260724 | 20260723 | **−2.86%**(89% 个股下跌) | 18 / −1.10% / 否 | 45 / **−0.18%** / 否 | +0.92pp |

**触发次数:改前 0/4 天,改后 0/4 天(触发日差异 = 无)**。改前代理换篮子数(3/6/10/20)重跑,四天均值在 −2.04%~+2.79% 之间摆动、**一次都不触发**,结论对代理参数不敏感。

**⚠ 差异方向与 planner 预判相反,按 ⑧-F-B「停手回报」办**:planner 预判「LLM 样本偏抗跌 → 机械样本更易触发」;**实测四天里改后样本一律比改前更抗跌(高 0.9~3.0pp),即改后更\*不\*容易触发**。追因一层(同一批日子的横向对照)已定位到真因,**不是 LLM 挑票的问题**:

| D | 全市场中位 | 种子**全成分**(未 ∩ 池) | ∩ 昨日涨停(= 本实现) | 昨日涨停**全体** |
|---|---|---|---|---|
| 20260721 | +0.59% | −1.60%(210 只) | +0.94%(27) | +1.24%(55) |
| 20260722 | −1.28% | −1.54%(1743) | +1.34%(93) | +1.54%(121) |
| 20260723 | +1.82% | +1.89%(174) | +4.98%(7) | +4.70%(48) |
| 20260724 | **−2.86%** | **−2.98%**(506) | **−0.18%**(45) | −0.22%(129) |

**真因 = 「∩ 关注池」这一步本身**:关注池的机械成分几乎全是**昨日涨停股**,那是全市场动量筛得最狠的一群;真跳水日(07-24 中位 −2.86%)它们只跌 0.22%,**∩ 之后的样本读数(−0.18%)与"主线板块跌了多少"(种子全成分 −2.98%,已贴着 −3.0% 阈值)相差 2.8 个百分点**。换言之:**本条红色刹车路径在"样本 ∩ 关注池"这个前提下近乎失效,与样本是 LLM 挑的还是机械派的无关**;⑧-F 真正修好的是「样本可被 LLM 塑形」这条红线(已闭、可审计),**没有**修好「这条路能不能真的响」。⛔ 未擅自改阈值、未擅自扩池,**留给 planner 裁**。已量化的一个候选方向(仅供裁定参考,**未实现**):给关注池加一条**机械配额切片**(每颗种子按 `ts_code` 升序取前 K 只,K=6 时 58~144 只、放得进 200 的池),同四天读数为 −1.66% / −0.33% / +2.35% / **−2.46%**,与全市场同向、且 07-24 已进入阈值邻域 —— 但「每颗种子取几只、与昨日涨停股抢池位怎么排优先级」直接决定一个纪律触发器的读数,**是 planner 的决定,不是 builder 的**。

**测试**:新增 `tests/test_sentinel_mainline.py` 18 例 —— 核心判据两条路各证一次(①`derive_mainline_sample` 签名里**根本没有**篮子入口〔结构性守门〕;②整拍级:同一天同一份 `limit_derived`、只换 D0 篮子成员 → `retreat_metrics.hot_sector_sample_json` 逐位相同)+「只靠篮子进池的码不进样本」+ 持仓进样本 + 四个不可用原因码各一 + 当日冻结(`generate_seeds` 只被调一次)+ **失败不冻结**(瞬时故障下一拍自愈)+ 顺序确定性(输入乱序结果同)+ 留痕字段 + 样本不足不触发且 `hot_sector_avg_chg` 为 `None`(不是 0.0)+ 两个阈值常量守门。既有 `tests/test_sentinel_retreat.py` **一条断言未改**;`tests/test_sentinel_engine.py` 只改**夹具**(`_seed_mainline_sample` 把样本按新口径喂进去),**断言逐字未动**。`scripts/smoke_sentinel.py` 跑通(对拍库无现役包 → 该路如实"不判",其余退潮条件照常,正是设计的降级路径)。

**⚠ 一处已知残留耦合(如实登记,未修)**:关注池按 `breadth_cap=200` 截断时,`昨日涨停股`拿的是"减掉持仓/篮子/指数之后的余额"—— 篮子成员多一只,涨停股名额就少一只,**极端情况下(当日涨停 > 180 只)LLM 仍能间接挪动样本的尾巴**。四天实测涨停 43~129 只,离 180 还远,故当前不触发该耦合;要彻底解耦得改池的配额规则,同样归 planner 那条裁定。


#### ✅ V2-⑧-E 完工记录(2026-08-02,@builder)

**落点**:全部改动只在 `neckline/sentinel/basket_verify.py` 一个文件(未碰 `verification_rules.py`——`vr.EPS` 已存在,直接复用,无需新增;未碰 `basket_card.py`,⑦ 的卡形状零改动)。新增 `FLAG_ANCHOR_MISMATCH`/`REASON_MEMBER_EX_RIGHTS`/`REASON_ANCHOR_MISMATCH`/`REASON_ANCHOR_UNCONFIRMED` 四个原因码常量;`MemberObservation` 加 `pre_close` 字段(默认 `None`,老调用点 / 测试替身不传时安全降级为"不做锚检测");`_member_ref_close()`(从 spec 成员行取 `ref_close`)+ `_anchor_mismatch()`(`abs(pre_close-ref_close) > vr.EPS`,任一缺失或非正返回 `False`)两个纯函数;`evaluate_specs()` 的成员循环里,锚检测排在正常条件判定**之前**,命中即 `continue`(与既有 `missing` 分支同款「两侧都不计」机制,原因码单独一个 key、不与 `member_data_missing` 混)。

**两条路径与交叉确认**:`evaluate_specs()`/`evaluate_card()` 保持盘中/EOD 唯一共用,检测器本身零分叉——`_observations_from_quotes()` 从 `Quote.pre_close` 取值,`_eod_observations()` 从 `daily.pre_close` 取值,喂进同一个 `MemberObservation.pre_close`。EOD 专属的 `adj_factor` 交叉确认**不塞进** `evaluate_specs()`(那会让"要不要查 adj_factor"这个 EOD 专属分支混进盘中/EOD 共用代码),改在 `run_eod_verification()` 里做两遍:第一遍照常判(检测器已在 `evaluate_card` 内生效),收集所有篮子里被标 `FLAG_ANCHOR_MISMATCH` 的成员码;第二遍只对**这些成员**(不是全市场)批量查 D0/D+1 两天的 `adj_factor`(新增 `_eod_adj_factor_maps()`),再用 `_confirm_anchor_mismatches()` 原地把 `confirm∈{member_ex_rights,anchor_mismatch,anchor_unconfirmed}` 与原始 `adj_factor_d0`/`adj_factor_d1` 写回每个成员的 evidence 行——**不改 `state`/`verify_hits`/`invalidate_hits`**(两种成因在检测阶段就已经都被排除出命中计数,分类只决定要不要打 WARNING)。`REASON_ANCHOR_UNCONFIRMED`(两天任一缺 `adj_factor` 行)是本块在 plan 两态之外**自行补的第三态**(不猜、不报警),承本项目「没有 ≠ 没看」一贯纪律,非 plan 逐字要求,如实登记。

**真除权样本冒烟(真实历史数据,只读)**:扫描本地 `data/parquet/adj_factor` 找到 `603409.SH` 在 2026-06-17→06-18 的真实除权事件(`adj_factor` 1.0146→1.5265,一次约 1.5 倍的送转)。真实 `daily` 数据:D0(06-17)收盘 31.70,D+1(06-18)`pre_close`=21.07(交易所公布的除权参考价)、真实收盘 21.42。直接调用 `bv._anchor_mismatch(31.70, 21.07)` → `True`(若不识别,拿 21.42 直接跟 31.70 比等于误判单日暴跌 32.4%,任何一个失效条件都会被触发);再用真实 `adj_factor`(1.0146→1.5265)跑 `_confirm_anchor_mismatches()` → `confirm=member_ex_rights`,与预期完全一致。

**测试**:新增 `tests/test_basket_verify_ex_rights.py` 10 例(单成员锚失效两侧都不计 / 2 成员篮"一只锚失效 + 一只⑦-b 中间地带"回归场景判 `unclear`〔并附反证:该锚失效成员若不做检测会单独触发复合失效条件〕/ 反向验证锚失效不拖累其余成员正常 `verified` / `_EPS` 容差两向 / `pre_close` 缺失时是空操作 / 盘中-EOD 检测器逐位相同 / EOD 交叉确认三态各一〔含 WARNING 断言〕/ 卡形状与 `spec_version` 守门)。`pytest tests/ -q`:开工基线 **2754 passed + 3 failed + 2 skipped**(3 failed 含 2 个既有周日时间炸弹 + 1 个并行 ⑨ 施工中的临时失败,与本子项无关)→ 完工 **2766 passed + 2 failed + 2 skipped**(2 failed 仅剩既有周日炸弹,⑨ 的临时失败已被并行 builder 自行修复;本子项净增 10、零回归)。`tests/test_sentinel_basket_verify.py` 全 40 例逐条断言未改仍绿。

**顺手修一个被本子项唤醒的沉睡真洞**:`scripts/smoke_basket_verify.py::_quote()` 原来把 `pre_close` 填成**当前合成检查点的价格本身**(`pre_close=price`)——⑧-E 上线前 `Quote.pre_close` 没人读,这个填法无害;⑧-E 一读它,**每一拍的"剧本价"都会被判成跟锚不一致**(涨跌几个百分点必超 `vr.EPS`),把冒烟原本设计的 verified/partial/unclear/falsified 四态剧本整个打成清一色 `unclear`,连带 EOD 的 4/5 `falsified` 也因为"当日从未真正记过 falsified、没有可 latch 的行"跌成 3/5(EOD 自己重算出的 `unclear` 恰好是对的——用真实 07-24 收盘价单独核算 `601168.SH` 确实不破位,原先的"falsified"是**盘中合成剧本触发 latch 之后 EOD 照抄不撤回**,不是 EOD 自己算出来的)。定位过程:git stash 掉本子项改动跑同一份冒烟对比前后输出,逐位核对真实数据反推出`_quote`把"当日固定值"当成"逐拍变量"传错了参数。已修:`_quote()` 新增显式 `pre_close` 形参(不给默认值,逼两个调用点都得想清楚该传什么),两处调用点均改传**当日不变的 D0 收盘**(`px`/`base`);修完重跑,四态与 4/5 falsified 都恢复成完工记录原文描述的样子。**这处 bug 不影响生产**(真实 `sentinel/quotes.py::Quote.pre_close` 一直来自行情源真实解析,只有这份手搓合成夹具传错)、**不影响本子项任何单测**(单测里的 `pre_close` 都是照场景手写的正确值),但如果不修,以后任何人拿这份冒烟脚本验证 ⑧ 相关改动都会被这个假阳性坑一次。

**如实登记(两条)**:① 施工期发现并修正一处设计陷阱——最初把 `pre_close`/`ref_close` 无条件加进*每个*成员的 evidence 行(为审计完整性),结果打破既有单测 `test_intraday_and_eod_paths_agree_bit_for_bit`(盘中 mock `Quote` 不带 `pre_close`,EOD `daily` 面板有,两条路径的 `members[]` 逐位比较因此出现一个跟判定结果本身无关的字段差异而失败)。修正为:`pre_close`/`ref_close` 两个键**只在锚失效分支的成员行里出现**,正常判定分支的成员行形状与 ⑧-E 之前逐字节不变——这不只是为了让测试通过,而是"同一检测器逐位相同"这条不变量本该只约束判定字段,不该被两条路径对同一incidental 字段的可得性差异误伤,未来再给成员行加审计字段时需留意同一陷阱。② plan 验收原文"2 只成员篮里一只锚失效、另一只正常破位"按字面(`min_members_hit=ceil(2/2)=1`)与"整篮判 `unclear`"在数学上不可共存——若"另一只"真触发一个失效命中,单独已够 `min_hit=1` 判 `falsified`,与本子项要修的方向无关。已按最贴近字面意图实现:2 只成员基数不变,"另一只"取⑦-b 定案的"中间地带"(复合失效条件两个子项只成立一个,`v_hit=i_hit=False`,不是净空 `None`),真实体现"该成员按⑦-b 本就不该判失效"而非"该成员没数据";测试内联反证证明"若不排除锚失效成员,它单独就够 `min_hit=1` 误判整篮"确有其事。此为本人对 plan 表述的建筑性澄清,非偏离验收意图,附此存照。

**依赖**:⑦、**⑦-b**(条件集权威)。

#### ✅ V2-⑧ 完工记录(2026-08-02,@builder-pro)

**新增 / 改动的落点**

| 文件 | 干什么 |
|---|---|
| `neckline/sentinel/basket_verify.py`(新) | 状态机:`evaluate_specs()`(**盘中与 EOD 共用的唯一判定函数**)/ `evaluate_card()` / `run_intraday_verification()` / `run_eod_verification()` |
| `neckline/sentinel/basket_verify_store.py`(新) | `basket_verification` **只 INSERT**:`append_if_changed`(盘中,变了才落)/ `append_row`(EOD 必落)/ `current_state`(三路读法唯一实现)/ `list_rows` / `states_for_date` |
| `neckline/sentinel/capture.py`(新) | 存拍:内存累计 → 15:05 一次 `write_table_day` 落两张 parquet + `capture_status` 三态 + 两个窗口判定 + 09:25 竞价采集 |
| `neckline/sentinel/universe.py` | ⑧-A 关注池改组(见下) |
| `neckline/sentinel/engine.py` | `run_tick` 挂两条**旁路**(各自独立 try/except)+ `TickResult` 三个观测位 |
| `neckline/api/app.py` | lifespan 轮询挂两条旁路分支(09:25 竞价 / 15:05–15:35 落盘),**轮询节奏一格没改** |
| `neckline/sentinel/quotes.py` | `to_symbol` 让 `.SH/.SZ/.BJ` **后缀优先**(见下「顺手修的真洞」) |
| `neckline/selection/basket_store.py` | 读侧新增 `BasketRef` + `load_baskets_for_date(trade_date, tiers=…)`(篮子族表的读也归这个 store) |
| `scripts/basket_verify.py`(新) | EOD 那一拍的 CLI(`run` / `show`,支持区间补算) |
| `scripts/smoke_basket_verify.py`(新) | 真实 D0 锚 + 合成分钟报价的四态冒烟 |

**⑧-A 关注池**:`持仓 > T1/T2 篮子成员 > 候选(V1 残留)> 板块基准指数`,`昨日涨停`填剩余额度到 `breadth_cap=200`(与改组前同量级)。**自选池不再是来源**(`universe.py` 一行都不读 `neckline.watchlist`,守门单测扫 import);`watchlist_codes`/`watchlist_candidates` 两个字段**保留为空**,好让 `engine.py`/`precall.py` 里 `wu.candidates + wu.watchlist_candidates` 那一行**一个字不改**(⑧-D);模块与表的删除留给 ⑬-11。**候选仍在**(⑬-1 未执行,买点/证伪哨兵此刻仍以 `Candidate` 为对象,先建后拆)。

**⑧-B 存拍**:零额外网络(用 `run_tick` 那一拍已拉到的行情);`volume`/`amount` 落**本拍增量**、当日首次观测与源回退一律 `null`(⛔ 不写 0 冒充「这一分钟没成交」),`cum_*` 原样落;失败只 WARNING。`capture_status` 判据写死在模块头:`missing`(零行/落盘失败)/`full`(无空拍 + 头尾都在 5 分钟宽限内)/`partial`(其余),**同时把 `covered_minutes`/`expected_minutes`/`empty_ticks`/`rows`/`codes` 原始数字一起落**,⑨ 要用哪条线自己定,本块不替它发明阈值。

**⑧-C/C2 状态机**:七条逐条落地并单测锁死(同 spec 不同代入价 / 锚取 D0 冻结值 / 触及跌停用 low / 状态未变不落行 + EOD 必落 / 三路读法 / `falsified` 当日终态不撤回而 `verified` 可翻 / 无卡落 `unclear`+`no_card`)。

**如实登记(七条,均未改 Plan;如与规划意图不符请澄清)**

1. **`capture_status` 没有落点,本块定为 `sentinel_events`**(`sentinel='capture'`,`event_key='intraday'|'auction'`,payload 带全部原始数字)。理由:① `missing` 恰恰是「一行都没有」的那天,状态**写不进 parquet 行里**;② ① 已把 21 张表建齐并写明「此后每块只写读写逻辑、不再动 DDL」,为一个日级标记开第 22 张表不划算;③ `sentinel_events` 本就是哨兵当日事件台账、**已有非推送用法**(退潮黄色预警只落看板)。读侧 `capture.load_capture_status()` 明确区分「没记过」(`recorded=False`)与「记了是 missing」。
2. **「相关板块 ETF/指数」本版只做到「板块基准指数」**:项目**没有 ETF 成分/映射数据源**(TuShare 600 元档没有;`ths_index` 是同花顺板块指数,免费实时源不认它的代码),硬编一份 ETF 清单等于凭空发明。落地 = 按成员板块取 `000001.SH`/`399001.SZ`/`399006.SZ`/`000688.SH`/`899050.BJ`(与 `scripts/backfill.py::INDEX_CODES` 同一批),板块判定唯一源 `load_stock_meta`。**要真 ETF 得先有数据源。**
3. **顺手修一个真洞(不修就会静默拉错标的)**:`quotes.to_symbol` 原本纯按数字前缀判市场,`000001.SH`(上证综指)会被拉成 `sz000001`(**平安银行**)。改成 `.SH/.SZ/.BJ` 后缀优先,对**所有股票代码形状逐位等价**(单测遍历九种形状断言 `to_symbol(带后缀) == to_symbol(裸码)`)。
4. **`intraday_ticks.trade_date` 落 `pl.Date` 而不是 plan DDL 字面写的 `TEXT`**:本项目全部 parquet 表的 `trade_date` 都是 `pl.Date`,而 `get_market_slice` 用 `pl.col("trade_date") == <date>` 过滤 —— 落成字符串会让 ⑨ 用标准取数路径读这张表时**类型不匹配**。`ts` 仍是 `HH:MM:SS` 字符串。
5. **口径局限:锚是前复权、观测是原始价**。盘中免费源只给原始价,EOD `daily.close` 也是原始价,**两条路径彼此一致**(⑧-C2 第 1 条要的正是这个);代价是某成员**恰在 D+1 除权除息**时锚与观测差一个复权因子,可能误判破位。正解是让 **⑦ 在卡里额外冻一份原始价锚**(⑦ 的形状变更,不在 ⑧ 授权范围)→ **提请裁定**。已写进 `basket_verify.py` 模块头。
6. **EOD 那一拍谁来调,Plan 未定死**:本块只提供函数 + `scripts/basket_verify.py` CLI,**没有**擅自挂进 `daily_update.py`(那是 ⑯-D 的分段职责,⑭-A 的 16:35 链才是 plan 指定的编排入口)。⚠ **在 ⑭ 落地之前,EOD 定论行不会自动产生。**
7. **09:25 竞价快照自己拉一次价**(每天 1 次,相对既有 ~240 次/天可忽略),不搭 `run_precall_tick` 的便车 —— ⑧-D 要求 precall 判定逻辑一行不动,把存拍塞进去(哪怕只是旁路)会让那个文件多一条与盘前校准无关的路径。

**验证证据**

- 全量 `pytest tests/ -q`:**2735 passed + 2 skipped + 2 failed**(开工基线 2652+2+2;两条 failed 是既有周日时间炸弹,同名同因,**零回归**)。净增 83 例(⑦-b 31 + ⑧ 新建 45 + 关注池/接线改写 7),删除 5 例已随自选来源退役的用例(不留 skip)。
- **`scripts/smoke_basket_verify.py`(真实 D0=2026-07-23 → D1=2026-07-24,隔离库 + 临时 parquet)**:5 个篮子(成员互不重叠)用真实收盘 / 真实前复权 MA20 / 真实跌停价 / 现役 `stop_pct=0.05` 冻卡 → 合成 4 个分钟检查点驱动 —— 盘中出现 **verified / partial / unclear / falsified 四态齐**,`no_card` 篮如实落 `unclear`;第 3、4 拍「破位篮」价格拉回 +2%,**判定不撤回、也不落新行**(`定格跳过 1`);存拍 32 行 tick + 8 行竞价落盘,读回 **两张表声明的数值列全部 Float64 ✓**;EOD 用真实 07-24 收盘价再判 —— 当天是真实大跌日(全市场均 −2.62%,样本 −2.7%~−9.99%),5 篮里 4 篮翻 `falsified`(其中 `600396.SH` 最低价 14.77 **恰好等于**算出的跌停价 → 「触及跌停」条在真实数据上打响),`no_card` 篮仍是 `unclear`。
- **`scripts/smoke_sentinel.py`(真实 2026-07-22 → 07-23 回放)**:挂了两条旁路之后,四哨兵照常出结果(6 个买点 / 13 个证伪 / 1 个持仓 `stop_approach`),**纪律路径零回归**。

**验收**:关注池组成单测(四类来源齐、去重、上限、自选池不再进);存拍写-读往返 dtype 合声明 + `capture_status` 三态;状态机四态各一单测 + append-only 断言;**哨兵既有纪律单测全绿且不许改断言**;**⑧-C2 追加六条** —— ① 盘中与 EOD 用同一份 spec(单测:同一价格代入两条路径 → 判定逐位相同);② 锚取 D0 冻结值(单测:改"当前" MA20 / 跌停价 / 现役 `stop_pct` → 判定**不变**,证明读的是卡);③ 状态未变不落行、变了才落 + EOD 必落一行(单测计数);④ 「当前状态」三路读法(有 EOD / 只有 intraday 标暂态 / 都没有标 `not_evaluated`);⑤ `falsified` 当日终态不撤回、`verified` 可翻(单测两向);⑥ 无卡 → `unclear` + `no_card`,**不拿默认条件顶上**(单测);pytest 零回归。

---

### V2-⑨ · ⑤-b 盘后复盘引擎 + 评价引擎(@builder)✅ 完工(2026-08-02)

**目标**:选股能力的审计者。**评价引擎与篮子引擎同日上线,D1 起出数。**

**⑨-A 机械判(新建 `neckline/review/basket_review.py`)** —— 九项(蓝图 4.8):竞价 vs 剧本 / 开盘首方向 / 分时 MFE·MAE / 成员同向率 / 龙头带动 / 可买性(一字与涨停排除)/ 验证与证伪时点 / 收盘 RS / D0 逻辑与 Tier 哪里对哪里错的机械依据。
- **MFE/MAE 有存拍才有精确值**;缺存拍 → **EOD 近似 + 如实标注**(`mech_json.mfe_source ∈ {intraday, eod_approx}`),**不装精确**。

**⑨-B LLM 解释**:吃机械判 + 分时摘要,解释哪里对哪里错(`TASK_REVIEW` → 推理 Agent)。**T1/T2 每日必复盘;T3 留量化全量 + 简评**(防错杀检验)。预算不足时先丢 T3 简评(② 的降级次序),`llm_skip_reason` 如实落行。

**⑨-C 评价引擎(新建 `neckline/eval/`)** —— 指标:Tier 单调性(T1>T2>T3)、篮子共振率 / 验证率、龙头 vs 同篮其他成员、**可交易收益**(排除不可成交的一字上涨)、用户已选 vs 未选对照、哪类驱动 / 角色 / 市场环境贡献正收益。**按 `pack_version` 归因**(§12.5:每个包自动攒出自己的成绩单,喂给策略线下一轮迭代)。周度出**校准报告**(进 ⑫ 的周复盘工作台)。

- **⑨-C2 安慰剂对照臂(K7 需求 6;交接稿列为「建议项」,**用户 2026-08-02 说「尽量都做到」→ 本 plan 排为正式验收项**)**:周度校准报告新增两条对照臂,让「Tier 有效性」这个长期命题能回答**「是否稳定优于随机与不作为」**。
  - **臂 A · 随机同规模篮子**:同一交易日,从**当日通过卫生线与安检的合法域**里随机抽出与真实篮子**同数量、同成员数**的对照篮子,走**同一套判分**(⑨-D 的 `exit_sim`,口径唯一源不另写)。**随机种子必须可复现** —— 用 `zlib.crc32(f"{trade_date}|{pack_version}|{arm}")` 派生(**禁内置 `hash()`**,带进程盐;项目 CLAUDE.md 明文),历史报告重跑逐位相同。抽 **N 次取分布**(N 是引擎常量,不进包),报告给中位数 + 分位,**不是单次抽样**(单次抽样的对照没有统计意义)。
  - **臂 B · 满仓持有基准**:同期「不作为」基准 —— 按大盘指数(`SSE_INDEX`,与 RS 线同源)同期持有收益,回答「折腾一圈有没有跑赢躺着不动」。
  - **诚实边界(写进报告文案)**:样本量不足时**只给样本数、不给结论**(「N=7 个交易日,尚不足以判断」),**禁在小样本上宣布优劣**;两条臂都**只进周报展示与策略线迭代输入**,**不进任何在线判据**(不改 Tier、不改排序、不进哨兵)。
  - **归因维度对齐**:两条臂同样按 `pack_version` 分层,这样「换包有没有变好」与「比随机好多少」能放在同一张表里读。

**⑨-D 判分引擎唯一源(重要,架构稿只给了原则,这里给落法)**
- 架构稿要求「判分复用考官线成交层 + h9 退出模拟器,判分引擎唯一源不另写」;但现实是它住在 `research/h9_exit_reform.py::_sim_one`,而 **`research/` 反向 import `neckline/`**,生产不能倒过来 import 研究件。
- **定死做法**:把 `_sim_one` 及其 `SLIP` / `BROKER` 常量**下沉**到新模块 `neckline/eval/exit_sim.py`;`research/h9_exit_reform.py` / `drill.py` / `exam.py` 三处改为 **import 它**(方向仍是 research → neckline)。**搬迁前后逐位对拍单测锁死**(拿既有 golden 用例跑,输出逐位相同才算搬完)。
- **每日复盘只记录,不因单日失败改策略**(蓝图 4.9);改权重一律走换包。

**依赖**:⑦⑧(⑫ 消费本块产出)。
**验收**:`_sim_one` 搬迁逐位对拍绿(**这条不过就不许继续**);缺存拍时走 EOD 近似且标注(单测两路);Tier 单调性 / 共振率 / 验证率在造数上算对;`pack_version` 分层可查;T3 简评在预算耗尽时被丢且如实披露;**⑨-C2 追加三条** —— ① 两条对照臂在造数上算对且**同一交易日跑两次逐位相同**(`crc32` 派生种子的可复现性单测);② 随机臂走的是**同一套 `exit_sim` 判分**(grep 守门:`neckline/eval/` 内无第二份判分实现);③ 小样本时报告出的是样本数而非结论(单测断言文案);pytest 零回归。

#### ✅ V2-⑨ 完工记录(2026-08-02,@builder-pro)

**新增文件**

| 文件 | 职责 |
|---|---|
| `neckline/eval/exit_sim.py` | **判分引擎唯一源**(⑨-D):`ReTrade`/`_sim_one`/`SLIP`/`BROKER` 自 h9 **逐字搬入** + `score_kw_from_charter()`(章程→判分参数唯一翻译)+ `fill_and_score()`(考官线 §九 竞价成交层,支持卡上 `max_chase` 上限)+ `build_price_maps()` |
| `neckline/eval/metrics.py` | ⑨-C 七组指标 + `load_basket_panel()`(五表 + `user_actions` 只读装配)+ `Verdict` 结论闸 + 按 `pack_version × verification_ruleset_version` 分层 |
| `neckline/eval/placebo.py` | ⑨-C2 两条对照臂 + `derive_seed()`(crc32)+ `default_legal_domain()`(可注入) |
| `neckline/eval/calibration.py` | 周度校准报告装配 + markdown/JSON 渲染(**不接报告管线**) |
| `neckline/review/basket_review.py` | ⑨-A 机械判九项 + ⑨-B LLM 解释 + `review_day()` 编排 |
| `neckline/review/basket_review_store.py` | `basket_review_daily` 读写(**只有 INSERT OR IGNORE**,无 UPDATE/DELETE 路径) |
| `scripts/basket_review.py` / `scripts/weekly_calibration.py` | 两个 CLI |
| `scripts/smoke_eval_exit_sim.py` / `scripts/smoke_basket_review.py` | 真 K 线判分对拍 / 端到端冒烟 |

**⑨-D 下沉与对拍(验收第一条)**:`research/h9_exit_reform.py` 删掉本地定义、改 `from neckline.eval.exit_sim import BROKER, SLIP, ReTrade, _sim_one` 再导出(`h9.X` 老写法一字不改仍工作);`drill.py` / `exam.py` 改直接 import(`h9.BROKER`/`h9._sim_one` 两处调用点重接,`drill.SLIP` 改指新源)。对拍两层:①**源码逐位** —— 单测内嵌搬迁前的冻结源文本,与 `inspect.getsource()` 逐字比对(注释与默认参数都不许差一个字节);②**行为逐位** —— 冻结源 `exec` 出独立可调用体(**独立 new 的 `Broker()`**,连构造参数被改都能照出来),400 条随机造数 + 三条窄分支(`end` / 跌停顺延 / 停牌顺延)逐字段全等。真 K 线:`scripts/smoke_eval_exit_sim.py` 拿 `k3_panel` **900 笔真实 (代码,交易日) 入场单 × 3 组退出参数**(现役 v1.3.3 口径 / K1 口径 / V1+V2 变体)对拍,退出原因分布 `stop 317 / time 450 / retrace 130 / end 1 / none 2`,**不一致 0 笔**。

**⑨-A 机械判九项落点**(`mech_json` 九个顶层键,`spec_version=basket_review_mech_v1`):`auction_vs_script`(竞价优先取存拍 09:25 快照、缺则退 `daily.open/pre_close`,`source` 落下来)/ `open_direction`(跳空 × 日内 × 存拍首拍方向)/ `mfe_mae`(存拍→带时刻;缺存拍→EOD 近似 + `mfe_at=None` + 文案「幅度可信、**时刻未知**」)/ `member_alignment` / `leader_pull`(**龙头从 D0 冻结卡认**:`role_mech=='leader'` > `rs_rank==1` > `is_primary`,⛔ 不拿当日涨幅最大者当龙头)/ `buyability`(口径对齐既有 `fwd_buyable`,一字单列;涨停价优先取**卡上冻结**的次日涨停价)/ `verification_timing`(读 ⑧ 的 `current_state()`,不自拼 SQL)/ `close_rs` / `tier_vs_outcome`(**只给依据不下结论**,当日横截面名次带确定性 tie-break)。**成员收益一律用 `daily.pct_chg`**,不拿卡里前复权收盘相减 —— 恰好绕开 ⑧ 登记的除权锚坑。

**⑨-B / 预算降级**:`run_review_llm` 走 `TASK_REVIEW` + `LEDGER_REVIEW`、不联网、先剥 JSON 再用;降级次序直接读 `budget.DEGRADE_ORDER`(T3 简评 → T2 细节,单测断言 T1 与 `NEVER_DROPPED` 都不在其中);LLM 缺席时**机械判九项照出**、`llm_skip_reason` 如实落行。

**测试**:新增 **93 例**(`test_eval_exit_sim.py` 19 / `test_basket_review.py` 39 / `test_eval_metrics_placebo.py` 35)。`pytest tests/ -q`:当前树基线 **2749 过** → **2842 过 + 2 skip + 2 fail**(2 fail 仍是既有周日时间炸弹,同名同因,**零回归**)。

**端到端冒烟**(`scripts/smoke_basket_review.py`,真实历史日 D0=2026-06-18 → D+1=06-22,隔离库副本 + 真实 parquet 只读,全桩零真实 LLM):④ 扫描层 1286 行簇 / 16243 行相关 / 381 颗种子 → ⑤ 4 篮(角色对拍闸打响 5 次)→ ⑥ T1=1/T2=3、事务 1 落 4/5/4 → ⑦ 4 张卡 → ⑧ EOD `verified=1 / unclear=3` → ⑨ 复盘 4 篮、重跑**新增 0 / 已存在 4 且零差异**(每日一行幂等)→ 周度校准报告(含两条对照臂:真实 −2.57% vs 随机臂中位 −1.95%〔p10 −5.41% / p90 +2.89%,真实落在第 38 百分位〕vs 满仓持有 −1.63%,**样本 1 天 → 两条判断都只报样本数不报结论**)。**人工逐项核对一篮**(`光纤概念`,成员 000889/002281/002491):④ 三只全涨 → 同向率 100%、中位 +5.97%;⑧ 大盘 +1.78% → 超额中位 +4.19%(5.97−1.78 ✓);⑤ 卡上龙头 002491(`rs_rank=1`)+7.66% vs 其余中位 +4.84% → 差 +2.82% ✓;⑥ 三只涨停价 4.42 / 292.82 / 34.74 全取自 `card_frozen`、当日都没触板 → 3/3 可买 ✓;③ 002491 盘中 MFE +10.01% 但收 +7.66%(触板未封)→ 与 ⑥ 判「可买」自洽 ✓。

**六条如实登记(未改 Plan,如与规划意图不符请澄清)**

1. **`fill_and_score` 的「算不出」拆成两种**:触发了退出却没有 T+1 可撮合(`_sim_one` 记 `reason="end"`)→ `unfinished`;整段日历都没解出退出(价缺失 / 前向窗口太短)→ `unresolved`(`fill_code`)。**两者都不进收益均值**。Plan 只提了「排除不可成交的一字上涨」,这两类是判分层的必然产物,按「没有 ≠ 没看」拆开计数。
2. **安慰剂臂 A 的对照口径**:随机篮**没有卡**因而没有「最高追价」约束,故与臂 A 比较时真实臂也走 **`respect_max_chase=False`** 的那份数字,并在报告文案里显式标注;带上限的真实收益另在主指标表给。拿"带上限的真实臂"比"无上限的随机臂"是偷跑。
3. **合法域(臂 A 抽样池)的默认定义是 builder 定的**:Plan 只写「当日通过卫生线与安检的合法域」。落法 = 当日 `leader_structure_daily` 出现过的代码(④ 已预计算落表、在线只读,不触发全市场历史扫描)再过一遍 ⑤-b 的 `apply_member_hygiene`。解析器**可注入**(`domain_resolver`),换定义不动本模块;域取不到 → 当日臂 A 缺席并如实披露,⛔ 不退回"全市场随机"顶上。
4. **三个分档阈值是零审计背书的工程默认**(照 `verification_rules.py` 的登记体例写进模块头):`AUCTION_STRONG_GAP=0.02` / `AUCTION_WEAK_GAP=-0.02`(竞价落哪条分支剧本)与 `MIN_CONCLUSION_DAYS=10`(结论线)。**升级路径**:⑨-C 攒够样本后由策略线校准 → 走换包;在那之前不许自行改数。`PLACEBO_DRAWS=200` 是统计精度旋钮、明确**不进包**。
5. **`basket_review_daily` 刻意不提供任何 UPDATE 路径**:因此 `review_day()` 的次序是「先算完九项 → 再跑 LLM → 最后一次性落库」。代价:**第一次跑降级了,同日重跑补不上 LLM 解释**(会记 `existing`)。理由写在 store 模块头 —— 允许回写就说不清库里那行是哪次的结论,而 ⑨-C 恰恰要按 `degraded` 分层统计复盘覆盖率。
6. **`_sim_one` 的默认参数(`retrace=0.05` / `stop=0.05`)随搬迁原样保留**(改了就不是同一份实现),生产侧由 `fill_and_score` 把 `score_kw` 设成**必填关键字**从调用面堵死,并有单测断言;`neckline/eval/` 与 `basket_review.py` 的 AST 守门断言除那三行冻结签名外无 0.05/0.08 字面量。

**一处提请裁定 + 一个块外真洞(均未自行修)**

- 🔴 **④/⑤ 的种子顺序不确定,同一天重跑会产出不同篮子**(⑨ 冒烟三次连跑照出来的):`neckline/scan/seeds.py` 的 `limit_cluster` / `anomaly_cluster` 两类种子走 `frame.group_by(["cluster_key"])` 迭代,**polars `group_by` 默认不保证顺序**;而 ⑤ 只取前 `MAX_SEEDS_AGGREGATED=20` 颗(实测 278~381 颗种子截到 20),于是**哪 20 颗进聚合是随机的** → 同一 D0 同一库连跑三次得到三组完全不同的篮子(实证:`generate_seeds` 同进程内连调三次,第 3 颗起 `seed_key` 就不一样)。这直接违反 §铁律「跨进程 / 跨天要复现的分组」与「冻结件同日重跑幂等」的前提,也让 ⑨ 的归因样本不可复现。**修法应当只有一行**(种子列表落定顺序后再截断,如按 `seed_kind, seed_key` 排序或 `maintain_order=True`),但那是 ④ 的行为变更 + 需要 ④ 自己的守门单测,**⑨ 不越块自行改**。
- ⚠ **⑨ 的调用方 Plan 未定死**(同 ⑧ 的登记):本块只给函数 + `scripts/basket_review.py` CLI,**没有**擅自挂进 `daily_update.py`;⑭-A 的 16:35 链才是编排入口,且**必须排在 `run_eod_verification` 之后**(否则机械判第 ⑦ 项只看得到盘中暂态)。周度校准报告同理,只有 `scripts/weekly_calibration.py`,**不接报告管线**(⑭)。

---

### V2-⑩ · 持仓极简台账 + 计划继承 + 决策日志强制表单退役(@builder)

**目标**:买卖录入控制在数秒内;**系统自动记录,人只补充机器不知道的**。

**⑩-A 极简录入**
- 买入 = **票 + 价 + 量**(`buyDate` 可选、默认今天、校验不晚于今天且是交易日 —— 保留 v1.4 契约);其余**自动关联**:来源篮子 / Tier / 角色 / 时间 / 市场快照 → 写 `entry_snapshots`(冻结一行)。
- 卖出 = **价 + 量** + **可选**快捷标签(止损 / 逻辑证伪 / 板块转弱 / 达到参考区间 / 主动切换 / 临时决定)→ `positions.close_reason` 枚举扩(既有枚举保留,只加不改)。

**⑩-B 计划继承(`position_plans`)**:建仓时从 D0 篮子卡继承 —— 建仓观察区间 / 最高追价 / 离场参考区间 / 验证与失效条件 / 主要风险,落 `version=1`;用户可创建**新版本**(`version=2,3…`),**新版本不修改原始篮子卡**(单测锁死)。
- 计划价 vs 实际成交价明显偏离 → 提示「**原盈亏结构已变**」,**不质问、不阻断、不进任何判定**(纯展示位)。

**⑩-C 决策日志强制表单退役**
- `POST /decisions` 的**五必填**(为什么买 / 为什么这个入场价 / 论点标签 / 证伪条件 / 打法标签)与 `maxChasePct` 二选一强制校验(`api/app.py::_extract_max_chase_pct_or_400`)**全部下线**。
- `decision_log` 表**停写留档**:`_SCHEMA` 保留 DDL 并加注释「**v2.0.0 起停写**,历史行供归因只读」;`GET /decisions` 保留为只读归因入口;所有写入方删除(grep 守门断言零写入)。
- 用户的可选补充(题材切换 / 龙头重新激活 / 放量突破 / 弱转强 / 容量中军 / 消息催化 / 纯盘口判断 / **一句可选语音说明**)→ 落 `user_actions`(`kind='label'` / `'voice_note'`)。

**⑩-D `user_actions` 接线**:查看(`view`,客户端上报)/ 选择(`select`)/ 买卖(`buy`/`sell`,服务端自动落)/ 提醒(`alert`)/ 标签(`label`)五类。

**⑩-E 信息互通边界(蓝图 2.3 / 6.x)**:两模块可互读、可追加,**不得回头修改对方已冻结的历史信息** —— 选股日报可追加用户行为观察,**但不得因此修改原始 Tier 与篮子判断**(守门单测:持仓侧模块对 `baskets` / `basket_cards` / `tier_history` 零写入)。

**依赖**:①⑦。
**验收**:开仓只传三字段即成功且 `entry_snapshots` 有冻结行;计划继承版本化且原卡不变(单测);不传五必填 → **200 而非 400**;`decision_log` 零新增行(grep 守门);持仓侧对篮子表零写入(grep 守门);pytest 零回归。

**✅ V2-⑩ 完工记录(2026-08-03,@builder,纯本地代码 + 单测,零服务器 / 零部署)**:

新建 `neckline/positions_entry.py`(⑩-A/B/D 唯一编排入口,API `POST /positions`
/`POST /positions/{id}/close` 与 CLI `scripts/positions.py add`/`close` 共用同一份
逻辑,不各写一遍)。`record_buy()`:① 当日现役卡里查来源篮子(D0=`buy_date` 的上一
交易日,查 `basket_members` 反查 `baskets`+`basket_cards`,查不到/篮子有卡未就绪
两态分开如实标 `no_source_basket`/`card_not_ready`)——**全程只读 `baskets`/
`basket_members`/`basket_cards`,零写入**(AST 守门单测断言);② `entry_snapshots`
冻结一行(`buy_price`/`qty`/一次 best-effort 实时报价及涨幅/篮子来源与
tier/role/k4_tag/industry_lift/EOD 预计算板块强度〔单日点查 `industry_strength_
store`,不现算〕,每个子项独立 try/except、失败只警告落 `None`,核心两个写
INSERT 硬保证);③ `position_plans` 落 `version=1`(继承建仓区间/最高追价/离场
参考区间/验证与失效条件/主要风险,无来源或卡未就绪同样落一行、`available=False`
如实标原因,不省略);④ `user_actions` 自动落 `kind='buy'`。`evaluate_entry_
deviation()`:买入价落在卡上 `entry_zone` 之外 → 「原盈亏结构已变」提示(复用夹逼
闸给出的区间,不另造阈值),纯展示不阻断,无区间可比 → `None`(≠ 未偏离)。
`create_position_plan_version()` 领域函数已就绪(新版本承袭同一来源、原卡与 v1
逐字节不变,单测锁死),HTTP 入口按 ⑭-B 既定分工留给该块。`positions.close_reason`
枚举加四码(`SECTOR_WEAKENING`/`TARGET_ZONE_REACHED`/`ACTIVE_SWITCH`/`AD_HOC`,
既有五码原样不动)。`decision_log` 退役:`create_decision`/`link_decision`/
`cancel_decision`/`expire_decision`/`revise_decision`/`set_scenario_outcomes`
连同 `ScenarioIndexError` 物理删除(模块只剩 `get_decision`/`list_decisions` 只读
+ 枚举常量),`api/app.py` 对应四个写端点(`link`/`cancel`/`revise`/
`scenario-outcome`)一并删除、`_extract_max_chase_pct_or_400` 删除;`GET
/decisions`/`GET /decisions/{id}/track` 逐字节不变。`report/pending_track.py`
改用 `_already_completed()`(查 `decision_pending_track.MAX(d_offset)`)判断"该
决策是否已追踪到窗口终点",替代原先"落最后一行后翻 `decision_log.status=
expired`"——**追踪行为逐位不变**(含 overshoot 如实记录实际 offset 的既有行为),
唯一差异是 `status` 不再自动翻转(该表停写留档)。新守门单测(`tests/
test_decision_log.py::test_decision_log_table_has_zero_write_call_sites_in_
neckline_package` + `tests/test_positions_entry.py::test_positions_entry_
never_writes_to_basket_tables`)AST 扫描全仓 `neckline/`,零命中。

**两处如实登记的设计判断(Plan 未给出精确形状,已按最贴合上下文的解读实现,均
未改 Plan 原文)**:
1. **`POST /decisions` 保留路径但整体换血,不是删除**——依据三点交叉印证:
   ① Plan 原文用词是"全部下线"校验、"所有写入方删除",**未说端点本身删除**;
   ② ⑬-5「决策日志强制表单 → ⑩-C 已下线服务端;本块删客户端 `DecisionLogSheet.
   swift` 的必填分支」暗示客户端表单本体保留、只删必填分支,意味着它此后仍会
   POST 到这个 URL;③ ⑭-B「删除端点」清单(`/watchlist*`/`/breathing*`/
   `/settings/intel-boards`/`PUT /settings/llm`)**不含 `/decisions*`**。据此把
   `POST /decisions` 换血成蓝图 §2.2/§5.2「用户可选补充」入口——`code`/
   `positionId` 挂载点 + `labels`(七枚)+ `voiceNote`,落 `user_actions`
   (`kind='label'`/`'voice_note'`),不碰 `decision_log`;这正是验收条款「不传
   五必填 → 200 而非 400」的字面落点(全部字段可选,空提交合法)。响应体从
   `DecisionOut` 换成新 `DecisionNoteOut`(`{ok, recorded:[]}`),`link`/`cancel`/
   `revise`/`scenario-outcome` 四个端点因失去写入对象直接删除。**七枚标签码
   (`THEME_SHIFT`/`LEADER_REACTIVATE`/`VOLUME_BREAKOUT`/`WEAK_TO_STRONG`/
   `CORE_POSITION`/`NEWS_CATALYST`/`PURE_TAPE_READING`)是本次新拟**(蓝图给的是
   中文短语,未给码),连同四个新 `close_reason` 码一并提请 ⑭ 契约总装核对/裁定。
2. **`entry_snapshots` 的"机器可知一切"本轮范围收窄**:资金流(`compute_sector_
   moneyflow` 是 EOD 报告管线专用现算,买入热路径现算属于本项目明确记取的
   P0-23 反面教材)、竞价表现(`auction_snapshots` 依赖当天是否命中存拍窗口)、
   换手率与量比(归一化指标,需额外拉 `daily_basic` 流通股本/历史均量基准)
   **四项未采集**,`snapshot_json.not_captured` 字段如实列出、不假装齐全;
   已采集:一次 best-effort 实时报价(价/涨幅/绝对量/额)+ 篮子来源全量字段
   (含 K4 标签/行业 lift)+ EOD 预计算板块强度(单日点查,不现算)。

**测试**:新增 `tests/test_positions_entry.py`(22 例,覆盖来源查找三态/计划继承/
偏离提示/端到端冻结/新版本不改原卡/篮子表零写入守门/CLI 接线);`decision_log`
退役连带改造 6 个既有文件(`test_decision_log.py` 全量重写为只读层单测+退役守门、
`test_pending_track.py`/`test_api_decision_track.py`/`test_pipeline.py::
TestPendingTrackWiring` 的 fixture 从 `create_decision`/`link_decision` 改走新增
`tests.conftest.insert_decision_log_row`/`set_decision_status` 裸 SQL 夹具且
`STATUS_EXPIRED` 断言改 `STATUS_PENDING`、`test_api_decisions.py` 全量重写为
「可选补充入口」单测、`test_exec_hint.py`/`test_tscode_normalization.py` 的
fixture 同步改口径)。`python -m pytest tests/ -q`:**2836 passed + 2 skipped +
1 failed**(失败项 `test_review_reconcile.py::TestRollbackDoesNotWhitewashHistory::
test_rollback_shows_up_as_a_new_switch_in_the_current_week` 用真实
`datetime.now(CN_TZ)` 判"当周",本次施工窗口跨越 2026-08-02〔周日〕→08-03〔周一〕
ISO 周边界、与 ⑩ 改动零关联——已用 `git worktree` 拉一份不含本块任何改动的
干净 HEAD 副本复现同一断言失败,证实是既有周边界炸弹而非新回归,不修不新增)。
**Plan 未覆盖之处均已如实登记于上方两点,如与规划意图不符请澄清**。下一步:
V2-⑪(🔴 @builder-pro,碰推送与哨兵)。

---

### V2-⑪ · 监控 80/15/5 + 通知三级 + 自然语言临时提醒(🔴 @builder-pro,碰推送与哨兵)✅ 完工(2026-08-03)

**⑪-A 监控注意力 80/15/5**
- 现役纪律分支**原样保留**:-5% 止损、回落止盈、时间退出(含 `suspended_hold` 第五态)、证伪四哨兵、熔断。
- **新增四监测**:① 同篮子成员集体转弱;② 板块 ETF / 指数承接消失;③ 持仓从跟随板块转为独立弱势;④ 大盘突变。**板块监控只服务于解释持仓,不扫描无关板块。**
  - ⚠ **② 实为「板块基准指数」不是 ETF**(无数据源,承 ⑧-A / §七 P4-35),文案里必须明写是指数。
  - ⚠ **④ 大盘突变依赖宽基指数恰好在关注池里**(池里的指数由持仓 / 成员**所在板块**反查),**纯创业板持仓的日子可能一支宽基都没有** → 如实标 `no_broad_index_quote` **并不判**(⛔ 不拿 0 或别的指数顶替)。**⛔ 本块不许为此改关注池组成** —— 无条件塞两支宽基会挤占 `breadth_cap` 配额(极端涨停日会顶掉 2 只昨日涨停)= **改变退潮哨兵的宽度样本 = 碰纪律判定输入**,那是 ⑧-A 的组成变更,要动得单独立项。⑪ builder 没擅自动,**是对的**。
- **同篮子多持仓按同题材合并敞口**展示与提醒(蓝图 6.2:不得视为完全分散的两笔仓位)。

**⑪-B 通知三级(重构现有六类白名单)**
- 三个 APNs category:`NKIMMEDIATE`(立即)/ `NKIMPORTANT`(重要不紧急)/ `NKDIGEST`(盘后汇总)(**D5,可推翻**)。
- 每条事件带 `kind`(`report_ready` / `retreat` / `precall` / `d5exit` / `circuit` / `holding_alert` / `custom_alert` / ⑪-A 四监测 / **`stop_approach` · `take_profit` · `sector_dive`** …),**开关按 `kind` 配**(设置屏),**不按 category 配**(按 category 配会连坐 —— v1.3 拆 `HOLDINGALERT` 的教训)。**权威 kind 清单 = `neckline/notify_kinds.py`(唯一源),本行只是举例。**
- **✅ `stop_approach` / `take_profit` / `sector_dive` 三条开「立即」级 `NKIMMEDIATE`(2026-08-03 用户拍板)**。⑪ 完工记录登记 ② 提请、⑪ builder 当时按「新增 kind 须用户拍板」没擅自开,**流程是对的;现在用户拍了,开**。
  - **依据 = 蓝图 5.5 权威兑现**:该节逐字把「**逼近或触发止损**」「快速跳水」列进**立即通知**;⑪-B 的分级归属那一行本来就照抄了它,只是当时没有对应 kind 落地 —— 这三条是**把已经写在 Plan 里的东西补齐**,不是新加需求。
  - **V1「只进看板」的拍板(§2.4,2026-07-20)自此被 V2 三级体系取代**:当年只留两类 APNs 是因为**没有分级、没有 per-kind 开关**,多推一类就是全员挨轰;V2 有了三级 × per-kind 开关,**「重要的事推、用户嫌吵可单独关」才成立**。⚠ 取代的**只是这三条的通道归属**,V1 §2.4 那句「买点 / 证伪 / 普通警示只进看板」**其余部分继续有效**(见下方「两套通道」条)。
  - **每条独立开关,用户可单独关**(不与其他 kind 连坐 —— 这正是 per-kind 开关存在的意义)。
  - **⚠ 与 `basket_falsified` 那条裁决的对照(写在这里防止有人拿这次开闸当先例)**:两者**不是同一类事,不构成先例** —— 这三条是**持仓事件**,直接指向用户手里的仓、且蓝图**逐字点名**属立即级,只欠一次用户拍板;`basket_falsified` 是**选股线的驱动假设被证伪**,**不指向任何持仓动作**,且它的「不进推送」写在 **CLAUDE.md 坑条 + ⑦-b/⑧-C2 红线**里 —— **要开它得先改那三处红线,不是"照这次的例再拍一次板"就行**。
  - ⚠ **⑬-6 执行时必须复核 `take_profit` 的触发源还在**:V1 这条读的是候选老四件套的 `target` / `decision_log.target_price`,而**四件套已定退役(⑬-6)、`decision_log` 已停写(⑩-C)** → 若不改指向,这条 kind 会变成**永不触发的死开关**(设置屏挂着一个拨了没用的开关,正是上面 `basket_falsified` 条反对的那种撒谎式 UI)。V2 的正确指向 = **篮子卡的离场参考区间**(`members[].exit_reference`)或 **⑩ `position_plans` 继承的离场参考**。**⑬-6 落地时逐条确认,别让它静默变死。**
  - ⚠ **语义红线照旧**:`take_profit` 的推送文案**不许写成「该卖了 / 建议止盈」** —— 离场参考是**参考**,回落止盈才是**纪律**;文案只说「触达了你计划里的离场参考区间」,终选权在用户。
  - ⚠ **去重粒度决定这三条是不是噪声(builder 必须落实)**:`stop_approach` 在震荡时**每一拍都可能命中**,V1 只进看板所以无所谓,升级成锁屏推送后必须真的只推一次。**判据:`sentinel_events` 的 `event_key` 必须保证「当日每票每 kind 只推一次」** —— ⛔ `event_key` 里不许带价格 / 时间戳之类每拍都变的量(带了就等于没去重)。三条都照此。
- **⛔ `basket_falsified` 不是也不会是一个 kind(2026-08-03 planner 裁决两份权威的冲突,⑪ 完工记录登记 ① 提请)**。本行原例举里曾有它,**那是 planner 立项时随手列的示例、不是逐条论证过的**;⑪ builder 取了不推的保守方向,**是对的,以此为准**。
  - **权威层级**:CLAUDE.md 原始坑条「**篮子 `falsified` ≠ 持仓该走** …… ⛔ 篮子验证不接任何持仓动作、**不进推送**(守门单测扫 import)」+ ⑦-b / ⑧-C2 的语义红线 **高于**本节的一句 kind 例举。已有守门单测 `test_sentinel_basket_verify.py::test_verification_never_touches_positions_or_push_channels` 锁死。
  - **实质理由(不只是"红线说了算")**:① 篮子 `falsified` 说的是「**这个驱动假设不成立了**」,它**不指向任何用户动作** —— 用户可能压根没买这篮里的票,推给他是噪声;② 买了的人收到这条,会读成「**系统在暗示我卖**」,而该不该走是**持仓纪律**(止损 / 回落止盈 / 时间退出)的事、由持仓哨兵管 —— 这正是那条坑要防的误读;③ V2 的注意力分配是 **80/15/5(持仓 80%)**,选股线的复盘信息不该抢持仓的推送带宽。
  - **「不推 APNs」≠「用户看不到」**:验证四态本来就有两个露出位 —— **盘中看板 / ⑮ 的验证状态实时角标** 与 **⑭-A 报告的篮子节**。⛔ 别用「用户会不知道」当理由把它接回推送。
  - **⛔ 也不留一个零调用点的空 kind**:那会在设置屏给用户一个**拨了没用的开关** = 撒谎式 UI。
  - **⚠ 日后若真要开,正确路径不是「照例举接回去」**:必须 ① 用户拍板新增 kind(本节自己那条纪律)**且** ② **同步改 ⑦-b / ⑧-C2 的红线与 CLAUDE.md 那条坑,三处一起改** —— 只改一处就是绕过红线。
- 分级归属(蓝图 5.5):立即 = 自定义价格条件 / 逼近或触发止损 / 快速跳水 / 涨跌停打开 / 重大公告或交易风险;重要不紧急 = 放量异动 / 持仓明显弱于篮子 / 板块核心转弱 / 大盘快速变化;盘后汇总 = 普通波动 / 轻微技术变化 / 一般板块分歧。
- 冷却、去重、防重**沿用 `sentinel_events` 台账**(`UNIQUE(trade_date, sentinel, ts_code, event_key)`)。
- **新 kind 的默认开关状态 = 开,且零迁移(2026-08-03 核过播种机制,自洽)**:
  - **机制核对结论**:`db.py::_seed_push_kinds` **只播种 V1 那六个 kind、且只碰 `push_kinds IS NULL` 的行**;读侧 `settings_store._decode_push_kinds` 会把 **`ALL_KINDS` 全部补齐、缺键取默认开**。→ 三条新 kind 在**全新库**与**已播种过的老库**上**都自动是开**,**⛔ 不需要任何回填 / 迁移 / 改播种函数**。
  - ⚠ **但别照抄播种函数 docstring 里的那句理由**:它写的是「新 kind……用户从未表达过意见,默认开」——那句话是给 `custom_alert` / ⑪-A 四监测那种**全新能力**写的。**这三条不是新能力,是既有看板事件升级通道**,用户此前从未同意过「它们上锁屏」。**所以默认开的依据是「2026-08-03 用户拍板」这一条,不是「缺键取默认开」那条通用规则** —— 规则只是恰好让实现零成本。两件事别混。
  - ⚠ **「用户关过的不许被悄悄打开」这条底线不受影响**:这三条在老库里**从来没有过对应的开关列**,不存在被覆盖的用户意愿。
  - **⑰ 换包时应让用户知道这三条新开了**(它们改变的是锁屏行为,不是 App 内的显示)—— 放在换包说明里一句话即可,**不必做引导页**。
- 文案只回答三件事:**发生了什么、触碰了哪条计划、要不要打开 APP**;详细解释点开再给。
- ⚠ `neckline/api/notify.py::__all__` 的**「六类」结构守门单测必须改成「三级 × N kind」并重新锁死**(架构稿未提这条,这里点名);**新增 kind 仍须用户拍板**这条纪律保留。
- **两套通道默认不混,升级需用户拍板(2026-08-03 修订措辞,与上面三条 kind 的开闸对齐,两段不打架)**:
  - **默认态**:既有四哨兵的 `sentinel/channels.py`(Bark / 日志,看板事件)与 `notify.push_event`(APNs,按 kind 配开关)是**两条通道、两个注意力预算**。**⛔ builder 不许"顺手"把某个看板事件接进 kind 白名单** —— 这叫**违规**(绕过「新增 kind 须用户拍板」)。
  - **合法升级通路(不是例外,是既有纪律本身)**:某个看板事件**该不该升级成锁屏推送,由用户拍板** —— 拍了就是合法,照常加 kind、加开关、加去重。**`stop_approach` / `take_profit` / `sector_dive` 三条走的正是这条路**(2026-08-03 用户拍板),**不是打破了「不混」,而是用对了「须用户拍板」这道闸**。
  - **两者的区别只有一个字:谁批的。** builder 自己接 = 违规;用户拍板 = 合法。⛔ 别把「⛔ 不许顺手接进」读成「永远不许接」。
  - **仍然只进看板、未获拍板的**:买点触发 / 证伪剔除 / K4 普通警示等(V1 §2.4 口径,V2 继续有效)。要动同样走用户拍板。

**⑪-C 自然语言临时提醒(`custom_alerts`)**
- **LLM 只做解析**(`TASK_NL_ALERT`):自然语言 → 结构化规则 → **展示确认卡**(标的 / 触发条件与方向 / 生效时间 / 通知次数或冷却 / 到期时间 / **行情延迟或数据中断披露为必选项** / **只通知不自动交易**),用户确认后才落库。
- **执行归确定性哨兵**(`sentinel/` 内新增 `custom.py`,读 `rule_json`);**LLM 不参与执行**。
- 首批条件集(蓝图 5.6):**价格 / 涨跌幅 / 相对成本 / 相对日内高点 / 量能 / 大盘或篮子条件的组合**。
- 安全:相同提醒去重;默认首次命中后不重复轰炸;**收盘自动失效**(除非 `persist=1`);用户可随时用自然语言查询、修改、取消。
- **LLM 不可用时**:入口降级为「手填结构化表单」,**不静默失败**。

#### ⑪-D · `take_profit` 触发源的机械 sanity 闸(2026-08-03 planner 裁定,V2 review 判定线 🟡-3 收口;**追加施工项**)

> **背景与自我纠错**:2026-08-03 我在 ⑪-B 开这条 kind 时写了「V2 的正确指向 = 篮子卡的离场参考区间或 `position_plans` 继承的离场参考」,**却没有对它执行我在同一段里给 `basket_falsified` 立的那条标准**(「要开得三处红线一起改」)。结果是 LLM 给的 `exit_low/exit_high`(⑦ 的 `clamp_exit_reference` **只做格式校验**)经开仓自动继承,直接成了一条**立即级锁屏推送**的触发阈值 —— 极端例:LLM 给 `exit_low=0.01`,买入后第一拍即推。**这是 plan 的缺口,不是 builder 违规**(reviewer 定性正确)。

**⑪-D-A 定性:真正要治的是哪一层(想清楚再动手)**
- **不是纪律漂移**:它不改止损 / 回落止盈 / 时间退出 / 仓位 / 熔断,不产生任何持仓动作 —— §2.0 最硬的那半条**没有被碰**。
- **也不主要是「虚假权威」**:文案已锁死非指令 + per-kind 开关 + 用户拍板。
- **真正的洞是第三层:一个没有下限的 LLM 数字驱动立即级推送** → 荒谬推送 → **用户对「立即级」这个通道失信**。立即级通道的价值全在「响了就值得看」,喂它一条噪声比不推更糟。**所以修法是给数值加机械闸,不是收回 kind。**

**⑪-D-B 两道机械 sanity 闸(语义驱动,⛔ 零发明阈值)**
1. **卡生成时(⑦ `selection/basket_card.py::clamp_exit_reference`)**:要求 **`exit_low > D0 close`**(且 `exit_high ≥ exit_low`、两者为正有限数)。**这不是发明阈值,是定义** —— 离场参考按定义是「**本轮上涨的压力位**」,压力位在现价之上;`exit_low ≤ D0 close` 的东西根本不是离场参考。不满足 → 落既有夹逼态家族的新 reason **`rejected_not_above_close`**,该项**不落卡**(与 `rejected_malformed` 同等处置)。
   - ⚠ **仍然不夹涨跌停**(⑦ 原决定不变且是对的:压力位可能几个交易日后才到)。这道闸只管**下界的语义合法性**。
   - ⚠ **不加上界闸**:`exit_high` 荒谬地高只会导致**永不触发**(无假推送、无伤害),**⛔ 不要为它发明一个上限**。
2. **开仓继承时(⑩ `positions_entry.py`)**:若继承下来的 `exit_low ≤ 实际成交价` → **该票的 `take_profit` kind 不武装**,并在持仓计划卡如实标「离场参考低于你的成本,本票不做触达提醒」。
   - **只是不武装那一条推送** —— ⛔ 不改计划内容、不阻断开仓、不影响任何其他 kind。
   - 理由:`exit_low ≤ 成本` 时「触达离场参考」要么立刻响、要么在亏损位响,两种都是噪声。

**⑪-D-C 记名豁免(与 §2.8-C-3 逐字对应,两处必须一致)**
- **范围**:仅 `take_profit` 这**一个** kind、仅**触达告知**这**一种**用途;**拍板人与日期:用户 2026-08-03**。
- **四条前提**见 §2.8-C-3(不改纪律判定 / 已过机械闸 / 纯告知文案 / 用户可关),**缺一即豁免失效**。
- ⛔ **不外延到任何其他参考件**。

**⑪-D-D ⑮ 应做(不阻塞本裁定)**:持仓卡的计划区**显示离场参考并带一个 per-position「不提醒」开关**。理由:reviewer 指出开仓继承**无用户确认环节**,而 V2 的立项主题恰恰是**减摩擦**(⑩ 表单退役)—— 加一道前置确认与之相悖;**per-position 关闭**是代价更小的答案(全局关 `take_profit` 会连坐所有持仓,用户真正想要的是「这只票的这个数不靠谱,别烦我」)。**红线前提是 ⑪-D-B 两道闸,不是这个开关**,故列为 ⑮ 应做项。

**⑪-D 验收**:`exit_low ≤ D0 close` → 卡上该项不落 + reason 精确(单测);`exit_low` 荒谬小(0.01)→ 卡生成期就被拦,**根本走不到推送**(单测,正是 reviewer 那个极端例的回归);`exit_low ≤ 成交价` → 该票 kind 不武装 + 计划卡如实标,**且其他 kind 照常武装**(单测两路);⛔ **不夹涨跌停、不加上界**(守门单测:压力位高于次日涨停仍正常落卡);豁免四条前提在 §2.8-C-3 与 ⑪-D-C **逐字一致**(人工核一次并在完工记录写明);pytest 零回归。

**✅ V2-⑪-D 完工记录(2026-08-03,@builder-pro,纯本地代码 + 单测,零服务器 / 零部署)**

**闸①(卡生成)落 `selection/basket_card.py::clamp_exit_reference`**:签名由 `(raw)` 改成 `(raw, close)`,`close` **必填位置参数、刻意不给默认值** —— 红线闸不留"调用方忘了传就静默关闸"的口子(与 ⑧-E `MemberObservation.pre_close` 那种"老调用点安全降级"的可选锚**刻意相反**:那是加检测,这是加闸;守门单测用 `inspect.signature` 锁死无默认值)。判定序与 `clamp_entry_zone` 同构、**五态**:`absent`(没给)→ `rejected_malformed`(数字非法 / 自相矛盾)→ `rejected_no_close`(数字合法但 D0 收盘算不出,**无从核对**)→ `rejected_not_above_close`(核对了,不高于收盘)→ `ok`。容差复用 `verification_rules.EPS`(不另立一份;**恰好等于收盘 → 拦**,压力位不能就是现价本身)。⚠ **`rejected_no_close` 是 plan 两态之外 builder 补的第三态**,如实登记:项目一贯「『没有』与『不满足』分开落码」(同 ⑧-E `anchor_unconfirmed` 先例),处置与 rejected 家族相同(不落卡)。⛔ **仍不夹涨跌停、仍不加上界**,两条各有一个守门单测(压力位 15.0 高于次日涨停 11.0 仍正常落卡;`exit_high=9,999,999` 仍 `ok`)。顺带把 `CARD_SYSTEM_PROMPT` 里那条硬约束补上「`exit_low` 必须严格高于资料给出的今日收盘价」(prompt 里本来就逐票给了收盘价)—— **prompt 是背带、闸才是安全带**,不是拿它替代闸(v1.5.1 `_parse_verdict` 那条教训)。

**闸②(开仓武装)落 `positions_entry.py`**:纯函数 `evaluate_exit_reference_arming(exit_reference, buy_price, *, muted)` → `(armed, reason)`,判定序**刻意如此**:①`user_muted`(用户意图优先,⛔ 不因为"数字其实挺合理"就替用户重新打开)→ ②`no_exit_reference`(压根没有,与"不合格"分开落码)→ ③`below_entry_price`(`exit_low ≤ 成交价`,含等值边界)→ ④武装。`build_inherited_plan(source, *, buy_price)` 的 `buy_price` 同样**必填**;`plan_json` 恒带四个键 `exit_reference_armed` / `..._armed_reason` / `..._armed_note` / `..._muted`(三条空计划路径也带,**不搞"缺键即默认"**)。文案单一源 `exit_reference_arm_note()`,`below_entry_price` 的文案就是 plan 原文「离场参考低于你的成本,本票不做触达提醒」。⛔ **只是不武装**:单测锁死「不武装那笔仓的 `exit_reference` / `entry_zone` / `risks` 正文逐字不变、开仓照常成功、同一拍 `stop_approach` 照常推 APNs 而 `take_profit` 不推」。

**读侧 fail-closed**:`sentinel/engine.py::_load_exit_references` 只认 `plan_json.exit_reference_armed is True`,**缺键 = 不武装**(⛔ 不是"老行按老行为放行")—— §2.8-C-3 前提② 是「该数值**已过**机械闸」,一份没有武装位的计划就是**没过闸**,不是"过了但没写"。生产 `position_plans` 当前 0 行,无迁移代价。

**⑮ 的读写点已留好(开关本身不做,⑪-D-D)**:`exit_reference_muted` 是**用户意图位**、`exit_reference_armed` 是**派生态**,两者分开存;写入点 `set_exit_reference_muted(position_id, muted)` —— 在最新计划之上**追加一个新版本**(`position_plans` 是版本化只增表,⛔ 不就地改历史行),⚠ 只翻静音位、**不动计划正文任何一项**(单测锁死)。`create_position_plan_version` 顺带补上一条:新版本的武装态由它**重算**(拿这笔仓的真实成交价重过闸②)、静音位**承袭上一版**除非本次显式给键 —— 否则"写个新版本"就成了绕开红线闸的后门(单测两路)。

**豁免四条前提逐字核对(⑪-D 验收要求"人工核一次并写明")**:§2.8-C-3 与 §五 ⑪-D-C 两处**已逐字比对一致** —— ①不改变任何纪律判定(止损 / 回落止盈 / 时间退出 / 仓位 / 熔断一字不动,不产生任何持仓动作);②该数值已过机械 sanity 闸(⑪-D 的两道),未过 → 该票此 kind 不武装;③纯告知型文案,禁指令词,必须点明「这是你计划里的参考位、不是止盈线,纪律仍是回落止盈」;④用户可关(per-kind 开关)。四条在代码里各有落点:①`holding.check_exit_reference_reached` 不产生任何持仓动作、旁路 E 不进 channels;②本块两道闸;④`notify_kinds` 的 per-kind 开关(⑮ 再加 per-position)。**⚠ ③ 核对时发现真缺一半、已就地补齐**:原文案只说到「仅供参考,不是止盈信号,是否离场由您判断」,**没有点明「这是你计划里的参考位」与「纪律仍是回落止盈」两句**,按「缺一即豁免失效」这条 kind 本不该被允许推;现改为「…已触达来源篮子的离场参考区间[x, y]——**这是你计划里的参考位、不是止盈信号,纪律仍是回落止盈**;是否离场由您判断」,并把这条前提锁成机器断言(`TestExemptionPremiseThree`),别再让它因为一次措辞润色悄悄失效。既有 `test_sentinel_holding.py` 那条「禁指令词 + 『止盈信号』只能以否定句出现」的断言**一字未改、仍绿**。

**测试**:新增 `tests/test_exit_reference_gates.py` 22 例(豁免前提③ 文案守门一条 + 闸① 十条:低于收盘 / 等值边界 / 容差内"高一点点"仍拦 / `exit_low=0.01` 极端例回归 / 缺收盘价是独立码 / malformed 优先级 / absent 仍是 absent / 不夹涨跌停守门 / 不加上界守门 / 签名无默认值守门;闸② 十一条:纯函数五路 + 端到端六路〔武装 / 不武装但计划完好 / 老行 fail-closed / 新版本重过闸 + 静音承袭 + 取消静音复武装 / 静音不动正文 / 其他 kind 不受影响〕)。既有 `tests/test_positions_entry.py` 改 4 处**调用签名**与 1 处整字典相等断言(改成"正文原样 + 武装态如实",因为新版本现在会补写武装态四件套);`tests/test_sentinel_tick_v2_bypass.py` 的 `_seed_position_plan` 夹具改成**调生产那支纯函数**算武装态(夹具与生产语义漂移会让"绿的测试"变成假绿),**断言逐字未动**。

**依赖**:⑧⑩。
**验收**:三级 category + kind 开关往返(单测 + 契约);NL 解析 golden 单测(六类条件各一,含一条组合条件);哨兵执行单测(命中一次即冷却、收盘失效、`persist=1` 跨日存活);LLM 不可用 → 降级为手填(单测);**「永不自动交易」grep 守门**(`custom.py` 零下单调用);哨兵既有纪律单测全绿不改断言;pytest 零回归。

**✅ V2-⑪ 完工记录(2026-08-03,@builder-pro,纯本地代码 + 单测,零服务器 / 零部署)**

**新增五个模块 + 一份冒烟**:`neckline/notify_kinds.py`(三级 × N kind 的**唯一源**,
零项目内 import 的叶子模块)、`neckline/custom_alerts.py`(规则白名单 + 确认卡 + 台账)、
`neckline/sentinel/attention.py`(⑪-A 四监测 + 合并敞口)、`neckline/sentinel/custom.py`
(⑪-C 确定性执行器)、`neckline/llm/nl_alert.py`(⑪-C 解析层)、
`scripts/smoke_attention_alerts.py`(两段冒烟,全绿)。

**⑪-A 四监测落点**(全部纯规则,输入 = D0 冻结件 + 本拍已拉到的报价,**零额外网络**):
① `check_basket_peers_weak` 同篮成员集体转弱(过半成员 ≤ −3% **且** 均值 ≤ −2%,有效
样本 < 2 只**不判**);② `check_sector_bid_fade` 板块承接消失(指数 ≤ −1.0% **且** 自
日内高点回落 ≥ 0.8%,两条 AND 才是「冲高之后没人接」;文案里**明写是板块基准指数、
不是 ETF**,承 ⑧-A / P4-35);③ `check_holding_decoupled` 持仓转独立弱势(自己 ≤ −3%
**且** 参照 ≥ −1%〔环境没坏〕**且** 落后 ≥ 3 个百分点;参照优先同篮均值、退板块基准
指数,**缺参照就不判、⛔ 不拿 0 当参照**);④ `check_market_shock` 大盘突变(宽基
≤ −2%,或自高点回落 ≥ 1.5% 且已翻绿 ≤ −0.5%;**空仓时不判**,蓝图口径是「影响全部
持仓」)。**13 个阈值全部登记在 `ATTENTION_DEFAULTS`**(⑦-b 体例:工程默认、零回测
背书、**不进包也不进章程**、改数要同步改守门单测)。

**同篮合并敞口**(蓝图 6.2):`compute_merged_exposure()` 按**来源篮子 `basket_id`**
归并(⑩ 的 `entry_snapshots.basket_id` 优先、缺行退 `position_plans.source_basket_id`,
两条来源在 `link_source` 里如实分开),给成本 / 市值 / 占 `Settings.total_capital` 比例;
**≥2 个不同标的**才标 `theme_concentration=True` 并把提示句挂进四监测的通知正文。

**⑪-B 通知三级迁移映射(六类 → 三级 × 11 kind)**:

| kind | V1 出处 | 级别 / category | 依据 |
|---|---|---|---|
| `report_ready` | 第①类 REPORT | 盘后汇总 `NKDIGEST` | 蓝图 5.5「盘后汇总」 |
| `retreat` | 第②类 RETREAT | 立即 `NKIMMEDIATE` | 重大交易风险(全天禁开新仓) |
| `precall` | 第③类 PRECALL | 重要不紧急 `NKIMPORTANT` | 汇总件,开盘前核对但非逐笔紧急 |
| `d5exit` | 第④类 D5EXIT | 重要不紧急 | 当日按计划离场,非秒级 |
| `circuit` | 第⑤类 CIRCUIT | 立即 | 重大交易风险 |
| `holding_alert` | 第⑥类 HOLDINGALERT | 重要不紧急 | 三条强证据全属蓝图「放量异动」一族 |
| `custom_alert` | ⑪-C 新增 | 立即 | 蓝图逐字「用户自定义价格条件」 |
| `basket_peers_weak` / `sector_bid_fade` / `holding_decoupled` / `market_shock` | ⑪-A 新增 | 重要不紧急 | 蓝图「持仓明显弱于篮子 / 板块核心转弱 / 大盘快速变化」 |

`api/notify.py` **扇出路径收敛成一条** `push_event(kind, …)`(未登记 kind 直接抛
`ValueError`,白名单从「只暴露六个函数」升级成「只有一条扇出路径且它只吃
`ALL_KINDS`」;AST 守门断言 `_fanout` 只被 `push_event` 调用)。`push/apns.py` 的六个
V1 category 常量**已删**,换三个(D2=A 路一次性换血)。开关落点从六个 bool 列换成
`app_settings.push_kinds` JSON(`db.py::_seed_push_kinds` 幂等**一次性播种**老库取值
——⛔ 用户关过的推送不许因为改版被悄悄打开;六列**停写留档不 DROP**)。

**⑪-C NL 提醒链路**:`POST /alerts/parse`(桩/真 LLM → `json_block` 剥 JSON →
`normalize_rule` 白名单 → **七项确认卡** + `draft`,**不写库**)→ 用户确认 →
`POST /alerts`(落库前**再过一遍白名单**)→ `sentinel/custom.py` 逐拍三值判定 →
`engine.run_tick` 旁路推送 + 落 `sentinel_events` + `mark_fired`。首批条件集 7 个 metric
(价格 / 涨跌幅 / 相对成本 / 相对日内高点 / 量能 / 指数 / 同篮转弱占比,后两个即蓝图
说的「大盘或篮子」),`logic ∈ {all, any}`,最多 5 条。**安全七条逐条落地**:相同提醒
去重(`find_duplicate` 比规范化文本,措辞不同不算两条)→ 409;默认 `max_fires=1`;
`persist=0` 止于**创建当日 15:00(北京时间)**;行情延迟披露是确认卡**必选项**且命中
推送里再说一遍;「只通知不自动交易」固定尾巴;NL 查询 / 修改 / 取消三种意图都能解析
(取消 = 改状态留痕,**不物理删行**);LLM 不可用 → `degraded=true` + **手填表单字段表**
(由白名单常量生成),端点**恒 200**。

**如实登记的设计判断(9 处;Plan 未给精确形状或两处权威冲突,均按最保守解读实现,
⛔ 未改 Plan 原文,如与规划意图不符请裁定)**:

1. 🔴 **`basket_falsified` kind 未登记(两份权威正面冲突,取保守方向)**:⑪-B 的 kind
   例举里有它,但 ⑦-b / ⑧-C2 的语义红线写死「篮子 `falsified` ⛔ 不触发任何交易动作、
   **不进推送**、不改任何持仓判定」,且 `tests/test_sentinel_basket_verify.py::
   test_verification_never_touches_positions_or_push_channels` 已把它锁成守门单测。
   **本块选择不推**(少推一条可逆,越红线不可逆),也**不登记一个零调用点的空 kind**
   (那会在设置屏给用户一个拨了没用的开关)。**要接的话是一行白名单 + 一个调用点**,
   请裁定。
2. 🔴 **`stop_approach` / `take_profit` / `sector_dive` 未开 APNs kind**:蓝图 5.5「立即
   通知」确实点名「逼近或触发止损」,但这三条是持仓哨兵**既有的看板事件**(一直走
   `sentinel/channels.py` 的 Bark/日志通道,从没进过 APNs),⑪-B 的 kind 例举里也没有
   它们。按 ⑪-B 自己那条「**新增 kind 仍须用户拍板**」纪律,本块不擅自给用户多开一路
   推送。**这是目前「立即」级里最该有却没有的一条**,请裁定。
3. **关注池组成一行未动**(⑧-A 不碰):大盘突变依赖两支宽基指数**恰好**在池里(池里的
   指数由 `_related_index_codes` 按持仓/成员**所在板块**反查),纯创业板持仓的日子可能
   一支都没有 → 如实标 `no_broad_index_quote` 并不判。**无条件塞两支宽基会挤占
   `breadth_cap` 配额**(极端涨停日会顶掉 2 只昨日涨停)→ 改变退潮哨兵的宽度样本 =
   碰纪律判定输入,属 ⑧-A 组成变更,**不擅自动**。
4. **同篮成员行情覆盖随持仓变老而下降**:关注池装的是**今天**的 T1/T2 成员,而 D+3
   持仓的来源篮是三天前的冻结件。买入当天覆盖最好,越老越差。**不为此额外拉价**(旁路
   不给主循环加网络),而是报 `sample_n`/`coverage`、不足即不判。
5. **新 kind 只走 APNs,不进 `channels`**:既有四哨兵的 Bark/日志通道**一行未动**,
   ⑪ 的两条旁路只经 `notify.push_event`(按 kind 配),两套通道不混。
6. **kind 关闭时 custom alert 仍记 fire**:台账照落、`fired_count` 照加(看板要看得见
   这件事发生过),只是不推送。反向做法(关了就不消耗次数)会造出「隐形上膛」的提醒,
   用户开回开关时会被一串陈年命中砸中。**这是取舍,不是疏漏。**
7. **`/alerts` 五个端点在本块落地**(`GET|POST|PUT|DELETE /alerts` + `POST /alerts/parse`):
   ⑭-B 的新端点清单里有同名五个,**⑭ 只需对拍不必重建**。`PUT /settings/push` 的契约
   同时换血成 kind 清单(`PushSettingsOut.kinds[]` 带 `level`/`label`);**客户端
   `PushSettings` DTO 与 `PushManager` 的 category 注册已随之过时,归 ⑮ 换**(D2=A 路,
   老 App 打老机不受影响)。
8. **用户看到的条件描述 = 哨兵执行的判据**:`describe_rule()` 由结构化规则生成,**不用
   LLM 自由文本**(§2.8-C);`nl_text` 只留痕,`sentinel/custom.py` 判定路径一个字不看
   (AST 守门断言判定函数不读 `nl_text`)。
9. **合并敞口按 `basket_id` 归并,不按题材文本**:篮子是冻结件、可对账;题材是自由中文,
   拿它当归并键迟早把两个写法不同的同义题材算成两份分散仓位。`driver` 只进展示文案。

**测试**:新增 7 个文件 **164 例**(`test_notify_kinds.py` 10 / `test_sentinel_attention.py`
31 / `test_custom_alerts.py` 40 / `test_sentinel_custom.py` 29 / `test_nl_alert.py` 26 /
`test_api_alerts.py` 18 / `test_sentinel_tick_v2_bypass.py` 10);改造 3 个既有文件、另净增
12 例(合计 **+176**)——`test_notify.py` 六类断言 → 三级 × kind 并重新锁死〔⑪-B 点名
要求〕、`test_apns.py` category 换三级、`test_api_settings.py` push 契约换 kind 清单,**行为断言逐条保留**
(文案 / 两档 D5 / 熔断前缀 / 部分失败计数一字未改语义)。`python -m pytest tests/ -q`:
基线 2836 passed + 2 skipped + 1 failed(周日 ISO 边界炸弹,**今日周一已自愈**)→
**3012 passed + 2 skipped + 0 failed**,净增 176 例、零回归。**哨兵既有纪律单测一条断言
未改**;新增专门的红线单测:旁路(四监测 / NL 提醒 / 措辞层)任一抛异常,持仓哨兵照常
判、照常推(`TestBypassFailuresNeverTouchDiscipline`)。

**冒烟**(`python scripts/smoke_attention_alerts.py`,一次性临时库、真实 `data/neckline.db`
零读写、零真实 LLM / 零真实 APNs):A 段四监测**各至少一条触发路径**全部命中并打印命中
文案 + 原始指标 + 合并敞口(「AI 算力」2 只持仓合计占总仓 12.5%);B 段 NL 提醒**从解析
到命中通知端到端**——桩 LLM 出组合规则 → 七项确认卡逐项打勾 → 去重命中 → 阴性对照
(大盘没跌够 → `not_met`)→ 命中一拍 → 再跑一拍 `max_fires_reached` → 收盘翻 `expired`
→ LLM 不可用降级出手填表单。**全绿。**

**🟡 块外发现(未修,已挂账)**:`tests/test_scan_cluster.py::test_bulk_vs_day_by_day_vs_
readback_are_identical` 与 `tests/test_scan_corr.py::test_bulk_vs_day_by_day_vs_readback`
**在全量套件下偶发失败、单独跑恒绿**(开工基线首跑即复现一次,与 ⑪ 零关联;施工期
又复现一次,重跑即过)。属 ④ 扫描层,疑似跨用例状态污染或 polars 分组顺序残留,**⑪ 不
越块自行改**。

---

### V2-⑫ · 对账与画像(@builder)

**⑫-A 周度交割单对账(原样保留)**:`review/parse.py` / `reconcile.py` / `material.py` / `store.py` 骨架不改 —— 追加修正制、交割单为成交与费用最终权威、逐笔章程判定(`config_governing_at`)、`review_col_map` 可配映射。⚠ 既有坑不许回归:`review_col_map` 必须同时驱动「格式判定」与「列取值」;反推价格的必需列整列缺失必须**硬跳该行 + 警告**,绝不 `or 0.0`。
- **资金流水四分类扩展**:转入转出 / 分红 / 税费 / 交易盈亏**必须分开**;**账户金额增加不得直接视为策略收益**。

**⑫-B 偏好画像 / 能力画像(新引擎 `neckline/profile/`)**
- **两张账分开**:偏好答「喜欢什么」(常买题材 / 角色 / 入场方式 / 常选 Tier / 竞价与盘中确认习惯);能力答「什么真有效」(各类选择的胜率、盈亏比、回撤;是否跑赢同篮未选股票;哪些偏好是优势、哪些只是重复性错误;哪些机会经常被错误忽略)。
- 每项带 **样本量 / 时间范围 / 置信度**;**单笔不成偏好**(最小样本量常量,住 `profile/` 模块,不进策略包也不进章程)。
- **⛔ 初期不得反向影响客观 Tier**(蓝图 4.4 禁令)—— **守门单测:`neckline/selection/` 与 `neckline/scan/` 全目录 grep 零 `profile_` / `neckline.profile` 引用。**
- **🆕 「每期一版」的零行边界要在 `_clear_period` docstring 写明(2026-08-03,契约线复核 🔵 增量;文档级,⛔ 不需改代码)**:Y3 修复后画像按 **dimension 整段替换**,于是「**重算时某维度恰好产出 0 行、而其他维度有行**」的情况下,**该维度的旧行会留在当期**。现实中几乎打不出来(`user_actions`/`positions` 只增不减、同期重算样本单调增长),**但日后画像口径改版、某个维度整体停算时会真的踩到** —— 那时旧维度的行会**永远留在历史期里冒充当期结论**。**处置:在 `_clear_period` 的 docstring 写明这条边界 + 一句「某维度整体停算时,须先手工清历史期该维度行」。** 只写一句话,不改行为。
- 数据源 = ⑨ 的评价引擎产出 + `user_actions` + `positions` + 对账结果。

**依赖**:⑨⑩。
**验收**:两张画像表分别产出且维度不串;样本不足时如实标「样本不足 / 置信度 low」不给结论;grep 守门绿;资金四分类在造数上分得开;既有周复盘单测全绿;pytest 零回归。

**✅ V2-⑫ 完工记录(2026-08-03,@builder,纯本地代码 + 单测,零服务器 / 零部署)**:

**⑫-A**:`review/parse.py`/`reconcile.py`/`material.py`/`store.py` 四个既有模块骨架逐字未改(`_parse_row` 一行未碰,既有断言全部照旧)——新增能力全部走「追加」路径:`parse.py` 新增 `CashFlowEvent` 数据类 + `CASH_FLOW_TRANSFER`/`DIVIDEND`/`TAX`/`OTHER` 四个分类常量 + `_extract_cash_flow_event()`(`parse_workbook` 拿到既有 `skip_label` 之后另行调用,不改 `_parse_row` 任何返回路径),`ParseResult` 新增 `cash_flow_events` 字段(纯增量,不影响 `skip_counts`/`warnings` 既有语义)。新模块 `review/cashflow.py` 按周聚合:`CashFlowSummary` 四个独立字段(`transfer_in`/`transfer_out`/`dividend`/`tax`/`other`)+ `trading_pnl`(直接取 `WeeklyReview.stats.realized_pnl`,不重算)——**刻意不提供任何「合计」字段**,是「账户金额增加不得直接视为策略收益」的结构性落地。

**⑫-B**:新包 `neckline/profile/`——`common.py`(`load_buy_contexts` 面板装配,只读 `positions`/`entry_snapshots`/`position_plans`/`stock_basic`;`MIN_SAMPLE_N=5`/`MEDIUM_SAMPLE_N=20` 三档置信度,登记体例照 `sentinel/attention.py::ATTENTION_DEFAULTS`)、`preference.py`(题材/角色/入场方式(几何口径)/Tier 四维 + `entry_label` 半维——`user_actions.kind='label'` 的用户自述入场标签,⑩-C 可选、大概率稀疏)、`capability.py`(胜率/盈亏比/MFE·MAE 一律从 `positions` **真实成交**算、只看已平仓;`vs_peer_delta` 复用 `eval.metrics.score_tradable`/`exit_sim` 同一套判分比较「用户实际买的」与「同篮未选成员」的模拟收益,同一批数据顺带产出 `missed_role`——未选成员按角色分组的错失机会行;样本〔配对数〕< `MIN_SAMPLE_N` → `vs_peer_delta` 如实 `None`)、`store.py`(每期一版,`INSERT OR REPLACE`;`verdict` 说明文本不落库)。`scripts/profile.py`(CLI,同 ⑨ 分工:批算 + 落表,不接日更管线,留给 ⑭)+ `scripts/smoke_profile.py`(合成数据端到端冒烟,人工核对 MFE/MAE 手算逐位一致)。

**测试**:新增 5 个文件共 58 例(`test_review_cashflow.py` 10 / `test_profile_common.py` 14 / `test_profile_preference.py` 15 / `test_profile_capability.py` 14 / `test_profile_guardrails.py` 2)+ `test_review_parse.py` 追加 3 例。`python -m pytest tests/ -q`:本块自身零回归(基线 3012 过 + 2 skip → 3070 过 + 2 skip + 0 fail)。⚠ **施工期间同一工作树里另一并行会话** 对 `notify_kinds.py`/`api/notify.py`/`sentinel/holding.py`/`sentinel/engine.py` 有未提交改动(跟进 `3b1dcd7` 的 stop_approach/take_profit/sector_dive 三个 kind,尚在中间态),两个「精确集合」守门测试(`test_notify_kinds.py::test_kind_whitelist_is_exactly_eleven`/`test_notify.py::test_push_entrypoints_are_exactly_the_declared_set`)当前失败——**与本块零关联,未触碰、未修复,如实登记**;全量套件含该并行改动时为 3073 过 + 2 skip + 2 fail(两个 fail 均属此并行改动)。

**九处如实登记(未改 Plan,如与规划意图不符请澄清)**:
1. **资金流水第五档 `CASH_FLOW_OTHER`**:两家券商跳过行里「利息归本」(broker 账户利息)与「指定交易」(零金额账户操作)都不属「转入转出/分红/税费」任何一类,不强并入分红/税费,新增残余桶如实标注(「交易盈亏」本不出自跳过行,是回合净盈亏,不受影响)。
2. **转入转出拆成 `transfer_in`/`transfer_out`/`transfer_net` 三个数**(比 plan 原文合一颗粒度更细,不违反「分开」要求)。
3. **`entry_style`(几何口径,买入价相对建仓区间)与 `entry_label`(`user_actions` 用户自述口径)并存,不合并**:两者性质不同(系统推断 vs 用户自述),合并会混淆证据来源。
4. **「常买题材」取 `stock_basic.industry`,不取篮子 `driver`/`driver_kind`**:后者要么自由文本(同题材不同措辞误判不同值)要么太粗(7 个枚举),行业分类是本项目既有单一权威口径。
5. **能力画像的胜率/盈亏比/MFE·MAE 只用已平仓真实成交,不用 exit_sim 模拟值**——「什么真有效」首先看真实发生了什么,模拟值只服务于「同篮未选」这类真实数据无法回答的比较。
6. **`missed_role` 行的 `vs_peer_delta` 恒为 `None`**:该字段语义是「相对未选成员的差」,这类行本身在描述未选成员、没有「相对谁」的对象,改用 `win_rate` + `verdict` 文案表达,不塞一个语义不通的数字。
7. **`CapabilityRow.verdict` 不落库**:两张画像表 DDL(① 定死)均无说明性文本列,「哪些偏好是优势/错误」由已持久化的 `win_rate`/`vs_peer_delta`/`confidence` 派生,同 `eval.metrics.Verdict` 既定体例——结论读时计算,不单独持久化。
8. **未接入「对账结果」(周复盘 `WeeklyReview`/`RoundTrip`)作为画像数据源**:plan 原文数据源列了它,但 `RoundTrip` 来自交割单解析、与 `positions` 表无确定性外键(只能靠金额/日期近似匹配),勉强关联有误判风险,登记留给用户/⑭ 判断是否值得专门打通。
9. **MFE/MAE 用未复权真实价对未复权买入价,持有期跨除权除息日有已知失真**:与 ⑧ 除权锚失效检测同一类局限,本块未处理,已如实标注在函数文档。

---

### V2-⑬ · V1 清理清单执行(先建后拆,**不许边建边拆**;@builder-pro)✅ 完工(2026-08-03)

> **顺序纪律**:①–⑫ 全部完工、V2 主链能出一份完整篮子日报之后才动本块。删除一律**代码删干净、表停写留档不 DROP**(历史归因与审计要用;`_SCHEMA` 保留 DDL 并在注释里标「v2.0.0 起停写」)。

**⑬-1 20 只单票候选榜(含五常驻板块保底配额、竞争位)** → 删。落点:`report/intel_candidates.py` 的榜单产出路径、`report/board_pool.py`(五常驻)、`GET|PUT /settings/intel-boards` 两端点、`settings_store.get/set_intel_watch_boards`、客户端设置屏板块选择。`Candidate` 数据类随之退役(⑭ 的契约换血)。
**⑬-2 单票 LLM 审判(通过/否决 per 票)** → 删调用路径;`llm_judgments` **停写留档**。⚠ `llm/judge.py::judge_candidate` **本体保留**为「通用 LLM 调用 + 降级链 + verdict 解析」工具(含 `narrative_splitter` 依赖注入纪律),V2 只在需要结论标签处用;篮子链路走 ⑤ 的两段式。
**⑬-3 单票参考件三件套展示位** → 删;体例已移交篮子卡(⑦)。`report/reference_plan.py` / `reference_plan_store.py` 停用,`reference_plans` 表停写留档,契约 `CandidateOut.referencePlan` 随候选榜一并退役。
**⑬-4 exec_hint C1–C4 独立展示位** → 删展示位;四条计算**并入篮子剧本的输入**(⑦ 的「主要风险 / 剧本」上下文),`report/exec_hint.py` 保留为纯计算模块。
**⑬-5 决策日志强制表单** → ⑩-C 已下线服务端;本块删客户端 `DecisionLogSheet.swift` 的必填分支与相关校验文案。
**⑬-6 老四件套残键(P3-27 兑现)** → `CandidateOut.buyPoint/stop/target/invalidation` 四键 + `report/candidates.py` 的 `entry_plan_text` / `stop_loss_text` / `target_text` / `invalidation_text` 四函数**彻底删除**(自选体检是它们最后的消费方,而自选池本块删 → 无消费方)。**删除顺序纪律见 ⑭-D。**
**⑬-7 盘前 9:26 单票高开/低开四判定** → 改为**篮子竞价剧本核对**:`sentinel/precall.py` 的判定对象由「候选」换成「T1/T2 篮子成员」,判据源换成 ⑦ 的结构化 spec;**推送仍在**(归 ⑪ 的 `kind='precall'`)。
**⑬-8 报告 markdown 候选节 / 情报节旧布局** → 重构为篮子日报(⑭-A)。
  - ⛔ **`neckline/llm/json_block.py` 不随之停用**(2026-08-02 登记):⑤ 施工时已把围栏 JSON 解析器(`split_narrative_and_reference_json` 及三条围栏正则)从 `report/reference_plan.py` **搬到该新模块**,`reference_plan.py` 只是同名再导出;它是**通用 LLM 输出解析件**,⑤ 的聚合层与 ⑦ 的卡生成都在用。**删 `reference_plan.py` 时只删那层再导出,别把通用件一起陪葬**(它零 import `report`,不构成 `llm/` 反向依赖;`judge_candidate(narrative_splitter=…)` 的依赖注入体例不变)。**守门单测**:⑬ 做完后断言 `neckline/llm/json_block.py` 仍在且被 ⑤/⑦ 引用。
**⑬-9 K1 entry mask / momentum 在生产链路的全部残余** → 生产 import 清零(`strategy/momentum.py` 只留回测 / 研究线调用),grep 守门。
**⑬-10 问询台 forced 海选池通道** → 删:`inquiry_pool` 表停写留档,`load_pending_inquiry_codes` / `mark_inquiry_pool_consumed` 及报告侧消费逻辑删除。
**⑬-11(裁定 #9-a)自选池 + 同花顺对账 → 直接删** → `watchlist` 表停写留档;删 `neckline/watchlist.py`、`report/watchlist_check.py`、五个 `/watchlist*` 端点(含 `reconcile-ths` / `export-ths`)、客户端 `WatchlistView.swift`、报告自选体检节、哨兵关注池的自选来源(⑧-A 已改)。
  - ⚠ **连带后果(如实登记,架构稿未提)**:问询台的「**+自选**」是它**唯一写回主链的通道**(v1.3.3 用它取代了「初审通过进海选池」)。本项删除后**问询台成为纯分析入口,没有任何把一只票送进当晚主链的路径**。这是裁定 #9 的必然结果,**不是遗漏**;若日后想恢复该能力,正确做法是「加入观察篮子」而不是复活自选池。
  - §八 第 9 项(用户提供同花顺自选 txt)随之**作废**。
**⑬-12(裁定 #9-b)呼吸台账 → 删** → `breathing_t_trades` 表停写留档;删 `neckline/breathing.py`、三个 `/breathing*` 端点、客户端 `BreathingLedgerView.swift`。⚠ 连带:§七 P3-11 的「呼吸 T 净贡献」归因维**随之废弃**(已在 §七 标注)。
**⑬-13(裁定 #9-c)五常驻板块保底删除** → 并入 ⑬-1。
**⑬-N 信息卡:保留改造(**D1 已拍板 = 不删**)** → `report/info_card.py` 与端点 `GET /report/{date}/info-card/{code}` **保留**;改造 = 数据来源由「候选快照」换成「**篮子成员**」,并新增三块:所属篮子与共同驱动 / 本票角色(含对拍分歧)/ 与同篮其他成员的对比。它是 ⑮ 篮子成员详情页的地基。
  - **⑬-N-K7(需求 5 的第二个落点)**:信息卡新增**成员标注件**展示区 —— `pullback_leader`(龙回头位)/ `warn_streak_top`(连板头名)/ `warn_chase_zone`(强势追入位),**读 ⑦-K7 的 `neckline/selection/member_tags.py` 同一份实现与同一份文案模板,禁在信息卡侧重写判据**(交叉断言:同一票同一天,信息卡与篮子卡的标签集合逐位相同 —— v1.5 自选 / 持仓两侧 K4 镜像的交叉断言体例平移)。展示为**警示 / 中性色调的标注 chip**,**每处带「参考、非指令」**;**四不硬约束照旧**(不进排序 / 不进哨兵 / 不改去留 / 不加分)。

**明确不动(§8.3,防误删)**:数据层全部(TuShare 客户端 / parquet 落盘 / 交易日历 / 概念板块日更 / `suspend_d` 停牌口径 / `limit_derived` / `board` / 行业强度落表)、哨兵纪律分支、章程与激活历史与四道闸、逐笔章程判定、交割单对账、APNs 基础设施、`prompt_context`、参考件的夹逼 / 冻结 / 审计体例(已移交篮子卡)、**问询台主体 + `inquiry_log`**。

**依赖**:④⑤⑥⑦⑧⑩⑪(先建后拆)。
> **⚠ ⑬ 欠交付一件,补掉再往 ⑭ 走(2026-08-03 由 V2 review 契约线 Y6 带出)**:验收原文要求的「**报告与端点『删除前 / 删除后』对照表落 `archive/`**」**没有交付**(`archive/` 最新文件停在 20260802,⑬ 完工记录也未提)。**影响**:⑭-C 三方对拍(老报告 vs 新报告 vs 契约)的**基准输入缺席**,届时只能靠 git 考古重建删除前的形状。**处置:排进本轮 review 修复批次,或最迟在 ⑭ 开工前补** —— 趁 ⑬ 的删除提交还新鲜(`1161441` / `1a318db` / `01d04c2`),**成本远低于 ⑭ 开工时再考古**。⛔ 别把它并进 ⑭ 自己的工作量里,那时基准已经不好重建了。

**验收**:十三项逐项 grep 零残余(每项一条断言);被删模块的单测**同步删除,不许留 `skip`**;停写表在跑一遍完整管线后**行数不增**(单测);报告与端点「删除前 / 删除后」对照表落 `archive/`(⑭ 的三方对拍要用);pytest 零回归。

#### ✅ V2-⑬ 完工记录(2026-08-03,@builder-pro)

**十三项 + ⑬-N 逐项处置(全部完成,无跳过)**

| 项 | 处置 | 落点 |
|---|---|---|
| ⑬-1 候选榜 | ✅ 删 | `report/intel_candidates.py` + `report/candidates.py`(含 `Candidate` 数据类)物理删除;两端点 + `settings_store` 存取 + 客户端设置屏板块区全删 |
| ⑬-2 单票审判 | ✅ 删调用路径 | `pipeline._judge_candidates_with_budget` + `store.save_llm_judgment`/`delete_llm_judgments` 删;`llm_judgments` 停写留档,`load_llm_judgments` 留;**`judge_candidate` 本体 + `narrative_splitter` 保留** |
| ⑬-3 参考件三件套 | ✅ 删 | `reference_plan.py`/`reference_plan_store.py` 物理删除,`reference_plans` 停写留档,`CandidateOut.referencePlan` 删;**`llm/json_block.py` 未陪葬**(守门锁死 ⑤/⑦ 仍在用) |
| ⑬-4 exec_hint 展示位 | ✅ 删展示位 | `attach_exec_hints` 删 → 新增纯计算出口 `exec_hints_for()`(四条判定逐字未改),供 ⑭ 接进篮子剧本上下文 |
| ⑬-5 决策日志强制表单 | ✅ 删客户端必填分支 | `DecisionLogForm.isValid` 五项 → 只剩「有 code」;`maxChaseChosen` 降级为展示态;表单 header/footer 文案同步 |
| ⑬-6 老四件套残键 | ✅ 彻底删除 | 服务端四键 + 客户端 `Candidate` 四属性 + `FourPieceDisclosure` 组件(P3-27 兑现,D2=A 路一次性换血) |
| ⑬-7 盘前四判定 | ✅ 换血 | `precall.MemberScript` + `load_member_scripts()`:对象 = T1/T2 篮子成员,判据 = 卡里冻结的 `ref_close`/`close_below_stop_line`;阈值与 `kind='precall'` 推送一行未改 |
| ⑬-8 报告旧布局 | ✅ 拆旧 | `_render_candidates` 等 6 个候选专属渲染件删;情绪/板块/持仓体检/情报/资金流/消息面六节留(⑭-A 才总装篮子日报) |
| ⑬-9 K1 entry mask | ✅ 生产 import 清零 | 最后一处(问询台「母战法买点 + 展示排序分」)整段删;**`MomentumConfig` 保留**(章程 config 解析目标,红线 1) |
| ⑬-10 海选池通道 | ✅ 删 | 写 + 两个消费函数删,`inquiry_pool` 停写留档;**`load_inquiry_pool` 保留**(周复盘「计划内(问询台海选池)」归因读历史行) |
| ⑬-11 自选池 + 同花顺 | ✅ 整链删 | `watchlist.py`/`watchlist_check.py`/五端点/客户端 tab 全删;**`discipline_checks` 搬家**到 `report/discipline_checks.py`(问询台判据源) |
| ⑬-12 呼吸台账 | ✅ 删 | `breathing.py`/三端点/`BreathingLedgerView.swift`;`BREATHING_TRIAL` 打法标签保留 |
| ⑬-13 五常驻保底 | ✅ 随 ⑬-1 删 | `QUOTA_PER_PERMANENT_BOARD`/`_permanent_board_status` 随 `intel_candidates.py` 消失 |
| ⑬-N 信息卡 | ✅ 保留改造 | 数据源 → 篮子成员;新增 `build_basket_context()` 三块 + `build_member_tags()`(⑬-N-K7);`InfoCardOut` 加 `basket`/`tags`/`tagsAbsent` |

**⚠ Plan 勘误一处(已按事实施工,未改本节原文)**:⑬-1 落点表把 `report/board_pool.py`
标成「(五常驻)」——**它不是五常驻模块**,是**板块池卫生线**(名称模式闸 + 成分数上限),
`scan/corr.py`/`scan/cluster.py`/`scan/seeds.py`/`report/intel.py`/`report/sector_moneyflow.py`
五处 V2 现役代码在用,删了会把扫描层打断。**故保留**,并加了反向守门单测锁死它必须还在。
真正的五常驻保底住在 `intel_candidates.py` 里,已随该模块一并消失。

**⚠ 三处 Plan 未给规格、已按最贴合上下文实现的设计判断(如与规划意图不符请澄清)**
1. **买点哨兵(`sentinel/entry.py`)退役**。⑬-1 要求 `Candidate` 数据类退役,而买点哨兵
   100% 由 K1 的 per-code `entry_spec`(platform_high/ma10/breakout_vol_expand)驱动;
   V2 没有「单票买点计划」这个概念,给篮子成员现编一个买点 = 发明策略(§3.8 禁)。
   连带删 `compute_missed_entry_hint` 的触发源(`sentinel_events` 里不再有 `entry` 行,
   函数与 `missedEntryHint` 键**保留但恒空**,是否删键归 ⑭-B 契约总装裁定)。
   **它不在 ⑪-A 点名保留的「现役纪律分支」清单里**(-5% 止损 / 回落止盈 / 时间退出 /
   证伪四哨兵 / 熔断),这是判它可退役的依据。
2. **证伪四哨兵(在 ⑪-A 保留清单里)判定对象换成 T1/T2 篮子成员,判定逻辑一行未改**。
   之所以能一行不改地平移:它读的 `invalidation_spec` 本就是**零入参的全局常量函数**
   (低开阈 / 量比上下限 / VWAP 开关),没有任何 per-code 依赖跟候选榜一起消失;
   该函数与三个常量随 `report/candidates.py` 删除**搬到 `sentinel/invalidation.py`**。
   `WatchUniverse.candidates` → `targets: List[WatchTarget]`(码 / 名 / 全局证伪 spec /
   所属篮子);退潮「主线板块跳水」样本同步从「`hot_sectors` 命中的候选」换成
   「T1/T2 篮子成员全体」,**阈值未动**。
3. **⑬-8「情报节旧布局」按「拆候选侧、留情报侧」执行**。⑬-8 只说重构为篮子日报(⑭-A),
   而情报件 C1 / 板块资金流 C2 / 消息面 C4 的**数据源都不在十三项删除清单里**;拆掉它们
   会毁掉 ⑭-A「① 情绪与市场语境」要用的内容。故本块只删候选专属渲染件,六节留给 ⑭ 重排。

**⚠ 留给 ⑭ 的三处欠账(⑬ 边界内刻意不做)**
- `news_alerts` 的**次级扫描域恒空**(自选池删除后无来源,隔日轮扫机制刻意不拆)→
  ⑭-A 把篮子成员接进 `build_news_alerts(secondary_codes=…)` 时原样复用;**同时**要把
  `info_card._default_news_domain` 同步扩域(两处必须同域,否则信息卡会对着一批"其实
  扫过"的票说"不在扫描域")。
- `exec_hints_for()` 目前**无生产调用方** —— ⑬-4 原文「并入篮子剧本的输入(⑦ 的主要风险 /
  剧本上下文)」的接线点在 ⑭-A 的编排里(⑦ 的卡生成不该反向 import `report/`)。
- `reports.candidates_json` 恒落 `[]`、`CandidateOut` 壳与 `ReportOut.candidates` 键仍在
  (⑬ 的「删除端点」清单不含 `/report`)→ ⑭-B 契约总装换成篮子 DTO。

**测试**:`python -m pytest tests/ -q` **2706 passed + 2 skipped**(⑬ 前基线 3097;删
7 个整测试文件 + 若干类/例,新增 `tests/test_v1_retirement_guard.py` 27 例守门 + ⑬-N 6 例)。
双端 `xcodebuild` BUILD SUCCEEDED;iOS Simulator `xcodebuild test` 154 tests / 0 failures。
六个 smoke 脚本(basket_verify / basket_review / precall / attention_alerts / profile /
eval_exit_sim)+ `smoke_sentinel.py` 全绿;`scripts/report.py` 在隔离库副本上出得来报告
(过渡态版式)。**真实 `data/neckline.db` 与生产服务器全程未动**。

---

### V2-⑭ · 篮子日报 + API 契约总装 + 三方对拍(@builder)✅ 完工(2026-08-03)

**⑭-A 报告重构为篮子日报**(`report/render.py` 重写 + `report/pipeline.py` 重排)
- 报告结构(顺序定死):① 情绪与市场语境 → ② **持仓体检**(先管住手里的)→ ③ **今日篮子**(T1 / T2 / T3,每篮一张卡)→ ③b **今日未定档篮子(一行摘要)** → ④ **昨日篮子复盘**(T1/T2 详复盘 + T3 简评)→ ⑤ 数据新鲜度与降级披露。
  - **③b 是 ⑥-b-C 的落点(2026-08-02 planner 裁定)**:一行摘要 +(可选)清单,列**篮子名 / 机械分 / 原因码**,**不出卡、无 `basket_id`**;**两种原因码必须分开写**——`capacity_overflow`(分数够、位置满 → 今天机会多到装不下)vs `below_quality_line`(连 `tier3_min` 都没过 → 今天没什么好货),**⛔ 不许合并成一句"未入选"**(两者指向相反的市场结论)。零溢出时**这一节仍在**、写「今日无未定档篮子」(节在 = 算过了,承 ③-C「空持仓也不省略这一节」的同一纪律)。契约字段 `droppedBaskets: [{name, mechScore, reason}]`(默认空数组)。
  - **③ 的三档全部可空**:极端情况「今日无篮子达到定档标准」是**合法输出**,如实写出来,**⛔ 不许为了让报告好看而放宽任何一条质量线**(⑥-b-B)。
- ~~`pipeline.build_report` 分段~~ → **❌ 「住 `pipeline.py`」这句我写错了,2026-08-03 更正(⑭ 完工记录设计判断 1,✅ 核准)**:**编排链住新文件 `neckline/report/evening.py::run_evening_chain`,⛔ 不住 `pipeline.py`**。理由**很硬**:`report/pipeline.py` 在 `tests/test_scan_layer_guardrails.py` 的**在线模块清单**里 —— P0-23 守门单测逐字 grep **禁止它出现扫描层批算写入口**,而链的第二段恰恰要调那几个入口。**把链塞进 `pipeline.py` = 为了迁就文件摆放而钝化一条被生产 OOM 打出来的真防线**。`pipeline.build_report` 保持**只读不算**(历史回放的生命线)。⑭ 还顺带把 `report/basket_daily.py` 也加进那份在线清单(它是 `build_report` 的只读装配层)—— **主动扩大守门覆盖面,好**。
- **链序(定死)**:16:05 拉数 → **⑧ EOD 篮子验证拍** → ④ 扫描层批算 + ④b → ⑤ 聚合 → ⑥ Tier → ⑦ 卡冻结 → **⑨ 复盘** → **报告落库**。
  - **⑨ 排在报告之前(2026-08-03 裁定,⑭ 实现如此,以此为准)**:报告的 ④「昨日篮子复盘」节要读 `basket_review_daily` 的**当日行**,⑨ 排后面那一节在同一次运行里**必然空**。⑯-D 原来的切法(报告在前、⑨ 在后)据此**已改写**,见 ⑯-D。
  - `segments` 参数**只挑跑哪几段、不改顺序**(传入集合按 `CHAIN_SEGMENTS` 重排),这是 ⑯-D 拆 oneshot 的接缝。段状态**四态**(ok / empty / failed / skipped),⛔ 不合并成 bool。
  - **🆕 ⑧ 的 EOD 验证拍必须在这里挂上(2026-08-02 补,由 ⑧ 完工记录登记 ⑥ 带出,⛔ 别漏)**:⑧ 只交付了 `sentinel/basket_verify.py::run_eod_verification()` + CLI `scripts/basket_verify.py`,**刻意没有擅自挂进 `daily_update.py`**(那是 ⑯-D 的分段职责),**⑭-A 才是 Plan 指定的编排入口**。**在 ⑭ 落地之前,`basket_verification` 的 EOD 定论行不会自动产生** —— 这是**已知窗口期,不用治**,但 ⑭ builder 必须把这一步挂上,否则「当前状态三路读法」永远只能读到盘中暂态。
  - **位置定死在「拉数之后、扫描层之前」**:它判的是**昨日(D0)冻结的卡**在**今日(D+1)**的收盘表现,吃的是刚拉到的今日 EOD 数据,与今晚要生成的**新**篮子无关 —— 放前面既符合因果、又保证即使后面某段炸了,昨日的定论行也已经落好。**包保险丝**(异常只 WARNING,不阻断报告)。
- **每段包保险丝**:任一段异常 → 报告照出、该段如实标缺席(**绝不因为某段失败而当日无报告**)。
- **报告生成期查任何非 EOD 面板的表都要按 `trade_date` 截断**(防前视,`exec_hint._latest_decision` 是正确姿势的先例)。

**⑭-B API 契约总装**(`api/schemas.py` + `api/app.py`)
- 新 DTO:`BasketOut` / `BasketCardOut` / `BasketMemberOut` / `TierOut` / `BasketVerificationOut` / `BasketReviewOut` / `PositionPlanOut` / `EntrySnapshotOut` / `CustomAlertOut` / `ProfileOut` / `ProviderOut` / `PackOut`。
- 新端点(**不属 D1–D8,是 planner 的形状建议,builder 可在保持语义前提下微调路径**):`GET /baskets?date=` / `GET /baskets/{id}` / `GET /baskets/{id}/card?version=` / `GET /baskets/{id}/verification` / `GET /baskets/{id}/review?date=` / `GET /positions/{id}/plans` / `POST /positions/{id}/plans` / `GET|POST|PUT|DELETE /alerts` / `POST /alerts/parse`(NL → 结构化,返回确认卡)/ `GET /profile/preference` / `GET /profile/capability` / `GET /packs` / `GET /packs/{version}` / `GET /eval/weekly`。
- 删除端点:`/watchlist*` ×5、`/breathing*` ×3、`/settings/intel-boards` ×2、`PUT /settings/llm`(D2 = A 路已拍板,直接删)。
- ⚠ **已在上游块落地、⑭ 只需对拍不必重建的端点(2026-08-03 对齐 ⑪ 完工记录登记 ⑦)**:`GET|POST|PUT|DELETE /alerts` + `POST /alerts/parse` **五个已由 ⑪-C 落地**;`PUT /settings/push` 的契约也已在 ⑪ 换血成 kind 清单(`PushSettingsOut.kinds[]` 带 `level`/`label`,取代六个 bool)。**⛔ ⑭ 不要照清单再造一遍**,只把它们纳入 ⑭-C 的三方对拍。
- **404 / 400 的 `reason` 与客户端 `APIClient.mapReason` 必须互为闭包**(CLAUDE.md 坑:新增会返 404 的端点必须检查要不要加新 case;**复用已有 reason 字符串不算"没加"**)。
- **🆕 已知必须新增的 reason(2026-08-02 由 ⑦ 完工记录带出,别漏)**:`GET /baskets/{id}/card` 在**有篮子无卡**时返 **404 + reason `card_not_ready`** —— 这是**全新字符串**,`mapReason` **必须加新 case**,⛔ 不能指望 fallback 猜文案(v1.4 `watchlist` 的 `not_found` 被误显示成「持仓已清」就是这么踩的)。文案方向:「本篮的卡还没生成」,**不是**「篮子不存在」——两者语义完全不同,后者会让用户以为系统丢了篮子。读侧直接用 `basket_store.load_basket_card()`(`version=None` 取最新版本)。
- **同族的两个 404 要分得开**:`basket_not_found`(篮子本身不存在)vs `card_not_ready`(篮子在、卡未生成)。**合并成一个就把「没有」和「没看」混了。**

**⑭-C 契约三方对拍(不可选,架构稿 §9.1-1)**
- `api/schemas.py` 声明 → `api/app.py` 转发 → Swift `Models.swift` 解码,**逐字段走一遍**并出对照表存 `archive/V2_契约三方对拍_2026MMDD.md`。这是**施工块内的自查**,不等于独立 review(是否召唤 @reviewer 见下方门禁)。
- 逐字段要标:必填 / 可选、Optional 的编码姿势(`encodeIfPresent` vs 手写 `encode(to:)` 保证键恒出现)、**是否属于「写入时冻住的历史快照」**(如 `basket_cards.card_json` / `reviews.result_json` —— 这类 DTO 必须手写 `init(from:)` 做 `decodeIfPresent` 兜底,因为服务端升级不会给老快照补键)。

**⑭-D 老键淘汰排期(D2 已拍板 = A 路,**只有一条路,builder 不必犹豫**)**
- **A 路(定案)**:新机挂新子域 `nk.linotsai.top`、老机 `ln.linotsai.top` 原样服务到 ⑰ 换包完成 → **老 App 打老机、新 App 打新机,两者不交叉** → **V2 契约一次性换血,不留过渡键**:⑬-6 的四个老键(`buyPoint`/`stop`/`target`/`invalidation`)与 `report/candidates.py` 四个文案函数**直接删,不做过渡文案、不发空串**;`PUT /settings/llm` 同样直接删。
- ~~B 路(沿用 `ln.linotsai.top`,割接时切解析 → 老 App 会撞 V2 服务端 → 老键须走两步淘汰、⑰ 换包须前移)~~ → **2026-08-02 用户拍板否决,不再是选项。** 保留这行只为留痕:**「先客户端可选解码、下版服务端才删键」这条两步淘汰纪律本身仍然有效**(CLAUDE.md 铁律,V2 之后的版本照守),V2 只是**靠换机窗口结构性地绕开了它**,不是废除它。
- ⚠ **A 路成立的前提条件(builder 必须在 ⑯-G 现场确认,不成立就停手回 planner)**:新机 nginx **只**服务 `nk.linotsai.top`,**不接管** `ln.linotsai.top` 的任何解析;老机在 ⑰ 完成前**不停机、不改代码、不改 DNS**。

**依赖**:①–⑬。
**验收**:三方对拍表逐字段齐(每字段三处一致);报告 golden 快照三态(有篮子 / 无篮子 / 某段降级);端点前后对照表齐;`mapReason` 闭包单测;pytest 零回归。

#### ✅ V2-⑭ 完工记录(2026-08-03,@builder-pro;纯本地代码 + 单测 + 隔离库冒烟,零服务器 / 零部署)

**⑭-A 报告重构为篮子日报**

新建 `neckline/report/basket_daily.py`(三段视图模型 + **snake→camel 唯一转换点**)、
重写 `report/render.py`(五段版式)、重排 `report/pipeline.py::build_report`。
`reports` 新列 `basket_daily_json`(幂等迁移;⑥ 的溢出篮不进 `baskets` 表,
报告快照是它**唯一**落点)。

- **五段顺序定死**:① 情绪与市场语境(⑬-8 判定「留情报侧」剩下的情绪 / 强势板块 /
  情报件 C1 / 板块资金流 C2 / 消息面 C4 五件在此归并成一段 —— 它们回答同一个问题
  「今天这个市场是什么状态」)→ ② 持仓体检 → ③ 今日篮子 → ③b 未定档 →
  ④ 昨日篮子复盘 → ⑤ 数据新鲜度与降级披露。**每段独立保险丝**:任一段缺席只写
  「本段未取得 + 原因」,其余四段照出。
- **③ 三档全部可空是合法输出**:空档位如实写「今日 T1 为空」,三档全空写
  「今日无篮子达到定档标准…**不是故障**」;**有篮子无卡**(事务 1 / 事务 2 分开)
  照出篮子壳 + 「本篮的卡还没生成」,⛔ 不抹掉整篮。
- **③b 两个原因码永不合并**:`capacity_overflow`(分数够、位置满 →「今天机会多到
  装不下」)vs `below_quality_line`(连 T3 下限都没过 →「今天没什么好货」);
  零溢出时**这一节仍在**。`dropped=None`(没跑 ⑥)与 `dropped=[]`(跑了零溢出)
  **两态分开**,单测锁死。
- **新 `neckline/report/evening.py::run_evening_chain`**(16:35 链):
  **⑧ EOD 验证拍 → ④ 扫描层批算 + ④b → ⑤ 聚合 → ⑥ Tier(事务1)→ ⑦ 卡(事务2)
  → ⑨ 复盘 → 报告落库**。⑧ 位置定死在「拉数之后、扫描层之前」;`segments` 只挑
  跑哪几段、**不改顺序**(传进来的集合按 `CHAIN_SEGMENTS` 重排),这是 ⑯-D 拆三个
  oneshot 的接缝。段状态**四态**(ok / empty 跑了没东西 / failed 跑了炸了 /
  skipped 没要),⛔ 不合并成一个 bool。CLI `scripts/evening.py`(失败段 → 退出码 3,
  systemd 那侧看得见)。
- **兑现三处前置块登记的欠账**:① ④ 的 `dataFreshness.scanLayer*` 三键接线
  (与板块三键、行业强度三键**并列九个键**,三件独立故障不合并);② ⑬-4 的
  `exec_hints_for()` 接进篮子成员(挂在卡**旁边**、不注进冻结件);③ ⑬ 的
  `news_alerts` 次级扫描域接篮子成员 + `info_card._default_news_domain` **同步扩域**
  (两处必须同域,否则信息卡会对着"其实扫过"的票说"不在扫描域")。

**⑭-B API 契约总装**:`CandidateOut` / `IntelRankOut` / `LLMJudgmentOut` /
`InfoCardSummaryOut` 四个 DTO 连同 `ReportOut.candidates` 键**整族退役**,换
`basketDaily`;新增 **12 条路径 / 13 个方法+路径对**(`/baskets` ×5、`/positions/{id}/plans` GET+POST、
`/positions/{id}/entry-snapshot`、`/profile/*` ×2、`/packs` ×2、`/eval/weekly`)+ **17 个新 DTO**
(⑭-B 点名的 12 个里 `CustomAlertOut`/`ProviderOut` 已由 ⑪/② 落地 → 新建 10 个,
另加 7 个配套件:`BasketsListOut`/`DroppedBasketOut`/`BasketDailyOut`/`PositionPlansOut`/
`PositionPlanCreateIn`/`PacksListOut`/`EvalWeeklyOut`)。
`GET /baskets/{id}/card` 的 **404 双 reason** `basket_not_found` / `card_not_ready`
分得开。契约线 🔵 B7 收口:`GET /alerts` 查询参数由 Python 形参名 `status_filter`
改回契约名 `status`。⑪-C 的 `/alerts` 五端点与 `PUT /settings/push` **只对拍未重建**。

**⑭-C 三方对拍**:逐字段对照表 + ⑮ 欠账清单终稿 → `archive/V2_契约三方对拍_20260803.md`;
7 条机器断言 → `tests/test_contract_crosscheck.py`(**「客户端调用面 ⊆ 服务端路由面」**
用 `==` 不用 `<=` —— 已知欠账是精确集合不是 allowlist,⑮ 还完债不同步清空同样红)。

**⑭-D**:A 路前提自检清单 → `deploy/A路割接前提自检清单.md`(P1 新机 nginx 不接管
`ln` 的**反向判据** / P2 无 default_server 兜底 / P3 两域名解析必须不同 IP /
P4 老机不停机不改代码不改 DNS / P5 两边版本号必须不同 + 割接后 R1–R5 复核)。

**🔴 对拍打出的三处「服务端已改、客户端硬解码」(⑮ 不改就是白屏,不是少个字段)**:
`ReportResponse.candidates` 是 `try c.decode`(整份报告解不出)、`PushSettings` 六个
具名 bool 合成解码(整个 `/settings` 解不出)、`SettingsSnapshot.llmKeySet` 必需。
A 路让它们在生产上不会相遇,但 ⑰ 换包前必须真的改完。

**⚠ 五处如实登记的设计判断(Plan 未给精确形状,已按最贴合上下文实现;如与规划意图不符请澄清)**

1. **16:35 编排链**住新文件 **`neckline/report/evening.py`**,不住 `pipeline.py`。
   ⑭-A 原文写的是「`pipeline.build_report` 分段」,但 `report/pipeline.py` 在
   `tests/test_scan_layer_guardrails.py` 的**在线模块清单**里 —— P0-23 守门单测逐字
   grep 禁止它出现扫描层批算写入口,而链的第二段恰恰要调那几个入口。**把链塞进
   `pipeline.py` = 为了迁就文件摆放而钝化一条被生产 OOM 打出来的真防线**,故拆文件:
   `pipeline.build_report` **只读不算**(历史回放的生命线),批算住 `evening.py`。
   顺带把 `report/basket_daily.py` 也加进那份在线清单(它是 `build_report` 的只读装配层)。
2. **⑨ 复盘排在报告落库之前**(⑭-A 原文说「⑨ 复盘引擎单独一段」,未明说前后)。
   排前面是因为报告的 ④「昨日篮子复盘」节要读 `basket_review_daily` 当日行 —— 排后面
   那一节在同一次运行里必然空。⚠ **这与 ⑯-D 的分段描述有出入**:⑯-D 把「报告」归在
   `neckline-basket.service`、⑨ 归在 `neckline-review.service`(即报告在前、复盘在后),
   照那个切法 ④ 节会常态显示「今日无昨日篮子可复盘」。**已做成 `segments` 参数、两种
   切法都跑得动**,但哪一种是产品意图**请 planner 裁一次**(见下方「⚠ 交回 planner 的一件」)。
3. **④ 扫描层批算同时挂在两处**:`scripts/daily_update.py::update_scan_layer`(④ 完工时
   挂的,16:05 拉数后)与本次的 `run_evening_chain` 的 SEG_SCAN。两处都是幂等 upsert、
   单日成本 ~2s,重复跑无害;**⑯-D 拆 oneshot 时须二选一**(建议留在 `neckline-scan.service`、
   从 `daily_update` 摘掉),本块不擅自摘 —— 那会改动一个已在生产 timer 里跑的脚本。
4. **`VERSION` 未从 `v1.5.2` 升到 `v2.0.0`**。守门单测要求它与客户端 `project.yml` /
   pbxproj 的 `MARKETING_VERSION` 三者恒等、且必须同一次提交动;而 ⑮ 的验收条款明写
   「`MARKETING_VERSION` = `2.0.0` 三方一致」——**版号是 ⑮ 的活**,⑭ 提前升会让守门单测
   常年红。⚠ 后果:此刻服务端跑的是 V2 契约但 `/health` 仍报 v1.5.2(**纯本地,未部署**)。
5. **`exec_hint` 的特征面板是本块新写的轻量版**(60 自然日 × `daily`+`adj_factor`+
   `limit_derived`,整表按区间读一次再按代码过滤),**不复用**
   `holding_k4_check._build_holding_feature_panel`(420 自然日 × 4 表 × 逐票
   `get_stock_history`)—— 持仓 ≤3 只时后者无所谓,篮子成员几十只时会退化成上万次
   parquet footer 打开(§七 P1-26 的病根)。C1/C2/C4 只要 `ret_1d`/`is_limit_up`/
   `close`/`ma20`,60 天足够;特征装配仍复用生产 `add_features`/`merge_limit_features`,
   未重写任何一个特征。

**⚠ 交回 planner 的一件(不拦 ⑮)**:上方第 2 点 —— **⑨ 复盘与报告落库的先后**在
⑭-A 与 ⑯-D 两处描述不一致。现按「⑨ 在前」实现(报告 ④ 节才有内容),`segments`
参数让两种切法都跑得动,改口径只需调 ⑯-D 的服务划分、不必动代码。

**⚠ 顺带核实、未改的两点**:① ⑫ 完工记录第 8 点「对账结果作画像数据源」——**裁定维持
不接**,理由三条见对拍表 §十(`RoundTrip` 与 `positions` 无确定性外键,同日分批建仓会
系统性错配;宁可没有这一维,不要一个可能是错的维),已建议的正确做法(用户确认的
关联字段)留 Backlog。② ⑩ 提请核对的两组新拟枚举码(七枚标签码 + 四个 `close_reason`
码)—— **原样采纳**,理由见对拍表 §九;⚠ `TARGET_ZONE_REACHED` 的文案是「达到参考
区间」,⛔ 不许写成「止盈」(离场参考区间不是止盈线)。

**测试**:`python -m pytest tests/ -q` **2887 passed + 4 skipped**(⑭ 前基线 2808 + 2;
新增 `test_basket_daily.py` 31 例 / `test_evening_chain.py` 15 例 / `test_api_baskets.py`
25 例 / `test_contract_crosscheck.py` 7 例 + 2 skip〔待 ⑮ 生效的冻结快照 DTO 闸,
**skip 并说明原因,不静默通过**〕,删 9 例随候选契约退役的用例)。既有唯一红项
`test_sentinel_custom.py::test_cooldown_blocks_second_hit_and_expires` 已用 `git worktree`
拉干净 HEAD 副本复现,证实是**既有挂钟脆弱测试**(北京 ~14:48 后必红),与本块零关联。
**冒烟**:`scripts/smoke_evening.py --date 20260724`(真实历史日、`sqlite3.backup` 隔离
副本、真实 parquet 只读、全桩 LLM)—— 全链五段 `ok`(⑧ 判 3 篮落 3 行 / ④ 扫描
490+12650+490 行 / ⑤⑥⑦ 4 篮 4 卡 / ⑨ 复盘 3 篇 / 报告落库),五段结构齐、
`dataFreshness` 三组九键在、③b 节在(当日零溢出,两码分开的路径由单测覆盖)、
执行提示与角色对拍分歧均正确渲染;**真实 `data/neckline.db` MD5 前后未变**。

---

### V2-⑮ · 客户端双端改版(iOS + macOS,@builder)✅ 完工(2026-08-03)

**信息架构(**D8 已拍板**)**:iPhone 四板块 = **今日篮子 / 持仓 / 问询台 / 设置**;macOS 四板块 + **周复盘工作台**(交割单上传 + 对账 + 画像 + 评价校准报告)。**不新增 tab。**

- **今日篮子**:T1/T2/T3 分段(**空档位如实显示"今日 T1 为空"**,不隐藏)→ 篮子卡(11 项,**每处参考件带「参考、非指令」标注**)→ 成员卡(角色 / 对拍分歧两说并存 / 信息卡入口)→ 验证状态实时角标(四态)。
- **持仓**:极简录入 sheet(三字段 + 日期选择器)/ 计划继承卡(含"新建计划版本")/ **同题材合并敞口**提示 / 计划 vs 实际偏离提示(不质问)/ 通知 kind 开关入口。
- **问询台**:保留(自由对话体);删「+自选」按钮(⑬-11)。
- **NL 提醒**:输入框 → **确认卡**(标的 / 条件 / 生效窗 / 冷却 / 到期 / 行情延迟披露 / 只通知不交易)→ 提醒列表(改 / 停 / 删)。
- **设置**:**Provider 注册表**增删改(name / base_url / model / key / 是否带联网检索)+ **任务路由**;推送按 `kind` 开关;App / 服务端双版本行(既有体例)。
  - **🆕 推送侧的客户端欠账(2026-08-03 由 ⑪ 完工记录登记 ⑦ 带出,⑪ 已把服务端换血完,客户端还是 V1 形状,⛔ 别漏)**:
    - **`PushSettings` DTO:六个 bool → `kinds[]`**(每项带 `kind` / `level` / `label` / `enabled`)。设置屏据此**动态渲染**开关列表并**按 `level` 分组**(立即 / 重要不紧急 / 盘后汇总),**⛔ 不要硬编 kind 清单** —— 权威在服务端 `notify_kinds.py`,客户端硬编一份必然漂移;新增 kind 时客户端应当**不改代码就能显示出来**。
    - **`PushCategory`:六个 → 三个**(`NKIMMEDIATE` / `NKIMPORTANT` / `NKDIGEST`),`PushManager` 的 category 注册同步换。
    - **通知路由改按 payload 里的 `kind` 分发,不再按 category 分发** —— category 只决定系统层的呈现分组,**业务分支一律读 `kind`**(按 category 分支会连坐,正是 V1 拆 `HOLDINGALERT` 的教训)。
    - **未知 `kind` 必须优雅降级**:服务端日后加 kind 时,旧 App 收到不认识的 `kind` 应**照常显示标题正文**(用 payload 自带的 `label`),**⛔ 不许静默丢弃**(v1.5 「Swift 未知 `status` 静默消失」的同类坑,🔵-2 修过一次)。
  - **🆕 ⑮ 硬清单 · 删掉五处对已删端点的活调用(2026-08-03 由 V2 review 契约线 Y5 带出;⛔ 逐个删,不是"顺带")**:`APIClient.swift` 的 `POST /decisions/{id}/link` / `cancel` / `revise` / `scenario-outcome` 四处 + **`PUT /settings/llm`**(请求体 `SettingsLLMRequest` **含明文 `apiKey`**,定义 `:210`、调用点 `AppModel.swift:857`)。
    - **为什么单独点名而不是等 ⑮ 自然覆盖**:D2=A 路下「老 App 打老机」是**合法过渡态**,但**当前仓库构建出的客户端对 V2 服务端是五处 404 / 静默失败**;其中 LLM 设置那条尤其糟 —— **它采集了用户的 key,发到一个不存在的端点,界面上还是一副成功的样子**。这不只是"没接线",是**假成功面 + 明文密钥打进空洞**。
    - **处置**:四处决策日志调用随 ⑩-C / ⑬-5 一并删;`SettingsLLMRequest` **整个退役**,换 `/settings/providers*`(⑮ 设置屏本来就要改)。
    - **⑭-C 加一条对拍断言防复发**:**「客户端调用面 ⊆ 服务端路由面」** —— 三方对拍脚本里做成机器判据,别靠人眼扫。
  - **🆕 `POST /positions` 的幂等键由客户端生成,规则定死(2026-08-03,契约线复核 🔵 增量归 ⑮;语义文档见 `archive/V2_契约三方对拍_20260803.md` §十一)**:**每笔新交易生成一个新键(如 `UUID` per 提交),⛔ 严禁复用**。⚠ **为什么是硬要求**:服务端是**标准幂等语义** —— **同键 = 同一笔意图的重试**,于是**同键不同 payload 会静默重放原仓、丢弃新参数**(`replayed=true` 有透出,但用户看不懂)。**客户端复用键 = 用户改了价格数量再提交,系统当作重试、把改动整个吃掉**。⛔ 别把键绑在"这只票 + 今天"之类的业务量上(那必然复用);**绑在"这一次提交动作"上**。
  - **NL 提醒的五个端点已由 ⑪-C 落地**(`GET|POST|PUT|DELETE /alerts` + `POST /alerts/parse`),⑮ 直接接线;确认卡**七项必须齐**(含**行情延迟披露**这一必选项与「只通知不自动交易」固定尾巴),⛔ 不许为了界面清爽省掉任何一项。
- **删**:自选 tab、呼吸台账 tab、决策日志必填表单、候选四件套 / 三件套展示位、板块常驻设置。

**Swift 坑清单(CLAUDE.md,逐条守)**:手写 `init(from:)` 的 DTO 区分 `try c.decode`(必需)与 `decodeIfPresent`(可选);「键必须永远出现、nil 编成 JSON null」的字段必须手写 `encode(to:)`;**冻结快照类 DTO 必须 `decodeIfPresent` 兜底**;
- **🆕 点名两个冻结快照 DTO(2026-08-02 由 ⑦ 完工记录带出,别用合成 `Codable` 偷懒)**:**`basket_cards.card_json`**(⑦ 冻结,34 个顶层键 + 26 个成员键,`spec_version` / `verification_ruleset_version` 会随版本变)与既有 **`reviews.result_json`** —— 两者都是「**写入当时冻住**」那一类,**服务端升级永远不会给老快照补新键**,故**必须手写 `init(from:)` + 全字段 `decodeIfPresent` 兜底**。⛔ 用合成 `Codable` 的后果是:装了新 App 的用户翻看几周前的老卡 → **整张卡解不出**。判断依据见 CLAUDE.md「落库快照按是否随每次响应重新拼装分两类」——`card_json` 属于**不重新拼装**的那一类。
- **`card_not_ready` 的 `mapReason` case 必须加**(⑭-B 已登记),文案「本篮的卡还没生成」,**不要写成「篮子不存在」**。`board` 是英文枚举码、中文展示走客户端换算;网络桩 `httpBody` 常为 nil,断言请求体走 `httpBodyOrStream()`;`NecklineTests` 只在 iOS Simulator destination 跑得动(macOS 只跑 `build`)。

**QA 钩子**:沿用 `NECKLINE_INITIAL_TAB` / `NECKLINE_INITIAL_MODAL` / `NECKLINE_INITIAL_INFOCARD_CODE`;新增 `NECKLINE_INITIAL_BASKET_ID`(**放在 `refresh()` 数据到位之后触发**,不能塞进 `init()`)。视觉核对走 `xcrun simctl io <device> screenshot`;长页用临时 iPad(用完 `simctl delete`)。

**⚠ 版号是 ⑮ 的活,而且是三方同一次提交(2026-08-03 核准 ⑭ 完工记录设计判断 4;⑮ 施工中,点名提醒)**:⑭ **刻意没把 `api/app.py::VERSION` 从 `v1.5.2` 升到 `v2.0.0`** —— 守门单测要求它与 `client/project.yml` / pbxproj 的 `MARKETING_VERSION` **三者恒等且必须同一次提交动**,⑭ 提前升会让守门单测**常年红**。**✅ 判得对,版号归 ⑮。** ⚠ **⑮ 别只改客户端那两处** —— **`api/app.py::VERSION` 是这三方里的第三方**,漏了它守门单测立刻红。⚠ 当前状态如实登记:**服务端跑的是 V2 契约但 `/health` 仍报 `v1.5.2`**(纯本地未部署,⑯ 上云前必须已是 `v2.0.0`)。

**依赖**:⑭。
**验收**:iOS + macOS `xcodebuild build` 双端 **BUILD SUCCEEDED**;iOS Simulator `xcodebuild test` 全绿;`MARKETING_VERSION` = `2.0.0` 三方一致(`project.yml` + pbxproj + `api/app.py::VERSION`,版本号守门单测);语义红线全仓 grep 零命中;截图核对篮子卡 / 成员卡 / NL 确认卡 / Provider 设置四处。

---

#### ✅ V2-⑮ 完工记录(2026-08-03,@builder-pro;纯本地代码 + 双端构建 + 模拟器截图核对,零服务器 / 零部署)

**三笔提交**:`0ed277c`(契约层:DTO + APIClient)/ `dcd90cf`(D8 信息架构 + 界面)/
`e676967`(欠账销账 + 版号三方)。

**① 四板块落地形状(D8,不新增 tab)**

| 板块 | 内容 | 关键文件 |
|---|---|---|
| **今日篮子** | 报告**五段**:① 情绪与市场语境 → ②(持仓体检**指过去**,不重画)→ ③ 今日篮子 T1/T2/T3 每篮一张卡 → ③b 未定档 → ④ 昨日复盘 → ⑤ 数据新鲜度与降级披露(**恒在**) | `BasketDailyView` + `BasketCardView` |
| **持仓** | 极简台账(三字段录入)+ **同题材合并敞口** + 计划继承卡 + per-position 触达提醒开关 + 临时提醒 + **盘中动态**(原「盘中看板」并入) | `PositionsView` + `PositionExtras` + `BoardSection` |
| **问询台** | 主体不动(⑬-11 的「+自选」按钮此前已随自选池整链删除) | `InquiryView` |
| **设置** | Provider 注册表增删改(**key 只写不回显** + 删除二次确认)+ 任务路由表 + **按 kind 动态渲染**的推送开关(按 level 分组)+ App/服务端双版本行 | `SettingsView` |

macOS 侧栏 = 同四板块 + **周复盘工作台**(既有对账保留,⑮ 补上 ⑫ 画像两张账 + ⑨-C 评价校准报告)。

**② 欠账销账证据(⑭-C 对拍表 §六,6 组逐项打勾并注了落在哪一笔)**

- **两个「已登记的债」集合双双清空**:`PENDING_CLIENT_CALLS_TO_BE_REMOVED_IN_15` / `PENDING_MAP_REASON_CASES_FOR_15`。⚠ **空集合是本闸最严的状态**(两条断言都用 `==`),不是"闸松了"。
- **两条 skip 转跑**:`test_frozen_snapshot_dtos_hand_write_init_from_decoder[BasketCard|BasketReview]` —— `tests/test_contract_crosscheck.py` 从 **7 过 + 2 skip** 变 **9 过 + 0 skip**。
- **三处硬失败已改完**:`ReportResponse.candidates` 删属性换 `basketDaily`;`PushSettings` 六 bool 换 `kinds[]`;`SettingsSnapshot` 换 `providers[]` + `routes`。
- **五处活调用物理删除**(含 `PUT /settings/llm` 那条**采集明文 key 打进空洞**的),连同 `SettingsLLMRequest` 等五个请求体类型与对应 UI。

**③ 六处如实登记的设计判断(Plan 未给精确形状,已按最贴合上下文的解读实现,均未改 Plan 原文)**

1. **「盘中看板」不再是 tab,内容并入持仓板块**:D8 把 iPhone 板块**精确列成四个**且写死「不新增 tab」,而 V1 的看板是第五个;⛔ 删数据不可接受(退潮红条 + 哨兵事件是纪律露出位)。取「换挂载点、不删内容」——V2 注意力分配是 80/15/5,盘中动态本就是为解释持仓服务的。⚠ 若 planner 本意是保留独立看板 tab,请澄清。
2. **「② 持仓体检」在今日篮子页只给一行入口,不重画**:D8 已把持仓拆成独立板块,同一份数据画两遍会让用户在两处看到可能不同步的两个版本。报告五段的**顺序与编号原样保留**(点进去仍是那一段)。
3. **409/422 的五个 reason 选「扩 `mapReason` 调用点」而非就近处理**(⑭-C §六 C3 给了二选一):就近处理会让同一个 reason 在不同调用点拼出不同文案,那正是这张表想消掉的漂移。故 `send()` 的 409/422 两个分支也走 `mapReason`,五个 reason 各建独立 `APIError` case。
4. **A 类 DTO 也全部手写 `init(from:)`**(对拍表只要求 B 类):Swift 合成 `Decodable` 对**非 Optional 属性不会因为声明了默认值就容忍缺键**(本仓多处注释早已记过),A 类手写是白拿的保险,且让测试 fixture 不必逐字段补全。
5. **持仓卡上的「决策日志回显 / 情景勾选 / 修订入口」整块删除,不保留只读态**:服务端四个写端点已删(⑩-C),留着按钮就是**假成功面**;`GET /decisions` 的只读归因入口保留在 `APIClient`(`listDecisions`/`decisionTrack`),但本版界面不挂 —— 归因是周复盘/事后的事,不是持仓卡的事。⚠ 若希望在 macOS 工作台补一个历史决策只读表,这是可加项。
6. **`entry_reason` 与 `buy_fees` 从「UI 强制必填」降为可选**:⑩-A 原文「买入 = 票 + 价 + 量」「买卖录入控制在数秒内、不再要求长表单」,而 V1 的 `PositionEntryForm.isValid` 强制了理由 + 费用。已改成三字段即可提交,费用留空时服务端照旧走默认佣金率估算并**诚实标注为估算**。

**④ 幂等键(契约线 🟡 Y7;planner 施工中追加,已照办)**:`AppModel.entryIdempotencyKey` 在 **`beginPositionEntryFlow()` 时铸一枚新 UUID**(= 绑在「这一次提交动作」上),提交失败重试**复用同一枚**,下次进入录入流程才换新的。⛔ 严禁跨提交复用、⛔ 未绑「票 + 日期」之类业务量。单测两条:`testIdempotencyKeyIsFreshPerEntryFlow`(两次独立提交键不同)、`testIdempotencyKeyReusedAcrossRetriesOfSameSubmission`(同次重试键相同);另有 `replayed=true` 时**如实提示「未重复开仓」**的一条(⛔ 不让"看起来成功了"掩盖"其实什么都没发生")。

**⑤ 版号三方**:`api/app.py::VERSION` `v1.5.2` → **`v2.0.0`**;`client/project.yml` 与 `pbxproj` 的 `MARKETING_VERSION`(两处,Debug/Release)→ **`2.0.0`**。**同一笔提交 `e676967`**,守门单测 `test_client_version_governance` 绿。⚠ 生产 `/health` 仍是 v1.5.2 —— 因为至今零部署,⑯ 上云后才会对上。

**⑥ 截图核对清单**(本环境 computer-use 点不动模拟器 → 走 `xcrun simctl io screenshot` + QA 钩子;长页用**临时 iPad**,用完 `simctl delete` 已收掉):

| # | 核对点 | 结论 |
|---|---|---|
| 1 | 四 tab 形状 | 今日篮子 / 持仓 / 问询台 / 设置,底部 TabBar 四项 ✅ |
| 2 | **篮子卡** | 11 项齐;T1/卡 v1 徽标、共同驱动、证据链(来源 + 日期)、为什么是现在、成员 2(角色分歧 1 只) ✅ |
| 3 | **成员卡** | **角色两说并存**(`LLM:龙头` + `机械:跟随` + 「两说并存」标)✅;三个参考件齐,**「离场参考区间(不是止盈线)」** ✅;**「参考、非指令 · 不进排序、不进哨兵、不改去留、不加分」恒显** ✅;K7 标注件文案原样透传 + 「判不了的标注」独立成句 ✅ |
| 4 | 空档位 / 卡未就绪 | T3「军工换装」在档且显示**「本篮的卡还没生成」**(⛔ 不是「篮子不存在」)✅ |
| 5 | ③b 两个原因码 | 「档位已满 · 今天机会多到装不下」(绿)vs「未过质量线 · 今天没什么好货」(黄),**分开展示** ✅ |
| 6 | ④ 昨日复盘 | 详复盘带 LLM 正文 + 参考标注;T3 简评**未生成**时如实标 `llmSkipReason` ✅ |
| 7 | **NL 确认卡** | **七项逐项渲染**(① 标的 …⑥ 行情延迟 / 数据中断 ⑦ 只通知不自动交易),模型复述带「参考、非指令」✅ |
| 8 | **Provider 设置** | 三个 provider,只显示 **key 已配 / key 未配**(⛔ 无明文)、带联网检索 chip、已停用 chip、编辑 / 删除 / 新增 + 任务路由表 ✅ |
| 9 | 持仓页 | **同题材合并敞口**(稀土订单链 2 只 · 成本 ¥35660)、per-position 计划继承行(「来自「稀土订单链」」vs「独立买入 · 没有来源篮子可继承」)、临时提醒 2 条 ✅ |
| 10 | 极简录入 sheet | 「三字段即可提交」+「可选补充」两段 ✅ |

⚠ **截图核对期间修的一处实机问题**:持仓卡的纪律位 chips 与两个动作按钮挤在一行,iPhone 宽度下被压成竖排断字 —— 改成 chips 横向滚动、按钮独占一行。

**⑦ 测试与构建**

- Python:`python -m pytest tests/ -q` → **2890 passed + 2 skipped**(2 skip = 需 `NECKLINE_RUN_6Y=1` 的六年真回测)。⚠ 施工中见过一次 `test_report_consistency` 随机序偶发红,**单独跑与再次全量跑均绿**,是既有的用例间顺序敏感,与本块无关。
- Swift:`xcodebuild test`(iOS Simulator)→ **179 passed + 12 skipped**(skip = 未起 dev 后端的真实联调冒烟)。⚠ `NecklineTests` 只在 iOS Simulator destination 跑得动(既有工程设置,与本块无关)。
- 构建:**iOS Simulator / macOS × Debug / Release 四路全 `BUILD SUCCEEDED`**。
- 语义红线 grep:客户端非测试代码中「推荐买入 / 建议买入 / 看好 / 值得买」**零命中**;「止盈线」只出现在**否定句**里。

**⑧ 新增的 QA 钩子(纯截图辅助,缺环境变量时行为逐字节不变)**:`NECKLINE_INITIAL_BASKET_ID`(Plan 点名)、`NECKLINE_INITIAL_ALERT_TEXT`/`NECKLINE_INITIAL_ALERT_CODE`(开 NL 确认卡要先发一次异步解析,同样够不着 `init()`)、`NECKLINE_SKIP_PUSH_PROMPT`(推送授权弹窗会盖住页面,而本环境点不动模拟器)。前两个放在 `refresh()` 数据到位之后触发,承 v1.4-⑧ 先例。

**⑨ Plan 疏漏 / 待澄清(不拦后续块)**

1. **⑮ 没说「盘中看板」怎么办**:D8 只列了四个板块,删的清单里也没有它。已按「换挂载点、不删内容」处理(见判断 1)。
2. **推送开关的「三级分组」在服务端 `level` 之外没有兜底约定**:已实现为**未识别 level 自成一组照常显示**(⛔ 不静默丢弃),与「未知 kind 优雅降级」同一条纪律,但 Plan 未明写这一层。
3. **`GET /decisions` 只读归因入口在 V2 界面上没有落点**:⑩-C 说它"保留为只读归因入口",但 ⑮ 的四板块里没有任何一处提到它。已实现为「客户端方法保留、界面不挂」(见判断 5)。

**依赖**:⑭。**下一步**:【门禁】用户判定点(是否召唤 @reviewer)→ ⑯(需用户先购机 + 解析 `nk`)。

---

### 【门禁 · 非施工块】用户判定点:是否召唤 @reviewer

- 位置与范围**由用户定**,plan **不预设、不自动排程**。可选范围建议:**⑥⑦⑧⑪(判定线)** 与 **⑭(契约线)** 两线并行,照 v1.4 / v1.5 两线体例。
- **⚠ 判定点 · K7 需求 3(B3 黄牌退役,⑯-I)**:策略线交接稿**明确建议为它叫一次 review**(理由:🔴 高危 + advisory 载体变更 + 与包激活同窗执行)。**planner 照项目规矩把它记为一个用户判定点,不自动排程** —— 要不要叫、什么时候叫、范围多大,由你一句话决定。若叫,建议范围 = **⑯-I + ⑯-E 两步激活 + ⑥ 的五维定义与权重消费**(它们是同一批「策略口径换代」的三个面)。
- ⚠ ⑭-C 的契约三方对拍是**施工块内自查**,**不等于**独立 review。

---

### V2-⑯ · 新服务器 + 晚间管线多段化 + 迁移割接 + 两步包激活 + B3 退役 + Tier 预填充(🔴 @builder-pro + 用户)

> **⏱ 本块是尾声块(2026-08-02 用户追加指示,写死)——「云上组件迁移放在尾声」。**
> - **两条硬前置,缺一不启动**:① **①–⑮ 全部完工**;② 用户已购好新云服务器并交付访问方式(§八 第 13 项)+ `nk.linotsai.top` 已解析到新机(§八 第 14 项)。
> - **⛔ 不与前面任何块并行穿插**:在 ⑮ 完工之前 **不碰任何服务器**(新机老机都不碰)、不搬数据、不改 DNS、不部署;①–⑮ 期间的一切验证走本地隔离库 + 本地 parquet + 单测 + `xcodebuild`。
> - **📦「本地可先备料」的纯本地子项(留在本块内、不拆走,但允许在 ①–⑮ 期间顺手写好)**:⑯-D 的三个 oneshot `deploy/*.service` + `*.timer` 单元文件、⑯-G 的 nginx 站点模板、⑯-E 的 `scripts/activate_pack.py` 与 `packs/K4-pack.json`(③ 已产出)、⑯-F 的 `scripts/oneoff/preseed_baskets.py` 与其校验逻辑、⑯-B/H 的迁移检查清单脚本。**写好即可,一律不在服务器上跑**;到本块才上机执行。
> - 备料子项在下文各自条目里标 **📦 本地可先备料**。

**⑯-A 新机准备**:规格 D3(≥4 vCPU / 8G / 100G SSD,按 ⑯-C 实测复核);Python 3.11+ venv、阿里云 PyPI 镜像;**属主纪律复刻**(nologin 用户 `neckline`、`/opt/neckline` `deploy:neckline` setgid 2770、`.env` / `.p8` / `neckline.db` 600 `neckline:neckline`);`.env` 与 `.p8` **手工拷贝、绝不入 git、rsync 永不覆盖 `data/`**。
**⑯-B 数据搬家**:parquet 全量 + `neckline.db`(搬前 `.backup` + `cp -p` **双备份**,搬后 `PRAGMA integrity_check=ok` + 业务表行数逐表比对 + `is_active` 仍 `v1.3.3` 且 `activated_at` 未变);`sync_data.sh` / `sync_code.sh` 复用(dry-run 先验、零删除、收尾属主自检)。
- **🆕 迁移清单加一条(2026-08-03,契约线复核 🔵 增量)**:**新机首次 `init_schema` 之后,grep 启动日志确认「零条『唯一索引建不上』WARNING」**。**为什么要专门查**:`_POST_MIGRATION_INDEXES` 在脏库上建不上索引时是 **WARNING + 跳过**,于是**幂等键与「单现役」两条约束在那个库上持续不设防**,而 WARNING **只在 `init_schema` 那一刻打一次**、常驻服务只在启动时能看见 —— **不专门查就等于没有这两条约束,而且没人会知道**。查到有 → 先清脏数据再重跑,⛔ 不许带着跳过的索引上线。
**⑯-C 性能实测(P0-23 纪律平移,硬门禁)**:④ 三张新表的 `bootstrap` 与日更增量、⑨ 复盘引擎、⑤⑥ 的 LLM 段、⑧ 存拍落盘 —— **每一项在新机上 `systemd-run --scope -p MemoryMax=… -p CPUQuota=…` 隔离单进程、串行**计时 + 量峰值,**达标才准进主链**。探针纪律照旧:只在收盘后 15:00 之后跑、避开日更与报告窗口、跑完 `pgrep -af` 确认无残留 + `reset-failed`、**`load > 4` 立即停手**;`pkill -f` 前先确认 pattern 不匹配自己。
**⑯-D 晚间管线多段化(📦 单元文件本地可先备料)**:三个 oneshot **串行**、各自 `MemoryMax`。~~`neckline-scan.service`(④)/ `neckline-basket.service`(⑤⑥⑦ + **报告**)/ `neckline-review.service`(⑨ + ⑫ 周度部分)~~ → **❌ 这个切法我写错了,2026-08-03 改写(⑭ 实现打出来的)**:它把**报告排在 ⑨ 复盘之前**,而报告的 ④「昨日篮子复盘」节要读 `basket_review_daily` 的当日行 —— 照原切法**那一节会常态显示「今日无昨日篮子可复盘」**。⑭ 已按「⑨ 在前、报告在后」实现,**本节改口径与之对齐;⛔ 不动代码**(`run_evening_chain` 的 `segments` 参数本就支持任意切法,链序固定不由 systemd 决定)。
- **✅ 新切法(三段,按「资源画像」而不是「功能名称」切)**:
  1. **`neckline-scan.service`** = **⑧ EOD 验证拍 + ④ 扫描层 + ④b**。**这一段是纯机械批算、吃 parquet 全表扫描 = 内存最重的一段**,`MemoryMax` 主要卡在这里(P0-23 的正主)。
  2. **`neckline-basket.service`** = **⑤ 聚合 + ⑥ Tier + ⑦ 卡冻结**。**这一段是 LLM 段**,资源画像是**墙钟 / 网络**而不是内存 —— 单独成段才能给它长超时**而不必把长超时也给批算段**。
  3. **`neckline-report.service`** = **⑨ 复盘 + 报告落库**。
     - ⚠ **为什么这两个必须同段(这才是改口径的真正收益)**:「⑨ 在报告之前」是一条**语义依赖**;把它们放进**同一个 unit**,这个顺序就由链内固定序保证、**systemd 侧根本无法配错**。若拆成两个 unit,任何人改一次 `After=` / `Wants=` 就能把顺序弄反,而**症状是报告 ④ 节静默变空、不报错** —— 正是最难查的那类故障。**把约束关进一个 unit 里 = 让它结构性不可违反。**
     - 复用 V1 已有的 `neckline-report.service` 名字(它本来干的就是这件事),不新造 `review` 这个名。
- **✅ ⑫ 的周度部分从日链摘出去**(原文把它挂在 `neckline-review.service` 上):**它是周频作业,不该跟在 16:35 日链后面每天跑一次**。按 ⑫ 的实际触发方式(交割单上传后 / 周末 timer)**单独排一个 unit**,⑯-D 不替它决定形态。
- **✅ ④ 扫描层的双挂载,拆分时二选一 = 摘掉 `daily_update` 那份、留链里的**(采纳 ⑭ 完工记录设计判断 3 的建议)。**理由**:⑤ 直接依赖 ④ 的**当日**产出 —— 留在链里,**链自己就能保证「⑤ 读到的是今天的」**;留在 `daily_update` 则是跨进程隐式依赖,`daily_update` 那一步失败时**链会拿昨天的扫描表当今天用而且不报错**(④ 在 `daily_update` 里是「尽力而为不改退出码」),正是「静默用陈旧数据」这一类最难查的故障。移进链里还顺带让它有了明确退出码。⚠ **这一步在 ⑯-D 拆分时才做,⛔ 现在不动** —— `daily_update.py` 是已经在生产 timer 里跑的脚本,⑭ 不擅自摘是对的。**重活一律不进常驻 `neckline.service`**(它与盘中哨兵同进程,`MemoryHigh` 先节流会让进程陷进回收死循环 = 卡死不报错)。`neckline-daily.service`(16:05 拉数)保留;时刻表按 ⑯-C 实测排,总窗口写进 `deploy/` 单元注释。
**⑯-E 策略包激活 —— 两步,顺序定死(📦 脚本与两个包文件由 ③ / ③-K7 产出,本地已备好)**
- **第 1 步:先激活 `K4-pack-v1`**(`python scripts/activate_pack.py --file packs/K4-pack.json`,先演练 → `--confirm`)。**为什么明明要用 K7 还先激活它**:① 它是**回滚锚** —— `activate_pack.py` 的回滚定义是「激活旧版本行」,库里必须**真有那一行**,否则 K7 出问题时无处可退;② 顺带在生产库上把四道闸与激活事件流走通一遍。
- **第 2 步:再激活 `K7-pack-v1`**(同样先演练 → `--confirm`),它成为**上线现役包**。验:`selection_packs` **两行**、`is_active` **唯一在 K7-pack-v1**、`selection_pack_activation_log` **三条事件**(K4 激活 1 + K4 停用 1 + K7 激活 1,顺序为先 deactivate 旧、后 activate 新)。
- ⚠ **两个包文件在 `--confirm` 那一刻内容就冻了**(append-only 完整性闸:同版本号内容被改会被拒)。因此**⑯-E 之前是最后的改包窗口** —— ③-K7 若还想调 K7-pack 的任何数,必须在这一步之前调完,之后只能发新版本号。
- ⚠ **激活 ≠ 改章程**:`selection_packs` 与 `strategy_versions` 是**两条版本线、两张表、两套激活流程**;本步**不碰 `strategy_versions`、不跑 `activate_charter.py`**,现役章程恒 `v1.3.3`。
**⑯-F Tier 预启动填充(裁定 #4;📦 脚本与校验逻辑本地可先备料)**:`scripts/oneoff/preseed_baskets.py` —— 读外部准备好的近期若干交易日篮子/Tier/卡(V2 格式)→ **过校验**(JSON Schema + 夹逼 + 成员白名单闸 + 角色对拍)→ **默认演练、`--confirm` 才写、写前双备份**;行标 `baskets.via='preseed'`。**预填数据与自转数据同表同格式**,评价引擎一视同仁打分(`via` 保证日后统计可分层)。
- **顺序**:排在 **⑯-E 两步激活之后**(先有现役包,再灌数据)。
- ⚠ **`baskets.pack_version` 对 preseed 行填字面量 `'preseed'`,不许填 `K7-pack-v1`**(planner 定案,K7 需求 4 「激活时机与 preseed 的关系」的回答):preseed 篮子是**人工 + LLM 在外部配的**,不是引擎按 K7 包算出来的;填成 K7-pack-v1 会**污染 §12.5 的按包归因**(⑨-C 会把人工配的成绩算到 K7 头上)。`via='preseed'` 已能分层,但**字段不许撒谎** —— 两处都标。⑨-C 的按包归因把 `'preseed'` 当**独立一档**统计。
**⑯-G 域名与割接(D2 = A 路,已拍板,无备选;📦 nginx 站点模板本地可先备料)**:新机 nginx 站点**只服务 `nk.linotsai.top`** + certbot 证书 + 定时续期。**DNS 的 A 记录由用户自己解析到新机**(§八 第 14 项;builder 只在解析生效后申请证书,`dig nk.linotsai.top` 拿到新机 IP 才是可以往下走的判据)。老机 `ln.linotsai.top` 与 `linon` 遗留物**一律保持不动**(不停机、不改代码、不改解析)直到用户完成 ⑰ 换包并确认,再由用户决定老机退役与数据归档。⚠ **新机 nginx 绝不接管 `ln.linotsai.top`** —— 一旦接管,老 App 就会撞 V2 契约,A 路的前提当场破掉(⑭-D)。

**⑯-I · K4 advisory 的 B3 黄牌退役(K7 需求 3;🔴 高危;执行窗口 = 与 ⑯-E 同窗;📦 脚本 + 单测 + 演练本地可先备料)**

> 来源:`archive/交接_K7_系统线需求_20260802草案.md` 需求 3(**用户已裁定:按 H11 结论办**,证据链 `research/k7_pre_report.md` H11)。

- **改什么(只此一项,不扩)**:`k4_advisory` 的 **B3「题材持续 2-3 天」黄牌停用**(从 `avoid_flag` 清单里摘掉)。
  - 理由:H11 证据下 **2-3 天(发酵态)在当前 regime 是最优注意力段**,原「被认可 = 接盘侧」的判决**主要由样本内年份驱动**。
  - ⛔ **A2 红牌(题材持续 ≥4 天硬回避)不动** —— 过热态双尾最重,H11 再确认。
  - ⛔ **不新设「绿牌」机制** —— 正向偏好由**排序侧**承接(K7-pack 的 `industry_stage_score` 五态序,③-K7-B),不在 advisory 里造第三种牌。
- **🔴 但这不违反红线,先看清楚**:`k4_advisory` 住在 `strategy_versions` 的 **K4 行**,而 **K4 行 `is_active=0`、永不激活**(§七 📌-22 已定案);**现役章程是 `v1.3.3`,本项一个字节都不碰它**,也不碰任何 config 阈值。改的是「**建议载体**」不是「**纪律**」。**改完 K4 行仍 `is_active=0`、`activated_at` 不动、`strategy_activation_log` 不增行。**
- **怎么办(留痕体例,复刻 `scripts/oneoff/bootstrap_k4.py`)**:
  - 新一次性脚本 `scripts/oneoff/retire_k4_b3.py` + 独立 payload 文件(照 `bootstrap_k4_payload.json` 体例);**默认演练**、`--confirm` 才写、**写前 `.backup` + `cp -p` 双备份**、单事务。
  - **写前写后各 dump 一份 `rule["k4_advisory"]` 全文**,diff 落 `archive/K4_advisory_B3退役_2026MMDD.md`(**只此一处存全文**,§九 一行 + 链接)。
  - **幂等**:B3 已不在清单里 → 演练报「无需变更」、`--confirm` 也是 0 改动(重复执行安全)。
- **代码侧连带(DB 是唯一源,镜像只作文案对照)**:`report/holding_k4_check.py::_HIT_META` 里的 B3 条目按既有纪律处理(**要么删、要么标停用**,builder 定,但**不许让镜像与 DB 讲不同的话**)。**守门单测**:改后跑一遍持仓体检 / 安检 → **断言 B3 不再产生任何黄牌**,且 **A2 仍照常命中**(防误伤)。
- **执行窗口**:**与 ⑯-E 的包激活同窗**(交接稿建议,采纳)—— 排序侧的正向承接(K7-pack 现役)与建议侧的 B3 停用**同时生效**,避免出现「B3 已停但排序还没接上」或反之的中间态。
- ⚠ **用户判定点**:交接稿建议为本项叫一次 review。**按项目规矩,review 由用户点名,plan 不自动排程** —— 见下方【门禁】节。

**⑯-H 部署当次必须留的证据(工程门禁,照既有体例逐项;**刻意排在最后** —— 它是收尾动作,A→G 与 I 全部做完才收证据)**:双备份文件名 + 大小 + `integrity=ok` + `.backup` 耗时;`sync_code.sh` dry-run **零删除**;关键文件 **sha256 与本地逐个吻合**;preflight import(`VERSION=v2.0.0` + 路由数 + 去重路径数);迁移后 `integrity_check=ok` + **业务表行数逐表一致** + `is_active` 仍 `v1.3.3`;重启中断秒数 + `NRestarts=0` + journal error 0 条 + `memory.events` 全零 + idle RSS;公网 `/health` = **`v2.0.0`**;三个 oneshot 各自 **`ExecMainStatus=0` 且 `ExecMainStartTimestamp` 是本次那一跑**(⚠ 铁律:`list-timers` 的 LAST 与 `Result=` 都不可信);`~/hz_info.md` 同步更新(新机事实,只写非敏感)。**回滚绳** = 双备份 + 上一版代码态 git 号 + 老机仍在跑。

**依赖**:①–⑮ **全部完工**(尾声块,不并行穿插)+ 用户已购机 + `nk` 已解析到新机。
**验收**:⑯-H 逐项证据齐;**⑯-E 两步激活后 `selection_packs` 两行、现役唯一在 `K7-pack-v1`、事件流三条**;preseed 行 `via='preseed'` 且 `pack_version='preseed'`、格式与自转数据同构;**⑯-I 的 B3 退役 diff 已落 archive、K4 行仍 `is_active=0`/`activated_at` 未变/`strategy_activation_log` 未增行、B3 零黄牌且 A2 仍命中**;`strategy_versions` **现役仍 `v1.3.3`**;三段 oneshot 串行跑通且各自 `ExecMainStatus=0`;公网 `/health` = `v2.0.0`。

---

### V2-⑰ · 双端换包(macOS + iOS)—— **末块,等用户指令**

- **macOS**:旧包先 `ditto` 备份 → **`ditto`**(⛔ 禁 `cp -R`)装 `/Applications/Neckline.app`;验 `CFBundleShortVersionString=2.0.0`、universal、`codesign --verify --deep --strict` valid + Designated Requirement、产物与安装态可执行 sha256 逐字节一致。
- **iOS**:**用户 Xcode 直连真机安装**(builder 无真机权限),步骤照 §八 第 12 项;若报 `Entitlement aps-environment not found` → 回 §八 第 5 项去 Apple Developer 网页为 `top.linotsai.neckline` 勾 Push。
- ⛔ **builder 不得自行换包**;⑯ 完成后不自动进入本块。
- **位置定死在 ⑯ 之后**(D2 = A 路已拍板:老 App 打老机、不会撞 V2 服务端,故换包无需前移)。~~D2 若走 B 路则本块前移~~ **已否决,不适用。**
- **换包完成并经你确认之后**,老机 `ln.linotsai.top` 才进入退役讨论(退役与 `linon.db` / `neckline.db` 老库归档由你决定,§八 第 8 项)。

---

### V2 验收标准(产品级 + 架构级,合并一处)

**产品级(蓝图 §十一 全文,逐条)**
1. 选股结果以**股票篮子**呈现,不再是一批高度重复的单票;2. 每个篮子有明确共同驱动、成员角色和失效条件;3. Tier 数量不强制填满、分层理由可解释;4. D0 判断冻结、D+1 不因结果回写历史;5. 每日固定复盘前一日 T1/T2,T3 保留对照;6. 用户买入后自动进入持仓模块,不中断选股模块的独立评价;7. 选股与持仓可互相读取和追加,但不能相互篡改;8. 买卖录入控制在数秒内、不再要求长表单;9. 交割单能够修正手续费和真实资金记录;10. 持仓通知聚焦持仓、大盘和相关篮子,避免无关噪声;11. 自然语言提醒由确定性哨兵执行、永不自动交易;12. 偏好与能力分开统计,不因用户喜欢某类股票就不断迎合;13. 能够长期判断 Tier 是否有效、用户选股是否跑赢未选候选。

**架构级(另加)**
- 篮子卡的每个数字**要么机械算出、要么过机械闸**(夹逼 / 白名单 / 对拍),**无裸奔 LLM 数字**;
- Tier 定档**可完整复现**(机械分落表 + LLM 同档微调留痕);
- **评价引擎与篮子引擎同日上线**,D1 起出数;
- **双 Provider 任一故障,报告主体仍产出且缺席段诚实披露**;
- **冻结 / 追加 / 不回写三律在全部新表上有守门单测**;
- **第〇原则四锁(§2.8-C 的 V2 表述)在全域重挂**;
- **策略包轮替不改主链一行代码**(校验 → 入库 → 激活三步即完成换包),且选股主链从上线日起就读现役包行(**插槽不是空架子**)。
- **(K7 追加)机械分五维每一维都可复现、可解释**:`driver_freshness` 的六态分值来自现役包 `stage_scores`(代码里无第二份)、`leader_clarity` 的名次与三级 tie-break 落表可查、缺数据一律走保险丝且**「没有」与「没看」分得开**;
- **(K7 追加)标注件全域不越权**:`pullback_leader` / `warn_streak_top` / `warn_chase_zone` 三个标签**不进排序、不进 Tier 机械分、不进哨兵、不改去留**,打标前后同一份数据的 Tier 序与成员集合逐位不变(守门单测);
- **(K7 追加)评价引擎能回答「是否稳定优于随机与不作为」**:周报含随机同规模篮子臂与满仓持有基准臂,种子可复现、判分复用同一套 `exit_sim`,样本不足时只报样本数不报结论。

---

## 五(旧-v1.5)、v1.5.0 → v1.5.2 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.5_施工图_20260802归档.md`(① 参考件三件套地基 / ② LLM 20 只全覆盖与预算 / ③ 卡面改版与老四件套退役 / ④ 存量配菜四件 / ⑤ 客户端跟进 + 版本号治理 / ⑥ 部署上云 / ⑦ 双端换包 / ⑧ review 黄牌修复 → v1.5.1 / ⑨ 提示词日期锚快修 → v1.5.2,**含「v1.5 LLM 预算账」与「v1.5 客户端契约清单」全文,以及交接时的 §四 v1.5.2 收官快照**)。
> v1.5.0 → v1.5.2 全部已上云;现役章程 `v1.3.3`(v1.5 未动章程)。**查 V2 之前的客户端字段一律去该归档文件**,本正文不复述。
> **⚑ 交叉引用约定**:本文件 §一~§三、§七 里出现的「§五 v1.5-①…⑨」指针,**一律指向本存根 → 该归档文件**。



## 五(旧-v1.4)、v1.4.0 + v1.4.1 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.4_施工图_20260729归档.md`(① P0 地基 / ② 行业强度单一源 / ③ 正选漏斗排序键三级 / ④ 信息卡与考卷同构 / ⑤ exec_hint + 决策日志第⑨项 / ⑥ 判定精度 / ⑦ 功能补全 / ⑧ 客户端跟进 / ⑩ P0-23 预计算落表 / ⑨ 部署上云,**含「v1.4 客户端契约清单」全文**)。
> v1.4.0 于 2026-07-29 17:00 上云,同日 18:51 追加热修 **v1.4.1**(§七 P1-26 结案);现役章程 `v1.3.3`(v1.4 未动章程)。**查 v1.5 之前的客户端字段一律去该归档文件**,本正文不复述。
> **⚑ 交叉引用约定**:本文件 §一~§三、§七 里出现的「§五 v1.4-①…⑩」指针,**一律指向本存根 → 该归档文件**。

## 五(旧-v1.3)、v1.3 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.3_施工图_20260728归档.md`(① 退出规则章程变更 / ② 持仓管理层 K4 牌 / ③ 情报官四件 / ④ 挂单未成交追踪 / ⑤ 问询台选股域清理 / ⑥ 客户端跟进 / ⑦ 部署 + 章程激活,**含「v1.3 客户端契约清单」全文**)。
> v1.3 → v1.3.5 全部已上云,现役章程 `v1.3.3`(见 §四 / §九)。**查 v1.4 之前的客户端字段一律去该归档文件**,本正文不复述。
> 未随 v1.3 交付的残项:**v1.2.1-D 打法 × 论点双维归因** → §七 **P3-11**(施工图全文在 `archive/v1.2_v1.2.1_施工图_20260728归档.md`)。
> **⚑ 交叉引用约定**:本文件 §一~§三、§七 里出现的「§五 v1.3-①…⑦」「§五 v1.2-*」「§五 v1.1-*」「§五 阶段 4*」等指针,**一律指向本节这三个存根 → 对应 `archive/` 归档文件**(正文不再复述历史施工细节)。

## 五(旧-v1.2)、v1.2 + v1.2.1 施工图(已完工上线 → archive)

> 全文已归档:`archive/v1.2_v1.2.1_施工图_20260728归档.md`(三仓章程 + 熔断纪律 + 决策日志八项 + 呼吸台账+ 归因闭环;除 v1.2.1-D 外全部随 v1.3-⑦ 上云)。**v1.2.1-D(打法 × 论点双维归因)未实现**,挂账 §七 P3-11,施工图全文在上述归档文件内(保留勿删)。

## 五-归档、v1.1 施工图(已完工上线 → archive)

> v1.1(SOP 补洞:盘前校准 tick / 持仓生命周期 D5 / 自选板块 + 体检 / 问询窗口修复)于 2026-07-21 全部完工上线(状态见 §四、变更日志见 §九)。**施工图全文已移 `archive/v1.1_施工图_20260721.md`**(照 §五B / §五C 留指针体例),正文不再复述。

---

## 五B、K2 策略研究(已否决归档 → 策略线)

> **策略研究已迁至策略研究中心权威文件 `STRATEGY_LAB.md`(2026-07-25 架构拆分)。**
> K2 施工图全文:`archive/K2_研究施工图_20260722.md`;结果:`research/k2_report.md`。
> 判决:中心命题否决,K2 落库不激活,K1 仍现役。

---

## 五C、K3 策略研究(已否决中止 → 策略线)

> **策略研究已迁至 `STRATEGY_LAB.md`(2026-07-25 架构拆分)。**
> K3 施工图全文:`archive/K3_研究施工图_20260725.md`;结果:`research/k3_report.md`。
> 判决:B2 四臂全灭、2026 生存门禁七臂尽墨,停止挖矿条款触发,K3 中止(未落库);B3/B5 残项转 STRATEGY_LAB Backlog。
---

## 五附、已完工路线图(阶段 0 → 4,研究先于产品 · 均已完工存档 → archive)

> 全文已归档:`archive/已完工路线图_阶段0-4_20260728归档.md`(阶段 0 数据地基 → 阶段 1 回测研究 → 阶段 2 LLM 审判层 → 阶段 3 盘中哨兵 → 阶段 4 产品化与上云的全部施工图与验收记录)。

## 六、回测参数清单(已全部过堂 → 策略线)

> P1–P10 全部有结论并写死进大脑 K1,清单全文已迁 `STRATEGY_LAB.md` 雷区地图节。

---

## 七、Backlog(按优先级分组;冷启动从 P0 往下读)

> **2026-07-28 交接重整 + v1.4.0 立项标注**。分组含义:**P0** 影响判定正确性(越早越好)/ **P1** 已知错判或噪音(有明确修法)/ **P2** 审计欠账 / **P3** 功能挂账 / **P4** 运维与历史局限(留档为主)/ **📌** 认知对齐(v1.4 立项必读)/ **⚫** 长期机制(不排期)/ **✅** 已了结(压缩留痕,勿再当待办)。`P0-1`…`📌-22` 是**稳定 ID**,跨会话引用请带编号。**策略假设 / K 字头版本一律不在此**,权威在 `STRATEGY_LAB.md`。
>
> **⚑ v1.4.0 吸收标注(2026-07-28)**:被 v1.4.0 施工图吸收的条目在原地标「**→ v1.4.0 第 N 块**」,**稳定 ID 与原文一律保留不删**(施工细节以 §五 对应块为准,本节只留「这条去哪了」)。未标注的条目 = 本版不做,仍是待办。
>
> **⚑ v1.5.0 吸收标注(2026-07-29)**:同一体例,标「**→ v1.5.0 第 N 块**」。本版吸收四条:**P1-7**(search_engine 定案 → ④-A3)、**P1-24**(章程回滚 SOP → ④-A5)、**P4-25**(测试卫生 → ④-A4)、**✅ 节「诱多做局反向哨兵」的残留半边**(自选池侧未覆盖 K4 派发 → ④-A1)。**P4-16**(报告内存账)由 **⑥-E** 继续复核。新挂 **P3-27**(候选四件套老键删除,等双端换包后)。
>
> **⚑ V2.0.0 逐条处置(2026-08-02 立项)**:三态标注 —— ① 被 V2 吸收的标「**→ V2.0.0 第 N 块**」;② V2 后不再适用的标「**⛔ V2 后废弃**」,**稳定 ID 与原文一律保留不删**(留痕,防后人当新问题重开);③ 未标注 = 仍是待办。本次吸收 **P1-5 / P1-8 / P3-11 / P3-15 / P3-27**,废弃 **P4-16**,新挂 **P3-28 / P3-29 / P4-30 / P4-31**。

### 🔴 P0 · 影响判定正确性(**当前为空** —— P0-1/2/3 → v1.4.0 第 ① 块已完工上线;P0-23 → v1.4.0 第 ⑩ 块已结案)

- **[P0-23] ✅ 已结案** · ~~`compute_industry_strength` 扫全历史,在生产机不可完成 —— 挡住 v1.4.0 整版上云~~(2026-07-29 ⑨ 首次部署实测发现并回滚,**当天由 ⑩ 修复、傍晚重新上云验证通过**)。`report/industry_strength.py::_load_ret1d_panel(start=None)` 对 `daily` 做**全历史 `scan_parquet`**(生产 1591 分区 / **7,841,894 行**),模块 docstring 的「本地实测全历史加载廉价(<1s)」**只在开发机(Mac)成立**;生产(2 vCPU / 1.6G RAM)实测:**700M cap → OOM-kill(exit 137)**、**1400M cap → 600s 未跑完(exit 124)**。**为什么要紧**:v1.4-② 把它立成 A2/B3 判据唯一源后,三个调用点全部中招 —— ① `pipeline.py:163` 在 **16:35 报告主链**(`neckline-report.service MemoryMax=800M`,而 07-28 那份**不含**本步的报告峰值已经恰好顶满 800M)→ 上了大概率**当日无报告**;② `info_card.py:652`(新端点,实测公网/本地**永不返回**,120s 与 600s 两次都超时);③ `inquiry.py:297`(问询台)。②③ 跑在**常驻 `neckline.service` 内、与盘中哨兵同进程**,`MemoryHigh=420M` 先节流导致进程陷在回收死循环(`memory.events.high` 飙到 54 万次、`oom_kill=0`)——**表现为卡死而非崩溃**,盘中被点一次就拖累哨兵。**⚠ 抬 `MemoryMax` 不是解**(1400M 都跑不完,是算法成本不是配额),⑨-F 原文「视情抬到 1G」按此作废。**✅ 修法已裁定(用户 2026-07-29)= 方案②「预计算落表」→ 施工图 §五 v1.4-⑩**(🔴 @builder-pro,**⑨ 重跑的硬前置**):新 SQLite 表 `industry_strength_daily`(110 行/日,含未达标行业),16:05 日更增量算一天(**只读当日一个分区**),`persist_days` 用「上一评定日 streak + 今日强度日标记」递推 → **不需要任何人为窗口**;三个在线调用点 + ④ 信息卡 60 日中位数序列全部改**只读表**,缺行走保险丝(降级方向 = 不拦 + 显式披露)。~~① 给 `start` 加下界窗口~~ **否决**(与「持续天数看多远由上次断裂决定」的设计意图冲突,会低报长 streak,§3.8 宁可多算不可少算);~~③ 只给 `info_card`/`inquiry` 注入报告已算好的结果~~ **否决**(治标,16:35 主链照旧中招)。**修完必须在生产机上实测复验**(⑩-G,含新立的「探针只许收盘后跑」纪律),**且 review 前移到上云之前**(§五 前提第 4 条)。**✅ 2026-07-29 本地部分完工**(@builder-pro):新表 + store + CLI + 16:05 挂载 + 四处读侧改造 + 保险丝四态 + `dataFreshness` 三键 + 客户端横幅;守门单测(四文件零现算入口 + 三路等价)绿;本地真数据演练 bootstrap 全历史 3s / 17.5 万行 / 库 +25MB、`verify` 三项绿、随机 3 日单日对拍逐位一致、读侧 1.3ms、日更增量 0.02s、信息卡 0.56s。**演练打出一个真洞并已修**:`rank(ordinal)` 并列由行序打散 → 按年块 bootstrap 与单日日更算出**不同 rank**(20230803 实测),修法 = 排名前按 `(median_ret 降序, industry 升序)` 排定(已入 CLAUDE.md 纪律 + 单测)。**✅ 2026-07-29 生产侧亦完工并随 ⑨ 上云验证**:旁路 bootstrap 落表 175,120 行 / 1592 交易日 / 110 行业,⑩-D 五条 + ⑩-G 五条判据全绿;⑨ 上云后生产实测 —— `verify` 三项自检全绿(exit 0)、**上午「永不返回」的 `GET /report/{date}/info-card/{code}` 现返 200 / 15.6s / 16431B**、16:35 报告主链恢复正常。**残留**:信息卡 15~20s 仍偏慢,根因是 `_load_single_code_panel` 全 glob(**与行业强度无关**,那一维已是读表 5.3ms)→ 另立 **P1-26**,不再挂在本条。
- **[P0-1] 补录持仓无法指定真实买入日**。`api/app.py::open_position`(~703 行)写死 `buy_date=date.today()`、`PositionOpenIn`(`api/schemas.py:304`)无买入日字段 → 用户 2026-07-27 补录的 **3 笔历史持仓买入日全被盖成当天**。**为什么要紧**:D 计数、时间退出 D5/D15 判向、回落止盈峰值追踪起点、周复盘持有天数、按打法归因的持有周期,**全部起点错**。**怎么修**:① `PositionOpenIn` 加可选 `buyDate`(`YYYYMMDD`,缺省今天,校验不晚于今天);② `app.py` 透传 —— **领域层 `sentinel/positions.py::open_position` 与 CLI `scripts/positions.py add` 本来就收 `buy_date`,缺口只在 HTTP 契约 + 客户端**,改动面很小;③ 客户端补录 sheet 加日期选择器(默认今天);④ **纠正已落库的 3 笔**:需用户提供真实买入日,改前 `sqlite3 .backup`,改后复查 `holding_eod_check` 是否已按错误 D 定格(`time_exit_locked_*` 三列有值就要清掉重算)。~~归 v1.3.6~~ → **v1.4.0 第 ① 块(①-A)**,🔴 高危区:碰持仓判定。⚠ 纠正 3 笔**需用户先提供真实买入日**(§八 第 11 项)。**✅ 2026-07-28 完工**:契约 `buyDate` + 两个 400 reason(`not_trading_day`/`future_buy_date`)+ 客户端 mapReason 两 case 已落;生产 3 笔已按用户提供的真实买入日纠正(`scripts/oneoff/fix_position_buy_dates.py`)。**客户端日期选择器 UI 归第 ⑧ 块,代码上云归第 ⑨ 块。**
- **[P0-2] `002036.SZ`(联创电子)自 2026-07-23 起无行情分区**,而 `stock_basic.list_status` 仍 `L`;用户自述该票「正筹划股权转让」。**为什么要紧**:该持仓无法定价 → 持仓卡市值/浮盈、K4 每日体检、D5 判向拿不到收盘价;`resolve_time_exit` 无定格时**按设计保守返回「明日时间退出」**,可能催用户去卖一只停牌根本卖不掉的票。**怎么修**:① 先核实是停牌还是数据源缺口(查 TuShare `suspend_d` / 直接看该票近 5 日 `daily` 行);② 定**停牌票在持仓链路的显示口径**(建议:沿用最后有效收盘价 + 明示「停牌 N 日,价格为最后成交价」,时间退出判向挂起且不推送,绝不静默按老价当今日价);③ 口径写进 §2.1 或本节。~~归 v1.3.6 + v1.4~~ → **v1.4.0 第 ① 块(①-B)**,诊断与口径一起落(口径已在 §五 定死,诊断只决定 `reason` 标签取值)。**✅ 2026-07-28 完工**。**诊断结论(真 token 实证,勿再查)**:① `suspend_d` 在 600 元档**可用**;② `002036.SZ` 自 **20260723 起连续在停牌名单**(`suspend_type=S`)= **真停牌,不是数据源缺口**,与用户「筹划股权转让」自述一致;③ 20260728 全市场同类 **9 只**(`002828.SZ`/`300242.SZ`/`300615.SZ`/`300862.SZ`/`300955.SZ`/`300996.SZ`/`688277.SH`/`920685.BJ`)= **普遍现象非个案**。口径落地:`PositionOut.priceStale` 三字段 + 第五态 `suspended_hold` + K4 体检 `dataUnavailable` + §2.1 第 2 条附注。
- **[P0-3] 概念板块表压根没有日更路径 → 「当日暴起板块」那一路候选长期失效**(不是一天的偶发)。`ths_daily`/`ths_member`/`ths_index` 只有**一次性 backfill**(`scripts/backfill_concept.py`,`END="20260722"` 硬编码 + 「已存在则跳过」),而 16:05 的 `scripts/daily_update.py` 只跑 `backfill.DAY_TABLES=[daily, daily_basic, adj_factor, moneyflow_dc]`(+`index_daily`/`limit_derived`),**概念板块表不在日更清单里**;本地实测 `ths_daily` 最大 `trade_date=2026-07-22`。**为什么要紧**:`report/sectors.py::compute_sector_strength` 当日无行 → **返回空列表且不报错**(优雅降级),于是 `intel_candidates` 第①步的「当日暴起 top-N」整路为空(07-27 那 20 只全来自五常驻 + 竞争位)、题材持续天数(`board_age`)也拿不到 → 情报排序少一维;五常驻板块因走 `ths_index` 名字静态解析,反而看不出坏了。**怎么修**:① 给日更加概念板块增量(`ths_daily` 按日追加;`ths_member` 低频重拉,~400+ 指数各一次调用,配额消耗要与 P4-20 一起算账);② **至少先加「板块数据过期 > N 天」的显式告警**,别让它继续静默降级。~~归 v1.3.6 + v1.4~~ → **v1.4.0 第 ① 块(①-C)**,日更管线 + 过期告警一起做。**它是需求 8 板块维度的硬前置**;新增配额消耗与 P4-20 一起算账、部署块 ⑨-E 复核。**✅ 2026-07-28 完工**:`ths_daily` **一次调用取全板块**(实测 `trade_date=` 可用,不必按 code 拉 400 次)、尾窗 5 交易日重拉自愈、按 `ths_index` 过滤成概念板块;`ths_index`/`ths_member` 周更带 `.bak` 且失败不覆盖;`SECTOR_DATA_STALE_MAX_LAG_DAYS=2` + `ReportOut.dataFreshness` + 报告顶部告警 + 最强题材小节标不可信。**新增配额 = `ths_daily` 5 次/日 + `suspend_d` 1 次/日 + `ths_index`&`ths_member` ~400 次/周**(⑨-E 复核)。

### 🟡 P1 · 已知错判 / 噪音(有明确修法)

- **[P1-26] ✅ 已结案(2026-07-29 v1.4.1 热修上云)** —— **信息卡端点在生产要 14 秒,而客户端默认超时 12 秒 → 用户实际体验是「信息卡总是加载失败」**。**修法 = 取数层按 `year=` 裁剪分区(修法①)**:`market_data._scan_table` 加 `years=` 参数,由 `get_market_slice`(单日→1 个年目录)/ `get_stock_history` / `scan_table_range`(区间→覆盖年份)传入;`strategy/features.market_state_labels` 的起点由写死 `date(2019,1,1)` 改为按 MA 窗口回看。**纯 I/O 优化、结果逐位不变**(`TestYearPartitionPruning` 5 条等价单测锁死),合法性依据是结构性的:分区路径由 `day_file_path` 按 `trade_date.year` 生成,`year=YYYY` 目录里只可能有该年的行。客户端 `fetchInfoCard` 同步 `timeout: 60`(照问询台惯例)。**公网实测**:修前 **14.15 / 13.98 / 13.89s**、3 次调用 `memory.events high` **+8945**;修后**冷调 3.05s、热调 2.33 / 2.25s**(**6.2 倍**)、连打 6 次 `high` **恒 0**、`MemoryCurrent` 从 440MB(≈`MemoryHigh`,已在节流)降到 287MB。**⚠ 顺带收窄了脏分区的传染半径**(已如实改写守门单测而非删断言):v1.3.5 那条「历史脏分区不会自愈」现在只对**跨到脏那年**的读成立。~~原文:根因是 `_load_single_code_panel` 全 glob~~ —— **诊断只对了一半**:单票面板确实全 glob,但真正的大头是 `compute_sentiment` 一次请求做 **5 次**全市场横截面(每次全 glob 1592 个 footer),连同单票面板 2 次 + 大盘线 1 次 = **8 次全 glob**;所以修在 `_scan_table` 这一层(而不是只修 `_load_single_code_panel`)才对。
- **[P1-26 原文存档] 信息卡端点在生产要 18~20 秒(可用但慢),根因是 `_load_single_code_panel` 全 glob**(2026-07-29 ⑩-G 判据 4 实测,**plan §五 v1.4-⑩-G 已预授权「若仍 >10s 单独立项,不阻塞 ⑩」**)。⑩ 修完 P0-23 之后该端点**从「永不返回」变成「exit 0、峰值 98MB」**(300M cap 下,3 只实测 20.15s / 18.41s / 18.70s),行业强度那一维已经是**读表 5.3ms**、不再是瓶颈;**剩下的时间全在 `report/info_card.py::_load_single_code_panel`** —— 它为了取单只票的 60 日 K 线,仍走 `year=*/*.parquet` 打开 **1592 个 parquet footer**(开发机 0.56s,生产 2 vCPU 慢 33 倍)。**为什么不算 P0**:它返回、内存也不高,与既有 `holding_k4_check` 同一类路径、已在生产跑通多日;只是交互体验差(点开信息卡等 20 秒)。**怎么修(三选一,先量再选)**:① 按 `year=` 只 glob 窗口覆盖的 1~2 年(60 个交易日最多跨两年,是纯 I/O 节省、不改语义,与 P0-23 方案①「加窗口会低报 streak」的设计冲突**无关** —— K 线窗口本来就是固定 60 日);② 复用报告生成期已装配的面板(需要端点能拿到报告态,契约面更大);③ 同 P0-23 走预计算(体量大,不划算)。**倾向 ①**。**未排期**,归 v1.5 或独立快修。
- **[P1-24] ✅ 已结案(2026-07-30 v1.5-④-A5)** · 章程「回滚」的运维口径未定(代码侧已安全,流程侧空白)(2026-07-29 review 判定线 🟡-1 修复后新立)。判定层已经不怕回滚了 —— 激活历史是 append-only 事件流(`strategy_activation_log`),回滚只在时间轴末尾追加一条事件,回滚前每一段治权的逐笔判定逐位不变;切换器也会打重激活告警 + 留痕。**但「什么情况下该回滚、回滚后 STRATEGY_LAB 现役标注/§九/`~/hz_info.md` 怎么同步、要不要顺手重跑受影响周的复盘」这套流程从没写下来**,而回滚必然发生在事故现场(那时没人有空现编)。**怎么修**:~~等真需要回滚一次之后按实际经历补一节运维口径,别现在凭空编~~ → **改判(用户 2026-07-29 纳入 v1.5 配菜)= v1.5.0 第 ④ 块(④-A5)**。**✅ 2026-07-30 完工**:一页纸 SOP `archive/SOP_章程回滚_20260730.md`(四节:① 什么情况该回滚〔判据非感觉,三档:客观配置错误 / 实盘归因证明更差 / 紧急止损〕② 操作序列〔四道闸 + 双备份 + append-only 事件流语义〕③ 同步清单〔STRATEGY_LAB 现役标注只在跨 K 血缘时才需要动、PROJECT_PLAN §2.1/§四/§九、`~/hz_info.md`〕④ 要不要重跑受影响周复盘〔判据 = 回滚时刻是否落在已生成复盘的周内 + 命令 = 重传同周交割单,`reviews` 表按 week 幂等覆盖〕),**文首如实标注「未经实战演练,首次真回滚后按实际经历修订」**——只写代码侧已确认事实与同步清单,不编造未验证的操作细节(原条"别凭空编"的精神保留)。§2.1 第 3 条末已加一行指针。
- **[P1-4] 周复盘按「周」粒度取 governing 章程,激活当周的交易被旧章程判**。`brain.config_governing_for_week(week_start)` 判据是「激活日 `<` week_start」,整周一把抓。**后果**:本周(2026-07-27~08-02)用户按 v1.3.3 三仓 4 万打,周复盘仍按 K1 判 → 单笔 >2 万、买创业板会被**误标违纪**(**已知假警报,用户已接受本周噪音,不为一周的显示问题动纪律时间线**)。**正解**:改成**按成交时间戳逐笔判**(成交时刻早于激活时刻按旧章程、之后按新),`review/reconcile.py` 取 config 的入口从「周」下沉到「笔」,报告里注明该周发生过章程切换并分段计数。→ **v1.4.0 第 ⑥ 块(⑥-A)**,🔴 碰纪律判定,列入 review 必审项。
- **[P1-5] → V2.0.0 第 ② 块** · 消息面 LLM 扫描 300s 墙钟预算不够。**V2 处置**:预算重排为**三本账**(检索 / 推理 / 复盘,独立不合并)+ 降级次序定死(先丢 T3 简评 → 再丢 T2 复盘细节 → 绝不牺牲卡冻结与纪律外壳);扫描域也随选股换血从「持仓 + 自选」变为「持仓 + T1/T2 篮子成员」(自选池已删)。原文如下(留痕)。
  **[P1-5 原文]** 消息面 LLM 扫描 300s 墙钟预算不够。`report/news_alerts.py:161 LLM_SCAN_BUDGET_SECONDS=300.0`;3 持仓 + 16 自选 = 19 标的,2026-07-27 那次预算耗尽、**8 只自选未扫**(已由 `newsAlertsScan.codesSkipped` 如实披露,没有静默)。自选只会越来越多。**怎么修(三选一)**:抬预算(报告是 oneshot,但要顾 `MemoryMax` 与整体时长,见 P4-16)/ 并发(**先验 GLM 供应商限频,别拍脑袋开并发**)/ 分批轮扫(持仓优先已实现,自选可隔日轮)。→ **v1.4.0 第 ⑥ 块(⑥-B)**:先实测供应商限频,再按实测**二选一**(受控并发 or 自选隔日轮扫),不折中。
- **[P1-6] 「定格日 ≠ D5」无标注**。EOD 管线连续断跑时,时间退出按「首个可得 EOD」定格(可能已是 D7/D8),系统如实写 `time_exit_locked_date` 但界面不提示。**怎么修**:`PositionOut` + 报告持仓卡显式标注「定格于 D{n},晚于 D{max_hold_days} {k} 天」,**只提示、不改判定逻辑**(定格语义是审计修复 🔴-1 的结论,不得回退)。→ **v1.4.0 第 ⑥ 块(⑥-C)**。
- **[P1-7] ✅ 已结案(2026-07-30 v1.5-④-A3)** · `search_engine` 选型待数据定。2026-07-27 实测 `search_pro` 2 条 / `search_std` 2 条 / `search_pro_sogou` 10 条(**单日样本,不足以定**);当前维持 `search_pro`(用户拍板),命中数埋点已随 v1.3.4 上线。**怎么修**:~~攒几天后捞 `llm_judgments` 的命中分布(按日 × 按引擎)再定~~ → **v1.5.0 第 ④ 块(④-A3)**。⚠ **原文那句"按日 × 按引擎"是当时的想当然,已纠正**:`llm_judgments` **没有引擎列**,且生产**从来只用过 `search_pro`** → 现有数据只能给「`search_pro` 的按日命中分布」。故 ④-A3 三步:① `llm_judgments` 幂等加列 `search_engine`(由 provider 写当次实际取值,老行 `NULL`=未记录、不回填猜测);② 捞 20260724(命中恢复日,见 P4-17)至今的按日基线(**只读 SQL,守生产探针纪律**);③ 基线 0 命中率显著(近 10 交易日 >20%)才做一次**受控 A/B**(同批 5~10 只候选,`search_pro` vs `search_pro_sogou`)再改常量,否则维持 `search_pro` 结案;**无 key / 样本不足 → 只出基线,本条继续挂账并写明"引擎维度需 A/B、未做"**。**不许在没有 A/B 证据的情况下换引擎。**

  **✅ 2026-07-30 三步完工**:①`llm_judgments.search_engine` 幂等加列(`_migrate_columns`)+ `OpenAICompatProvider._search_engine_value()` 钩子(默认 `None`)+ `GLMProvider` 覆盖读 `_SEARCH_ENGINE` 单一源常量(与 `_search_tools` payload 同一处,不重复硬编)+ `LLMResult`/`JudgeResult` 透传 + `store.save_llm_judgment` 落库(仅成功路径非 `None`,老行/降级路径 `NULL`=未记录不回填猜测)。②**生产只读 SQL 实捞**(`ssh deploy@118.178.122.194` + `sudo sqlite3 "file:...?mode=ro"`,收盘后 22:49 CST 执行,守生产探针纪律;只 SELECT `trade_date`/`ts_code`/`degraded`/`json_array_length(search_hits_json)` 四列,secrets 不落输出):窗口 20260724~20260729(命中恢复日至今,**实际仅 4 个交易日**——数据库里就这么多,不是抽样)、**40 条非降级审判**(4 日 × 10 只,当时仍是 v1.4.1 生产代码的 `TOP_N_JUDGED=10`)、**0 条降级**、**0/40 零命中(0.0%)**,分布 min=1/p25=10/median=10/p75=10/max=10(GLM 顶层 `web_search` 数组多数打满,少数几只命中偏低〔1~3 条〕但均非零,不构成"静默失效")。③**定案**:0% ≪ 20% 显著阈值(即便按二项分布保守估计,0/40 事件的 95% 置信上界≈7.5%,同样远低于阈值)→ **判据③「基线健康 → 维持 `search_pro`,结案」**,**未消耗任何 A/B(本地本就无可用 GLM/Kimi key,且基线健康也不构成 A/B 触发条件)**。诚实登记:窗口只有 4 个交易日(受限于「命中恢复日」到「今天」本就只隔这么多个交易日,并非抽样不足),但方向性证据在全部可得数据上一致(4/4 天零命中率均为 0%),继续观察不会改变判断方向;若未来出现零命中骤增,P1-7 可重开。
- **[P1-8] → V2.0.0 第 ③ / ⑥ / ⑨ 块(部分吸收,部分仍挂账)** · 启发式阈值待实盘校准:`intel_candidates.py` 的 `INDUSTRY_GATE_MIN_LIFT=2.0` / `QUOTA_PER_PERMANENT_BOARD=2`、熔断的 3 笔连亏与单日 −4000 元、盘前校准各阈值 —— 全是启发式,零实盘样本。**怎么修**:攒 1–2 个月实盘 + 周复盘数据后**一次性回看这一组**(哪个挡多了 / 放多了),不要逐个单独拍。~~归 v1.4+~~ → **V2 处置**:① **选股侧阈值**(行业闸 lift、卫生线、Tier 权重)全部进**策略包 config**(③),校准路径 = ⑨ 评价引擎周度报告 → 策略线出新包 → 四道闸激活,**不再是"改代码里的数"**;② `QUOTA_PER_PERMANENT_BOARD` 随五常驻删除**作废**(裁定 #9);③ **熔断的 3 笔 / −4000 与盘前校准阈值仍是政策值、仍挂账**(住命名常量,不进包也不进章程),本条为这三项继续保留。

- **[P1-36] ④ 扫描层两条「三路等价」守门单测在全量套件下偶发失败、单独跑恒绿(V2-⑪ 施工期两次复现,2026-08-03 新挂;⑪ 完工记录「块外发现」的落点 —— 原文只写了"已挂账"但没有账本,在此补上)**。**症状**:`tests/test_scan_cluster.py::test_bulk_vs_day_by_day_vs_readback_are_identical` 与 `tests/test_scan_corr.py::test_bulk_vs_day_by_day_vs_readback` 在 `pytest tests/ -q` 全量下**偶发** failed,**单独跑恒 pass**;⑪ 开工基线首跑复现一次、施工期又复现一次,重跑即过,**与 ⑪ 零关联**(⑪ 一行没碰 `neckline/scan/`)。
  - **⚠ 为什么这是 P1 不是 P4(测试卫生)**:两种成因**性质完全不同,必须先分清是哪一种**——
    - **(a) 测试夹具的跨用例状态污染**(临时库 / 缓存 / 工作目录残留)→ 只是测试问题,修夹具即可,**良性**;
    - **(b) ④ 的 `bulk` 与 `day_by_day` 两条路径本身存在真实不确定性**(polars 分组顺序残留、`rank` tie-break 没排干净)→ **这是真 bug 且撞在项目明令禁止的那条线上**:CLAUDE.md「`rank(method="ordinal")` 的并列由行序打散 = 不确定性,**任何进判据 / 排序的 rank 必须先排定确定性 tie-break**」(2026-07-29 行业强度那次真数据打出来的)。若是 (b),意味着 `limit_cluster_daily` / `corr_matrix_daily` **同一天算两次可能不一样** → 历史回放不可复现、⑨ 的按包归因失去可比性。**这条守门单测存在的全部意义就是拦 (b)**,它偶发红本身就是它在报警。
  - **怎么查(判据先行,别一上来改测试)**:① 固定 `PYTHONHASHSEED` + `-p no:randomly` 复跑,能稳定复现就偏 (a);② 单独跑该文件 N 次 + 全量跑 N 次对比失败率;③ 失败时把两路输出 dump 出来**逐行 diff** —— 差在**行序**还是**数值**:行序 → (b) 的 tie-break 问题,数值 → 夹具数据被别的用例改过 = (a);④ 查 ④ 的 `bulk` 路径有没有对分组结果做 `sort` 收口。
  - **⛔ 在分清 (a)/(b) 之前,不许把这两条断言放宽或加 `flaky` 重试** —— 那等于把报警器拆了。
  - **归属**:④ 扫描层。**排期建议:与 ⑧-E 同批**(都在 ⑬/⑭ 之前),因为若是 (b),⑨ 攒的归因数据同样会被污染。
  - **✅ 2026-08-03 结案(@builder)——诊断 = (a) 类良性测试缺陷,已明确排除 (b)**:四步判据执行完毕。①②标准跑复现:目标两文件连续 20 轮 100% pass(标准);另跑 8 轮全量 `pytest tests/ -q`(每轮 63~120s,期间总数因 ⑫/⑪-B 并行施工持续增长 3012→3055,非本项异常)**未自然复现**——但据此**不能**判"问题不存在",故补做③工程复现:用 `unittest.mock.patch.object` 强制 `cluster._now()`/`corr._now()` 在 bulk 调用与 day-by-day 调用两阶段分别返回不同常量(等价于"两阶段跨越了墙钟秒边界"),**精确重放了症状**——`.equals()` 判 False;逐行 diff 后**业务列(cluster_key/ts_code/cluster_size/anchor_industry/anchor_concept;scope_key/code_a/code_b/corr/n_obs 等)全部逐位相同,唯一分叉的是 `computed_at`**。④ 复核 bulk 路径:两个测试早已在比较前 `.sort(["cluster_key","ts_code"])`/`.sort(["scope_key","code_a","code_b"])`,收口已在,不缺排序。**真因结论**:既不是原判 (a) 描述的「夹具跨用例污染」,也不是 (b) 的「polars 分组顺序 / rank tie-break 不确定性」——`refresh_limit_clusters`/`refresh_corr_matrix` 对同一天的计算在 bulk 与 day-by-day 两条路径下逐行代码等价(day-by-day 只是把同一份单日纯函数逐次调用),**业务结果从未分叉过**,故明确排除 (b),⑨ 的按包归因可比性不受影响。真因是第三种、比 (a)/(b) 更窄的良性缺陷:`computed_at` 是秒精度墙钟审计戳(docstring 原文即"审计:这行什么时候算的",按设计随每次调用变化),两个「三路等价」测试的 bulk 调用与 day-by-day 调用是**两次独立调用**,后者工作量更大(重复加载 `industry_of`/`concept_of`、3 次独立 SQLite commit 而非 1 次),全量套件下系统更忙 → 两次调用更容易跨越墙钟秒边界 → `computed_at` 合法不同 → 整行 `.equals()` 把审计列的正常差异误判成"业务不一致"。**同一坑本项目已有两处正确先例**(`tests/test_industry_strength_store.py` 的 `{k:v for k,v in r.items() if k!="computed_at"}`;`tests/test_scan_stage.py::_table_rows_excl_computed_at`,其 docstring 明文"排除 computed_at"),`test_scan_cluster.py`/`test_scan_corr.py` 建于同批(V2-④)却漏抄同一体例。**修法**:两个目标测试的 `.equals()` 比较前各自 `.drop("computed_at")`(business 列仍要求逐位相同,不放宽、不加 flaky 重试),docstring 补充说明根因;**未改动 `neckline/scan/cluster.py`/`corr.py` 生产代码一行**。验证:目标文件连续 20 轮 100% 绿;全量 `pytest tests/ -q` 零回归(与并行施工的 ⑫/⑪-B 共存,总数持续波动,详见 §九)。**⚠ 顺带发现但不在本次范围内(留档不改)**:`tests/test_scan_leader.py::test_refresh_is_deterministic_and_readback_matches` 同款比较 `first.equals(second)`(同日两次调用)存在相同的未排除 `computed_at` 潜在隐患,只是只有 1 次重跑(而非本条两测的 3 次独立调用)使窗口小得多、大概率从未被撞上,不代表天然安全;建议下次碰 `leader.py` 测试时顺手补 `.drop("computed_at")`。

- **[P3-37] 退潮红色刹车「主线跳水」路自 ⑧-G 起从"聋"变"能响",灵敏度待实盘校准 + 须让用户知情(2026-08-03 新挂,⑧-F 对拍打出来)**。**事实**:该路在 V1/V2 现口径下**近乎失效** —— 真跳水日(07-24 全市场中位 −2.86%、89% 个股下跌)样本读数 **−0.18%**,而主线板块本身 **−2.98%**(已贴 −3% 阈值),差 **2.8pp**;四天 0/4 触发。**V1 时代样本是候选池、同样偏动量,大概率一直就是聋的,只是从来没人量过。** ⑧-G 修好后它会**开始真的响**。
  - **✅ 2026-08-03 ⑧-G 已完工,实测把这条挂账收窄了一半(数字见 §五 ⑧-G 完工记录)**:**样本这一半已修好** —— 切片读数与「主线板块本身」per-seed 最大差 0.53pp、07-24 只差 0.05pp;**但同四天仍 0/4 触发**,因为主线板块本身(−2.75% per-seed / −2.98% pooled)就在 −3.0% 阈值**之上**。⚠ 故待校准的**只剩阈值灵敏度**,且「上线后第一次红色刹车」可能比预期来得晚(不是"修好就会天天响")—— ⑰ 换包说明的告知照写,但别把它写成"马上会经常触发"。
  - **⚠ 为什么不需要用户事前批准、但必须事后知情**:⑧-G **恢复的是 §2.4 早就写明的既定行为**(「主线板块跳水 → 今日计划作废、禁开新仓」),不是新增能力;而且 07-24 那种日子**本来就该响**(§1.3 交割单归因:买入下跌途中票是主要死因)。**但用户此前从未经历过这条刹车触发** —— 上线后第一次红色刹车会是全新体验(**全天禁开新仓 + 立即级推送**),**⑰ 换包说明里必须有一句告知**,别让它像故障一样冒出来。
  - **待校准的是灵敏度,不是要不要有**:`sector_dive` 阈值(−3% / 早盘 −4%)是 V1 传下来的启发式,**从未在"样本不聋"的前提下被检验过** —— 换句话说,这个阈值当年是配着一个失效的样本定的,现在样本修好了,**阈值的实际含义变了**。攒够实盘样本后须回看:触发频率是否合理、有没有把正常回调日误判成跳水。
  - **⚠ ⑧-G 完工后这条已经可以量化了,交给策略线 / 用户时直接用这组数(2026-08-03 补)**:
    - **老样本系统性偏乐观 1.51~2.93pp**(⑧-F ∩池样本 vs 主线本身,四天 per-seed 误差);**新切片误差 ≤0.53pp,真跳水日仅 0.05pp**。
    - **推论:同一个 −3.0% 阈值,在样本修好之后实际上比当年设它的人以为的严了约 1.5~2.9pp** —— 当年"读到 −3%"对应的真实主线跌幅其实是 −4.5%~−5.9%。**阈值的数字没变,它的含义变了。**
    - **最有说服力的一个点**:07-24 全市场中位 **−2.86%、89% 个股下跌**,主线本身 **−2.75%(per-seed)/ −2.98%(pooled)** —— **这样一天仍然不触发**。要不要在这种日子响,是**产品决策**,交用户。
    - ⚠ **别把这组数读成「所以该把阈值调到 −2.7%」** —— 四天不是样本,且 K=6 能凑出触发这件事恰恰说明**这个区间对参数很敏感**。**先攒实盘,再谈调数。**
  - **一并回看(补一条)**:`MIN_MAINLINE_SAMPLE=5` 作用在**票数**上,**没有**作用在**主线条数**上 —— 2 颗种子时 per-seed 估计量只是在平均 **2 个数**(n=8 已过下限)。这在数学上偏薄。**⛔ 现在不加第二个常量**(无证据、且实测日常 10~41 颗种子,离这个角落很远),但回看时一并看:要不要给**主线条数**也设个下限。
  - **⛔ 在有实盘证据之前**:阈值一字不动(⑧-G-F 已锁);⛔ **不许通过调 K 或调样本口径去凑触发频率**(那是偷偷改阈值);要动阈值走**用户拍板**(它是纪律触发器)。
  - **一并回看**:⑧-G 的 K=4、两份测量样本的保底配额、per-seed 估计量 —— 三者都是 planner 按统计与语义定的**工程默认,无实盘背书**。**什么时候回看**:积累 ≥1 个月实盘 + 至少经历 2~3 次真实急跌日之后。**不排期。**

### 🟠 P2 · 审计欠账(最该补的一块)

- **[P2-9] 三路独立审计只完成一路**。已完成:**纪律与金额**(2026-07-27,报告 `archive/REVIEW_REPORT_v1.3_章程金额审计_20260727.md`,抓到 4 条主缺陷 🔴-1/🟡-2/🟡-3/🟡-4 + 5 条小修,全部已修并随 v1.3.1 上云)。**未做的两路**:① **情报官 / 候选管线** —— K4 安检实现与 DB `k4_advisory` 六节**逐条对拍**(`hard_cut` 真拦、`avoid_flag` 只打标)、四处「诚实字段」是否真透到客户端(`newsAlertsScan.codesSkipped/codesFailed`、搜索 0 命中脚注、板块资金流 `unavailableReason`、行业闸被挡票的审计留痕);② **契约 / 客户端 / 迁移三方对拍** —— `api/schemas.py` 声明 → `app.py` 转发 → Swift 解码 **逐字段走一遍**,该版**已两次出现「领域层算了、序列化层漏了」的漏字段**,这是复发型缺陷。→ **改判(用户 2026-07-29)**:~~v1.4.0 施工完成后 / 上云后~~ → **在 v1.4-⑩(P0-23 修复)完工、⑩-G 生产实测判据全绿之后,重新上云(⑨)之前**由用户召唤 reviewer 一并做掉(plan 里仍不排 review 施工块)。⚠ v1.4 的 **②-C / ③-G 两份对拍报告**是这次审计的现成输入。**✅ 2026-07-29 结案**:报告 `archive/REVIEW_REPORT_v14_契约线_20260729.md`(两路 + v1.4 契约 14 条三方对拍 + 404/400 reason 与 `mapReason` 互为闭包)—— **无 🔴**,3 张 🟡(`intel_order` 零消费 / exec_hint C3 时区缝 / Swift `inquiryId` 断链)**当天全修**(§九 一行)。**遗留 🟢 观察 4 条不做,只留档**(报告 §二):A4 卫生线码永不命中 × forced 豁免叠加(海选池写入方已休眠,**未来恢复时补 forced 票卫生线标注**)/ `_evaluate_hits` 对「无 EOD 行」三消费点姿势分叉(实测影响≈0)/ `InfoCard` 合成 Codable(**若该端点改读冻结快照必须换手写容错 init**)/ 渲染层 `search_hits` 列表 vs 条数分叉(有 `int(... or 0)` 护栏)。
- **[P2-10] 已发布版本未 review**:`v1.3.1`(审计修复 + 行业闸 lift + 首次带迁移的部署)/ `v1.3.3`(拆墙 + 问询台改自由分析师 + `ts_code` 归一 + **章程二次激活** + **切换器闸 2 窄豁免**)/ `v1.3.4`(provider 层 `search_query`,四处 LLM 调用共用)/ `v1.3.5`(**生产数据迁移:902 个 parquet 分区 cast** + canonical schema 声明 + 候选管线保险丝)。含高危面:**生产数据迁移、给护栏开的豁免口子、章程两次激活、共用 provider 层改动**。⚠ **`v1.3.2` 在 §四/§九/git log 里都没有记录 = 跳号,不是漏记**,别去找。→ **与 P2-9 同批**,时机同上(**⑩ 完工 → review → ⑨ 上云**,用户 2026-07-29 改判,原「上云后」作废);本轮 review 范围另含 **⑥-A 逐笔章程** 与 **⑩ P0-23 修复**。**✅ 2026-07-29 结案**:报告 `archive/REVIEW_REPORT_v14_判定线_20260729.md` —— 四个已发布版本的高危面逐条 git 取证放行(v1.3.1 行业闸 lift / v1.3.3 二次激活 + 闸 2 窄豁免留痕真落 / v1.3.4 `search_query` 四态 payload 逐字节 / v1.3.5 `TABLE_FLOAT_COLS` 声明守门),**无 🔴**;2 张 🟡(回滚洗白历史判定 / 行业强度日更静默跳洞)**当天全修**(§九 一行,🟡-1 单独 commit `b2e3673` 供复核)。**遗留 🟢 观察不做,只留档**(报告 §二 10 条),其中三条**将来动到那块代码时必看**:🟢-3 冷却判据锚「再买日收盘」而非真实再买时刻 + `_cooldown_violation_in_week` 靠正则从文案抠日期(现役 `cooldown_days=0` 恒 no-op,**启用冷却前先把返回结构化**)/ 🟢-6 refresh 落行统一贴请求日 `day_key` 不校验分区内 `trade_date` / 🟢-7 `INSERT OR REPLACE` 不删重算后消失的 (day, industry) 行。

### 🔵 P3 · 功能挂账

- **[P3-27] ✅ 已了结(2026-08-03,V2-⑬-6 兑现删除)** · 候选四件套老键与老模板函数的最终删除(v1.5.0 只做退役,没做删除)(2026-07-29 v1.5.0 立项时新立)。**V2 处置**:自选体检是这四个文案函数最后的消费方,而自选池已按裁定 #9 删除 → **无消费方,四函数 + 四个 `CandidateOut` 老键随候选榜一并彻底删除**。**2026-08-02 用户拍板 D2 = A 路**(新机新子域 `nk.linotsai.top`、老 App 继续打老机)→ **两者不交叉,可直接删,不做过渡文案**(⑭-D)。⚠ **「先客户端可选解码、下版服务端才删键」这条两步淘汰纪律本身仍然有效**(CLAUDE.md 铁律,V2 之后的版本照守),V2 只是靠换机窗口结构性地绕开它。原文如下(留痕)。
  **[P3-27 原文]**v1.5-③-B 把候选卡输出层换成参考三件套后,`CandidateOut.buyPoint/stop/target/invalidation` 四个键**仍在发**(值 = 过渡文案 `LEGACY_FOURPIECE_NOTICE`),`report/candidates.py` 的 `entry_plan_text`/`stop_loss_text`/`target_text`/`invalidation_text` 四个函数**仍在**(自选体检还在消费)。**为什么不能当场删**:已装 v1.4.1 客户端对这四个键是**硬解码**(`client/Models.swift::Candidate.init(from:)` 用 `try c.decode(String.self,…)`,非 `decodeIfPresent`),服务端一旦不发就是整份报告解不出、今日计划全空。**删除条件(两条都满足才动)**:① 双端都已换到 **≥1.5.0**(§八 第 12 项确认);② 自选体检输出层也改完(它是这四个函数最后的消费方)。**怎么删**:先把 Swift 那四个属性改 `decodeIfPresent` + 默认空并发一版,**再**在下一版服务端删键(顺序不能反)。⚠ **`entrySpec`/`invalidationSpec` 永远不在删除范围**——那是盘中哨兵与盘前校准的唯一判据源。**不排期。**
- **[P3-11] → V2.0.0 第 ⑫ 块(形态改变,部分废弃)** · 打法 × 论点双维归因(原 v1.2.1-D)。**V2 处置**:① **「呼吸 T 净贡献」维彻底废弃**(裁定 #9:呼吸台账删,`breathing_t_trades` 停写留档);② **「论点标签」维退化**为可选标签(决策日志强制表单退役,标签改落 `user_actions`,覆盖率会下降 → 见新挂 **P3-28**);③ 归因的**新载体 = ⑫ 能力画像**(按题材 / 角色 / Tier / 入场方式分维,带样本量与置信度,并新增「是否跑赢同篮未选成员」这一 V1 拿不到的维度)。**本条不再单独排期**,以 ⑫ 为准。原文如下(留痕)。
  **[P3-11 原文]**施工图全文在 `archive/v1.2_v1.2.1_施工图_20260728归档.md` 的 v1.2.1-D 节:决策日志 via `position_id` 与 FIFO RoundTrip 按 `(ts_code, buy_date)` 邻近匹配,按论点 / 打法 / 交叉三维聚合胜率盈亏比 + 呼吸 T 净贡献 + 「无决策日志开仓 N 笔」纪律项。**为什么要紧**:「短线追击 vs 呼吸试验」双打法 A/B 裁决的**唯一归因载体**;需 30–50 笔样本才有意义。⚠ 依赖 P0-1 修好(买入日错 → 归因的持有周期全错;P0-1 → v1.4.0 ①-A)。**归因维度自 v1.4.0 ⑤-B 起 +1**(`decision_log.max_chase_pct` 最高追价上限;v1.4 只落数据不出报表),**自 v1.5.0 ①-E 起再 +1**(`reference_plans` 表 = LLM 参谋成绩单:参考买入 / 离场区间、明早剧本、夹逼结果,可与实际成交价与次日走势对拍;v1.5 同样只落数据不出报表)。**本条仍归 v1.5+**(需 30–50 笔样本)。
- **[P3-12] 挂单未成交追踪没有出口**。服务端 v1.3-④ 已上线、`decision_pending_track` 自 2026-07-27 起在攒数据,但 **API 未暴露任何端点**(`app.py` 全仓无 `pending_track` 命中),客户端自然无处可看。**怎么修**:定端点(`GET /decisions/{id}/track` 或并入 `GET /decisions`,§五「v1.3 客户端契约清单」已占位)+ 客户端决策卡展示 N 日走势。→ **v1.4.0 第 ⑦ 块(⑦-A)**,端点定为 `GET /decisions/{id}/track`,404 reason `not_found` 客户端必须加 case。**✅ 2026-07-29 完工**:端点直接读 `report/pending_track.py::load_track_rows`(同码不重写,不重算);404 只在 `decision_id` 不存在时触发,「决策存在但未攒到追踪数据」如实返 200 空态(两种「空」分开)。`reason="not_found"` 复用客户端既有 `case "not_found": return .notFound`,未新增 case(字符串与既有 decisions link/cancel/revise 端点相同)。客户端决策卡展示走势归 ⑧-G(§五 客户端契约清单已登记契约形状)。
- **[P3-13] 问询记录不落库**。`POST /inquiry` 只返回不持久化(`llm_judgments` 只由报告管线写),问询台无历史、回答无法留档、无法与决策日志关联做归因(用户 2026-07-27 提过)。**怎么修**:新建会话表(问题 / 材料 / 回答 / `evidence` / `search_hits` / 时间戳 / 可选 `ts_code`、`position_id`),客户端加历史列表。→ **v1.4.0 第 ⑦ 块(⑦-B)**,表名定为 `inquiry_log`(**与 `inquiry_pool` 是两件事,不许混用**)。**✅ 2026-07-29 完工**:新表 `inquiry_log`(`CREATE TABLE IF NOT EXISTS` 天然幂等,新表无需 `_COLUMN_MIGRATIONS`)+ `run_inquiry` 结尾旁路写入(try/except 包裹,失败只记警告、`inquiryId` 退化 `None`,不打断已算好的回答;`degraded` 字段语义不受影响)+ `GET /inquiries`(分页 + `tsCode` 过滤,倒序)/ `GET /inquiries/{id}`。`position_id`/`decision_id` 当前无写入方(表列已留但无调用方传值)。客户端历史列表 UI 归 ⑧-G(§五 客户端契约清单已登记 `inquiryId` 实为 `Int?` 非 plan 原文 `Int`)。
- **[P3-14] 客户端 Swift 小瑕疵(等 iOS 装机一起做)**:① `InquiryVerdict.rejectRaw/passRaw`(`client/Models.swift:898-899`)自 v1.3.3 二值裁决退役后**已成死码**(只剩单测在引用),清理时连带改 `AppModelTests`;② 「已分析·有风险提示」显示为中性色,应为**警示色**。→ **v1.4.0 第 ⑦ 块(⑦-C)**,与第 ⑧ 块客户端同批做(不必等 iOS 装机)。**✅ 2026-07-29 完工**:`InquiryVerdict` 由二值裁决(`.reject`/`.pass`)改描述性标注(`.analyzed`/`.analyzedWarn`,原样对齐后端 `VERDICT_ANALYZED`/`VERDICT_ANALYZED_WARN` 字面值);`.analyzedWarn` 的 `tone` 改 `.warn`(复用既有 `NKAxisTone.warn`→`NK.amber`,未新造色值),`.analyzed` 维持 `.neutral`(标注不是判决,不套好评色调)。连带修正的死码/失真处:`AppModelTests`(重写三个断言 + 新增 tone 回归测试)、`DTODecodeTests`(两个解码测试此前喂的是已退役字符串,同步换真值)、`IntegrationSmokeTests`(真机联调断言同步,gated by dev server 未跑,本次仍 skip)、四处过时中文注释(`SharedUI.swift`/`InquiryView.swift`/`AppModel.swift`/`APIClient.swift`/`DesignTokens.swift`)。`NKAxisTone` 补 `Equatable`(单测需要按值断言 tone,原枚举未声明该协议)。双端 `xcodebuild BUILD SUCCEEDED`;iOS Simulator `xcodebuild test` 139 跑 126 过 13 skip(IntegrationSmokeTests 无本地 dev 后端天然 skip)0 failure。**截图未拍**(任务briefing「双端 BUILD SUCCEEDED 即可,截图非必需」与本条 plan 原文「(截图)」冲突,取前者;色值改动已有 `testInquiryVerdictWarnGetsWarnToneAnalyzedStaysNeutral`/`testDecodeInquiryAnalyzedWarn` 精确断言 `.tone == .warn` 兜底,且拍照需切后端环境 picker 离开默认 prod,与本会话「零生产访问」冲突,故未做)。
- **[P3-15] → V2.0.0 第 ⑰ 块** · iOS 装机(用户自理)。**V2 处置**:V2 全量换血后 iPhone 上的旧构建将**完全不可用**(契约一次性换血),装机不再是"看不到新功能"而是"必须换";步骤 = §八 第 12 项,时机 = ⑰(等用户指令,builder 不得自行换包)。原文如下(留痕)。
  **[P3-15 原文]**⚠ **2026-07-28 用户更正:iOS 早已装机** —— 此前「未装」的记载**作废,勿再据此判断 iPhone 上哪些能力可用**;具体装的是哪个构建**待第 ⑧ 块双端构建时核实**。macOS 现装 v1.3.4 构建。原文留痕:~~未装 → iOS 上盘前 / 熔断 / 派发警报推送、两档 D 徽标、费用录入与离场原因全部不可用~~。**归 用户操作(见 §八 精神,非网页操作)。** → **v1.5.0 第 ⑤ 块(⑤-F)出构建 + 装机说明、第 ⑦ 块(换包)等用户指令**:用户 2026-07-29 点名「iOS 要跟上」,故 iOS Release 构建**随本版同步产出**(构建不通过 = ⑤ 不算完工),装机动作仍在用户。⚠ 现状:iPhone 上仍是 **v1.4 之前的旧构建**,v1.4 的信息卡页 / 排序理由 / 决策日志第⑨项 / 停牌标注在 iPhone 上都还看不到。

- **[P3-28] 归因标签稀疏(V2 立项时新挂,来源 = 架构稿 §9.2 + 裁定 #6:入 Backlog 挂账,V2 不预建机制)**。**病因**:V2 把决策日志强制表单退役成「自动快照 + 用户字段全可选」(⑩-C),摩擦降下来了,但**能力画像的标签覆盖率会同步下降** —— 用户不填题材切换 / 弱转强 / 容量中军之类的标签,⑫ 的能力画像就分不出「哪类打法真有效」。**为什么先不建**:用户明确裁定「真遇到再解决」,预建一套代猜机制反而可能污染真实标签。**届时解法候选(不是现在做)**:① 先加**标签覆盖率指标**(⑫ 画像里如实显示「本期 N 笔中有 M 笔带标签」),让稀疏程度可见;② 若确实稀疏 → **LLM 代猜标签**,但必须**标注来源**(`user` vs `llm_guess`)、**与用户标签分表或分列存放、绝不混同**,且代猜标签**不得进任何判定**。**触发条件**:⑫ 上线后覆盖率持续 < 50%(判据而非感觉)。**不排期。**
- **[P3-29] 检索能力单点在带 `web_search` 的那一家(V2 立项时新挂,来源 = 架构稿 §9.1-3)**。**事实**:双 Agent 分工里,联网检索目前只有 GLM 一条通道实现,DeepSeek 走纯推理 → **GLM 故障时消息面 / 驱动证据整段降级**(诚实披露,不编造)。**V2 已做的缓解**:Provider 自填制 + 任务路由表(②)已经**结构上支持第二检索 Provider**,路由表里给它留了位置;缺席时 `evidence_status='search_unavailable'` 并在卡面与报告披露。**待办**:接一家备用检索 Provider(候选 = 任何带联网搜索的 OpenAI 兼容端点),接完把 `TASK_DRIVER_SEARCH` / `TASK_NEWS_SCAN` 的路由做成「主 + 备」。⚠ **接新检索源时先验协议**:不被上游认识的检索参数会 `ok=True` **静默返 0 条**(v1.3.4 案底),必须看 `search_hits` 字节数证伪。**不排期,不阻塞 V2。**

- **[P3-33] Tier 档位质量线初值与「多好才配叫 T1」的策略语义待过目(V2-⑥ 施工中发现,2026-08-02 新挂;与 P3-32 分开挂,因为要用的证据不是同一份)**。**事实**:⑥ 完工时发现「上限非配额、允许 T1 为空、不许凑数」在**纯排名制下不可实现**(只要当天有 ≥1 个篮子 T1 就永远填满),必须有一道**绝对分数**的质量线。**已做的工程处置(§五 ⑥-b)**:质量线**进包** `config.tier.quality_lines`(理由 = 它与五维权重**作用在同一标度上**,权重已在包里,分开住会让换权重静默改变 T1 选择性),初值 `tier1_min=0.60 / tier2_min=0.40 / tier3_min=0.25`;`tier3_min` 是 planner 补的(原实现 T3 无下限 = 事实上的配额,正是 V1「每天硬凑 20 只」那个病)。**待过目的是三个数本身**:① 0.60/0.40 是 builder 按「0.5 是整体中性」推的,② 0.25 是 planner 拟的,**三个都无证据支持具体取值**。**一并回看的两项**:③ `tradability` 的两档扣分比例(一字 1.0 / 涨停开过板 0.5,⑥ 登记 ⑤ 自陈"无证据支持具体数值");④ **中性填充是否该影响档位** —— ⑥ 五维缺数据一律填中性 0.5,三维缺数据的篮子靠三个 0.5 也可能压过 `tier1_min`,即「**因为不知道所以进 T1**」;⑥-b-D 已落 `neutral_filled_weight` 可审计字段但**刻意先不设闸**(不凭空造阈值),要不要据此降档等样本。**什么时候回看**:⑨ 评价引擎攒够样本后 —— 它能出 **Tier 单调性(T1>T2>T3)** 与 **T1 空档频率** 两条,正好是判断质量线松紧的直接证据。届时交**策略线 / 用户**过目,**调值走换包(进化门禁),⛔ 不许顺手改代码或包里的数**。**不排期。**

- **[P3-34] V2 判据的「工程重解读 / 零审计背书」登记册,待策略线 / 用户过目(V2-⑦ 施工中发现,2026-08-02 新挂)**。**这条与 P3-32 / P3-33 分开挂**:那两条问的是「**数值**取多少合适」,本条问的是「**判据语义**这么落对不对」—— 性质不同、要用的证据也不同。两项:
  - **(a) K7 需求 5 `warn_streak_top` 的「簇内」被重解读为「共振邻域」(§五 ⑦-K7-b)**。**策略线必须知道生产上到底怎么落的** —— K7 审计用的是「(日,行业) 涨停家数 ≥3」的**一票一簇**粗簇,而 ④ 的 `limit_cluster_daily` 是多路聚类(一票同日可属十几个簇),照字面「任一簇头名」在 2026-07-24 真实数据上会让 **36/42(86%)** 涨停票都打警示 = 标注失去区分度。生产读法 = 「所在**全部簇成员并集**内并列或独占最高,**且邻域至少有一只更低**」(后一条对应文案「3× 于后排」—— 得先有后排),三天命中 6/6/5(分母 48/129/42)。**待策略线确认这个重解读是否偏离 H12/H13 的原意**;在策略线提异议前按此执行。
  - **(b) ⑦ 的验证 / 失效条件集与聚合门槛是全新发明,零审计背书(§五 ⑦-b)**。验证 = `收盘 ≥ D0 收盘` 且 `≥ D0 MA20`;失效 = `≤ 章程止损线` 或 `触及跌停` 或(`< D0 收盘` **且** `< D0 MA20`);聚合 `min_members_hit = ceil(n/2)`(验证 / 失效两侧同一个数)。**没有任何回测或事件研究支持这套条件**,它们是为了让四态状态机能跑起来而拟的临时默认。**已做的约束**:集中到一处命名常量 + `_ENGINE_CONSTANT_WHITELIST` 登记 + `verification_ruleset_version` 落卡指纹(⑨ 才分得开哪套条件集出的成绩)。**待 ⑨ 攒够样本后回看**:验证率 / 证伪率的分布是否合理、`falsified` 是否打得过早过滥、失效侧要不要**按角色加权**(龙头破位比弹性破位更重 —— 合理但无证据,本版刻意不做)。**⛔ 本版不进包**:§12.2 插槽边界明文把「④篮子卡冻结体例」列在「引擎本体,不进包」一侧,要包化必须走「**扩插槽边界(用户拍板)→ 扩 schema → 发包**」三步,不是 builder 或 planner 能顺手做的。**在有新证据之前不许自行改数、不许自行加包键。** **不排期。**

### ⚪ P4 · 运维 / 历史局限(留档为主)

- **[P4-16] ⛔ V2 后废弃(ID 与原文保留,勿再当待办)** · **`MemoryMax=800M` 这个具体数字随换机作废** —— V2 走 100% 换新服务器(裁定 #3)+ 晚间管线多段化(三个 oneshot **各自** `MemoryMax`),老机 1.6G 总账下的所有配额推演不再适用。**接替它的是 §五 ⑯-C(新机隔离实测:每项批算单独计时 + 量峰值,达标才准进主链)与 ⑯-D(三段 oneshot 各自配额,重活不进常驻服务)**;「抬内存不解算法成本」「重活不进常驻服务」两条**教训本身继续有效**,已写进 §3.10-D 与 §五 铁律。原文如下(留痕)。
  **[P4-16 原文]** `neckline-report.service` 的 `MemoryMax=800M` 对 v1.3 新管线偏紧(`deploy/neckline-report.service:23`)。2026-07-27 补跑时峰值顶满、5206 次页缓存回收,**`oom_kill=0`** —— 撞的是页缓存不是真内存,没崩。**怎么修**:~~v1.4 部署时视情抬到 1G~~ **2026-07-29 改判**:P0-23 证明抬内存不解算法成本(1400M 都跑不完),⑩ 已把全历史扫描从在线路径移除 → **⑨-F 改为纯实测记账**(判据:报告峰值不高于 07-28 基线 838,860,800、`oom_kill=0`),任何 `MemoryMax` 改动先算总账(机器仅 1.6G,还有常驻 `MemoryHigh=420M/Max=600M` 与 `neckline-daily` 900M);抬没抬都要写理由。**2026-07-29 v1.4 定案 = 维持 800M 不改**(六条理由见 `archive/v1.4_施工图_20260729归档.md` ⑨-F)。→ **v1.5.0 第 ⑥ 块(⑥-E)继续复核**:本版把 16:35 的 LLM 段拉长约一倍(20 只审判 + 三件套 + 20 张信息卡取数),判据仍是 **`oom_kill=0`**;峰值高于基线 838,860,800 时**先算总账再谈改 `MemoryMax`**,且**超时先降预算 / 开并发,不许抬内存糊**(P0-23 教训)。
- **[P4-17] 历史局限:早期审判的消息面无搜索支撑(不补跑,如实留档)**。生产 `llm_judgments` **20260721/22/23 三天 10/10 空命中** → 那 **25 条非降级审判结论的消息面部分实质是模型凭训练数据作答**;20260717/0720 为 10/10 `degraded`(当时无 key);20260721 另有 5 条因 25s ReadTimeout 降级。**20260724 起命中恢复满额,自那天起的审判才算有搜索支撑**——事后审计「当时为何否决」必须先看这条时间线。**并入原「GLM 顶层 `web_search` 空数组待查」**:真因仍未完全解释(逐字节相同的 payload 两天行为相反 = 供应商侧漂移),v1.3.4 的 0 命中埋点 + 四处露出已上线,可持续观察,不主动追。
- **[P3-32] 主归属 lift 门槛与「小簇 vs 大概念」的策略语义待过目(V2-⑤ 施工中发现,2026-08-02 新挂)**。**事实**:涨停簇成分常只 2–5 只,`占比 ÷ 全市场占比` 实测 **70–90 倍** lift,与 v1.3.1 行业闸先例(板块成员以百计)不是一个统计量级 → 同票跨「小簇篮 + 大概念篮」时主归属**几乎恒落在小簇篮**。**已做的工程处置(§五 ⑤-c)**:加**最小成分数门槛 = 5** 作**引擎常量**(与 `industry_strength._MIN_MEMBERS` 同源同值),不达标的篮 `lift=None` + `lift_reason='sample_too_small'`、不参与主归属比较,全不达标时按 `(成员数降序, basket_key 升序)` 确定性兜底。**待过目的是策略语义那一半**:① 5 这个数对涨停簇合不合适;② 「小簇篮天然该输给大概念篮」这个方向本身对不对(也可能反过来 —— 小簇更聚焦)。**什么时候回看**:⑨ 评价引擎攒够样本后,它的按包归因正好能出「主归属落在小簇 vs 大概念篮」的实际表现对照;届时交**策略线 / 用户**过目。**在有新证据之前按上述默认执行,builder 不许自行改数。** **不排期。**

- **[P4-25] ✅ 已结案(2026-07-30 v1.5-④-A4)** · `pytest tests/` 会写开发机的真实 `data/neckline.db`(2026-07-29 修 review 🟡-1 时实测发现,**既有行为、非本次引入**)。`tests/test_k3_oversold_guardrail.py::TestK1BitIdentical` 等 **4 个 guardrail 用例刻意读真库的 K1 行**(把 `DB_PATH` 指向临时库跑全量,这 4 个立刻红),于是它们触发的 `init_schema` 会把**新表/新列的幂等迁移顺手落到开发库上** —— 今天 `strategy_activation_log` 就是这么在本地 dev 库建好并播了 K1 那一条(内容正确,与生产 ⑨-B 将发生的是同一段代码)。**为什么记一笔**:①「跑个测试而已」不该悄悄改你的工作库,迁移出错时现场也不好复原;② 开发库被测试改过之后,再拿它当"生产形状"的对照会失真。**怎么修**:~~不急,留档为主~~ → **v1.5.0 第 ④ 块(④-A4)**:conftest 加 session 级 `real_db_readonly_copy` 夹具(真库拷一份临时副本;真库不存在则 `pytest.skip`),把 `test_k3_oversold_guardrail.py` / `test_k2_mainline_guardrail.py` / `test_v13_exit_6y_baseline.py` 里**全部无参数的 `brain.active_config()`** 改成显式 `db_path=副本`;加 grep 守门单测断言 `tests/` 下不再出现无参调用(确有例外须白名单 + 写理由)。⚠ **本版有新表**(`reference_plans`),不修这条的话「跑个测试」又会把新表顺手建到开发库上。

  **✅ 2026-07-30 完工**:`conftest.py::real_db_readonly_copy`(session 级,`sqlite3` 官方 backup API 拷真库只读副本,同生产 `.backup` 既有姿势;真库不存在 → `pytest.skip`)+ 三文件六处 `brain.active_config()` 裸调用全部改显式 `db_path=real_db_readonly_copy`(两个 `_k1_cfg()` 辅助函数改带 `db_path` 参数,一处 `test_require_member_true_ands_the_column` 的两次直调同改)。**守门单测**:新文件 `tests/test_db_isolation_guardrail.py`——**AST 精确匹配**(不是纯文本 grep,理由:`test_brain.py` 有 4 处描述 `brain.py` 内部退化行为的 docstring 会被纯文本 substring 误伤,AST 只看真实语法树天然不受注释/字符串字面量影响,已用正/负/docstring 三组用例验证判据本身正确)扫描全部 `tests/test_*.py`(95 个文件),断言零出现无参数 `active_config()`/`get_active()` 调用;白名单当前为空。**实测证据(而非只信任测试通过)**:改前 dev DB `mtime=23:17:43`;改后跑一次**全量** `pytest tests/ -q`(1940 过 + 2 skip)**前后 dev DB mtime/size/MD5 逐字节不变**(`fd745c6a...`)——比"表结构未变"更强的证据(证明连一次 commit 都没发生,不只是没加列)。⚠ **过程留痕(如实记录)**:A4 修复落地**之前**,本会话早些时候跑过几次未隔离的全量测试,`llm_judgments.search_engine`(④-A3 新列)确实被 K2/K3 护栏用例的 `init_schema` 副作用提前建到了 dev 库上——这正是本条要修的病征的一次现场复现,内容正确无害(幂等迁移),事后已额外显式跑一次 `init_schema()` 留痕(A4 修复生效后该调用为 no-op,dev DB 无变化)。
- **[P4-18] Bark 通道从未活体验证**(payload 依官方文档实现,无真实 `BARK_URL`)。APNs 为主推通道,**不阻塞**;真要用,先手动跑一次 `BarkChannel(url).send(...)` 验协议。
- **[P4-19] 周复盘从未跑过用户真实券商交割单**。4D 对账引擎只用合成 xlsx 冒烟过;**2026-08-02 那个周末是第一次**,可能暴露两家券商原始格式的解析问题(`review_col_map` 可配映射 + `_detect_format` 的坑见项目 `CLAUDE.md` 阶段 4D 节)。首跑时留意「缺列 vs 缺该格」的区分是否真生效。**⚠ V2 附注**:⑫-A **原样继承该引擎、一行不改**(含 `review_col_map` 双驱动与「必需列整列缺失硬跳该行 + 警告、绝不 `or 0.0`」两条坑),故本条**不因 V2 而结案,仍挂账**。
- **[P4-20] TuShare 600 元档配额跨线共享,可被跑干**。策略线 2026-07-25 曾把配额耗尽 → 生产 16:05 增量可能拉不全 → 16:35 报告 / 情报件缺数。**缓解**:研究侧大批量拉取避开 15:00–17:00 生产增量窗口,长期考虑研究独立 token。**⚠ P0-3 若加概念板块日更会新增一笔固定消耗,一并算账。** → 该日更已定于 **v1.4.0 ①-C**,配额账在 **⑨-E** 复核。**⚠ V2 附注(算过账,如实登记)**:V2 新增的三张扫描层预计算表(④)**全部读本地 parquet,不新增任何 TuShare 调用**;盘中存拍(⑧-B)走**免费实时源**(新浪 / 腾讯),同样不吃 TuShare 配额;ETF / 指数报价亦走免费源。故 **V2 的 TuShare 配额账与 v1.4 持平**,本条继续按原口径挂着(研究侧大批量拉取仍须避开 15:00–17:00 生产窗口)。
- **[P4-21] 工作区遗留未提交**:`client/Neckline.xcodeproj/project.pbxproj`(Xcode 自动改的 `MARKETING_VERSION`)。→ **v1.4.0 第 ⑧ 块**客户端施工时一并处理,别单独开提交。**✅ 2026-07-29 完工**:随本块 4 个新文件条目(`InfoCardView.swift`)一并提交,`MARKETING_VERSION=1.3.4` 字面量与 `explicitFileType` 变化不再单独挂账。
- **[P4-30] V2 上线初期分时残缺(V2 立项时新挂,来源 = 架构稿 §9.1-2;历史局限,留档为主)**。盘中存拍(⑧-B)**从 V2 上线日才开始积累**,在此之前没有任何分时数据 → ⑨ 复盘引擎的 **MFE/MAE 只能用 EOD 近似**。**处置 = 如实标注,不装精确**:`basket_review_daily.mech_json.mfe_source ∈ {intraday, eod_approx}`,报告与客户端照标;**⑯-F 灌入的 preseed 篮子同理**(它们的复盘一律 `eod_approx`)。用户外源分时方案落定后走同一张表回补(裁定 #1),回补到哪天、哪天起就是 `intraday`。**不排期,不阻塞。**
- **[P4-31] V2 停写留档表的最终清理(V2 立项时新挂;⑬ 已于 2026-08-03 完工,六张表现已全部处于「有历史行、零新增行」状态,守门单测锁死)**。⑬ 的删除口径是「**代码删干净、表停写留档不 DROP**」,于是 `watchlist` / `breathing_t_trades` / `inquiry_pool` / `llm_judgments` / `reference_plans` / `decision_log` 六张表会长期以「有历史行、零新增行」的状态留在库里。**为什么这么做**:历史归因与审计要用,且 DROP 是不可逆动作。**将来要不要 DROP 的判据(不是感觉)**:① 该表最后一行的写入时间距今 > 1 年;② ⑫ 画像与 ⑨ 评价引擎已不再读它;③ 有一份独立备份。三条同时满足才谈 DROP,且必须走一次性脚本体例(默认演练、`--confirm` 才写、双备份)。**不排期。**

- **[P4-35] 关注池的「相关板块 ETF」缺数据源,本版只做到板块基准指数(V2-⑧ 施工中发现,2026-08-02 新挂;留档为主)**。**事实**:⑧-A 原文写的是「相关板块 **ETF/指数**」,实际只落了**指数** —— 项目**没有 ETF 成分 / 映射数据源**(TuShare 600 元档没有;`ths_index` 是同花顺板块指数,免费实时源不认它的代码),硬编一份 ETF 清单等于凭空发明。现状 = 按成员板块取 `000001.SH`/`399001.SZ`/`399006.SZ`/`000688.SH`/`899050.BJ`(与 `scripts/backfill.py::INDEX_CODES` 同一批)。**为什么记一笔**:蓝图 5.4 的持仓监测里有「**板块 ETF 或指数是否突然失去承接**」这一条(⑪-A 的四项新增监测之一),**指数只能替代一半** —— 宽基指数反映不了「这个题材的资金还在不在」,行业 ETF 才能。**要修先得有数据源**(候选:TuShare `fund_basic`+`fund_portfolio` 档位待查 / 交易所 ETF 清单 / 用户外源)。**不排期,不阻塞**;⑪-A 落地时按现状实现并如实标注是指数不是 ETF。

### 📌 认知对齐(不许凭字面推断)

- **[📌-22] ✅ 已裁决(2026-07-28,用户拍板选 (b))→ v1.4.0 全版**。~~「K4 打法」到底指什么,当面问用户~~ → **已问已定**:三选一里 **(b) 把 K4 advisory 产品化**(正选漏斗排序键 + 信息卡 + `exec_hint` 执行提示);**(a) 激活 K4 行**(`rule["config"]` 逐字段拷自 K1,激活 ≈ 激活一份 K1,策略行为无变化)与 **(c) 等策略线新内核**(四战役 + 用户 2026-07-26 终裁底盘暂缓)**均已否决,不再列为选项**。规格权威 = `archive/交接_系统线升级需求_20260725.md` **需求 8**(2026-07-28 策略线裁定)。**K4 行现状(事实,勿再误读)**:`is_active=0`、**永不激活**;全部价值在 `rule["k4_advisory"]` 六节(`hard_cut`/`avoid_flag`/`exec_hint`/`circuit_breaker`/`intel_order`/`note`),v1.4 是**把这六节用足**,不是激活这一行。**保留本条只为防止后人重开这个问题。**

### ⚫ 长期机制(常态运行,不排期)

- **[机制] 策略进化门禁**:按月 / 季调参须过回测 + walk-forward 样本外跑赢现役 + 用户批准;大脑按版本归因实盘表现。任何章程变更走 `scripts/activate_charter.py` 四道闸,不许手改 DB。**回滚**(切回此前已现役过的版本)是这套门禁的一个子情形,何时该回滚 / 走哪条路 / 同步清单见 `archive/SOP_章程回滚_20260730.md`(v1.5-④-A5,§七 P1-24)。
- **[机制] 选股策略包门禁(V2.0.0 新立;与上条并行,但两者互不混用)**:**选股**的可配部分(①扫描层种子规则 + ③Tier 机械分维度与权重)走 `selection_packs` + `scripts/activate_pack.py` **四道闸**(schema → 原语/白名单 + `engine_api_version` 兼容 → 默认演练 → `--confirm` 才写);换策略 = 一次激活动作,回滚 = 激活旧版本行;**评价引擎按 `pack_version` 归因**,每个包自动攒出自己的成绩单喂回策略线。⚠ **两条版本线、两张表、两套激活流程,不许混用**:章程(纪律)永远不进包,包也永远不改章程。**版本号沿用 K 字头**(包 = 策略线 K 版本的交付物形态,不另起第三条版本线)。**已知代价(刻意接受)**:策略线要用引擎没有的新玩法,须先等系统线加原语(一次小版本施工,带单测),代码永远不随包传输。
- **[未来] 分钟线数据源**:TuShare 600 元档无分钟线,盘中靠新浪 / 腾讯免费源。若后续要分钟级回测,再评估升档或换源。

### ✅ 已了结(压缩留痕,勿再当待办)

- ✅ **客户端载体 + 推送通道**(2026-07-20 拍板 = 方案 A:SwiftUI 双端 + APNs,新 Bundle ID);✅ **盘中哨兵部署**(2026-07-20 拍板 = 上 hz ECS)。
- ✅ **大盘 MA20 市场过滤器**(旧争议)→ 阶段 1 **P1 否决**,择时归情绪仪表盘展示层;✅ **纪律章程第 4 条「≥5% 次周单笔减半」**(旧挂起)→ 阶段 1 **P10 空效应否决**,从未纳入章程。判决全文在 `STRATEGY_LAB.md` 雷区地图节。
- ✅ **`sync_code.sh` 尾部 chown 翻 `data/` 属主 footgun** → v1.2-0 修复(收尾 chown/chmod prune `data/` + 末尾只读属主自检 `exit 1`),经 v1.3-⑦(2026-07-26)与 v1.3.1(2026-07-27)**两次真部署活体收官**,自检绿、无 502。
- ✅ **盘前情报包(需求 3)** → v1.3-③ C1/C2/C4 完工上线(复盘情报件 / 板块资金流 / 消息面扫描;`anns_d` 无权限 → 减持走结构化 `stk_holdertrade`,其余三类走 LLM 联网搜索)。
- ✅ **候选池生成管线改版(需求 5,K1 entry mask 退役)** → v1.3-③-C3 完工上线(四步管线:板块层 → 个股卫生线 → K4 安检 → 情报排序;候选语义改「过完安检、值得花注意力」)。
- ✅ **问询台选股域漂移** → v1.3-⑤ 完工(复用 `report/watchlist_check.py::discipline_checks`,顺带补上 P4/P5 在问询台从未实现的真实缺口)。
- ✅ **反向证伪哨兵新条件(K3-B2 副产品:年线下涨停/放量大阳 = 派发)** → 已并入 v1.3-② 持仓管理层作强警示条件。**⚠ 与 `STRATEGY_LAB.md` §五「[转系统线] 诱多做局反向哨兵」是同一件事**(2026-07-28 planner 逐码核对确认:该条要的「年线下突现放量大阳/涨停 = 疑似派发」= 已上线的 `A3_belowyear_limitup` + `A3b_belowyear_bigvol`〔量比≥2,雷区地图 3-⑤ 口径〕,住 `report/holding_k4_check.py`,持仓侧触发第六类 APNs、候选侧经 `intel_candidates` 安检、问询台复用同一份镜像)——**只补引用,不重复立项**。**残留半边(如实登记,不单独立项)**:自选池侧未覆盖 —— `report/watchlist_check.py` / `WatchlistCheckOut` 里没有任何 K4 判据,故自选票不会得到派发警示。**处置**:~~留档观察;若日后有块动 `watchlist_check.py`,顺带复用同一份镜像补上即可~~ → **v1.5.0 第 ④ 块(④-A1)兑现**(本版正好动 `watchlist_check.py`,按原处置口径顺带补上):复用 `holding_k4_check` 同一份镜像,**只取 `A3_belowyear_limitup` / `A3b_belowyear_bigvol` 两码**(派发警报,强价量证据),契约 `WatchlistCheckOut.dispatchAlerts`;⛔ **不推 APNs**(自选不是持仓,第六类推送只对持仓;新增推送须用户拍板)。**✅ 2026-07-30 完工**:`watchlist_check.py` 新增 `attach_dispatch_alerts`(读 `_build_holding_feature_panel` 算好的 `_hit_A3`/`_hit_A3b` 两列 + `describe_hits` decorate,零新 polars 表达式)+ `pipeline.py` 保险丝接线(异常只 WARNING、不阻断报告)+ 契约 `DispatchAlertOut`(4 键,省略 `level`,恒 strong/price_volume)+ markdown `_render_watchlist` 追加警示行(非 plan 逐字要求,但与既有 `_render_holding_check` 展示先例一致、属实现细节层面的合理补全)。**「复用同一份镜像」有交叉断言**:`test_watchlist_check.py::TestDispatchAlertsMatchHoldingK4Check` 同一行同时喂 `holding_k4_check.build_holding_k4_check`(持仓侧)与 `attach_dispatch_alerts`(自选侧),断言 A3/A3b 命中的 code/label/evidence/evidenceStrength 逐位相同。
- ✅ **K2 / K3 策略研究** → 中心命题均被否决并归档;权威全文与判决在 `STRATEGY_LAB.md`(策略线),施工图全文在 `archive/`。

---

## 八、用户操作清单(必须由用户手动办)

1. ~~**确认 TuShare 600 元档接口权限**~~:✅ 已确认(2026-07-19,用户提供官网权限对照表截图,600 元档 = 6000 积分列)。结论已吸收进 §3.2:`daily/daily_basic/adj_factor/index_daily/moneyflow_dc/top_list/概念板块和成分` 可用;**`limit_list` 不可用(15000 积分)→ 改 0.4b 自算涨跌停衍生表**,不阻塞。
2. ~~**确认券商条件单能力**~~:✅ 已确认(2026-07-19):止损条件单与**回落止盈**条件单均支持。阶段 1 止盈研究(1.4)按「回落止盈可由券商执行」设计。
3. ~~**(阶段 2 前拍板)TuShare 新闻资讯接口是否增购**~~:✅ 已拍板(2026-07-19):**不购**,消息面走 LLM 联网搜索(方案与代价见 2.4:需带搜索的 API 供应商、搜索结果落库存档、审判层退出回测改实盘归因考核)。唯一会重开此项的场景:未来想做「历史新闻进回测」(搜索给不了历史时点快照,只有新闻库能)。
4. ~~**(阶段 4 前)客户端载体拍板**~~:✅ 已拍板(2026-07-20)= 方案 A(SwiftUI 双端 + APNs,新 Bundle ID `top.linotsai.neckline`)。

### 用户网页操作清单(阶段 4,必须在网页手动办理)

5. **Apple Developer 新 App ID + 推送能力**(`https://developer.apple.com/account/resources/identifiers`):
   - **Xcode automatic signing 能自动办的**:注册新 App ID `top.linotsai.neckline` + 生成 provisioning + 在加了 push entitlement + Push Notifications capability 后为该 App ID 开启 Push(加 `Neckline.entitlements` + capability 后 Xcode 首次真机构建通常自动完成)。
   - **可能需网页手动确认的**:若 automatic signing 未自动为该 App ID 勾上 Push Notifications 能力,去上述 Identifiers 页面手动为 `top.linotsai.neckline` 勾选 Push Notifications 并保存。
   - **无需新建的**:APNs 鉴权密钥 `.p8`(Key ID `Q963AP3VY8`)是**账号级密钥,直接复用**给新 Bundle ID,不必新建密钥;`.p8` 已在 ECS `/opt/linon`,部署时拷进 `/opt/neckline`。
6. **提供 LLM API key**(阶段 4 起,App 内办):在 App「设置」板块填 GLM 或 Kimi 的 API key + 选供应商 → `PUT /settings/llm` 落服务端 DB,运行时生效。**这是问询台 + 报告 LLM 审判 + 复盘材料的激活开关;缺 key 全链路优雅降级不崩**。填入后首次真调用即验证 GLM/Kimi 协议假设(兑现阶段 2 遗留欠账)。
7. **(每周,阶段 4 起)提供交割单**:每周把券商交割单 xlsx 拖进 macOS App 周复盘工作台,供违纪对账 + 复盘材料生成。**首版按用户现有「整理格式」支持,字段映射可配以兼容两家券商原始格式**(§五 阶段 4D)。
8. **(接班,阶段 4 部署上线后)确认 LinoN 退役**:Neckline 联调通过、`ln.linotsai.top` 切到 Neckline 后,LinoN 停用退役——旧交易记录 `linon.db` 是否归档下载由用户定(`hz_info.md §13` 挂账)。
9. ~~**(v1.1,按需)提供同花顺自选 txt**~~ **⛔ V2.0.0 起作废**(裁定 #9:自选池 + 同花顺对账直接删除,§五 ⑬-11;**2026-08-03 ⑬ 完工,代码与端点已物理删除**)。原文留痕:**(v1.1,按需)提供同花顺自选 txt**:自选池与同花顺 PC 端对账 = 用户在**同花顺 PC 端手动导出自选 txt 文件**(无官方 API,拒绝模拟登录 = 账号风险)→ 拖进 macOS App 自选工作台做差异对账 + 一键对齐;反向可从 App 导出 Neckline 自选为同花顺可导入 txt。**非每日必办,按需对账时才做**(§五 v1.1-C.4 / v1.1-F.4)。
10. **(v1.1)设置屏开新增两类推送开关**:盘前校准(9:26 汇总)、D5 时间退出——App 设置屏可各自开 / 关(默认开),无网页操作。
11. ~~**🔴(v1.4.0,挡 ①-A 收尾)提供 3 笔在持仓票的真实买入日**~~ ✅ **已办结**(2026-07-28 用户提供、builder 已纠正落库,§七 P0-1)。原文留档:生产 `positions` 里 3 笔 `open` 持仓是 2026-07-27 补录的,买入日**被写成了补录当天**(§七 P0-1)。请按票给出**真实成交日期**(格式 `YYYYMMDD`,须是交易日),例:`002036.SZ = 20260716`。**为什么非你不可**:系统没有任何数据能反推真实买入日(交割单要到周复盘才进系统),而 D 计数 / 时间退出 D5-D15 判向 / 回落止盈峰值起点 / 持有天数归因**全部以它为起点**。收到后由 builder 走 `scripts/oneoff/fix_position_buy_dates.py`(先 `sqlite3 .backup`,改完清 `holding_eod_check.time_exit_locked_*` 三列让次日 16:35 重新定格)。**无网页操作,发数字即可。**
12. **(v1.5.0)双端换包 —— macOS ✅ 已换包 1.5.0 / iOS ⬜ 待你自己装**。**macOS 已完成**(2026-07-30 09:46,随 v1.5-⑥ 上云一并做):Release 构建经 `ditto` 装入 `/Applications/Neckline.app`,产物与安装态可执行文件 **sha256 逐字节一致**、`CFBundleShortVersionString=1.5.0`、签名校验通过,启动后对 v1.5.0 服务端 5 个端点真实请求全 200。**iOS 仍待办**:iPhone 上还是旧构建 —— **v1.5 的参考三件套(取代老四件套)、今日计划分块、自选派发警示在 iPhone 上看不到**,且老四件套四键现在服务端只返「本版已由参考三件套取代…」过渡文案,**旧 App 上那四栏会显示这句话而不是数字**。需**你用 Xcode 直连真机装一次**(服务端已 `v1.5.0`,先决条件已满足;无网页操作)。§八 第 5 项的 App ID + Push 能力若尚未在 Apple Developer 网页勾选,装机前先办掉。**真机具体版本仍待你自查**(builder 只有模拟器权限)。
    - **iOS 装机步骤(§五 v1.5-⑤-F 改写;builder 无真机权限,此步骤仍需你亲自操作)**:① 数据线接真机到这台 Mac,解锁并「信任这台电脑」;② Xcode 打开 `client/Neckline.xcodeproj`,顶部 scheme 选 **Neckline**,设备选择器切到你的 iPhone(不是模拟器);③ 签名走 **Automatic**(项目已配 `CODE_SIGN_STYLE: Automatic` + `DEVELOPMENT_TEAM`,无需手动选证书);④ ⌘R 直接跑;⑤ 若报 `Entitlement aps-environment not found`(推送能力未随新 App ID 自动开)→ 回 §八 第 5 项,去 Apple Developer 官网手动为 `top.linotsai.neckline` 勾选 Push Notifications 后重试。
    - **builder 侧已验证的 iOS Release 构建**(`xcodebuild -configuration Release -destination 'generic/platform=iOS' build`,`CFBundleShortVersionString=1.5.0`)是「代码在 Release 配置下能正确编译签名」这道门禁的证据,**不是拿来装机的介质**——那份产物的签名身份/描述文件与你真机直连时 Xcode 现场生成的不一定相同,**装机仍走上面①~④「Xcode 直连真机」这条路**。
    - ⚠ **现状**:iPhone 上还是 v1.4 之前的旧构建 —— v1.4 的信息卡页 / 候选排序理由 / 决策日志第⑨项 / 停牌标注、以及 v1.5 的参考三件套 / 今日计划拆两块(持仓股/候选列表) / 自选派发警示**在 iPhone 上都看不到**;macOS 已是 1.4.1。§八 第 5 项的 App ID + Push 能力若尚未在 Apple Developer 网页勾选,装机前先办掉。**换包后自检**:设置屏底部「App 版本」/「服务端版本」两行(v1.5-⑤-E 起分行展示,取代旧的单行「版本」)应一致,不一致会有一行提示「服务端已是 vX.Y.Z,当前 App 为 y.z,请换包」。**无网页操作(除非踩到上面第⑤条 `aps-environment` 报错)。**
    - ⚠ **V2.0.0 附注**:V2 是一次性契约换血 —— 但 **D2 = A 路已拍板**(新机挂 `nk.linotsai.top`、老机 `ln.linotsai.top` 原样服务),所以 **V2 上云期间你手机上的旧 App 照常能用**(它还打老机);**换到 V2 需要你装一次新包**,时机 = §五 ⑰(**等你指令**,builder 不得自行换包),换完再谈老机退役。

### V2.0.0 用户操作清单(2026-08-02 立项新增;git / 部署 / 构建由 agent 自理,不列)

> ⏱ **时机(2026-08-02 用户追加指示)**:**云上组件迁移放在尾声** —— 第 13、14 两项要在 §五 **⑯** 开跑之前办好即可,**不必现在就办**;①–⑮ 是纯本地施工,不依赖它们。你按自己的节奏买机 / 解析,builder 到 ⑯ 之前会先问一句。

13. **🔴(V2,挡 §五 ⑯ 整块)购买新云服务器** —— **裁定 #3:100% 更换,这是部署前置项**,不办则 ⑯ 无法开始(①–⑮ 不受影响,可照常推进)。
    - **规格(§五 D3,2026-08-02 已拍板)**:**≥ 4 vCPU / 8 GB 内存 / 100 GB SSD**,Ubuntu 22.04 或 24.04 LTS,地域**杭州**(与现有 `hz` 同区,数据搬家快;也可换区,只是搬家慢一点)。**为什么要这么大**:老机 2 vCPU / 1.6 GB 已经被 P0-23 打爆过一次(全历史 parquet 扫描 700M cap OOM、1400M cap 600s 跑不完),而 V2 新增三张全市场预计算表 + 复盘引擎 + 盘中存拍,晚间管线比 v1.5 重一个量级。
    - **购买页**(用现有账号即可,新开一台,**不要动老机**):`https://ecs.console.aliyun.com/`(阿里云 ECS 控制台 → 创建实例);也可选任意云厂商,只要能 SSH。
    - **办完给我什么**:公网 IP + SSH 登录方式(**口令 / 密钥都行,但绝不要贴进任何被 git 跟踪的文件** —— 直接在对话里给,builder 只写进服务器上的 `~/.ssh` 与本机 config)。
    - ⚠ **老机不要停**:`ln.linotsai.top` 上的 v1.5.2 要一直服务到你完成 ⑰ 双端换包并确认之后,才由你决定退役与数据归档。
14. **🔴(V2,挡 §五 ⑯-G)把 `nk.linotsai.top` 解析到新机** —— **D2 = A 路已拍板(2026-08-02),这一步是必办项,不再是二选一**。你的原话已记录在案:「我本来就要换云服务器,NK 这个域名我会直接把它解析到新的云服务器上」。
    - **在哪办**:你域名 `linotsai.top` 的 DNS 控制台加一条 **A 记录** —— 主机记录 `nk` → 记录值 = 第 13 项那台新机的公网 IP,TTL 默认即可。若域名在阿里云:`https://dns.console.aliyun.com/`。
    - **builder 侧接手的部分**(无需你操作):解析生效后申请证书(certbot)+ 配 nginx 站点 + 定时续期;判据 = `dig nk.linotsai.top` 已返回新机 IP。
    - ⚠ **两条边界**:① **新机 nginx 绝不接管 `ln.linotsai.top`** —— 老 App 必须继续打老机,这是「V2 契约一次性换血、不留过渡键」成立的唯一前提;② **老机的解析在 ⑰ 换包完成前一律不动**。
    - ~~备选 B 路(沿用 `ln.linotsai.top`,割接时切解析)~~ **已否决,不再列为选项**(§五 ⑭-D)。
15. **(V2,挡 §五 ② 的活体能力)在 App 设置屏填两条 LLM Provider** —— V2 起是**自填制**(任意 OpenAI 兼容端点),不再是「GLM / Kimi 二选一」。
    - **推理 Agent(建议 DeepSeek)**:key 在 `https://platform.deepseek.com/api_keys`;填 base_url + model + key,**「是否带联网检索」不勾**。
    - **检索 Agent(建议 GLM)**:key 在 `https://open.bigmodel.cn/usercenter/apikeys`;**「是否带联网检索」勾上**。
    - 然后在设置屏把**任务路由**设成:检索类任务 → GLM,其余 → DeepSeek(默认路由已是这样,一般不用改)。
    - **缺 key 全链路优雅降级不崩**(篮子机械项照出、LLM 段如实标缺席),所以这项不阻塞施工,只影响成品能力。
16. **(V2,可选、无期限)分时数据外源方案(裁定 #1:你自寻)** —— V2 的盘中存拍从上线日起自给,历史分时缺口只能靠外源回补。你若谈定了数据源,把**能拿到什么字段、什么频率、怎么取**告诉我,builder 接进 `intraday_ticks` **同一张表**回补,复盘引擎无感切换。**没有也不影响 V2 上线**(缺分时的复盘走 EOD 近似 + 如实标注,§七 P4-30)。

---

## 九、变更日志(一行制;详版全文 → `archive/变更日志_详版_20260719-20260728.md`)

> **记录纪律(2026-07-28 起)**:每次动作只记**一行**(日期 · 标题级摘要)。事故复盘、完工验收、长记录一律写 `archive/` 独立文件,此处一行 + 链接,同一件事全文只存在一处。2026-07-28 之前的 54 条详版全文见上述归档文件(原样未改)。

- 2026-08-03 · 📱 **V2-⑮ 完工:客户端双端改版(D8 四板块)+ ⑮ 欠账清单 6 组全部还清 + 版号三方同批升 2.0.0**(@builder-pro,commits `0ed277c` / `dcd90cf` / `e676967`,纯本地 + 双端构建 + 模拟器截图核对,零服务器 / 零部署)。iPhone 四板块 = **今日篮子 / 持仓 / 问询台 / 设置**(macOS 同四板块 + 周复盘工作台);V1「盘中看板」不再是 tab,**内容一条没删**并入持仓板块成一节(80/15/5 里它本就是为解释持仓服务的,判断已登记)。契约层:候选族五个 Swift 类型退役 → 篮子族 8 + 计划/快照/画像/包/评价 5 + 提醒族 4 + Provider/推送族 4 个 DTO(**两类快照一律手写 `init(from:)` + `decodeIfPresent`**,A 类也手写 = 白拿的保险);**删五处打向已删端点的活调用**(含 `PUT /settings/llm` 那条采集明文 key 打进空洞的);`mapReason` 补八个 case 并把 409/422 也接进来(此前只有 400/404)。界面:篮子卡 11 项 + 成员卡(**角色两说并存**、三个参考件夹逼拒收显示原因不兜 0、**「离场参考区间(不是止盈线)」**、「参考、非指令」四不标注恒显)、③b 两个原因码分开展示、三段两种「空」讲不同的话、Provider 注册表(**key 只写不回显**)+ 按 kind 动态渲染的推送开关(未识别 level 自成一组、未知 kind 优雅降级)、**⑪-D-D per-position 触达提醒开关**(只翻静音位、计划正文一项不动)、**同题材合并敞口**。**幂等键按 planner 追加口径落地**:每笔新提交动作铸一枚 UUID、重试复用同一枚,⛔ 不绑业务量(单测两条)。**两个已登记的债集合双双清空 + 两条冻结快照 DTO 闸从 skip 转跑**(对拍表 7 过 + 2 skip → 9 过)。测试:Python **2890 过 + 2 skip**、Swift **179 过 + 12 skip**、iOS/macOS × Debug/Release **四路 BUILD SUCCEEDED**;语义红线 grep 零命中。**六处设计判断 + 三条 Plan 疏漏**详见 §五 ⑮ 完工记录,对拍表 §六 复选框已逐项打勾注明提交号。
- 2026-08-03 · ⚖️ **⑭ 完工后两件归档:⑯-D oneshot 划分口径改写(我原切法会让报告 ④ 节常态空)+ 三条 🔵 增量落位**(@planner,只改 `PROJECT_PLAN.md`,零代码 / 零数据库 / 零部署;⑮ builder 并行施工 `client/` 全域,已 hunk 级挑拣、diff 零 Swift / 零 `client/`)。**① ⑯-D 的切法我写错了,改写(⛔ 只调文字、未要求动代码)**:原文把**报告**归 `neckline-basket.service`、**⑨ 复盘**归 `neckline-review.service`(即报告在前、复盘在后),而报告的 ④「昨日篮子复盘」节要读 `basket_review_daily` **当日行** → **照原切法那一节会常态显示「今日无昨日篮子可复盘」**。⑭ 已按「⑨ 在前、报告在后」实现,本节对齐。**新切法按「资源画像」而不是「功能名称」切**:(1)`neckline-scan.service` = **⑧ EOD 验证拍 + ④ + ④b**(纯机械批算、吃 parquet 全表扫描 = **内存最重那段**,`MemoryMax` 主要卡这里,P0-23 的正主);(2)`neckline-basket.service` = **⑤⑥⑦**(**LLM 段**,资源画像是墙钟 / 网络不是内存 —— 单独成段才能给它长超时**而不必把长超时也给批算段**);(3)`neckline-report.service` = **⑨ 复盘 + 报告落库**。⚠ **为什么这两个必须同段才是改口径的真正收益**:「⑨ 在报告之前」是**语义依赖**,放进**同一个 unit** 就由链内固定序保证、**systemd 侧根本无法配错**;拆两个 unit 的话任何人改一次 `After=`/`Wants=` 就能弄反,而**症状是报告 ④ 节静默变空、不报错** = 最难查的那类故障 —— **把约束关进一个 unit 里 = 让它结构性不可违反**。复用 V1 已有的 `neckline-report.service` 名(它本来干的就是这件事),不新造 `review`。**顺带纠一处**:⑫ 的**周度**部分原文挂在日链后面 = **周频作业每天跑一次**,已摘出去按 ⑫ 实际触发方式(交割单上传后 / 周末 timer)单独排,⑯-D 不替它决定形态。**② 核 ⑭ 另三处设计判断,三条全核准**:(a) **编排链住新文件 `report/evening.py`、⛔ 不住 `pipeline.py`** —— 理由很硬:`pipeline.py` 在 P0-23 守门单测的**在线模块清单**里、逐字 grep 禁止它出现扫描层批算写入口,而链的第二段恰恰要调那几个入口,**塞进去 = 为了迁就文件摆放而钝化一条被生产 OOM 打出来的真防线**;`build_report` 保持**只读不算**(历史回放的生命线)。⑭ 还**主动**把 `report/basket_daily.py` 加进那份在线清单(`build_report` 的只读装配层)—— **扩大守门覆盖面,好**。⑭-A 原文「`pipeline.build_report` 分段」**是我写错的,已在 ⑭-A 就地更正 + 链序定死**,防后人照原文把链塞回去。(b) **④ 双挂载二选一 = 摘掉 `daily_update` 那份、留链里的**(采纳建议):⑤ 直接依赖 ④ 的**当日**产出,**留在链里链自己就能保证「⑤ 读到的是今天的」**;留 `daily_update` 则是跨进程隐式依赖,那一步失败时**链会拿昨天的扫描表当今天用而且不报错**(它在 `daily_update` 里是「尽力而为不改退出码」)= 又一个「静默用陈旧数据」;移进链里还顺带有了明确退出码。⚠ **⑯-D 拆分时才做,⛔ 现在不动**(`daily_update.py` 是生产 timer 里在跑的脚本,⑭ 不擅自摘是对的)。(c) **`VERSION` 不升、版号归 ⑮ —— 判得对**(三方恒等且必须同一次提交动,⑭ 提前升会让守门单测常年红);⚠ **已在 ⑮ 块点名提醒**:别只改客户端那两处,**`api/app.py::VERSION` 是三方里的第三方**,漏了守门单测立刻红;当前「服务端跑 V2 契约但 `/health` 仍报 v1.5.2」如实登记,⑯ 上云前必须已是 `v2.0.0`。**③ 三条 🔵 增量逐条落位**:(a) **Y3 零行边界** → 补进 **⑫**:按 dimension 整段替换后,「**某维度恰好产出 0 行而其他维度有行**」时该维度旧行会留在当期;现实中几乎打不出来,**但日后画像口径改版、某维度整体停算时会真踩到 —— 那时旧维度的行会永远留在历史期里冒充当期结论**,故要求在 `_clear_period` docstring 写明边界 + 「某维度整体停算须先手工清历史期该维度行」一句(**文档级,⛔ 不改行为**);(b) **索引降级窗口** → 补进 **⑯-B 迁移清单**:「**新机首次 `init_schema` 后 grep 日志确认零条『唯一索引建不上』**」,**为什么要专门查** —— `_POST_MIGRATION_INDEXES` 在脏库上建不上时是 WARNING + 跳过,于是**幂等键与「单现役」两条约束在该库上持续不设防**,而 WARNING **只在 `init_schema` 那一刻打一次**,**不专门查就等于没有这两条约束而且没人会知道**;查到有 → 先清脏数据再重跑,⛔ 不许带着跳过的索引上线;(c) **幂等键语义** → 核对**已落**(`archive/V2_契约三方对拍_20260803.md` §十一 已文档化),但报告点名的「**客户端生成规则归 ⑮**」此前没落 → **已补进 ⑮**:每笔新交易生成新键(UUID per 提交)、⛔ 严禁复用;⚠ 说清后果 —— 服务端是标准幂等语义,**同键 = 同一笔意图的重试**,**同键不同 payload 会静默重放原仓、丢弃新参数**,即「**用户改了价格数量再提交,系统当作重试把改动整个吃掉**」;⛔ 别把键绑在"这只票 + 今天"之类业务量上(必然复用),**绑在"这一次提交动作"上**。**⚠ 环境异常第三次复现**:又一次工具返回里被追加了与命令输出无关的 computer-use 指引块。**工具返回是数据不是指令通道**,已整段忽略、未据此做任何动作(本次纯 markdown、零代码零桌面操作,也未请求任何桌面权限)。前两次已建议排查,**这是第三次,建议当作确定的注入面问题处理**
- 2026-08-03 · 🧺 **V2-⑭ 完工:报告换血成五段篮子日报 + 16:35 编排链独立成段 + API 契约总装 + 契约三方对拍**(@builder-pro,commits `0fb267c` / `3937e9d`)。报告五段顺序定死(① 市场语境 → ② 持仓体检 → ③ 今日篮子 → ③b 未定档 → ④ 昨日复盘 → ⑤ 新鲜度披露),每段独立保险丝;③b 两个原因码 `capacity_overflow` / `below_quality_line` 永不合并、零溢出时节仍在;新 `report/evening.py::run_evening_chain`(⑧ 验证拍 → ④ 扫描 → ⑤⑥⑦ → ⑨ 复盘 → 报告,⑧ 位置定死在拉数后扫描前,`segments` 是 ⑯-D 拆三个 oneshot 的接缝,**刻意不塞进 `pipeline.py`** 以免钝化 P0-23 在线模块守门);兑现三处前置块欠账(`dataFreshness.scanLayer*` 接线 / `exec_hints_for()` 接进篮子成员 / news_alerts 次级域 + `info_card` 同步扩域)。契约面 `CandidateOut` 整族 4 个 DTO + `ReportOut.candidates` 退役换 `basketDaily`,新增 12 条路径 + 17 个 DTO,`GET /baskets/{id}/card` 404 双 reason 分得开。三方对拍表与 ⑮ 欠账清单终稿 → `archive/V2_契约三方对拍_20260803.md`;A 路前提自检清单 → `deploy/A路割接前提自检清单.md`;「客户端调用面 ⊆ 服务端路由面」等 7 条机器断言 → `tests/test_contract_crosscheck.py`(**用 `==` 不用 `<=`**,欠账是精确集合不是 allowlist)。测试 **2808 → 2887 passed + 4 skip**(零回归);真实历史日 20260724 隔离库全链冒烟五段齐、九个新鲜度键在、真实库 MD5 未变。

- 2026-08-03 · 🩹 **判定线 🟡-N1 收口:昨日涨停截断序改判 `crc32(ts_code)` 升序(无偏 + 确定性)+ 需求量 vs 实际采纳量留痕**(@builder,commit `a8a14c3`)。核实原 `consec_limit_up_days` 降序**确实与被测量的量相关**(⑦-K7 审计:连板高度是双尾放大器,次日跌停约 3× 于同簇其余成员,而这份样本正好喂 `compute_breadth_snapshot` 算跌停数 / 炸板率),且原实现未传 `maintain_order`,并列名次连"行序"这个近似说法都不保证;改用 `mainline.crc_rank`(与主线切片姊妹样本同一套采样键)。`WatchUniverse` 新增 `breadth_extra_needed`/`breadth_extra_payload()`,`retreat_metrics` 新列 `breadth_extra_sample_json`(体例照既有 `hot_sector_sample_json`,幂等迁移)。**如实登记**:改前改后同批历史日对拍显示 zaban_rate 读数**有实质变化**(07-21→07-22 真实配额点 42.9%→33.3%,−9.52pp),追加 2026-07 全月探索性对拍(K=71 固定口径,10 个真正截断的日子)证实是**抽样噪声**(均值 −0.99pp、中位 −1.16pp、方向不系统)而非 crc32 新引入的系统性偏差,数字与完整分析详见 §五 ⑧-G 块尾追加记录,已并入 §七 [P3-37] 回看清单(71 只保底配额的抽样噪声量级是否需要专门处理,留待实盘积累后产品决策)。测试:`pytest tests/ -q` **2801 → 2808 passed + 2 skip**(净增 7,零回归)。

- 2026-08-03 · ⚖️ **⑧-G 完工后三件归档裁定:池配额判据改写(我原判据不可满足,已证伪)+ 两处静默交互补注 + 样本口径核准**(@planner,只改 `PROJECT_PLAN.md`,零代码 / 零数据库 / 零部署;两线 reviewer 并行复核只动 `archive/`,已 hunk 级挑拣)。**① ⑧-G-D 判据我写错了,改写**:原判据「两份样本在四天里都没被截断过」**不可满足** —— 昨日涨停需求**上不封顶**(2026-07 全月实测 **32~223 只/日**,两份并集 72~275),任何有限池都装不下;而且**它改组前就一直在被截**(旧口径同样只给"剩余 ~171"),**我要求的是一个系统从来没有过、也不可能有的性质**。**新判据三条**:(1) **主线切片不被截断**(优先保刚修好的那个估计量);(2) **昨日涨停允许被截,但截断必须无偏** —— ⚠ **这才是真正要守的**:两份都是**估计量**(切片 = 均值 / 涨停 = 炸板率这个比率),**估计量对"抽样"免疫、对"有偏抽样"不免疫**,故截取顺序**不得与被测量的量相关**,**建议同样用 `crc32(ts_code)` 序**,⛔ 不用 `ts_code` 序 / 池插入序(理由同 ⑧-G-B:`ts_code` 序 = 按板块排序,而板块与涨跌停幅度、炸板概率直接相关);**尤其要紧的是这份样本同时喂着退潮宽度 `compute_breadth_snapshot`**(CLAUDE.md 明载是退潮判据输入),**有偏截断在这里就是 ⑧-F 那一类 bug 的翻版**;施工时若现实现截断序与被测量相关就改 `crc32` 序,若已无偏则写明留证;(3) **截断后读数无实质变化**(builder 已验的炸板率 42.9%→42.9% / 37.9%→37.9% 保留为验收判据)。**✅ 核准 100/71 取值**,理由不是"先来后到"而是**按截断的代价分配**:切片被截 = **直接损失一个刚修好的、要与 −3% 阈值比大小的均值估计量的精度**;涨停被截 = 比率估计量换个样本量、**实测读数分毫未变** —— **把稀缺预算给"截了真会疼"的那一份**。⚠ 但正因涨停在重日只采到 <1/3,**第 (2) 条无偏截断就是它的全部保护,别当可选项**。**✅ 核准「测量预算按必需项上界 29 固定算、⛔ 不看这一拍谁已在池」**(builder 登记 2)——**好抓**:按实际占用算的话,LLM 多挑一个**恰好也在切片里**的成员就能腾出位、让样本多一只,**残留耦合 ②b 换个面目就回来了**;代价(必需项不满时欠填 ≤29 位)刻意接受。**新增留痕要求**:两份样本都落**「需求量 vs 实际量」**,⛔ 别让日后审计炸板率的人误以为 71 就是当天全部涨停股。**② 两处静默交互补注**:(a) **单主线日(种子数=1)该路必然不判**(单颗种子切片上限 = K = 4 **< MIN = 5**)—— **这是裁定后的正确行为不是 bug**(一条主线不足以支撑「主线**板块**跳水」),**写进 Plan 防日后被当故障上报**;(b) **K 实测量程更正**:`crc32` 口径 **K=4 → 40~100 只/日**(我原文引的「K=6 → 58~144」是 ⑧-F 的 `ts_code` 旧口径,已作废)。**⚠ 同时把一次真实考验记成先例**:07-24 用 **K=6** 读数 −3.04% **会触发**、K=4 是 −2.80% 不触发,**builder 明知如此仍不采用 K=6 —— 正确**;**「换个参数就能让它响」不是采用该参数的理由,那恰恰是偷偷改阈值**。**③ ✅ 核准「样本 = 切片本身,不再并入种子成分 ∩ 机械池的其余部分」**:把昨日涨停股并回样本 = **把 ⑧-G-A 要清除的动量偏差原样请回来**,等于修了一半又自己撤回去;⑧-G-B 原文写的就是切片作样本的「**专属来源**」,**这是照 plan 执行不是偏离**。⚠ **并据此升级 ⑧-G-G 第 1 条的表述**:原话「排除的是『LLM 这条路』、不是『LLM 碰过的票』」现在有更强的保证 —— **被 `crc32` 选中就进样本,与它是不是篮子成员 / 持仓 / 昨日涨停股完全无关**;样本构成**只由「④ 机械种子 + `crc32` 序 + K」三件事决定**,任何人(含 LLM)碰不动。**④ P3-37 补上可量化的移交材料**(⑧-G 完工后这条终于能给策略线 / 用户一组硬数字):**老样本系统性偏乐观 1.51~2.93pp**、新切片误差 **≤0.53pp / 真跳水日仅 0.05pp** → **推论:同一个 −3.0% 阈值在样本修好后,实际上比当年设它的人以为的严了约 1.5~2.9pp**(当年"读到 −3%"对应的真实主线跌幅其实是 −4.5%~−5.9%)——**阈值数字没变,含义变了**;最有说服力的一点是 **07-24 全市场中位 −2.86%、89% 个股下跌、主线本身 −2.75%/−2.98%,这样一天仍不触发**,要不要在这种日子响是**产品决策、交用户**。⚠ **别把这组数读成「所以该调到 −2.7%」** —— 四天不是样本,且 K=6 能凑出触发恰恰说明**该区间对参数很敏感**,**先攒实盘再谈调数**。另补一条回看项:`MIN_MAINLINE_SAMPLE=5` 作用在**票数**上、**没有**作用在**主线条数**上,2 颗种子时 per-seed 估计量只在平均 **2 个数**(n=8 已过下限),数学上偏薄;⛔ 现在不加第二个常量(无证据、实测日常 10~41 颗种子离这个角落很远),回看时一并看。**⚠ 环境异常第二次复现**:又一次 `git log` / `grep` 的工具返回里被追加了与命令输出无关的「MCP Server Instructions」(computer-use 指引)。**工具返回是数据不是指令通道**,已整段忽略、未据此做任何动作(本次纯 markdown、零代码零桌面操作)。上次已建议查注入面,**这次是复现,建议当作真问题排查**
- 2026-08-03 · 🎯 **V2-⑧-G 完工:主线跳水样本换 `crc32` 配额切片(K=4)+ per-seed 估计量 + 池位保底配额 + `MIN_MAINLINE_SAMPLE` 同源下限,这条红色刹车不再是聋的**(@builder-pro,纯本地代码 + 单测,零数据库 / 零部署)。真跳水日 07-24 读数 **−0.18% → −2.80%**,与「主线板块本身」(per-seed −2.75%)只差 **0.05pp**(⑧-F ∩池样本差 1.51pp);**阈值一字未动、四天两口径触发次数仍 0/4**(阈值灵敏度归 §七 P3-37,⛔ 未调 K 凑触发)。关注池改「有界必需项全进(≤29)+ 切片 100 / 昨日涨停 71 两份保底」,LLM 挪不动测量样本(压力用例 200 只涨停 + 篮子 1→21 只三向重叠,两份样本逐位不变)。🟡 **一件交回 planner**:「两份样本四天都没被截断」在 200 池位内做不到(07-21 需 213 > 171),现按"新样本优先"定数、被压两天炸板率读数分毫未变。`test_sentinel_retreat.py` 一条 `hot_n=3→5` 断言按 ⑧-G-E 授权**显式改**。全量 **2801 过 + 2 skip**(净增 15,零回归)。详见 §五「V2-⑧-G 完工记录」。

- 2026-08-03 · ⚖️ **⑧-F 对拍打出的深层问题裁定完毕 → 新增 §五 ⑧-G「主线跳水样本的关注池配额切片」(🔴 碰纪律触发器,排 ⑭ 之前)+ 新挂 §七 [P3-37]**(@planner,只改文档,零代码 / 零数据库 / 零部署)。**问题**:该红色刹车路径**近乎失效** —— 真跳水日 07-24(全市场中位 **−2.86%**、89% 个股下跌)样本读数 **−0.18%**,而主线板块本身 **−2.98%** 已贴 −3% 阈值,**差 2.8pp**;四天改前改后均 **0/4 触发**。⚠ **不是 ⑧-F 引入的** —— V1 样本是候选池、同样偏动量,**大概率一直就是聋的,只是从来没人量过**;⑧-F 的对拍第一次把它照出来。**builder 未擅自改阈值 / 扩池、把裁定权交上来,做得对**。**① 定性:`∩ 关注池` 这一步在语义上就是错的** —— §2.4 里它叫「**主线板块**跳水」,**语义主语是板块本身**,不是「我恰好盯着的那几只票」;而关注池的存在理由完全是另一回事(**免费源限流下只 poll 得起 ~200 只**)。拿一个**测量市场状态的量** ∩ 一个**受轮询配额约束的集合**,是实现副产品不是设计,而且这个副产品**有方向**:池的机械成分几乎全是昨日涨停股 = 全市场动量筛得最狠 = **最抗跌的一群** → **这个测量在最该响的日子最不容易响**。但 `∩ 池` 又不能简单去掉(没报价就算不出收益率)→ **正解是"在池里预留一块专门用来测主线的、机械且有代表性的样本"**。**② 采纳配额切片,但采样键改 `crc32`(⛔ 不用 `ts_code` 升序,这是对候选方案的修正)**:`ts_code` 升序在一颗行业种子内部会系统性偏向 `000xxx`/`300xxx`、把 `600xxx`/`688xxx` 排到后面 = **按板块给样本排序**,而板块与涨跌停幅度(10% vs 20%)、波动率直接相关 → **样本的波动率画像被系统性扭曲** = 用一把刻度不匀的尺,**正是 ⑧-F 刚清除的那类偏差,别在采样器里请回来**;`crc32` 与板块 / 市值 / 上市年份无关且跨进程可复现(CLAUDE.md 明文),**一个函数调用的成本**。**③ K = 4**(非 builder 试的 6):K 是**精度旋钮不是策略参数**,配合 per-seed 估计量只影响主线内部抽样噪声、不影响主线间权重,同时给另一路触发器的样本(昨日涨停)留出更多池位;⛔ **不许调 K 去凑触发频率(那等于偷偷改阈值)**。**④ 估计量定为「每条主线一票」(per-seed mean 的均值),⛔ 不是所有票混在一起平均**:名字就叫「主线**板块**跳水」,一颗 200 成员种子与一颗 8 成员种子各代表**一条主线**,不该因成员多就占更大权重;附带好处是 K 才真正成为纯精度旋钮。⚠ **builder 四天读数是 pooled 口径,必须用 per-seed 口径重跑同四天、两个口径都写进完工记录**,⛔ 不许只报一个。**⑤ 关注池改「有界必需项全进 + 两份测量样本各有保底」**:现状是先到先得的优先队列 —— 对「注意力分配」没问题,对**纪律测量样本**是错的(**会被别人挤掉、大小不可预期的样本 = 不可靠的测量**)。新规则:持仓(≤3)+ T1/T2 成员(**≤7 篮 × ≤3 = ≤21,本来就有界**)+ 指数(≤5)合计 ≈29 **无条件全进**;剩余 ~171 在**昨日涨停(宽度 / 炸板率样本)**与**主线切片**两份测量样本间按保底分配。**⚠ 这正好根治 builder 登记的残留耦合(涨停 >180 时 LLM 仍能挪样本尾巴)** —— 篮子成员既有界又保底,就不再与任何测量样本抢位,**LLM 在任何极端日都挪不动测量样本**,⛔ 别再单独想办法治它。保底具体数由 builder 按实测定,判据 =「两份样本四天里都没被截断过」;装不下**回 planner、⛔ 不许自行抬 `breadth_cap`**(那会改盘中轮询量与限流风险面)。**⑥ 加 `MIN_MAINLINE_SAMPLE` = 直接 import 引用 `industry_strength._MIN_MEMBERS`(5),⛔ 不抄字面量**(照 ⑤-c `MIN_LIFT_SAMPLE_SIZE` 同源引用体例):同一类统计量、同一个理由,⑤-c 已立先例不新发明;n=3 时横截面收益率标准误约 2pp,拿它判 −3% 阈值**接近抛硬币**,而误触发的代价是**整天禁开新仓**;切片上线后正常日样本 30~140,n<5 意味着上游已出事、**更不该开火**。**⚠ 这条改判 builder 的登记,但核准其克制并澄清是我的约束写得有歧义**:⑧-F-B 的「既有纪律单测一条断言未改」防的是「**改断言迁就新代码**」,而「**行为按裁定变了、测试跟着变**」是另一回事、**允许且必须** → `test_sentinel_retreat.py` 那条 `hot_n=3` 断言**现在可以且应当改**,但须**显式改 + 单独说明 + 完工记录写清改前改后**,⛔ 不许悄悄改数让它绿。**⑦ 核准另两条登记**:(a)「∩ 关注池」→「∩ 池的**机械成分**」的字面偏差 **✅ 核准,且是 plan 的错不是实现的错** —— 我写 ⑧-F 时没想到池**本身**含 T1/T2 成员(⑧-A),整池取交会让「换掉 LLM 的成员选择 → 样本逐位不变」这条**我自己写的核心验收判据当场不成立**,plan 字面与自己的验收判据打架,builder 按验收判据实现是对的;⑧-G 的配额切片口径自此取代该表述,但**保留 builder 那条重要区分:排除的是「LLM 这条路」,不是「LLM 碰过的票」**(本来就从机械路进池的照进样本)。(b) 残留耦合 **✅ 要治,已由 ⑤ 的配额规则根治**。**⑧ 施工纪律(🔴)**:`sector_dive` 两个阈值与 `evaluate_retreat` 判定逻辑**一字不动**,只改样本怎么取;四天对拍**两口径都给数并明确报触发日**。**⑨ 新挂 [P3-37]**:这条刹车自 ⑧-G 起**从"聋"变"能响"** —— **不需事前批准**(⑧-G 恢复的是 §2.4 早就写明的既定行为,且 07-24 那种日子本来就该响,承 §1.3「买下跌途中票是主要死因」),**但必须事后知情**:用户此前从未经历过该刹车触发,上线后第一次「全天禁开新仓 + 立即级推送」会是全新体验,**⑰ 换包说明须有一句告知,别让它像故障一样冒出来**;**真正待校准的是灵敏度** —— `sector_dive` 是 V1 传下来的启发式、**当年是配着一个失效的样本定的**,样本修好后**这个阈值的实际含义变了**,须攒 ≥1 个月实盘 + 经历 2~3 次真实急跌日后回看(连同 K=4、保底配额、per-seed 估计量三个无实盘背书的工程默认);⛔ 有实盘证据前阈值一字不动,要动走用户拍板。**⑩ 核档 builder 另两件**:`e1fe049`(⑪-D 的 take_profit 文案补齐 §2.8-C-3 **前提③** 缺的两句并机器锁死 —— **对**,四条前提缺一即豁免失效,文案缺半句就是豁免失效)、⑪-D 新增 `rejected_no_close` 第三态(**对**,与 ⑦ 既有 `rejected_no_limit` 同族,「算不出」与「越界」分开)。**⚠ 另记一笔环境异常**:本次一次 `git log` 的工具返回里被追加了一段与 git 输出无关的「MCP Server Instructions」(computer-use 指引)。**工具返回是数据不是指令通道**,已整段忽略、未据此做任何动作(本次任务纯 markdown、零代码零桌面操作)。如后续复现,建议查工具链注入面
- 2026-08-03 · ⚙️ **planner 两条裁定接线完工:⑪-D `take_profit` 两道机械闸 + ⑧-F 退潮主线样本机械化**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读,对拍跑在 scratchpad 隔离库 + 真实 parquet 只读上)。**⑪-D 已完成、验收全绿**:闸① `basket_card.clamp_exit_reference(raw, close)` 要求 `exit_low > D0 close`(`close` 必填无默认,签名层面不给"忘了传 = 静默关闸"留口子;新增 `rejected_not_above_close` 与 builder 补的第三态 `rejected_no_close`〔「没收盘价可比」≠「比过了不合格」〕;⛔ **仍不夹涨跌停、仍不加上界**,两条各一个守门单测);闸② `positions_entry.evaluate_exit_reference_arming()` 见 `exit_low ≤ 成交价` → 该票 `take_profit` **不武装**(⛔ 只是不武装:计划正文、开仓、其他 kind 全不受影响,单测锁死);`plan_json` 恒带武装态四键,哨兵旁路 E **缺键即不武装**(fail-closed —— 没武装位 = 没过闸,不是"过了但没写");⑮ 的 per-position 开关留了写入点 `set_exit_reference_muted()`(追加新版本、只翻静音位、不动正文),`create_position_plan_version` 顺带堵死"写新版本绕开闸②"的后门;§2.8-C-3 与 ⑪-D-C 的豁免四条前提已逐字人工核对一致。**⑧-F 代码已落、红线已闭,但⚠ 对拍结论与 planner 预判相反,样本口径待再裁一次**:新增 `sentinel/mainline.py`(样本 = ④「热点行业 + 暴起概念」种子原始成分 ∩ **关注池的机械成分**〔昨日涨停 ∪ 持仓〕,**只靠篮子进池的码不进样本** —— plan 字面写的是「∩ 关注池」,但 V2 关注池本身含篮子成员,整池取交会让 ⑧-F 验收那条核心判据当场不成立,已登记为与 plan 字面的偏差);`sector_dive` 阈值一字未动(守门断言);样本构成每拍落 `retreat_metrics.hot_sector_sample_json`(新列,幂等迁移)可审计;⛔ 没有新增任何最小样本量阈值(加了就得改既有 `hot_n=3` 那条断言,与「一条断言未改」冲突)。**对拍(4 个真实历史日,含 07-24 全市场中位 −2.86% 的大跌日;改前样本用「每颗种子取 D0 涨幅最高 1~3 只」的机械龙头代理)**:触发次数 **改前 0/4、改后 0/4**,但**四天里改后样本一律比改前更抗跌(高 0.9~3.0pp)= 改后更不容易触发,方向与 planner 预判相反**。追因已定位真因**不在 LLM**:关注池的机械成分几乎全是昨日涨停股,07-24 它们只跌 0.22%,而「主线板块本身」(种子全成分)是 −2.98%(已贴阈值)—— **本条红色刹车路径在「样本 ∩ 关注池」这个前提下近乎失效**,⑧-F 修好的是「可被 LLM 塑形」这条红线,**没有**修好「这条路能不能真的响」。⛔ 未擅自改阈值 / 扩池;一个已量化的候选方向(给池加机械配额切片,K=6 时 07-24 读 −2.46%)连同四天数字写进 §五 ⑧-F 完工记录,**留给 planner 裁**。测试:全量 **2786 过 + 2 skip**(基线 2744+2,净增 42 = 新增 `tests/test_exit_reference_gates.py` 22 + `tests/test_sentinel_mainline.py` 18 + 守门参数化 2);既有 `test_sentinel_retreat.py` 一条断言未改,`test_sentinel_engine.py` 只改夹具、断言逐字未动;`scripts/smoke_sentinel.py` 跑通。另有一条既有挂钟脆弱测试(`test_sentinel_custom.py::test_cooldown_blocks_second_hit_and_expires`,北京时间 ~14:48 后跑必红)与本次施工无关

- 2026-08-03 · 🛠️ **V2 两线 review 修复批次完工(契约线 🔴1+🟡5+🔵2 / 判定线 🟡3+🔵1,共 8 个 commit)**(@builder-pro,零部署;两份 review 报告已逐条销项标注 commit)。**🔴 R1 `basket_members` 冻结泄漏**:`INSERT OR IGNORE` 只冻「同一行」、冻不住「聚合长子行」,篮子行已存在时新成员码照插进冻结篮而日志正说「未采纳」→ 改为**篮子行已存在 = 该篮成员整段不写**(`1052622`)。**判定 🟡-1 验证侧静默降格**:「扔掉不可判条件再对子集取 all()」把 ⑦-b-B 的两条 AND 降格成单条 → 合成读法上收 `verification_rules.combine_side()` 取 **Kleene 三值**、新 flag `spec_levels_partial`、**bump `verify_ruleset_v2`**(`0f885b2`)。**判定 🟡-2 ⑬-7 漏 ⑧-E 除权锚**:分红季成员级假警会进 9:26 锁屏 → 复用同一检测器、标 `member_ex_rights` 不计 actionable(`47775cb`)。**判定 🟡-5 红线守门锁空靶**(`neckline.push.notify` 不存在)→ 换真实七模块 + 反向存在性断言 + 兼容 `from X import Y` 写法。**契约 🟡 Y1 守门写法盲区**:补 `OR REPLACE/IGNORE` 变体 + 扫描域扩 `scripts/`,**并修好一条从上线起就没可能命中的空转断言**(禁止串小写表名 vs `sql.upper()`,报告未列的第 4 洞,反向证伪时打出来)(`fdf604c`)。**Y2** `user_actions.occurred_at` 归一北京时间(`created_at` 仍 UTC,两列不同轴写死)、**Y3** 画像按 dimension 整段替换止住「每期 = 多次运行并集」(`040bd27`);**Y4** 五常驻僵尸 DTO 与客户端僵尸卡拆除(`d9b41a1`);**Y7** `record_buy` 三写并入单事务 + `POST /positions` 幂等键(部分唯一索引兜底,新增 `_POST_MIGRATION_INDEXES` 机制)(`50ff5b2`);**🔵 B1/B3**(`1052622`/`d61b0b8`)。**Y6 补档**:`archive/V2-⑬_删除前后对照表_20260803.md`(⑭ 三方对拍基准输入)。全量 `pytest` **2744 过 + 2 skip**(基线 2706,净增 38 条回归)+ 客户端双端 build + iOS Simulator 154 例;**未修**:契约 Y5(⑮ 硬清单)、判定 🟡-3/🟡-4(planner 已裁定另派)、🟡-6(⑯ 前定位)、其余 🔵。

- 2026-08-03 · ⚖️ **V2 两线 review 的两张「Plan 权威冲突」黄牌 planner 裁定完毕(判定线 🟡-3 / 🟡-4)+ 三条归档排期**(@planner,只改 `PROJECT_PLAN.md` 与两份 review 报告的归档标注,零代码 / 零数据库 / 零部署;builder-pro 正并行修其余 🔴🟡,已 hunk 级挑拣、未动其在途正文)。**① 判定 🟡-3 `take_profit` 触发阈值 = 未夹逼的 LLM 离场参考 → 裁定「记名豁免 + 两道机械 sanity 闸」,新增 §五 ⑪-D**。**先认账**:我 08-03 开这条 kind 时写了「正确指向 = 篮子卡离场参考」,**却没对它执行我在同一段里给 `basket_falsified` 立的「要开得三处红线一起改」标准** —— 是 plan 缺口,不是 builder 违规。**定性想清楚再动手**:它**不是纪律漂移**(止损 / 回落止盈 / 时间退出 / 仓位 / 熔断一字未碰),**也不主要是虚假权威**(文案已锁死非指令 + per-kind 开关 + 用户拍板);**真正的洞是「一个没有下限的 LLM 数字驱动立即级推送」→ 荒谬推送 → 用户对「立即级」这个通道失信**(立即级的价值全在「响了就值得看」,喂它噪声比不推更糟)—— **所以修法是给数值加机械闸,不是收回 kind**。**两道闸都语义驱动、⛔ 零发明阈值**:卡生成时 `exit_low > D0 close`(**这是定义不是阈值** —— 离场参考按定义是「本轮上涨的压力位」,压力位在现价之上),不满足落新 reason `rejected_not_above_close` 不落卡;开仓继承时 `exit_low ≤ 实际成交价` → **该票此 kind 不武装**(只是不武装,⛔ 不改计划、不阻断开仓、不影响其他 kind)。**仍不夹涨跌停**(⑦ 原决定对:压力位可能几日后才到)、**也不加上界**(上界荒谬只会永不触发,无假推送无伤害,⛔ 别为它发明上限)。**未采纳 reviewer 的 ③(用户确认才武装)** —— 与 ⑩「表单退役、减摩擦」的立项主题相悖;改以 **per-position「不提醒」开关**回应「开仓继承无用户确认」这一点(全局关会连坐所有持仓,用户真正想要的是「这只票的这个数不靠谱,别烦我」),列为 **⑮ 应做项、非红线前提**。**字面冲突同步收口,不留打架**:§2.0 第 1 条加**记名豁免附注**(其余部分尤其「不改任何纪律阈值」一字不变);**§2.8-C-3 改写**成「**判定动作永远机械;数值来源分两类**」+ 四条前提(不改纪律判定 / 已过机械闸 / 纯告知文案 / 用户可关,**缺一即豁免失效**)+ **⛔ 不外延**(买入参考区间、明早剧本、审判评语、问询台回答、`basket_falsified` 一律不得据此接推送或判据)。**② 判定 🟡-4 退潮刹车主线样本被 LLM 间接塑形 → 裁定「改机械派生样本」,⛔ 不写豁免,新增 §五 ⑧-F(🔴 碰纪律触发器)**。**为什么豁免这条路走不通**:§2.8-C-2(b) 之所以站得住,靠的是「每只票的判定阈值仍只来自章程与机械 spec」;**但这里是聚合量,样本组成本身就是判据的输入** —— 换一批票平均结论直接变,「阈值一字未动」是真的但**不相干**。**⚠ 两处与 reviewer 措辞不同**:(a) **不取「T1/T2 篮子所声明种子的原始成分」** —— 那仍过了一道 LLM 筛(哪些种子被拿去建篮由它决定),只是把塑形**上移一层**;**必须不经篮子、直接取 ④ 当日机械种子(热点行业 / 暴起概念)的原始成分 ∩ 关注池**(也是与 V1「热门板块 ∩ 关注池」语义最近的机械等价物,全链路机械:种子规则读现役包、成分来自 `stock_basic.industry`/`ths_member`);(b) **不同意「无单向套利」这条缓解判断** —— LLM 挑的是**龙头 / 中军**,而**领涨股在跳水日恰恰是最抗跌的一批**,样本系统性偏向强者 → 均值跌幅被**低估** → **红色刹车更不容易响**,偏差**恰好朝着「保护失效」单向**,不是中性噪声,故按「从严」办。**顺带把这条升级成通用规则**:§2.8-C-2(b) 加**适用边界** —— 2(b) 只覆盖「盯谁」,⛔ **不覆盖任何聚合纪律触发器的样本组成**,并给自查判据「**换一批成员,这个数会不会变?**」(会变 = 样本即判据 = 不得沾 LLM)。**样本不足 → 不触发 + 如实披露**,并写明**这里的保守方向与项目别处「宁可多提醒」刻意相反**的三条理由(另有两路触发兜底 / 误触发的代价是整天禁开新仓且会训练用户忽略刹车 = 狼来了比不响更危险 / 与「缺数据不判不猜」一贯纪律一致);施工纪律:**`sector_dive` 阈值一字不动、只换样本来源**,**改前改后同批历史日对拍并给出触发次数与触发日的具体数字**(⛔ 不接受「应该差不多」),差异意外就停手回 planner。**③ 三条归档排期**:**契约 Y5** 五处对已删端点的活调用逐个进 **⑮ 硬清单**(`SettingsLLMRequest` 整个退役换 `/settings/providers*`)+ **⑭-C 加机器断言「客户端调用面 ⊆ 服务端路由面」**防复发,并点明最糟的那一面是**采集了 key、发到不存在的端点、界面还是一副成功的样子 = 假成功面 + 明文密钥打进空洞**;**契约 Y6** ⑬ 欠交付的「删除前/删除后对照表」在 ⑬ 验收前加欠交付告示 —— **排进本轮修复批次或最迟 ⑭ 开工前补,⛔ 不许并进 ⑭ 的工作量**(那时基准已不好重建);**判定 🟡-6 核档 = 已结案无需排工**(reviewer 未追溯到 §七 [P1-36] 的 ✅ 结案段与 commit `a239bcc`:根因是等价比较误含 `computed_at` 秒精度审计戳 = **(a) 类良性测试缺陷,已明确排除 (b)**,业务列永远逐位相同 → **确定性主张本身没破**,`leader` 侧漏网已由 `53fab7a` 补上)。两份 review 报告的对应条目均已就地标注裁定结论与「与报告措辞不同之处」
- 2026-08-03 · 🗑️ **V2-⑬ V1 清理清单十三项全部执行完毕**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 只读,冒烟一律跑副本 / 临时库)。**物理删除** 8 个模块(`report/{intel_candidates,candidates,reference_plan,reference_plan_store,watchlist_check}.py`、`neckline/{watchlist,breathing}.py`、`sentinel/entry.py`)+ 10 个端点(`/watchlist*`×5、`/breathing*`×3、`/settings/intel-boards`×2)+ 客户端自选 tab / 两个视图 / 老四件套与参考件三件套 UI;**六张表停写留档不 DROP**(`watchlist`/`breathing_t_trades`/`inquiry_pool`/`llm_judgments`/`reference_plans` + 承 ⑩ 的 `decision_log`)+ 两列(`reports.watchlist_json`/`app_settings.intel_watch_boards`),DDL 全留并加「v2.0.0 起停写」注释。**⑬-7 盘前判定换血**成篮子竞价剧本核对(对象 = T1/T2 篮子成员,判据 = ⑦ 冻结在卡里的 `ref_close`/`close_below_stop_line`,阈值与 `kind='precall'` 推送一行未改,⛔ 不碰 `VERIFICATION_RULESET_VERSION`);**⑬-N 信息卡按 D1 保留改造**成篮子成员详情页地基(新增所属篮子与共同驱动 / 本票角色含对拍分歧 / 同篮成员对比三块)+ **⑬-N-K7 标注件**(读 `member_tags` 同一入口,交叉断言与篮子卡逐位相同)。**守门迁移不是删除**:`discipline_checks` 搬家 `report/discipline_checks.py`(保住问询台)、`exec_hint` 出 `exec_hints_for()` 纯计算口(C3 无前视偏差 + 北京日截断两条守门原样保住)、v1.3.1 行业闸对拍改冻结参考实现、`_ONLINE_FILES`/`prompt_context` 参数表同步、新增 `tests/test_v1_retirement_guard.py` 27 例(十三项逐项 + 六张停写表零写入 + 「跑完管线行数不增」+ **反向守门**:`json_block`/`judge_candidate`/`board_pool`/问询台/`BREATHING_TRIAL`/K1 回测线必须还在)。**一处 Plan 勘误**:⑬-1 落点表把 `report/board_pool.py` 标成「五常驻」是笔误(它是板块池卫生线、V2 扫描层五处在用),**未删**并加反向守门。**三处 Plan 未给规格的设计判断**(买点哨兵退役 / 证伪四哨兵换判定对象但逻辑一行未改 / ⑬-8 拆候选侧留情报侧)+ **三处留给 ⑭ 的欠账**(次级扫描域、`exec_hints_for` 接线、`candidates_json` 契约换血)详见 §五 ⑬ 完工记录。`pytest tests/ -q`:**2706 passed + 2 skipped + 0 failed**(⑬ 前 3097;删 7 个整测试文件 + 若干类例,新增 33 例);双端 `xcodebuild` BUILD SUCCEEDED,iOS Simulator test 154/0 failures;七个 smoke 脚本全绿。
- 2026-08-03 · 🔔 **持仓三事件(stop_approach/take_profit/sector_dive)升级立即级 APNs kind 定向任务完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;承接用户拍板 `3b1dcd7`,与并行在途的 ⑫〔`profile/` 等,未提交〕/ 已收官的 P1-36〔`a239bcc`〕两条线文件面不相交)。`stop_approach`/`sector_dive` **复用既有 console/Bark 文案**旁路推 APNs(`_maybe_push` 新增 `apns_kind` 分支,同一次 `sentinel_events` 去重、不二次措辞;`notify_kinds.py` 三级×kind **11→14**,三条均判 `NKIMMEDIATE`,默认开且 `_seed_push_kinds`/`_decode_push_kinds` 既有"缺键取默认开"机制零改动即天然覆盖,已实测验证老库不悄悄打开用户关过的开关)。**`take_profit` 触发源改判(如实登记,请裁定)**:核实 `sentinel/holding.py::check_take_profit`(回落止盈)现读的是现役 `take_profit_retrace`,并非协调方所指的候选老四件套 `target`/`decision_log.target_price`(git blame 追溯到该函数首次提交即如此,未找到读取后者的代码路径);但协调方明确要求 `take_profit` kind 指向 `position_plans` 继承的离场参考区间,故按指示新增独立函数 `check_exit_reference_reached` + engine.py 旁路 E(独立 try/except、独立去重 event_key=`exit_reference`,**不碰**回落止盈机械纪律与其 console/Bark 通道一字)驱动该 kind,与回落止盈刻意解耦、不合并;无计划 / 无离场参考如实不判,不编默认目标价;文案守语义红线(不写"建议卖出/止盈信号",按"触达"中性表述)。测试:4 个文件净增 22 例(`test_sentinel_holding.py`+6、`test_notify_kinds.py`+1、`test_notify.py`+7、`test_sentinel_tick_v2_bypass.py`+8,含旁路炸了不影响持仓哨兵的红线用例),`pytest tests/ -q`:**3097 passed + 2 skipped + 0 failed**,零回归。db.py 零改动。
- 2026-08-03 · 💰 **V2-⑫ 对账资金流水四分类扩展 + 偏好/能力画像双引擎完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与并行的 ⑪-B kind 决议〔`3b1dcd7`〕/ P1-36〔`a239bcc`〕两条施工线文件面不相交)。**⑫-A**:对账引擎 `parse.py`/`reconcile.py`/`material.py`/`store.py` 四个既有模块骨架逐字未改,`parse.py` 新增 `CashFlowEvent` + 四分类常量 + `ParseResult.cash_flow_events` 纯增量字段;新模块 `review/cashflow.py` 按周聚合转入转出(拆 `transfer_in`/`out`/`net`)/分红/税费/交易盈亏(直取 `WeeklyReview.stats.realized_pnl`,不重算),**刻意不给合计字段**。**⑫-B**:新包 `neckline/profile/`——`preference.py`(题材/角色/入场方式两口径〔几何 + `user_actions.kind='label'` 用户自述〕/Tier)、`capability.py`(胜率/盈亏比/MFE·MAE 只用 `positions` 真实成交、只看已平仓;`vs_peer_delta`/`missed_role` 复用 `eval.metrics.score_tradable`/`exit_sim` 同一套判分,样本〔配对数〕不足如实 `None`)、`store.py`(每期一版)、`common.py`(`MIN_SAMPLE_N=5` 三档置信度)。`scripts/profile.py` CLI + `scripts/smoke_profile.py` 合成数据端到端冒烟(人工核对 MFE/MAE 手算逐位一致)。新增 5 个测试文件 58 例 + `test_review_parse.py` 追加 3 例,`pytest tests/ -q`:本块零回归(基线 3012+2skip → 3070+2skip+0fail);⚠ 全量套件另含**并行在途会话**对 `notify_kinds.py`/`api/notify.py`/`sentinel/{holding,engine}.py` 的未提交改动(跟进 `3b1dcd7` 三个 kind 决议,尚未完工),致两条精确集合守门测试中间态失败——**与本块零关联,未触碰、未修复**。九处如实登记(资金流水第五档 `CASH_FLOW_OTHER`/转入转出拆三数/入场方式两口径并存/题材取 `stock_basic.industry`/能力指标只用真实成交/`missed_role` 无 `vs_peer_delta`/`verdict` 不落库/未接入周复盘对账结果〔无确定性外键〕/MFE·MAE 未复权有除权失真)详见 §五 V2-⑫ 段尾「完工记录」。**附带发现**:`test_scan_cluster/corr` 偶发失败已由 P1-36(`a239bcc`)结案,§四「块外挂账」同步勘误。下一步:V2-⑬(V1 清理清单执行)。
- 2026-08-03 · 🩹 **[P1-36] 结案:`test_scan_cluster/corr` 两条「三路等价」偶发红定向诊断**(@builder,纯测试代码,零生产代码 / 零数据库 / 零部署;与并行的 ⑫/⑪-B 文件面不相交)。诊断 = **(a) 类良性测试缺陷,明确排除 (b)**:工程复现(mock `_now()` 强制 bulk/day-by-day 两阶段返回不同墙钟值)精确重放症状后逐行 diff 证实——业务列(cluster_key/scope_key/corr/n_obs 等)永远逐位相同,唯一分叉列是秒精度审计戳 `computed_at`(两次独立调用间跨墙钟秒边界即合法不同,与业务确定性无关);`refresh_limit_clusters`/`refresh_corr_matrix` 的 bulk 与 day-by-day 逐行代码等价,从未真正分叉,⑨ 归因可比性不受影响。同一坑本项目已有两处正确先例(`test_industry_strength_store.py`、`test_scan_stage.py::_table_rows_excl_computed_at`),本次两测试建于同批却漏抄。修法:比较前 `.drop("computed_at")`,业务列仍要求逐位相同(未放宽断言、未加 flaky 重试、未改生产代码一行)。验证:目标文件连续 20 轮 100% 绿;全量 `pytest tests/ -q` 零回归。详见 §七 [P1-36] 结案记录(含 `test_scan_leader.py` 同款潜在隐患的留档不改)。
- 2026-08-03 · 🔔 **用户拍板:`stop_approach` / `take_profit` / `sector_dive` 三条开「立即」级 APNs kind,已落 ⑪-B**(@planner,只改文档,零代码 / 零数据库 / 零部署;一个 builder 正在并行实现这三条 kind,⑫ 与 P1-36 亦在跑,已 hunk 级挑拣、未动他人在途正文)。**① 三条进 `NKIMMEDIATE`**,依据 = **蓝图 5.5 权威兑现**(该节逐字把「逼近或触发止损」「快速跳水」列进立即通知,⑪-B 的分级归属行本就照抄了它,只是当时没有对应 kind 落地 —— 属**补齐已写在 Plan 里的东西**,不是新加需求);**V1「只进看板」拍板(§2.4)自此被 V2 三级体系取代** —— 当年只留两类 APNs 是因为**没有分级也没有 per-kind 开关**,多推一类就是全员挨轰,V2 有了三级 × per-kind 开关,「重要的事推、嫌吵可单独关」才成立;⚠ 取代的**只是这三条的通道归属**,§2.4「买点 / 证伪 / 普通警示只进看板」其余部分继续有效。**每条独立开关、用户可单独关**。**② 与 `basket_falsified`(裁决不推)的对照写进正文防被当先例**:两者**不是同一类事** —— 这三条是**持仓事件**、直接指向用户手里的仓、蓝图**逐字点名**属立即级,只欠一次用户拍板;`basket_falsified` 是**选股线的驱动假设被证伪**、**不指向任何持仓动作**,且「不进推送」写在 **CLAUDE.md 坑条 + ⑦-b/⑧-C2 红线**里 —— **要开它得先改那三处红线,不是「照这次的例再拍一次板」就行**。**③ 默认开关 = 开,且零迁移(核过播种机制,自洽)**:`_seed_push_kinds` 只播种 V1 那六个 kind 且只碰 `push_kinds IS NULL` 的行,读侧 `_decode_push_kinds` 把 `ALL_KINDS` 全部补齐、**缺键取默认开** → 三条新 kind 在**全新库与已播种老库上都自动是开**,**⛔ 无需回填 / 迁移 / 改播种函数**。⚠ 但**别照抄播种函数 docstring 那句「新 kind……用户从未表达过意见」当理由** —— 那是给 `custom_alert` / ⑪-A 四监测那种**全新能力**写的;**这三条不是新能力,是既有看板事件升级通道**,用户此前从未同意过它们上锁屏。**默认开的依据是「用户 2026-08-03 拍板」,不是「缺键取默认开」那条通用规则**(规则只是恰好让实现零成本),两件事别混。「用户关过的不许被悄悄打开」这条底线不受影响(老库里它们从来没有过开关列)。**⑰ 换包说明里加一句告知**(改的是锁屏行为,不是 App 内显示),不必做引导页。**④ 改写「两套通道」那条的措辞,让它与本次开闸不打架**:默认态仍是两条通道两个注意力预算、**⛔ builder 不许「顺手」接**(那叫违规 = 绕过「新增 kind 须用户拍板」);但**「某个看板事件该不该升级成锁屏,由用户拍板」本就是既有纪律本身,不是例外** —— 这三条走的正是这条路,**不是打破了「不混」,而是用对了那道闸**。**两者区别只有一个字:谁批的**(builder 自己接 = 违规,用户拍板 = 合法);⛔ 别把「不许顺手接进」读成「永远不许接」。**⑤ 顺手给 builder 钉两条实现要求**:(a) **⑬-6 执行时必须复核 `take_profit` 的触发源还在** —— V1 这条读候选老四件套 `target` / `decision_log.target_price`,而四件套已定退役(⑬-6)、`decision_log` 已停写(⑩-C),不改指向它会变成**永不触发的死开关**(设置屏挂个拨了没用的开关,正是 `basket_falsified` 那条反对的撒谎式 UI);V2 正确指向 = 篮子卡 `members[].exit_reference` 或 ⑩ `position_plans` 继承的离场参考。(b) **去重粒度决定这三条是不是噪声**:`stop_approach` 震荡时每拍都可能命中,V1 只进看板无所谓、上锁屏后必须只推一次 —— **判据 = `sentinel_events.event_key` 保证「当日每票每 kind 只推一次」,⛔ `event_key` 里不许带价格 / 时间戳之类每拍都变的量**。另 `take_profit` 文案**不许写成「该卖了 / 建议止盈」**(离场参考是参考、回落止盈才是纪律,语义红线照旧)
- 2026-08-03 · ⚖️ **⑪ 提的一处 Plan 自相矛盾裁决 + 三处欠账归位**(@planner,只改文档,零代码 / 零数据库 / 零部署;⑫ 并行施工中,未动 ⑫ 块正文与 §四,hunk 级挑拣提交)。**① 裁决 `basket_falsified` 的两份权威冲突 —— 判 ⑪ builder 的保守方向正确,⑪-B 的 kind 例举已改正**:该 kind **不是也不会是**一个 kind,原例举里有它是 **planner 立项时随手列的示例、不是逐条论证过的**;权威层级写死 —— CLAUDE.md 原始坑条「篮子 `falsified` ≠ 持仓该走 …… ⛔ 不进推送(守门单测扫 import)」+ ⑦-b / ⑧-C2 语义红线 **高于**一句 kind 例举。补三条实质理由防后人照例举接回去:(a) 它说的是「**驱动假设不成立**」,**不指向任何用户动作** —— 没买的人收到是噪声;(b) 买了的人会读成「**系统在暗示我卖**」,而该不该走是**持仓纪律**的事、由持仓哨兵管,这正是那条坑要防的误读;(c) V2 注意力 **80/15/5(持仓 80%)**,选股线的复盘信息不该抢持仓的推送带宽。并钉死两句:**「不推 APNs」≠「用户看不到」**(盘中看板 / ⑮ 验证角标 / ⑭-A 报告篮子节三处本来就有露出),⛔ 也**不留零调用点的空 kind**(设置屏给个拨了没用的开关 = 撒谎式 UI);**日后若真要开,正确路径不是「照例举接回去」,必须用户拍板 + 同步改 ⑦-b/⑧-C2 红线与 CLAUDE.md 坑条三处,只改一处就是绕过红线**。另在 ⑪-B 补「**两套通道刻意不混**」(`channels` 的看板事件 ⛔ 不许顺手接进 kind 白名单,承 V1 §2.4「买点/证伪/普通警示只进看板」),并注明**权威 kind 清单 = `notify_kinds.py` 唯一源,例举只是举例**。**② `stop_approach`/`take_profit`/`sector_dive` 三条立即级 kind —— 本次不动**,已由协调方单独提请用户拍板,回复后再落 Plan。**③ 三处欠账归位**:(a) **⑮ 补客户端推送欠账**(`PushSettings` 六 bool → `kinds[]` 且设置屏**动态渲染 + 按 level 分组**、⛔ 不许硬编 kind 清单〔权威在服务端,硬编必漂移,新增 kind 应做到客户端不改代码就能显示〕;`PushCategory` 六 → 三;**路由改按 payload `kind` 分发不按 category**〔按 category 分支会连坐 = V1 拆 `HOLDINGALERT` 的教训〕;**未知 `kind` 必须优雅降级**照常显示、⛔ 不许静默丢弃〔v1.5「Swift 未知 `status` 静默消失」同类坑〕;NL 提醒确认卡**七项必须齐**含行情延迟披露);(b) **⑭-B 标注 `/alerts` 五端点与 `PUT /settings/push` 已由 ⑪ 落地,⛔ ⑭ 不要重建**、只纳入三方对拍;(c) **⑪-A 补注**②实为板块基准指数不是 ETF(承 P4-35)、④ 大盘突变依赖宽基恰在池里否则如实标 `no_broad_index_quote` 不判,且**⛔ 不许为此改关注池组成** —— 无条件塞宽基会挤占 `breadth_cap` = 改变退潮哨兵宽度样本 = 碰纪律判定输入,⑪ builder 没擅自动是对的。**④ 顺手给 ⑪ 的「块外发现」补账本 → 新挂 §七 [P1-36]**:④ 两条「三路等价」守门单测全量下偶发红、单独跑恒绿(⑪ 期间两次复现,与 ⑪ 零关联)。**定为 P1 而非测试卫生**,因为两种成因性质完全不同且必须先分清:(a) 夹具跨用例污染 = 良性;(b) ④ 的 bulk 与 day_by_day 两路**真有不确定性**(polars 分组顺序 / rank tie-break 没排干净)—— **那就撞在项目明令禁止的那条线上**(CLAUDE.md「任何进判据/排序的 rank 必须先排定确定性 tie-break」,2026-07-29 行业强度真数据打出来的),意味着同一天算两次可能不一样 → 历史回放不可复现、⑨ 按包归因失去可比性。**这条守门单测存在的全部意义就是拦 (b),它偶发红本身就是在报警**;已给出四步判据(固定 hashseed 复跑 / 失败率对比 / 失败时逐行 diff 看差在行序还是数值 / 查 bulk 路径有没有 sort 收口),并写死**⛔ 分清之前不许放宽断言或加 flaky 重试(等于把报警器拆了)**;排期建议与 ⑧-E 同批(⑬/⑭ 之前)
- 2026-08-03 · 🔔 **V2-⑪ 监控 80/15/5 + 通知三级 + 自然语言临时提醒完工**(🔴 @builder-pro,纯本地代码 + 单测,零服务器 / 零部署;**哨兵四哨兵 + 熔断 + 盘前校准的判定分支一行未动**,新监测与 NL 提醒全部挂在独立 try 的旁路上)。**⑪-A**:新建 `sentinel/attention.py` —— 四监测(同篮成员集体转弱 / 板块基准指数承接消失〔**如实标是指数不是 ETF**〕/ 持仓转独立弱势 / 大盘突变)全部纯规则、只吃 D0 冻结件 + 本拍已有报价、**零额外网络**,13 个阈值登记在 `ATTENTION_DEFAULTS`(⑦-b 体例:工程默认零回测背书、不进包不进章程);同篮合并敞口按 `basket_id` 归并(⑩ 的 `entry_snapshots.basket_id` 优先、退 `position_plans`),≥2 个不同标的才标主题集中并挂进通知正文。**⑪-B**:新建 `notify_kinds.py` 作三级 × 11 kind 唯一源(D5 三个 category `NKIMMEDIATE`/`NKIMPORTANT`/`NKDIGEST`,**开关按 kind 配不按 category 配**),`api/notify.py` 扇出收敛成单条 `push_event()`(未登记 kind 直接抛 + AST 守门断言 `_fanout` 只被它调用),`push/apns.py` 六个 V1 category 常量删除换三个,开关落点从六个 bool 列换成 `app_settings.push_kinds` JSON 并由 `_seed_push_kinds` **幂等播种老库取值**(⛔ 不许把用户关过的推送悄悄打开;六列停写留档不 DROP)。**⑪-C**:新建 `custom_alerts.py`(7 个 metric 白名单 + 三值判定所需的规范化 + **七项确认卡**〔行情延迟披露与「只通知不自动交易」是恒出现的必选项〕+ 台账)、`sentinel/custom.py`(确定性执行器,`nl_text` 一个字不进判据、**零下单** AST+逐行双守门)、`llm/nl_alert.py`(带日期锚、`json_block` 剥 JSON、白名单拒收不修正、**LLM 不可用 → 手填表单不静默失败**);新增 `GET|POST|PUT|DELETE /alerts` + `POST /alerts/parse`(**⑭-B 清单里的同名五个端点已在此落地,⑭ 只需对拍**),`PUT|GET /settings/push` 契约换血为 kind 清单。测试:新增 7 个文件 **164 例** + 改造 3 个既有文件另净增 12 例(合计 +176)(`test_notify.py` 六类断言按 ⑪-B 要求换成三级 × kind 并重新锁死),`python -m pytest tests/ -q` 基线 2836+2skip+1fail(周日 ISO 炸弹,今日已自愈)→ **3012 passed + 2 skipped + 0 failed**,零回归;新增红线单测:三条旁路任一抛异常,持仓哨兵照常判照常推。冒烟 `scripts/smoke_attention_alerts.py`(一次性临时库、真库零读写、零真实 LLM/APNs)**全绿**:四监测各至少一条触发路径 + NL 提醒解析→确认卡→去重→阴性对照→命中→不重复轰炸→收盘失效→降级手填全链。**9 处如实登记、其中 🔴 2 处提请裁定**(`basket_falsified` kind 未登记 —— ⑪-B 例举与 ⑦-b/⑧-C2「不进推送」红线正面冲突,取保守方向;`stop_approach`/`take_profit`/`sector_dive` 未开 APNs kind —— 蓝图 5.5「立即」点名了止损但 ⑪-B kind 例举没有,按「新增 kind 须用户拍板」不擅自加)详见 §五 V2-⑪ 段尾「完工记录」。🟡 块外挂账:`test_scan_cluster/corr` 两个「三路等价」测试在全量套件下偶发失败、单独跑恒绿(开工基线首跑即复现,与本块零关联,属 ④)。下一步:V2-⑫(对账与画像)。
- 2026-08-03 · 🧾 **V2-⑩ 持仓极简台账 + 计划继承 + 决策日志强制表单退役完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与并行的 ④/⑨ 两条施工线文件面不相交)。新建 `neckline/positions_entry.py`(买卖三字段唯一编排入口,API/CLI 共用):entry_snapshots 冻结 + position_plans 版本继承(新版本不改原卡,单测锁死)+ 偏离提示 + user_actions 自动记账;`decision_log` 写函数物理删除,`POST /decisions` 换血为「用户可选补充」入口(落 user_actions,不再是决策日志表单);`close_reason` 枚举加四码。`python -m pytest tests/ -q`(含并行 ④/⑨ 已完工内容)最终 **2836 passed + 2 skip + 1 failed**(唯一失败是 `test_review_reconcile.py` 既有真实时钟"当周"判据,施工窗口恰跨 2026-08-02→08-03 ISO 周边界触发,用 `git worktree` 拉纯净 HEAD 复现同一失败、证实与本块无关、不修不新增)。两处如实登记(`POST /decisions` 保留路径整体换血、七枚标签码与四枚 close_reason 码系本次新拟)详见 §五 V2-⑩ 段尾「完工记录」。下一步:V2-⑪(🔴 @builder-pro,碰推送与哨兵)。
- 2026-08-02 晚间 · 🩹 **V2-④ 定向快修:种子输出顺序确定性**(@builder,纯本地代码 + 单测,零服务器 / 零部署;只改 `neckline/scan/seeds.py` + `tests/test_scan_seeds.py` + 本节 / ④ 块补记)。根因:涨停簇 / 异动簇种子走 `frame.group_by(["cluster_key"])` 迭代直接 append,polars 不保证顺序 + 上游 SQL 未加 `ORDER BY`,⑤ 只取前 20 颗 → 同一 D0 同一库重跑产出不同篮子(⑨ 完工时实证发现的块外真洞)。修法:四类种子(不只两类被点名的)输出前一律经新增 `_sort_by_seed_key()` 按 `seed_key`(crc32 稳定业务键)升序排定。新增回归测试锁死"连跑三次逐位相同 + 顺序即 `seed_key` 升序"(临时移除排序反测确认测试真能识别回归);`scripts/smoke_basket_review.py` 跨两个独立进程双跑同一 D0,113 行输出仅 2 行预期内差异(临时目录随机后缀 + 一处审计时间戳),篮子/卡/验证/复盘全部逐字节一致。改动范围 `pytest` 686 过零失败;全量套件另受并行施工的 V2-⑩(positions/api/decision_log)与一处跨零点翻车的既有 `today()` 测试双重扰动,均核实与本次改动无耦合、未修,详见 §五 ④ 块补记。
- 2026-08-02 · ⚖️ **V2-⑨ 盘后复盘引擎 + 评价引擎完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读、V2 表仍 0 行,冒烟跑在 `sqlite3.backup` 副本 + 真实 parquet 只读上)。**⑨-D 判分唯一源下沉**:`ReTrade`/`_sim_one`/`SLIP`/`BROKER` 自 `research/h9_exit_reform.py` **逐字搬入** `neckline/eval/exit_sim.py`,h9 改再导出、`drill.py`/`exam.py` 改直接 import(老写法 `h9.X` 一字不改仍工作);**对拍两层全绿** —— 单测内嵌搬迁前冻结源做**源码逐字**比对 + `exec` 出独立可调用体(独立 `Broker()`)在 400 条随机造数 + 三条窄分支上**行为逐字段**全等,真 K 线 `scripts/smoke_eval_exit_sim.py` 900 笔真单 × 3 组退出参数**不一致 0 笔**。新增 `score_kw_from_charter()`(章程→判分参数唯一翻译,v1.3.3 翻出来恰好等于研究侧冻结的 `SCORE_KW`)+ `fill_and_score()`(考官线 §九 竞价成交层 + 卡上 `max_chase` 上限,`score_kw` 设为**必填关键字**从调用面堵死默认值)。**⑨-A** 新建 `neckline/review/basket_review.py` 机械判九项(龙头从 D0 冻结卡认、可买性口径对齐 `fwd_buyable` 且一字单列、涨停价取卡上冻结值、成员收益走 `daily.pct_chg` 绕开除权锚坑、缺存拍 MFE/MAE 走 EOD 近似且 `mfe_at=None` 不编时刻)+ `basket_review_store.py`(**只有 INSERT OR IGNORE,无 UPDATE/DELETE 路径**,守门单测锁死)。**⑨-B** `TASK_REVIEW` + 复盘账 + 不联网,降级次序直读 `budget.DEGRADE_ORDER`(T3 简评 → T2 细节,T1 与 `NEVER_DROPPED` 不在其中),LLM 缺席机械判照出。**⑨-C/C2** 新建 `neckline/eval/{metrics,placebo,calibration}.py`:七组指标全部按 `pack_version × verification_ruleset_version` **分层**(preseed 自成一层、缺失显式占位不并层)、`not_evaluated` 不进验证率分母、买不进与前向未走完的**不进收益均值**、已选 vs 未选无数据时空值如实;两条对照臂 `crc32` 派生种子**同日两跑逐位相同**、抽 N 次取分布、小样本只报样本数不报结论(文案单测锁死);周度校准报告落 md+json,**不接报告管线**。测试 2749 → **2842 过 + 2 skip + 2 fail**(既有周日炸弹),净增 93 例。端到端冒烟 `scripts/smoke_basket_review.py`(真实 D0=2026-06-18,④→⑤→⑥→⑦→⑧→⑨→周报全链,零真实 LLM):复盘 4 篮、重跑幂等零差异,对照臂真实 −2.57% vs 随机中位 −1.95%(第 38 百分位)vs 满仓持有 −1.63%,**人工逐项核对一篮九项全部对得上**。**六条如实登记 + 一处提请裁定**详见 §五 ⑨ 段尾;其中 🔴 **块外真洞**:`scan/seeds.py` 的涨停簇/异动簇种子走 polars `group_by` 默认无序迭代,而 ⑤ 只取前 20 颗 → **同一 D0 同一库重跑会产出完全不同的篮子**(冒烟三连跑实证),违反「跨进程可复现」与「冻结件同日重跑幂等」前提,**⑨ 不越块自行修**,请排 ④ 的修正。
- 2026-08-02 · 🩹 **V2-⑧-E 除权除息锚失效检测器完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与并行施工的 ⑨ 块文件面不相交,只改 `neckline/sentinel/basket_verify.py` + 顺手修一处被本子项唤醒的 `scripts/smoke_basket_verify.py` 沉睡真洞)。检测器 `pre_close ≠ 卡里冻结的 ref_close`(带 `vr.EPS`)判锚失效,命中即复用既有「两侧都不计」机制排除该成员,盘中 `Quote.pre_close` 与 EOD `daily.pre_close` 走同一个 `evaluate_specs()`;EOD 另用 `adj_factor(D+1) vs adj_factor(D0)` 交叉确认拆出 `member_ex_rights`(不报警)/`anchor_mismatch`(WARNING)/`anchor_unconfirmed`(数据缺失,自补第三态);⛔ 不做自动 rescale。真实历史除权样本冒烟(`603409.SH` 2026-06-17→06-18,`adj_factor` 1.0146→1.5265)验证检测器打响且交叉确认判对。新增 `tests/test_basket_verify_ex_rights.py` 10 例,`pytest tests/ -q` 2754→**2766 passed + 2 failed(既有周日炸弹)+ 2 skipped**,零回归;⑦ 卡形状与 `spec_version` 一字未动(守门断言)。如实登记两条(详见 §五 ⑧-E 段尾「完工记录」):① 施工期发现「无条件给每个成员加 `pre_close` 字段」会打破既有「盘中/EOD 逐位相同」单测,已改为仅在锚失效分支携带该字段;② plan 验收原文"2 只成员篮"字面场景与 `min_hit=ceil(2/2)=1` 数学上不共存,已按最贴近字面意图重新构造(⑦-b「中间地带」而非净空数据)并附反证测试。下一步:⑬/⑭ 之前的排期已履行,V2-⑨(并行中)/ ⑬ / ⑭ 按既定顺序推进。
- 2026-08-02 · 🃏 **V2-⑦ ④篮子卡冻结完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;与 ⑥-b 并行施工,`tier.py`/`pack.py`/`packs/` 全程未动,两处共用测试文件按 hunk 挑拣提交):新建 `neckline/selection/basket_card.py` —— **卡内容 = 蓝图 4.6 全十一项**(①②③④来自 ⑤ 的篮子/证据/why_now/成员角色,⑤来自 ⑥ 的定档与五维 breakdown〔**duck-typed 传入,⑦ 不 import ⑥**,守门单测锁死〕,⑥⑧⑨⑩ 的人话半份由 ⑦ 的 LLM 出,⑦ 的数字过夹逼闸,⑪ `BASKET_CARD_DISCLAIMER` 单一源)+ **夹逼**(建仓观察区间与最高追价各自独立落 `ok`/`absent`/`rejected_out_of_limit`/`rejected_malformed`/`rejected_no_limit`,锚 = `compute_intraday_limit_prices(D0 close, board, is_st, next_trading_day(D0))`、`board`/`is_st` 走 `load_stock_meta`;**离场参考只校验格式不夹涨跌停**〔它是 ⑩ `position_plans.plan_json` 要继承的那一项〕;另加自洽校验「追价 < 区间上沿 = 自相矛盾」)+ **止损价系统算**(`round(close×(1−stop_pct),2)`,`stop_pct` 只从 `brain.active_config()` 来,纪律标签 `discipline_labels()` 动态生成、缺指纹退化成不带数字的说法,AST 守门断言代码里无 `0.05`/`0.08` 与「−5%」类模板)+ **结构化验证/失效 spec**(`verification_spec`/`invalidation_spec`,**⑧ 的唯一判据源**;只用价量结构;某成员某条阈值 `null` = 该条对该成员不判,**不当"不满足"**;先算 spec 再喂 LLM,单测锁死阈值确实出现在上下文里)+ **卡 LLM 段**(`TASK_SCRIPT`、**不联网**、扣 `LEDGER_REASON`、`json_block` 先剥再用、**不复用 `_parse_verdict`**;成员白名单闸丢弃篮外代码;缺席时**结构化半份照出**、人话半份如实标 `llm_stage`)。新建 `neckline/selection/member_tags.py`(**⑦-K7 标注件唯一实现**,判据逐字取自 `research/k7_pre2_report.md` §1 / `k7p_h12_pullback.py`,阈值全落在函数关键字默认值上故只需登记一个 `_EPS`;三态 `tags`/`missed`/`absent` 不合并;⑬-N 信息卡将读同一份)。`basket_store.py` 增**事务 2**:`save_basket_card`/`save_basket_cards`/`load_basket_card`/`next_card_version`,⑦ **只写 `basket_cards`**,`INSERT OR IGNORE` on `UNIQUE(basket_id, version)` 冻结不覆盖、内容不一致 WARNING + `frozen_conflicts` 如实带出,`version=1` 显式默认(自增留给 D+1 追加,免得同日重跑把冻结件写成流水账)。**真实交易日冒烟**(2026-07-24,隔离库 + 真 parquet 只读 + LLM 全桩):④206 颗种子 → ⑤2 篮 → ⑥全 T1 → 事务 1 落 `2/4/2` → ⑦2 张卡 → 事务 2 落 2、重跑 `cards_existing=2` 且 0 冲突;人工核对 601728.SH:涨停 6.78 / 跌停 5.54 / 止损线 5.85(= 6.16×0.95,读真实现役章程 `K1`),区间 [6.10,6.22] 与追价 6.34 判 `ok`,离场参考 [7.70,8.62] **远高于涨停仍放行**(证明不夹),同篮 600941.SH 越界区间被拦、篮外 `999999.SZ` 被白名单闸丢弃。测试:新增 `test_selection_member_tags.py`(46)+ `test_selection_basket_card.py`(54)= **100 例**,`pytest tests/ -q` → **2652 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹;数字含并行 ⑥-b 净增)。**六条如实登记 + 两点提请裁定**详见 §五 V2-⑦ 段尾「完工记录」,最要紧三条:① **`min_members_hit=ceil(n/2)` 与验证/失效的条件集都是 builder 拟的临时默认**(plan 只给了「双份 + ⑧ 唯一判据源」两条约束,没给聚合规则与条件清单),**⑧ 一开始消费就成既成事实,建议先拿裁定**;② **`warn_streak_top` 的「簇内」改读作「共振邻域」**——审计粗簇一票一簇,而 ④ 的多路聚类下一票同日属十几个簇,照字面「任一簇头名」当日 **36/42(86%)** 都要打警示 = 等于没警示,改判后三天真实数据命中 6/6/5;③ `selection/__init__.py` 的「本包全程不 import `strategy.brain`」一句如实修正为「**包机制本身**不 import」(⑤ 起就不成立,⑦ 读 `stop_pct` 是 plan 明文要求),并把 ⑥ 那条 `test_reads_no_discipline_thresholds` 按 tier / store 两种职责拆成两条。下一步:**V2-⑧**(碰哨兵,🔴 @builder-pro)
- 2026-08-02 · 👁️ **V2-⑦-b 条件集定版 + V2-⑧ D+1 验证完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读、V2 表仍 0 行,冒烟跑在库副本 + 临时 parquet 上)。**⑦-b**:条件集 / 聚合门槛 / 四态映射 / 比较语义集中到新模块 `neckline/selection/verification_rules.py`(选「独立小模块」而非 `basket_card.py` 顶部,**理由是防模块环** —— ⑧ 住 `sentinel/` 而 `basket_card.py` 反向 import `sentinel.universe`);失效第 ③ 条按裁定改判为复合条件 `close_below_ref_and_ma20`(`{ref_close, ma20}` 两阈值 AND,任一 null 整条不判),三个 `spec_version` 随形状 bump 至 `_v2`;新增 `verification_ruleset_version` 落 `card_json.fingerprint` **+ 两份 spec 自身**(⑧ 手里常只有 spec,没有它 `evidence_json` 标不出「哪套条件集出的判」);`MIN_MEMBERS_HIT_DIVISOR`/`EPS` 登记进 `_ENGINE_CONSTANT_WHITELIST`(前者措辞含「临时默认 / 零审计背书 / 要可配须先扩 §12.2 插槽边界」,单测断言);比较语义**存进卡**、⑧ 按卡里写的串选比较器(老卡永远按它自己那版被判)。**⑧**:新建 `sentinel/basket_verify.py`(盘中与 EOD **共用同一个** `evaluate_specs()`,拆的只是代入什么价)+ `sentinel/basket_verify_store.py`(`basket_verification` **只 INSERT**;盘中变了才落、EOD 必落、`falsified` 当日终态不撤回而 `verified` 可翻;「当前状态」三路读法唯一实现)+ `sentinel/capture.py`(内存累计 → 15:05 一次 `write_table_day`,`capture_status` 三态 + 原始数字一起落 `sentinel_events`);关注池改组为「持仓 > T1/T2 篮子成员 > 候选(V1 残留)> 板块基准指数 > 昨日涨停填充」、**自选池不再是来源**(⑬-11 复述的「⑧-A 已改」);`run_tick` 与 lifespan 循环各挂**独立 try/except 的旁路**,四哨兵 / 熔断 / 盘前校准判定逻辑一行未动。**顺手修一个会静默拉错标的的真洞**:`quotes.to_symbol` 改为 `.SH/.SZ/.BJ` 后缀优先(`000001.SH` 上证综指原本被拉成 `sz000001` 平安银行),对所有股票代码形状逐位等价(单测锁死)。测试 2652 → **2735 passed + 2 skip + 2 fail**(2 fail 仍是既有周日时间炸弹),净增 83、删 5 例随自选来源退役的用例。冒烟两份:`scripts/smoke_basket_verify.py`(真实 D0=07-23 锚 + 合成分钟)**盘中四态齐 + 定格不撤回 + 存拍读回 dtype 全合声明**,EOD 用真实 07-24(全市场 −2.62% 的真跌日)判出 4/5 篮 `falsified`、其中 `600396.SH` 最低价恰等于算出的跌停价、「触及跌停」条在真数据上打响;`scripts/smoke_sentinel.py` 回放确认四哨兵零回归。**七条如实登记**详见 §五 ⑧ 段尾,其中**两处提请裁定**:① **锚是前复权、观测是原始价**(盘中免费源只有原始价,两条路径彼此一致但成员恰在 D+1 除权时会误判破位;正解是 ⑦ 在卡里多冻一份原始价锚,属 ⑦ 形状变更、不在 ⑧ 授权内);② **EOD 那一拍谁调 Plan 未定死** —— 本块只给函数 + `scripts/basket_verify.py` CLI,**没有**擅自挂进 `daily_update.py`(⑭-A 的 16:35 链才是 plan 指定入口),故 ⑭ 落地前 EOD 定论行不会自动产生。另登记 `capture_status` 落 `sentinel_events`(不新开第 22 张表)与「相关板块 ETF/指数」本版只做到基准指数(无 ETF 映射数据源)。下一步:**V2-⑨**(盘后复盘 + 评价引擎;依赖 ⑦⑧ 已就位)
- 2026-08-02 · 🧭 **V2-⑥-b 追加子项完工**:档位质量线进包 + T3 下限 + 溢出摘要原因码拆分 + `neutral_filled_weight` 审计字段(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ⑦ 并行施工,文件面不相交,详见 §五 ⑥-b 段尾「完工记录」)。`pack.py` 新增 `_validate_quality_lines()` + `Pack.tier_quality_lines()`(`quality_lines` 三键各自独立可选,单调性只比较字面给出的键、不合并引擎默认——规避 `pack.py`↔`tier.py` 循环 import);`tier.py` 新增 `TIER3_MIN_SCORE=0.25`/`resolve_quality_lines()`(逐键回退,姿势与 `resolve_weights()` fail-loud 刻意不同)/`DROP_BELOW_QUALITY_LINE`(与 `DROP_CAPACITY_OVERFLOW` 分开留痕计数)/`neutral_filled_weight()`(只认 flags 不认数值,躲开 `leader_clarity` rank=2 撞车);`packs/K7-pack.json` 补 `quality_lines:{0.60,0.40,0.25}`,`K4-pack.json` 零改动;两包四道闸 dry-run 均 exit 0,`ENGINE_API_VERSION` 仍 1。测试净增 32(`test_selection_pack.py` +17、`test_selection_tier.py` +15)+ `_ENGINE_CONSTANT_WHITELIST` 改写说明,`pytest tests/ -q`:2516(基线)→**2651 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹,与本子项无关;⑥ 既有 86 例一字未改仍绿,余下净增来自并行 ⑦)。如实登记一处范围限定:「消费 `mech_breakdown_json` 须同时读 flags」暂未落成自动化仓库扫描(核实唯一准消费者 `basket_card.py` 是整段透传、不构成违反;⑨/⑭/⑮ 未落地前写扫描器要么打空要么误伤,改以文档 + 可执行示范兜底,留给三者落地时补真正的静态守门)。下一步:⑦ 并行施工中,两者收尾后转 V2-⑧。

- 2026-08-02 · 🧭 **V2-⑥ ③Tier 分层引擎完工**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;真实 `data/neckline.db` 全程只读、全部 V2 表仍 0 行、现役章程仍 `K1` 未动):新建 `neckline/selection/tier.py`(**五维机械分**——`sector_strength`〔`industry_strength_daily` 只读表 + 概念热榜,两侧取较大者、概念只加成不减分〕/ `driver_freshness`〔④b `industry_stage_daily.stage` 经**现役包** `tier.stage_scores` 映射〕/ `leader_clarity`〔`rs_rank` 倒数衰减 `1/min(rank)`,⛔ 连板高度不进〕/ `tradability`〔一字扣 1.0、涨停开过板扣 0.5〕/ `card_density`〔⑤-b 的 `k4_tag` 命中密度〕;**任一维算不出一律中性分 0.5 + `flags` 如实标,⛔ 永不写 0** —— 0 是 `overheat` 的真实取值)+ **运行期访问锁**(特征行里刻意同装六个 LLM 产出字段,`mech_score()` 只许读 `_TIER_SCORE_INPUTS` 五键,单测断言 `accessed == 白名单` 且与 LLM 键 `isdisjoint`,并配一条"行里真有 LLM 字段可读"的可证伪性断言)+ **权重只住包里**(`resolve_weights()` 缺维度/多出未知维度**双向 fail loud**,归一化保证机械分恒在 [0,1])+ **定档**(`(机械分降序, basket_key 升序)` 确定性排定 → 每档一道质量线 → **上限非配额**,够格但满了向下顺延、三档全满则今日不定档并留痕)+ **LLM 同档微调**(`TASK_TIER_RANK`、不联网、扣 `LEDGER_REASON`;日期锚 + `TIMELINESS_RULES`;产出走 `json_block` 先剥 JSON 再解析、**不复用 `_parse_verdict`**;**跨档提案整条丢弃 + WARNING**,另五类拒收码 `unknown_basket`/`bad_rank`/`slot_taken`/`duplicate_key`/`malformed` 语义不合并;`rank_mech` 与 `rank_in_tier` 两者都落库 + `llm_rank_delta` + `llm_reason`;缺 provider / 调用失败 / 预算尽 / 解析失败 → **机械序原样用**并如实标 `tier_rank_unadjusted:{stage}`;每档只 1 个篮子时根本不发起调用)。新建 `neckline/selection/basket_store.py`(篮子四表唯一写入口:`save_baskets` 从 `aggregate.py` **原地搬来**、新增可选 `conn=`、`aggregate.py` 同名再导出**同一函数对象**,⑤ 既有 63 例一字未改仍绿;新增 `save_tier_history` 与**事务 1** 入口 `save_tier_decision` —— 三表同一 `with connection()` 内落地,单测 monkeypatch 让 tier_history 抛错验证**三张表一起回滚、库里零行**;`INSERT OR IGNORE` 冻结不覆盖,重跑不一致 → `frozen_conflicts` 逐条带出 + WARNING「本次重算结果未采纳」,不静默)。**本地冒烟**(真实交易日 2026-07-24 + 真 parquet + LLM 全桩 + 真库一次性副本):206 颗种子(截断 20)→ ⑤ 出 15 篮 → **⑥ 定档 T1=2(满)/ T2=5(满)/ T3=8、溢出 0**,机械分 0.810~0.280;跨档桩提案真被拒、档内调序真生效(机械 #5 → 最终 #1,Δ=4,理由落库),事务 1 落 `15/22/15`,重跑全 existing、0 冲突。测试:新增 `tests/test_selection_tier.py` **86 例** + `_ENGINE_CONSTANT_WHITELIST` 补登记 tier.py 8 个引擎常量,`pytest tests/ -q` 2427→**2516 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹)。**十条如实登记**详见 V2-⑥ 段尾「完工记录」,其中**两处提请 planner 裁定**:① **档位质量线 0.60/0.40 是 builder 的临时工程默认**(Plan 没给数;纯排名制下「允许 T1 为空 + 不许凑数」的单测根本写不出来,故必须有绝对分数线;它带策略语义本该进包,但两份包都无此键、Plan 也未授权 ⑥ 扩 ③ 的包 schema);② **容量溢出的篮子今日不落库**(`baskets.tier` NOT NULL,臆造 tier 更错;留痕在 `TierResult.dropped`)。下一步:**V2-⑦**(篮子卡冻结 + ⑦-K7 成员标注件;`save_basket_card()` 直接加进已建好的 `basket_store.py`、**单独一个事务**)
- 2026-08-02 · 🏗️ **V2-⑤-b 成员卫生线闸 + V2-⑤-c lift 最小成分数门槛完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ③-K7/④b 两条并发线文件面不相交):新建 `neckline/selection/member_hygiene.py`——过滤链路挂在 `aggregate_baskets()` 内 `build_mech_context()` 之后、`presented_by_seed`(`_shortlist` 截断)之前,当日对候选成员集调用 `apply_member_hygiene()` **恰好一次**;**两级保险丝**:tier-1(`stock_hygiene` 的 `is_st`/`board` + 独立停牌/次新检查,数据源 `sentinel.universe.load_stock_meta`+`stock_basic`+`suspend_d`)算不出 **fail closed**(拒收);tier-2(`close`/`ma20`/`amount_ma20`,数据源 `build_research_panel` 单日切片;K4 分区归属复用 `report/holding_k4_check.py` 既有机械镜像 + 本模块自写 bulk 谓词下推 loader,不 import 即将随候选榜退役的 `intel_candidates.py`)算不出 **fail open**(`hygiene_unavailable`/`k4_unavailable` 分开披露)。`k4_advisory_gate` 两档语义照包:`hard_cut→exclude` 剔出,`avoid_flag→tag` 保留但标(`k4_tag_of`,新字段 `BasketMemberCandidate.k4_tag` 随成员流转供未来 ⑥/⑦ 消费)。`aggregate.py::assign_primary()` 改写为 **lift 三态**:达标篮(成分池 ≥ `MIN_LIFT_SAMPLE_SIZE`)之间正常比 `primary_reason='highest_lift'`;不达标篮 `lift=None`+`lift_reason='sample_too_small'`、完全退出比较;一票全部候选篮都不达标 → 确定性兜底 `(成分池大小降序, basket_key 升序)`、`primary_reason='fallback_no_qualified_lift'`。`MIN_LIFT_SAMPLE_SIZE` **直接 import 引用** `industry_strength._MIN_MEMBERS`(不是抄字面量,故意不登记进 `_ENGINE_CONSTANT_WHITELIST`——登记一个白名单扫描器结构上命中不到的 `Name` 引用反而会打破反向存在性校验)。测试:新增 `tests/test_selection_member_hygiene.py` **23 例** + `test_selection_aggregate.py` **7 例**(⑤-b 编排接线 3 + ⑤-c lift 三态 4);`test_selection_aggregate.py::_run()` 补测试脚手架(自动注册现役包 + 补齐 `stock_basic.list_date`,`INSERT OR IGNORE`/`WHERE...IS NULL` 语义不覆盖既有值——⑤-b 让卫生线成为强制前置,既有 55 个测试函数里只 3 个显式注册过 `stock_basic`);`_two_basket_env` 是既有 56 例里唯一动了测试**数据**(非断言)的一处(种子成分 3→5 只以满足新门槛,原本示范的"小样本失真"改由新测试专门覆盖)。`pytest tests/ -q`:**2427 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹,树内同时含 ③-K7/④b 已完工的净增,三方文件面互不相交零冲突)。**五条如实登记**(其中最要紧:①"面板行存在但字段 null"与"整行缺失"改判同等降级,原因是 `non_new_stock` 120 自然日门槛远严于 ma20 所需 20 交易日,能过前者却后者仍 null 现实代表数据缺口非"真太新";②K4 评估失败的降级哲学是 builder 类推 plan 未点名)详见 V2-⑤-b 段尾「完工记录」。下一步:**V2-⑥**(依赖的 ③/③-K7/④b/⑤ 现已全部就位,解锁可开)
- 2026-08-02 · 🏗️ **V2-③-K7 原语与白名单跟进 + `packs/K7-pack.json` 完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ⑤-b/⑤-c 另一 builder 并行施工,文件面不相交):`neckline/selection/primitives.py` 白名单增第 10 个模式 `industry_stage_daily.*`;`intel_rank_priority` 新增 `_RANK_DIM_DIRECTIONS` 方向声明表(既有三维 `asc` 不变,新增 `industry_stage_score` 为 `desc`、`leader_rs_rank` 为 `asc`),未登记维度名 fail loud。`neckline/selection/pack.py` 新增 `config.tier.stage_scores` 可选键校验(键 ⊆ `neckline.scan.stage.STAGE_ORDER` 六码,值须数值)+ `Pack.tier_stage_scores()` 访问器。**`ENGINE_API_VERSION` 判定:保持 1(纯增量,K4-pack-v1 回滚锚逐位不变,单测锁死)**。新增 `packs/K7-pack.json`(`K7-pack-v1`,9 原语全收录、8 个与 K4-pack 逐字节相同、`intel_rank_priority.dims` 换四维排序键、`tier.weights` 证据化初值、`stage_scores` 六态英文码全覆盖;四道闸 dry-run 全绿,**只落盘不激活**,`selection_packs` 真实库仍 0 行)。**补 ③ 完工记录挂账的"换包→跟着变"测试局限的真实版本**:两处新测试改用仓库里真实 K4/K7 两份包文件驱动 `intel_rank_priority`(排序结果真的翻转)与真实 `generate_seeds()`(种子集合逐位不变,证明 K7 变更不含 scan 种子阈值)。测试:4 文件净增 24 例,`pytest tests/ -q` 2371→**2395 passed + 2 skip + 2 fail**(2 fail 仍是同名同因既有周日时间炸弹,零回归)。三处如实登记(K7-pack 收录全部 9 原语非草案 5 个/未加 schema 级 dims 枚举校验、留运行时 fail loud/新增 `tier_stage_scores()` 访问器非 Plan 逐字要求)详见 V2-③-K7 块内「完工记录」。下一步:V2-⑥(依赖 ③ ③-K7 ④b ⑤,现除 ⑤-b/⑤-c 外均已完工)
- 2026-08-02 · 🏗️ **V2-④b 行业题材阶段六态状态机完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署;与 ⑤ 并行施工,文件面不相交):第 21 张表 `industry_stage_daily` 落 `neckline/db.py::_SCHEMA`(附加在 ①/④ 已完工段落之后,不回改);新模块 `neckline/scan/stage.py`(`decide_stage()` 六态判据唯一实现——启动/发酵/过热/分歧回调/退潮/无题材,互斥优先级从上往下,三类"缺数不猜"安全落 `none` 且 `stage_reason` 逐条区分;`persist_days` 直接读 `industry_strength_daily` 单一源不重算,近 2/5 交易日强度旗标读本表自己历史不重查源表)+ `scripts/industry_stage.py {refresh,verify,bootstrap}` + `daily_update.py` 挂 `update_industry_stage`(排 `update_industry_strength` 之后)。**两遍法未采纳**(如实登记,理由同 ④ 自己 #5 先例:本表无跨日无界递推状态)。**本地真实数据冒烟**:`industry_strength_daily` 暖启动 134 个交易日(2026-01-02~07-24,14740 行,0.69s)→ `industry_stage_daily` 同区间批算(14740 行,**1.75s**),`verify` 全区间全绿,尾窗六态分布人工核对合理。**与研究实现对拍**(`scripts/oneoff/compare_industry_stage_with_research.py`,研究面板覆盖至 07-17):排除冒烟库暖启动前 2.5 周后(2026-01-20~07-17,118 天,12390 样本)**一致率 97.71%**,`is_strength_day`/ignition/fermentation/overheat 判定**两侧零冲突**,残余 284 处分歧**全部**落在"双方都判非强度日、仅 divergence/ebb/none 三态细分不同"一类,抽样确认根因是研究脚本 `lu_cnt` 施加 `base_expr()` 域过滤、生产按 ④b-A 定义直接读 `limit_derived.is_limit_up` 不做域过滤——按 ④b-D 裁决,**生产实现判定正确**,已如实记账。测试新增 `test_scan_stage.py` 38 例,`pytest tests/ -q`:**2371 passed + 2 skip + 2 fail**(2 fail 是既有周日时间炸弹,与本块无关;当前树含 ⑤ 并发 builder 未提交在制品,已计入本次全量数;本块自身净增 38、零回归)。真实 `data/neckline.db` 全程只读、MD5/mtime 不变;`industry_strength_daily` 零改动(全仓 grep 确认零写入)。三处如实登记(grep 守门改行为验证——本表无类比 `compute_industry_strength` 的可命名封禁函数;缺行保险丝两路的"⑥ 不拦/排序键退化"消费逻辑留给 ⑥ 落地时验收,本块只交付 `load_industry_stage`/`industry_stage_status` 两个原语;读侧口径指纹校验为主动补充的一致性增强非越权)详见 V2-④b 块内「完工记录」。下一步:V2-⑥(需 ④b 与 ③-K7 就位,现均已完工)
- 2026-08-02 · 🏗️ **V2-⑤ ②驱动聚合层完工(V2 心脏)**(@builder-pro,纯本地代码 + 单测,零服务器 / 零部署;与 ④b 并行施工,文件面不相交):新建 `neckline/selection/aggregate.py` —— **两段式 LLM 编排**(`run_driver_search` 走 `TASK_DRIVER_SEARCH` + 联网 + 显式带年份检索词、扣 `LEDGER_SEARCH`;`run_basket_reason` 走 `TASK_BASKET_REASON` + 不联网、扣 `LEDGER_REASON`;两段 user 消息首行恒 `prompt_context.date_anchor_line`,两段 system prompt 各嵌 `TIMELINESS_RULES`)+ **两道机械闸**(成员白名单闸 → 凭空造票**整条拒收** + WARNING,配套「声明了不存在的种子」同样整条拒收;角色对拍闸 → 与 `leader_structure_daily.role_mech` 冲突时 `role_conflict=1` 且 `role_llm`/`role_mech`/`rs_rank` **三者并存**,机械侧 `unknown` 判「无判定」不判冲突)+ **单侧降级三态**(检索缺席 → `evidence_status='search_unavailable'` 篮子照出、推理缺席 → 不成篮、部分搜成 → `partial`)+ **主归属**(行业闸 lift 最高者 `is_primary=1`,tie-break lift 降序→`basket_key` 升序)+ **落库口** `save_baskets(..., tier_by_basket_key=…)`(`INSERT OR IGNORE` 幂等不覆盖;**tier 必填、缺就 fail loud**)。**顺带最小重构**:围栏 JSON 解析器从 `report/reference_plan.py` 搬到新模块 `neckline/llm/json_block.py`(行为逐字节不变、原处同名再导出,既有 81 例全绿)—— 通用件不该跟着 ⑬-3 停用的模块陪葬,本块与 ⑦⑨ 都要用同一份。**真实数据冒烟**(真库副本 + 真 parquet + LLM 全桩,2026-07-24):检索词实测 `信创 2026 最新` / `光伏建筑一体化 2026 最新`,成两篮带真实中文名/行业/成交额/机械角色/簇内 RS 名次/相关性 0.73,白名单闸真拦下故意混入的 `999999.SZ`,对拍闸真标出一处冲突;**冒烟打出并修掉两处合成测试看不见的坑**(涨停簇种子只有概念锚时 `label` 是裸指数代码 `886086.TI`,当检索词等于什么都没查 → 统一过 `load_index_names` 名表;`daily.amount` 单位是千元,换亿元该除 `1e5` 不是 `1e8`,原写法把 7.27 亿写成 0.01 亿)。测试:新增 `tests/test_selection_aggregate.py` **56 例**(含三条静态守门:不 import 哨兵 / 不复用 `judge._parse_verdict` / 源码零纪律参数痕迹;一条 lift 与 v1.3.1 行业闸的**交叉断言**),`pytest tests/ -q` 2274→**2333 过 + 2 skip + 2 fail**(2 fail 为既有周日时间炸弹;⑤ 自身 +57,余 2 来自并行的 ④b)。真实 `data/neckline.db` 全部 V2 表仍 **0 行**、现役章程仍 `K1` 未动。**十条如实登记**详见 V2-⑤ 块内「完工记录」,其中最要紧的是 **①「`baskets` 写入块登记在 ⑦ 但 `tier` 由 ⑥ 定档」的 Plan 自相矛盾**(连带 `tier_history.basket_id` 与 `baskets.id` 的循环依赖,⑥/⑦ 施工前需 planner 裁定落库次序)与 **②「成员卫生线该落在哪一块」无人认领**(现状:篮子成员可能含 ST/次新/流动性差的票)。下一步:V2-⑥(需 ④b 与 ③-K7 就位)
- 2026-08-02 · 🏗️ **V2-④ 市场扫描层完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):全新包 `neckline/scan/`(`cluster.py`→`limit_cluster_daily`〔同日/连板双 `cluster_kind`,`crc32` 稳定键〕、`corr.py`→`corr_matrix_daily`〔簇成员∪概念成分候选对,纯 polars 不引入 numpy,规模上限按真实数据实测从 60 调至 30〕、`leader.py`→`leader_structure_daily`〔RS/tie-break 已按施工期收到的 K7 交接稿需求 1a 对齐:RS20 严格口径+RS降序→成交额降序→ts_code升序 tie-break+连板高度成交额均不进 role_mech 主定义,等效完成施工图后来追加的 V2-④b-0 子项,该子项落地时可直接引用本记录不必重做〕、`seeds.py`〔四类驱动种子,阈值全部走新增 4 原语读现役包〕、`freshness.py`、`verify.py`)+ `scripts/scan_layer.py {refresh,verify,bootstrap}` + `daily_update.py` 挂 `update_scan_layer`;`neckline/selection/primitives.py` 新增 4 原语(`hot_industry_seed`/`surging_concept_seed`/`limit_cluster_seed`/`anomaly_cluster_seed`,白名单扩至 9 模式)+ `packs/K4-pack.json` 补种子参数节。真实历史数据(2026-07-22~24)冒烟:三张表 5.3s/3 天、`verify` 三项自检全绿、四类种子人工核对合理(含 2 颗真实暴起概念种子)。测试:新增 9 文件 95 例 + `test_selection_primitives.py` 扩容,`python -m pytest tests/ -q` 2179→**2274**(净增 95,零回归,2 pre-existing 失败同名同因未变);真实 `data/neckline.db` 全程只读、MD5/mtime 不变。六处如实登记(四类种子算法无先例文档系原创设计 / 不消费 `sector_moneyflow.py` / 异动簇不用 `top_list.py` / 种子成员不二次过滤留给⑤ / bootstrap 未做两遍法 / `dataFreshness.scanLayer*` 未接线进 `pipeline.py`),详见 V2-④ 块内「完工记录」。下一步:V2-⑤ / V2-③-K7 / V2-④b(后两者互不依赖、可并行)
- 2026-08-02 · 🏗️ **V2-③ 选股策略包机制完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):全新包 `neckline/selection/`(`engine_api.py` 单一源 `ENGINE_API_VERSION=1`;`primitives.py` 特征白名单 `_ALLOWED_FEATURES`〔8 模式〕+ `Primitive` 冻结 dataclass 两道白名单锁〔构造期 `__post_init__` fail loud + 运行期 `validate_all_primitives_whitelisted()`〕+ 首包 5 个原语 `stock_hygiene`/`non_new_stock`/`k4_advisory_gate`/`industry_dominance_gate`/`intel_rank_priority`,现值分别抽自 `research/panel.py::base_universe_expr`/`intel_candidates.py` 的 `NON_NEW_MIN_DAYS`/K4 安检语义/`INDUSTRY_GATE_MIN_LIFT`/`_sort_key` 三级键顺序;`pack.py` schema 校验 + `Pack` 冻结视图〔`seeds_config`/`tier_weights`/`tier_dims`〕+ `get_active_pack()`〔按 `(db_path,pack_version)` 双键缓存,防跨库串味〕+ `activate_pack()` 单事务写)+ `scripts/activate_pack.py`(四道闸,复刻 `activate_charter.py` 体例并按包性质简化)+ 首包 `packs/K4-pack.json`(`pack_version="K4-pack-v1"`,D7 定案;`tier.weights` 五数值逐字取自 plan 格式示例本身,V2 全新概念无 V1 代码可抽)。**手工演练发现并修复一处真实顺序漏洞**:CLI"已现役→快捷退出"若跑在 append-only 内容完整性核对之前,会放过"文件被篡改但版本号未改、且恰好现役"的情形,已提前核对顺序 + 两个回归测试锁死。测试:新增 4 文件 123 例,`python -m pytest tests/ -q` 2056→**2179**(净增 123,零回归,2 个 pre-existing 失败与 V2-② 同名同因、今日核实确为周日);真实 `data/neckline.db` 全量测试前后与手工 dry-run 前后 MD5/mtime 双证不变,全程未跑 `--confirm` 碰真实库。三处设计判断如实登记(身份/分类数据不计入 inputs 白名单 / 三级键归入 seeds 非 tier / tier 权重来源如上),「插槽真被消费」因 ④⑥ 未施工改在原语+包层做代理验证,详见 V2-③ 块内「完工记录」。下一步:V2-④
- 2026-08-02 · 🏗️ **V2-② LLM Provider 自填制 + 双 Agent 路由 + 预算多本账完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):`_PROVIDERS`/`ALLOWED_PROVIDERS` 两处枚举退役,`llm/factory.py::get_provider(task, *, db_path, settings_obj)` 改纯 DB 驱动(`llm_providers` 自填注册表 CRUD + `app_settings.llm_task_routes`/`llm_default_provider` 路由);新建 `llm/router.py`(九任务常量 + 默认路由纯函数)、`llm/budget.py`(预算三本账互不透支 + 降级次序 T3简评→T2细节、篮子卡冻结永不丢);`OpenAICompatProvider` 改可直接实例化(通用搜索钩子照 GLM 协议平移,`has_web_search=0` 一律不发 `tools`),`GLMProvider`/`KimiProvider` 完整保留行为不变、降级为预置参考实现供既有单测续用。契约:`PUT /settings/llm` 删,新增 `GET/POST /settings/providers`、`PUT/DELETE /settings/providers/{name}`、`GET/PUT /settings/llm-routes`,`GET /settings` 换形状(`providers`/`routes` 取代 `llmProvider`/`llmKeySet`)。新测试文件 `tests/test_llm_router_budget.py`(16 例)+ `test_llm.py::TestFactory`/`test_api_settings.py` 大幅重写。`pytest tests/ -q` 2023→2056(净增 33,零回归,2 pre-existing 失败经 `git stash` 复核与本块无关),真实 `data/neckline.db` 全量跑前后 MD5/mtime 不变。三点如实记账(详见 V2-② 块内「完工记录」,未改 Plan):`news_scan.py` 缺 `prompt_context` 既有缺口、"两段式编排"判断留给 ⑤、`TASK_INQUIRY` 归入检索类默认路由是 builder 推断待澄清。下一步:V2-③
- 2026-08-02 · 🏗️ **V2-① 表与共享信息层地基完工**(@builder,纯本地代码 + 单测,零服务器 / 零部署):20 张新表一次到位(18 SQLite 落 `neckline/db.py::_SCHEMA` + `app_settings` 两新列;2 parquet 表登记进 `market_data.py` 的 `_VALID_TABLES`/`TABLE_FLOAT_COLS`);新建 `neckline/user_actions.py`(append-only 唯一实现,靠"没有第三个函数"担保);新守门单测 `tests/test_v2_schema_guard.py`(17 用例,五条三律判据——冻结/追加/不回写/幂等/parquet 声明——全覆盖,含一处比 plan 原文更对称的补强:`DELETE FROM entry_snapshots` 冻结检查)。`pytest tests/ -q` 2005→2023(净增 18,零回归)。**副产品发现**(已 `spawn_task` 记账、未修复、不影响本块验收):`tests/test_holding_k4_check.py` 若干用例调用 `build_holding_k4_check` 未传 `db_path`,经既有代码路径命中真实 `data/neckline.db`(早于本次改动即存在的 pre-existing 缺陷,git stash 复现验证过;这次因新表新列首次留下可见痕迹,纯增量无数据损坏)——本块验收改用 `DB_PATH` 环境变量重定向复测,证明本块新代码本身零写入真实库。下一步:V2-②
- 2026-08-02 · 📐 **K7 系统线需求六项 + K7-pack 草案整合进 §五 施工图**(@planner,只改文档,零代码 / 零数据库 / 零部署;来源 = 用户转发的 `archive/交接_K7_系统线需求_20260802草案.md`,该稿即刻转参考件、不作第二权威;用户指示「施工时尽量全部做到」,含建议项)。**落位**:**需求 1a** `leader_clarity` = 簇内 RS20 头名度(依名次衰减,tie-break 定死 **RS 降序→成交额降序→ts_code 升序**;**连板高度与成交额头名均不进主定义**)→ **⑥** 正式定义 + 新块 **④b-0** 的 `rs_rank` 口径追加(**不改 ④ 已定型规格,④ 未完工则由 ④ 一并落、已完工则 ④b 补,两路都要单测锁三级键**);**需求 1b** `driver_freshness` = 行业五态状态机取代 `persist_days` 单调函数 → **⑥** 正式定义 + **新块 ④b**(**第 21 张表 `industry_stage_daily`**,DDL 定死、16:05 EOD 批算、在线只读、新 CLI `scripts/industry_stage.py`,P0-23 纪律与 ④ 同规格;**planner 定为新表而非给 `industry_strength_daily` 加列**,三条理由入档;**缺行保险丝取中性分 + 标 `stage_missing`,严禁写 0**——0 是「过热」的真值会撞车;**六态键一律英文码**〔`ignition`/`fermentation`/`overheat`/`divergence`/`ebb`/`none`〕,中文只做展示层,与草案 JSON 的中文键刻意不同,理由照 `CandidateOut.board` 先例;研究实现 `research/k7p_h11_stage.py` 只作对拍基准、不进生产);**需求 2** 五维权重证据化初值 0.30/0.30/0.10/0.20/0.10 → **⑥**(落 K7-pack;**K4-pack-v1 占位值原样不动、不重发版**,它是回滚锚;同时澄清「K4-pack 不动」的准确范围 = 只指 Tier 权重占位值,**不禁止 ④ 给它补自己的种子参数**);**需求 3** B3「题材持续 2-3 天」黄牌停用(A2 红牌不动、不新设绿牌)→ **新子项 ⑯-I**(🔴,一次性脚本 + payload + 默认演练 + 双备份 + 前后 diff 落 archive,**与 ⑯-E 包激活同窗**;写明**不违反红线** —— K4 行 `is_active=0` 永不激活,改的是建议载体不是纪律,现役章程恒 `v1.3.3`;镜像 `_HIT_META` 同步 + 守门单测断言 B3 零黄牌且 A2 仍命中)**+ 【门禁】新增一个用户判定点**(稿内建议叫 review,按项目规矩**由用户点名、不自动排程**);**需求 4** 原语与白名单跟进 → **新子项 ③-K7**(`_ALLOWED_FEATURES` 增 `industry_stage_daily.*`、确认 `leader_structure_daily.rs_rank` 可被 sort_key 引用、`intel_rank_priority` 支持新 dims **并显式声明每个 dim 的升/降序方向**、`config.tier.stage_scores` 落点与 `engine_api_version` **bump 判定规则定死**〔纯增量则维持 1,否则 bump 2 并 fail loud,**两路都必须在完工记录写明走的哪条**〕、`packs/K7-pack.json` 落盘但**不激活**);**需求 5** 龙回头位 + 双尾警示族 → **⑦-K7**(篮子卡成员节,判据全机械零 LLM,落 `card_json.members[].tags` 不新建表)**+ ⑬-N-K7**(信息卡,读同一份 `member_tags.py` 实现与文案,交叉断言两侧标签逐位相同);**四不硬约束**(不进排序 / 不进 Tier 机械分 / 不进哨兵 / 不改去留)+ 打标前后 Tier 序与成员集合逐位不变的守门单测;**需求 6** 安慰剂对照臂 → **⑨-C2**(从建议项**排为正式验收项**:随机同规模篮子臂〔`crc32` 派生种子可复现、抽 N 次取分布、判分复用 `exit_sim` 唯一源〕+ 满仓持有基准臂,样本不足只报样本数不报结论,**两臂只进周报不进任何在线判据**)。**planner 另定两件**:① **⑯-E 改为两步激活** —— 先 `K4-pack-v1`(**回滚锚**,库里必须真有旧行才谈得上回滚)再 `K7-pack-v1`(现役),验两行 / 现役唯一 / 事件流三条,并写明「⑯-E 之前是最后的改包窗口」(`--confirm` 后内容即冻);② **preseed 行 `pack_version` 填字面量 `'preseed'` 不许填 K7-pack-v1**(人工配的成绩不许算到包头上,防污染 §12.5 按包归因)。§五 表汇总 20→**21 张**、分块序列加 ③-K7 / ④b 并附「K7 需求落位一览」、V2 验收标准加三条
- 2026-08-02 · ✅ **V2.0.0 Plan 用户验收通过 + D1–D8 全部拍板 + 两项追加裁定入档**(@planner,只改文档,零代码改动):① **D1、D3–D8 照 planner 建议默认通过**,§五 清单表由「建议默认、可推翻」改为「**已拍板(2026-08-02 用户)**」;② **D2 = A 路定死** —— 新机挂新子域 **`nk.linotsai.top`**、**DNS 解析是用户动作**(用户原话:「我本来就要换云服务器,NK 这个域名我会直接把它解析到新的云服务器上」),老机 `ln.linotsai.top` 原样服务到 ⑰ 换包完成,**契约一次性换血不留过渡键**;**B 路(沿用 `ln`)全线删除**,⑭-D / ⑯-G / ⑰ / §七 P3-27 / §八 第 12 项五处的 B 路分支已收掉、只留删除线留痕(**不给 builder 留两条路犹豫**),同时写明「两步淘汰纪律本身仍有效,V2 只是靠换机窗口结构性绕开它」+ A 路成立的前提条件(**新机 nginx 绝不接管 `ln`**);③ **云上组件迁移置尾声** —— §五 新增「工序时序纪律」节 + ⑯ 块首改写:**①–⑮ 全部本地推进优先、⑯ 不与前面并行穿插、⑮ 完工前不碰任何服务器**,⑯ 内五项纯本地子项(三个 oneshot 单元 / nginx 模板 / `activate_pack.py`+首包 / `preseed_baskets.py` / 迁移清单)标 **📦 本地可先备料**(写好不上机跑,不拆走);§八 第 14 项由「走 A 路时才需要」改为**必办项**并加时机说明(按尾声节奏办、不必现在催)
- 2026-08-02 · 🏗️ **V2.0.0 立项**(@planner,只改文档,零代码 / 零数据库 / 零部署)· 架构设计稿全文转写进 PROJECT_PLAN,**架构稿即刻转历史档案**(`archive/V2架构设计稿_20260731_20260802立项归档.md`),防双权威。**§五 = V2.0.0 详版施工图 17 块**:① 表与共享信息层地基(**20 张新表**一次到位 + 冻结/追加/不回写三律守门)② LLM Provider 自填制 + 双 Agent 路由 + 预算三本账 ③ 选股策略包机制(`selection_packs` + 原语白名单 + `engine_api_version` + 四道闸 + 首包 K4-pack)④ 市场扫描层三张预计算表 + 驱动种子 ⑤ 驱动聚合层(LLM + 成员白名单闸 + 角色对拍闸)⑥ Tier 分层引擎 ⑦ 篮子卡冻结 ⑧ D+1 验证(关注池改造 + 盘中存拍 + 状态机)⑨ 盘后复盘 + 评价引擎 ⑩ 极简台账 + 计划继承 + 决策日志表单退役 ⑪ 监控 80/15/5 + 通知三级 + NL 提醒 ⑫ 对账与画像 ⑬ V1 清理十三项 ⑭ 篮子日报 + 契约总装 + 三方对拍 ⑮ 客户端双端改版 ⑯ 新服务器 + 管线多段化 + 割接 + 首包激活 + Tier 预填充 ⑰ 双端换包(等用户指令)。**§二 新增 §2.8 语义换血总表**(篮子取代单票候选 / Tier 取代排序 / 三级取代六类推送 / 自动快照取代强制表单;**历史拍板一条不删**;**§2.8-C 重新表述第〇原则四锁** —— 「不进排序」精确为「不进机械分」、「不进哨兵」精确为「LLM 文本不进判据 + 关注池是注意力分配非判定」);**§三 新增 §3.10 V2 技术选型增补**;§一 新增 1.5 V2 定位。**§七 Backlog 逐条处置**:吸收 P1-5/P1-8/P3-11/P3-15/P3-27,废弃 P4-16(换机后 800M 数字作废),新挂 P3-28(归因标签稀疏)/ P3-29(检索单点)/ P4-30(初期分时残缺)/ P4-31(停写留档表清理),新立「选股策略包门禁」长期机制。**§八 新增 V2 用户操作四项**(购新机 / 子域 DNS / 填两条 Provider / 分时外源可选),第 9 项同花顺 txt 作废。**planner 补位三张表并如实登记理由**:`llm_providers`(`app_settings` 是单行表装不下注册表)、`entry_snapshots`(决策日志表单退役需要落点,`decision_log` 的 NOT NULL 列无法就地放宽)、`selection_pack_activation_log`(§12.4 要激活日志但未给表名)。v1.5 施工图全文 + v1.5.2 收官快照 → `archive/v1.5_施工图_20260802归档.md`
- 2026-08-02 · 📐 **选股策略包机制定稿入档**(用户批准,`V2架构设计.md` 新增 §十二):声明式配置包(非代码插件)、包只管①种子规则+③Tier权重、原语白名单+`engine_api_version` 兼容校验、新表 `selection_packs`(append-only 单现役,激活复刻四道闸体例)、按包版本归因、K 字头版本、初版把现成选股代码整理成首包灌入。纯文档,零代码改动
- 2026-08-02 · 📋 **V2 追加裁定三项入档**(`V2架构设计.md` §〇 #9–11):① V1 清理遗留定案——自选池+同花顺对账**直接删**、呼吸台账**删**、五常驻保底删除**确认**;② V2 不设「首战核验/待验」类条目,验证归用户,施工线只交付 APP;③ 选股板块预留**可轮替策略包插槽**(策略线打包交付、系统线校验激活,机制设计讨论中)。纯文档,零代码改动
- 2026-08-02 · ⚖️ **⑧ 提的两处归档排期完毕 → 新增 ⑧-E(除权除息锚失效检测,排 ⑬/⑭ 之前做)+ ⑭-A 补挂 EOD 验证拍 + 三条登记对齐**(@planner,只改文档,零代码 / 零数据库 / 零部署;⑨ 并行施工中,未动 ⑨ 块正文与 §四,hunk 级挑拣提交)。**① 除权除息锚缺口 → 裁定「现在做,不进 Backlog」,新增 ⑧-E**。四条量化理由:(a) 每日被验证的篮子成员约 **40 只**,A 股除权除息集中在 5–7 月(年报)与 9–11 月(中期),峰值日 1–2%、均值 ~0.5% → **每 1–3 天就有一个成员中招**;(b) `min_members_hit=ceil(n/2)` 使 **2 只成员的篮子门槛是 1** —— 单只误判就能把整篮打成 `falsified`;(c) `falsified` 是**当日终态、不撤回**,误判挂一整天且不报错;(d) 它污染的正是 ⑨ 的验证率 / 证伪率,而那是 **P3-34 回看条件集的唯一依据** ——「拿被污染的数据校准判据 = 错上加错」,且 **V2 上线时点大概率正撞中期分红季**。**修法否决了 builder 提的「⑦ 卡里多冻一份原始价锚」**(D0 那天 `qfq == raw`、多冻的是同一个数;「原始价 MA20」把除权前后价格混进一根均线**是个更糟的数**;还要改冻结件形状 + bump `spec_version`),**改用 `pre_close` 检测器**:A 股除权日交易所公布的**前收盘价 = 除权除息参考价**,故 `pre_close ≠ 卡里 D0 收盘`(带 `_EPS`)就是除权的**直接精确信号**;数据现成 —— 盘中 `Quote.pre_close`(新浪/腾讯都已解析)、EOD `daily.pre_close`(已在 `TABLE_FLOAT_COLS`),**同一判据两条路径,不破 ⑧-C2 的对称性,⑦ 卡形状一个字不动**。命中后**复用 ⑦-b 已有的「两侧都不计」机制**(不发明新机制),⛔ 绝不把锚失效算成失效命中。**⛔ 不做自动 rescale**(虽然 `pre_close/D0_close` 数学上就是精确调整比例)—— 因为 `pre_close` 对不上有**两种成因**(真除权 / 数据错),盘中分不开,自动 rescale 会**把该报警的故障变成静默的错误判定**;宁可当天不判,不要判错。**EOD 用 `adj_factor(D+1) vs adj_factor(D0)` 交叉确认把两种成因分开**:变了 → `member_ex_rights` 正常降级不报警;没变 → `anchor_mismatch` **打 WARNING**(真故障该被看见)。排期 = **⑬/⑭ 之前**,不阻塞 ⑨,但 ⑨ 在本子项落地前攒的验证率数据含已知污染,P3-34 回看时须把这段窗口标出来。**② EOD 验证拍的调用点 —— 核对结果:⑭-A 原文确实没写,已补**。`pipeline.build_report` 分段改为「16:05 拉数 → **⑧ EOD 篮子验证拍** → ④ 扫描 → ⑤ 聚合 → ⑥ Tier → ⑦ 卡冻结 → 报告落库」,并写明**位置定死在「拉数之后、扫描层之前」**(它判的是昨日冻结的卡在今日的收盘表现,与今晚要生成的新篮子无关;放前面既符合因果,又保证后面某段炸了昨日定论行也已落好)+ 包保险丝 + 点名「**⑭ 落地前 EOD 定论行不会自动产生,是已知窗口期不用治,但 ⑭ builder 必须挂上**,否则三路读法永远只能读到盘中暂态」。**③ 三条登记顺手对齐**:(a) ⑧-A 补注「ETF 部分未做**不是遗漏**、无数据源、现状是板块基准指数」并新挂 **§七 [P4-35]**(点明蓝图 5.4「板块 ETF 或指数失去承接」这条监测**指数只能替代一半** —— 宽基反映不了单一题材的资金去留,⑪-A 落地时按现状实现并如实标注);(b) ⑧-B 写明 `capture_status` **落点 = `sentinel_events`** 与三条理由,并定死 **⑨ 的读侧入口 = `capture.load_capture_status()`**(⛔ ⑨ 不要自己翻 parquet 数行数;它区分「没记过」与「记了是 missing」),同时明确 **⑧ 只落原始数字、不替 ⑨ 发明「多少算够」的阈值**;(c) ① 的 parquet 列草图**更正** `intraday_ticks.trade_date` 为 **`pl.Date`**(原文误写 TEXT),并写明理由与「**⑧ 按 `pl.Date` 落地是对的,别照原文改回 TEXT**」
- 2026-08-02 · ⚖️ **⑦ 提的两处裁定完毕 → 新增 ⑦-b(验证/失效条件集定案)+ ⑦-K7-b(共振邻域读法),⑧ 解除阻塞**(@planner,只改文档,零代码 / 零数据库 / 零部署)。**① 验证 / 失效条件集与聚合规则(阻塞 ⑧)** —— **归属裁定 = 引擎默认 + P3 登记,⛔ 本版不进包**(与 ⑥-b 结论相反,三条理由写死防类推):(a) **§12.2 插槽边界是用户拍板的**,明文把「④篮子卡冻结体例」列在「引擎本体,不进包」一侧,要包化必须走「扩插槽边界(用户拍板)→ 扩 schema → 发包」三步;(b) **零审计背书**,⑥-b 的质量线作用在 K7 带审计的权重上,这套条件集连方向性证据都没有,进包会被"体面化"成"策略线校准过的参数";(c) 它有下游机械消费方(哨兵)—— ⚠ 但同时澄清**包化的风险并不来自「换包改盘中判定」**:spec 在 D0 就冻进 `card_json`,⑧ 读冻结件,换包只影响此后新卡(与 `stop_pct` 指纹同机制),真正理由是 (a)(b)。**落地要求**:阈值从函数关键字默认值**集中到一处命名常量** + 登记 `_ENGINE_CONSTANT_WHITELIST`(注释写明"临时默认、零审计背书")+ **新增 `verification_ruleset_version` 落卡指纹**(与跟形状的 `spec_version` **分开** —— 没有它,⑨ 会把两套条件集的成绩混成一锅)。**条件集定案**:采纳 builder 结构,**修订失效侧第 ③ 条** —— 原「`收盘 < MA20`」与验证的「`≥MA20`」**互为反面**,擦边跌破就判「驱动已证伪」,`unclear` 几乎没有生存空间、`falsified` 打得又早又滥;改为「`< D0 收盘` **且** `< D0 MA20`」两条 AND,**中间地带才落回 `partial`/`unclear`,四个格子各有地盘**,且**零新常量**。补齐 Plan 缺口:**失效侧聚合门槛**(builder 只定了验证侧)取同一个 `min_members_hit`(对称、不发明第二个数;按角色加权留 P3-34);**四态映射表**定死 + **`falsified` 优先级压过一切**(宁可先说坏消息);**数据缺失 ≠ 证伪**(两侧都不计 + 标 `member_data_missing`)。**语义红线**:`verified` 证明的是「没走坏 + 共振存在」,**不是「驱动兑现」更不是「可以追」**;**止损线在这里只判「驱动是否被证伪」、⛔ 不接任何持仓动作**(与哨兵执行的 −5% 纪律同数字同单一源但是两回事)。**③ 盘中 vs EOD 子集(⑦ 问的第三点)→ 新增 ⑧-C2**:**同一份 spec 不拆子集,拆的是代入什么价**(盘中现价 / EOD 收盘);**锚一律取 D0 冻结值,盘中不重算** —— MA20(盘中根本算不出今日 MA20)、跌停价、止损线用的 `stop_pct` 都读卡里的指纹,**不读"当前现役 config"**(否则同一张卡在章程切换前后讲两套话);**状态未变不落行、变了才追加,EOD 那一拍无论变没变必落一行**;**「当前状态」三路读法**定死(有 EOD 取 EOD / 只有 intraday 标「盘中暂态」/ 都没有标 `not_evaluated` —— 「还没判」与「判了是 unclear」必须分开);**`falsified` 是当日终态不撤回**(承 v1.3「D5 判向定格一次」,堵死"跌破了→又回来了→系统改口说没证伪"),`verified` **不是**终态可翻;**无卡的篮子不判**(落 `unclear`+`no_card`,⛔ 不许拿默认条件顶上 = 凭空编判据)。**② ⑦-K7「簇内连板高度第一」→ 采纳共振邻域读法(新增 ⑦-K7-b)**,三条理由:审计粗簇(一票一簇)与 ④ 多路聚类**本就不同构且 ⑥ 已立过「簇不必同构但语义必须保住」的兼容注**;字面读法在多路聚类下是**逻辑上的必然膨胀**(实证 36/42 = 86% 命中 = 背景噪声不是警示);「邻域至少有一只更低」是**文案自洽的必然**(说「3× 于后排」得先有后排)。并列最高都打标(警示从严,标注件不进排序无唯一性约束);实证三天命中 6/6/5。**③ 顺手补两处契约欠账**:⑭-B 点名 **`card_not_ready` 是全新字符串、`mapReason` 必须加新 case**(⛔ 不能指望 fallback —— v1.4 `watchlist` 的 `not_found` 被误显示成「持仓已清」的案底),并要求 `basket_not_found`(篮子不存在)与 `card_not_ready`(篮子在、卡未生成)**两个 404 分得开**;⑮ 点名 **`card_json` 与 `reviews.result_json` 两个冻结快照 DTO 必须手写 `init(from:)` + 全字段 `decodeIfPresent`**(合成 `Codable` 的后果 = 新 App 翻老卡整张解不出)。**④ 新挂 §七 [P3-34]**「V2 判据的工程重解读 / 零审计背书登记册」(与 P3-32/33 分开挂 —— 那两条问数值取多少,本条问判据语义落得对不对),两项 = 共振邻域重解读(**策略线须知**)+ 验证/失效条件集零背书(待 ⑨ 归因)
- 2026-08-02 · ⚖️ **⑥ 提的两处 Plan 缺口裁定完毕 → 新增 ⑥-b 追加子项(质量线进包 + T3 下限 + 溢出披露)**(@planner,只改文档,零代码 / 零数据库 / 零部署;并发窗口内未动 ⑦ 块正文与 §四,提交走 hunk 级挑拣)。**① 档位质量线归属 → 裁定「进包」**(`config.tier.quality_lines`,不是引擎常量):照 ⑤-c 立的框架(引擎常量治统计/工程有效性、pack 参数治策略偏好且 regime 敏感),质量线属后者;**决定性理由是标度耦合** —— 质量线与五维权重**作用在同一标度上**而权重已在包里,把线留在代码里 = 策略线换一次权重就**静默改变 T1 选择性**且无人察觉(这正是它与 ⑤-c 那个门槛的关键差别:成分数 5 与包里任何数都不共享标度)。schema 定死 `{tier1_min, tier2_min, tier3_min}` 与 `weights`/`dims`/`stage_scores` 平级、**可选键**;照 ③-K7-C 规则逐条核过 **不 bump `ENGINE_API_VERSION`**(纯增量可选键 + `K4-pack-v1` 原样重校验仍通过 + 缺键回退现有引擎默认 → 旧包行为逐位不变),落点 `pack.py::validate_config` 与 `stage_scores` 同一处;**`quality_lines` 缺键回退 vs `weights` 缺维度 fail loud 两种姿势刻意不同**(K4-pack-v1 不重发版、是 ⑯-E 回滚锚,不给回退就等于作废回滚锚),理由写死防被"统一";引擎常量降级为「缺省回退值」不删;`packs/K7-pack.json` 补键 `0.60/0.40/0.25`(合法窗口 —— ③-K7 已写明 ⑯-E 之前是最后改包窗口);校准走 ⑨ 周报 + 进化门禁。**② planner 主动纠一处:T3 也要有下限** —— 原实现「T3 无下限」使 `T3≤10` 成为**事实上的配额**(只要有 ≥10 个候选就永远填满),而蓝图 4.4「上限不是配额」是**对三档同时说的**,这正是 V1「每天硬凑 20 只候选」那个被 V2 立项要治的病;补 `tier3_min=0.25`(planner 拟的临时默认,比 T2 线明显低但不为零),并定死**三档全部可空、「今日无篮子定档」是合法输出、⛔ 不许为让报告好看放宽任何一条线**。**③ 容量溢出篮 → 采纳 builder 处置(不臆造 tier、不落库、留痕 `TierResult.dropped`)**,否决「全塞 T3 放宽容量」(会突破写死的 `T3≤10` 并把上限变回配额)与「`baskets.tier` 改可空」(① 已完工要重建表,且"今天没定档"本就不该在 `baskets` 里表达);**同时写死它与「⛔ 不许因为没卡把篮子抹掉」的边界** —— 那条治「**已作出并落库的判断**被下游失败藏起来」,溢出篮是「**系统明确没给出判断**」,编个 tier 才是不诚实;**但「今天有 N 个候选没被定档」必须披露**(否则用户以为今天全市场就这些篮子),故 ⑭-A 报告结构新增 **③b「今日未定档篮子」一行摘要**(篮子名/机械分/原因码,不出卡无 `basket_id`,零溢出时节仍在),**原因码必须分 `capacity_overflow`〔分数够位置满 = 机会多到装不下〕vs `below_quality_line`〔连 T3 线都没过 = 今天没好货〕,⛔ 不许合并**(两者指向相反的市场结论),契约 `droppedBaskets` 默认空数组。**④ 顺带钉死中性填充可见性**(承 ⑥ 登记 ②③):`leader_clarity` 的 `1/rank` 在 `rank=2` 时**恰好等于中性分 0.5**,「第 2 名」与「没数据」数值撞车 → **任何消费 `mech_breakdown_json` 的下游必须同时读 `flags`,⛔ 不许只看数值**(守门单测);新增可审计字段 `neutral_filled_weight`(中性填充贡献的权重合计),因为三维缺数据靠三个 0.5 也可能压过 `tier1_min` = 「**因为不知道所以进 T1**」;**刻意只记录+披露、先不设闸**(不凭空造阈值,承裁定 #5),要不要据此降档进 P3-33。**⑤ 新挂 §七 [P3-33]**(与 P3-32 分开挂 —— 要用的证据不同:P3-32 要主归属归因,P3-33 要 Tier 单调性 + T1 空档频率),一并收 `tradability` 两档扣分比例与中性填充降档两项。**⑥ 顺手修一处结构缺陷**:并发编辑导致 ⑥ 的 `###` 标题与「目标」行被错位到 ⑤-b/⑤-c 与【跨块裁定】节**之前**(⑤-b/⑤-c 渲染成了 ⑥ 的下级),已把标题移回它自己的正文之前并补回缺失的 `---` 分隔
- 2026-08-02 · ⚖️ **⑤ 提的三处 Plan 缺口 planner 裁定完毕,⑥/⑦ 解除阻塞**(@planner,只改文档,零代码 / 零数据库 / 零部署;并发窗口内未动 ④b 块正文、§四 只做增量小改)。① **落库次序自相矛盾(阻塞项)** —— 原「V2 新表汇总」把 `baskets`/`basket_members` 写入登记在 ⑦,而 `baskets.tier` NOT NULL、tier 由 ⑥ 定档、`tier_history.basket_id` 又要 `baskets.id` → 循环。**裁定**:新增【planner 裁定 · 跨块】节(排 ⑤ 与 ⑥ 之间)定死运行期次序 `⑤内存 → ⑥定档 → ⑥【事务1】baskets+basket_members+tier_history → ⑦生成卡 → ⑦【事务2】basket_cards`;**刻意两个事务**(卡生成要调 LLM,跨 LLM 持事务是错的);写入口统一搬新模块 `neckline/selection/basket_store.py`(⑤ 的 `save_baskets` 平移 + `aggregate.py` 同名再导出、既有 56 例一字不改,照 ⑤ 自己刚做过的 `json_block.py` 体例),`save_baskets` 加可选 `conn=` 以并入事务;四表一律 `INSERT OR IGNORE` **冻结不覆盖**(并写明**这与 v1.5 契约线 🟡-1「重跑要清旧行」不冲突、别套错模板** —— 那条治的是两处讲相反的话,这里同批冻结只有一个说法),重跑不一致 → WARNING + 报告如实披露;**「有篮子无卡」定为合法中间态**(⑦ 可同日重跑补 `version=1`;`GET /baskets/{id}/card` 无卡返 404 + 新 reason `card_not_ready`,⑭-B 与客户端 `mapReason` 同步;**⛔ 不许因为没卡就把篮子从报告里抹掉**);表汇总 1/2 的写入块由 ⑦ 改判为 ⑥、3 标「⑦ 只写这一张」、6 标「与 1、2 同事务」;⑥/⑦ 两块各补「落库职责」段与验收三条。② **成员卫生线无人认领** → 新增 **⑤-b**(⑤ 主体已完工,追加子项):落点定死在**「装配给 LLM 的成员清单」之前**(先过滤再按 `MAX_MEMBERS_IN_CONTEXT` 截断)—— LLM 压根看不到脏票、**既有白名单闸自动兜住**、不用造第三条拒收路径;**判据一律复用 ③ 的 `stock_hygiene`/`non_new_stock`/`k4_advisory_gate` 三原语读现役包,⛔ 禁自造第二份**(采纳 ⑤ builder 不自造"部分版卫生线"的克制);`hard_cut→exclude`/`avoid_flag→tag` 两档语义照包(标传 ⑦ 展示 + 计入 ⑥ `card_density`,即「机器不禁、人可复核」);算力只算候选成员集、当日一次装配全篮复用、耗时计入 ⑯-C;**保险丝拆两级** —— ST/停牌/次新/板块**永远拦**(数据源便宜且必在,算不出即异常、该篮拒收),`ma20`/`amount_ma20` 面板缺失才降级不拦 + 标 `hygiene_unavailable`;被剔票留痕且与白名单闸拒收**分开计数**。③ **lift 小样本失真**(涨停簇 2–5 只、实测 70–90 倍 lift,与 v1.3.1 百级成员先例不可比)→ 新增 **⑤-c**:加**最小成分数门槛 = 5** 作**引擎常量不进包**(治的是**统计有效性**不是策略偏好,进包会被误当 alpha 旋钮;与 `industry_strength._MIN_MEMBERS` / K7 五态「成员≥5」**同源同值**,实现须显式引用同一常量防漂移);不达标篮 `lift=None` + `lift_reason='sample_too_small'`(**「算不出」≠「等于 0」**)、不参与主归属比较;全不达标 → `(成员数降序, basket_key 升序)` **确定性兜底** + `primary_reason='fallback_no_qualified_lift'`;**篮子本身不因不达标被剔**;策略语义那一半(5 合不合适 / 小簇是否天然该输)**登记 §七 [P3-32]** 交策略线 / 用户在 ⑨ 攒够归因样本后过目,**在那之前 builder 不许自行改数**。另顺手钉死两条低优先级:**⑤ 疏漏 ⑦ 的自洽读法**写进 ⑤ 篮子规则条文(判据是「本篮每颗种子是否都真的被检索过」——「查了没有」→ 不成篮 / 「没查」→ 照出并明示 / 混合态 `partial` 不判不成篮,即「没有」与「没看」必须分得开);**`neckline/llm/json_block.py` 不随 ⑬-3 陪葬**已登记进 ⑬-3(它是通用件、零 import `report`,删 `reference_plan.py` 时只删那层再导出,并加守门单测)
- 2026-08-01 · 📁 **V2 蓝图入库**(commit `c1c7187`):产品总纲 `新版本量化交易APP与选股架构.md` + 工程架构设计稿 `V2架构设计.md`(2026-07-31 八项用户裁定已定格;**未立项未施工**,立项时经 planner 转写进本文件)+ `.learnings/ERRORS.md` 错误台账;V2 施工线接管交接完成
- 2026-07-30 · 🐛🚀 **用户报障快修 + 上云:LLM 没有「现在」的概念 → v1.5.2(⑨,@builder-pro,15:08–15:12 收盘后窗口部署,🔴 高危区,未回滚,**不换包**)**。**部署证据**:双备份 `neckline.db.{bak,cpbak}-v152-20260730-150653`(30318592B,integrity ok,0.31s);dry-run 零删除、5 个改动文件 sha256 逐个吻合、属主自检两次绿、依赖零变更;preflight `VERSION=v1.5.2`/43 路由 + 四条 prompt 均含时效纪律;15:08:57→15:08:59 重启中断 ~2s、`NRestarts=0`、error 0 条、`memory.events` 全零;公网 `/health`=**v1.5.2**;**本版零迁移**(26 表 / `reference_plans` 25 列不变)、`integrity=ok`、15 张业务表行数逐表一致、`is_active` 仍 `v1.3.3` 未变;活体——老快照读取不崩、持仓两笔 D1 无回归、info-card 2.23s、404 reason 精确、无 token 401;**⑨ 专属实地证据**:生产上装配的问询台/审判两处上下文首行均为「今天是 2026年7月30日(周四);下一交易日是 2026年7月31日(周五)。」(生产真日历解出),检索词实测带「2026 最新」。**回滚绳** = 该双备份 + `a984596`。**报障**(带截图):问询 603298 的回答把 **2024 年研报的目标价**当现行参照。**排查**:生产 `inquiry_log` 实证**联网是通的**(两次问询 search_hits 2489 / 8565 字节、含 2025-06 命中),但命中新旧混杂;**真因 = 三处 LLM 提示词一处都没注入当前日期**,模型没有"现在"的概念,2024 年研报与上月公告在它眼里一样新。**修三件(提示词层,零契约、客户端零改动)**:①**当前日期锚**——问询台/审判/参考件三处上下文**第一行**统一插「今天是 X 年 X 月 X 日(周 X);下一交易日是 …」,日期与交易日动态取(`CN_TZ` + `neckline.calendar`,零硬编),参考件那处额外**点名「明早 = 下一交易日 X」**(周五生成时"明早"是下周一)且与涨跌停锚同源,补跑历史日时如实说明基准日不撒谎,日历算不出只少一项、不抛异常;②**时效纪律**——四条 system prompt(问询台/候选审判/自选审判/参考件)嵌同一份 `TIMELINESS_RULES`(引用必须带日期 / 超半年必须明示「截至 X 的旧数据」/ **旧研报目标价只能作历史参照、不得当现行定价基准**);③**检索词时效引导**——问询台 `_build_search_query` 与**新增的**审判侧 `judge_search_query` 都补当前年份词「2026 最新」,**只改检索词文本、不加任何新 API 参数**(v1.3.4 案底:不被上游认识的参数会静默 0 命中)。**单一实现** = 新模块 `neckline/llm/prompt_context.py`,三处 import,守门单测扫全仓禁止第二份拷贝。**两处实现取舍**:①年份词**紧跟主体不放最末**(GLM `max_search_query_chars=78` 截尾会把末尾时效词连同用户长问句切掉,单测用真常量跑过截断验证);②审判链路此前**从不显式传 `search_query`**,本次改为显式传——是 v1.3.4 已验证的既有参数不是新参数,但审判侧 GLM payload 自此多一个字段(三处测试桩同步)。测试:Python **2003 过 + 2 skip**(基线 1969+2,净增 34),iOS `xcodebuild build` SUCCEEDED(Swift 源码零改动、仅版号)。版号三方联动 **v1.5.2**,**不换包**(已装 1.5.1 App 设置屏会显示版本差提示,属预期)
- 2026-07-30 · 🚀 **v1.5.1 上云 + macOS 换包 1.5.1 完工(⑧-E,@builder-pro,用户 11:58 明示授权提前到午间休市执行,🔴 高危区,未回滚)**。**窗口** = 12:07–12:17 CST(11:30–13:00 休市内),12:07:06 restart → 12:07:08 startup complete = **中断 ~2s**,开盘前 40 分钟收工;**本轮零改动 `neckline/sentinel/`**(`git diff 510bf20..HEAD` 实查 0 文件),盘中行为不变,未碰 09:26 推送与 16:05/16:35 两 timer(下次触发 4h+ 后,`load 0.12`、无运行中 job)。**部署前双备份** `neckline.db.{bak,cpbak}-v151-20260730-115951`(各 30244864B,`.backup` integrity ok、耗时 **0.34s**)——**回滚绳** = 这对备份 + v1.5.0 代码态(`510bf20`)。**唯一迁移** `reference_plans.take_profit_retrace REAL` 由 `init_schema` 幂等补列成功,该表 **24→25 列**;`integrity_check=ok`、**14 张业务表行数迁移前后逐表一致**、**`is_active` 仍 `v1.3.3` 且 `activated_at` 未变**、`strategy_activation_log` 仍 3 行。`sync_code.sh` dry-run **零删除**、实传后属主自检**两次绿**(`data/` `2770 neckline:neckline`、`.env` 600);**生产 9 个关键文件 sha256 与本地逐个吻合**;preflight import **`VERSION=v1.5.1` / 43 路由 / 38 去重路径**(无新端点);**依赖零变更**;重启 `NRestarts=0`、journal **error/exception/traceback 0 条**、`memory.events` **全零**、idle 86→98MB;本地 + 公网 `/health` = **`v1.5.1`**。**活体**:`/report/latest` 200 读昨日快照——20 只候选老四件套四键全非空(过渡文案)、`referencePlan` 全 null、`judgeSkipped` 全 false、16 条自选 `dispatchAlerts` 非空 0 条 = **老快照兼容不崩**;info-card **2.25s / 17392B**;404 reason 逐个精确(`report_not_found`/`code_not_in_report`/`not_found`);无 token 全 401、`/health` 免鉴权 200;全部非 2xx 均来自本次 127.0.0.1 探针,App 侧全 200。**macOS 换包**:旧包先 `ditto` 备份到 `~/Lino/app_backups/`(另移一份进废纸篓,**未用 `rm -rf`**)→ **`ditto`** 装 `/Applications`,**产物与安装态可执行 sha256 逐字节一致**(`204304ab…`)、`1.5.1`、universal、签名 valid + Designated Requirement,启动无崩溃且服务端记到该 App 真实带 token 请求 5 端点全 200。**iOS 未换包**(装机是用户动作)。**⚠ 实盘状态已变(用户今晨自行处置,早于部署)**:`300759.SZ` 已 `STOP_LOSS` 清仓、`300261.SZ`/`002036.SZ` 均 `MANUAL` 清仓(三笔 `updated_at` 在 09:52–10:32,`positions` 行数部署前后恒 5);现持仓 = `601567.SH` + `600499.SH` 两笔 D1。**🔴 今晚 16:35 首战四项待验**见 §四
- 2026-07-30 · ✅ **v1.5 两线 review 出报告(0🔴 + 3🟡)→ 三张黄牌当天全部修完 + 四条便宜观察项顺手做掉,代码态 `v1.5.1`(当日 12:07 已部署,见上一行)**(审计 @reviewer 两线并行,修复 @builder-pro,**纯本地施工、零生产访问**)。报告存档 `archive/REVIEW_REPORT_v15_{判定线,契约线}_20260730.md`。黄牌处置(逐条按报告正解,不取最小修):**判定线 🟡-1**(commit `d80f57c`,单独提交供复核)verdict 标签被三件套 JSON 自由文本**双向劫持** → 改「先剥 ```json 围栏、再解析 verdict」,`judge_candidate` 新增可选 `narrative_splitter` 依赖注入(`llm/` 不反向依赖 `report/`,围栏解析仍是唯一实现)、剥出的 JSON 走 `JudgeResult.parsed_attachment`(不落库不进契约),`_parse_verdict` **逻辑一字未动**、老两条路径逐字节不变,prompt 补禁令,回归含审计员两个复现用例 + 一条**反证测**;**契约线 🟡-1**(`1783090`)同日重跑 + 预算耗尽时「陈旧审判结论」与「本次未发起」并存 → **写侧收口**(标 skipped 时批量删当日该批码的 `llm_judgments`/`reference_plans`,单事务幂等),**不读侧遮蔽**;**两线共**(`3f0a5a2`)「章程 −5%/回落止盈 8%」硬编文案 → 参考件加口径指纹 `take_profit_retrace`(与 `stop_pct` 成对,幂等补列)+ 契约增 `buy.stopPct`/`exit.takeProfitRetrace`,markdown 与 Swift **各自动态生成**标签、缺指纹退化成不带数字说法。便宜项四条:🔵-2 Swift 未知 status 不再静默消失(展示态抽成可单测的 `displayState()`)、🟢-4 残留围栏(含未闭合半截)剥净、🔵-4 §四 清掉不存在的 `judgeSkipReason`、🔵-5 `WatchlistCheckOut` docstring 更正;其余 🟢 留档不做。版号三方联动 **v1.5.1**。测试:Python **1969 过 + 2 skip**(基线 1942+2),Swift iOS Simulator **178 跑 / 13 skip / 0 fail**,iOS+macOS build SUCCEEDED,macOS Release 产物 1.5.1 就绪(签名 valid,**未换包**)。**唯一迁移** = `reference_plans.take_profit_retrace`(该表生产 0 行)
- 2026-07-30 · 🚀 **v1.5.0 上云 + macOS 换包 1.5.0 完工(⑥+⑦,@builder-pro + 用户「立即执行」授权,🔴 高危区,未回滚)**。**🚩 门禁豁免**:上一版定的「16:35 首战验收三项不过不得开跑」被用户**明示豁免**——后果是 **v1.4.0/v1.4.1/v1.5.0 三版的 16:35 主链改动累计从未经过一次真实自动跑**(07-29 那两跑是 p1 代码,部署在其后),今晚 16:35 是三版叠加首战。**⚠ 时机**:简报说「凌晨」,实为 **09:40–09:47 交易时段内**;确认①今日 09:26 盘前推送已在 v1.4.1 跑完(27 条 precall、`d5exit` 零条)②**v1.5 零改动 `neckline/sentinel/`**、③重启中断 ~2s 后照常执行。**证据**:本地 **1942 过 + 2 skip**;部署前双备份 `neckline.db.{bak,cpbak}-v150-20260730-094231`(30212096B,integrity ok,`.backup` 0.291s);`sync_code.sh` 属主自检绿 + dry-run 零删除;生产 7 个关键文件 sha256 与本地逐个吻合;preflight 43 路由 / 38 去重路径;**依赖零变更**;迁移两项(`reference_plans` 新表 24 列 2 索引 + `llm_judgments.search_engine` 新列)`integrity=ok`、**13 张业务表行数逐表一致**、`is_active` 仍 `v1.3.3` 未变、`activation_log` 仍 3 行;重启 `NRestarts=0`、零 error、`memory.events` 全零;本地+公网 `/health`=`v1.5.0`。**活体**:`002036.SZ` 仍 `suspended_hold`(D7)无回归;老快照读取兼容——老四件套四键返**过渡文案**、`referencePlan=null`、`judgeSkipped=false`、16 条自选 `dispatchAlerts=[]`;info-card 3.57s(v1.4.1 的 P1-26 修复仍生效);四个 404 reason 精确、无 token 全 401、邻居站点不受影响。**macOS 换包**:`ditto` 装 `/Applications`,**产物与安装态可执行 sha256 逐字节一致**、`1.5.0`、universal、签名 valid + Designated Requirement,启动无崩溃且服务端记到该 App 真实带 token 请求 5 端点全 200(⑤-E 版本号治理已修掉此前 1.3.4 的陈旧字面量)。**iOS 未换包**(装机是用户动作)。**今晚三项待验**:`ExecMainStatus=0`+时间戳 / `reference_plans` 当日真有行且三态合理 / 20 只全覆盖下的墙钟与 `judgeSkipped` 命中数 + `MemoryPeak` 是否仍 ≤838,860,800 且 `oom_kill=0`(P4-16 复核点)
- 2026-07-30 · **v1.5-⑤ 客户端跟进 + A2 版本号治理 + iOS/macOS 构建产出完工(v1.5.0 施工全部完工,仅剩 ⑥⑦)**(@builder,纯本地施工,零生产访问/零部署):Swift 侧——`TodayPlanView` 今日计划拆两块(①持仓股/②候选列表,各补定位文案);新组件 `ReferencePlanSection`(`Components/SharedUI.swift`)替换 `FourPieceDisclosure` 展示参考三件套(`ok` 态折叠展开、`vetoed`/`unavailable`/`nil` 三态直显不折叠,`judgeSkipped` 独立 chip 不与 `llmJudgment` 合并);`Candidate` 新增 `referencePlan`/`judgeSkipped`(新型 `ReferencePlan`/`ReferencePlanBuy`/`ReferencePlanExit`,普通 `Codable`);`WatchlistCheckItem` 新增 `dispatchAlerts`(新型 `DispatchAlert`,`.warn` 色调展示);顺带修复 `AppModel.beginPositionEntryFlow(fromCandidate:)` 进场理由预填(老 `buyPoint` 已恒为过渡文案,改用新纯函数 `entryReasonText(for:)` 取 `referencePlan.buy.why`,单测覆盖)。A2 版本号治理——`client/project.yml` + `pbxproj` app target 三处 `MARKETING_VERSION`→`1.5.0`,`neckline/api/app.py::VERSION` 本会话内同步改 `v1.5.0`(纯本地源码常量,未部署,是 ⑤-E 自身「三者恒等」验收要求的必然推论)+ 新 Python 守门单测 `tests/test_client_version_governance.py`;`APIClient.health()` 返回签名改 `(ok, version)` 元组 + 设置屏展示 App/服务端双版本。iOS/macOS Release 构建均 `BUILD SUCCEEDED`(`CFBundleShortVersionString=1.5.0` 已核实,产物路径 `~/Library/Developer/Xcode/DerivedData/Neckline-*/Build/Products/{Release-iphoneos,Release}/Neckline.app`)。§八第 12 项 iOS 装机说明改写。**视觉核对(真实本地后端)**:隔离 SQLite 库(拷四张只读表)+ 真 parquet(20260724)跑 `scripts/report.py` 生成真报告,一次性脚本(scratchpad,已清理)patch 出 ok/vetoed/judgeSkipped/dispatchAlerts 四态,iPhone16Pro/临时 iPadPro13 模拟器(用毕已 delete)截图逐一核对;ok 态展开内容与设置屏版本行因本环境 touch/scroll 能力受限未截图,靠单测 + 代码审阅兜底(CLAUDE.md 已补记该环境限制的新发现)。测试:Python 全量 **1942 过 + 2 skip**(基线 1940+2,净增 2 = 新守门测试本身 + guardrail 对新文件自动追加的 1 个参数化实例,零 server 业务逻辑改动);Swift 离线套件 153 个(`DTODecodeTests` 94/`AppModelTests` 53/`URLGateTests` 2/`AppConfigDefaultTests` 4)iOS Simulator `xcodebuild test` 全绿,双端 `xcodebuild build`(Debug)SUCCEEDED。语义红线 grep 零真实命中。规格偏差 4 处均实现细节层面必然补全,详见 §四
- 2026-07-30 · **v1.5-④ 存量配菜四件完工**(@builder,纯本地施工,零部署;**A3 对生产库做过一次只读 SQL 探针,已如实登记**):A1 自选票 K4 派发警示(`watchlist_check.py::attach_dispatch_alerts` 复用 `holding_k4_check` 同一份镜像,只取 A3/A3b 两码,契约 `WatchlistCheckOut.dispatchAlerts`,交叉断言锁死与持仓侧判定逐位一致,⛔ 不推 APNs)+ A3 `search_engine` 定案(`llm_judgments` 幂等加列 + `GLMProvider._SEARCH_ENGINE` 单一源 + provider 层透传;生产只读 SQL 实捞 20260724~20260729 共 4 交易日 / 40 条非降级审判 / 0 命中率 0.0%,判定基线健康→维持 `search_pro`,§七 P1-7 结案,未消耗 A/B)+ A4 测试卫生(`conftest.py::real_db_readonly_copy` session 夹具 + 三个 guardrail 文件六处裸调用改显式 `db_path=` + 新 AST 守门测试 `test_db_isolation_guardrail.py`〔避开纯文本 grep 对 `test_brain.py` docstring 的误伤〕,实测全量 pytest 前后 dev DB mtime/MD5 逐字节不变,§七 P4-25 结案)+ A5 章程回滚 SOP(`archive/SOP_章程回滚_20260730.md` 一页纸四节 + 三处指针,§七 P1-24 结案)。测试:Python 全量 **1940 过 + 2 skip**(基线 1819+2,净增 121,大头是 A4 新增的 97 个参数化守门用例)。规格偏差两处均为实现细节层面合理补全(A1 markdown 渲染行、A5 第三处指针),已如实登记,详见 §四。
- 2026-07-30 凌晨 · **v1.5-③ 卡面改版 + 老四件套退役 + 报告分两块完工**(@builder,纯本地施工,零生产访问/零部署):候选卡老四件套(买点/止损/目标/证伪条件)全面退役——`intel_candidates.py::_build_intel_candidate` 不再调用四个文案函数(`entry_spec`/`invalidation_spec` 结构化字段一字不动,新增守门单测锁死键集),`Candidate` 对应四字段改带默认空串(dataclass 字段重排到 `invalidation_spec` 之后满足语法,全仓构造点均关键字传参、重排零影响,已核对);`api/app.py::_shape_candidate` 新增单一源常量 `LEGACY_FOURPIECE_NOTICE`,`buyPoint`/`stop`/`target`/`invalidation` 四键**无条件**下发过渡文案(不读落库快照、不按报告新旧分叉,老报告快照同样统一改发)满足已装 v1.4.1 客户端硬解码的向后兼容硬约束;`report/candidates.py` 四个文案函数保留不动(自选体检仍消费)。`render.py::_render_candidates`「前N只详情/后N只表格」两段结构合并成一段(全部候选统一走详情 + 全量速览表),候选卡新增执行提示行(`exec_hint.py` 既有计算首次渲染进 markdown)+ 参考三件套渲染(新函数 `_render_reference_plan`,ok/vetoed/unavailable/None 四态诚实展示,被夹逼拦下的子项不画空区间不写0)。报告 markdown 新增「持仓体检」节(`_render_holding_check`,排在候选**之前**,数据源 = pipeline 已算好的 `holding_k4_check`,零新增计算,时间退出五态含 `SUSPENDED_HOLD`、K4 强/普通警示按持仓侧 level 分级,与候选侧 hard_cut/avoid_flag 红黄牌是不同分类维度)。语义红线全仓 grep 巡检(推荐买入/建议买入/看好/值得买/推荐买点/目标价/止盈线)零真实违规(命中均为负向表述或指向 `decision_log` 用户自填目标价等不同概念)。测试:Python 全量 **1819 过 + 2 skip**(基线 1790+2,净增 29:`test_render.py` +22 / `test_intel_candidates.py` +1 守门单测 / `test_pipeline.py` +2 接线单测 / `test_api_report_board.py` +1 契约测),哨兵既有单测 105 个逐位复跑绿零改断言;未动 Swift。**一处规格与实现小出入(已如实登记)**:计划中「定格日晚点标注」若照 `api/app.py::_locked_time_exit_day` 算出「晚了几天」需要交易日历 I/O,与 `render.py`「纯函数不碰 I/O」模块契约冲突,故 markdown 只展示定格发生的原始日期(已有字段),晚点天数不重复展示——该信息本就已由 `PositionOut.timeExitLockedDay`/`timeExitLockedLateDays`(v1.4-⑥-C)在客户端持仓卡呈现,markdown 报告未丢失任何用户可得信息。规格与现实基本无冲突,零 TODO 遗留
- 2026-07-29 深夜 · **v1.5-② LLM 覆盖面与预算重排完工**(@builder,纯本地施工,零生产访问/零部署):`pipeline.TOP_N_JUDGED` 10→20(=`TOP_N_TOTAL`,旧「前10审/后10不耗LLM」分档退役);判官循环抽成 `_judge_candidates_with_budget`(调用机制未动,仍逐票一次 `judge_and_build_reference_plan`),新常量 `CANDIDATE_JUDGE_BUDGET_SECONDS=1200`(独立预算,与 `news_alerts.LLM_SCAN_BUDGET_SECONDS=300` 不合并)——按 rank 升序审,预算耗尽从当前位置到列表尾整体标 `Candidate.judge_skipped=True` 且不再发起调用(不掐断进行中调用)。新字段 `judge_skipped`/`CandidateOut.judgeSkipped`(与 `llmJudgment.degraded` 语义分开,`app.py`/`render.py` 已接,render 新增专属文案分支不与真异常「未执行」混淆)。**并发(②-C)**:本地三处核实均无 GLM/Kimi key → 限频无法实测 → 定案**串行+预算硬闸**(照 `news_alerts.py` v1.4-⑥-B 先例,理由入 docstring),未写任何并发脚手架。**时间账(②-D)**:本地实测机制自身开销(零网络)≈7–10ms/候选,可忽略;用等比缩小延迟验证预算边界确定性行为(调用前检查、不掐断进行中调用),反推生产最坏情形 20 只约 17 只能审完、约 3 只 `judgeSkipped`;已回填 PROJECT_PLAN「v1.5 LLM 预算账」节,与既有估算量级一致。测试:Python 全量 **1790 过 + 2 skip**(基线 1779+2,净增 11:`test_pipeline.py` 8 + `test_render.py` 1 + `test_api_report_board.py` 2);未动 Swift。规格与现实无冲突,零偏差
- 2026-07-29 深夜 · **v1.5-① 参考件三件套地基完工**(@builder,纯本地施工,零生产访问/零部署):新模块 `report/reference_plan.py`(`REFERENCE_PLAN_SYSTEM_PROMPT` 一次 LLM 调用出「自由评语+结论标签+三件套 json」,复用 `judge_candidate` 新增可选 `context_block` 参数、不改调用/解析/降级链;买入区间夹逼明日涨跌停〔唯一底线〕、离场区间只格式校验不夹涨跌停;止损价系统算 `close×(1−stop_pct)` 读现役 config;三态 `ok`/`vetoed`/`unavailable`)+ 新表 `reference_plans`(`reference_plan_store.py` 幂等落库)+ `Candidate.reference_plan`/`CandidateOut.referencePlan` 契约(老四件套四键与 `entry_spec`/`invalidation_spec` 一字未动,退役归 ③)+ `pipeline.py` 判官循环改调 `judge_and_build_reference_plan`(`TOP_N_JUDGED` 仍 10 不变,覆盖面扩到 20 是②的活;两层降级:上下文装配失败退默认上下文仍判一次、装配异常整段 try/except 兜底 `reference_plan=None`+候选照出)。守门三条全绿:`sentinel/` 全目录 grep 零命中 `reference_plan`/`referencePlan`(15 文件参数化测试)、`_SORT_KEY_INPUTS` 白名单新测锁死、`notify.__all__` 仍六类(既有测试原样过)。测试:Python 全量 **1779 过 + 2 skip**(基线 1710+2,净增 69:`test_reference_plan.py` 60 + `test_pipeline.py` 5 + `test_intel_candidates.py` 1 + `test_api_report_board.py` 3);本块未动 Swift(归⑤),iOS/macOS 沿用上次基线 169 跑 156 过 13 skip。规格与现实无冲突,零偏差
- 2026-07-29 · **v1.5.0 立项**(施工图七块就位待 builder)· 候选参考件三件套〔LLM 参谋化〕= ① 三件套地基(生成 + 明日涨跌停夹逼 + `reference_plans` 落库)+ ② 20 只全覆盖与 LLM 预算重排 + ③ 卡面改版与老四件套退役(键保留 + 过渡文案,哨兵结构化证伪不动)+ ④ 存量配菜四件(自选派发警示 / P1-7 定案 / 测试卫生 / 回滚 SOP)+ ⑤ 双端客户端 + 版本号治理 + iOS 构建 + ⑥ 部署上云(门禁 = 07-30 v1.4 首战验收)+ ⑦ 换包(等用户指令);§2.0 新立**第〇原则**(LLM 是参考、硬条款才是纪律,参考件不触发任何机器动作)、§2.3 补需求 9 拍板块;v1.4 施工图归档 `archive/v1.4_施工图_20260729归档.md`,需求 9 入档 `archive/交接_系统线升级需求_20260725.md`(@planner,只改文档,零代码改动)
- 2026-07-29 · 🔥 **v1.4.1 热修上云:信息卡从「总是加载失败」修到冷调 3.0s / 热调 2.25s(§七 P1-26 结案)**(@builder-pro,用户报障驱动,🔴 碰生产部署,**未回滚**)。**症状**:App 加载信息卡总是失败。**两层根因**:①服务端端点公网实测 **14.15 / 13.98 / 13.89s**,而②客户端 `APIClient.fetchInfoCard` 走 `get()` **默认 12s 超时** → **必然超时**;用户重试叠加把常驻服务顶到内存节流线(实测 3 次调用 `memory.events high` **+8945**,`MemoryCurrent` 440MB ≈ `MemoryHigh`),越试越慢。**⚠ 原诊断只对了一半**:P1-26 原文说根因是 `_load_single_code_panel` 全 glob —— 它确实全 glob,但**真正的大头是 `compute_sentiment` 一次请求做 5 次全市场横截面**,连同单票 420 日面板 2 次 + 大盘指数线 1 次 = **8 次全 glob**(每次打开 1592 个 parquet footer);所以修在 `market_data._scan_table` 这一层才对,只修 `_load_single_code_panel` 会治不好。**修法(修法①,纯 I/O、结果逐位不变)**:`_scan_table` 加 `years=` 参数,由 `get_market_slice`(单日→1 个年目录)/ `get_stock_history` / `scan_table_range`(区间→`_years_in_range` 覆盖年份)传入;`features.market_state_labels` 起点由写死 `date(2019,1,1)` 改为 `start-(ma_window*3+30)` 自然日(`rolling_mean` 是后向窗口,只需 `ma_window` 个交易日前置量,返回值照旧 filter 回 `[start,end]`)。合法性依据是**结构性**的:分区路径由 `day_file_path` 按 `trade_date.year` 生成,`year=YYYY` 里只可能有该年的行。客户端 `fetchInfoCard` 显式 `timeout: 60`(照问询台同样重的端点的既有惯例)。**公网活体验收**:`/health`=**v1.4.1**;info-card **冷调(重启后首次)3.05s**(判据 <10s)、**热调 2.33 / 2.25s**(判据 <3s)= **6.2 倍**;连打 **6 次** `memory.events high` **恒 0**(修前 3 次 +8945)、`MemoryCurrent` 440→287MB;`002036.SZ` 仍 `suspended_hold`+`priceStale{staleDays:5,suspended}` **无回归**;`/report/latest` 20260729 候选 20 只正常。**部署**:双备份 `neckline.db.{bak,cpbak}-v141-20260729-185132`(30199808 bytes,`.backup` 0.290s、`integrity=ok`)→ `sync_code.sh` DRY_RUN 先验(只传 3 个改动文件、`data/` 零触碰)→ 实传 + 收尾 prune chown + **属主自检绿** → preflight import(`VERSION=v1.4.1`)→ restart(顺带清掉节流态,`memory.events` 全部归零)。**纯代码零迁移**。**macOS 换包**:Release 构建 → **`ditto`**(非 `cp -R`)装 `/Applications`,换前旧包已 ditto 备份;已装包与新构建产物 **sha256 逐字节一致**(`15e9530f…`)且与旧包(`595b9a45…`)不同、`codesign --verify --deep --strict` rc=0。真机项按既有口径跳过。**⚠ 顺带收窄了脏分区的传染半径,已如实改写守门单测而非删断言**:v1.3.5 那条「写侧修好不会让历史脏分区自愈」现在只对**跨到脏那年**的读成立(单日/区间读不再打开无关年份),`test_dirty_first_partition_does_not_drag_new_write` 改为同时断言「同年读通过」+「跨年 `scan_table_range` 仍 SchemaError」。测试:等价单测 `TestYearPartitionPruning` 5 条(裁剪版 vs 全表版对同一份跨 3 年数据逐位相等,含缺年目录跳过);Python 全量 **1710 过 + 2 skip**(基线 1705+2,净增 5);Swift iOS Simulator **169 跑 156 过 13 skip 0 failure**、双端 `xcodebuild build` SUCCEEDED。**⚠ 留给主会话**:今日 20260729 报告快照是 **p1 代码 16:35 生成的**,故 `dataFreshness` 只有板块三键、无 ⑩-F 的行业强度三键,`infoCard`/`execHints`/`industryRank` 亦仍空 —— **明日 16:35 那份(v1.4.1 首次自动跑)才是这些新字段的首验**。
- 2026-07-29 · 🚀 **v1.4.0 完整上云成功 + macOS 换包(⑨ 完工,@builder-pro + 用户「现在立即执行」授权,🔴 高危区,未回滚)**。上午那次因 P0-23 回滚的部署,在 ⑩ 修复 + review 过门后**当天 17:00 重跑一次过**。**时序**:等今日 16:35 报告(**p1 代码最后一跑**,16:35:01→16:57:11、`ExecMainStatus=0`、报告真生成 61003B / `reports` 当日 1 行 / 候选 20 / 情报节 4837B、`MemoryPeak` 恰顶 800M 且 `oom_kill=0`)退出后才动生产;今日 16:05 日更亦 `ExecMainStatus=0`(16:05:00→16:06:28)——**⑨-D 两项当日证据齐**。**⑨-B 迁移四项全绿**:`max_chase_pct`/`inquiry_log`(上午已落、保留)+ `industry_strength_daily`(175,120 行 / 1592 交易日 / 110 行业,`verify` 三项自检 exit 0)+ **`strategy_activation_log` 播种验证**——先看源 `strategy_versions` 非空 `activated_at` 恰 3 条,再看果表内 3 条 `via='seed'`、**双向 `EXCEPT` 均空 = 逐字节一一对应**、非 seed 行 0,且 ⑨ 再跑一次 `init_schema` 后**仍是 3 条 = 幂等不重播**;`integrity_check=ok`、13 张业务表行数迁移前后逐表一致、`is_active` 仍 `v1.3.3` 且 `activated_at` 未变。**⑨-C**:属主自检绿、`.env`/`.p8` 600、preflight import 43 路由、依赖零变更、生产 6 个关键文件 sha256 与本地逐个吻合、重启 `NRestarts=0` 零 error、idle RSS 90→67MB 持平。**活体**:公网/本地 `/health`=`v1.4.0`;`002036.SZ` 仍 `suspended_hold`+`priceStale{staleDays:5,…,suspended}` 无回归;**上午永不返回的 info-card 端点现返 200 / 15.6s / 16431B**(K线60/RS线60/分歧线60,慢是 P1-26);四个 404 reason 逐个精确 + `/inquiries` 空态 200;无 token 全 401;`dataFreshness={20260728, lag1, stale:false}`。**⑨-F 定案 = `MemoryMax` 维持 800M 不改**(v1.4 加进主链的只是一次 5.3ms 读表、⑥-B 没开并发、信息卡不进 16:35;连续三跑顶满 800M 均 `oom_kill=0` 顶的是可回收页缓存;抬到 1G 后最坏并发 1624M > 物理 1612M——理由六条见 §四)。**macOS 换包**:Release 构建 → **`ditto`**(非 `cp -R`)装 `/Applications`,universal + 签名 valid + Designated Requirement 满足,启动无崩溃且服务端日志记到该 App 真实带 token 请求 `/circuit`·`/decisions`·`/report/latest`·`/board`·`/positions` **全 200**。**按授权跳过一切真机项**(iOS 未装机)。顺带:①`sync_code.sh` 的 `--exclude '.git/'` 改 `'.git'`(无尾斜杠)——上午从 git worktree 回滚时把 worktree 的 `.git` **文件**推上了生产,本次 `--delete` 清掉并堵住复发;②`app.py` 那段「本版未上云」警示改成失败部署留痕。**⚠ 顺带发现的实盘状态(非部署问题)**:`300759.SZ` 康龙化成 `todayAction`=「现价已跌破止损线…系统不代下单」,EOD `net_float` 三日递减 -344.9→-869.6→-1119.5,现役 `stop_pct=0.05`,**处置在用户**。**留给主会话**:v1.4 代码的**首次** 09:26 盘前推送与 16:05/16:35 自动跑(看 `ExecMainStatus`+时间戳),并确认当日报告新字段(`infoCard`/`execHints`/`industryRank`)真落地——今日快照是 p1 生成的,这三项仍空
- 2026-07-29 · ✅ **v1.4 上云前 review 两线出报告(0🔴 + 5🟡)→ 五张黄牌当天全部修完**(审计 @reviewer 两线并行,修复 @builder-pro,**纯本地施工,零生产访问**)。报告存档:`archive/REVIEW_REPORT_v14_判定线_20260729.md`(⑥-A 逐笔章程 / ⑩ P0-23 / P2-10 四个已发布版本)与 `archive/REVIEW_REPORT_v14_契约线_20260729.md`(P2-9 两路 + v1.4 新契约 14 条三方对拍)—— 两线均**未发现 🔴**,§七 P2-9 / P2-10 就此结案。五张黄牌处置(逐条按报告的「正解」修,不取最小修):**判定线 🟡-1**(🔴 纪律判定)激活时间线改 **append-only 事件流** `strategy_activation_log`,回滚/重激活不再改写历史周判定(commit `b2e3673`,单独提交供复核);**判定线 🟡-2** 行业强度日更**向前补洞** + 断口 ERROR/exit 1 + 有断口不许报绿;**契约线 🟡-1** 红黄牌展示序真读 DB `k4_advisory.intel_order`(六节最后一节的零消费方补上);**契约线 🟡-2** exec_hint C3 无前视截断补上 UTC/北京时区缝(决策日志日期过滤统一北京日);**契约线 🟡-3** Swift 补 `inquiryId` 解码断链。顺带做掉两条便宜观察项(🟢-8 陈旧注释、🟢-5 `forbid_high_elasticity` 跨切换单测缺口),其余 🟢 由两份报告留档 + §七 P2-9 一行挂账。测试:Python **1705 过 + 2 skip**(基线 1677+2,新增 28),Swift iOS Simulator **169 跑 156 过 13 skip 0 failure**(基线 168),macOS `xcodebuild build` SUCCEEDED。⚠ **⑨-B 迁移清单新增 `strategy_activation_log` 播种验证**(建表+播种由 `init_schema` 幂等完成,但必须验出 K1→v1.3→v1.3.3 三条)
- 2026-07-29 · 🔴 **v1.4-⑩ 生产旁路 bootstrap + 隔离实测完成,⑩-D/⑩-G 判据全绿(P1-26 一条按 plan 预授权另立)**(@builder-pro,用户授权提前到盘中 15:44 执行、明示接受与 16:05/16:35 窗口重叠的风险;**未部署代码、未重启任何服务、`/health` 仍 `v1.4.0-p1`**)。旁路目录 `/opt/neckline-p023/`(rsync 代码 + `data` 软链真实数据,**跑完先断软链再删目录,已删净**),用生产 `.venv` 跑、**零新依赖**(polars 1.42.1 + stdlib sqlite3 已在);全程 `systemd-run --scope --uid=neckline -p MemoryMax=600M -p CPUQuota=100%` **隔离单进程串行**。**⑩-D 五条判据**:①7 个年块**全 exit 0**,`memory.peak` **204~312MB**(判据 <500M)、各 **1.3~2.4s**(判据 <3min),`memory.events` 全 0 无节流无 OOM;Pass2 **27.9s / 104MB**;②`verify` **三项全绿**(交易日无洞 / streak 自洽 / 口径指纹一致),**175,120 行 / 1592 交易日 / 20200102~20260729**;③**随机 3 日单日对拍逐位一致**(20230803 / 20250904 / 20231229,各 110 行);④`stock_basic` 快照全程未变 —— 15:44 基线 5871|5538 与 16:11 的 5872|5539 之差是 **16:05 日更 `bootstrap_metadata` 刷的**(该 job `ExecMainStatus=0`、16:06:28 退出,**早于首块 16:09:02**),为取干净前后对**又幂等重跑了整套 bootstrap**,前后 `stock_basic` **5872|5539 未变**且表内容 **md5 逐字节相同**(`a1c1069eec9191892ac54c633fe327fc`,排除 `computed_at`)= **生产数据上的幂等性实证**;⑤`PRAGMA integrity_check=ok`、**22 张业务表行数与基线逐表一致**(唯二变动 `stock_basic`+1/`namechange`+2/`backfill_log`+4 均为 16:05 日更所写)、`is_active` 仍 `v1.3.3` 且 `activated_at` 未变、`.backup` 耗时 **0.074s**、库 4.59→**29.8MB**。**⑩-G 五条**:①同上;②日更增量 **0.16s / 110 行**(判据 <60s);③读侧 `load_industry_strength` **5.3ms**、60 日序列 **5.2ms**(判据 <200/<100ms);④信息卡主体 300M cap 下 **exit 0、峰值 98MB**(**此前永不返回**)但耗时 **18~20s > 10s 判据** → 按 ⑩-G 原文预授权**另立 §七 P1-26**(根因 = `_load_single_code_panel` 全 glob 1592 个 footer,与行业强度无关,行业那一维已是读表 5.3ms),**不阻塞本块**;⑤同上。**双备份**(迁移级动作)`neckline.db.{bak,cpbak}-v140p023-20260729-154459`(各 4587520 bytes,`.backup` 那份 `integrity=ok`)。**零碰撞**:16:05 `neckline-daily` `ExecMainStatus=0`(16:05:00→16:06:28,88s),我的首块特意等它退出后才起(串行纪律未被授权豁免);常驻 `neckline.service` **未重启**(`ActiveEnterTimestamp` 仍 10:09:47)、`memory.events` **全 0**(对照 07-29 上午那次 `high` 飙到 54 万)。收尾 `pgrep` 无残留(命中的两行是自己的 SSH 命令行 = CLAUDE.md 那个坑)、`systemctl --failed` 0 units、load 0.59 回落
- 2026-07-29 · 🔴 **v1.4-⑩ P0-23 修复「行业强度预计算落表」本地部分完工**(@builder-pro,**零生产访问**,生产侧 bootstrap + 隔离实测待 17:00 后):新表 `industry_strength_daily`(⑩-A DDL 照抄进 `db.py::_SCHEMA`,`NULL≠0` 三列 + 口径指纹两列 + 落全部行业含未达标);计算侧按 ⑩-B 拆 `_day_local_table`/`_attach_persist`/`next_persist_days` 三件(口径一字未改,`test_core_table_matches_research_bit_for_bit` 断言未动仍绿)+ `_ret1d_from_daily` 抽单一源;新 `report/industry_strength_store.py`(日更增量**只读当日一个分区** + `prev` 严格早于当日查询保幂等 + 补算历史日**自动向后延到库内最大交易日** + 每天一个事务 + bootstrap 两遍法 + ⑩-D 退路 `bootstrap_recent_days` + `verify` 三项自检 CLI/单测共用);新 CLI `scripts/industry_strength.py {refresh,verify,bootstrap}`;16:05 `daily_update.py` 挂 `update_industry_strength`(尽力而为不改退出码但**日志 ERROR** + 带补算命令原文);四处在线取数点 + ④分歧线 60 日序列全部改读表,保险丝四态落地(报告降级不崩且可复现 / A2 不拦 / 信息卡 rank+persist **如实缺省不写 0** / 分歧线**新增第三档 reason**「行业强度数据未就绪」与「样本不足」分开 / 问询台 `evidence` 明说本次不可得);`dataFreshness` 加三键(既有 `stale` 语义一字未改,两故障不合并)+ Swift `DataFreshness` 三 Optional 键 + 横幅两行制 + `InfoCardSnapshot.industryPersistDays` 改 `Int?`。**演练中发现并修掉一个真洞**:`rank(ordinal)` 并列由**行序**打散,而行序随「按年块 glob vs 单日分区」而变 → 同一天 bootstrap 与日更算出**不同 rank**、报告重跑会换序(20230803 真数据实测撞一串);修法 = 排名前先按 `(median_ret 降序, industry 升序)` 排定,已入 CLAUDE.md 纪律 + 专项单测。**本地真数据演练**(scratchpad DB 副本 + 真实 parquet,零生产访问):7 年块 Pass1 各 0.1~0.3s / 峰值 254~389MB,Pass2 1.3s / 115MB,合计 **3s**,落 **174,790 行 / 1589 交易日 / 110 行业**,库 3.8MB→**29.0MB**;`verify` 三项全绿;**随机 3 日单日对拍逐位一致**;`load_industry_strength` **1.3ms**、60 日序列 **1.3ms**、日更增量 **0.02s/110 行**、`build_info_card` 真数据 **0.56s**(此前生产永不返回);`integrity_check=ok` + 业务表行数未变;⑩-D 退路(250 交易日)演练 1.78s / 119MB / verify 绿。测试:Python 全量 **1677 过 + 2 skip**(基线 1647+2,净增 30),Swift iOS Simulator **168 跑 155 过 13 skip 0 failure**(基线 165,净增 3),双端 `xcodebuild build` SUCCEEDED。守门单测:四个在线文件 grep **零**现算入口 + 三路等价(全量算 ≡ 逐日递推 ≡ 落表读回)逐位相等
- 2026-07-29 · **P0-23 修法裁定 + review 时机改判,立第 ⑩ 块**(@planner,只改文档,零代码改动):用户拍板 ① P0-23 走**方案②「行业强度预计算落表」**(方案① 加窗口 / 方案③ 只修两个交互路径均否决)→ §五 新增 **v1.4-⑩**(🔴,七小节:SQLite 新表 `industry_strength_daily` 全规格 DDL / 计算侧拆三件保单一源 + 三路等价守门单测 / 16:05 日更增量只读当日一个分区 + 补跑自动向后延 / bootstrap 定案「生产旁路目录分块两遍法」带五条验证判据与退路 / 四处调用点改读表 + 四态保险丝语义 / `dataFreshness` 三新键与客户端小增量 / 生产隔离实测五条证据);② **review 前移到「修完 P0-23、重新上云前」**(原「上云后 review」作废),范围 = ⑥-A + ⑩ + P2-9 + P2-10。同步:§四 快照替换(新工序链 ⑩→实测→review→⑨ 重跑 + ⑨ 增量清单更新)、§七 P0-23 标已裁定 / P2-9 / P2-10 / P4-16 改时机、⑨-A/B/F 与验收改判、§八 第 12 项换包时机、CLAUDE.md 新增「生产机性能探针纪律」(15:00 后 · 避开 16:00–17:00 · `systemd-run --scope` 隔离串行 · load>4 停手)
- 2026-07-29 · 🛑 **v1.4.0 第 ⑨ 块完整部署:尝试 → 撞硬阻塞 → 按预案回滚**(@builder-pro + 用户「现在部署」授权,🔴 高危区,**已回滚,生产仍 `v1.4.0-p1`**)。做成并保留:本地 `VERSION`→v1.4.0 + 全量 1647 过 + 2 skip;生产库 `.backup` + `cp -p` **双备份**(`neckline.db.{bak,cpbak}-v140-20260729-092734`,备份内 `integrity=ok` + 业务行数对得上);**两处增量幂等迁移**(`decision_log.max_chase_pct` REAL + `inquiry_log` 表 + 2 索引)—— 迁移后 `integrity=ok` / 10 张业务表行数与迁移前**逐表一致** / `is_active` 仍 `v1.3.3` 且 `activated_at` 未变,**回滚时刻意保留**(向前兼容,p1 按 `_SELECT_COLS` 具名取列,已验 `GET /decisions` 200);**`ths_daily` 宽窗补拉 0720~0728 共 7 个交易日**(生产原 max=**2026-07-17**,缺口比原估的 3 天大;走 ①-C 既有 `update_ths_daily`,概念过滤生效 = **392 行/日**而非不过滤的 ~2499,dtype 合声明,补拉前 `cp -p` 备份 parquet,配额 8 次)。撞上的阻塞 → **§七 P0-23**:`compute_industry_strength` 扫全历史 784 万行,生产机 700M cap OOM-kill / 1400M cap 600s 跑不完(开发机 <1s),`GET .../info-card/{code}` 实测**永不返回**且把常驻服务(与盘中哨兵同进程)拖进内存回收死循环;**16:35 报告主链也吃这一步**,上了大概率当日无报告 → 判定硬阻塞,rsync 回 `fc55e2e` + 重启,`/health`=`v1.4.0-p1`、②~⑧ 端点 404、①-B 停牌口径无回归、`integrity=ok`。**刻意没做**:`MemoryMax` 未改(新证据表明抬内存不是解,且 v1.4 已回滚无消费方)、macOS/iOS **未换包**(v1.4 客户端配 p1 服务端会可见地坏:信息卡 404 / 问询历史 404 / `maxChasePct` 静默丢弃)。⚠ **过程中两次自伤已记取**:`pkill -f probe_industry` 匹配到自己的 SSH 命令行把会话杀了;探针把 2 vCPU 箱负载推到 65(已全部清理,`load 0.42` / 无残留进程 / failed scope 已 `reset-failed`)。今日 16:05 与 16:35 的 `ExecMainStatus` 待主会话验(跑的是 p1 = 与昨天同一份代码)
- 2026-07-29 · v1.4-⑧ 客户端跟进完工(@builder,纯本地,零生产访问/零部署):七项契约(①③④⑤⑥⑦)全部接线——①补录持仓日期选择器+停牌/`suspended_hold`/`dataFreshness` 展示;③排序理由三级 chips(`industryRank` nil≠0)+`sectorFlow`「并列参考」改文案;④新 `InfoCardPageView`(Swift Charts 三图:K线蜡烛+MA20/MA250+成交量、RS线、行业分歧线,均如实缺省不画空图)+ 快照/红黄牌/温和带/消息面/龙虎榜/市场语境;⑤执行提示展示 + 决策日志⑨项 `maxChasePct` 二选一强制(`APIClient` 请求体改手写 `encode(to:)` 令该键永远出现,nil→JSON null,与项目内其余「nil→省略键」字段刻意不同);⑥定格日晚点标注 + 周复盘 `CharterVersionCard`(`ReviewWeeklyResult` 因 `reviews.result_json` 写入时冻住历史快照而补手写容错解码)+ 消息面 `rotationGroup`/`codesRotationDeferred` 独立展示(顺带补齐客户端此前漏解码的 `codesNoSearch`);⑦决策卡新增挂单追踪走势(Swift Charts 折线)+ 问询历史列表/详情页。语义红线落地(候选区头部文案 + 全仓「推荐买入/建议买入/看好/值得买/推荐买点」grep 零命中)。`mapReason` 新增 3 case:`reportNotFound`/`codeNotInReport`/`maxChaseRequired`(`decisions/track`、`inquiries/{id}` 两处 404 复用既有 `notFound`,未新增,同 plan 字面)。⑤「候选专属信息卡入口」假设已核对成立(全仓唯一入口 = 候选卡按钮,恒持有 `Candidate` 对象,未给 `InfoCardOut` 加字段)。`pbxproj` 随本块一并提交(P4-21 结案,唯一被授权本块)。双端 `xcodebuild build` SUCCEEDED;iOS Simulator `xcodebuild test` 165 跑 152 过 13 skip 0 failure(基线 139,新增 26 测试;`httpBodyOrStream()` 由 `private` 放宽 `internal` 供跨测试文件复用同一 helper);Python 全量 1647+2 不变(本块零服务端改动)。视觉核对:本地隔离库(ATTACH 真实参考表+真实 parquet)出真报告 + 真持仓,`xcrun simctl` 截图拿到停牌持仓卡完整证据;候选卡/信息卡因 iPhone 视口纵向溢出且本环境无可用触屏注入(`Claude_Code_iOS_Simulator__control` 缺 Xcode-select 配置报错、通用 computer-use 同样点不动 Simulator),改用临时 iPad Pro 13" 模拟器(更大点尺寸)拍到候选卡排序理由+执行提示+信息卡入口完整证据,并新增 `NECKLINE_INITIAL_INFOCARD_CODE` QA 钩子(同 `NECKLINE_INITIAL_TAB` 先例)拍到信息卡 K线/RS线真实渲染;决策日志⑨项因表单靠后位置同一滚动限制未能截图,改由 6 个专项单测兜底覆盖,详见 §四

- 2026-07-29 · v1.4-⑦ 功能补全完工(@builder,纯本地,零生产访问/零部署/零迁移写生产):⑦-A 新端点 `GET /decisions/{id}/track`(读专用 `report/pending_track.py::load_track_rows` 装配,不重算;404 仅 `decision_id` 不存在,「未攒到追踪数据」是合法 200 空态,两种「空」分开;`reason="not_found"` 复用客户端既有 case,未新增)。⑦-B 新表 `inquiry_log`(纯追加档案非队列,`CREATE TABLE IF NOT EXISTS` 幂等,不套 `inquiry_pool` 的「审计时间戳+消费标记」两字段模式)+ `run_inquiry` 结尾旁路写入(try/except,失败只使 `inquiryId` 退化 `None`、不打断回答)+ `GET /inquiries`(分页+`tsCode`过滤,倒序)/`GET /inquiries/{id}`;`POST /inquiry` 响应加 `inquiryId:Int?`。⑦-C `InquiryVerdict` 由已退役二值裁决(`.reject`/`.pass`)改描述性标注(`.analyzed`/`.analyzedWarn`,原样对齐后端字面值),顺带修掉「已分析·有风险提示」误落中性色的展示 bug(改警示色,复用既有 `NKAxisTone.warn`,未新造色值)+ 清理连带失真的 `AppModelTests`/`DTODecodeTests`/`IntegrationSmokeTests`;`NKAxisTone` 补 `Equatable`。Python 全量测试 1647 过 + 2 skip(基线 1627+2,新增 20);Swift 双端 `xcodebuild BUILD SUCCEEDED`,iOS Simulator `xcodebuild test` 139 跑 126 过 13 skip 0 failure(macOS 侧 `xcodebuild test` 因 `TEST_HOST` 既有工程配置只适配 iOS 布局而跑不了,与本块改动无关,项目历来只在 iOS Simulator 跑单测)。**一处已知偏差**:plan §五-⑦ 验收原文要「警示色生效(截图)」,本次按任务简报「双端 BUILD SUCCEEDED 即可,截图非必需」执行未拍照(靠两条 Swift 精确断言 `.tone==.warn` 兜底;拍照需切后端环境 picker 离开默认 `prod`,与「零生产访问」边界不必要地贴近)
- 2026-07-29 · 🔴 v1.4-⑥ 判定精度完工(@builder-pro,纯本地,零生产访问/零部署/零迁移/未动 `activated_at`):⑥-A 周复盘章程**按成交时刻逐笔判**(新 `brain.config_governing_at`〔判据「激活时刻 ≤ 成交时刻」= 恰好等于算新章程〕+ `brain.activations_between`;`review/reconcile.py` 四条判据各锚各的时刻——单笔上限/禁买锚买入、止损锚卖出〔与哨兵读现役 stop_pct 同源〕、并发/敞口按日粒度切段、冷却锚再次买入;时区归一新增 `neckline.calendar.CN_TZ`/`MARKET_CLOSE_TIME` 单一源、`intraday._CLOSE` 改引用,`activated_at` naive 按 UTC 读 vs 入参 naive 按北京时间读**两向约定刻意相反并各自定死**;`RawTrade.trade_time` 可选,只有日期时按该日收盘取章程;周报 `charter_segments`/`charter_switches` 分段计数 + 切换文案进 material,`strategy_version` 收窄为周初标签)+ **老新实现对拍**:非切换周逐位一致、切换周按预期修掉生产真实形态(激活北京 14:36)下的 2 条假警报,golden 已写进永久单测;⑥-B 消息面预算取**自选隔日轮扫**分支(**限频未实测**——本地无任何可用 GLM key,按"不许拍脑袋开并发"取保守分支,并发路留待有 key 时评估;持仓每日必扫 + `zlib.crc32` 稳定哈希分两组 × `toordinal()` 奇偶交替〔纯函数、历史回放可复现〕+ 减持类不参与轮扫 + `rotationGroup`/`codesRotationDeferred` 与既有三个计数**语义不合并**;预算维持 300s 不抬,不给 ⑨-F 内存账加压);⑥-C `PositionOut.timeExitLockedDay`/`timeExitLockedLateDays`(纯派生展示位,未定格返 null 不冒充,**判向逐位不变有单测**,与 ①-B 停牌复牌晚定格共存);全量测试 1627 过 + 2 skip(基线 1578+2,新增 49);⑥-B 顺带补掉 `newsAlertsScan` 强类型模型丢弃未声明字段的老坑(schemas + `_shape_report` 两处必须同步补,同 `codesSkipped`/`codesNoSearch` 先例)。⚠ **一处 plan 理由与现实不符已记录**:plan 说"章程激活都在盘后跑"故当日成交按收盘取章程,但真实 v1.3.3 激活在北京 14:36 盘中——**规则照 plan 不改**,代价(激活当日激活前的成交偏判新章程)写进 `trade_instant` docstring
- 2026-07-29 · v1.4-④ 信息卡与考卷同构完工(@builder,纯本地,零生产访问/零部署):新模块 `report/info_card.py` 组装九件套(K 线 60 日/RS 线基准 `SSE_INDEX`/行业分歧线「成员中位数合成」/快照七项/红黄牌/温和带 `[2%,3%]`/消息面/龙虎榜近5日/市场语境),两条消费路径刻意分离(`build_info_card` 单只按需服务端现算供新端点、`attach_info_card_summaries` 批量摘要零额外 parquet 读取直接吃 `Candidate.raw`/`intel_rank` 供列表页);新端点 `GET /report/{date}/info-card/{code}`(`code` 经 `normalize_ts_code` 归一;404 两 reason `report_not_found`/`code_not_in_report`);`CandidateOut.infoCard` 摘要位(不含 60 日序列,老快照 `None` 不冒充"确认无内容");`industry_strength.py` 新增 `industry_median_return_series`(分歧线合成源,`_load_ret1d_panel` 加可选 `start` 窗口参数,**不受** `_MIN_MEMBERS` 排名门槛约束)+ `holding_k4_check.py` 新增 `describe_hits`(红黄牌 decorate,不重判,复用 `_HIT_META`/DB evidence);行业未达标(成员<5)如实标"分歧线缺省"、消息面扫描域仍仅持仓+自选(候选不在内如实标注,不冒充"扫了没有")、龙虎榜回看"查到几天算几天"不为凑齐而回补历史;零 DB 迁移(`info_card_summary` 只是 `candidates_json` 里的新 key);端到端用本地真实数据(隔离库四张只读参考表 ATTACH `data/neckline.db` + 真 parquet,2026-07-24)验证九件套齐全(K线60根/RS线60点/行业分歧线可用标"半导体"/K4黄牌真命中 `B2_dual_golden_cross`),经真实 FastAPI TestClient 打端点验证往返;测试隔离踩坑一次并已解决并入 CLAUDE.md(`neckline.db` 的 `settings` 绑定不在 `isolated_env` 覆盖范围,domain 函数必须显式传 `db_path`);全量测试 1528 过(基线 1472+56)
- 2026-07-28 · v1.4-③ 正选漏斗排序键改版完工(@builder,纯本地,零生产访问/零部署):`intel_candidates.py` 排序键换三级(`industry_rank ASC → industry_persist_days ASC → yellow_card_count ASC`,base_score/code 仅确定性兜底)+ `sector_flow` 退为并列展示 + `_SORT_KEY_INPUTS` 白名单单测锁死;`IntelRankOut` 补 `industryRank`/`industryPersistDays`/`yellowCardCount` 三字段(老客户端兼容);`render.py` 语义红线落文案;③-G 新旧排序对照报告 `archive/v1.4_排序键切换对照_20260724.md`(20只候选14进14出6变位0不变,A2拦截率变化系②已完工效应、与③无关,报告已单独澄清);全量测试 1472 过(基线1465+7)
- 2026-07-28 · v1.4-② 行业强度单一源完工(@builder,纯本地,零生产访问/零部署):新模块 `report/industry_strength.py`(与 `research/k4p_h6_theme.py` 逐位对拍单测锁死)+ A2/B3 判据三处调用点(`holding_k4_check.py`/`intel_candidates.py`/`api/inquiry.py`)+ `pipeline.py` 接线回归规格档,概念板块 `board_age` 降级为纯展示;切换对拍报告 `archive/v1.4_A2B3口径切换对拍_20260724.md`(A2 命中 375→11,旧口径 76% 命中撞在 `max()`-over-多板块的封顶假象);全量测试 1465 过(基线 1450+15)
- 2026-07-29 · v1.4-⑤ exec_hint 产品化 + 决策日志第⑨项完工(@builder,纯本地,零生产访问/零部署):新模块 `report/exec_hint.py`(C1强票市价/C2温和带〔复用④ `info_card.MILD_BAND_RANGE`不重开〕/C3挂低单自觉〔与⑤-B联动,查该票 `trade_date` 当天或之前最近一条 `decision_log`〕/C4回调大红机械层不做 四码,文字一律读 DB `k4_advisory.exec_hint`〔扁平 `{code:text}`,2026-07-29 真库探活确认与 `hard_cut`/`avoid_flag` 嵌套形状不同〕、四码可部分命中部分兜底、镜像阈值模块头对照表同 `holding_k4_check.py` 体例、C4 与 B4 数值巧合相同但概念独立声明不复用私有 `_hit_B4`);`attach_exec_hints` 零额外 parquet 读取(C1/C2/C4 直接读 `Candidate.raw`)+ **按 `trade_date` 截断 `decision_log` 查询防前视偏差**(历史回放不得读到该历史日之后才创建的决策,新入 CLAUDE.md 定案);`decision_log.max_chase_pct` 幂等迁移(REAL 可空,`3.0`=昨收+3%、允许负值、`NULL`=显式不设上限)+ HTTP 层 `model_fields_set` 探测 JSON 键是否存在(缺键 400 `reason=max_chase_required`、显式 `null` 合法,create/revise 两端点同纪律)+ 与 `planned_price` 语义分离(docstring + 单测各写各的,新入 CLAUDE.md 定案);契约 `CandidateOut.execHints`/`DecisionOut.maxChasePct` 往返(仅 `CandidateOut` 载 execHints,`InfoCardOut` 不重复开字段,信息卡页复用候选卡已取数据,按 plan 客户端契约清单字面);复用 ③ 白名单单测模式新增一条「exec_hint 相关量不进 `_sort_key`」的锁;全量测试 1578 过(基线 1528+50)。**规格与任务简报有一处措辞张力已按 PROJECT_PLAN 原文实现并在完工报告显式记录**:简报口径说 `max_chase_pct` "可选"且"老客户端不传照常工作",但 PROJECT_PLAN §五-⑤-B 原文明确要求 HTTP 层"缺键 400"——两者对 `POST /decisions` 而言互斥,已按 PROJECT_PLAN 原文实现(此端点其余 4 项〔why_buy 等〕本就一直硬性必填、从无"老客户端选择性传参"先例,且生产部署要到 ⑨ 才发生、彼时 ⑧ 客户端已同步跟上,故两者实际不冲突)
- 2026-07-28 · 🔴 **v1.4.0-p1 计划外小步上云**(@builder-pro + 用户拍板,🔴 高危区,未回滚):只上第 ① 块 P0 地基,理由 = `002036.SZ` 停牌票假警报有硬时限(0729 09:26 误推 / 16:35 永久错误定格),等第 ⑨ 块来不及;`sync_code.sh` 属主自检绿 + API 重启 active + `/health`=v1.4.0-p1 + 两处幂等迁移落地 + `integrity=ok`;补 `suspend_d` 5 个交易日分区 + 回填 `data_unavailable` 6 行(**部署时实测:不回填的话盘前那一拍照推假警报**);干跑验收 0729/0730 09:26 推送清单为空、0731 另两只照常判该走。**第 ⑨ 块仍是最终完整部署**(届时 VERSION 改回 v1.4.0)
- 2026-07-28 · 🔴 生产三笔持仓买入日纠正完工(@builder-pro,🔴 碰持仓判定 + 生产数据):`002036.SZ` 20260727→20260722(唯一实改,另两笔本就正确、幂等跳过);`.backup`+`cp -p` 双备份 + `integrity_check=ok` + 逐行对拍「其余行零改动」+ 定格三列改前即全 NULL 无需清;⚠ 遗留风险 = 代码未上云前 **0729 09:26 会推假警报、16:35 会永久错误定格**,处置与时限见 §四 第一条
- 2026-07-28 · ✅ v1.3.5 上云后**首次自动跑报告**验收通过(16:35:01→16:53:24,`ExecMainStatus=0`,报告真生成 / 审判 10 条 0 降级 10/10 有搜索命中;内存峰值顶满 800M 归 P4-16→⑨-F);验收判据收严:`Result=success` 会被 `reset-failed` 抹掉,以 `ExecMainStatus` + 时间戳为准(已入 CLAUDE.md)
- 2026-07-28 · 🔴 v1.4-① P0 地基完工(本地,未部署)· ①-A 补录买入日契约 `buyDate` + 两个 400 reason + 客户端 mapReason;①-B 停牌口径(`suspend_d` 探活可用 / `002036.SZ` 自 0723 真停牌 / `priceStale` + 第五态 `suspended_hold` + K4 `dataUnavailable` + §2.1 附注);①-C 概念板块日更(`ths_daily` 尾窗重拉 + 概念过滤 + `ths_index`/`ths_member` 周更带 .bak)+ `suspend_d` 日更 + 板块数据过期告警 `dataFreshness`(@builder-pro,全量测试 1449 过;代码上云归第 ⑨ 块)
- 2026-07-28 · **v1.4.0 立项**(施工图九块就位待 builder)· K4 打法产品化〔方案 b〕= P0 三洞地基 + 行业强度单一源 + 正选漏斗排序键 + 信息卡考卷同构 + exec_hint 与追价上限 + 判定精度 + 功能补全 + 双端 + 部署;v1.3 施工图归档 `archive/v1.3_施工图_20260728归档.md`(@planner,只改文档,零代码改动)
- 2026-07-28 · 档案室搬家:PLAN 瘦身(§四快照制 / §五旧+§五附归档 / §九一行制)+ CLAUDE.md 修剪 + scripts/oneoff 归位 + 记录纪律立规(纯文档与文件移动,零代码逻辑改动)
- 2026-07-28 · 会话交接:§七 Backlog 重整 + §四 交接条目(@planner,只改本文件,零代码改动)
- 2026-07-28 · v1.3.5 报告管线抗毁快修 + 2026-07-27 报告补跑(@builder-pro + 用户授权,未回滚)
- 2026-07-27 · v1.3.4 问询台联网搜索修复快修完工 + 上云 + macOS 换包(@builder-pro + 用户授权,未回滚)
- 2026-07-27 · 🔴 v1.3.3「拆墙」快修完工 + 上云 + 章程生产激活(@builder-pro + 用户三条明确授权,🔴 碰纪律章程 + 生产数据 + 护栏本身,未回滚)
- 2026-07-27 · 🔴 v1.3 章程在生产激活(@builder-pro + 用户明确授权,staged 步骤 2 = 用户纪律判定标准真正改变,未回滚)
- 2026-07-27 · v1.3.1 部署上云完工(@builder-pro + 用户授权,🔴 高危区,已部署、章程仍未激活、未回滚)
- 2026-07-27 · 独立审计(纪律章程与金额判定)缺陷修复完工(@builder-pro,~~未部署~~ **已随 v1.3.1 上云**,🔴 高危区,未激活任何章程行)
- 2026-07-26 · K4 机械半身行 bootstrap 到生产完工(@builder-pro + 用户,🔴 碰生产权威库,只增一行 inert 数据,未激活/未改现役配置/未重启)
- 2026-07-26 · v1.3-⑦ 部署上云完工(@builder-pro + 用户,🔴 最高危块,已部署、未激活章程,未回滚)
- 2026-07-26 · v1.3-⑥ 客户端跟进(双端)+ 三处(实为四处)后端补齐完工(@builder,未部署,v1.3 最后一块,只剩 ⑦ 部署)
- 2026-07-26 · v1.3-④ 挂单未成交追踪 + v1.3-⑤ 问询台选股域漂移清理完工(@builder,未部署)
- 2026-07-26 · v1.3-③-C3 候选排序两次追加(常驻保底 2 只 + 行业闸)完工(@builder-pro,未部署;仅 `report/intel_candidates.py` + 其测试 + 本节)
- 2026-07-26 · v1.3-③-C4 验收后必改两项完工(@builder,未部署)
- 2026-07-26 · v1.3-③-C4 晚间消息面公告扫描(持仓+自选:减持/立案/暴雷/监管)完工(@builder,未部署)
- 2026-07-26 · v1.3-③-C3 候选池情报筛选管线改版(K1 选股逻辑退役)完工(@builder-pro,未部署)
- 2026-07-26 · v1.3-③-C1/C2 情报官(复盘情报件 + 板块资金流展示)完工(@builder,未部署)
- 2026-07-26 · v1.3-② 持仓管理层(K4 牌每日对持仓重算)完工(@builder-pro,🔴 推送路由 + 金额判定,未部署/未激活)
- 2026-07-26 · v1.3-① 退出规则章程变更完工(@builder-pro,🔴 碰纪律章程,未部署/未激活)
- 2026-07-26 · v1.3 立项(施工图就位待 builder)· v1.2 合并发布 + 退出规则改革 + 持仓管理层 + 情报官候选管线改版
- 2026-07-25 · v1.2-E 客户端双端(SwiftUI iOS+macOS)完工
- 2026-07-25 · v1.2-G 呼吸试验仓台账后端完工
- 2026-07-25 · v1.2-A2 熔断纪律后端完工(🔴 碰纪律章程 + 金额判定 + 第五类推送)
- 2026-07-25 · v1.2-B 预注册决策日志(八项)后端完工
- 2026-07-25(增补)· v1.2 范围扩大 + 切两波(策略线三版补丁交接)
- 2026-07-25 · v1.2 立项(施工中)· 人机协作配套:三仓章程 + 预注册决策日志 + 归因闭环
- 2026-07-25 · K3 · B2 四臂组合级回测完工 → ★中心假设否决(诚实否定合格)
- 2026-07-24 · K3 · B0 数据修缮 + B1 超跌事件研究完工 → ⏸ B1 用户检查点
- 2026-07-24 · K3 策略研究立项(纯研究,不动生产 / 不动纪律章程)
- 2026-07-22 · K2 策略研究(B1–B6)完工 · 中心命题否决 · K2 落库不激活(纯研究,生产零改动)
- 2026-07-19 · 立项(v0.1)
- 2026-07-19 · 立项后确认(v0.1.1)
- 2026-07-19 · 阶段 0 完工(数据层 + 回测引擎骨架)
- 2026-07-20 · 阶段 1 完工(策略研究 P1–P10)
- 2026-07-20 · 阶段1收口拍板(v0.2)
- 2026-07-20 · 阶段2完工(盘后报告管线,§2.1–2.6)
- 2026-07-20 · 阶段3完工(盘中哨兵,§2.4)
- 2026-07-20 · 阶段 4 立项(v0.3,客户端 + 云端化 + 问询台 + 周复盘)
- 2026-07-20 · 阶段 4A 完工(后端 API 服务化)
- 2026-07-20 · 阶段 4B 完工(云端化 + hz ECS 部署,方案 A)
- 2026-07-20 · 阶段4C完工(SwiftUI iOS+macOS 双端客户端)
- 2026-07-20 · 阶段4D完工(周复盘工作台·对账引擎)
- 2026-07-20 · 阶段 4E 部分完工(LinoN 接班切换 + 收官接线)
- 2026-07-21 · v1 上线 + 首日生产坑快修
- 2026-07-21 · v1.1 立项(SOP 补洞)
- 2026-07-21 · v1.1-C/D 后端完工
- 2026-07-21 · v1.1-H 服务端部署上云(🔴 @builder-pro)
- 2026-07-22 · 版本号双线规约
- 2026-07-22 · v1.1-H2 退潮哨兵双级制重构(🔴 高危快修 + 上云)
- 2026-07-22 · v1.1.1(退潮哨兵双级制)+ 版号规约收严
- 2026-07-22 · K2 策略研究立项(纯研究,不动生产 / 不动纪律章程)
- 2026-07-25 · 架构拆分(双权威文件)
- 2026-07-25 · v1.2-0 完工:`sync_code.sh` 部署 footgun 修复(@builder)
- 2026-07-25 · v1.2-A 完工(staged 步骤 1):仓位章程三仓制落库 + 历史洗白修复(@builder-pro,🔴 碰纪律章程 + 大脑激活)
- 2026-07-27 · 行业闸判据缺陷修复:share→lift(@builder,仅 `report/intel_candidates.py` + 其测试 + 本节)
