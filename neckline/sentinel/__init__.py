"""盘中哨兵(plan 阶段 3 / §2.4)。轮询新浪/腾讯免费实时源(1 分钟一拍),四哨兵——
买点(entry)、退潮(retreat)、持仓(holding)、证伪(invalidation)——只执行前晚
盘后报告(阶段 2)写死的计划,永不在盘中产生新决策、永不推荐新票。

铁律(§2.4,凌驾本包一切实现细节):
    1. 买点哨兵:候选触达预设买点且确认条件成立(量能折算、站稳 VWAP)才推送。
    2. 退潮哨兵:盘中情绪恶化 → 推「今日计划作废、禁开新仓」红色刹车,并联动
       抑制买点哨兵(退潮触发当日不再推新的买点确认)。
    3. 持仓哨兵:持仓票放量跳水逼近止损线 / 触达回落止盈区 / 所属板块跳水预警。
    4. 证伪哨兵:候选命中前晚写死的证伪条件(只用价量结构,不看资金面)→ 推
       「剔除勿进」。

模块地图:
    · `quotes.py`      —— 新浪主/腾讯备实时源,批量拉取 + 解析 + 归一 Quote。
    · `intraday.py`    —— VWAP / 已开盘时长 / 量能折算 / 盘中时段判定,纯函数。
    · `universe.py`    —— 组装当日「关注池」(候选 + 持仓 + 昨日涨停股),供批量拉价。
    · `entry.py` / `invalidation.py` / `holding.py` / `retreat.py` —— 四哨兵判定,纯函数。
    · `positions.py`   —— 极简持仓台账(SQLite `positions` 表)CRUD。
    · `dedup.py`       —— 事件防重台账(SQLite `sentinel_events` 表)。
    · `channels.py`    —— 推送通道抽象:控制台(默认)/ Bark(§3.5,阶段4 前先备着)。
    · `engine.py`      —— 单拍编排:取关注池 → 拉价 → 四哨兵判定 → 防重 → 推送。

工程原则(继承全项目铁律):全部判定逻辑纯函数化 + 依赖注入(transport/db_path/
parquet_dir 均可覆盖),便于喂假行情单测,不联网也能跑通全部判定分支。
"""
