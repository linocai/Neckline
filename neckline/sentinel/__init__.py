"""盘中哨兵(plan 阶段 3 / §2.4 + V2 §五 ⑧⑪⑬)。轮询新浪/腾讯免费实时源(1 分钟
一拍),只执行**前晚盘后已经写死的计划**,永不在盘中产生新决策、永不推荐新票。

⚠ **V2-⑬-1 换血(2026-08-03;本模块头 2026-08-04 按契约线审计 🔵 B8 校正)**:V1 的
「四哨兵」里的**买点哨兵 `entry.py` 已随单票候选榜整链物理删除**,不要再照旧文找它。
现役判定文件与它们**问的问题**:

铁律(§2.4,凌驾本包一切实现细节):
    1. 退潮哨兵:盘中情绪恶化 → 推「今日计划作废、禁开新仓」红色刹车(纪律层动作)。
       ⚠ 主线样本取 ④ 扫描层机械种子派生(⑧-F),**不取 LLM 挑的篮子成员**。
    2. 持仓哨兵:持仓票放量跳水逼近止损线 / 触达回落止盈区 / 所属板块跳水预警。
    3. 证伪哨兵:**T1/T2 篮子成员**命中前晚写死的证伪条件(只用价量结构,不看资金面)
       → 推「剔除勿进」。⑬-1 前判的是「昨晚 20 只候选」,判定逻辑一行未改(那三个
       阈值本就是零入参全局常量),换的只是判定对象。
    4. 熔断与自定义提醒(⑪):用户自己下的确定性规则,**永不自动交易**。

模块地图:
    · `quotes.py`      —— 新浪主/腾讯备实时源,批量拉取 + 解析 + 归一 Quote。
    · `intraday.py`    —— VWAP / 已开盘时长 / 量能折算 / 盘中时段判定,纯函数。
    · `universe.py`    —— 组装当日「关注池」(持仓 + T1/T2 篮子成员 + 板块基准指数 +
                          昨日涨停,**代理样本不是全市场**,见 CLAUDE.md)。
    · `invalidation.py` / `holding.py` / `retreat.py` / `custom.py` —— 判定,纯函数。
    · `mainline.py`    —— ⑧-F/G 主线样本派生(机械种子 ∩ 池 + crc32 配额切片)。
    · `precall.py`     —— 9:20–9:30 盘前校准(篮子竞价剧本核对 + D5 时间退出汇总)。
    · `basket_verify.py` / `basket_verify_store.py` —— ⑦-b/⑧ 篮子验证四态状态机。
      ⛔ **`falsified` 不接任何持仓动作、不进推送**(与本包其余判定分属两个问题)。
    · `capture.py`     —— ⑧-B 盘中存拍**旁路**(内存累计 + 15:05 一次落盘)。
    · `positions.py`   —— 极简持仓台账(SQLite `positions` 表)CRUD。
    · `dedup.py`       —— 事件防重台账(SQLite `sentinel_events` 表)。
    · `channels.py`    —— 推送通道抽象:控制台(默认)/ Bark(§3.5)。
    · `circuit.py`     —— 熔断(⑪-A),`attention.py` —— 关注度/来源篮子归因。
    · `engine.py`      —— 单拍编排:取关注池 → 拉价 → 判定 → 防重 → 推送。

工程原则(继承全项目铁律):全部判定逻辑纯函数化 + 依赖注入(transport/db_path/
parquet_dir 均可覆盖),便于喂假行情单测,不联网也能跑通全部判定分支。
"""
