# Neckline 周度作业(plan §五 V2.2-④-E,承 V2.1-⑥ 逐字继承;§七 P3-42 在 V2.2 结案)。
#
# 跑 `scripts/weekly.py` 三件:① 画像批算落表 ② 周度校准报告落盘(自带 V2.2-④ 的双时钟
# 成绩单与四分类建议)③ 交易时钟对账。**对账(券商交割单)仍等用户上传**——它的必需输入
# 只能由用户手动给(§八 第 7 项),排 timer 也补不出没上传的那一份。
#
# 🔴 **⛔ 永不加 `RemainAfterExit=yes`**(§七 **P0-45** 连带铁律):加了它,`Type=oneshot`
#    跑完不落回 inactive,timer 就永远算不出 NEXT —— **首次必成、之后必哑**,而且是
#    **静默**的。`tests/test_scripts_weekly.py` 把这条钉成机器判据(本文件出现该字样即红)。
#
# 🔴 **由 timer 直接触发,⛔ 不经 `.target`**(同上 P0-45):`.target` 没有"跑完"的概念、
#    不会自己落下。周度只有三步、本就不需要 target 的编排能力,没有理由去碰那个坑。
#
# ⛔ **不推送**:本作业不新增任何推送 kind → 不触发「新增推送须用户拍板」这条纪律。
#
# **退出码语义**:三步各自 try/except、一步失败另一步照跑;**任一步失败 → exit 1**。
# 验收看 `ExecMainStatus=0` **且** `ExecMainStartTimestamp` 是本次那一跑
# (§铁律:`Result=success` 不够 —— `reset-failed` 会把 Result 抹回 success)。
#
# ✅ **`TimeoutStartSec` / `MemoryMax` 已生产隔离实测校准(2026-08-11)** —— 占位值销案。
#
#    **实测条件**:nk 上 `systemd-run --unit=… --property=User=neckline
#    --property=Group=neckline --property=MemoryMax=…` 瞬态 service(⛔ 未用 root 的
#    `--scope` —— 那会把行情文件写成 root 属主);窗口 `--week 20260808`
#    (= 20260803→20260807,4 个交易日 / 22 篮),与周六首跑逐位相同。
#
#    **读数**:
#      · **墙钟 14.28s**(14:14:42.92 → 14:14:57.20),CPU 39.2s;安慰剂对照臂**真跑满**
#        (1 层 / 4 日 / `draws=200`),`degraded=[]`、`ExecMainStatus=0`。
#      · **内存**:`MemoryPeak` 读数**不可信**(cgroup 对已在页缓存的文件页不重复计费,
#        本次直接 `[not set]`)→ 按纪律**压低 `MemoryMax` 反证**:
#        **400M 扛住**(`Result=success`)/ **256M 被 OOM-kill**(`Result=oom-kill`,
#        status 9)→ 真峰值落在 **(256M, 400M]**。
#
#    **⚠ 这个墙钟是下界,不是上界**:实测那一跑 `member_unfinished=49` —— 前向窗口
#    (地平线 15 个交易日)还没走完,`_sim_one_h` 的内层只走了 4~6 天就到数据末端。
#    等窗口成熟,内层要走满 15 天,迭代数约 3~4 倍;再叠加更忙的一周(篮子数翻倍)
#    → 估计上界约 100~150s。**下面两个数按这个估计留余量,不是照 14s 卡着填的。**
#
#    **选值理由**:
#      · `TimeoutStartSec=900`(15 分钟)≈ 估计上界的 6~9 倍。⛔ 不再用 3600 ——
#        那是占位值,一个小时的超时等于"实际上没有超时",真挂死也要等一小时才发现。
#      · `MemoryMax=800M` = **实测扛住值 400M 的 2 倍**。⛔ 不填 400M(那是边缘,
#        换一周数据就可能 OOM);⛔ 也不再用 1400M(比实测需要多 3.5 倍,
#        真泄漏时兜不住)。nk 是 3.8 GiB,800M 占比安全。
#
#    🔴 **改这两个数必须重新实测**(同一条 P0-23 纪律),⛔ 别凭感觉调。
#
# 安装:
#   sudo cp /opt/neckline/deploy/neckline-weekly.{service,timer} /etc/systemd/system/
#   sudo systemctl daemon-reload && sudo systemctl enable --now neckline-weekly.timer

[Unit]
Description=Neckline weekly job: profile batch + calibration artifact + dual-clock attribution
Documentation=file:///opt/neckline/PROJECT_PLAN.md
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=neckline
Group=neckline
WorkingDirectory=/opt/neckline
EnvironmentFile=/opt/neckline/.env
ExecStart=/opt/neckline/.venv/bin/python scripts/weekly.py
# ✅ 2026-08-11 生产隔离实测校准值(理由与读数见文件头,⛔ 改它必须重新实测)。
TimeoutStartSec=900
MemoryMax=800M
