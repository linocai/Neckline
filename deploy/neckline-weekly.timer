# Neckline 周度作业定时(plan §五 V2.2-④-E,承 V2.1-⑥ 逐字继承)。
#
# 🔴 **直接触发单个 `oneshot` service,⛔ 不用 `.target`**(§七 **P0-45**):`.target`
#    没有"跑完"的概念、不会自己落下 → timer 卡在 `SubState=running`、NEXT 永不重算 →
#    **首次必成、之后必哑**。本项目已验证的安全形态是 `neckline-daily.timer` 那条
#    (直触 service)。周度只有三步、不需要 target 的编排能力。
#    `tests/test_scripts_weekly.py` 把「本文件不含 `Unit=…target`」钉成机器判据。
#
# ⚠ **`Persistent=true`,与 evening 的 `false` 刻意不同**(V2.1-⑥ 定的,理由写在这儿):
#    周频作业漏一次就是**一周**;而它**幂等、不推送、不写业务判定表**(画像按 `as_of`
#    覆盖、校准按窗口覆盖文件、时钟对账以 `positions` 为真相),补跑无害。
#    evening 会推 APNs,所以那条**不许**补跑 —— 两者的差别是"补跑会不会打扰用户"。
#
# ⚠ **09:00 这个点位**:周六非交易日、与任何行情作业不冲突;`scripts/weekly.py` 默认
#    跑**上一个完整自然周**(周六早上「本周」的周五刚收盘,取上一周才是走完的窗口)。
#    ⚠ 部署 / 重启窗口要**避开周六 09:00**(§五 〇b 铁律 4 已写死)。
#
# 🔴 **验收必须验第二次**(P0-45 通用教训:单次跑通证明不了排程还活着):
#    首跑后 `systemctl show neckline-weekly.timer -p SubState -p NextElapseUSecRealtime`
#    必须是 **`waiting` + 有值**;再用 drop-in 把 `ExecStart` 临时换 `/bin/true`、
#    `OnCalendar` 临时改成分钟级,**观测连续两次触发**后删 drop-in 复原
#    (⛔ 别用真跑验第二次)。
#
# ⚠ **在已卡住的机器上首次加 timer:先 `stop` 再改**(P0-45 次生事故):`daemon-reload`
#    异步落下 unit → timer 以过期的 `LastTriggerUSec` 为基点算日历槽 → 算出**已过去的**
#    槽 → **当场补跑一遍**。`Persistent=false` 拦不住(它只管重启后补)。
#
# 安装见 neckline-weekly.service 头部。

[Unit]
Description=Run Neckline weekly job at 09:00 on Saturdays

[Timer]
OnCalendar=Sat 09:00 Asia/Shanghai
Persistent=true
Unit=neckline-weekly.service

[Install]
WantedBy=timers.target
