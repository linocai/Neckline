# K4 前置 · 用户漏斗逐层审计(事件研究档)— 预注册 + 结果报告

> **性质**:K4 前置研究,梯子便宜档(事件研究),审计**用户人肉漏斗 v2**(定稿见
> `STRATEGY_LAB.md` §四)中三个可测层的真实贡献。**不是策略候选回测**,不落
> `strategy_versions`,不碰生产。新证据条款:三场战役测的是机器发明的漏斗,
> 用户真实漏斗从未被测过。
> **预注册纪律**:本节定义清单先 commit(git 时间戳为证)再跑数;定义一经预
> 注册不因结果回改;敏感性只在 ±1 格内;三假设一次战役,不加塞第四个。
> **判决姿势(承 K3)**:均值口径测不到左尾——每个假设同时报 期望 + 胜率 +
> 左尾 p5/p10 + 次日跌停暴露;净收益扣双边成本(`DEFAULT_COST_ONESIDE=0.0015`);
> **2026 分段单列**(生存视角);诚实否定是合格交付。

---

## 0. 预注册:域、窗口、口径(2026-07-25,先于一切结果)

- **主判决域**:`base_universe_expr()` 基础域(非ST / 非北交所 / close≥2 /
  amount_ma20≥2000万 / ma20 非空)+ 剔次新(`days_since_listing ≥ 120`),
  **全板块(主板 + 创业板 + 科创板)**。理由:用户漏斗实际操作域含高弹板块,
  审计必须贴用户实操——与 K1 的主板 only **刻意不同**,此差异写明不回避。
- **五板块近似域(仅 secondary 分层,不作主判决)**:科技/药/电/机器人/材料
  按概念板块关键词映射成分并集;当前快照回溯存在成分回填偏差 + `ths_member`
  已知成分洞(K2 教训),故只作参考分层,报告须标注。若成分数据不可用或洞
  过大,此分层降级为"按 board 板块分层",不影响主判决。
- **窗口**:2020-01-01 ~ 面板最新交易日(报告写明实际截止日);**2026 分段
  单列**;年份 / 市场状态(`sse_above_ma`)/ 板块分层照 K3 姿势。
- **前向口径**:复用面板 `fwd_ret_1..5` 既有约定(施工前先读
  `neckline/strategy/features.py` 确认到底是 T+1 开盘买入口径还是收盘口径,
  **报告 §0 结果节如实记录确认到的口径**);`fwd_buyable` 过滤次日一字涨停
  买不进;停牌行随面板既有处理。**任何两组对比必须同口径同持有窗**。
- **可交易性**:事件日本身为一字涨停/跌停的行剔除(用 `is_limit_up`/
  `is_limit_down` + 涨跌停价复用 `limit_derived`,不重推幅度规则)。
- **工程边界**:runner 全在 `research/`(命名 `k4p_*.py`);新特征列只进研究
  面板缓存(承 `research/k3_panel.py` 姿势,不改生产列语义);生产零改动,
  不碰 `MomentumConfig`,无需护栏单测。

## 1. 预注册假设 H1:量能堆积判主升 vs 突然启动判轮动(启动性质判别层)

- **来源**:用户研习的短线战法总结 §六(`/Users/linotsai/Lino/whynotme/总结.md`):
  "持续的量能堆积更可能对应主升;突然启动、缺乏持续资金进入的更可能只是轮动"。
- **事件(启动日 D)**:`ret_1d ≥ 5%` 且 `vol > vol_ma20 × 1.5`(放量上行日),
  域内,当日非一字板。粗网格:涨幅阈值 {4%, 5%, 6%} × 量能倍数 {1.5, 2.0}。
- **分组**(需新特征 `vol_above_ma20_cnt3` = 前 3 个交易日中 `vol > vol_ma20`
  的天数,per ts_code rolling,shift(1) 防未来函数):
  - **堆积组**:启动日前 3 日中 ≥2 日放量(`vol_above_ma20_cnt3 ≥ 2`);
  - **突拉组**:前 3 日全部平量(`vol_above_ma20_cnt3 == 0`);
  - 中间态(=1)单列不参与主对比。
- **判决口径**:堆积组 `fwd_ret_1..5` 期望/胜率显著优于突拉组,且 2026 分段
  同向 → 大师判别器成立(进入用户情报包的"启动性质"标注);反向或无差异 →
  诚实否决。左尾与次日跌停暴露同报(突拉组若左尾更肥,即"一日游/诱多"的
  数字化)。

## 2. 预注册假设 H2:量比/换手带(入池闸门层)

- **来源**:用户漏斗 v1/v2 第②层——"量比 ≥1.5、换手率 5%~10% 才入自选"。
- **事件**:`vol_ratio_5 ≥ 1.5` 且 `turnover_rate ∈ [5%, 10%]`,域内。
  (`vol_ratio_5` = 量比的 EOD 近似:当日量 / 前 5 日均量,与行情软件盘中量比
  同族;面板现成列。)
- **对照组**(细分,不只一个"带外"):①量比达标但换手 <5%;②量比达标但
  换手 >10%;③换手达标但量比 <1.5;④双不达标。
- **敏感性 ±1 格**:量比 {1.2, 1.5, 2.0} × 换手带 {[3,8], [5,10], [7,15]}。
- **稳健性分层(不改判决)**:`close > ma20`(用户漏斗的"趋势向上"叠加态)。
- **判决口径**:带内组 fwd 期望/胜率/左尾优于各对照 → 闸门有贡献;若某对照
  组(如换手 >10%)反而更好 → 如实报告"闸门画错了位置",给用户改带建议。

## 3. 预注册假设 H3:挂低单逆向选择(执行层)

- **来源**:用户下单习惯——挂单价低于现价 0.5%~1%,等回踩成交;假设(我方
  提出待证):能成交的不成比例是转弱的,真启动的挂不到=专接下坠、错过起飞。
- **拟买入日 D**:域内 且 `vol_ratio_5 ≥ 1.5` 且 `turnover_rate ∈ [5,10]` 且
  `close > ma20`(近似用户"筛完想买"状态)。
- **模拟**:挂单价 = `close_D × (1-δ)`,**δ ∈ {0.5%, 1.0%}(用户习惯值)**。
  成交判定:`low_{D+1} ≤ 挂单价` 且 D+1 非停牌、非一字跌停(需新特征
  `fwd_low_1`/`fwd_open_1`,只进研究面板)。
- **两口径对比(严格同持有窗)**:
  - 成交组:成交价 = 挂单价,持有至同一退出日(与面板 fwd 口径对齐的 D+N
    收盘,N 取 2/3 两档)收益,扣成本;
  - 市价对照:同一批 D 全体按面板既有 fwd 口径(市价买入)收益;
  - 未成交子集:单列其"错过收益"(市价口径)。
  - D+1 一字涨停时两口径同剔(市价也买不进),保持可比。
- **判决口径**:成交组期望 < 市价对照 且 未成交子集错过收益显著为正 →
  逆向选择成立,建议用户改单(市价/更小 δ);反之 → 挂低单习惯无害或有利,
  如实报告。δ 两档分别出数。

## 4. 产出与检查点

- 本文件逐节补结果:每假设一节(方法确认 / 结果表 / 分层 / 敏感性 / 左尾 /
  2026 分段 / 判决),末节**「漏斗层判决表」**汇总:每层 贡献正/负/无 +
  2026 分段方向 + 给用户的行动建议草案。
- **用户检查点**:结果呈用户过堂后才定漏斗 v3 / 是否升级组合级(贵档);
  本战役不自动升级。
- 排第二场的候选(预注册在案,本场不跑):当日涨幅带(需日内近似口径,争议
  单独处理)、MACD/KDJ 金叉、题材持续性(紧邻 K2 坟,需新证据论证)。

---

(以下为结果节,施工时逐节填写;预注册截止线 = 本 commit)
