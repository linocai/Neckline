# K4 机械半身组装 + 联合削尾验收 — 规格 + 结果报告(路线图第三步)

> **性质**:非新假设战役——工程组装 + **联合验收**。六层判决的避坑规则各自
> 孤立测过(三役报告),本步唯一的新信息是:①并集后的**联合削尾效果**;
> ②**覆盖率**(剔/标注占域比,防"剔到无票可选");③规则间**重叠度**。
> 验收标准先 commit 再跑数(纪律同前);生产零改动(落库为新增 `is_active=0`
> 行,不碰现役 K1);诚实报告,联合效果若弱于单层预期,如实写。

## 1. K4 机械半身规格(v3 判决集,2026-07-25 用户批准"全部按判决来")

- **A. 硬剔过滤(候选池级,机器执行)**:
  A1 换手率 >10%(H2:次日跌停 3.37%,九倍于低换手);
  A2 题材持续 ≥4 天的行业成员(H6:-1.07%,跌停暴露 1.04%);
  A3 年线下涨停日(K3 臂④+战役三复核:诱多域);
  A4 base 域既有卫生线(ST/次新 120/流动性,沿 `base_universe_expr`)。
- **B. 回避标注(减分,情报包展示给人判,机器不禁止)**:
  B1 量能堆积大涨日(H1:cnt3≥2 & ret≥5% & 放量,次日跌停 3.7×);
  B2 双金叉态(H4:四态垫底);
  B3 题材持续 2-3 天成员(H6:-0.59%);
  B4 追强大红(close>ma20 & 昨日 ret_1d>5%,H5 口径A:-1.05%/-1.43%)。
- **C. 执行规则(情报包提示,人侧执行)**:强票市价/小 δ(H3);当日 2-3%
  温和红盘为低方差首选带(H5);挂低单自知样本偏弱、下调预期(H3);
  回调大红机械层不做(战役三)。
- **D. 熔断(已批,归系统线)**:3 连止损 / 单日 -4000 → 停手复盘解锁。

## 2. 联合验收(预注册标准,先于跑数)

- **口径**:沿三役(k4p_common;T+1 开盘 fwd;净扣 30bp;左尾 p5/p10;
  次日/持有3日跌停暴露;2026 分段单列)。
- **对比三层**:域内全体 → 硬剔后(A)→ 硬剔+回避后(A+B),各报
  期望/胜率/p5/p10/跌停暴露;分层:年份/板块/close>ma20 上行子域。
- **验收线(全部满足才算过)**:
  ① A+B 后左尾 p5 与次日跌停暴露均优于域内全体,**2026 分段同向**;
  ② 覆盖率合理:硬剔占域 <30%,硬剔+标注合计 <70%(留足可选票);
  ③ K1 现役选股集合不受影响(K4 为 advisory,不改任何现役行为)。
- **重叠度矩阵**:各规则两两命中重叠占比(情报包展示排序要用)。

## 3. 落库(验收过线后)

- `brain.save_version("K4", rule={"config": <K1 现役 config 逐位拷贝不改>,
  "k4_advisory": <§1 规格结构化>, "verdict": "mechanical=pure-avoidance;
  alpha=human-intraday-layer(journal-adjudicated)"}, changelog=...,
  metrics=<联合验收数字>, activate=False)`。
- 落库后断言:K1 仍唯一现役(`list_versions()`:K1 active=1 / K2 0 / K4 0)。
- 情报包实现归系统线(交接 memo 需求 3 的字段依据即本报告 §1)。

---

(以下为结果节,施工时逐节填写;预注册截止线 = 本 commit)
